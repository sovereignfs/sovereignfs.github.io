import{_ as t,o,c as a,a2 as i}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"Sovereign Mobile","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-mobile/blob/main/CONCEPT.md"},"headers":[],"relativePath":"apps/mobile/index.md","filePath":"apps/mobile/index.md"}'),n={name:"apps/mobile/index.md"};function s(r,e,l,d,c,h){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="sovereign-mobile" tabindex="-1">Sovereign Mobile <a class="header-anchor" href="#sovereign-mobile" aria-label="Permalink to &quot;Sovereign Mobile&quot;">​</a></h1><h2 id="concept-paper-draft-v0-1" tabindex="-1">Concept Paper (Draft v0.1) <a class="header-anchor" href="#concept-paper-draft-v0-1" aria-label="Permalink to &quot;Concept Paper (Draft v0.1)&quot;">​</a></h2><h3 id="vision" tabindex="-1">Vision <a class="header-anchor" href="#vision" aria-label="Permalink to &quot;Vision&quot;">​</a></h3><p>Sovereign Mobile is the native iOS and Android client for <a href="https://github.com/sovereignfs/sovereign" target="_blank" rel="noreferrer">Sovereign</a>, the self-hostable workspace runtime. It is a <strong>minimal Capacitor shell</strong>, not a second implementation of the platform: on first launch it asks the user for the URL of their own self-hosted Sovereign instance, validates it, persists it, and loads the full Sovereign workspace — auth, plugins, shell layout, everything — in a native WebView. One binary is published to the App Store and the Play Store; it works against any self-hosted instance, not a fixed backend.</p><p>On top of that thin WebView shell sits a <strong>device bridge</strong>: a narrow, versioned channel that lets web content already running inside the WebView (the platform itself, and any plugin it hosts) reach native device capabilities — geolocation, camera, calendar, NFC, haptics, push, and whatever else a plugin legitimately needs — that browser Web APIs don&#39;t cover or don&#39;t cover well enough inside a WebView. Plugin authors never touch Capacitor directly; they call one portable SDK surface (<code>sdk.device.*</code>), and that surface silently routes to the bridge only when running inside this shell, falling back to standard Web APIs everywhere else.</p><h3 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h3><p>Sovereign v1 ships as an installable PWA. That covers the &quot;install from the browser&quot; path, but it does not give self-hosters a listing in the App Store or Play Store, and a PWA cannot reach a meaningful slice of native capability inside a WebView — reliable push notifications, biometric unlock, a native photo/document picker, calendar and contacts integration, NFC, background work. Users who expect &quot;a real app&quot; don&#39;t get one, and plugin authors who need any of the above have no portable way to ask for it.</p><p>Sovereign&#39;s product model is self-hosted and plugin-first. A native mobile app that reimplements auth, routing, or plugin hosting natively would fork the platform into a second, drifting implementation — exactly what the existing desktop shell (<code>sovereign-desktop</code>, Tauri) was built to avoid. The same discipline has to hold for mobile.</p><h3 id="the-solution" tabindex="-1">The solution <a class="header-anchor" href="#the-solution" aria-label="Permalink to &quot;The solution&quot;">​</a></h3><p>Two layers, kept deliberately separate:</p><ol><li><p><strong>The shell.</strong> A Capacitor app that does nothing but onboard an instance URL, persist a list of instances, and load the active one into a WKWebView / Android System WebView. It owns store presence, native permission declarations, and WebView lifecycle — nothing about auth, plugins, or layout. This is a close port of the already-shipped <code>sovereign-desktop</code> Tauri shell; the two follow the same architectural pattern (Nextcloud/Bitwarden/Element-style thin native wrapper) so a contributor who knows one recognizes the other.</p></li><li><p><strong>The bridge.</strong> A capability-negotiated channel, shared with <code>sovereign-desktop</code>, that plugins reach through <code>sdk.device.*</code> alone. The shell advertises which capabilities it implements and at which version; plugins ask, get a typed result (available / denied / dismissed / unavailable), and never branch on Capacitor or on which native shell they happen to be running inside. Browser and PWA users keep working through the plain Web API fallback tier — nothing about a plugin&#39;s code changes because it happens to run inside this shell.</p></li></ol><h3 id="core-principles" tabindex="-1">Core principles <a class="header-anchor" href="#core-principles" aria-label="Permalink to &quot;Core principles&quot;">​</a></h3><p><strong>Not a second platform.</strong> Everything user-facing — auth, roles, sessions, plugin routing, shell layout, CSP — is served by the user&#39;s own instance, unchanged. If a feature could live in the instance instead of the shell, it must.</p><p><strong>One binary, any instance.</strong> No build-per-deployment. The same App Store/Play Store binary works against any self-hosted Sovereign instance the user points it at, mirroring the desktop shell&#39;s model and self-hosting&#39;s whole premise: users own their deployment, the client doesn&#39;t assume a fixed backend.</p><p><strong>Bridge, not a fork.</strong> Device capability access is additive plumbing on top of the existing plugin/SDK contract, not a parallel native plugin system. A plugin that calls <code>sdk.device.getLocation()</code> runs unchanged in a browser, an installed PWA, and this shell — the SDK detects where it&#39;s running and routes accordingly.</p><p><strong>Capability, not trust-by-default.</strong> Nothing native is reachable from web content by default. Each device capability is a named, versioned entry a plugin must declare, request, and (per RFC 0083) be user-consented into before a call resolves — never a blanket &quot;this app can use the camera&quot; toggle inherited by every plugin that happens to be installed.</p><p><strong>No shell-side product logic.</strong> The shell renders one thing: the user&#39;s own instance. Store-facing behavior (privacy labels, permission strings) has to be literally true of what the shell does, not aspirational copy.</p><h3 id="relationship-to-the-wider-ecosystem" tabindex="-1">Relationship to the wider ecosystem <a class="header-anchor" href="#relationship-to-the-wider-ecosystem" aria-label="Permalink to &quot;Relationship to the wider ecosystem&quot;">​</a></h3><p><code>sovereign-mobile</code> is a sibling repository to the <code>sovereign</code> monorepo, not a package inside it — same relationship <code>sovereign-desktop</code> already has. Its own governing design lives centrally:</p><ul><li><a href="https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0058-native-mobile-app-shell.md" target="_blank" rel="noreferrer">sovereign RFC 0058</a> — the shell&#39;s design (this repo&#39;s scope, distribution model, onboarding flow, device API tiering).</li><li><a href="https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0083-device-bridge-capability-contract.md" target="_blank" rel="noreferrer">sovereign RFC 0083</a> — the bridge protocol (<code>@sovereignfs/bridge</code> + <code>@sovereignfs/sdk/device-client</code>), shared with <code>sovereign-desktop</code> so the two shells cannot drift. <code>sovereign-mobile</code> implements the Capacitor <strong>transport</strong> of that contract; it does not own the protocol.</li><li><a href="https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0080-plugin-surface-model.md" target="_blank" rel="noreferrer">sovereign RFC 0080</a> — <code>sdk.device.getSurface()</code> and the manifest <code>surfaces</code> field, the mechanism behind &quot;some plugin features only show up inside a native shell.&quot; Kept deliberately separate from capability/permission checks. Referenced here because this shell is one of the two surfaces (<code>mobile</code>, alongside <code>desktop</code>) that model answers.</li><li><a href="https://github.com/sovereignfs/sovereign/blob/main/docs/epics/mobile.md" target="_blank" rel="noreferrer">sovereign epic 20</a> and <a href="https://github.com/sovereignfs/sovereign/blob/main/docs/workstreams/0002-native-mobile-app-release.md" target="_blank" rel="noreferrer">workstream 0002</a> — the task breakdown and release sequencing that created this repository.</li></ul><p>This repo owns only what&#39;s native-shell-specific: the Capacitor scaffold, iOS/Android project files, onboarding UI, instance persistence, WebView navigation policy, native permission declarations, store release metadata, and the shell-side implementation of the shared bridge transport. Decisions about the bridge <em>protocol</em> itself, the <code>sdk.device.*</code> surface, and platform-side consent UI are made in <code>sovereign</code> and consumed here, not re-decided.</p><p><strong>Distinct from <code>sovereign-edge</code>.</strong> A separate, unrelated repository in this workspace — a standalone, fully offline on-device AI app with its own React Native UI and its own inference/connector stack. It shares no code, no users, and no architecture with this repo beyond both being native mobile apps. Do not conflate the two.</p><h3 id="device-api-strategy" tabindex="-1">Device API strategy <a class="header-anchor" href="#device-api-strategy" aria-label="Permalink to &quot;Device API strategy&quot;">​</a></h3><p>Three tiers, in order of preference (from sovereign RFC 0058, restated here because it&#39;s this repo&#39;s central design constraint):</p><table tabindex="0"><thead><tr><th>Tier</th><th>What</th><th>Examples</th><th>Works outside this shell?</th></tr></thead><tbody><tr><td>Web APIs</td><td>Standard browser APIs, already work in a WebView</td><td><code>navigator.geolocation</code>, <code>getUserMedia</code>, Web Push where supported</td><td>Yes</td></tr><tr><td>Capacitor plugins</td><td>Native code, reached only from inside this shell</td><td>Native photo picker, calendar/contacts, NFC, APNs/FCM push, biometrics, haptics</td><td>No</td></tr><tr><td><code>sdk.device.*</code></td><td>SDK-level abstraction; detects environment, routes</td><td><code>sdk.device.getLocation()</code>, <code>sdk.device.capturePhoto()</code>, future calls</td><td>Yes, with fallback</td></tr></tbody></table><p>Plugin authors write to <code>sdk.device.*</code> only. NFC, calendar, geolocation, and camera are all instances of the same pattern — each is an additive capability registry entry plus a transport implementation on each shell, not a new mechanism. Sovereign RFC 0083 ships a deliberately thin v1 slice (<code>haptics.impact</code>, <code>notifications.native</code>) to prove the contract; broader capabilities (camera, calendar, NFC, geolocation, biometrics) land afterward as the same additive pattern, sequenced by product need rather than shipped speculatively.</p><h3 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h3><p>Sovereign Mobile is not:</p><ul><li>A second implementation of Sovereign&#39;s runtime, auth, or plugin system.</li><li>A fixed-backend app — it has no built-in instance, and never will.</li><li>A vehicle for native-only product features that don&#39;t also work, in some form, in a browser. Anything shell-exclusive belongs in the platform&#39;s <code>surfaces</code> availability model (RFC 0080), decided deliberately, not by what happens to be easy to build natively.</li><li>A general native plugin-loading platform for third parties. The bridge exposes a fixed, versioned capability registry that the platform defines; it is not a way for a plugin to ship arbitrary native code.</li></ul><h3 id="phasing" tabindex="-1">Phasing <a class="header-anchor" href="#phasing" aria-label="Permalink to &quot;Phasing&quot;">​</a></h3><ol><li><strong>Phase 1 — whole-instance shell.</strong> Onboarding, instance persistence and switching, WebView loading, navigation policy, store release on both platforms. No device bridge yet beyond what RFC 0083&#39;s thin v1 slice ships. This is <a href="https://github.com/sovereignfs/sovereign/blob/main/docs/workstreams/0002-native-mobile-app-release.md" target="_blank" rel="noreferrer">workstream 0002</a>&#39;s scope and this repo&#39;s initial reason to exist.</li><li><strong>Phase 2 — bridge transport.</strong> Implement this shell&#39;s side of <code>@sovereignfs/bridge</code>: handshake, capability negotiation, the v1 thin slice (<code>haptics.impact</code>, <code>notifications.native</code>), and the consent UX the platform&#39;s <code>plugins/account</code> surface drives.</li><li><strong>Phase 3 — capability breadth.</strong> Add device capabilities behind the same bridge contract as product need identifies them — geolocation, camera, calendar, NFC, background work among them — each gated by its own permission string, privacy declaration, and (per plugin) explicit user consent grant.</li></ol><p>Nothing in Phase 3 is scoped or committed yet; see <a href="./docs/research/0002-device-capability-candidates.html">docs/research/0002-device-capability-candidates.md</a> for the open list.</p><h3 id="long-term-vision" tabindex="-1">Long-term vision <a class="header-anchor" href="#long-term-vision" aria-label="Permalink to &quot;Long-term vision&quot;">​</a></h3><p>A user should be able to search the App Store or Play Store for &quot;Sovereign,&quot; install one app, point it at their own self-hosted deployment, and get everything their browser gives them today plus what only a native shell can: reliable push, a native camera/photo/calendar picker, biometric unlock, NFC where a plugin needs it — all through the same portable <code>sdk.device.*</code> surface plugin authors already use, so nothing about a plugin&#39;s code has to fork the moment it wants to run well on a phone.</p>',34)])])}const b=t(n,[["render",s]]);export{g as __pageData,b as default};
