import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Progressive user verification","description":"","frontmatter":{"rfc":29,"title":"Progressive user verification","status":"Accepted","date":"June 2026","author":"kasunben","scope":"packages/db, apps/auth, runtime, packages/sdk, packages/manifest, plugins/account, plugins/console, .env.example, docs\\n","incorporated_into_plan":"Yes (phased) — epic tasks 1.8 (Phase 1, infrastructure) + 1.9 (Phase 2, capability opt-in)"},"headers":[],"relativePath":"rfcs/0035-progressive-user-verification.md","filePath":"rfcs/0035-progressive-user-verification.md"}'),n={name:"rfcs/0035-progressive-user-verification.md"};function o(r,e,l,d,c,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="rfc-0035-—-progressive-user-verification" tabindex="-1">RFC 0035 — Progressive User Verification <a class="header-anchor" href="#rfc-0035-—-progressive-user-verification" aria-label="Permalink to &quot;RFC 0035 — Progressive User Verification&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Sovereign tracks an <code>emailVerified</code> flag on every user account but never sets it to <code>true</code> and never enforces it. MFA is fully implemented but purely opt-in with no platform policy. This RFC introduces a <strong>four-level user verification model</strong> — <code>registered → email_verified → mfa_enrolled → admin_vouched</code> — where each level corresponds to a concrete, auditable proof-of-identity event.</p><p>The current level is stored denormalised on the user record and propagated through the existing session header chain as <code>x-sovereign-verification-level</code>. Middleware, route guards, capability definitions, and plugin manifests can all gate on it without any new enforcement mechanism. Existing users and capabilities are unaffected at adoption time; individual gates opt in to a minimum level in a separate follow-on task.</p><hr><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p><strong>The <code>emailVerified</code> field is decorative today.</strong> Every user account in every Sovereign instance has <code>emailVerified = false</code>. There is no email confirmation flow, no enforcement, and no operator control over whether email verification is required. The field has existed since Task 0.3.9 (auth server scaffold) and has never been wired up.</p><p><strong>MFA has no policy surface.</strong> TOTP and passkey enrollment landed in RFC 0012 and work well for users who choose to enrol. But operators running a team instance have no way to mandate MFA. There is no platform concept of &quot;this user has proved their identity strongly&quot; that plugins or capabilities can test without reinventing it per-feature.</p><p><strong>The capability system (RFC 0021) is role-based, not identity-strength-based.</strong> A <code>platform:user</code> with a confirmed email and a registered passkey looks identical to a brand-new unverified account. Plugins that want to gate on identity strength — a plugin handling sensitive documents, a monetisation plugin requiring a real email for receipts — have no SDK surface to call.</p><p><strong>Federation will need a portable trust signal.</strong> Post-v1 federation (SRS §1.4) requires instances to communicate how much they vouch for a given user identity. Getting the data model right now avoids a breaking schema change when that work begins.</p><hr><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><h3 id="dormant-email-verified-field" tabindex="-1">Dormant email-verified field <a class="header-anchor" href="#dormant-email-verified-field" aria-label="Permalink to &quot;Dormant email-verified field&quot;">​</a></h3><p><code>emailVerified: integer(&#39;email_verified&#39;, { mode: &#39;boolean&#39; }).notNull().default(false)</code> exists in <code>packages/db/src/schema/sqlite/platform.ts:31</code> and its Postgres equivalent. better-auth manages a <code>verification</code> table (signed, time-limited tokens) and exposes <code>POST /api/auth/send-verification-email</code> — the plumbing already exists; it has never been called.</p><p><strong>Update:</strong> the <code>emailVerified</code> field on better-auth&#39;s own <code>user</code> table (a separate database from the one <code>platform.ts</code> describes — <code>apps/auth</code> intentionally does not depend on <code>packages/db</code>, see <code>apps/auth/src/db.ts</code>) is no longer dormant. A scoped-down implementation of this RFC&#39;s §5.3 (email verification flow only — no <code>verification_level</code>/MFA/admin-vouch machinery) shipped using better-auth&#39;s native <code>emailAndPassword.requireEmailVerification</code> + <code>emailVerification.sendVerificationEmail</code> support directly, gated by <code>AUTH_REQUIRE_EMAIL_VERIFICATION</code> (default <code>true</code>) — see <code>apps/auth/src/auth.ts</code> and <code>docs/security.md</code>. A future implementer of epic task 1.8 should build the <code>verification_level</code> ladder on top of this existing enforcement rather than duplicating the email-send/verify wiring.</p><h3 id="mfa-state-is-readable-but-not-propagated-to-session-headers" tabindex="-1">MFA state is readable but not propagated to session headers <a class="header-anchor" href="#mfa-state-is-readable-but-not-propagated-to-session-headers" aria-label="Permalink to &quot;MFA state is readable but not propagated to session headers&quot;">​</a></h3><p>TOTP and passkeys are stored in better-auth&#39;s <code>twoFactor</code> and <code>passkey</code> tables. <code>twoFactorEnabled</code> is returned by <code>GET /api/auth/get-session?disableCookieCache=true</code> (the cache-bypass pattern used in <code>runtime/app/(platform)/(plugins)/account/security/page.tsx:28</code>). MFA state is <strong>not</strong> in the signed session cookie cache and <strong>not</strong> in the middleware header chain — apps that need it must fetch it on demand.</p><h3 id="session-header-chain" tabindex="-1">Session header chain <a class="header-anchor" href="#session-header-chain" aria-label="Permalink to &quot;Session header chain&quot;">​</a></h3><p><code>runtime/middleware.ts:290–306</code> (session-verified path) injects:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>x-sovereign-user-id</span></span>
<span class="line"><span>x-sovereign-user-role</span></span>
<span class="line"><span>x-sovereign-user-capabilities   (JSON array)</span></span></code></pre></div><p><code>runtime/src/session-verify.ts</code> reads these from the signed cookie cache (<code>session.cookieCache</code>, 300 s, HMAC key = <code>SOVEREIGN_AUTH_SECRET ?? AUTH_SECRET</code>) or falls back to <code>GET /api/verify</code>. The <code>@sovereignfs/sdk</code> reads them in <code>packages/sdk/src/auth.ts:16–29</code> and populates <code>session.user.role</code> and <code>session.user.capabilities</code>.</p><h3 id="capability-system" tabindex="-1">Capability system <a class="header-anchor" href="#capability-system" aria-label="Permalink to &quot;Capability system&quot;">​</a></h3><p><code>runtime/src/capabilities.ts</code> defines four hardcoded role presets with 11 capabilities. <code>hasCapability(role, cap)</code> is enforced in middleware (route-level, offline) and in <code>runtime/src/route-guard.ts</code> (Node handler level). Plugins call <code>sdk.auth.hasCapability(session, cap)</code>. There is no concept of a minimum identity level in a capability definition.</p><h3 id="packages-mailer" tabindex="-1"><code>packages/mailer</code> <a class="header-anchor" href="#packages-mailer" aria-label="Permalink to &quot;\`packages/mailer\`&quot;">​</a></h3><p>A fully functional SMTP abstraction (<code>packages/mailer</code>) is already wired into <code>apps/auth</code>. Email delivery works today for invites; it is the right transport for verification emails.</p><hr><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_5-1-—-trust-level-model" tabindex="-1">5.1 — Trust level model <a class="header-anchor" href="#_5-1-—-trust-level-model" aria-label="Permalink to &quot;5.1 — Trust level model&quot;">​</a></h3><p>Four discrete levels, each triggered by a concrete, auditable event:</p><table tabindex="0"><thead><tr><th>Level</th><th>Constant</th><th>Trigger</th></tr></thead><tbody><tr><td>0</td><td><code>registered</code></td><td>Account exists (invite accepted, or first-user self-sign-up)</td></tr><tr><td>1</td><td><code>email_verified</code></td><td>User clicked a signed confirmation link</td></tr><tr><td>2</td><td><code>mfa_enrolled</code></td><td>At least one TOTP authenticator or passkey registered and active</td></tr><tr><td>3</td><td><code>admin_vouched</code></td><td>A <code>platform:owner</code> or <code>platform:admin</code> has explicitly vouched the user</td></tr></tbody></table><p>Levels are <strong>cumulative</strong> in meaning but not strictly sequential in enforcement. Level 2 implies the user has proved something stronger than email ownership; operators may waive Level 1 (see §5.3) in which case a user can be promoted to Level 2 without passing through Level 1. The system records an event timestamp for each level reached — levels can be revoked and re-acquired, but the event log is append-only for audit purposes.</p><h3 id="_5-2-—-data-model" tabindex="-1">5.2 — Data model <a class="header-anchor" href="#_5-2-—-data-model" aria-label="Permalink to &quot;5.2 — Data model&quot;">​</a></h3><p>Two new columns on the Sovereign <code>users</code> table, added via Drizzle migration on top of better-auth&#39;s base schema (the same pattern used for <code>role</code>, which Sovereign adds to better-auth&#39;s user record):</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- SQLite</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN verification_level </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INTEGER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN verification_events </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TEXT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Postgres (identical structure; text not jsonb for dialect parity)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN verification_level </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INTEGER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN verification_events </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TEXT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>verification_events</code> stores a JSON object (serialised as <code>text</code> in both dialects, following the <code>sidebar_plugins</code> precedent from Task 2.13):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> VerificationEvents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  email_verified_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Unix timestamp (seconds)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  mfa_enrolled_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  mfa_removed_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// set when all MFA methods removed</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  vouched_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  vouched_by</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// actor user ID</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  vouched_revoked_at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>verification_level</code> is denormalised for fast middleware reads — it is recomputed and updated each time a verification event fires, so the middleware never has to parse <code>verification_events</code> to answer &quot;what is this user&#39;s current level?&quot;</p><p>Schemas: <code>packages/db/src/schema/sqlite/platform.ts</code> and <code>packages/db/src/schema/postgres/platform.ts</code>. Migration: <code>packages/db/migrations/{sqlite,postgres}/0007_user_verification.sql</code>.</p><h3 id="_5-3-—-email-verification-flow" tabindex="-1">5.3 — Email verification flow <a class="header-anchor" href="#_5-3-—-email-verification-flow" aria-label="Permalink to &quot;5.3 — Email verification flow&quot;">​</a></h3><p><strong>On account creation</strong> (first user or invite-accepted new user), the runtime calls <code>POST /api/auth/send-verification-email</code> immediately after better-auth creates the account. This endpoint uses better-auth&#39;s <code>verification</code> table to generate and store a signed, time-limited token and sends it via <code>packages/mailer</code>.</p><p><strong>On link click</strong>, better-auth marks the token consumed and fires an <code>onEmailVerification</code> callback. The callback (wired in <code>apps/auth/src/auth.ts</code>) runs:</p><ol><li>Sets <code>emailVerified = true</code> on the better-auth user record (already the field&#39;s intended purpose).</li><li>Updates <code>verification_level = Math.max(current_level, 1)</code> and appends <code>email_verified_at</code> to <code>verification_events</code>.</li><li>Invalidates the session cookie cache (clears both cookie variants, same pattern as profile mutations) so the new level appears in the next request.</li></ol><p><strong>Operator opt-out:</strong> If <code>REQUIRE_EMAIL_VERIFICATION=false</code> (new env var, added to <code>.env.example</code>), the send step is skipped and the system auto-promotes the user to Level 1 at account creation. This is intended for air-gapped or internal deployments where the invite mechanism itself is sufficient proof. Default is <code>true</code> (verification required).</p><p><strong>Resend:</strong> A <code>POST /api/auth/send-verification-email</code> route already exists in better-auth. The <code>/verify-email</code> interstitial page (see §5.7) exposes a resend button.</p><h3 id="_5-4-—-mfa-enrollment-trigger" tabindex="-1">5.4 — MFA enrollment trigger <a class="header-anchor" href="#_5-4-—-mfa-enrollment-trigger" aria-label="Permalink to &quot;5.4 — MFA enrollment trigger&quot;">​</a></h3><p>When better-auth&#39;s <code>twoFactor</code> plugin fires <code>onTwoFactorEnabled</code> or the passkey plugin fires <code>onPasskeyCreated</code>, an <code>apps/auth/src/auth.ts</code> hook runs:</p><ol><li><code>verification_level = Math.max(current_level, 2)</code>.</li><li>Appends <code>mfa_enrolled_at</code> to <code>verification_events</code> (only on first enrollment; subsequent enrollments of a second device do not re-write the timestamp).</li><li>Invalidates the session cookie cache.</li></ol><p><strong>If all MFA methods are removed</strong> (<code>onTwoFactorDisabled</code> / last passkey deleted), the hook:</p><ol><li>Drops <code>verification_level</code> back to <code>Math.min(current_level, 1)</code> (or 0 if email also unverified).</li><li>Appends <code>mfa_removed_at</code> to <code>verification_events</code> for the audit trail.</li><li>Invalidates the session cookie cache.</li></ol><p>The <code>mfa_enrolled_at</code> timestamp is preserved — the events object records history, not current state.</p><h3 id="_5-5-—-admin-vouch-flow" tabindex="-1">5.5 — Admin-vouch flow <a class="header-anchor" href="#_5-5-—-admin-vouch-flow" aria-label="Permalink to &quot;5.5 — Admin-vouch flow&quot;">​</a></h3><p>Console → Users → (user row action menu) → <strong>Vouch</strong>. The vouch action:</p><ol><li>Is gated to <code>platform:owner</code> and <code>platform:admin</code> only.</li><li>Sets <code>verification_level = Math.max(current_level, 3)</code>, records <code>vouched_by</code> + <code>vouched_at</code>.</li><li>Invalidates the target user&#39;s session cache (requires knowing their session cookie; if not possible server-side, the new level takes effect at next login or cache expiry ≤ 300 s).</li></ol><p><strong>Revoke vouch:</strong> Same UI, <strong>Revoke vouch</strong> option. Sets <code>verification_level = Math.min(current_level, 2)</code>, clears <code>vouched_by</code>, appends <code>vouched_revoked_at</code>. Does not erase <code>vouched_at</code> (audit trail).</p><p>A <code>POST /api/admin/users/[id]/vouch</code> and <code>DELETE /api/admin/users/[id]/vouch</code> route pair handle this.</p><h3 id="_5-6-—-session-propagation" tabindex="-1">5.6 — Session propagation <a class="header-anchor" href="#_5-6-—-session-propagation" aria-label="Permalink to &quot;5.6 — Session propagation&quot;">​</a></h3><p><code>runtime/src/session-verify.ts</code> reads <code>verification_level</code> from the user record (either from an extended signed cookie cache or from the live <code>/api/verify</code> call — implementation decision deferred to the task). <code>runtime/middleware.ts</code> injects:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>x-sovereign-verification-level: 2</span></span></code></pre></div><p>alongside the existing role and capabilities headers. The value is always a plain integer string.</p><p><code>packages/sdk/src/auth.ts</code> reads this header and populates a new field on the session object:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">session.user.verificationLevel: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span></span></code></pre></div><p>This is a minor addition to <code>@sovereignfs/sdk</code> — the field is new, so no existing call site breaks. Semver impact: <strong>minor bump</strong> for <code>@sovereignfs/sdk</code> (new public field on the session type).</p><h3 id="_5-7-—-capability-gate-integration" tabindex="-1">5.7 — Capability gate integration <a class="header-anchor" href="#_5-7-—-capability-gate-integration" aria-label="Permalink to &quot;5.7 — Capability gate integration&quot;">​</a></h3><p><code>runtime/src/capabilities.ts</code> gains an optional field on capability definitions:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CapabilityDefinition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // existing fields …</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  minVerificationLevel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// undefined = no gate (default, backwards-compatible)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>hasCapability(role, cap, userLevel?)</code> gains an optional third argument. If <code>minVerificationLevel</code> is set on the capability and <code>userLevel &lt; minVerificationLevel</code>, the check returns <code>false</code> regardless of role. This is purely additive — no existing capability has <code>minVerificationLevel</code> set at adoption time. Individual capabilities opt in during the follow-on task (Phase 2).</p><h3 id="_5-8-—-manifest-declaration" tabindex="-1">5.8 — Manifest declaration <a class="header-anchor" href="#_5-8-—-manifest-declaration" aria-label="Permalink to &quot;5.8 — Manifest declaration&quot;">​</a></h3><p>Plugin manifests may declare a minimum verification level required to access the plugin:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;min_verification_level&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>packages/manifest</code> validates this field (integer 0–3, optional, default 0). The runtime access check and registry listing surface it as a human-readable label: <code>requires_email_verified</code> (≥ 1), <code>requires_mfa</code> (≥ 2), <code>admin_vouched_only</code> (= 3). Plugins do not need to restate it in their own capability checks — the runtime enforces it at the plugin route boundary, the same place it enforces the <code>capabilities</code> manifest field today (RFC 0022).</p><p>Semver impact: additive field on the manifest — <strong>patch bump</strong> for <code>@sovereignfs/manifest</code>.</p><h3 id="_5-9-—-interstitial-nudge-ui" tabindex="-1">5.9 — Interstitial / nudge UI <a class="header-anchor" href="#_5-9-—-interstitial-nudge-ui" aria-label="Permalink to &quot;5.9 — Interstitial / nudge UI&quot;">​</a></h3><p><strong>Level 0 → Level 1 gate (hard block when <code>REQUIRE_EMAIL_VERIFICATION=true</code>):</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[user logs in]</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>middleware: verification_level === 0 AND REQUIRE_EMAIL_VERIFICATION=true?</span></span>
<span class="line"><span>      │ yes</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>redirect 303 → /verify-email  (new runtime route, public — not behind plugin auth)</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>/verify-email page: &quot;Check your inbox — we sent a link to {email}.&quot;</span></span>
<span class="line"><span>      │  [Resend] button → POST /api/auth/send-verification-email</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>user clicks link → level promoted to 1 → redirect to intended destination</span></span></code></pre></div><p>When <code>REQUIRE_EMAIL_VERIFICATION=false</code>, no redirect — the user starts at Level 1 and the page is never shown.</p><p><strong>Level 1 → Level 2 nudge (soft, not a hard block unless <code>REQUIRE_MFA=true</code>):</strong></p><p>Routes or plugins that declare <code>minVerificationLevel: 2</code> show an inline banner: &quot;Strengthen your account — enable MFA in Account → Security to access this feature.&quot; The banner links to the Account Security page. If <code>REQUIRE_MFA=true</code> (new operator env var), the banner becomes a hard block with the same redirect-based pattern as the Level 1 gate.</p><p><strong>Level 2 → Level 3 (admin-vouch only):</strong> Features requiring Level 3 show a message: &quot;Access to this feature requires admin approval. Contact your workspace owner.&quot; No self-service path exists by design.</p><hr><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><h3 id="email-verification-new-user" tabindex="-1">Email verification (new user) <a class="header-anchor" href="#email-verification-new-user" aria-label="Permalink to &quot;Email verification (new user)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>signup / invite-accept</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ├─ REQUIRE_EMAIL_VERIFICATION=true</span></span>
<span class="line"><span>    │       │</span></span>
<span class="line"><span>    │       ▼</span></span>
<span class="line"><span>    │   send verification email</span></span>
<span class="line"><span>    │       │</span></span>
<span class="line"><span>    │       ▼</span></span>
<span class="line"><span>    │   redirect to /verify-email (&quot;check your inbox&quot;)</span></span>
<span class="line"><span>    │       │  [Resend]</span></span>
<span class="line"><span>    │       ▼</span></span>
<span class="line"><span>    │   user clicks link in email</span></span>
<span class="line"><span>    │       │</span></span>
<span class="line"><span>    │       ▼</span></span>
<span class="line"><span>    │   level 0 → 1, redirect to /</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    └─ REQUIRE_EMAIL_VERIFICATION=false</span></span>
<span class="line"><span>            │</span></span>
<span class="line"><span>            ▼</span></span>
<span class="line"><span>        auto-promote 0 → 1</span></span>
<span class="line"><span>        proceed to /</span></span></code></pre></div><h3 id="admin-vouch-console" tabindex="-1">Admin-vouch (Console) <a class="header-anchor" href="#admin-vouch-console" aria-label="Permalink to &quot;Admin-vouch (Console)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Console → Users → (row) → ⋯ → Vouch</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>confirmation dialog: &quot;Vouch for {name}? This marks their identity as admin-confirmed.&quot;</span></span>
<span class="line"><span>    │  [Confirm]</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>POST /api/admin/users/{id}/vouch</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>level → 3, vouched_by = actorId, vouched_at = now</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>activity log: &quot;user.vouched&quot; (actor = admin, subject = user)</span></span></code></pre></div><hr><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><p><strong>Single <code>emailVerified</code> boolean, no levels.</strong> Simpler to implement and reason about, but leaves no room for MFA-strength gating or admin-vouch without adding more booleans later. A level integer is only marginally more complex and avoids the schema churn.</p><p><strong>Compute level on-the-fly from existing fields.</strong> <code>emailVerified</code>, <code>twoFactorEnabled</code> (better-auth field), and a new <code>admin_vouched</code> boolean could be combined at read time without a denormalised <code>verification_level</code> column. Rejected because middleware runs at the edge and must not issue a DB query per request. A denormalised integer is a single column read from the already-fetched user row.</p><p><strong>Sticky levels (once Level 2, always Level 2).</strong> Simpler mental model, but breaks the invariant that Level 2 means &quot;currently has active MFA.&quot; If a user removes all MFA methods, Level 2 is meaningless as an identity-strength signal. The drop-on-removal design is more conservative and more honest.</p><p><strong>Plugin-callable <code>sdk.verification.setLevel()</code>.</strong> Allowing plugins (e.g., a KYC plugin) to advance a user to Level 3 would make the system extensible. Rejected for v1 because it creates an unvetted trust surface: a plugin could advance any user&#39;s level without an admin in the loop. Admin Console vouch keeps a human accountable for Level 3 grants. Post-v1, a narrow SDK method <code>sdk.verification.recordVerification(event)</code> for certified plugins is a reasonable extension (see Open questions §3).</p><p><strong>New <code>user_verifications</code> table instead of JSON column.</strong> A normalised table would make per-event queries easier but adds a join to every user fetch. The <code>verification_events</code> JSON column follows the precedent set by <code>account_prefs.sidebar_plugins</code> and is sufficient for v1 audit purposes. If structured querying of events becomes necessary, a migration can extract the column later.</p><hr><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><p><strong><code>REQUIRE_EMAIL_VERIFICATION=false</code> UX:</strong> When email verification is waived (air-gapped deploy), should the Account → Security section still show &quot;Email not verified&quot; as an informational notice, or should the UI suppress it entirely to avoid confusing users in a context where verification doesn&#39;t apply?</p></li><li><p><strong>MFA-level stickiness:</strong> The current proposal drops <code>verification_level</code> from 2 to 1 when all MFA methods are removed. An alternative is to keep it at 2 but mark <code>mfa_active: false</code> as a separate flag. The latter allows &quot;was once Level 2&quot; to remain visible in the Console even after MFA removal. Is the distinction worth the added complexity?</p></li><li><p><strong>Plugin-callable level advancement (Level 3):</strong> Should a certified plugin (e.g., a KYC / identity verification plugin available in the registry) be able to call an SDK method to advance a user to Level 3 programmatically, bypassing the manual Console vouch? If yes, what certification or manifest gate prevents an untrusted plugin from abusing this?</p></li><li><p><strong>Migration of existing users:</strong> At the time this ships, all existing users are Level 0 with <code>emailVerified = false</code>. Two strategies:</p><ul><li><strong>Auto-promote:</strong> If <code>emailVerified = true</code> (it currently never is), set Level 1. All others stay at 0 and must verify. Clean but results in every existing user being at Level 0.</li><li><strong>Grandfather:</strong> Set all existing users to Level 1 on migration day; only new users go through the email flow. Avoids disruption but bypasses the intent of the gate for existing accounts. Recommendation: auto-promote (strategy 1) — the field has never been set to <code>true</code>, so this is equivalent to starting fresh. Flag this in upgrade notes.</li></ul></li><li><p><strong>Session cache extension:</strong> The signed session cookie cache currently carries <code>role</code> and <code>capabilities</code>. Adding <code>verification_level</code> to the cache payload extends the 300 s staleness window to verification changes (level promotions or revocations take up to 300 s to propagate to live sessions). Is this acceptable, or should verification level always be fetched live (at the cost of an auth server call on every request)?</p></li></ol><hr><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p><strong>Phase 1 — Infrastructure (this RFC, one task):</strong> Data model migration, email verification flow, MFA hooks, admin-vouch API + Console UI, session propagation, SDK field, <code>/verify-email</code> interstitial, <code>REQUIRE_EMAIL_VERIFICATION</code> env var. No existing capability definition changes — zero functional impact on current users or plugins. Packages affected: <code>@sovereignfs/db</code> (patch), <code>@sovereignfs/sdk</code> (minor — new session field), <code>@sovereignfs/manifest</code> (patch — new optional field), <code>runtime</code> (minor), <code>plugins/account</code> (minor — security page reflects level), <code>plugins/console</code> (minor — vouch action in Users table).</p><p><strong>Phase 2 — Capability opt-in (separate task):</strong> Individual capability definitions gain <code>minVerificationLevel</code> where appropriate. Plugin manifest enforcement gate is activated. Operators who have enabled <code>REQUIRE_EMAIL_VERIFICATION</code> see functional gates for the first time.</p><p><strong>Phase 3 — Federation (post-v1):</strong><code>verification_level</code> is included in the cross-instance user identity payload. Receiving instances can choose to accept or re-verify based on the sending instance&#39;s trust policy.</p><hr><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>June 2026</td><td>Initial draft</td></tr></tbody></table>`,102)])])}const g=a(n,[["render",o]]);export{u as __pageData,g as default};
