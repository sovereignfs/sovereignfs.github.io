import{_ as t,o as i,c as s,a2 as a}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Launcher","description":"","frontmatter":{},"headers":[],"relativePath":"plugins/launcher.md","filePath":"plugins/launcher.md"}'),n={name:"plugins/launcher.md"};function o(r,e,d,l,c,h){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="launcher" tabindex="-1">Launcher <a class="header-anchor" href="#launcher" aria-label="Permalink to &quot;Launcher&quot;">​</a></h1><p><strong>Version:</strong> 0.1<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Purpose:</strong> Canonical specification for the Sovereign Launcher plugin — the single source of truth for its manifest, access model, functional requirements, and build plan.<br><strong>Status:</strong> v0.1 implemented (Task 0.4.5)</p><hr><p>Launcher is the default home screen for a Sovereign instance. It lists all installed and enabled plugins the current user has access to, giving every user a single entry point regardless of how many plugins are installed.</p><p>The plugin ships in the monorepo (<code>type: platform</code>) and serves the platform root <code>/</code> by default. An admin can promote any other plugin to serve <code>/</code> instead (see PLT-12/PLT-13 and CON-11), in which case the Launcher remains accessible at <code>/launcher</code>.</p><p><strong>Design principle:</strong> the Launcher should be the simplest possible entry point in v0.1 — a clean grid of icons. Feature richness (quick-actions, recent activity, sub-project shortcuts) is explicitly deferred to post-v1.</p><h2 id="contents" tabindex="-1">Contents <a class="header-anchor" href="#contents" aria-label="Permalink to &quot;Contents&quot;">​</a></h2><ul><li><a href="#identity-and-manifest">Identity and manifest</a></li><li><a href="#access-control">Access control</a></li><li><a href="#functional-requirements">Functional requirements</a></li><li><a href="#sidebar-behaviour">Sidebar behaviour</a></li><li><a href="#directory-structure">Directory structure</a></li><li><a href="#data-model">Data model</a></li><li><a href="#sdk-dependencies">SDK dependencies</a></li><li><a href="#ui">UI</a></li><li><a href="#build-plan">Build plan</a></li><li><a href="#open-questions">Open questions</a></li><li><a href="#changelog">Changelog</a></li></ul><hr><h2 id="identity-and-manifest" tabindex="-1">Identity and manifest <a class="header-anchor" href="#identity-and-manifest" aria-label="Permalink to &quot;Identity and manifest&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>fs.sovereign.launcher</code></td></tr><tr><td><code>name</code></td><td><code>Launcher</code></td></tr><tr><td><code>type</code></td><td><code>platform</code></td></tr><tr><td><code>runtime</code></td><td><code>native</code></td></tr><tr><td><code>routePrefix</code></td><td><code>/launcher</code></td></tr><tr><td><code>shell</code></td><td><code>default</code></td></tr><tr><td><code>adminOnly</code></td><td>omitted (<code>false</code>)</td></tr><tr><td><code>icon</code></td><td><code>icon.svg</code></td></tr><tr><td><code>permissions</code></td><td><code>auth:session</code>, <code>db:readOnly</code></td></tr><tr><td><code>compatibility.minPlatformVersion</code></td><td><code>0.4.0</code></td></tr></tbody></table><p>Proposed <code>manifest.json</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;fs.sovereign.launcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Launcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Home screen — lists all installed plugins for easy access.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;platform&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;runtime&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;native&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;routePrefix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/launcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;shell&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;icon&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;icon.svg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auth:session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;db:readOnly&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;compatibility&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;minPlatformVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.4.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>No <code>repository</code> field — platform plugins live in the monorepo. The Launcher declares <code>db:readOnly</code> (not <code>db:readWrite</code>) — it only reads the plugin registry and writes nothing in v1.</p><hr><h2 id="access-control" tabindex="-1">Access control <a class="header-anchor" href="#access-control" aria-label="Permalink to &quot;Access control&quot;">​</a></h2><p>Launcher is available to all authenticated users. There is no admin-only gate.</p><p>Within the launcher:</p><ul><li>Each plugin tile is shown only if the current user has access to that plugin (i.e. the plugin is installed, enabled, and either <code>adminOnly: false</code> or the user is <code>platform:admin</code>).</li><li>Platform chrome plugins (<code>fs.sovereign.launcher</code>, <code>fs.sovereign.account</code>, <code>fs.sovereign.console</code>) are excluded from the tile grid — they are always accessible via the sidebar chrome, not through the Launcher grid.</li><li><code>adminOnly: true</code> plugins appear in a separate &quot;Admin&quot; section, visible only to <code>platform:admin</code> users.</li></ul><hr><h2 id="functional-requirements" tabindex="-1">Functional requirements <a class="header-anchor" href="#functional-requirements" aria-label="Permalink to &quot;Functional requirements&quot;">​</a></h2><p>Requirements are versioned to their milestone. IDs are stable — never renumber or reuse an LCH-* id.</p><h3 id="v0-1-—-core" tabindex="-1">v0.1 — Core <a class="header-anchor" href="#v0-1-—-core" aria-label="Permalink to &quot;v0.1 — Core&quot;">​</a></h3><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>LCH-01</td><td>Display all installed, enabled, accessible plugins as tiles. Each tile shows the plugin&#39;s icon (from the <code>icon</code> manifest field, or a generated monogram if absent), name, and description.</td></tr><tr><td>LCH-02</td><td>Clicking a tile navigates to the plugin&#39;s <code>routePrefix</code>.</td></tr><tr><td>LCH-03</td><td>Plugins with <code>adminOnly: true</code> are shown in a separate &quot;Admin&quot; section below the main grid. This section is hidden for <code>platform:user</code> role users.</td></tr><tr><td>LCH-04</td><td>Platform chrome plugins (<code>fs.sovereign.launcher</code>, <code>fs.sovereign.account</code>, <code>fs.sovereign.console</code>) are excluded from all grid sections.</td></tr><tr><td>LCH-05</td><td>If no non-chrome plugins are installed, show an empty-state message with a pointer to the Console plugin to install plugins.</td></tr><tr><td>LCH-09</td><td>A plugin with manifest <code>development: true</code> shows an amber &quot;In development&quot; badge in its tile&#39;s bottom badge row, alongside the type badge — purely informational for badging purposes (no effect on which tiles render), but see LCH-10 for a real ordering effect. Mirrors CON-15&#39;s Console Plugins page badge, sourced from the same manifest field.</td></tr><tr><td>LCH-10</td><td><code>development: true</code> plugins sort after non-development ones in both the Launcher grid&#39;s default order and the sidebar&#39;s middle icon section&#39;s default order (<code>selectLauncherPlugins</code>/<code>selectSidebarPlugins</code>, <code>runtime/src/launcher-plugins.ts</code>), relative order otherwise preserved. This is the <em>default</em> order only — a user&#39;s own saved sidebar reordering (Account → Preferences → Sidebar, Task 2.13) still fully overrides it for any plugin id already in their saved order; a newly installed plugin is appended per this rule.</td></tr><tr><td>LCH-11</td><td><code>SOVEREIGN_HIDE_DEVELOPMENT_PLUGINS</code> (env) removes <code>development: true</code> plugins from the Launcher grid and the sidebar&#39;s middle icon section entirely — no tile, no icon, routes 404 — rather than just sorting/badging them (LCH-09/LCH-10). No per-plugin exception: an id in this state is folded into <code>getDisabledPluginIds</code> (<code>runtime/src/plugin-status.ts</code>) unconditionally, same mechanism the middleware route gate reads, so a direct URL hit 404s too.</td></tr></tbody></table><h3 id="v0-2-—-richer-tiles-post-v1" tabindex="-1">v0.2 — Richer tiles (post-v1) <a class="header-anchor" href="#v0-2-—-richer-tiles-post-v1" aria-label="Permalink to &quot;v0.2 — Richer tiles (post-v1)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>LCH-06</td><td>Multi-project plugins (e.g. Plainwrite) show a compact list of the user&#39;s recent sub-projects inline in the tile, plus a &quot;New project&quot; shortcut.</td></tr><tr><td>LCH-07</td><td>Search and filter the plugin grid by name or description.</td></tr><tr><td>LCH-08</td><td>Tiles display a badge for unread notification counts (once the notification SDK surface is available).</td></tr></tbody></table><hr><h2 id="sidebar-behaviour" tabindex="-1">Sidebar behaviour <a class="header-anchor" href="#sidebar-behaviour" aria-label="Permalink to &quot;Sidebar behaviour&quot;">​</a></h2><p>The Launcher plugin drives the <strong>first icon</strong> in the sidebar middle section. This icon is special:</p><ul><li>It always appears first, regardless of install order.</li><li>It points to <code>/</code> (the platform root), not to <code>/launcher</code> directly. The platform serves the configured root plugin in place at <code>/</code> — the runtime rewrites <code>/</code> to that plugin&#39;s <code>routePrefix</code> (default: <code>/launcher</code>; admin can change this via CON-11), so the URL stays <code>/</code> while the plugin renders.</li><li>It cannot be hidden or reordered by users (v1).</li><li>Its icon is the configured root plugin&#39;s <code>icon.svg</code> (not necessarily the Launcher&#39;s own icon, if the admin has promoted a different plugin to root).</li><li>When the Launcher is <strong>not</strong> the configured root plugin, it appears in the middle section as a regular icon linking to <code>/launcher</code> — the only chrome plugin that can appear there. This guarantees the Launcher always remains reachable from the sidebar (PLT-12).</li></ul><hr><h2 id="directory-structure" tabindex="-1">Directory structure <a class="header-anchor" href="#directory-structure" aria-label="Permalink to &quot;Directory structure&quot;">​</a></h2><p>Launcher lives in the monorepo under <code>plugins/launcher/</code>. As built (v0.1):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/launcher/</span></span>
<span class="line"><span>├── manifest.json</span></span>
<span class="line"><span>├── icon.svg                     # Launcher icon — four-square grid symbol</span></span>
<span class="line"><span>├── package.json</span></span>
<span class="line"><span>└── app/</span></span>
<span class="line"><span>    ├── page.tsx                 # Plugin grid: main + admin sections, empty state</span></span>
<span class="line"><span>    ├── launcher.module.css      # Grid, tile, admin-section, empty-state styles</span></span>
<span class="line"><span>    └── _components/</span></span>
<span class="line"><span>        ├── PluginGrid.tsx       # Responsive grid of tiles</span></span>
<span class="line"><span>        ├── PluginTile.tsx       # Single tile: monogram, name, description</span></span>
<span class="line"><span>        └── monogram.ts          # Two-letter monogram fallback (pure; unit-tested)</span></span></code></pre></div><p>Components live under <code>app/_components/</code> (a private App Router folder, not a sibling <code>components/</code> as originally sketched) because the generate script composes only each plugin&#39;s <code>app/</code> tree into the runtime — anything outside <code>app/</code> is not copied and would fail to resolve at runtime.</p><p>No <code>db/</code>, <code>migrations/</code>, or <code>lib/</code> directories — Launcher has no private tables and no complex business logic.</p><hr><h2 id="data-model" tabindex="-1">Data model <a class="header-anchor" href="#data-model" aria-label="Permalink to &quot;Data model&quot;">​</a></h2><p>Launcher has no plugin-specific database tables. It reads the platform plugin registry (maintained by the runtime) — exposing the installed plugin list, their manifest fields (<code>name</code>, <code>description</code>, <code>routePrefix</code>, <code>adminOnly</code>), and their enabled/disabled status.</p><p><strong>As built (v0.1):</strong> because the SDK boundary rule forbids a plugin importing the runtime registry or internal packages, and <code>sdk.db</code> does not land until Task 0.5.05, the Launcher reads this list from a <strong>session-gated</strong> runtime route, <code>GET /api/plugins</code>, forwarding the caller&#39;s session cookie. The route applies all access filtering server-side via <code>selectLauncherPlugins()</code> (excludes chrome plugins and disabled plugins; hides <code>adminOnly</code> plugins from non-admins) and returns each plugin&#39;s <code>adminOnly</code> flag so the page can section the tiles. This fetch is replaced by <code>sdk.db</code> when 0.5.05 lands.</p><p><strong>Sidebar order, and why hiding a sidebar icon doesn&#39;t hide the Launcher tile (Task 2.22):</strong> <code>GET /api/plugins</code> also applies the user&#39;s saved sidebar order (Account → Preferences → Sidebar, Task 2.13) via <code>applySidebarOrder()</code>, so tile order in the grid matches icon order in the sidebar strip. But it calls that helper with <code>dropHidden: false</code> — unlike the sidebar chrome (<code>(platform)/layout.tsx</code>), which calls it with <code>dropHidden: true</code>. The Launcher is deliberately the &quot;see everything&quot; view: a plugin a user hid from their sidebar strip still gets a tile here, only reordered. Hiding from the sidebar and hiding from the Launcher are two different, independently- resolved concerns sharing one saved-order data structure (<code>account_prefs.sidebar_plugins</code>) — see <code>runtime/src/launcher-plugins.ts</code>&#39;s <code>applySidebarOrder</code> doc comment for the full mechanism.</p><hr><h2 id="sdk-dependencies" tabindex="-1">SDK dependencies <a class="header-anchor" href="#sdk-dependencies" aria-label="Permalink to &quot;SDK dependencies&quot;">​</a></h2><table tabindex="0"><thead><tr><th>SDK surface</th><th>Used for</th><th>Available from</th></tr></thead><tbody><tr><td><code>sdk.auth</code></td><td>Current user session; role check for the admin section</td><td>Task 0.4.2</td></tr><tr><td><code>sdk.db</code></td><td>Read plugin registry (installed plugins + their manifest metadata)</td><td>Task 0.5.5</td></tr></tbody></table><p>In v0.1, the registry read is served by the runtime route <code>GET /api/plugins</code> (see <a href="#data-model">Data model</a>) until <code>sdk.db</code> is wired. <code>sdk.auth.getSession()</code> supplies the role used to decide whether the Admin section renders.</p><hr><h2 id="ui" tabindex="-1">UI <a class="header-anchor" href="#ui" aria-label="Permalink to &quot;UI&quot;">​</a></h2><p>Launcher consumes <code>@sovereignfs/ui</code> exclusively.</p><p><strong>Layout:</strong> Responsive grid of plugin tiles. Tiles are square cards with the plugin icon centred above the name and a one-line description below. On mobile the grid collapses to a narrower column count.</p><p><strong>Empty state:</strong> When no non-chrome plugins are installed, a full-bleed empty state with a &quot;No plugins installed&quot; message and a link to <code>/console</code> (admin only) or a &quot;Contact your admin&quot; note (regular users).</p><p><strong>Net-new primitives likely needed in <code>packages/ui</code>:</strong></p><ul><li><strong>Plugin tile card</strong> — square card with icon area, name, and optional description. Reusable wherever a plugin is represented as a navigable item. Should be tokenised for hover/active states.</li><li><strong>Monogram avatar</strong> — generates a one- or two-letter initial circle from a string. Used as fallback when <code>icon</code> is absent. Also reusable in Account plugin (user initials fallback).</li></ul><hr><h2 id="build-plan" tabindex="-1">Build plan <a class="header-anchor" href="#build-plan" aria-label="Permalink to &quot;Build plan&quot;">​</a></h2><p>Two milestones.</p><h3 id="v0-1-—-core-lch-01–05" tabindex="-1">v0.1 — Core (LCH-01–05) <a class="header-anchor" href="#v0-1-—-core-lch-01–05" aria-label="Permalink to &quot;v0.1 — Core (LCH-01–05)&quot;">​</a></h3><p>Plugin grid page, tile component, admin section logic, empty state. Reads registry via SDK stub (direct table access in v0.1; migrated to <code>sdk.db</code> once 0.5.05 lands).</p><p><strong>Done when:</strong> An authenticated user can navigate to <code>/launcher</code> and see all installed, accessible plugins as tiles; clicking a tile navigates to the plugin. Admin users see a separate &quot;Admin&quot; section for <code>adminOnly</code> plugins.</p><h3 id="v0-2-—-richer-tiles-lch-06–08" tabindex="-1">v0.2 — Richer tiles (LCH-06–08) <a class="header-anchor" href="#v0-2-—-richer-tiles-lch-06–08" aria-label="Permalink to &quot;v0.2 — Richer tiles (LCH-06–08)&quot;">​</a></h3><p>Multi-project tile expansion, search, notification badges.</p><hr><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><p><strong>Root plugin icon in sidebar.</strong> When admin promotes a different plugin as root (e.g. Tasks), the first sidebar icon should show that plugin&#39;s icon, not the Launcher&#39;s. The sidebar shell must read the current <code>root_plugin_id</code> from platform settings and resolve its icon at render time. Confirm this is handled in the shell layer, not in the Launcher plugin itself.</p></li><li><p><strong>Plugin registry SDK surface.</strong> The Launcher reads installed plugins via <code>sdk.db</code>. The exact shape of the plugin registry table(s) in <code>packages/db</code> is determined by Task 0.4.3 (Plugin registry). The Launcher&#39;s <code>page.tsx</code> should be written against the registry table shape once that task is complete.</p></li><li><p><strong>Icon resolution.</strong> The <code>icon</code> field in a plugin&#39;s manifest is a path relative to the plugin root. The runtime must resolve this to a servable URL for use in <code>&lt;img&gt;</code> tags. How this is served (static file serving, a dedicated API route, inline SVG) is a runtime concern — confirm the mechanism in Task 0.3.10.</p></li></ol><hr><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft — platform home screen spec.</td></tr><tr><td>0.1</td><td>Jun 2026</td><td>v0.1 implemented (Task 0.4.5). Deviations from this spec: components live under <code>app/_components/</code> (not a sibling <code>components/</code>) since composition copies only <code>app/</code>; registry is read via the gated <code>/api/plugins</code> route (forwarding the session cookie) rather than <code>sdk.db</code>, which lands in 0.5.05; tiles use monograms pending an icon-serving pipeline (Q3).</td></tr></tbody></table>`,66)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
