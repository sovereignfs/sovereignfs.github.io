import{_ as s,o as t,c as o,a2 as i}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Troubleshooting","description":"","frontmatter":{"docSection":"operators","docType":"guide","audiences":["operator","contributor"]},"headers":[],"relativePath":"troubleshooting.md","filePath":"troubleshooting.md"}'),n={name:"troubleshooting.md"};function a(r,e,d,l,h,c){return t(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h1><p>Known issues, workarounds, and debugging notes for Sovereign development and self-hosting.</p><hr><h2 id="development-server" tabindex="-1">Development server <a class="header-anchor" href="#development-server" aria-label="Permalink to &quot;Development server&quot;">​</a></h2><h3 id="better-sqlite3-node-was-compiled-against-a-different-node-js-version" tabindex="-1"><code>better-sqlite3.node</code> was compiled against a different Node.js version <a class="header-anchor" href="#better-sqlite3-node-was-compiled-against-a-different-node-js-version" aria-label="Permalink to &quot;\`better-sqlite3.node\` was compiled against a different Node.js version&quot;">​</a></h3><p><strong>Symptom:</strong> <code>pnpm dev</code> starts compiling auth/runtime instrumentation, then fails with an error like:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>The module &#39;.../node_modules/better-sqlite3/build/Release/better_sqlite3.node&#39;</span></span>
<span class="line"><span>was compiled against a different Node.js version using NODE_MODULE_VERSION ...</span></span>
<span class="line"><span>This version of Node.js requires NODE_MODULE_VERSION ...</span></span>
<span class="line"><span>Please try re-compiling or re-installing the module</span></span></code></pre></div><p><strong>Cause:</strong> <code>better-sqlite3</code> is a native Node module. Its compiled binary is tied to the Node ABI version that was active when <code>node_modules</code> was installed. This happens when the checkout&#39;s dependencies were installed or rebuilt with one Node version and <code>pnpm dev</code> later runs with another. Sovereign pins local development to Node 24, matching the Docker <code>node:24-alpine</code> images, so Node 26-built bindings will fail under a Node 24 dev process.</p><p><strong>Diagnose:</strong> In the same terminal where <code>pnpm dev</code> fails, check the active Node version and ABI:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;process.version + &#39; ABI &#39; + process.versions.modules&quot;</span></span></code></pre></div><p>If the error says the installed binary was compiled for a different <code>NODE_MODULE_VERSION</code>, first make sure the repository&#39;s pinned Node is active:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> use</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;process.version + &#39; ABI &#39; + process.versions.modules&quot;</span></span></code></pre></div><p>The expected local major version is Node 24. If your shell reports a different major, fix that before rebuilding dependencies.</p><p><strong>Fix:</strong> First try rebuilding the native dependency:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rebuild</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> better-sqlite3</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><p>If the mismatch persists, remove the stale install and reinstall:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -rf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node_modules</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><p>Make sure the same Node version is active for both <code>pnpm install</code> and <code>pnpm dev</code>. The repo includes <code>.nvmrc</code>, <code>.node-version</code>, <code>engines.node</code>, and pnpm <code>engine-strict</code>/<code>use-node-version</code> settings so unsupported Node majors fail early instead of silently compiling incompatible native bindings. Docker production images build their own dependencies inside the container, so this is a local/native development issue.</p><hr><h3 id="_404-on-first-navigation-to-an-overlay-plugin-after-server-restart" tabindex="-1">404 on first navigation to an overlay plugin after server restart <a class="header-anchor" href="#_404-on-first-navigation-to-an-overlay-plugin-after-server-restart" aria-label="Permalink to &quot;404 on first navigation to an overlay plugin after server restart&quot;">​</a></h3><p><strong>Symptom:</strong> Clicking the Console or Account icon in the sidebar shows &quot;404 — This page could not be found.&quot; The server log shows <code>200</code> for the same request (or the route compiles immediately before the 200 line).</p><p><strong>Cause:</strong> Overlay plugins (<code>shell: &quot;overlay&quot;</code>) compose into two places:</p><ol><li>A full-page fallback under <code>(plugins)/&lt;routePrefix&gt;/</code></li><li>An interception copy under <code>(plugins)/@modal/(.)&lt;routePrefix&gt;/</code></li></ol><p>Next.js compiles App Router routes <strong>lazily</strong> — each route only compiles on its first request. On a cold dev-server start, the <code>/(.)console</code> (or <code>/(.)account</code>) interception route has never been compiled. A soft-navigation that arrives during that compilation window gets a broken React tree: the <code>@modal</code> slot can&#39;t resolve the intercepted segment and renders nothing, while the children stay on the previous page. The result looks like a 404 even though the HTTP response is 200.</p><p><strong>Workaround:</strong> Do a hard reload (<strong>Cmd+R</strong> on macOS, <strong>Ctrl+R</strong> on Windows/Linux) after the server finishes compiling. The route will be ready by then and the hard reload fetches a fresh server render.</p><p><strong>Why it doesn&#39;t recur:</strong> Once compiled, the interception route stays in Next.js&#39;s in-memory route cache for the lifetime of the dev-server process. Subsequent soft-navigations work immediately.</p><p><strong>Not a bug in the compose output:</strong> You can verify the routes are correctly placed with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runtime/app/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">platform</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">plugins</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/@modal/</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Should show: (.)account  (.)console  default.tsx  layout.tsx</span></span></code></pre></div><p>If the interception directories are missing, re-run <code>pnpm generate</code> to recompose plugin routes.</p><hr><h3 id="intermittent-404-on-plugin-routes-during-development-spurious-recompiles" tabindex="-1">Intermittent 404 on plugin routes during development (spurious recompiles) <a class="header-anchor" href="#intermittent-404-on-plugin-routes-during-development-spurious-recompiles" aria-label="Permalink to &quot;Intermittent 404 on plugin routes during development (spurious recompiles)&quot;">​</a></h3><p><strong>Symptom:</strong> A plugin page that was working suddenly 404s on soft-nav without any code change. A hard reload fixes it.</p><p><strong>Cause (historical):</strong> Before the <code>syncDir()</code> fix in <code>scripts/generate-registry.ts</code>, the generate watcher re-copied every plugin route file on every manifest change — updating mtimes even when file content was unchanged. Next.js&#39;s dev route watcher treated those mtime changes as route invalidations, evicting compiled route entries from its cache. A soft-nav during the recompile window got the same broken-React-tree 404 as above.</p><p><strong>Current behaviour:</strong> <code>syncDir()</code> performs a content-aware incremental copy: it only writes a file if its size or mtime differs from the destination. Unchanged files are not touched, so their mtimes stay stable and Next.js does not invalidate them.</p><p>If you see this in a fresh checkout, check that <code>syncDir</code> is in use in <code>composePlugins()</code> inside <code>scripts/generate-registry.ts</code>.</p><hr><h3 id="console-or-account-404-persists-after-hard-reload" tabindex="-1"><code>/(.)console</code> or <code>/(.)account</code> 404 persists after hard reload <a class="header-anchor" href="#console-or-account-404-persists-after-hard-reload" aria-label="Permalink to &quot;\`/(.)console\` or \`/(.)account\` 404 persists after hard reload&quot;">​</a></h3><p>If Cmd+R doesn&#39;t fix it, the interception copy is likely missing from disk. This can happen if:</p><ul><li><code>pnpm generate</code> was interrupted mid-run.</li><li>The <code>(plugins)/.gitignore</code> keep-list was accidentally edited to exclude <code>(.)console</code> or <code>(.)account</code>.</li></ul><p>Check and re-run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runtime/app/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">platform</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">plugins</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/@modal/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generate</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # recomposes all plugin routes</span></span></code></pre></div><hr><h2 id="ios-pwa-mobile" tabindex="-1">iOS PWA / mobile <a class="header-anchor" href="#ios-pwa-mobile" aria-label="Permalink to &quot;iOS PWA / mobile&quot;">​</a></h2><p>The layout entries below are two distinct iOS-specific bugs, both surfaced only on real devices (installed standalone PWA and/or mobile Safari). They look similar — &quot;empty space at the bottom&quot; — but have different causes and fixes. The push entry is a configuration trap that only manifests on Apple devices.</p><h3 id="push-notifications-never-arrive-on-ios-the-in-app-bell-works" tabindex="-1">Push notifications never arrive on iOS (the in-app bell works) <a class="header-anchor" href="#push-notifications-never-arrive-on-ios-the-in-app-bell-works" aria-label="Permalink to &quot;Push notifications never arrive on iOS (the in-app bell works)&quot;">​</a></h3><p><strong>Symptom:</strong> Notifications appear in the in-app notification pane, and push works on Android/desktop Chrome — but an iPhone/iPad running the installed PWA never shows a system notification.</p><p><strong>Most common cause — <code>VAPID_CONTACT</code> unset.</strong> Apple&#39;s push service validates the VAPID JWT subject and rejects the <code>mailto:admin@localhost</code> fallback with 403, so every send to an iOS device fails while Chrome&#39;s push service (which is lenient) keeps working. Set <code>VAPID_CONTACT=mailto:&lt;address you monitor&gt;</code> in the runtime&#39;s environment and restart — no re-subscription is needed; the subject is stamped per send. The runtime warns at first send (<code>push: VAPID_CONTACT is unset or points at localhost…</code>) when this applies.</p><p><strong>Checklist if that isn&#39;t it</strong> (check the runtime logs for <code>push: send failed</code> entries with <code>pushService: web.push.apple.com</code> — the <code>statusCode</code>/<code>body</code> fields say which of these it is):</p><ol><li>iOS 16.4+ and the app <strong>installed via Add to Home Screen and opened from the home-screen icon</strong> — Safari-tab subscriptions don&#39;t exist on iOS.</li><li>Push enabled <strong>from within the installed PWA</strong> (Account → Notifications → enable on this device), and iOS Settings → Notifications shows the app with Allow Notifications on.</li><li>The notification&#39;s category isn&#39;t muted in Account → Notifications (muted categories skip the push branch entirely — nothing is logged).</li><li><code>VAPID_PUBLIC_KEY</code>/<code>VAPID_PRIVATE_KEY</code> set on the <strong>runtime</strong> service (not just the auth service) — without both, push is disabled platform-wide and the Account toggle hides itself.</li><li>Web Push requires the production build: the service worker (and therefore push) is disabled in <code>pnpm dev</code>.</li></ol><h3 id="sign-in-screens-rubber-band-bounce-on-press-and-drag" tabindex="-1">Sign-in screens rubber-band / bounce on press-and-drag <a class="header-anchor" href="#sign-in-screens-rubber-band-bounce-on-press-and-drag" aria-label="Permalink to &quot;Sign-in screens rubber-band / bounce on press-and-drag&quot;">​</a></h3><p><strong>Symptom:</strong> On iOS (Safari or the installed PWA), pressing and dragging a finger on the sign-in / register / 2FA screens makes the <strong>whole page bounce</strong> — the card slides up under the status bar, a band of empty background appears below, then it springs back on release. The content already fits; nothing should scroll.</p><p><strong>Cause:</strong> <code>.page</code> used <code>min-height: 100vh</code> with the <strong>document</strong> (<code>html</code>/<code>body</code>) as the scroller. iOS applies elastic overscroll (rubber-band) to the document scroller <strong>even when nothing overflows</strong>, so any drag bounces the page. The runtime shell was already hardened against this, but the auth screens — the standalone <code>apps/auth</code> app and the runtime&#39;s own <code>/login</code>, <code>/register</code>, <code>/login/2fa</code> (which live outside the <code>(platform)</code> shell) — were not.</p><p><strong>Fix:</strong> Stop the document from being the scroller and suppress the bounce:</p><ul><li>Set <code>overscroll-behavior: none</code> on <code>html, body</code> (and <code>overflow: hidden</code> where the whole app is auth-only, e.g. <code>apps/auth/app/globals.css</code>). In the runtime, <code>overscroll-behavior: none</code> goes in the global <code>globals.css</code> — a CSS Module <strong>cannot</strong> carry a purely <code>:global(html)</code> rule (it fails the &quot;pure selector&quot; build check), so it can&#39;t be scoped inside <code>login.module.css</code>.</li><li>Make <code>.page</code> the viewport-sized scroll surface: <code>height: 100dvh</code>, <code>overflow-y: auto</code>, <code>overscroll-behavior: none</code>, and <strong><code>align-items: safe center</code></strong> so a taller form (register / 2FA) still scrolls from the top instead of being centred with its top clipped above the scroll origin.</li></ul><h3 id="empty-strip-below-the-footer-login-card-in-the-installed-pwa" tabindex="-1">Empty strip below the footer / login card in the installed PWA <a class="header-anchor" href="#empty-strip-below-the-footer-login-card-in-the-installed-pwa" aria-label="Permalink to &quot;Empty strip below the footer / login card in the installed PWA&quot;">​</a></h3><p><strong>Symptom:</strong> In the <strong>installed standalone PWA</strong>, a strip of the page background shows <strong>below the bottom nav</strong> on Home (and below the login card after logout). It&#39;s <strong>intermittent</strong> — the same screen sometimes renders correctly and sometimes with the strip, depending on the launch.</p><p><strong>Cause:</strong> iOS standalone PWAs intermittently report the viewport <strong>~status-bar height short at launch/resume</strong> — e.g. <code>793</code> instead of the true <code>852</code> — and the short value appears across <code>window.visualViewport.height</code>, <code>window.innerHeight</code> <strong>and CSS <code>100dvh</code> simultaneously</strong>, never self-correcting until a reflow. Anything sized to those units (the shell&#39;s <code>height: var(--sv-vh)</code>, the auth <code>.page</code>&#39;s <code>100dvh</code>) therefore comes up short, exposing the darker <code>&lt;body&gt;</code> backdrop below it. <strong><code>window.screen.height</code> stays correct</strong> throughout.</p><p>Confirmed by an on-device readout: in the bad state <code>visualViewport.height</code>, <code>innerHeight</code> and <code>100dvh</code> were all <code>793</code> while <code>screen.height</code> was <code>852</code>; in the good state all four were <code>852</code>.</p><p><strong>Fix:</strong> Derive the full-screen height from <code>window.screen.height</code> — the one reliable metric — rather than the viewport units:</p><ul><li>Shared helper <code>runtime/src/viewport-height.ts</code> returns <code>screen.height</code> <strong>in portrait standalone with the keyboard closed</strong>, and falls back to the measured visual viewport everywhere else (browser tabs, where it must sit inside the browser chrome; landscape, where <code>screen.height</code> is unreliable; and while the software keyboard is up, where the shrunk height is wanted so fixed footers stay above the keyboard).</li><li><code>ClientShell</code> pushes it onto <code>--sv-vh</code> for the platform shell; a small <code>ViewportHeightSync</code> client component does the same for the auth pages (which are outside the shell), and their <code>.page</code> consumes <code>height: var(--sv-vh, 100dvh)</code>.</li></ul><p><strong>Diagnosing on-device without Web Inspector:</strong> if a USB/Web-Inspector connection isn&#39;t available, a throwaway fixed-position overlay that prints <code>visualViewport.height</code> / <code>innerHeight</code> / a measured <code>100dvh</code> probe / <code>screen.height</code> / the shell&#39;s <code>offsetHeight</code> (updated on <code>resize</code>, <code>orientationchange</code> and <code>visibilitychange</code>) is enough to read the values straight off the phone.</p><hr><h2 id="docker-production" tabindex="-1">Docker / production <a class="header-anchor" href="#docker-production" aria-label="Permalink to &quot;Docker / production&quot;">​</a></h2><p>See <a href="./self-hosting.html"><code>docs/self-hosting.md</code></a> for Docker-specific troubleshooting (volume ownership, <code>pnpm-workspace.yaml</code> marker, IPv4 healthcheck, etc.).</p><hr><h3 id="full-page-404-opening-an-overlay-route-account-console-in-production-or-the-desktop-app" tabindex="-1">Full-page 404 opening an overlay route (Account/Console) in production or the desktop app <a class="header-anchor" href="#full-page-404-opening-an-overlay-route-account-console-in-production-or-the-desktop-app" aria-label="Permalink to &quot;Full-page 404 opening an overlay route (Account/Console) in production or the desktop app&quot;">​</a></h3><p><strong>Symptom:</strong> Clicking the Account or Console icon shows the styled full-page &quot;404 — This page could not be found. / Go home&quot; — not the dialog. Most often seen <strong>right after opening the app</strong> (or the desktop shell), and a <strong>reload loads the full dialog and the 404 goes away</strong>. This is the production/deployed cousin of the dev-server cold-compile 404 documented above; the visible result is the same styled 404, but the cause is different.</p><p><strong>Cause:</strong> Overlay pages are React Server Components that fetch during render (e.g. <code>security/page.tsx</code> fetches the session and passkeys from the auth server; Console pages self-fetch their data). On a cold client — the <code>@modal</code> interception route not yet initialised in the client router tree, or a fetch failing because the auth server is briefly unreachable (the middleware still lets the request through on its 300 s signed session-data cookie cache) — the overlay page throws during render. Next.js 15 surfaces an <strong>unhandled</strong> RSC error in a parallel-route slot as a full-page 404 rather than routing it to the slot&#39;s error boundary.</p><p><strong>Current protection (why it should not recur):</strong></p><ol><li><code>runtime/app/(platform)/(plugins)/@modal/error.tsx</code> — the slot-level error boundary. It confines any overlay render error to a recoverable &quot;This page could not be loaded / Try again&quot; state <strong>inside the open dialog</strong>, so the failure never escapes the slot as a 404.</li><li>Per-page fetch guards — <code>account/{profile,preferences,data,activity}</code> and <code>console/{plugins,users,health,activity}</code> wrap their self-fetches in <code>try/catch</code> with safe fallbacks so a failed response returns empty data instead of throwing.</li></ol><p>If the styled 404 reappears on a deployed instance, the most likely explanation is that the instance is running a build from <strong>before</strong> these fixes (2026-06-28), or the boundary/guards regressed. Confirm the running build with <code>GET /api/admin/health</code> → <code>platformVersion</code>, and check the browser devtools Network tab at the moment of the 404 for a failed RSC request to an overlay route.</p><p><strong>Residual gap:</strong> <code>plugins/account/app/security/page.tsx</code> still has unguarded <code>await fetch(...)</code>/<code>sdk.auth.listSessions()</code> calls — it currently relies solely on the <code>@modal/error.tsx</code> boundary rather than its own fallbacks like the sibling pages. Wrapping those in <code>try/catch</code> would make the Security tab resilient on its own.</p><p><strong>Desktop shell note:</strong> the shell has no reload shortcut wired yet, so the &quot;just reload&quot; workaround is not available to end users there. Because the shell is a thin WebView that renders whatever the instance serves, a 404 seen in the desktop app is instance-side — debug it against the instance, not the shell.</p><hr><h3 id="login-loops-back-to-the-login-page-on-a-vps-deployment" tabindex="-1">Login loops back to the login page on a VPS deployment <a class="header-anchor" href="#login-loops-back-to-the-login-page-on-a-vps-deployment" aria-label="Permalink to &quot;Login loops back to the login page on a VPS deployment&quot;">​</a></h3><p><strong>Symptom:</strong> Navigating to the runtime (<code>http://VPS_IP:4000</code>) immediately redirects to the auth login page. After signing in, the browser is sent to <code>localhost:4000</code> (or <code>localhost:3000</code>) instead of the server — and the login page appears again.</p><p><strong>Cause:</strong> <code>AUTH_BASE_URL</code> and <code>NEXT_PUBLIC_RUNTIME_URL</code> were not set, so they defaulted to <code>localhost:*</code>. Those addresses are only reachable from inside the server itself; a remote browser that follows them goes nowhere (or to the wrong machine entirely) and ends up back at the login page.</p><p><strong>Fix:</strong> Add these three variables to <code>.env</code> before starting the stack:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AUTH_BASE_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">http://YOUR_VPS_IP_OR_DOMAIN:4001</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NEXT_PUBLIC_RUNTIME_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">http://YOUR_VPS_IP_OR_DOMAIN:4000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AUTH_TRUSTED_ORIGINS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">http://auth:3001</span></span></code></pre></div><p>Restart both containers after editing <code>.env</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> down</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>See <a href="./self-hosting.html#deploying-to-a-real-vps-or-server">Deploying to a real VPS or server</a> for the full setup with TLS/domain examples.</p><hr><h3 id="docker-image-not-found-when-using-sovereign-version" tabindex="-1">Docker image not found when using <code>SOVEREIGN_VERSION</code> <a class="header-anchor" href="#docker-image-not-found-when-using-sovereign-version" aria-label="Permalink to &quot;Docker image not found when using \`SOVEREIGN_VERSION\`&quot;">​</a></h3><p><strong>Symptom:</strong> <code>docker compose -f docker-compose.prod.yml up -d</code> fails with <code>manifest unknown</code> or <code>not found</code> when <code>SOVEREIGN_VERSION</code> is set.</p><p><strong>Cause:</strong> The image names in <code>docker-compose.prod.yml</code> reference <code>ghcr.io/sovereignfs/sovereign-auth</code> and <code>ghcr.io/sovereignfs/sovereign-runtime</code>. If you see a different org name, the file may be out of date.</p><p><strong>Fix:</strong> Pull the latest <code>docker-compose.prod.yml</code> from the repo — the org name was corrected to <code>sovereignfs</code> to match the GitHub Container Registry packages:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div>`,88)])])}const u=s(n,[["render",a]]);export{g as __pageData,u as default};
