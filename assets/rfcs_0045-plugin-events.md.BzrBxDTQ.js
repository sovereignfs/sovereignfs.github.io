import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Plugin events and realtime channels","description":"","frontmatter":{"rfc":37,"title":"Plugin events and realtime channels","status":"Implemented","date":"June 2026","author":"kasunben","scope":"packages/sdk, runtime, packages/manifest, docs; builds on RFC 0015 and RFC 0034","incorporated_into_plan":"Yes — epic task 3.17","sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0045-plugin-events.md"},"headers":[],"relativePath":"rfcs/0045-plugin-events.md","filePath":"rfcs/0045-plugin-events.md"}'),n={name:"rfcs/0045-plugin-events.md"};function o(l,e,r,d,c,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="rfc-0045-—-plugin-events-and-realtime-channels" tabindex="-1">RFC 0045 — Plugin Events and Realtime Channels <a class="header-anchor" href="#rfc-0045-—-plugin-events-and-realtime-channels" aria-label="Permalink to &quot;RFC 0045 — Plugin Events and Realtime Channels&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Implement <code>sdk.events</code> as a plugin-scoped publish/subscribe surface for realtime UI synchronization. It is for low-latency application state updates: list changes, presence, cursors, record edits, progress updates, and other ephemeral events.</p><p>It is not a durable queue, not a notification inbox, and not an audit log. Durable user-visible notifications remain <code>sdk.notifications</code>; audit remains <code>sdk.activity</code>.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Several plugin classes need realtime coordination: shared lists, collaborative canvases, progress indicators, live logs, presence, and collaborative editing. Polling works for small cases but creates latency, duplicate fetches, and inconsistent implementations.</p><p>The platform already has notification transport work for polling, SSE, and Redis pub/sub. This RFC generalizes the realtime transport concept into a plugin SDK surface while keeping delivery semantics intentionally modest.</p><h2 id="current-state" tabindex="-1">Current state <a class="header-anchor" href="#current-state" aria-label="Permalink to &quot;Current state&quot;">​</a></h2><ul><li><code>sdk.events</code> is reserved and throws <code>NotImplementedError</code>.</li><li>Notification transport supports polling/SSE/Redis for notification delivery.</li><li>There is no general plugin realtime channel.</li><li>Plugins can build local polling routes, but there is no shared pattern.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><p><strong>As shipped, this section&#39;s <code>authorizeChannel</code> sketch changed — see the 0.2 changelog entry for the reasoning:</strong> channel authorization is manifest-declared (a new <code>events</code> manifest field, <code>entry</code> file default- exporting an <code>EventChannelAuthorizer</code>) and composed at generate time, the same pattern RFC 0046&#39;s <code>schedules</code>/<code>jobs</code> already use — not a call-based <code>sdk.events.authorizeChannel()</code> registered at runtime. This resolves open question 1 below. The sketch is left as originally written for historical context; see <code>docs/plugin-development.md</code>&#39;s &quot;<code>events</code> — realtime channels&quot; section for the actual shipped surface and examples.</p><h3 id="sdk-surface" tabindex="-1">SDK surface <a class="header-anchor" href="#sdk-surface" aria-label="Permalink to &quot;SDK surface&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EventEnvelope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  channel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  createdAt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.events.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">publish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  channel: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  type: string;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  payload: unknown;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;void&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Client subscription uses a runtime route rather than direct SDK call:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /api/events/stream?pluginId=&lt;id&gt;&amp;channel=&lt;channel&gt;</span></span></code></pre></div><p>The route requires a session and checks that the current user may subscribe to that channel.</p><h3 id="channel-naming" tabindex="-1">Channel naming <a class="header-anchor" href="#channel-naming" aria-label="Permalink to &quot;Channel naming&quot;">​</a></h3><p>Channels are plugin-scoped:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;pluginId&gt;:&lt;resource-kind&gt;:&lt;resource-id&gt;</span></span></code></pre></div><p>Plugins pass only the local channel part to the SDK:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.events.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">publish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  channel: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`list:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">listId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;item.checked&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  payload: { itemId },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The runtime prefixes the calling plugin ID and actor context.</p><h3 id="authorization" tabindex="-1">Authorization <a class="header-anchor" href="#authorization" aria-label="Permalink to &quot;Authorization&quot;">​</a></h3><p>Publishing requires <code>events:publish</code>. Subscribing requires <code>events:subscribe</code>. The plugin must provide channel authorization metadata or a route-level check.</p><p>Phase 1 uses a plugin-owned authorization callback registered server-side:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.events.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">authorizeChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;list:*&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">channel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> userCanReadList</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userId, channel.resourceId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>If no authorization callback is registered, subscription fails closed.</p><h3 id="delivery-semantics" tabindex="-1">Delivery semantics <a class="header-anchor" href="#delivery-semantics" aria-label="Permalink to &quot;Delivery semantics&quot;">​</a></h3><p>Phase 1 semantics:</p><ul><li>best-effort delivery;</li><li>no guaranteed ordering across processes;</li><li>no replay after disconnect;</li><li>bounded payload size;</li><li>no durable persistence;</li><li>clients reconnect and refetch state after reconnect.</li></ul><p>Plugins must treat events as invalidation/update hints, not the sole source of truth.</p><h3 id="transport" tabindex="-1">Transport <a class="header-anchor" href="#transport" aria-label="Permalink to &quot;Transport&quot;">​</a></h3><p>The event broker can reuse the notification broker transport modes:</p><ul><li>in-process SSE for single-node deployments;</li><li>Redis pub/sub for multi-process/multi-node deployments;</li><li>polling fallback for environments where streaming is unavailable.</li></ul><h2 id="security-requirements" tabindex="-1">Security requirements <a class="header-anchor" href="#security-requirements" aria-label="Permalink to &quot;Security requirements&quot;">​</a></h2><ul><li>Session required for subscriptions.</li><li>Channel authorization required for every subscription.</li><li>Plugins cannot publish as another plugin.</li><li>Payload size is capped.</li><li>Event payloads are not written to activity logs by default.</li><li>Events do not cross tenant boundaries.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="use-notifications-for-realtime-sync" tabindex="-1">Use notifications for realtime sync <a class="header-anchor" href="#use-notifications-for-realtime-sync" aria-label="Permalink to &quot;Use notifications for realtime sync&quot;">​</a></h3><p>Rejected. Notifications are durable, user-facing inbox items. Realtime sync events are ephemeral and often too noisy for an inbox.</p><h3 id="require-each-plugin-to-implement-sse" tabindex="-1">Require each plugin to implement SSE <a class="header-anchor" href="#require-each-plugin-to-implement-sse" aria-label="Permalink to &quot;Require each plugin to implement SSE&quot;">​</a></h3><p>Rejected. It duplicates transport and Redis/pub-sub handling.</p><h3 id="implement-crdt-collaboration-in-the-platform" tabindex="-1">Implement CRDT collaboration in the platform <a class="header-anchor" href="#implement-crdt-collaboration-in-the-platform" aria-label="Permalink to &quot;Implement CRDT collaboration in the platform&quot;">​</a></h3><p>Deferred. The platform should provide transport; plugins own their data model and conflict-resolution semantics.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><p><em>Resolved — see the 0.2 changelog entry for the full implementation:</em></p><ol><li><strong>Should channel authorization be callback-based, manifest-declared, or route based?</strong> Manifest-declared: an <code>events</code> field (<code>pattern</code>/<code>entry</code>/ <code>description</code>), composed at generate time into <code>runtime/generated/plugin-events.ts</code>, the same static-import pattern RFC 0046&#39;s <code>schedules</code>/<code>jobs</code> use. A call-based <code>sdk.events.authorizeChannel()</code> was considered but rejected for the same reason <code>schedules</code>/<code>jobs</code> didn&#39;t do this either — no reliable moment for plugin code to register a callback before the first subscribe request needs it.</li><li><strong>Should the platform offer short replay windows for reconnects?</strong> Partially — <code>/api/events/poll</code> reads a small, bounded (50 events / 5 minutes), <strong>per-process, in-memory</strong> ring buffer, not a true replay log. It exists primarily as the polling-fallback transport, but a reconnecting SSE client could also use it to catch up on very recent history. Not durable, not cross-process-consistent — see the ring buffer&#39;s own doc comment (<code>runtime/src/event-ring-buffer.ts</code>) and Delivery semantics above, which this doesn&#39;t change.</li><li><strong>Should event payload schemas be declared in manifests?</strong> Not in this leg. Payloads are opaque <code>unknown</code> on both the publish and authorize sides, capped at 16 KB. Left as a possible follow-up if a real need for compile-time payload validation emerges.</li><li><strong>How should Redis transport be required in production multi-node deployments?</strong> Same operator-facing shape as <code>NOTIFICATION_TRANSPORT</code> (RFC 0034) — a dedicated <code>SOVEREIGN_EVENTS_TRANSPORT</code> env var (<code>sse</code> default, <code>redis</code>, <code>polling</code>), reusing the existing <code>REDIS_URL</code> rather than a second connection string. Deliberately a <em>separate</em> env var from <code>NOTIFICATION_TRANSPORT</code>, not a shared one — events and notifications scale differently and an operator may want to disable/resize one without touching the other.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li>✅ Add event broker abstraction using existing transport patterns (<code>runtime/src/event-broker.ts</code> + in-process/Redis implementations — structurally mirrors, but does not share code with, the notification broker; see its own doc comment for why they&#39;re kept separate).</li><li>✅ Add <code>sdk.events.publish()</code> and the subscription route (plus <code>/api/events/poll</code> for the polling fallback — see open question 2).</li><li>✅ Add channel authorization (manifest-declared, not a call-based API — see open question 1).</li><li>✅ Add tests for authorization, disconnect/reconnect, and disabled plugins.</li><li>✅ Document polling fallback and state-refetch requirements (<code>docs/plugin-development.md</code>).</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>June 2026</td><td>Initial draft</td></tr><tr><td>0.2</td><td>August 2026</td><td>Full <code>sdk.events</code> shipped (epic task 3.17, workstream 0015 leg 2). Manifest <code>events</code> field (<code>pattern</code>/<code>entry</code>/<code>description</code>) composed by <code>scripts/generate-registry.ts</code> into <code>runtime/generated/plugin-events.ts</code>, resolving open question 1 — channel authorization is manifest-declared + generate-time static import, not a call-based <code>authorizeChannel()</code>. <code>sdk.events.publish()</code> (<code>packages/sdk/src/events.ts</code>), gated on a new <code>events:publish</code> manifest permission and enforced host-side (<code>requireEventsPluginContext</code>, <code>runtime/src/plugin-events.ts</code>, mirroring <code>plugin-mailer.ts</code>&#39;s <code>requireMailerPluginContext</code>); payloads capped at 16 KB (<code>runtime/src/event-limits.ts</code>, <code>EventPayloadTooLargeError</code>). New, independent <code>runtime/src/event-broker.ts</code> (in-process EventEmitter + Redis pub/sub implementations, <code>SOVEREIGN_EVENTS_TRANSPORT</code> env var, reuses <code>REDIS_URL</code>) — deliberately not a shared instance with the notification broker (different keyspace, different payload shape, different security model; see that file&#39;s doc comment). <code>runtime/src/event-authorization.ts</code>&#39;s <code>authorizeChannel()</code> matches a subscribing user&#39;s channel against every manifest-declared pattern for the target plugin (simple prefix match on a trailing <code>:*</code> wildcard, enforced at manifest-validation time), invokes each matching handler, and fails closed — no match, every match false, or every match throwing all deny; any one match returning <code>true</code> allows. <code>GET /api/events/stream</code> (SSE, <code>runtime/app/api/events/stream/route.ts</code>) and <code>GET /api/events/poll</code> (<code>.../poll/route.ts</code>) share one validation path (<code>runtime/src/event-subscribe-guard.ts</code>): session, <code>pluginId</code>/<code>channel</code> query params, target plugin installed + declares <code>events:subscribe</code> + not disabled, then channel authorization — polling is not a lesser-checked shortcut. <code>events</code> added to <code>RESERVED_API_SEGMENTS</code> (<code>runtime/src/api-namespace.ts</code>). Resolved open question 2 (replay windows) via a small bounded (50 events / 5 min), per-process, in-memory ring buffer (<code>runtime/src/event-ring-buffer.ts</code>) that backs <code>/api/events/poll</code> — explicitly not durable persistence, satisfying this RFC&#39;s &quot;no durable persistence&quot; requirement while still giving non-SSE clients something to read; idempotent on event id since the Redis broker&#39;s own publish echoes back through its subscription. Open question 3 (manifest-declared payload schemas) remains unresolved — payloads stay opaque <code>unknown</code>, capped by size only.</td></tr></tbody></table>`,50)])])}const k=s(n,[["render",o]]);export{u as __pageData,k as default};
