import{_ as s,o as i,c as t,a2 as a}from"./chunks/framework.CHeM0PsO.js";const k=JSON.parse('{"title":"RFC 0083 — Device bridge and capability contract","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0083-device-bridge-capability-contract.md"},"headers":[],"relativePath":"rfcs/0083-device-bridge-capability-contract.md","filePath":"rfcs/0083-device-bridge-capability-contract.md"}'),n={name:"rfcs/0083-device-bridge-capability-contract.md"};function o(r,e,d,l,h,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0083-—-device-bridge-and-capability-contract" tabindex="-1">RFC 0083 — Device bridge and capability contract <a class="header-anchor" href="#rfc-0083-—-device-bridge-and-capability-contract" aria-label="Permalink to &quot;RFC 0083 — Device bridge and capability contract&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/sdk</code> (new <code>@sovereignfs/sdk/device-client</code> subpath — the capability <strong>contract</strong> plus <code>provideBridge()</code>; stays dependency-free), new <code>packages/bridge</code> (<code>@sovereignfs/bridge</code> — <strong>published</strong>; the <strong>implementation</strong>: transports, protocol, and the shell-side helper consumed by <code>sovereign-mobile</code> and <code>sovereign-desktop</code>), <code>packages/manifest</code> (new <code>device:*</code> permissions), <code>runtime</code> (per-user device consent grants), <code>plugins/account</code> (consent management UI), <code>docs/plugin-development.md</code>, <code>docs/architecture-rules.md</code>, <code>docs/sdk-stability.md</code>. Builds on RFC 0080 (surface model — supplies transport detection); implements the &quot;SDK abstraction&quot; tier that RFC 0058 §&quot;Device API strategy&quot; and RFC 0038 §&quot;Device API tier&quot; both specify but neither designs; supersedes the <code>sdk.device.notify()</code> / <code>sdk.device.secureStore.*</code> sketches in RFC 0038&#39;s epic tasks. Related research: <a href="./../research/0006-standalone-plugin-apps.html">0006</a>.<br><strong>Incorporated into plan:</strong> Yes — epic tasks 3.34–3.35.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Define the contract by which web content running on a Sovereign instance reaches <strong>device capabilities</strong> through a native shell — and make it one contract, shared by the Capacitor mobile shell and the Tauri desktop shell, with a Web-API fallback tier so the same plugin code runs unchanged in a plain browser.</p><p>Three pieces:</p><ol><li><strong>A contract/implementation split</strong> — <code>@sovereignfs/sdk/device-client</code> declares the capability registry, types, and <code>provideBridge()</code>; a new published <code>@sovereignfs/bridge</code> implements the transports and protocol and registers itself. Both external shell repositories consume the same published implementation, so the two shells cannot drift apart in protocol, and the SDK keeps its zero runtime dependencies by reusing the <code>provideHost()</code> handoff pattern.</li><li><strong>Capability negotiation</strong> — the shell announces, at handshake, exactly which capabilities it supports and at which version. Plugins ask <code>supports(&#39;haptics.impact&#39;, 1)</code>. <strong>Nothing ever compares shell versions.</strong></li><li><strong><code>sdk.device.*</code> capability calls</strong> — the plugin-facing surface, browser-only, returning a typed discriminated result rather than throwing, so &quot;unavailable&quot;, &quot;denied&quot;, &quot;dismissed&quot;, and &quot;failed&quot; stay four distinct product outcomes.</li></ol><p>v1 deliberately ships a <strong>thin slice</strong> — enough capabilities to prove the contract, not the full device surface: <code>haptics.impact</code> and <code>notifications.native</code> for plugins, plus a platform-internal <code>secureStorage</code> tier. Every later capability (camera, biometrics, push, background) is an additive registry entry plus two transport implementations, not a new mechanism.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>RFC 0058 and RFC 0038 both state that plugin developers call <code>sdk.device.*</code> only, and that the SDK &quot;detects the environment and routes to the correct tier&quot;. Neither designs that routing, and <code>packages/sdk/src/device.ts</code> does not exist. The consequence today is six scheduled epic tasks (17.2, 17.4, 17.7, 20.3, 20.5–20.8) that each presume a bridge nobody has specified — so each would arrive at its own, and the mobile and desktop shells would diverge on the first capability that shipped to both.</p><p>The immediate need is also concrete: the platform is preparing to serve three surfaces at once — PWA (web and mobile), Tauri desktop, Capacitor mobile. The shells live in separate repositories on their own release cadences. Without a versioned protocol owned here, &quot;the platform and plugin runtime serve all three surfaces&quot; is an aspiration rather than a contract.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong><code>sdk.device.*</code> does not exist.</strong> <code>packages/sdk/src/device.ts</code> is absent. Epic 17.1&#39;s deliverables record the deferral explicitly: &quot;<code>packages/sdk</code> patch — add <code>&quot;desktop&quot;</code> environment to <code>sdk.device.*</code> routing — <strong>deferred to task 17.7</strong>: <code>sdk.device.*</code> does not exist in <code>packages/sdk</code> yet&quot;. Task 17.7 then says that if the surface still hasn&#39;t landed, &quot;ship the desktop check as part of its first implementation instead&quot;. That first implementation is RFC 0080&#39;s task 3.32; this RFC is the capability layer on top.</li><li><strong>The entire <code>SdkHost</code> is server-side — and this is the load-bearing constraint for this RFC.</strong> Every namespace in <code>packages/sdk/src/host.ts</code> (<code>db</code>, <code>mailer</code>, <code>email</code>, <code>platform</code>, <code>directory</code>, <code>data</code>, <code>activity</code>, <code>portability</code>, <code>plugins</code>, <code>notifications</code>, <code>storage</code>, <code>e2ee</code>, <code>secrets</code>, <code>connections</code>) routes through <code>requireHost()</code>, which reads a <code>Symbol.for(&#39;@sovereignfs/sdk:host&#39;)</code>-keyed global registered once from <code>runtime/instrumentation.ts</code>. <strong>Device capabilities cannot use any of it</strong> — a camera, a haptic motor, and a keychain live in the WebView, and no server round-trip can reach them.</li><li><strong>The precedent to follow instead is <code>@sovereignfs/sdk/offline</code>.</strong> Its own doc comment records why it is a dedicated subpath rather than part of the barrel: the barrel transitively reaches server-only <code>next/headers</code>, and Next&#39;s client/server boundary check flags the whole reachable module graph, so a <code>&#39;use client&#39;</code> component importing from the barrel fails to build. <code>offline.ts</code> has no <code>SdkHost</code> entry and no <code>provideHost()</code> wiring at all — architecturally unlike every other namespace. The device bridge is the second member of that family. The <code>e2ee-crypto</code> / <code>e2ee-device</code> modules are the third.</li><li><strong>RFC 0080 supplies transport detection, not invocation.</strong> Its server tier (<code>getSurface()</code> from the injected <code>x-sovereign-surface</code> header) and its client tier (<code>useDeviceEnvironment()</code>, <code>installed</code>) answer <em>where am I</em>. This RFC answers <em>what can I do here, and how do I ask</em>.</li><li><strong>No capability-negotiation or feature-detection pattern exists in the repo.</strong><code>compatibility.minPlatformVersion</code> / <code>maxPlatformVersion</code> (<code>packages/manifest/src/schema.ts:241-251</code>, RFC 0024) gate a plugin against the platform — both of which the operator controls. A shell is controlled by neither the operator nor the platform, so that mechanism does not transfer.</li><li><strong>The permission enum has no device entries.</strong> <code>permissionSchema</code> (<code>packages/manifest/src/schema.ts:19-36</code>) currently holds <code>auth:session</code>, <code>db:readWrite</code>, <code>db:readOnly</code>, <code>mailer:send</code>, <code>mailer:sendExternal</code>, <code>storage:readWrite</code>, <code>notifications:send</code>, <code>events:publish</code>, <code>events:subscribe</code>, <code>data:provide</code>, <code>data:consume</code>, <code>data:export</code>, <code>data:import</code>, <code>activity:write</code>, <code>e2ee:use</code>, <code>admin:*</code>, <code>offline:write</code>.</li><li><strong>RFC 0078 §6 established the honest position on permission enforcement</strong>: a permission is centrally enforceable only where every call funnels through a shared host boundary the platform controls (as <code>mailer:send</code> does via <code>requireMailerPluginContext()</code> inside <code>provideHost({...})</code>). Where no such boundary exists, a permission is &quot;review/install-time metadata with an optional convenience helper, not a guarantee&quot;. That precedent applies here and is sharpened in §5.</li><li><strong>Notification infrastructure already exists</strong> — RFC 0015&#39;s Notification Center and RFC 0016&#39;s web push, with <code>sdk.notifications.send()</code> server-side and a pluggable transport broker (RFC 0034). The web tier of <code>notifications.native</code> should route into this rather than parallel it.</li><li><strong><code>@sovereignfs/sdk</code> and <code>@sovereignfs/ui</code> are published contracts under NFR-04</strong>: patch releases must never contain breaking changes; a breaking change needs at minimum a minor bump plus a migration note. A capability registry must therefore be designed for additive growth from day one.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-contract-in-the-sdk-implementation-in-sovereignfs-bridge" tabindex="-1">1. Contract in the SDK, implementation in <code>@sovereignfs/bridge</code> <a class="header-anchor" href="#_1-contract-in-the-sdk-implementation-in-sovereignfs-bridge" aria-label="Permalink to &quot;1. Contract in the SDK, implementation in \`@sovereignfs/bridge\`&quot;">​</a></h3><p>The split follows the pattern this repository already uses for exactly this problem — the SDK needing to call something it must not depend on.</p><p><strong><code>@sovereignfs/sdk/device-client</code></strong> (a new subpath, joining the five that already exist) owns the <strong>contract</strong>:</p><ul><li>The capability registry: capability names, versions, request/response types.</li><li><code>DeviceResult</code> and the handshake shape.</li><li>The plugin-facing <code>sdk.device.*</code> API.</li><li><code>provideBridge(impl)</code> — the registration entry point.</li></ul><p><strong><code>@sovereignfs/bridge</code></strong> (new package at <code>packages/bridge</code>, published) owns the <strong>implementation</strong>:</p><ul><li>The <code>web</code>, <code>capacitor</code>, and <code>tauri</code> transports.</li><li>The wire protocol and handshake mechanics.</li><li>The shell-side handler helper.</li></ul><p>The SDK keeps <strong>zero runtime dependencies</strong>. <code>@sovereignfs/bridge</code> depends on <code>@sovereignfs/sdk</code> as a <strong><code>devDependency</code> only</strong> — types are erased at build, so its published output carries no runtime dependency while the type contract still has exactly one source of truth. The dependency arrow points implementation → contract, the same direction <code>runtime</code> → <code>@sovereignfs/sdk</code> already points.</p><p>Registration mirrors <code>provideHost()</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// @sovereignfs/sdk/device-client — declares, never implements</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BridgeImpl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  handshake</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">BridgeHandshake</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">capability</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DeviceResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> provideBridge</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">impl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BridgeImpl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>runtime</code> registers it from a client bootstrap — the client-side analogue of <code>runtime/instrumentation.ts</code> calling <code>provideHost()</code>.</p><p><strong>The registration must be stored on a <code>Symbol.for</code>-keyed global, not a module-level variable</strong>, for the reason <code>packages/sdk/src/host.ts</code> already documents at length: Next compiles instrumentation, route handlers, and server actions into separate bundles that each get their own module instance, and dev HMR resets module state. There is a second reason here — a plugin could install a different major of <code>@sovereignfs/bridge</code> than the one the platform ships, giving two copies with two independent handshake states, one of which would never resolve. A <code>Symbol.for</code> global makes one registration win in both cases.</p><p><strong>Two entry points</strong>, so neither side pulls the other&#39;s code:</p><table tabindex="0"><thead><tr><th>Entry</th><th>Consumer</th><th>Contains</th></tr></thead><tbody><tr><td><code>@sovereignfs/bridge</code></td><td><code>runtime</code> (page side)</td><td>Transports, handshake client</td></tr><tr><td><code>@sovereignfs/bridge/shell</code></td><td>the two shell repos</td><td>Shell-side handler helper</td></tr></tbody></table><p><code>@sovereignfs/bridge</code> itself has zero runtime dependencies — no React, no Next, no Node built-ins — and never reaches <code>next/headers</code>, <code>@sovereignfs/db</code>, or anything in <code>SdkHost</code>. It is published and semver-disciplined like the SDK, because two external repositories on independent release cadences consume it.</p><blockquote><p><strong>Why not simply have <code>packages/sdk</code> depend on <code>@sovereignfs/bridge</code>?</strong> It was the first design considered and it is worse on the facts. <code>packages/sdk</code> has no <code>dependencies</code> field at all today — <code>next</code> is a <code>peerDependency</code> and everything else is a devDependency — and <code>@sovereignfs/ui</code> is the same. &quot;SDK zero-deps&quot; is therefore a literal, uniform property of both published packages rather than a rule about two specific imports, and a real dependency at version 1.27.0 would be the first. It would also make the dual-major duplication problem above a live risk instead of a guarded one, and give plugin developers a transitive install for a package they should never need to know exists. The <code>provideHost()</code> pattern costs nothing and needs no rule amendment.</p></blockquote><h3 id="_2-transports" tabindex="-1">2. Transports <a class="header-anchor" href="#_2-transports" aria-label="Permalink to &quot;2. Transports&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BridgeTransport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;web&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;capacitor&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;tauri&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Transport</th><th>Mechanism</th><th>Notes</th></tr></thead><tbody><tr><td><code>capacitor</code></td><td>Capacitor plugin calls, mediated by the shell</td><td>Promise-based, plugin-namespaced</td></tr><tr><td><code>tauri</code></td><td>Tauri command invocation, mediated by the shell</td><td>Command-name based — a structurally different shape</td></tr><tr><td><code>web</code></td><td>Standard Web APIs</td><td>The always-available floor; reports the rest unavailable</td></tr></tbody></table><p>The two native bridge shapes are genuinely different, which is precisely why plugins must not touch them. RFC 0058 already requires this (&quot;Plugin developers call <code>sdk.device.*</code> only. They do not import Capacitor plugins or branch directly on native shell internals&quot;); this RFC makes it mechanically true.</p><h3 id="_3-handshake-and-capability-negotiation" tabindex="-1">3. Handshake and capability negotiation <a class="header-anchor" href="#_3-handshake-and-capability-negotiation" aria-label="Permalink to &quot;3. Handshake and capability negotiation&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CapabilityDescriptor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// e.g. &#39;haptics.impact&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// integer, incremented on a breaking payload change</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BridgeHandshake</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Framing/protocol version of @sovereignfs/bridge itself. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  protocolVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  shell</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;sovereign-mobile&#39; | &#39;sovereign-desktop&#39; | …</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// informational only — never branch on it</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    platform</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ios&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;android&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;macos&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;windows&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;linux&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;web&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;web&#39; added in leg 1 — see open question 8</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  };</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Exactly what THIS shell build supports. The authoritative list. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  capabilities</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CapabilityDescriptor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>The rule that makes cross-cadence releases survivable:</strong></p><blockquote><p>Plugins and the platform never compare shell versions. They ask whether a named capability at a given version is present. <code>shell.version</code> exists for diagnostics and bug reports only, and branching on it is a review-blocking mistake.</p></blockquote><p>This is not stylistic. A shell updates on the <strong>user&#39;s</strong> schedule — via an app store, subject to a review queue, and often with auto-update disabled entirely — while the instance updates on the <strong>operator&#39;s</strong> schedule. Skew is therefore normal and runs in both directions:</p><table tabindex="0"><thead><tr><th>Situation</th><th>Behavior under negotiation</th></tr></thead><tbody><tr><td>New instance, old shell</td><td>Newer capability simply absent from <code>capabilities</code> → <code>unavailable</code>, handled path</td></tr><tr><td>Old instance, new shell</td><td>Unknown capability names ignored; nothing breaks</td></tr><tr><td>Capability payload changed</td><td>Shipped as a new <code>version</code>; the shell may advertise both while clients migrate</td></tr><tr><td>Shell not present at all</td><td>Transport is <code>web</code>; capabilities resolve to the Web-API floor or <code>unavailable</code></td></tr></tbody></table><p>No coordinated release is ever required between this repository and either shell repository. That property is the main reason this RFC exists as a protocol rather than a set of per-capability SDK methods.</p><h3 id="_4-typed-results-—-no-throws-for-expected-outcomes" tabindex="-1">4. Typed results — no throws for expected outcomes <a class="header-anchor" href="#_4-typed-results-—-no-throws-for-expected-outcomes" aria-label="Permalink to &quot;4. Typed results — no throws for expected outcomes&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeviceResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ok&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;unavailable&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">capability</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// not on this surface, or shell too old</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;denied&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// user/OS refused, persistently</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;dismissed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// user cancelled this attempt</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;failed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// hardware or runtime error</span></span></code></pre></div><p><code>denied</code> and <code>dismissed</code> are deliberately distinct, because they demand different UI: a denied capability needs a &quot;you&#39;ll need to enable this in Settings&quot; path, since asking again will not prompt; a dismissed one should simply let the user try again. Collapsing them — or collapsing either into a thrown error — destroys information the caller cannot recover, and it is the single most common way device abstractions become unpleasant to use.</p><p>Exceptions are reserved for programmer error (unknown capability name, malformed payload), never for user or environment outcomes.</p><h3 id="_5-permission-model-—-and-where-it-genuinely-bites" tabindex="-1">5. Permission model — and where it genuinely bites <a class="header-anchor" href="#_5-permission-model-—-and-where-it-genuinely-bites" aria-label="Permalink to &quot;5. Permission model — and where it genuinely bites&quot;">​</a></h3><p>Two layers, plus the OS.</p><p><strong>a. Manifest declaration.</strong> New <code>device:*</code> entries in <code>permissionSchema</code>:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;device:haptics&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;device:notifications&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }</span></span></code></pre></div><p>v1 adds only the capabilities v1 ships (§7). Each later capability adds its own permission in the same task that adds the capability — never a broad <code>device:*</code> grant.</p><p><strong>b. Per-user runtime consent.</strong> For any capability that touches personal data or hardware the user would reasonably want to control, the platform records a per-user, per-plugin, per-capability grant, managed in the Account plugin alongthe existing data-consent surface (<code>packages/sdk/src/data.ts</code>&#39;s consent model is the precedent). Absent a grant, the bridge prompts once and stores the outcome; a <code>denied</code> grant short-circuits without reaching the OS.</p><p><strong>c. The OS prompt</strong> still applies on top, and always wins.</p><p><strong>Now the honest part, stated plainly because it would otherwise be assumed away.</strong></p><p><strong>Client-side plugin identity is self-declared and not verifiable.</strong><code>x-sovereign-plugin-id</code> is a <em>server</em> header injected by middleware. In the browser, every plugin&#39;s client code shares one origin and one JavaScript context, so any client code can claim any plugin id when calling the bridge. Consequently:</p><table tabindex="0"><thead><tr><th>What</th><th>Enforcement strength</th></tr></thead><tbody><tr><td>Native-only capability, shell exposes only the narrow bridge</td><td><strong>Real</strong> — the bridge is the sole route to the native API, so an undeclared capability call can be refused</td></tr><tr><td>Web-API-backed capability</td><td><strong>Advisory</strong> — a plugin can call <code>navigator.*</code> directly; the platform cannot intercept browser globals</td></tr><tr><td>Isolation <em>between</em> plugins</td><td><strong>None client-side</strong> — same origin, same JS context, self-declared identity</td></tr></tbody></table><p>So <code>device:*</code> permissions are install-time and review-time metadata plus the input to an honest consent prompt (&quot;<em>Tally</em> wants to use haptics&quot;), <strong>not</strong> a security boundary between plugins. This is the same conclusion RFC 0078 §6 reached for <code>offline:write</code> and the same posture RFC 0080 §2 takes on the surface signal. <code>docs/plugin-development.md</code> must say so in those words.</p><p><strong>The one structural lever, and it is a shell requirement:</strong></p><blockquote><p>The shell should expose <strong>only</strong> the narrow bridge surface to page JavaScript, not raw <code>window.Capacitor</code>, <code>window.__TAURI__</code>, or any direct plugin/command object. That is what makes the first row of the table above true rather than aspirational, and it belongs in both shell repositories&#39; review checklists.</p></blockquote><p><strong>How achievable that is differs by shell, and the difference is not yet verified.</strong> Tauri&#39;s command surface is opt-in — the shell declares which commands exist, so exposing only bridge commands is straightforward. Capacitor, by contrast, <strong>injects its own runtime into the WebView by design</strong>, and page JavaScript is the intended caller of <code>Capacitor.Plugins.*</code>. Whether a Capacitor shell can meaningfully withhold that object from page scripts while still using Capacitor plugins itself is an open implementation question.</p><p>If it cannot, the honest consequence is that on the <code>capacitor</code> transport the first row of the table collapses into the second — native capability permissions become advisory there too, and the enforcement claim holds only for Tauri. That would not invalidate the contract (portability, negotiation, and typed results are unaffected), but it would need saying in <code>docs/plugin-development.md</code> rather than quietly leaving a stronger claim in place. <strong>Verify before relying on the enforcement claim for mobile</strong> — workstream 0003 leg 4.</p><h3 id="_6-plugin-facing-surface" tabindex="-1">6. Plugin-facing surface <a class="header-anchor" href="#_6-plugin-facing-surface" aria-label="Permalink to &quot;6. Plugin-facing surface&quot;">​</a></h3><p>On the existing browser-only subpath from RFC 0080:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// @sovereignfs/sdk/device-client</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> supports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">capability</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BridgeTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getShellInfo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BridgeHandshake</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;shell&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> haptics</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  impact</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">style</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;light&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;medium&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;heavy&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DeviceResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> nativeNotifications</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">url</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DeviceResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  getPermission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;granted&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;denied&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;prompt&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;unsupported&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // \`pluginId\` added in leg 2 — this module is browser-only, so there is no</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // server-injected header to attribute the consent grant to; the caller</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // must self-declare (see §5&#39;s honesty section — this is exactly that).</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  requestPermission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DeviceResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;granted&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;denied&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>supports()</code> is synchronous and safe to call in render <strong>only after mount</strong> — before the handshake resolves it returns <code>false</code>. This is deliberate and matches <code>useDeviceEnvironment()</code> returning <code>null</code> pre-mount: capabilities are progressive enhancement, and a component must render a working state without them.</p><h3 id="_7-v1-capability-slice" tabindex="-1">7. v1 capability slice <a class="header-anchor" href="#_7-v1-capability-slice" aria-label="Permalink to &quot;7. v1 capability slice&quot;">​</a></h3><p>Deliberately thin — enough to prove negotiation, permissions, transports, and result semantics end to end.</p><table tabindex="0"><thead><tr><th>Capability</th><th>Plugin-facing</th><th>capacitor</th><th>tauri</th><th>web</th></tr></thead><tbody><tr><td><code>haptics.impact</code></td><td>Yes</td><td>Haptics plugin</td><td>no-op <code>unavailable</code></td><td>Vibration API where present</td></tr><tr><td><code>notifications.native</code></td><td>Yes</td><td>Local notification</td><td>OS notification</td><td>Web Notifications / existing web push</td></tr><tr><td><code>secureStorage.*</code></td><td><strong>Yes, since RFC 0093</strong></td><td>Keychain/Keystore</td><td>Keychain equivalent</td><td><code>unavailable</code></td></tr></tbody></table><p><strong><code>haptics.impact</code></strong> is chosen first precisely because it is trivial, needs no permission prompt, and has a clean no-op fallback — it proves the round trip without any of the interesting risk.</p><p><strong><code>notifications.native</code></strong> is the first capability with real product value, and its web tier routes into the already-shipped Notification Center / web push infrastructure rather than paralleling it. This is the &quot;SDK routes to the correct tier&quot; promise made real on shipped code.</p><p><strong><code>secureStorage</code> was platform-internal in v1, not plugin-facing — superseded by RFC 0093.</strong> The reasoning at the time was a direct consequence of §5: since client-side plugin identity is self-declared, a plugin-facing keychain would let any plugin&#39;s client code read any other&#39;s entries, and its only planned consumer was RFC 0082 §5&#39;s durable-session sequel (an OAuth refresh token in the OS keychain), which doesn&#39;t need plugin exposure at all. RFC 0093 (the <code>device-only</code> offline tier&#39;s key custody design) later made it plugin-facing anyway: <code>sdk.device.secureStorage.{get,set,remove,keys,clear}()</code> in <code>device-client.ts</code>, gated by a new <code>device:secureStorage</code> manifest permission. The identity- verification gap above was never closed — it is accepted, not solved, and documented as such in <code>docs/sdk-stability.md</code>: &quot;secureStorage is the one exception worth naming: its own hardware-backed device-auth gate (Keychain/ Keystore or WebAuthn PRF) is real even though the plugin-id attribution around it is not.&quot; The trade RFC 0093 made is that the value being protected is inherently the calling plugin&#39;s own data (each <code>device-only</code> plugin&#39;s own local records, scoped by the <code>pluginId</code> each call self-declares) — a plugin that lies about its id can only read another plugin&#39;s storage <em>location</em>, not bypass the device-auth gate that protects the underlying key, which is the property that actually matters for RFC 0093&#39;s threat model. This paragraph is kept as history rather than deleted, so a reader lands on both the original reasoning and what changed it, not just the current state.</p><p><strong>Unresolved tension for whoever builds RFC 0082 §5&#39;s sequel next: the wire contract has no per-item access-control policy.</strong> RFC 0093&#39;s <code>secureStorage</code> requires a live biometric-or-passcode prompt on <strong>every</strong> operation, gated in the native implementation itself (see that RFC&#39;s §2 and its now-resolved open question 1) — not a caller-supplied option, since <code>invoke(&#39;secureStorage&#39;, {op, pluginId, key, value?})</code> has no field for it. RFC 0082 §5&#39;s own refresh -token consumer explicitly wants &quot;silent recovery after a data purge, and biometric unlock&quot; — read together, that sounds like it wants <strong>both</strong> an ungated silent read (to exchange the token for a session cookie automatically) <strong>and</strong> a gated one (to unlock), which the current single-capability-name, uniformly-gated design cannot express. Neither RFC resolves this because neither consumer has been built yet (RFC 0082 §5 is explicitly &quot;designed but not built,&quot; gated on RFC 0072&#39;s own addendum) — it isn&#39;t a live bug. Whichever lands second should either confirm uniform gating is actually fine for its own use case, or this contract needs a policy field before both consumers can share one capability name.</p><h3 id="_8-relationship-to-existing-epic-tasks" tabindex="-1">8. Relationship to existing epic tasks <a class="header-anchor" href="#_8-relationship-to-existing-epic-tasks" aria-label="Permalink to &quot;8. Relationship to existing epic tasks&quot;">​</a></h3><p>Six scheduled tasks currently presume a bridge. Under this RFC each becomes an implementation of it rather than a parallel invention:</p><table tabindex="0"><thead><tr><th>Task</th><th>Was</th><th>Becomes</th></tr></thead><tbody><tr><td>17.7</td><td>&quot;SDK <code>desktop</code> environment&quot;</td><td>Subsumed: RFC 0080 task 3.32 + this RFC&#39;s transport detection</td></tr><tr><td>20.3</td><td>&quot;Mobile SDK native environment and bridge adapter&quot;</td><td>The Capacitor transport of <code>@sovereignfs/bridge</code></td></tr><tr><td>17.2 (notify half)</td><td><code>sdk.device.notify()</code> sketch</td><td>The Tauri transport of <code>notifications.native</code></td></tr><tr><td>17.4</td><td><code>sdk.device.secureStore.*</code> sketch</td><td>The Tauri transport of <code>secureStorage</code></td></tr><tr><td>20.5</td><td>Native push (APNs/FCM)</td><td>A capability registry entry + Capacitor transport</td></tr><tr><td>20.6–20.8</td><td>Photo picker, biometrics, haptics</td><td>Registry entries + transports; haptics lands in v1 here</td></tr></tbody></table><p>RFC 0038&#39;s inline API sketches (<code>sdk.device.notify()</code>, <code>sdk.device.secureStore.set/get/delete</code>) are <strong>superseded</strong> by §6&#39;s shapes — same intent, typed results and negotiation added. Those epic tasks should be updated when picked up rather than implemented as written.</p><h3 id="docker-config-impact" tabindex="-1">Docker / config impact <a class="header-anchor" href="#docker-config-impact" aria-label="Permalink to &quot;Docker / config impact&quot;">​</a></h3><p>A new published workspace package: <code>tsup</code> build, <code>turbo.json</code> pipeline entry, <code>transpilePackages</code> in both <code>next.config.ts</code> files, and catalog-pinned dev deps per the pnpm <code>catalog:</code> convention. No new env var, port, on-disk path, or native dependency in this repository. The shells&#39; native permission declarations live in their own repositories.</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><p><strong>Capability present:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin mounts → handshake resolves → supports(&#39;haptics.impact&#39;, 1) === true</span></span>
<span class="line"><span>              → user taps → haptics.impact(&#39;light&#39;) → { status: &#39;ok&#39; }</span></span></code></pre></div><p><strong>Capability absent (plain browser, or older shell):</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin mounts → supports(...) === false → affordance not rendered at all</span></span>
<span class="line"><span>              → (if called anyway) → { status: &#39;unavailable&#39;, capability: … }</span></span></code></pre></div><p><strong>Consent, first use:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin calls notifications.native.show(...)</span></span>
<span class="line"><span>  → manifest declares device:notifications ✓</span></span>
<span class="line"><span>  → no stored grant → platform prompt: &quot;Tally wants to send notifications&quot;</span></span>
<span class="line"><span>       ├─ Allow → OS prompt → granted → { status: &#39;ok&#39; }</span></span>
<span class="line"><span>       ├─ Not now → { status: &#39;dismissed&#39; }   (may ask again)</span></span>
<span class="line"><span>       └─ Never  → grant stored as denied → { status: &#39;denied&#39; } without OS prompt</span></span></code></pre></div><blockquote><p><strong>v1 simplification (leg 2, by explicit developer decision, not a silent cut):</strong> the &quot;platform prompt&quot; step above does not exist in v1 — building a global overlay primitive any plugin could trigger was comparable in size to leg 1 on its own, for a case (&quot;Do not proceed if&quot; above) that a much smaller change already satisfies. <code>requestPermission(pluginId)</code> records the consent grant and goes straight to the OS/browser prompt; the calling plugin&#39;s own UI (an &quot;Enable notifications&quot; button, shown while the user is already inside that plugin) is what supplies the naming context instead — the standard web pattern. Revisit a platform-rendered prompt only if this proves insufficient in practice.</p></blockquote><p><strong>Undeclared permission:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin calls haptics.impact() without device:haptics in its manifest</span></span>
<span class="line"><span>  → native transport: refused → { status: &#39;unavailable&#39; }</span></span>
<span class="line"><span>  → web transport: advisory only — documented as not enforceable</span></span></code></pre></div><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Extend <code>SdkHost</code> with a <code>device</code> namespace.</strong> Rejected on a hard architectural fact: <code>SdkHost</code> is server-side, registered via a Node-process global, and no server round-trip can reach a camera or a haptic motor. The browser-only-subpath pattern (<code>@sovereignfs/sdk/offline</code>) is the only shape that works.</li><li><strong>Let plugins call Capacitor/Tauri APIs directly.</strong> Rejected — already forbidden by RFC 0058, and it would couple every plugin to one shell, destroying the portability that makes shipping a plugin once possible.</li><li><strong>Version-based compatibility</strong> (<code>minShellVersion</code>, mirroring RFC 0024). Rejected: it assumes one party controls both sides&#39; release cadence. Users control shell updates and operators control instance updates, so version gates would strand working combinations and require coordinated releases across three repositories. Capability negotiation removes the need entirely.</li><li><strong>Duplicate protocol types in each shell repository, spec-only here.</strong> Rejected — this is exactly the divergence RFC 0082 rejected separate shell repositories over, and protocol drift between two shells is far more expensive to discover than a shared dependency is to maintain.</li><li><strong>Throwing on unavailable/denied.</strong> Rejected — collapses four distinct product outcomes into one, and forces every caller into <code>try/catch</code> for the entirely expected case of &quot;this is a browser&quot;.</li><li><strong>A single broad <code>device:*</code> permission.</strong> Rejected — a plugin that wants haptics should not thereby be declaring camera access; the whole value of install-time metadata is its specificity.</li><li><strong>Plugin-facing <code>secureStorage</code> in v1.</strong> Rejected for now — unsafe while client-side plugin identity is self-declared (§5). Revisit only alongside a verifiable client identity mechanism.</li><li><strong><code>packages/sdk</code> taking <code>@sovereignfs/bridge</code> as a runtime dependency.</strong> The first design considered, and rejected on the facts (§1): the SDK has no <code>dependencies</code> field at all today and neither does <code>@sovereignfs/ui</code>, so &quot;zero-deps&quot; is a literal property of both published packages rather than a rule about two specific imports. A dependency would also expose the dual-major duplication risk and give plugin developers a transitive install for a package they should never need to know about. The <code>provideBridge()</code> handoff — the same pattern <code>provideHost()</code> already uses for the same problem — achieves one source of truth at no cost.</li><li><strong>Plugin developers importing <code>@sovereignfs/bridge</code> directly</strong> instead of <code>sdk.device.*</code>. Rejected — <code>sdk.device.*</code> is the plugin-facing contract named in SRS §3.12, RFC 0058, RFC 0038, and CLAUDE.md, and &quot;bridge&quot; is an implementation concept (transports, handshakes) that plugin authors should never have to reason about.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><s>Does the per-user consent grant belong in the existing data-consent tables (RFC 0002&#39;s model, surfaced in Account → Data) or its own store?</s><strong>Resolved in workstream 0003 leg 2, as recommended</strong>: its own store — a new <code>device_consent_grants</code> table, compound-keyed <code>(user_id, plugin_id, capability)</code> and hard-deleted on revoke, mirroring <code>user_capability_grants</code>&#39;s shape rather than <code>consent_grants</code>&#39;s (which is keyed by consumer/provider/contract/version — the wrong shape for &quot;a capability granted to a plugin&quot;).</li><li><s>Should <code>protocolVersion</code> mismatch be fatal or degrade to <code>web</code>?</s><strong>Resolved in workstream 0003 leg 1, as recommended</strong>: degrade to the <code>web</code> transport with a recorded <code>console.warn</code>, never fatal — an old shell can never hard-break an instance. Implemented in <code>@sovereignfs/bridge</code>&#39;s <code>handshake()</code>/<code>invoke()</code>.</li><li>Where does the &quot;shell too old for this capability&quot; message surface to the <em>user</em> — silently absent affordance, or an explicit &quot;update your app&quot; hint? Leaning silently absent for v1, since a hint the user cannot act on (auto-update disabled, review queue) is worse than no hint.</li><li><s>Does the <code>web</code> transport belong in <code>@sovereignfs/bridge</code> or in <code>@sovereignfs/sdk/device-client</code>?</s> <strong>Resolved by §1&#39;s split:</strong> all three transports live in <code>@sovereignfs/bridge</code>, since the SDK subpath now holds only the contract and must stay dependency-free and implementation-free.</li><li>Whether a verifiable client-side plugin identity is achievable at all on a shared origin, and if so at what cost. It gates plugin-facing <code>secureStorage</code> and any future capability where cross-plugin isolation matters.</li><li>Can a Capacitor shell withhold <code>window.Capacitor</code> from page scripts while still using Capacitor plugins itself (§5)? If not, native permission enforcement is advisory on mobile and real only on desktop, and the docs must say so. Must be answered in workstream 0003 leg 4, before the enforcement claim is relied on.</li><li><s>Where does the client bootstrap that calls <code>provideBridge()</code> live in the runtime, and how is it guaranteed to run before a plugin&#39;s first <code>supports()</code> call?</s> <strong>Resolved in workstream 0003 leg 1</strong>: a module-level call to <code>installWebBridge()</code> at the top of <code>ClientShell.tsx</code>, the platform layout&#39;s single client-side entry point rendered around every authenticated route — runs once as soon as that chunk is evaluated, the closest client-side analogue to <code>instrumentation.ts</code>&#39;s server-side guarantee.</li><li>Found during leg 1, not anticipated by this RFC: <code>BridgeHandshake.shell.platform</code> (§3) had no value for &quot;no native shell is present&quot; — the web transport&#39;s own handshake must still return a real <code>shell</code> object. Resolved by extending the union with <code>&#39;web&#39;</code> rather than inventing a misleading OS guess.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Epic task 3.34</strong> — the contract in <code>@sovereignfs/sdk/device-client</code> (types, registry, <code>DeviceResult</code>, <code>provideBridge()</code>, <code>Symbol.for</code> storage) plus <code>packages/bridge</code>&#39;s protocol, <code>web</code> transport, and shell-side helper. Published as <code>@sovereignfs/bridge</code> 0.1.0; <code>@sovereignfs/sdk</code> minor bump for the new subpath. No plugin-facing capability calls yet.</li><li><strong>Epic task 3.35</strong> — <code>device:*</code> permissions in the manifest, per-user consent grants plus the Account UI, and <code>@sovereignfs/sdk/device-client</code>&#39;s plugin-facing <code>haptics</code>/<code>nativeNotifications</code>/<code>supports</code> surface with the web tier working end to end. <code>@sovereignfs/sdk</code> and <code>@sovereignfs/manifest</code> minor bumps.</li><li>External: <code>sovereign-desktop</code> implements the <code>tauri</code> transport (reworking tasks 17.2&#39;s notify half and 17.4 to this contract).</li><li>External: <code>sovereign-mobile</code> implements the <code>capacitor</code> transport (task 20.3, rescoped from &quot;bridge adapter&quot; to &quot;the Capacitor transport&quot;).</li><li>Later capabilities land one at a time — registry entry, permission, both transports, docs — each with its own privacy and store-review note. Tasks 20.5–20.8 and 17.2&#39;s tray half follow this path.</li></ol><p>Sequenced across epics and repositories as <a href="./../workstreams/0003-device-bridge-across-surfaces.html">workstream 0003</a>.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.2</td><td>August 2026</td><td>Corrected a stale claim: §7 said <code>secureStorage</code> was platform-internal, not plugin-facing — RFC 0093 superseded that decision (it is now plugin-facing, gated by the <code>device:secureStorage</code> permission), and this section&#39;s prose and capability table were out of sync with the shipped SDK surface. Updated to record both the original reasoning and what changed it. Flagged a not-yet-live tension for whoever builds RFC 0082 §5&#39;s sequel: the wire contract has no per-item access-control policy, and RFC 0093&#39;s uniform every-operation gating may not fit that consumer&#39;s &quot;silent recovery and biometric unlock&quot; framing.</td></tr><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr></tbody></table>`,95)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
