import{_ as t,o as s,c as a,a2 as o}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Warden","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/plugins/warden.md"},"headers":[],"relativePath":"plugins/warden.md","filePath":"plugins/warden.md"}'),n={name:"plugins/warden.md"};function i(d,e,r,l,c,h){return s(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="warden" tabindex="-1">Warden <a class="header-anchor" href="#warden" aria-label="Permalink to &quot;Warden&quot;">​</a></h1><p><strong>Version:</strong> 0.1<br><strong>Date:</strong> September 2026<br><strong>Author:</strong> kasunben<br><strong>Purpose:</strong> Canonical specification for the Sovereign Warden plugin — the single source of truth for its manifest, access model, functional requirements, data model, and build plan.<br><strong>Status:</strong> Fully implemented (RFC 0063, three revisions) — epic tasks 22.1–22.12, all ✅</p><hr><p>Warden is Sovereign&#39;s built-in personal AI chat assistant. Each user connects their own OpenAI-API-compatible model provider(s) — OpenRouter, a direct vendor, or a self-hosted server — with an API key, and chats against them in multiple named, pinnable sessions. If the operator also runs the optional local inference add-on (<code>apps/harness</code>), its model is folded into the same picker automatically, with no configuration and no special status beyond &quot;free, no key needed.&quot;</p><p><strong><code>type</code> is <code>sovereign</code>, not <code>platform</code>.</strong> Unlike Account, Console, and Launcher — the three chrome plugins that administer the platform&#39;s own core data and are exempt from plugin activation entirely — Warden is an ordinary installable app. It ships in this monorepo (built into the runtime image by default, unlike a <code>community</code> plugin an operator clones separately, or an <code>example-plugins/</code> entry gated behind <code>SOVEREIGN_EXAMPLES_ENABLED</code>), but on a fresh instance it starts with no <code>plugin_status</code> row and is therefore inactive until an admin activates it from Console&#39;s Plugins page (CON-07), the same as any other non-chrome plugin. It also gets its own fully isolated database store rather than sharing the platform schema — see <a href="#data-model">Data model</a>. The manifest&#39;s <code>repository</code> field (required for <code>sovereign</code>/<code>community</code> types) points back at this same monorepo path, since Warden has no separate repository of its own.</p><p>The plugin is reachable at <code>/warden</code> (<code>routePrefix</code>) through the ordinary Launcher grid and sidebar, not as shell chrome. Its manifest also currently carries <code>development: true</code> — an amber &quot;in development&quot; badge next to its name in Console&#39;s Plugins table and on its Launcher tile (CON-15), purely informational and with no effect on routing or access.</p><h2 id="contents" tabindex="-1">Contents <a class="header-anchor" href="#contents" aria-label="Permalink to &quot;Contents&quot;">​</a></h2><ul><li><a href="#identity-and-manifest">Identity and manifest</a></li><li><a href="#access-control">Access control</a></li><li><a href="#functional-requirements">Functional requirements</a></li><li><a href="#directory-structure">Directory structure</a></li><li><a href="#data-model">Data model</a></li><li><a href="#sdk-dependencies">SDK dependencies</a></li><li><a href="#ui">UI</a></li><li><a href="#build-plan">Build plan</a></li><li><a href="#open-questions">Open questions</a></li><li><a href="#changelog">Changelog</a></li></ul><hr><h2 id="identity-and-manifest" tabindex="-1">Identity and manifest <a class="header-anchor" href="#identity-and-manifest" aria-label="Permalink to &quot;Identity and manifest&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>fs.sovereign.warden</code></td></tr><tr><td><code>name</code></td><td><code>Warden</code></td></tr><tr><td><code>type</code></td><td><code>sovereign</code></td></tr><tr><td><code>runtime</code></td><td><code>native</code></td></tr><tr><td><code>routePrefix</code></td><td><code>/warden</code></td></tr><tr><td><code>shell</code></td><td><code>default</code></td></tr><tr><td><code>adminOnly</code></td><td>omitted (<code>false</code>)</td></tr><tr><td><code>icon</code></td><td><code>icon.svg</code></td></tr><tr><td><code>development</code></td><td><code>true</code> — &quot;in development&quot; badge only (CON-15); no effect on routing, access policy, or the enable/disable default.</td></tr><tr><td><code>disabled</code></td><td><code>false</code> — the build-time hard-disable flag; was <code>true</code> throughout phase 1 (see <a href="#build-plan">Build plan</a>).</td></tr><tr><td><code>repository</code></td><td><code>https://github.com/sovereignfs/sovereign/tree/main/plugins/warden</code></td></tr><tr><td><code>permissions</code></td><td><code>auth:session</code>, <code>db:readWrite</code>, <code>data:export</code>, <code>data:import</code></td></tr><tr><td><code>compatibility.minPlatformVersion</code></td><td><code>0.87.0</code></td></tr></tbody></table><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;fs.sovereign.warden&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Warden&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;development&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;disabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.11.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;A personal AI chat assistant, powered by a model provider you choose.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sovereign&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;repository&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://github.com/sovereignfs/sovereign/tree/main/plugins/warden&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;runtime&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;native&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;routePrefix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/warden&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;shell&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;icon&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;icon.svg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auth:session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;db:readWrite&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;data:export&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;data:import&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;compatibility&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;minPlatformVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.87.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Notes:</p><ul><li>No <code>sdk.connections</code>/<code>sdk.secrets</code> permission is declared because none is required — <code>user</code>-scoped secrets and connections need no manifest permission at all (RFC 0043), unlike <code>instance</code>-scoped ones.</li><li>No <code>storage:readWrite</code> permission either: attachments are processed in-memory for the current request only and are never written to <code>sdk.storage</code> or any table (see <a href="#functional-requirements">Functional requirements</a>).</li><li><strong><code>data:import</code> is declared but currently unearned.</strong> Warden&#39;s own <code>_lib/portability.ts</code> registers <code>provideExport</code>/<code>provideDelete</code> only — no <code>sdk.portability.provideImport()</code> handler exists anywhere in the plugin (confirmed by grep). <code>docs/plugin-development.md</code>&#39;s portability section says to &quot;declare <code>data:export</code>/<code>data:import</code> only once you&#39;ve actually registered the matching hook,&quot; and <code>runtime/src/portability/restore.ts</code> bears this out at runtime: an import bundle containing a <code>fs.sovereign.warden</code> section is accepted (the plugin is &quot;eligible&quot; per the declared permission) but always resolves to <code>status: &#39;skipped&#39;, warning: &#39;no import handler registered for this plugin&#39;</code> — safe (nothing is silently dropped or corrupted), but the permission grants no actual capability today. See <a href="#sdk-dependencies">SDK dependencies</a>.</li><li><code>type</code> changed from <code>platform</code> to <code>sovereign</code> mid-implementation (task 22.5), once Warden&#39;s first real tables were added — a <code>platform</code> plugin routes <code>sdk.db</code> to the shared, unisolated schema, which is wrong for a plugin that owns genuine data of its own. The <code>repository</code> field became required at the same time as a consequence of that type change.</li><li><code>shell: &quot;default&quot;</code> — a full page under the platform sidebar, not a dialog.</li></ul><hr><h2 id="access-control" tabindex="-1">Access control <a class="header-anchor" href="#access-control" aria-label="Permalink to &quot;Access control&quot;">​</a></h2><p>Warden has no <code>adminOnly</code> gate. Every read and write is scoped strictly to the calling user: providers, sessions, messages, model-visibility choices, and the default-model setting are all looked up by <code>userId</code>/<code>tenantId</code>, and every function that takes a caller-supplied id — <code>_lib/sessions.ts</code>&#39;s <code>getOwnSession()</code>, <code>_lib/providers.ts</code>&#39;s <code>getOwnProvider()</code> — re-verifies ownership in code after the read rather than trusting a query filter alone. A guessed or leaked session/connection id belonging to another user returns &quot;not found,&quot; never that user&#39;s data.</p><p>Two access layers apply above the plugin&#39;s own per-user scoping, both standard for any non-chrome plugin (<code>docs/plugins/console.md</code>&#39;s CON-07/CON-13):</p><ul><li><strong>Activation.</strong> A fresh instance creates no <code>plugin_status</code> row for Warden, so it is inactive by default until an admin activates it from Console&#39;s Plugins page.</li><li><strong>Access policy.</strong> Once active, Console&#39;s per-plugin access policy (Everyone, Admins and owners, Selected users, Selected groups, or Disabled) governs who may open <code>/warden</code> at all, independent of the plugin&#39;s own enabled/disabled state.</li></ul><p>Provider API keys never live in a Warden-owned column. They&#39;re stored via <code>sdk.secrets</code> (<code>scope: &#39;user&#39;</code>, RFC 0043), referenced from an <code>sdk.connections</code> row (RFC 0049) that Warden&#39;s <code>_lib/providers.ts</code> manages — deleting a provider deletes its linked secret in the same action. A key is never returned from a server action to the client or rendered into the DOM; only a provider&#39;s label, base URL, and live health status are.</p><p>A user-supplied provider base URL is validated by <code>_lib/url-safety.ts</code> (loopback, link-local, cloud-metadata, and known internal Compose service names are rejected) both when the provider is saved and again immediately before every request; the exact IP address that validation resolves is then pinned for the outbound connection itself (<code>_lib/pinned-fetch.ts</code>, an undici <code>Agent</code> with a caller-supplied <code>lookup</code>) so a second, independent DNS lookup at connection time can&#39;t answer differently and reintroduce the rebind the validation step exists to close.</p><hr><h2 id="functional-requirements" tabindex="-1">Functional requirements <a class="header-anchor" href="#functional-requirements" aria-label="Permalink to &quot;Functional requirements&quot;">​</a></h2><p>Requirements below reflect Warden as shipped across RFC 0063&#39;s three revisions — there is no separate future milestone the way Account&#39;s v0.2 or Console&#39;s phased CON-* additions are; see <a href="#build-plan">Build plan</a> for how the requirements arrived in three phases.</p><h3 id="core-rfc-0063" tabindex="-1">Core (RFC 0063) <a class="header-anchor" href="#core-rfc-0063" aria-label="Permalink to &quot;Core (RFC 0063)&quot;">​</a></h3><h4 id="providers" tabindex="-1">Providers <a class="header-anchor" href="#providers" aria-label="Permalink to &quot;Providers&quot;">​</a></h4><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>WRD-01</td><td>Configure one or more OpenAI-API-compatible model providers (label, base URL, API key) via a first-run setup flow or Settings → Providers; edit or remove any provider later. The API key is never re-displayed once saved.</td></tr><tr><td>WRD-02</td><td>Each provider&#39;s reachability is checked live (unreachable, auth-rejected, or OK) and shown per-provider without failing the rest of the list; a &quot;Recheck providers&quot; action forces a fresh check instead of the short-lived discovery cache.</td></tr><tr><td>WRD-03</td><td>A provider&#39;s base URL is validated against an SSRF guard at save time and again before every request, with the resolved address pinned for the connection itself to close a DNS-rebind race between validation and use.</td></tr></tbody></table><h4 id="models" tabindex="-1">Models <a class="header-anchor" href="#models" aria-label="Permalink to &quot;Models&quot;">​</a></h4><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>WRD-04</td><td>Discover and merge every configured provider&#39;s live model catalog with the local <code>apps/harness</code> engine&#39;s model (when reachable and ready) into one selectable list; refreshed on a short server-side cache and by explicit &quot;Recheck providers&quot;/&quot;Recheck models&quot; actions that bypass it.</td></tr><tr><td>WRD-05</td><td>Curate per-model visibility from Settings → Models: a provider-sourced model is hidden from the picker by default (a single catalog can run into the hundreds), the local model is shown by default, and either can be flipped per user.</td></tr><tr><td>WRD-06</td><td>Set a default model for brand-new sessions from Settings → General; existing sessions keep whichever model they were already using.</td></tr></tbody></table><h4 id="sessions-and-sidebar" tabindex="-1">Sessions and sidebar <a class="header-anchor" href="#sessions-and-sidebar" aria-label="Permalink to &quot;Sessions and sidebar&quot;">​</a></h4><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>WRD-07</td><td>Hold multiple independent, named chat sessions per user instead of one persisted thread. A session row is created lazily, only on its first sent message — never eagerly when &quot;+ New&quot; is clicked.</td></tr><tr><td>WRD-08</td><td>List sessions in a collapsible sidebar (collapsed by default), grouped into Pinned (up to 5, most-recently-pinned first) above Recent (the 10 most-recently-active); collapse state persists per browser.</td></tr><tr><td>WRD-09</td><td>Rename, pin/unpin, or delete a session from its row&#39;s overflow menu. Pinning a 6th session is rejected with a message to unpin one first rather than silently evicting the oldest pin; delete has no &quot;recently deleted&quot; recovery.</td></tr><tr><td>WRD-10</td><td>A new session&#39;s title defaults to a synchronous truncation of its first user message (no model call involved) and can be renamed manually at any time.</td></tr></tbody></table><h4 id="chat" tabindex="-1">Chat <a class="header-anchor" href="#chat" aria-label="Permalink to &quot;Chat&quot;">​</a></h4><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>WRD-11</td><td>Send a message to the session&#39;s selected model and stream the reply incrementally, with controls to stop an in-progress reply and to copy any reply to the clipboard. Both sides of the exchange persist to that session in the background once the reply completes, without blocking the visible stream.</td></tr><tr><td>WRD-12</td><td>Attach one image per message (sent to the model for that turn only and never persisted — a text placeholder stands in for it in history) or one text/Markdown/PDF document (extracted to plain text server-side, truncated past a size ceiling, and folded into the message as ordinary persisted text).</td></tr><tr><td>WRD-13</td><td>Toggle Incognito from the composer toolbar: a separate, never-written-to-storage scratch conversation, orthogonal to whichever session is selected underneath. Turning it off, or navigating away, discards it permanently with no recovery path; it cannot be combined with an attachment.</td></tr><tr><td>WRD-14</td><td>Pick a model from a provider-grouped popover in the composer (local model first, then each connected provider&#39;s own group), with a footer linking directly into Settings → Providers and Settings → Models.</td></tr><tr><td>WRD-15</td><td>Server-enforced limits apply regardless of what the client sends: a maximum input length, a maximum output length, and a bounded number of recent turns replayed to the model as context on every request.</td></tr></tbody></table><h4 id="settings-and-portability" tabindex="-1">Settings and portability <a class="header-anchor" href="#settings-and-portability" aria-label="Permalink to &quot;Settings and portability&quot;">​</a></h4><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>WRD-16</td><td>From Settings → General, delete every unpinned session inactive for longer than a user-chosen number of days, on demand; pinned sessions are never touched regardless of age. Not a scheduled job — Warden declares no <code>sdk.schedules</code> capability.</td></tr><tr><td>WRD-17</td><td>Provide an entry point to the account-wide data export from Settings → General (&quot;Export my data&quot;). Warden participates in that export and in account deletion for all of its own data — chat history, per-model visibility overrides, and the default-model setting — via <code>sdk.portability</code>.</td></tr></tbody></table><hr><h2 id="directory-structure" tabindex="-1">Directory structure <a class="header-anchor" href="#directory-structure" aria-label="Permalink to &quot;Directory structure&quot;">​</a></h2><p>Warden lives in the monorepo under <code>plugins/warden/</code>, with its own isolated database store (migrations under <code>migrations/sqlite/</code> and <code>migrations/postgres/</code> — see <a href="#data-model">Data model</a>).</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/warden/</span></span>
<span class="line"><span>├── manifest.json</span></span>
<span class="line"><span>├── icon.svg</span></span>
<span class="line"><span>├── package.json</span></span>
<span class="line"><span>├── drizzle.config.ts / drizzle.config.pg.ts</span></span>
<span class="line"><span>├── migrations/</span></span>
<span class="line"><span>│   ├── sqlite/                       # warden_sessions/messages, visibility overrides, user settings</span></span>
<span class="line"><span>│   └── postgres/                     # mirrored Postgres migrations</span></span>
<span class="line"><span>└── app/</span></span>
<span class="line"><span>    ├── layout.tsx                    # best-effort sdk.portability registration</span></span>
<span class="line"><span>    ├── error.tsx</span></span>
<span class="line"><span>    ├── actions.ts                    # server actions: providers, model visibility, sessions, settings</span></span>
<span class="line"><span>    ├── warden.module.css</span></span>
<span class="line"><span>    ├── api/</span></span>
<span class="line"><span>    │   └── chat/</span></span>
<span class="line"><span>    │       └── route.ts              # streaming chat completion Route Handler (RFC 0063 §5)</span></span>
<span class="line"><span>    ├── (chat)/                       # route group sharing one sidebar/shell across its children</span></span>
<span class="line"><span>    │   ├── layout.tsx                #   resolves sessions + sidebar data once for /warden and /warden/new</span></span>
<span class="line"><span>    │   ├── loading.tsx</span></span>
<span class="line"><span>    │   ├── page.tsx                  #   /warden — continues the most recently active session</span></span>
<span class="line"><span>    │   ├── new/page.tsx              #   /warden/new — always a blank composer</span></span>
<span class="line"><span>    │   ├── providers/page.tsx        #   /warden/providers — rendered inside the same shell</span></span>
<span class="line"><span>    │   └── models/page.tsx           #   /warden/models — rendered inside the same shell</span></span>
<span class="line"><span>    ├── _components/                  # WardenChatPage, ChatView, ModelPickerPopover, WardenSidebar,</span></span>
<span class="line"><span>    │                                 # WardenLayoutShell, WardenSettingsDialog, GeneralSettings,</span></span>
<span class="line"><span>    │                                 # ProvidersView/ProviderRow/AddProviderForm, ModelsView/ModelToggleRow,</span></span>
<span class="line"><span>    │                                 # SetupPrompt</span></span>
<span class="line"><span>    ├── _lib/                         # sessions, providers, model-discovery, model-visibility(-policy),</span></span>
<span class="line"><span>    │                                 # user-settings, harness-client, provider-chat, pinned-fetch,</span></span>
<span class="line"><span>    │                                 # url-safety, attachments, limits, active-session, stream-capture,</span></span>
<span class="line"><span>    │                                 # portability</span></span>
<span class="line"><span>    └── _db/</span></span>
<span class="line"><span>        └── schema.ts / schema.postgres.ts</span></span></code></pre></div><p><code>app/api/chat/route.ts</code> is a plugin-owned Next.js Route Handler rather than a server action — the one way to stream a completion incrementally to the browser, and (per its own doc comment) the first precedent in this repo for a plugin using a Route Handler instead of server actions exclusively.</p><p><code>/warden/providers</code> and <code>/warden/models</code> were briefly consolidated into a single <code>/warden/settings</code> (tabs) route during RFC 0063&#39;s third revision, then split back into standalone routes once the persistent sidebar existed — a full-page settings surface meant the sidebar disappeared out from under the user. What remains of that consolidation is the small General tab (default model, retention, export link), now a <code>Dialog</code> (<code>WardenSettingsDialog</code>) opened from the sidebar footer rather than a route. See <a href="#build-plan">Build plan</a>.</p><hr><h2 id="data-model" tabindex="-1">Data model <a class="header-anchor" href="#data-model" aria-label="Permalink to &quot;Data model&quot;">​</a></h2><p>Warden&#39;s four tables live in its own <strong>isolated</strong> database store — a dedicated sqld namespace or Postgres schema (<code>plugin_fs_sovereign_warden</code>, per <code>docs/plugin-database.md</code>&#39;s <code>plugin_&lt;id-with-underscores&gt;</code> naming rule), never the shared platform schema, since <code>type: sovereign</code> plugins are always isolated (the one exception, <code>type: platform</code>, applies only to Account, Console, and Launcher). Every table is tenant- and user-scoped.</p><h3 id="warden-sessions" tabindex="-1"><code>warden_sessions</code> <a class="header-anchor" href="#warden-sessions" aria-label="Permalink to &quot;\`warden_sessions\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text</td><td>PK.</td></tr><tr><td><code>tenant_id</code></td><td>string</td><td></td></tr><tr><td><code>user_id</code></td><td>string</td><td></td></tr><tr><td><code>title</code></td><td>text</td><td>Nullable until the first user message sets it (<code>deriveTitle()</code>), or the user renames it.</td></tr><tr><td><code>pinned_at</code></td><td>integer</td><td>Nullable. Non-null = pinned; sorts the pinned group (most-recently-pinned first), max 5.</td></tr><tr><td><code>last_active_at</code></td><td>integer</td><td>Bumped only when a message is actually sent in this session — the sidebar&#39;s &quot;recent&quot; sort key.</td></tr><tr><td><code>created_at</code></td><td>integer</td><td></td></tr></tbody></table><h3 id="warden-messages" tabindex="-1"><code>warden_messages</code> <a class="header-anchor" href="#warden-messages" aria-label="Permalink to &quot;\`warden_messages\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text</td><td>PK.</td></tr><tr><td><code>session_id</code></td><td>text</td><td>References <code>warden_sessions.id</code>.</td></tr><tr><td><code>role</code></td><td>text</td><td><code>&#39;user&#39;</code> | <code>&#39;assistant&#39;</code>.</td></tr><tr><td><code>content</code></td><td>text</td><td>Plain text — never encrypted; see the schema&#39;s own doc comment on why (not classified as sensitive, same posture as any other plugin&#39;s free-text content).</td></tr><tr><td><code>provider_id</code></td><td>text</td><td>The <code>sdk.connections</code> id that answered, or null for the local <code>apps/harness</code> model. Not a DB foreign key — connections live in the platform&#39;s own <code>plugin_connections</code> table.</td></tr><tr><td><code>model</code></td><td>text</td><td></td></tr><tr><td><code>created_at</code></td><td>integer</td><td></td></tr></tbody></table><h3 id="warden-model-visibility-overrides" tabindex="-1"><code>warden_model_visibility_overrides</code> <a class="header-anchor" href="#warden-model-visibility-overrides" aria-label="Permalink to &quot;\`warden_model_visibility_overrides\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text</td><td>PK.</td></tr><tr><td><code>tenant_id</code></td><td>string</td><td></td></tr><tr><td><code>user_id</code></td><td>string</td><td></td></tr><tr><td><code>model_key</code></td><td>text</td><td><code>&#39;local&#39;</code>, or <code>\${providerId}:\${modelId}</code>. A row means &quot;flip this key away from its own computed default&quot; — exceptions only.</td></tr><tr><td><code>created_at</code></td><td>integer</td><td></td></tr></tbody></table><h3 id="warden-user-settings" tabindex="-1"><code>warden_user_settings</code> <a class="header-anchor" href="#warden-user-settings" aria-label="Permalink to &quot;\`warden_user_settings\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text</td><td>PK.</td></tr><tr><td><code>tenant_id</code></td><td>string</td><td></td></tr><tr><td><code>user_id</code></td><td>string</td><td>One row per user, get-or-create.</td></tr><tr><td><code>default_model_key</code></td><td>text</td><td>Nullable — no row/null means &quot;fall back to the first visible model.&quot;</td></tr><tr><td><code>created_at</code></td><td>integer</td><td></td></tr></tbody></table><p>Provider configuration itself is <strong>not</strong> a Warden-owned table — a provider is an <code>sdk.connections</code> row (<code>provider: &#39;openai-compatible&#39;</code>, <code>scope: &#39;user&#39;</code>, RFC 0049) with its API key held behind an <code>sdk.secrets</code> reference (RFC 0043). Both live in the platform&#39;s own <code>plugin_connections</code>/<code>plugin_secrets</code> tables, shared by every plugin that uses those SDK surfaces — not in <code>plugin_fs_sovereign_warden</code>.</p><hr><h2 id="sdk-dependencies" tabindex="-1">SDK dependencies <a class="header-anchor" href="#sdk-dependencies" aria-label="Permalink to &quot;SDK dependencies&quot;">​</a></h2><table tabindex="0"><thead><tr><th>SDK surface</th><th>Used for</th><th>Available from</th></tr></thead><tbody><tr><td><code>sdk.auth</code></td><td>Current user session (<code>requireSession()</code> in every page and action)</td><td>Task 0.4.2</td></tr><tr><td><code>sdk.db</code></td><td>Read/write Warden&#39;s own isolated tables (above)</td><td>Task 0.5.5</td></tr><tr><td><code>sdk.connections</code></td><td>Per-user provider registry — label, base URL, live health status (RFC 0049)</td><td>Task 22.4</td></tr><tr><td><code>sdk.secrets</code></td><td>Provider API key storage, <code>scope: &#39;user&#39;</code> (RFC 0043)</td><td>Task 22.4</td></tr><tr><td><code>sdk.portability</code></td><td><code>provideExport</code>/<code>provideDelete</code> for chat history and preferences (RFC 0007)</td><td>Task 22.5</td></tr></tbody></table><p><code>sdk.connections</code> and <code>sdk.secrets</code> need no manifest permission for <code>user</code>-scoped use, which is why neither appears in Warden&#39;s <code>permissions</code> array (see <a href="#identity-and-manifest">Identity and manifest</a>&#39;s notes). <code>sdk.storage</code> is deliberately never used — an attached image is sent to the model for one request and discarded; nothing about attachments is written to disk or a table. No <code>provideImport</code> handler is registered despite the manifest declaring <code>data:import</code> — see the note in <a href="#identity-and-manifest">Identity and manifest</a>.</p><hr><h2 id="ui" tabindex="-1">UI <a class="header-anchor" href="#ui" aria-label="Permalink to &quot;UI&quot;">​</a></h2><p>Warden consumes <code>@sovereignfs/ui</code> exclusively: <code>PageContainer</code>, <code>PageHeader</code>, <code>ThreeColumnLayout</code> (via <code>WardenLayoutShell</code>), <code>NavList</code>, <code>Dialog</code>, <code>ConfirmDialog</code>, <code>Popover</code>, <code>Menu</code>, <code>Button</code>, <code>Select</code>, <code>QuantityStepper</code>, <code>Textarea</code>, <code>Message</code>/<code>MessageScroller</code>, <code>Markdown</code>, <code>EmptyState</code>, <code>Spinner</code>, <code>Icon</code>, <code>Tooltip</code>, <code>useToast</code>, and <code>useCommitOnEnterOrBlur</code>.</p><p><strong>Layout.</strong> A collapsible two-column shell (<code>WardenLayoutShell</code>) wraps <code>/warden</code>, <code>/warden/new</code>, <code>/warden/providers</code>, and <code>/warden/models</code> — the sidebar (<code>WardenSidebar</code>) and its collapse state live in the shared <code>(chat)</code> route-group layout, so they persist across navigation between those routes instead of being torn down and rebuilt on every click. Collapsed is the default; the collapse toggle relocates with visibility (main column when collapsed, inside the sidebar itself when expanded) so collapsing it never also hides the only way to bring it back. A third sidebar column is reserved in the layout for a future, undesigned phase (RFC 0063 §10) but is not built or shown today.</p><p><strong>Composer.</strong> Centered in the main column for a session with no messages yet; docks to the bottom the instant the first message is sent — the same tree in both states, only a modifier class changes, so nothing in the composer (including the incognito toggle) ever remounts across that transition. The model picker is a <code>Popover</code> grouped by provider; incognito is an icon toggle in the composer toolbar (not the old chat header).</p><p><strong>Settings.</strong> General (default model, retention, export link) is a <code>Dialog</code> opened from the sidebar footer. Providers and Models are full routes rendered inside the same shell, not tabs on a separate page — see <a href="#directory-structure">Directory structure</a> for why.</p><p><strong>Rendering.</strong> Assistant replies render through <code>@sovereignfs/ui</code>&#39;s <code>Markdown</code> component (extended with ordered lists and fenced code blocks for LLM output), not as raw text or via <code>dangerouslySetInnerHTML</code>.</p><p>Two curated icons were added to <code>@sovereignfs/ui</code>&#39;s <code>Icon</code> set specifically for this plugin&#39;s sidebar: <code>panel-left</code> (collapse toggle) and <code>pin</code>.</p><p>For the optional local inference add-on this UI can fold in automatically (<code>apps/harness</code> — health checks, model download, environment variables), see <a href="./../self-hosting.html#wardens-harness-engine-rfc-0063-workstream-0014-legs-2-3">docs/self-hosting.md</a>, which stays the operational reference for that one piece; this document covers the plugin as a whole.</p><hr><h2 id="build-plan" tabindex="-1">Build plan <a class="header-anchor" href="#build-plan" aria-label="Permalink to &quot;Build plan&quot;">​</a></h2><p>Fully implemented, shipped across three RFC 0063 revisions as epic tasks 22.1–22.12 (all ✅). Summarized here as shipped history rather than a forward-looking task list — see <code>docs/epics/core-assistant.md</code> for full task-by-task deliverables and verification, and RFC 0063 for the design rationale behind each revision.</p><h3 id="phase-1-—-local-engine-only-chat-tasks-22-1–22-3" tabindex="-1">Phase 1 — local-engine-only chat (tasks 22.1–22.3) <a class="header-anchor" href="#phase-1-—-local-engine-only-chat-tasks-22-1–22-3" aria-label="Permalink to &quot;Phase 1 — local-engine-only chat (tasks 22.1–22.3)&quot;">​</a></h3><p>Benchmarked and stood up <code>apps/harness</code> (a first-party llama.cpp wrapper, Research 0015) and shipped Warden as a plugin with ephemeral, unpersisted chat against it only, with zero tool execution. Shipped and verified end to end against a real local model, then deliberately hard-disabled (<code>disabled: true</code>) — Research 0015&#39;s own benchmark showed representative self-hosting hardware straining even on the smallest supported model.</p><h3 id="phase-2-—-bring-your-own-provider-chat-persistence-incognito-tasks-22-4–22-5" tabindex="-1">Phase 2 — bring-your-own provider chat, persistence, incognito (tasks 22.4–22.5) <a class="header-anchor" href="#phase-2-—-bring-your-own-provider-chat-persistence-incognito-tasks-22-4–22-5" aria-label="Permalink to &quot;Phase 2 — bring-your-own provider chat, persistence, incognito (tasks 22.4–22.5)&quot;">​</a></h3><p>Replaced the local engine as the only option with per-user OpenAI-API-compatible provider configuration, live per-provider model discovery merged with <code>apps/harness</code>&#39;s model as one optional zero-config entry, a single persisted conversation per user by default, and an Incognito toggle preserving the original ephemeral behavior as an opt-in. Verified against a real self-hosted mock provider; <code>disabled: true</code> removed.</p><h3 id="phase-3-—-multi-session-ui-sidebar-settings-composer-tasks-22-8–22-11" tabindex="-1">Phase 3 — multi-session UI: sidebar, settings, composer (tasks 22.8–22.11) <a class="header-anchor" href="#phase-3-—-multi-session-ui-sidebar-settings-composer-tasks-22-8–22-11" aria-label="Permalink to &quot;Phase 3 — multi-session UI: sidebar, settings, composer (tasks 22.8–22.11)&quot;">​</a></h3><p>Replaced the single persisted conversation with <code>warden_sessions</code> — multiple named, pinnable sessions — behind a collapsible sidebar (Pinned/Recent groups, rename/pin/delete), a consolidated Settings surface, and a Claude-style composer with a provider-grouped model-picker popover and incognito relocated into the toolbar. Verified live at each of the four legs (workstream 0021), confirming the sidebar&#39;s selected session and the composer&#39;s active session could never disagree and that a 6th pin attempt was rejected server-side.</p><h3 id="correctness-and-follow-through-pass-task-22-12" tabindex="-1">Correctness and follow-through pass (task 22.12) <a class="header-anchor" href="#correctness-and-follow-through-pass-task-22-12" aria-label="Permalink to &quot;Correctness and follow-through pass (task 22.12)&quot;">​</a></h3><p>Closed defects found reviewing the phase 3 legs together: session switching now actually remounts the conversation; the collapsible shell no longer discards in-flight streams or incognito content on toggle; an attachment staged before Incognito can no longer leak into persisted history; a stranded user turn is cleaned up if streaming fails mid-response; account deletion covers all four of Warden&#39;s tables (two preference tables were previously missed); and assistant replies render through <code>Markdown</code> instead of raw text. Also reversed part of task 22.9&#39;s settings consolidation: Providers and Models are standalone routes again, rendered inside the persistent sidebar shell rather than as tabs on a page that hid it.</p><h3 id="unrelated-remediation-tasks-22-6–22-7" tabindex="-1">Unrelated remediation (tasks 22.6–22.7) <a class="header-anchor" href="#unrelated-remediation-tasks-22-6–22-7" aria-label="Permalink to &quot;Unrelated remediation (tasks 22.6–22.7)&quot;">​</a></h3><p>Two audit-driven fixes, not part of any phase above: pinning the DNS-resolved address for outbound provider requests to close a rebind race in the SSRF guard (task 22.6), and collapsing the account-deletion handler from a per-session query loop to a fixed 4 queries via <code>inArray</code> (task 22.7).</p><hr><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><p>Carried forward from RFC 0063 — genuinely undesigned or unscheduled, not gaps in this document:</p><ol><li><strong>Tool selection and execution.</strong> RFC 0047&#39;s plugin tool contracts are the sanctioned mechanism, with Warden as the intended first flagship consumer — not started; depends on RFC 0047 shipping.</li><li><strong>A floating quick-access action button</strong>, reachable from any screen. Needs a new shell-chrome extension point that doesn&#39;t exist yet.</li><li><strong>Voice input/output.</strong> Not designed.</li><li><strong>The reserved third sidebar column&#39;s actual functionality.</strong> Space is kept in the layout; nothing is designed for it yet.</li><li><strong>Real memory beyond recency-based context truncation</strong> — whether Warden eventually needs summarization or a proper memory layer, the &quot;multi-agentic harness&quot; direction this plugin is ultimately meant to grow into.</li><li><strong>RFC 0040 (Sovereign Harness)&#39;s full revisit</strong> — whether Harness ends up being this foundation extended with orchestration/memory, or a separate later product built on top of it. This RFC only resolved RFC 0040&#39;s narrower &quot;user-supplied API keys&quot; question.</li><li><strong>Sidebar cutoff beyond 10 recent sessions.</strong> Currently a hard cutoff (older sessions unreachable from the sidebar); a &quot;load more&quot; or search affordance is unbuilt.</li><li><strong>A dedicated mobile shell.</strong> All of RFC 0063&#39;s third revision is scoped to desktop/web; componentization keeps the door open, but no mobile UI is designed or scheduled.</li><li><strong>A shared, admin-configured default provider</strong> for users who don&#39;t want to bring their own key — considered and rejected as out of scope for the current per-user design, not ruled out permanently.</li></ol><hr><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Sept 2026</td><td>Initial documentation — captures Warden as implemented through RFC 0063&#39;s third revision and epic tasks 22.1–22.12 (all ✅).</td></tr></tbody></table>`,86)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
