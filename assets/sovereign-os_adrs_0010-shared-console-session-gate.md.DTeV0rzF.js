import{_ as o,o as t,c as i,a2 as a}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"ADR-0010: Shared Console-Session Gate for Pi-hole and Future Service Panels","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/adrs/0010-shared-console-session-gate.md"},"headers":[],"relativePath":"sovereign-os/adrs/0010-shared-console-session-gate.md","filePath":"sovereign-os/adrs/0010-shared-console-session-gate.md"}'),n={name:"sovereign-os/adrs/0010-shared-console-session-gate.md"};function s(r,e,l,d,c,h){return t(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="adr-0010-shared-console-session-gate-for-pi-hole-and-future-service-panels" tabindex="-1">ADR-0010: Shared Console-Session Gate for Pi-hole and Future Service Panels <a class="header-anchor" href="#adr-0010-shared-console-session-gate-for-pi-hole-and-future-service-panels" aria-label="Permalink to &quot;ADR-0010: Shared Console-Session Gate for Pi-hole and Future Service Panels&quot;">​</a></h1><p><strong>Status:</strong> Accepted <strong>Date:</strong> 2026-08-06 <strong>Decision owner:</strong> Project creator (delegated to the assistant for this decision, same as ADR-0008/ADR-0009 — see Decision below) <strong>Related ADRs:</strong> <a href="./0007-console-authentication.html">ADR-0007</a>, <a href="./0008-console-privileged-action-invocation.html">ADR-0008</a><strong>Related RFCs:</strong> None <strong>Related milestone:</strong> None (raised ahead of the next service integration, not tied to a named ROADMAP item yet) <strong>Supersedes:</strong> None</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR-0007 gave Console its own session-based login, deliberately kept as a credential separate from Pi-hole&#39;s own admin password and the <code>sovereign</code> Linux account — three trust boundaries, on purpose, so that compromising one doesn&#39;t hand over the others. That decision is not being revisited here.</p><p>The question this ADR answers is different: as Sovereign adds more LAN-facing service panels beyond Pi-hole (the project owner&#39;s own phrasing: &quot;Pi-hole and other services we are going to add&quot;), should reaching each of those panels keep requiring its own separate sign-in, or can one Console sign-in also gate the others — something SSO-shaped, without actually building SSO?</p><p>Today, <code>/dns/</code> (Pi-hole&#39;s admin UI) is reverse-proxied straight through by Nginx with no gate of its own — reachable by anyone on the LAN who knows the URL, protected only by Pi-hole&#39;s own built-in login screen. As more services are added behind the same Nginx instance, repeating that pattern means each one grows its own exposed-until-you-hit-its-login surface, and repeating &quot;build a whole separate auth system per service&quot; is neither realistic nor consistent with how Console&#39;s own auth was scoped in ADR-0007.</p><h3 id="why-not-literal-sso" tabindex="-1">Why not literal SSO <a class="header-anchor" href="#why-not-literal-sso" aria-label="Permalink to &quot;Why not literal SSO&quot;">​</a></h3><p>A shared identity provider (OIDC/OAuth2, a self-hosted IdP like Keycloak or Authelia, or hand-rolling token issuance/validation across services) was considered and rejected for the same reason ADR-0007 already rejected it for Console alone: this is a single-household, single-admin appliance with no multi-tenant or multi-role concept, and standing up real identity-provider infrastructure — new services, new attack surface, new things to keep patched — is disproportionate to what one household actually needs, which is &quot;don&#39;t make me sign in three times for one visit,&quot; not &quot;federate identity across services.&quot;</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Gate other services&#39; Nginx locations behind Console&#39;s existing session cookie, at the proxy layer, using Nginx&#39;s <code>auth_request</code> module — one sign-in unlocks reachability to every gated panel, without merging credentials, issuing shared tokens, or building anything resembling a real identity provider.</strong></p><ul><li>A new, loopback-only, unauthenticated-body <code>GET /api/v1/auth/verify</code> endpoint on the existing <code>console-auth</code> service (port 8091): looks up the session cookie via the same <code>lookup_session()</code> already used by <code>_require_authenticated_session()</code>, and returns a bare <code>204</code> if valid, <code>401</code> otherwise. No JSON body, no CSRF check — <code>auth_request</code> subrequests carry the original request&#39;s cookies but not a way to attach a custom header the browser didn&#39;t already send, and CSRF protects state-changing POSTs, not read navigation to a panel that&#39;s about to enforce its own login anyway.</li><li>Nginx&#39;s <code>/dns/</code> location gains <code>auth_request /api/v1/auth/verify-internal;</code> pointed at an <code>internal</code>-only location that proxies to the new endpoint, the standard <code>auth_request</code> pattern (the subrequest URI must stay internal so it can&#39;t be hit directly from the LAN).</li><li><code>error_page 401 = @signin;</code> redirects an ungated visitor to <code>/console/?next=/dns/</code>, i.e., Console&#39;s own sign-in page, rather than Pi-hole&#39;s. Console&#39;s existing sign-in flow already knows how to reach a signed-in state; it just needs to honor <code>?next=</code> on success by redirecting there instead of staying on <code>/console/</code>.</li><li>Any future service panel this project adds behind Nginx picks up the same protection by adding the same two lines (<code>auth_request</code> + the shared <code>@signin</code> error page) to its own <code>location</code> block — no new service-specific auth code, no new credential store, no per-service session logic.</li><li>Pi-hole&#39;s own login screen is <strong>not removed or bypassed</strong>. A visitor who clears the gate still sees Pi-hole&#39;s own login prompt underneath, because Sovereign does not control Pi-hole&#39;s session internals and reimplementing or bypassing them is out of scope (and would mean holding a second, Sovereign-managed copy of Pi-hole&#39;s own auth state, which is exactly the kind of coupling ADR-0007 avoided by keeping the Pi-hole credential separate in the first place). This is a <em>reachability</em> gate, not a <em>session-sharing</em> one: it stops an unauthenticated LAN visitor from ever seeing Pi-hole&#39;s login form at all, but doesn&#39;t remove the second, lighter prompt once they&#39;re through. In practice, using the same password for both keeps that second prompt to one quick re-entry rather than a real second credential to remember.</li></ul><h2 id="what-this-actually-protects" tabindex="-1">What this actually protects <a class="header-anchor" href="#what-this-actually-protects" aria-label="Permalink to &quot;What this actually protects&quot;">​</a></h2><p>The same question ADR-0007 already asked for Console itself, now extended to everything proxied alongside it: not a secret, but who&#39;s allowed to <em>reach</em> a panel that can change household-wide state (Pi-hole&#39;s DNS filtering config, and whatever a future service exposes). Today, anyone on the LAN can already reach <code>/dns/</code> and start guessing Pi-hole&#39;s password with no rate limiting Sovereign controls itself (that&#39;s entirely up to Pi-hole&#39;s own login implementation). Gating the whole location behind Console&#39;s already-rate-limited, already-hardened session check closes that reachability gap for every current and future panel at once, in one place, instead of needing every new service to solve it individually.</p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><h3 id="option-a-—-real-sso-oidc-oauth2-via-a-self-hosted-identity-provider-rejected" tabindex="-1">Option A — Real SSO (OIDC/OAuth2 via a self-hosted identity provider) (rejected) <a class="header-anchor" href="#option-a-—-real-sso-oidc-oauth2-via-a-self-hosted-identity-provider-rejected" aria-label="Permalink to &quot;Option A — Real SSO (OIDC/OAuth2 via a self-hosted identity provider) (rejected)&quot;">​</a></h3><p>Stand up Keycloak, Authelia, or similar; every service authenticates against it.</p><ul><li><strong>Rejected</strong> for the same reason ADR-0007 rejected third-party identity providers for Console alone: disproportionate infrastructure, new attack surface, and a multi-tenant/role feature set this single-admin appliance has no use for. Also directly conflicts with this project&#39;s stated self-sufficiency-over-dependencies values (the same reasoning ADR-0006 applied to reject cloud KMS, and ADR-0007 applied to reject OAuth).</li></ul><h3 id="option-b-—-one-shared-credential-across-all-services-no-proxy-gate" tabindex="-1">Option B — One shared credential across all services, no proxy gate <a class="header-anchor" href="#option-b-—-one-shared-credential-across-all-services-no-proxy-gate" aria-label="Permalink to &quot;Option B — One shared credential across all services, no proxy gate&quot;">​</a></h3><p>Set every service&#39;s own password to match Console&#39;s, and stop there — no <code>auth_request</code>, just a documented convention that they&#39;re kept in sync.</p><ul><li>Zero new code.</li><li><strong>Rejected as insufficient on its own</strong>: it does nothing about reachability — Pi-hole&#39;s login form (and whatever a future service&#39;s is) stays fully exposed to anyone on the LAN, with no rate limiting or hardening Sovereign controls, and &quot;remember to keep two passwords in sync by hand&quot; is a real drift risk with no enforcement. Worth doing <em>in addition</em> to the proxy gate (see Decision&#39;s last bullet) as a low-friction way to shrink the second, still-visible login prompt, but not a substitute for actually gating reachability.</li></ul><h3 id="option-c-—-auth-request-proxy-gate-against-console-s-session-chosen" tabindex="-1">Option C — <code>auth_request</code> proxy gate against Console&#39;s session (chosen) <a class="header-anchor" href="#option-c-—-auth-request-proxy-gate-against-console-s-session-chosen" aria-label="Permalink to &quot;Option C — `auth_request` proxy gate against Console&#39;s session (chosen)&quot;">​</a></h3><p>As described in Decision.</p><ul><li>Reuses ADR-0007&#39;s already-hardware-qualified session mechanism (<code>console-auth</code>, rate limiting, session expiry) rather than building anything new for authentication itself — only a small new read-only verify endpoint and an Nginx directive per gated location.</li><li>Scales to future services by repeating a two-line Nginx pattern, not a new subsystem per service.</li><li><strong>Downside, accepted:</strong> not true SSO — a gated service still shows its own login underneath. Judged an acceptable, honestly-scoped tradeoff given Option A&#39;s disproportionate cost for what this appliance actually needs (see Context).</li></ul><h3 id="option-d-—-sovereign-managed-credential-injection-console-logs-into-pi-hole-on-the-user-s-behalf-and-forwards-its-session" tabindex="-1">Option D — Sovereign-managed credential injection (Console logs into Pi-hole on the user&#39;s behalf and forwards its session) <a class="header-anchor" href="#option-d-—-sovereign-managed-credential-injection-console-logs-into-pi-hole-on-the-user-s-behalf-and-forwards-its-session" aria-label="Permalink to &quot;Option D — Sovereign-managed credential injection (Console logs into Pi-hole on the user&#39;s behalf and forwards its session)&quot;">​</a></h3><p>Have <code>console-auth</code> (or a new privileged helper) hold Pi-hole&#39;s credential, perform Pi-hole&#39;s own login flow server-side, and forward or rewrite Pi-hole&#39;s resulting session cookie to the browser after a successful Console sign-in.</p><ul><li>Would produce true single-session UX (no second prompt at all).</li><li><strong>Rejected:</strong> requires Sovereign to hold and manage a second service&#39;s credential on the user&#39;s behalf, script against Pi-hole&#39;s own undocumented internal login flow (fragile across Pi-hole upgrades, the same kind of coupling the Pi-hole API assessment already flagged as a compatibility risk), and reintroduces exactly the shared-secret coupling ADR-0007 deliberately avoided by keeping Pi-hole&#39;s credential separate. Disproportionate complexity and fragility for saving one password re-entry.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>One sign-in gates reachability to every current and future LAN-facing service panel, closing an existing gap (Pi-hole&#39;s admin UI has no Sovereign-controlled reachability protection today) without waiting for each service to grow its own.</li><li>No new credential, session store, or identity system — reuses ADR-0007&#39;s already-accepted, already-hardware-qualified mechanism entirely.</li><li>Extends to a new service by adding a two-line Nginx pattern to its <code>location</code> block, not new per-service code.</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li>Not real SSO: a gated panel still shows its own login prompt once the proxy gate is passed. Households that want zero-friction single sign-on across every panel don&#39;t get that from this decision alone (Option B&#39;s shared-password convention narrows, but doesn&#39;t eliminate, the second prompt).</li><li>A new always-reachable-from-Nginx endpoint (<code>/api/v1/auth/verify</code>, internal-only but still new production surface) is called on every request to every gated location — needs to stay cheap (in-memory session lookup, no disk I/O on the hot path) so it doesn&#39;t become a latency or availability dependency for panels that would otherwise work fine on their own.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li>If <code>console-auth</code> (port 8091) is down, every gated panel becomes unreachable too, not just Console itself — a new availability coupling that doesn&#39;t exist today (Pi-hole currently works independently of Console&#39;s health). Needs an explicit decision, at implementation time, on Nginx&#39;s <code>error_page</code>/<code>proxy_intercept_errors</code> behavior if the verify subrequest itself fails to connect, rather than defaulting to whatever Nginx does out of the box.</li><li><code>?next=</code> redirect targets must be validated against an allowlist of known internal paths before Console honors them post-login, to avoid turning the sign-in flow into an open redirect.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><p>See Options above; no alternatives outside those four were seriously considered, since Option A (real SSO) and Option C (proxy session gate) bound the realistic range for this project&#39;s stated scale and values.</p><h2 id="validation-and-revisit-conditions" tabindex="-1">Validation and Revisit Conditions <a class="header-anchor" href="#validation-and-revisit-conditions" aria-label="Permalink to &quot;Validation and Revisit Conditions&quot;">​</a></h2><p><strong>Implemented and hardware-qualified (2026-08-06).</strong> The <code>/api/v1/auth/verify</code> endpoint, the <code>auth_request</code>/<code>error_page</code>/<code>@signin</code> Nginx wiring, and the <code>?next=</code> sign-in redirect (with its allowlist) are all built and verified end-to-end on the real Raspberry Pi 5 qualification device, through the real Nginx proxy, against the real <code>console-auth</code> service, from a real browser — see the <a href="./../research/shared-console-session-gate-hardware-qualification-report.html">qualification report</a> for the full method and evidence, including a real implementation bug this pass caught before it could reach a release: the original <code>nginx-full</code> package pin was wrong for this image&#39;s actual Debian 13 (trixie) base (no <code>nginx-full</code> package exists there; the plain <code>nginx</code> package already ships <code>auth_request</code>), corrected during this same qualification pass.</p><p>Confirmed: an unauthenticated LAN client reaching <code>/dns/</code> is bounced to Console&#39;s sign-in with the path preserved; after signing in, the same browser is bounced back and the gate lets the request through to Pi-hole, which then correctly applies its own separate, unaffected login. Not yet separately tested: <code>console-auth</code> being down (this ADR&#39;s own named risk) — the gate&#39;s fail-open/fail-closed behavior in that specific failure mode remains unverified.</p><p>Revisit this ADR if: a future service&#39;s integration needs genuinely different reachability rules than &quot;same gate as everything else&quot; (e.g. a service that must stay reachable even when signed out), or if the second-prompt friction from Option C&#39;s honest limitation turns out to matter enough in practice to justify Option D&#39;s added complexity.</p>',39)])])}const g=o(n,[["render",s]]);export{p as __pageData,g as default};
