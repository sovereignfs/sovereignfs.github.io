import{_ as t,o as s,c as i,a2 as a}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0096 — Plugin pre-deletion veto hook","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0096-plugin-deletion-veto-hook.md"},"headers":[],"relativePath":"rfcs/0096-plugin-deletion-veto-hook.md","filePath":"rfcs/0096-plugin-deletion-veto-hook.md"}'),o={name:"rfcs/0096-plugin-deletion-veto-hook.md"};function n(r,e,d,l,c,h){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0096-—-plugin-pre-deletion-veto-hook" tabindex="-1">RFC 0096 — Plugin pre-deletion veto hook <a class="header-anchor" href="#rfc-0096-—-plugin-pre-deletion-veto-hook" aria-label="Permalink to &quot;RFC 0096 — Plugin pre-deletion veto hook&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> August 2026<br><strong>Author:</strong> Claude Code<br><strong>Scope:</strong> <code>packages/manifest</code> (new <code>deletion:veto</code> permission), <code>packages/sdk</code> (<code>DeletionCheckRequest</code>/<code>DeletionCheckResult</code> types, <code>sdk.auth.verifyPlatformCall()</code>), <code>runtime/src/user-deletion.ts</code>, <code>runtime/app/api/account/route.ts</code>, <code>runtime/app/api/admin/users/[id]/route.ts</code>, <code>plugins/console</code> (admin override UI), <code>plugins/account</code> (blocked-deletion message), <code>docs/plugin-development.md</code>, <code>docs/architecture-rules.md</code>; <strong>amends <a href="./0033-user-data-deletion.html">RFC 0033</a></strong> (adds a pre-deletion check the existing cascade does not have; RFC 0033 stays &quot;Implemented&quot; for everything it already covers).<br><strong>Incorporated into plan:</strong> No — documentation-first. Design only; not yet scheduled in <code>ROADMAP.md</code>.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Gives a plugin a way to <strong>block</strong> account deletion before it starts, not just clean up after it. A plugin declares a new manifest permission, <code>deletion:veto</code>, and implements a fixed route, <code>POST &lt;routePrefix&gt;/api/deletion-check</code>; the runtime calls it for every enabled plugin that declares the permission, before either deletion route proceeds. Any plugin that says no blocks the deletion with a <code>409</code> and a human-readable reason — with one exception: an admin (never a self-deleting user) can pass a second, explicit query flag to override a plugin&#39;s veto, and that override is written to the audit log. A plugin whose check fails or times out blocks the deletion the same way a real veto would (fail-closed), reusing the existing 30s-per-handler timeout RFC 0033 already established for cleanup handlers.</p><p>This also folds in the one veto the platform already has — &quot;the sole <code>platform:owner</code> cannot self-delete&quot; — currently hand-written twice, once per deletion route. Both routes call one new function; the duplication goes away as a side effect of building the general mechanism.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p><a href="./../research/0020-plugin-deletion-veto-hook.html">Research 0020</a> found that RFC 0033&#39;s <code>provideDelete</code> hook runs plugin cleanup <strong>after</strong> deletion has already started, in parallel, with no way for a handler to object — a handler that throws only gets logged; the cascade continues regardless. That&#39;s fine for a plugin whose data is wholly owned by the deleted user, but several plugins have data where one user&#39;s row affects another user&#39;s correctness:</p><ul><li><strong>Tally</strong> deliberately leaves every <code>expenses</code>/<code>expense_splits</code>/<code>settlements</code> row in place rather than delete a joint ledger other members&#39; balances depend on — but wants a real block (&quot;don&#39;t let someone with an outstanding balance delete their account&quot;), and there is no SDK surface for that today. Tally&#39;s own <code>SPEC.md</code> §7 independently reached the same conclusion while speccing this behavior.</li><li><strong>Docs</strong> already ships a real, if narrow, corruption case: a sole-owner folder with no other members cascade-deletes every document filed in it, including documents owned by a <em>different</em> user (<code>plugins/sovereign-plugin-docs.local/app/_lib/portability.ts:519-546</code>).</li><li><strong>Sheets</strong> and <strong>Kanban</strong> have real collaborator/owner data models (<code>workbookMembers</code>; <code>boardMembers</code>/<code>projectMembers</code>) and register no deletion handler at all — &quot;protected&quot; only by RFC 0033&#39;s default fallback (an unregistered plugin&#39;s rows are left in place), not by design.</li></ul><p>Three of six plugins that register any deletion handler already touch this shape; two more have a collaborative data model and no handler. This is a recurring gap, not a one-off, and it will keep recurring as more plugins ship shared or joint data — each one rediscovering, independently, that the platform gives them no way to say &quot;not yet.&quot;</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><code>runtime/src/user-deletion.ts:49</code> (<code>deleteUser()</code>) runs every plugin&#39;s <code>provideDelete</code> handler in parallel, each in its own try/catch, raced against a 30s timeout (<code>DELETION_TIMEOUT_MS = 30_000</code>, line 18; race at lines 84–99). A handler&#39;s error is recorded in <code>DeletionSummary.errors</code>; the cascade proceeds regardless (lines 76, 94–121).</li><li>The one existing veto — sole <code>platform:owner</code> cannot self-delete — is hand-written twice, with no shared code and no override: <code>runtime/app/api/account/route.ts:51-65</code> (self-service, returns <code>409</code>) and <code>runtime/app/api/admin/users/[id]/route.ts:39-54</code> (admin-initiated, its own separately-written check). Both call <code>deleteUser()</code> unconditionally afterward — <code>runtime/app/api/account/route.ts:89</code>, <code>runtime/app/api/admin/users/[id]/route.ts:88</code>.</li><li><code>packages/sdk/src/portability.ts:83-99</code> defines <code>DeletionContext</code>, <code>DeletionResult</code>, and <code>DeletionHandler</code>; <code>provideDelete()</code> (line 166) registers a handler into an in-process, <code>Symbol.for</code>-keyed global (<code>runtime/src/portability/registry.ts</code>) that resets on restart and is only populated once a plugin&#39;s own route has served a request in the current process. Research 0020 flagged this as an acceptable risk for export/import (failure mode: incomplete backup) but not for a hook whose entire job is preventing corruption — a plugin that silently has no registered veto is worse than a plugin that silently has no export.</li><li>Plugin-owned API routes are real and already composed into the live runtime today, independent of this RFC: <code>plugins/warden/app/api/chat/route.ts</code> is copied by <code>scripts/generate/compose-routes.ts</code>&#39;s <code>composePlugins()</code> (lines 174–215) into <code>runtime/app/(platform)/(plugins)/warden/api/chat/route.ts</code> and served at <code>/warden/api/chat</code>. This is separate from <code>runtime/src/api-namespace.ts</code>&#39;s <code>RESERVED_API_SEGMENTS</code>, which only governs the top-level public <code>/api/&lt;slug&gt;/*</code> namespace via the <code>apiProvider</code> manifest flag.</li><li>There is no existing precedent, anywhere in <code>runtime/src</code>, for the runtime calling a plugin&#39;s logic over same-process HTTP. Every current runtime→plugin invocation — <code>provideDelete</code>/<code>provideExport</code>/<code>provideImport</code>, job handlers (<code>runtime/src/jobs.ts:171-172</code>), schedule handlers (<code>runtime/src/scheduler.ts:85-93</code>) — is an in-process function reference. The closest existing same-process self-fetch pattern is <code>runtime/src/middleware/plugin-gate.ts</code>&#39;s calls to the runtime&#39;s own admin API (<code>SELF_URL</code>, line 39; e.g. line 85), authenticated with <code>Authorization: Bearer \${process.env.SOVEREIGN_ADMIN_KEY}</code> and checked on the receiving end by <code>checkAdminKey()</code> (<code>runtime/src/admin-guard.ts:8-20</code>).</li><li><code>packages/manifest/src/schema.ts:20-45</code> (<code>permissionSchema</code>) is a closed <code>z.enum(...)</code> — adding a new permission means adding a new literal to this list, the same way <code>data:export</code>/<code>data:import</code> (lines 33–34) were added for RFC 0007.</li><li><code>plugins/console</code>&#39;s admin actions already read <code>process.env.SOVEREIGN_ADMIN_KEY</code> directly and send it as a bearer token to <code>runtime/app/api/admin/*</code> routes (<code>plugins/console/app/users/actions.ts:34,53,75</code>) — same-process plugin code already has, and legitimately uses, read access to this env var today, for a first-party plugin.</li><li>Sovereign plugins of <code>type: sovereign</code>, <code>runtime: native</code> (which covers every plugin discussed in this RFC) are built and served from the <strong>same</strong> Next.js process as the runtime (<code>runtime/package.json:9-10</code> — one <code>next build</code>/<code>next start</code>). There is no per-plugin process isolation.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="manifest-permission" tabindex="-1">Manifest permission <a class="header-anchor" href="#manifest-permission" aria-label="Permalink to &quot;Manifest permission&quot;">​</a></h3><p>Add <code>&#39;deletion:veto&#39;</code> to <code>permissionSchema</code> (<code>packages/manifest/src/schema.ts:20-45</code>), next to <code>data:export</code>/<code>data:import</code>. A plugin opts in by adding it to its own <code>permissions</code> array — same as any other permission — and by implementing the route below. Declaring the permission with no route implemented is a plugin bug (the check call fails closed and blocks every deletion — see Fail-closed behavior below), not a platform concern; <code>docs/plugin-development.md</code>&#39;s permissions table documents the contract.</p><h3 id="the-deletion-check-route" tabindex="-1">The deletion-check route <a class="header-anchor" href="#the-deletion-check-route" aria-label="Permalink to &quot;The deletion-check route&quot;">​</a></h3><p>A plugin declaring <code>deletion:veto</code> must implement:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST &lt;routePrefix&gt;/api/deletion-check</span></span>
<span class="line"><span>Authorization: Bearer &lt;SOVEREIGN_ADMIN_KEY&gt;</span></span>
<span class="line"><span>Body: DeletionCheckRequest = { userId: string; tenantId: string }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>200 → DeletionCheckResult = { allowed: boolean; reason?: string }</span></span></code></pre></div><p><code>reason</code> is required when <code>allowed: false</code> — it is the only thing shown to the admin (and, for a self-service attempt, the user) explaining the block. Any non-<code>200</code> response, a malformed body, a thrown error, or no response within the existing 30s <code>DELETION_TIMEOUT_MS</code> is treated identically to <code>{ allowed: false }</code> — see Fail-closed behavior.</p><p><code>DeletionCheckRequest</code>/<code>DeletionCheckResult</code> are added as type-only exports next to <code>DeletionContext</code>/<code>DeletionResult</code> in <code>packages/sdk/src/portability.ts</code> — plugin authors get type safety for their route handler without any new SDK registration function. There is deliberately no <code>sdk.portability.provideDeletionCheck()</code>: that would recreate the in-process registry this RFC exists to avoid (see Alternatives considered).</p><p>This reuses the exact composition mechanism Warden&#39;s <code>app/api/chat/route.ts</code> already proves works — no change to <code>compose-routes.ts</code> is needed. What&#39;s new is the runtime actually calling in, and authenticating that call.</p><h3 id="authenticating-the-runtime-s-call" tabindex="-1">Authenticating the runtime&#39;s call <a class="header-anchor" href="#authenticating-the-runtime-s-call" aria-label="Permalink to &quot;Authenticating the runtime&#39;s call&quot;">​</a></h3><p>The runtime calls <code>\${SELF_URL}\${manifest.routePrefix}/api/deletion-check</code> (same <code>SELF_URL</code> convention as <code>plugin-gate.ts:39</code>) with <code>Authorization: Bearer \${process.env.SOVEREIGN_ADMIN_KEY}</code> — the same bearer-token pattern already used for every other same-process runtime self-call.</p><p><strong>This does not cryptographically distinguish &quot;the runtime&quot; from &quot;another installed plugin.&quot;</strong> Every <code>type: sovereign, runtime: native</code> plugin runs in the same process and can already read <code>process.env.SOVEREIGN_ADMIN_KEY</code> — Console already does, legitimately, for its own admin calls. A per-call signed token was considered and rejected (see Alternatives considered): same-process code sharing means there is no secret a plugin&#39;s own JS cannot read, so a stronger token buys ceremony, not a stronger boundary. The platform&#39;s existing trust model already treats an installed, admin-enabled plugin as trusted code — it can read arbitrary env vars, and nothing here changes that. What the bearer check <strong>does</strong> protect against, and the reason it&#39;s still worth doing rather than leaving the route open, is an <strong>external, unprivileged caller</strong> hitting <code>POST /tally/api/deletion-check</code> directly — without the key, they get <code>403</code>, the same protection <code>checkAdminKey()</code> gives every other admin-only route today. This is documented explicitly as an accepted limitation, not silently assumed away — see Security considerations.</p><p>A new SDK helper, <code>sdk.auth.verifyPlatformCall(request: Request): boolean</code> (<code>packages/sdk/src/auth.ts</code>, alongside <code>hasCapability</code>), wraps the check so plugin authors write <code>if (!sdk.auth.verifyPlatformCall(request)) return new Response(null, { status: 403 })</code> instead of hand-rolling a comparison against <code>process.env.SOVEREIGN_ADMIN_KEY</code> themselves. Plugin code cannot import <code>checkAdminKey()</code> directly — it lives in <code>runtime/src</code>, off-limits under the SDK boundary rule — so this helper is the one correct, tested way to do the check, rather than leaving every plugin author to get the comparison right on their own.</p><h3 id="the-shared-call-site" tabindex="-1">The shared call site <a class="header-anchor" href="#the-shared-call-site" aria-label="Permalink to &quot;The shared call site&quot;">​</a></h3><p><code>runtime/src/user-deletion.ts</code> gains a new function, run as the first step of <code>deleteUser()</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PreDeletionCheckResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blocked</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blocked</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blockedBy</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">overridable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> runPreDeletionChecks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  opts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">overridePluginVetoes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PreDeletionCheckResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><p>It runs two layers, in order:</p><ol><li><strong>The platform&#39;s own sole-owner check</strong>, in-process (no HTTP, no manifest involved) — the exact logic currently duplicated at <code>runtime/app/api/account/route.ts:51-65</code> and <code>runtime/app/api/admin/users/[id]/route.ts:39-54</code>, moved here once. <code>blockedBy: &#39;platform:sole-owner&#39;</code>, <code>overridable: false</code> — this is a hard invariant (an instance cannot have zero owners), not a plugin&#39;s data-integrity objection, and <code>opts.overridePluginVetoes</code> has no effect on it.</li><li><strong>Every enabled plugin declaring <code>deletion:veto</code></strong> (from <code>getInstalledPlugins()</code>, <code>runtime/src/registry.ts:18</code>, filtered by <code>permissions.includes(&#39;deletion:veto&#39;)</code> and cross-referenced against active <code>plugin_status</code> rows — a <strong>disabled</strong> plugin&#39;s veto does not run, matching how <code>plugin-catalog.ts</code>&#39;s <code>getPluginCatalog()</code> already combines the two), called in parallel via <code>Promise.race</code> against the existing <code>DELETION_TIMEOUT_MS</code>, exactly mirroring the pattern <code>deleteUser()</code> already uses for <code>provideDelete</code> cleanup handlers. The first <code>allowed: false</code> (or failure — see below) short-circuits the rest. <code>blockedBy: &lt;pluginId&gt;</code>, <code>overridable: true</code>.</li></ol><p><code>deleteUser()</code>&#39;s own signature changes to surface this:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeletionOutcome</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blocked</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blockedBy</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">blocked</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeletionSummary</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> deleteUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  opts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">overridePluginVetoes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DeletionOutcome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><p>Both routes call this one function and branch on <code>outcome.blocked</code>, returning <code>409</code> with <code>{ error: outcome.reason, blockedBy: outcome.blockedBy }</code> when blocked — the exact status code the sole-owner check already returns today, now generalized. This removes both routes&#39; own copies of the sole-owner check as a direct consequence of centralizing the call site, which was the actual duplication problem Research 0020 flagged, not an extra cleanup step bolted on afterward.</p><h3 id="admin-override" tabindex="-1">Admin override <a class="header-anchor" href="#admin-override" aria-label="Permalink to &quot;Admin override&quot;">​</a></h3><p><code>runtime/app/api/account/route.ts</code> (self-service) <strong>never</strong> passes <code>overridePluginVetoes</code> — its call site has no such option, so a self-deleting user can never bypass a plugin&#39;s veto, structurally, not just by convention.</p><p><code>runtime/app/api/admin/users/[id]/route.ts</code> (admin-initiated) accepts a <strong>second</strong>, independent query flag: <code>?deleteData=true&amp;overridePluginVetoes=true</code>. <code>deleteData=true</code> alone still runs <code>runPreDeletionChecks()</code> and returns <code>409</code> on any <code>overridable: true</code> block; only the second flag suppresses it. The sole-owner block (<code>overridable: false</code>) ignores this flag entirely — there is no way to delete an instance&#39;s last owner through this route, override or not.</p><p>When an override actually suppresses a real block (i.e. <code>runPreDeletionChecks()</code> would otherwise have blocked), <code>deleteUser()</code> writes a new activity-log event, <code>account.deletion_veto_overridden</code>, before proceeding — actor: the admin; metadata: <code>{ targetUserId, blockedBy, reason }</code>. If the flag is passed but nothing was actually blocked, nothing is logged — the audit trail records overrides that happened, not defensive flags that were passed and did nothing.</p><h3 id="fail-closed-behavior" tabindex="-1">Fail-closed behavior <a class="header-anchor" href="#fail-closed-behavior" aria-label="Permalink to &quot;Fail-closed behavior&quot;">​</a></h3><p>A plugin&#39;s deletion-check call is treated as <code>{ allowed: false }</code> if it throws, times out, returns a non-<code>200</code> status, or returns a body that doesn&#39;t parse as <code>DeletionCheckResult</code>. <code>reason</code> in that case is a fixed platform string — <code>&quot;&lt;pluginId&gt;&#39;s deletion check failed to respond&quot;</code> — not anything plugin-supplied, since there is nothing plugin-supplied to show. This block is <code>overridable: true</code> (an admin still has an escape hatch for an instance with a broken plugin — the availability risk Research 0020 flagged for a instance-wide fail-closed default), but the resulting <code>account.deletion_veto_overridden</code> log entry&#39;s <code>reason</code> makes clear this was a <strong>failed check</strong>, not a plugin&#39;s real objection, so an admin reviewing the audit log later can tell the difference.</p><h3 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h3><p><strong>Account → Data</strong> (self-service): the existing &quot;Delete my account&quot; confirmation dialog&#39;s password-verification call now returns <code>409</code> with a <code>reason</code> when blocked. The dialog shows that reason in place of the generic error and offers no override — consistent with &quot;self-service can never bypass a veto.&quot; The user&#39;s only path forward is whatever the plugin&#39;s reason describes (e.g. &quot;settle your balance in Tally first&quot;).</p><p><strong>Console → Users</strong>: the existing <code>[Delete…]</code> confirmation dialog&#39;s call to <code>DELETE /api/admin/users/[id]?deleteData=true</code> now also may return <code>409</code>. On a block with <code>overridable: true</code>, the dialog shows the reason and a second, explicit confirmation step — a checkbox or a re-styled second button, not a silent retry — before resubmitting with <code>&amp;overridePluginVetoes=true</code>. On <code>overridable: false</code> (the sole-owner case), the dialog shows the reason with no override control at all, matching today&#39;s behavior.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><p><strong>Keep the in-process SDK-registration model</strong> (<code>sdk.portability.provideDeletionCheck()</code>, matching <code>provideExport</code>/<code>provideImport</code>/<code>provideDelete</code>) — Research 0020&#39;s own Recommendation section originally favored this, for consistency with the three existing hooks. Rejected once the registry&#39;s reset-on-restart gap was weighed against this hook&#39;s actual stakes: a plugin whose process hasn&#39;t served a request since the last restart would have <strong>no registered veto at all</strong>, and deletion would proceed as if the plugin had no objection — a silent correctness gap, not a degraded-but-safe fallback. Export/import degrade to &quot;incomplete backup&quot; the same way; this hook degrades to &quot;the corruption it exists to prevent, happening anyway.&quot; A startup warm-up pass (visiting every installed plugin once after boot to force registration) was also considered as a mitigation and rejected — it narrows the window but doesn&#39;t close it (a plugin enabled after boot, or a dev-mode hot reload, still has the gap), and manifest-declared permissions have no such window at all: the manifest is static, known at every deletion attempt regardless of whether the plugin&#39;s process code has run yet.</p><p><strong>A stronger per-call signed token instead of <code>SOVEREIGN_ADMIN_KEY</code></strong> — e.g. the runtime generates a single-use, cryptographically random token per deletion attempt and expects the plugin to echo it back for verification. Rejected: verifying an echoed token still requires the plugin to check it against <em>something</em> the runtime told it, and same-process JS has no facility to keep that something secret from other same-process JS — there is no Node-level sandboxing between plugins and the runtime here. A stronger token adds real implementation complexity for a boundary that same-process code-sharing cannot actually provide, regardless of which secret is used. See Security considerations for what the bearer check is actually good for.</p><p><strong>Soft warning only, no real block</strong> (<code>provideDeletionWarning()</code>, surfaced in the confirmation UI without blocking the API call) — this is Research 0020&#39;s Option 2. Rejected as a standalone mechanism: a user or script calling <code>DELETE /api/account</code> directly bypasses whatever the UI showed, leaving the actual corruption risk unsolved. Kept as a side effect of this design instead of a separate hook — the same <code>reason</code> string a veto returns is exactly what the UI already needs to show; there is no second mechanism to build.</p><p><strong>Status quo</strong> — no platform mechanism, left to each plugin&#39;s own handler design. Rejected per Research 0020&#39;s survey: three of six plugins registering any deletion handler already touch this pattern, and Docs&#39; own workaround already shipped a real gap despite genuine effort. The pattern will keep recurring.</p><h2 id="security-considerations" tabindex="-1">Security considerations <a class="header-anchor" href="#security-considerations" aria-label="Permalink to &quot;Security considerations&quot;">​</a></h2><ul><li><strong>The runtime→plugin bearer check does not defend against a malicious co-resident plugin.</strong> <code>SOVEREIGN_ADMIN_KEY</code> is a <code>process.env</code> value, and every <code>type: sovereign, runtime: native</code> plugin shares the runtime&#39;s own process — nothing stops a plugin&#39;s server code from reading it and either forging a call to another plugin&#39;s <code>/api/deletion-check</code> route, or reading it to detect it&#39;s being asked a real deletion-check question. This is not a new hole this RFC introduces: the platform&#39;s trust model already treats an admin-installed plugin as trusted code with full same-process access (<code>plugins/console</code> already reads and uses this exact key), and an adversarial-plugin threat model doesn&#39;t exist anywhere else in the platform today either. It is accepted here explicitly, not silently inherited.</li><li><strong>What the bearer check does protect: an external, unprivileged caller.</strong> Without <code>SOVEREIGN_ADMIN_KEY</code>, a request to <code>POST /tally/api/deletion-check</code> from outside the process gets <code>403</code>, same as every other admin-gated route. Absent this check, the route would otherwise be reachable by anyone who can reach the plugin&#39;s own public routePrefix — a real information-disclosure risk (e.g. probing whether a given user has an outstanding balance) even setting aside deletion entirely.</li><li><strong>Fail-closed has an availability cost, by design.</strong> A broken or slow <code>deletion:veto</code> plugin blocks every admin and self-service deletion attempt on the instance until either the plugin is fixed or an admin exercises the override on each attempt. This is the deliberate tradeoff Research 0020&#39;s Decision recorded: silently letting deletions through when a check can&#39;t be trusted (fail-open) reintroduces exactly the corruption this hook exists to prevent. The override exists specifically so this isn&#39;t a hard availability failure, only friction.</li><li><strong>A self-deleting user can never bypass a veto.</strong> <code>overridePluginVetoes</code> has no code path from the self-service route — there is no query parameter, flag, or header a user could pass to trigger it. Only the admin-initiated route accepts it, and doing so is written to the audit log every time it actually suppresses a block.</li><li><strong>The sole-owner block is never overridable</strong>, by either route, regardless of any flag — an instance cannot reach zero <code>platform:owner</code> users through this mechanism.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Docs&#39; sole-owner-folder cascade bug</strong> (Research 0020, finding 4) is real and already shipped, independent of whether this RFC lands. Out of this RFC&#39;s scope — flagged here so it isn&#39;t lost, and because once <code>deletion:veto</code> exists, Docs is a natural first adopter for the underlying case this bug is a symptom of.</li><li><strong>Console health visibility for a chronically-broken veto check.</strong> A plugin whose <code>/api/deletion-check</code> route is permanently broken makes every admin deletion attempt for every user fail closed, one 409 at a time, each requiring its own override click. Should Console surface this as a standing health warning (similar to existing plugin health/status signals) rather than relying on an admin to notice the pattern across repeated per-attempt failures? Left open — the per-attempt fail-closed behavior and audit log are the v1 mechanism either way.</li><li><strong>Reason text handling.</strong> A plugin&#39;s <code>reason</code> string is shown directly to an admin (and, for a non-overridable block, to the deleting user themselves). Does it need length capping or sanitization beyond what the UI&#39;s existing text-rendering already does for other plugin-supplied strings (e.g. activity log descriptions)? Likely no new handling needed — flagged for the implementer to confirm against existing precedent rather than assumed.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Documentation-first: this RFC is not yet scheduled. Once accepted, implementation is one platform-side epic task (manifest permission, SDK types + <code>verifyPlatformCall()</code>, the <code>runPreDeletionChecks()</code>/<code>deleteUser()</code> refactor, both routes, both UI flows) — <code>docs/plugin-development.md</code>&#39;s permissions table and <code>docs/architecture-rules.md</code> need the corresponding doc updates in the same PR per the platform&#39;s parity convention. Plugin adoption (Tally, Docs, Sheets, Kanban each implementing their own <code>/api/deletion-check</code> route) is separate follow-up work per plugin, not part of this RFC&#39;s own implementation — the platform capability has to exist before any plugin can build against it.</p><p><strong>Semver:</strong> <code>@sovereignfs/manifest</code> — minor (new permission enum value, additive). <code>@sovereignfs/sdk</code> — minor (<code>DeletionCheckRequest</code>/<code>DeletionCheckResult</code> type exports, <code>sdk.auth.verifyPlatformCall()</code>; both additive, no existing signature changes). <code>runtime</code> — minor (<code>deleteUser()</code>&#39;s signature change is internal to <code>runtime/src</code>, not a public contract; both routes&#39; response shape gains a <code>blockedBy</code> field on the existing <code>409</code>, additive). <code>plugins/console</code>, <code>plugins/account</code> — patch (UI changes, no manifest/permission changes of their own).</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>August 2026</td><td>Initial draft</td></tr></tbody></table>`,56)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
