import{_ as s,o as t,c as a,a2 as n}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"RFC 0012 — Passkeys & TOTP multi-factor auth","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0012-passkeys-and-mfa.md","filePath":"rfcs/0012-passkeys-and-mfa.md"}'),o={name:"rfcs/0012-passkeys-and-mfa.md"};function i(r,e,d,l,c,h){return t(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="rfc-0012-—-passkeys-totp-multi-factor-auth" tabindex="-1">RFC 0012 — Passkeys &amp; TOTP multi-factor auth <a class="header-anchor" href="#rfc-0012-—-passkeys-totp-multi-factor-auth" aria-label="Permalink to &quot;RFC 0012 — Passkeys &amp; TOTP multi-factor auth&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>apps/auth</code> (better-auth plugins + login flow), runtime middleware &amp; <code>/api/verify</code>, <code>plugins/account</code> (Security tab), <code>bin/sv</code> (break-glass), Console admin API, <code>packages/sdk</code> (auth surface), <code>.env.example</code>, Docker (new dependency), docs, SRS<br><strong>Incorporated into plan:</strong> Yes — scheduled as roadmap Task 0.5.27; documentation-first. This RFC records the feasibility findings, the technical design against the existing better-auth stack, and the end-to-end UI flows. Scheduling, SRS requirement IDs (proposed AUTH-09+), and task allocation are deferred; prioritization comes later.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Add a second factor and a phishing-resistant primary credential to Sovereign&#39;s sign-in:</p><ul><li><strong>TOTP multi-factor auth — authenticator apps only.</strong> No SMS, no email OTP. A user scans a QR code into an authenticator app (Aegis, 1Password, Google Authenticator, …) and confirms a 6-digit code on every subsequent login.</li><li><strong>Passkeys (WebAuthn).</strong> Usable <strong>two ways</strong>: as a second factor <em>and</em> as a <strong>passwordless primary credential</strong> (&quot;Sign in with a passkey&quot;).</li><li><strong>Backup codes</strong> generated at TOTP enrollment as the everyday recovery net, backed by an <strong>admin reset</strong> path and a last-resort <strong><code>sv</code> CLI break-glass</strong>.</li></ul><p>Both are built on better-auth&#39;s <strong>first-party</strong> plugins — core <code>better-auth/plugins/two-factor</code> and the companion <code>@better-auth/passkey</code> package (SimpleWebAuthn under the hood). Sovereign owns the <strong>policy, recovery, and UX</strong>; it does <strong>not</strong> hand-roll WebAuthn ceremonies or TOTP/HMAC. This complements RFC 0008 (transport + at-rest hardening) with <strong>account-level</strong> hardening.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign authenticates with <strong>email + password only</strong> (SRS AUTH-01). For a privacy-first, self-hosted workspace that holds a user&#39;s whole working life, a single password is the weakest link: credential phishing and password reuse are the realistic attack, not a stolen disk. RFC 0008 hardens the transport and the data at rest; it does nothing for a stolen or phished password. This RFC closes that gap.</p><p>The design constraints follow directly from Sovereign&#39;s positioning:</p><ul><li><strong>No SMS / no email OTP.</strong> SMS is phishable and SIM-swappable, requires a paid gateway, and leaks the user&#39;s phone number to a third party — incompatible with the no-telemetry, self-host ethos. Authenticator-app TOTP needs no external service. (Email OTP is also declined: it is no stronger than the password-reset channel it would share, and it makes the inbox a single point of failure.)</li><li><strong>Passwordless via passkeys</strong> is the modern, phishing-resistant default and costs the user nothing recurring.</li><li><strong>Recovery must work without a support desk.</strong> A self-hoster who loses their authenticator has no one to call — recovery is a first-class part of the design, not an afterthought.</li></ul><p>There is <strong>no prior mention</strong> of MFA, 2FA, passkeys, WebAuthn, TOTP, or OTP anywhere in the SRS, the other RFCs, or the code — this is a clean addition.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>better-auth 1.6.16</strong> is installed; only <code>nextCookies()</code> is wired today (<code>apps/auth/src/auth.ts:89</code>). Email+password is enabled with <code>autoSignIn</code> (<code>apps/auth/src/auth.ts:30</code>).</li><li><strong>First-party MFA plugins are available in the installed version</strong> (see Feasibility): TOTP + backup codes ship in core better-auth; passkeys are a separate package.</li><li><strong>Schema is created at startup, idempotently.</strong> <code>apps/auth/instrumentation.ts</code> runs <code>getMigrations(getAuthOptions())</code> (better-auth&#39;s own tables) plus <code>ensureAuthTables()</code> (Sovereign&#39;s <code>invites</code> / <code>auth_settings</code>). better-auth&#39;s <code>getMigrations()</code> <strong>auto-discovers plugin tables</strong>, so enabling the two-factor / passkey plugins creates their tables with no bespoke DDL — but it does exercise the dialect-agnostic SQLite/Postgres path and the standalone Docker migrate flow.</li><li><strong>Session / cookie-cache flow (AUTH-05/06).</strong> The runtime middleware verifies a signed <code>better-auth.session_data</code> snapshot offline (<code>runtime/src/session-verify.ts</code>), falling back to <code>/api/verify</code> and forwarding the re-issued <code>Set-Cookie</code>. Anything that changes a user&#39;s session-visible state must invalidate that cache cookie or it stays stale up to <code>cookieCache.maxAge</code> (300s, <code>apps/auth/src/auth.ts:28</code>).</li><li><strong><code>freshAge: 0</code> is deliberately set</strong> (<code>apps/auth/src/auth.ts:22</code>) so day-old sessions can still list/manage themselves; a regression test pins it. This has a direct consequence for MFA (see §4e).</li><li><strong>Server-to-server calls to better-auth need an <code>Origin</code> header</strong> equal to the auth base URL; the SDK already does this via <code>authFetch</code> (<code>packages/sdk/src/auth.ts</code>).</li><li><strong>The Account Security tab</strong> (<code>plugins/account/app/security/page.tsx</code>) already renders password-change and active-session sections — the natural home for MFA management. The session-cache-invalidation pattern lives in <code>plugins/account/app/actions.ts</code>.</li></ul><h2 id="feasibility-verified-against-the-installed-packages" tabindex="-1">Feasibility (verified against the installed packages) <a class="header-anchor" href="#feasibility-verified-against-the-installed-packages" aria-label="Permalink to &quot;Feasibility (verified against the installed packages)&quot;">​</a></h2><ul><li><strong>TOTP + backup codes — core better-auth.</strong> <code>better-auth/plugins/two-factor</code> is present in 1.6.16 (the <code>TOTP</code>, <code>backupCode</code>, <code>OTPOptions</code>, <code>trustDevice</code> symbols are in the shipped types). We enable <code>totp</code> + <code>backupCodes</code> and <strong>omit the <code>otp</code> (email/SMS) option entirely</strong> → authenticator apps only, exactly as required.</li><li><strong>Passkeys — separate dependency.</strong> The passkey plugin is <strong>not</strong> in the core package; it is <code>@better-auth/passkey</code> (server) + <code>@better-auth/passkey/client</code> (browser), SimpleWebAuthn-backed. It supports passwordless <code>signIn.passkey()</code> (with conditional-UI autofill) and <code>addPasskey()</code> for an already-authenticated user. Server config: <code>rpID</code>, <code>rpName</code>, <code>origin</code>. <strong>This is a new runtime dependency on <code>apps/auth</code></strong> with Docker/build impact (see §4h).</li><li><strong>Multi-step login is supported.</strong> The two-factor plugin returns a <code>twoFactorRedirect</code> signal after a correct password when 2FA is enabled, which drives a dedicated challenge step.</li></ul><h2 id="locked-decisions" tabindex="-1">Locked decisions <a class="header-anchor" href="#locked-decisions" aria-label="Permalink to &quot;Locked decisions&quot;">​</a></h2><ol><li><strong>Passkey role:</strong> both — second factor <strong>and</strong> passwordless primary login.</li><li><strong>Enforcement:</strong> opt-in per user for the first cut; <strong>admin-mandated MFA</strong> is specified as an explicit <strong>later phase</strong> (§4f), not phase one.</li><li><strong>Recovery (priority order):</strong> backup codes → admin reset → <code>sv</code> CLI break-glass (§4g).</li><li><strong>Status:</strong> Implemented, pre-v1 intent, prioritization deferred.</li></ol><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_4a-totp-authenticator-apps-only" tabindex="-1">4a. TOTP (authenticator apps only) <a class="header-anchor" href="#_4a-totp-authenticator-apps-only" aria-label="Permalink to &quot;4a. TOTP (authenticator apps only)&quot;">​</a></h3><p>Enable better-auth&#39;s two-factor plugin with TOTP and backup codes, and <strong>without</strong> the <code>otp</code> (email) option:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// apps/auth/src/auth.ts — illustrative</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { twoFactor } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;better-auth/plugins/two-factor&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">twoFactor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  issuer: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Sovereign&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// shown in the authenticator app</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  totp: { digits: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, period: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  backupCodes: { amount: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // NOTE: no \`otp: {...}\` — email/SMS OTP is intentionally not enabled.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><strong>Enrollment</strong> (in the Account Security tab): the user clicks <em>Enable two-factor</em>, re-enters their password, and is shown a <strong>QR code + the secret in text</strong>. They scan it, enter one generated code to prove the pairing works, and only then is TOTP marked active — at which point the <strong>backup codes are revealed once</strong> with a copy/download prompt and a &quot;I&#39;ve saved these&quot; confirmation. <strong>Disabling</strong> TOTP requires a password re-prompt (§4e).</p><h3 id="_4b-passkeys-webauthn" tabindex="-1">4b. Passkeys (WebAuthn) <a class="header-anchor" href="#_4b-passkeys-webauthn" aria-label="Permalink to &quot;4b. Passkeys (WebAuthn)&quot;">​</a></h3><p>Add <code>@better-auth/passkey</code> on the server and <code>@better-auth/passkey/client</code> on the auth client, configured with the relying-party identity:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">passkey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rpID: env.webAuthnRpId, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// e.g. &#39;localhost&#39; (dev) or &#39;example.com&#39; (prod)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rpName: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Sovereign&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  origin: env.webAuthnOrigin, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the browser-facing origin of the ceremony page</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><ul><li><strong>Add a passkey</strong> from the Account Security tab via <code>addPasskey()</code> (the user is already authenticated). Enrolled credentials are listed with a friendly device name, created/last-used timestamps (mirroring the existing session list), and <strong>rename / remove</strong> controls.</li><li><strong>Passwordless sign-in</strong> on the login page: a <em>Sign in with a passkey</em> button calling <code>signIn.passkey()</code>, plus <strong>conditional-UI autofill</strong> so a passkey is offered inline on the email field when the platform supports it.</li><li><strong>Passkey as a second factor:</strong> when a user has both a password and a passkey, the passkey can satisfy the post-password challenge (§4c).</li></ul><p>See §4h for the relying-party / origin deployment constraint — it is the single most important operational detail in this RFC.</p><h3 id="_4c-multi-step-login" tabindex="-1">4c. Multi-step login <a class="header-anchor" href="#_4c-multi-step-login" aria-label="Permalink to &quot;4c. Multi-step login&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                 ┌─────────────────────────┐</span></span>
<span class="line"><span>   /login  ──────▶  email + password submit │</span></span>
<span class="line"><span>                 └───────────┬─────────────┘</span></span>
<span class="line"><span>                             │ correct credentials</span></span>
<span class="line"><span>                 ┌───────────▼─────────────┐</span></span>
<span class="line"><span>                 │  any second factor set?  │</span></span>
<span class="line"><span>                 └─────┬──────────────┬─────┘</span></span>
<span class="line"><span>                    no │              │ yes (twoFactorRedirect)</span></span>
<span class="line"><span>            ┌──────────▼───┐   ┌──────▼───────────────────────────────┐</span></span>
<span class="line"><span>            │ issue session│   │  /login/2fa challenge:                │</span></span>
<span class="line"><span>            └──────────────┘   │   • TOTP 6-digit code                 │</span></span>
<span class="line"><span>                               │   • or &quot;use a passkey&quot; (WebAuthn)     │</span></span>
<span class="line"><span>                               │   • or &quot;use a backup code&quot;            │</span></span>
<span class="line"><span>                               └──────┬───────────────────────────────┘</span></span>
<span class="line"><span>                                      │ verified</span></span>
<span class="line"><span>                               ┌──────▼───────┐</span></span>
<span class="line"><span>                               │ issue session│</span></span>
<span class="line"><span>                               └──────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   Passwordless path: &quot;Sign in with a passkey&quot; → signIn.passkey() → issue session</span></span>
<span class="line"><span>   (skips password and the challenge step entirely).</span></span></code></pre></div><p>The challenge page lives in <code>apps/auth/app/</code> alongside the existing login page. The passwordless path bypasses both the password field and the challenge step.</p><h3 id="_4d-session-cache-interaction" tabindex="-1">4d. Session / cache interaction <a class="header-anchor" href="#_4d-session-cache-interaction" aria-label="Permalink to &quot;4d. Session / cache interaction&quot;">​</a></h3><p>Enabling or disabling any factor changes the user&#39;s security posture but <strong>not</strong> the <code>session_data</code> snapshot&#39;s currently-cached fields — yet a stale snapshot must not let a just-disabled factor linger or hide a just-enabled one in chrome state. Any factor mutation must <strong>invalidate both <code>better-auth.session_data</code> cookie variants</strong> (plain + <code>__Secure-</code>, <code>maxAge: 0</code>) exactly as profile mutations already do (<code>plugins/account/app/actions.ts</code>), forcing the next request to re-verify via <code>/api/verify</code>. The session token itself is untouched (no forced logout).</p><h3 id="_4e-the-freshage-0-tension" tabindex="-1">4e. The <code>freshAge: 0</code> tension <a class="header-anchor" href="#_4e-the-freshage-0-tension" aria-label="Permalink to &quot;4e. The \`freshAge: 0\` tension&quot;">​</a></h3><p>Sovereign deliberately disables better-auth&#39;s fresh-session gate (<code>apps/auth/src/auth.ts:22</code>) so long-lived self-host sessions can manage themselves. The conventional way to protect security-sensitive changes — &quot;your session must be fresh (re-authenticated recently)&quot; — is therefore <strong>unavailable by design</strong>. Instead, security-sensitive actions <strong>re-prompt for the password</strong> inline:</p><ul><li>enabling or disabling TOTP,</li><li>removing the <strong>last</strong> passkey (removing a non-last one is lower-risk),</li><li>viewing or regenerating backup codes.</li></ul><p>This keeps the protection local to the action without re-introducing a freshness gate (the regression test that pins <code>freshAge === 0</code> stays valid).</p><h3 id="_4f-enforcement-phased" tabindex="-1">4f. Enforcement (phased) <a class="header-anchor" href="#_4f-enforcement-phased" aria-label="Permalink to &quot;4f. Enforcement (phased)&quot;">​</a></h3><ul><li><strong>Phase 1 (first cut): opt-in per user.</strong> Each user enables MFA for themselves; no platform mandate. Matches a single-admin self-host.</li><li><strong>Later phase: admin-mandated MFA.</strong> A Console <strong><code>require_mfa</code></strong> toggle, <strong>dual-written</strong> like <code>invite_only</code> (platform <code>platform_settings</code> for <code>sdk.platform.getConfig()</code> + the auth server&#39;s <code>auth_settings</code>, the auth copy authoritative for enforcement). When on, a user without a second factor is sent through a <strong>setup gate</strong> on next login before reaching the workspace. This is specified, not built in phase one.</li></ul><h3 id="_4g-recovery-priority-order" tabindex="-1">4g. Recovery (priority order) <a class="header-anchor" href="#_4g-recovery-priority-order" aria-label="Permalink to &quot;4g. Recovery (priority order)&quot;">​</a></h3><ol><li><strong>Backup codes (everyday net).</strong> Generated at TOTP enrollment, single-use, shown once. A backup code is accepted at the challenge step in place of a TOTP code. Regenerating invalidates the old set (password re-prompt, §4e).</li><li><strong>Admin reset (a second person can help).</strong> An admin clears another user&#39;s MFA factors via the Console admin API, <code>SOVEREIGN_ADMIN_KEY</code>-gated, mirroring the existing <code>apps/auth/app/api/admin/users/*</code> routes. The user falls back to password-only and can re-enroll. Surfaced in the Console user-management UI.</li><li><strong><code>sv</code> CLI break-glass (the sole admin&#39;s last resort).</strong> A command — e.g. <code>sv user reset-mfa &lt;email&gt;</code> — that disables/clears a user&#39;s factors directly against the auth DB. This is the <strong>only</strong> rescue for a sole admin who has locked themselves out, because no one above them can run the admin reset. Documented explicitly as a last resort requiring host/DB access.</li></ol><h3 id="_4h-deployment-the-webauthn-relying-party-constraint" tabindex="-1">4h. Deployment &amp; the WebAuthn relying-party constraint <a class="header-anchor" href="#_4h-deployment-the-webauthn-relying-party-constraint" aria-label="Permalink to &quot;4h. Deployment &amp; the WebAuthn relying-party constraint&quot;">​</a></h3><p>WebAuthn binds credentials to a <strong>relying-party ID (<code>rpID</code>)</strong> — a registrable domain — and validates the <strong>origin</strong> of each ceremony. Sovereign serves the <strong>login/challenge pages from <code>apps/auth</code></strong> but lets a logged-in user <strong>add a passkey from the runtime</strong> (the Account plugin). These can be different origins:</p><ul><li><strong>Dev:</strong> runtime is <code>localhost:3000</code>, auth is <code>localhost:3001</code>. WebAuthn ignores the port for <code>rpID</code>, so both share <code>rpID = localhost</code> and it &quot;just works&quot;.</li><li><strong>Production:</strong> the auth and runtime <strong>browser-facing</strong> origins <strong>must share a registrable domain.</strong> E.g. <code>app.example.com</code> (runtime) + <code>auth.example.com</code> (auth) → <code>rpID = example.com</code>. A deployment that exposes them on unrelated domains cannot use passkeys. This must be stated prominently in <code>docs/self-hosting.md</code>.</li></ul><p>New env vars (final names TBD): <strong><code>AUTH_WEBAUTHN_RP_ID</code></strong>, <strong><code>AUTH_WEBAUTHN_RP_NAME</code></strong>, and an origin (derivable from <code>AUTH_BASE_URL</code> / <code>NEXT_PUBLIC_RUNTIME_URL</code>, or explicit). On implementation this carries <strong>Docker and docs impact</strong>: a new <code>@better-auth/passkey</code> (+ SimpleWebAuthn) dependency on the auth app affects the image build, and <code>.env.example</code> + <code>docs/self-hosting.md</code></p><ul><li>the <code>runtime/src/docs-parity.test.ts</code> env-var parity check must be updated in the same change. (Flagged here; nothing changes in this doc-only draft.)</li></ul><h3 id="_4i-csp-headers" tabindex="-1">4i. CSP / headers <a class="header-anchor" href="#_4i-csp-headers" aria-label="Permalink to &quot;4i. CSP / headers&quot;">​</a></h3><p>WebAuthn uses the <code>navigator.credentials</code> browser API, which is <strong>not</strong> governed by CSP <code>connect-src</code> (no relaxation needed), and the TOTP/passkey ceremonies are same-origin. RFC 0008&#39;s strict nonce-based CSP (no <code>&#39;unsafe-inline&#39;</code> scripts) stands unchanged; any client JS the challenge page needs uses the per-request nonce. No CSP changes are anticipated.</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><p><strong>Enroll TOTP</strong> — Account → Security → <em>Enable two-factor</em> → password re-prompt → QR + secret shown → enter one code to confirm → backup codes revealed once (copy/download + confirm saved) → TOTP active; session cache invalidated (§4d).</p><p><strong>Enroll passkey</strong> — Account → Security → <em>Add a passkey</em> → browser WebAuthn prompt (<code>addPasskey()</code>) → name the credential → appears in the passkey list with created/last-used; rename/remove available (removing the last passkey re-prompts for password, §4e).</p><p><strong>Passwordless login</strong> — <code>/login</code> → <em>Sign in with a passkey</em> (or autofill on the email field) → <code>signIn.passkey()</code> → session issued, redirect to runtime. No password, no challenge step.</p><p><strong>2FA challenge at login</strong> — <code>/login</code> → email+password → <code>twoFactorRedirect</code> → <code>/login/2fa</code> → enter TOTP code <em>or</em> choose &quot;use a passkey&quot; <em>or</em> &quot;use a backup code&quot; → session issued.</p><p><strong>Backup-code use</strong> — at the 2FA challenge, <em>Use a backup code</em> → enter one unused code → session issued; that code is now spent. (When codes run low, prompt to regenerate.)</p><p><strong>Admin reset</strong> — Console → Users → pick user → <em>Reset MFA</em> (admin-key-gated) → the user&#39;s factors are cleared; they sign in with password and may re-enroll.</p><p><strong>CLI break-glass</strong> — operator with host access runs <code>sv user reset-mfa &lt;email&gt;</code> → factors cleared directly in the auth DB → the locked-out (sole) admin can sign in again.</p><h2 id="sdk-surface" tabindex="-1">SDK surface <a class="header-anchor" href="#sdk-surface" aria-label="Permalink to &quot;SDK surface&quot;">​</a></h2><p>MFA enrollment, challenge, and management are <strong>UI + auth-server concerns</strong>, not plugin-facing capabilities. <strong>Phase one adds no new <code>sdk.auth.*</code> methods.</strong> The existing surface (<code>getSession</code>, <code>requireSession</code>, <code>changePassword</code>, <code>listSessions</code>, <code>revokeSession</code>, <code>signOut</code>) is unchanged; a session is a session regardless of how many factors produced it.</p><p>If a later need arises (e.g. a plugin wanting to read whether the current user has MFA enabled), it follows the established <strong>reserved-stub pattern</strong> (<code>packages/sdk/src/unimplemented.ts</code> — throw <code>NotImplementedError</code>, additive <strong>minor</strong> bump) rather than expanding the surface speculatively now.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Hand-rolled TOTP / WebAuthn.</strong> Reinvents audited cryptographic ceremonies for no benefit. Rejected in favour of better-auth&#39;s first-party plugins.</li><li><strong>SMS OTP.</strong> Phishable, SIM-swappable, requires a paid gateway, and leaks a phone number to a third party — fundamentally at odds with the self-host, no-telemetry stance. Rejected (and the explicit requirement).</li><li><strong>Email OTP.</strong> No stronger than the password-reset channel it shares, and it makes the inbox a single point of failure. Declined; authenticator TOTP only.</li><li><strong>Third-party IdP / SSO (OIDC, social login).</strong> Useful for orgs, but out of scope for v1&#39;s single-tenant model and adds an external dependency. Possible future RFC, not this one.</li><li><strong>Passkeys as second factor only (no passwordless).</strong> Simpler, but forfeits the main benefit of passkeys (phishing-resistant passwordless primary auth). Rejected per the locked decision to support both.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Requirement IDs.</strong> Proposed <strong>AUTH-09+</strong> (highest existing is AUTH-08) — not assigned in the SRS until this RFC is accepted and scheduled.</li><li><strong><code>trustDevice</code> (&quot;remember this device for 30 days&quot;).</strong> better-auth supports it; in scope, or does it weaken the guarantee too much for a security feature?</li><li><strong>Admin-reset UX.</strong> Exact placement/confirmation in Console user management, and whether it should also force-revoke the user&#39;s active sessions.</li><li><strong><code>@better-auth/passkey</code> packaging.</strong> It is a new runtime dependency on <code>apps/auth</code> (Docker/build impact) — confirm the standalone image trace and <code>allowBuilds</code> implications for any native SimpleWebAuthn bits.</li><li><strong>Production origin guidance.</strong> Best default for <code>rpID</code> derivation, and whether to fail-fast at startup when passkeys are enabled but the auth/runtime origins can&#39;t share a registrable domain.</li><li><strong>Mandatory-MFA grace flow.</strong> When <code>require_mfa</code> is later turned on, how much grace before existing password-only users are forced through setup?</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Documentation-first (this RFC).</strong> Feasibility + design + UI flows captured; no code, no SRS edits, no scheduling.</li><li><strong>When accepted &amp; scheduled</strong> (a security task, sibling to RFC 0008&#39;s Tier 0/1 line): enable two-factor (TOTP + backup codes), add <code>@better-auth/passkey</code>, build the Account Security UI, the multi-step login + challenge page, backup codes, and admin reset + <code>sv</code> break-glass. Assign AUTH-09+ and a decision-log row at that point.</li><li><strong>Later phase:</strong> admin-mandated MFA (<code>require_mfa</code> toggle + setup gate) and any <code>trustDevice</code> decision.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft; TOTP (authenticator-only) + passkeys (2FA and passwordless) on better-auth&#39;s first-party plugins; recovery + UI flows; documentation-first, prioritization deferred.</td></tr><tr><td>0.2</td><td>Jun 2026</td><td>Accepted; scheduled in the roadmap as Task 0.5.27.</td></tr></tbody></table>`,67)])])}const u=s(o,[["render",i]]);export{g as __pageData,u as default};
