import{_ as t,o,c as a,a2 as n}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse(`{"title":"Health connector: why \\"step count\\" isn't HealthKit","description":"","frontmatter":{"id":8,"title":"Health connector: why \\"step count\\" isn't HealthKit","status":"Decided (redefine scope) — parked pending a paid Apple Developer account for anything beyond step count","date":"August 2026","author":"Claude Code (session with the developer)","scope":"mobile","summary":"Candidate connector — health data, proposed to start from step count","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/research/0008-health-step-count.md"},"headers":[],"relativePath":"sovereign-edge/research/0008-health-step-count.md","filePath":"sovereign-edge/research/0008-health-step-count.md"}`),i={name:"sovereign-edge/research/0008-health-step-count.md"};function s(r,e,l,c,d,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="research-0008-—-health-connector-why-step-count-isn-t-healthkit" tabindex="-1">Research 0008 — Health connector: why &quot;step count&quot; isn&#39;t HealthKit <a class="header-anchor" href="#research-0008-—-health-connector-why-step-count-isn-t-healthkit" aria-label="Permalink to &quot;Research 0008 — Health connector: why &quot;step count&quot; isn&#39;t HealthKit&quot;">​</a></h1><p><strong>Related:</strong> <a href="./0002-react-native-framework-choice.html">research 0002</a> (the &quot;no EAS, project holds its own keys&quot; decision — unrelated mechanism, same kind of paid-account dependency), task 0.3 / epic <a href="./../epics/mobile/infrastructure.html">Infrastructure</a> (established that this project currently has only a free Apple Personal Team, no paid Developer Program membership)</p><hr><h2 id="question" tabindex="-1">Question <a class="header-anchor" href="#question" aria-label="Permalink to &quot;Question&quot;">​</a></h2><p>Is a Health connector — starting from step count — buildable given this project&#39;s current Apple account status, and what would the full version (beyond steps) actually require?</p><h2 id="findings" tabindex="-1">Findings <a class="header-anchor" href="#findings" aria-label="Permalink to &quot;Findings&quot;">​</a></h2><ul><li><strong>iOS HealthKit read access requires the <code>com.apple.developer.healthkit</code> entitlement on the App ID.</strong> Entitlements are provisioned through the Apple Developer Portal and are <strong>not available to a free Personal Team</strong>. Task 0.3 (native build tooling) established that this project is currently signed in with exactly that — free team <code>8CJGS4873L</code>, no paid Apple Developer Program membership. This is a hard blocker for HealthKit, not a scoping choice: the iOS half cannot be built at all until that membership exists.</li><li>No first-party Expo SDK module for HealthKit exists. The nearest precedent in this repo is <code>modules/sovereign-hashing</code> (task 0.5) — a hand-rolled native Expo module written because no existing package was fast enough. A HealthKit module would follow the same shape (native Swift + a JS bridge + a config plugin adding the entitlement and <code>NSHealthShareUsageDescription</code>), but HealthKit&#39;s query/authorization/ background-delivery surface is a real subsystem, a meaningfully bigger native-code lift than a stateless hashing function.</li><li><strong>Android&#39;s Health Connect</strong> (the current unified health API, superseding Google Fit for new integrations) can read step count via <code>StepsRecord</code>, again with no first-party Expo module — a community package or custom module either way. Health Connect itself is bundled on Android 14+ but needs a separate Play Store install on Android 9–13, an install-time dependency this app doesn&#39;t control.</li><li><strong>Step count specifically has a lighter path that skips both frameworks entirely.</strong> iOS Core Motion&#39;s <code>CMPedometer</code> reads step count straight from the motion coprocessor, no HealthKit involved, needing only <code>NSMotionUsageDescription</code> — no entitlement. Android&#39;s step-count sensor (<code>TYPE_STEP_COUNTER</code>/<code>TYPE_STEP_DETECTOR</code>) is a plain sensor, not a Health Connect query. Both are already exposed via <code>expo-sensors</code>&#39; <code>Pedometer</code> — an installable Expo package, no custom native module, no entitlement, no paid account.</li><li>This means <strong>&quot;Health connector, starting with step count&quot; and &quot;a HealthKit/Health Connect connector&quot; are not the same task.</strong> The former is achievable today via <code>expo-sensors</code>; the latter is blocked. Naming the step-count feature a &quot;Health connector&quot; would misstate what was actually built and imply access to a framework (heart rate, sleep, workouts) that isn&#39;t there.</li><li>Tradeoff of the Pedometer path: it only ever gives step count (and on iOS, some derived distance/flights-climbed data) — it is not a foothold toward the broader Health framework. Getting there later still requires the entitlement/paid-account/custom-module work described above.</li></ul><h2 id="options-considered" tabindex="-1">Options considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options considered&quot;">​</a></h2><p><strong>A. Build against HealthKit/Health Connect now, for step count.</strong> Not viable today — blocked on the iOS entitlement/paid account; would also ship Android-only in the interim, which this repo&#39;s own &quot;both platforms&quot; bar (task 0.1&#39;s review checklist) treats as incomplete.</p><p><strong>B. Build step count via <code>expo-sensors</code>&#39; Pedometer, named and scoped explicitly as &quot;step count,&quot; not &quot;Health data.&quot; Defer the broader Health connector until a paid Apple Developer Program membership exists.</strong></p><p><strong>C. Park all health/step work until the paid-account question resolves, to avoid shipping two different things under one name.</strong></p><h2 id="recommendation" tabindex="-1">Recommendation <a class="header-anchor" href="#recommendation" aria-label="Permalink to &quot;Recommendation&quot;">​</a></h2><p>Option B. It delivers the stated MVP today, on both platforms, with no entitlement blocker, using an already-installable package — but it should be named and scoped as a step-count feature, not marketed or documented as a &quot;Health connector,&quot; so the name doesn&#39;t imply HealthKit/Health Connect access that hasn&#39;t been built.</p><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><ul><li>Step count ships via <code>expo-sensors</code> Pedometer, not HealthKit/Health Connect.</li><li>The broader Health connector (heart rate, sleep, workouts — genuine HealthKit/Health Connect access) is parked until a paid Apple Developer Program membership exists. No epic file for that broader scope until then.</li><li>Likely modeled as a read-only query tool (e.g. <code>device.get_step_count</code>), not a create/update/delete surface like Calendar or Tasks.</li><li><strong>Resolved, per <a href="./0005-calendar-connector.html">0005</a>&#39;s reading of the actual implementation:</strong> this is a Tier 3 connector — a native sensor read, not an HTTP call, same as Calendar and Device. It shares the same unbuilt prerequisite (Tier 3 manifest schema variant, generalized grant scope, <code>case 3</code> runtime dispatch), plus one wrinkle the other two don&#39;t have: it&#39;s read-only, so its &quot;scope&quot; is closer to a capability name than an origin-shaped permission — worth confirming the generalized grant-scope design (settled once, in the shared Tier 3 work) actually covers a read-only capability cleanly, not just write-oriented ones like <code>calendar.write</code> or <code>device.set_torch</code>.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Android step-counter sensor reliability/availability varies by OEM (flagged in the earlier phone-capabilities conversation) — worth a real multi-device check before claiming platform parity.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><p><strong>Not yet scheduled</strong>, unlike Calendar and Device: those two were prioritized into <a href="./../../ROADMAP.html">ROADMAP.md</a> Phase 2 (0.2.2, 0.2.3) with their own epics (<a href="./../epics/mobile/calendar-connector.html">calendar-connector.md</a>, <a href="./../epics/mobile/device-connector.html">device-connector.md</a>); step-count was not picked up in that pass. It is blocked on the same task <a href="./../epics/mobile/connector-framework.html#-26--tier-3-connector-scaffolding">2.6</a> (Tier 3 scaffolding) either way, and — per the Decisions above — is smaller and lower-risk than Calendar once that lands. Whoever picks this up next should write its own epic (mirroring calendar-connector.md / device-connector.md) and give it a ROADMAP slot at that point, rather than assuming one already exists.</p>',19)])])}const m=t(i,[["render",s]]);export{u as __pageData,m as default};
