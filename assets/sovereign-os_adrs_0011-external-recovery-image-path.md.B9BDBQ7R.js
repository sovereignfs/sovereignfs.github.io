import{_ as a,o as t,c as o,a2 as i}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"ADR-0011: External Recovery-Image Path for a Fully Unbootable Device","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/adrs/0011-external-recovery-image-path.md"},"headers":[],"relativePath":"sovereign-os/adrs/0011-external-recovery-image-path.md","filePath":"sovereign-os/adrs/0011-external-recovery-image-path.md"}'),r={name:"sovereign-os/adrs/0011-external-recovery-image-path.md"};function n(s,e,c,d,l,h){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="adr-0011-external-recovery-image-path-for-a-fully-unbootable-device" tabindex="-1">ADR-0011: External Recovery-Image Path for a Fully Unbootable Device <a class="header-anchor" href="#adr-0011-external-recovery-image-path-for-a-fully-unbootable-device" aria-label="Permalink to &quot;ADR-0011: External Recovery-Image Path for a Fully Unbootable Device&quot;">​</a></h1><p><strong>Status:</strong> Accepted <strong>Date:</strong> 2026-08-07 <strong>Decision owner:</strong> Project creator <strong>Related RFCs:</strong> <a href="./../rfcs/0016-full-base-os-updates.html">RFC-0016</a><strong>Related ADRs:</strong> None <strong>Related milestone:</strong> ROADMAP item 6, &quot;Full Base-OS Updates&quot; <strong>Supersedes:</strong> None</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>RFC-0016 names &quot;an external recovery-image path&quot; as one of the design requirements for full base-OS updates, but explicitly left it undesigned: its own Failure and Recovery section flags the both-slots-unbootable case as needing &quot;real physical-recovery implications... is the backstop here and needs its own design pass,&quot; and its Unresolved Questions section asks outright what that path &quot;actually looks like — this RFC&#39;s <code>tryboot</code> fallback covers boot failure, not both-slots-corrupted or physical-media failure.&quot;</p><p>Today&#39;s actual baseline, already shipped and documented in <a href="./../operations/update-recovery-and-compatibility.html">update-recovery-and-compatibility.md</a>: if the device won&#39;t boot to a usable state at all, the only recovery path is reflashing the SD card with a known-good image — which wipes the persistent DATA partition entirely (Pi-hole configuration, gravity database, admin credentials, everything). That page says plainly: <em>&quot;There is currently no tested &#39;reflash, then restore my data&#39; path; only in-place restore during a live device has been qualified.&quot;</em></p><h3 id="why-this-can-t-be-a-partition-on-the-disk-itself" tabindex="-1">Why this can&#39;t be a partition on the disk itself <a class="header-anchor" href="#why-this-can-t-be-a-partition-on-the-disk-itself" aria-label="Permalink to &quot;Why this can&#39;t be a partition on the disk itself&quot;">​</a></h3><p>RFC-0016&#39;s own Compatibility and Migration section already established the constraint that rules out an on-device recovery partition: <code>data</code> grows via <code>growpart</code> at first boot to consume every remaining byte of physical media after boot and root, so there is no unclaimed space left for a third thing once a device has completed first boot — on <em>any</em> device flashed with <em>any</em> image shipped to date, not something specific to this ADR. &quot;External&quot; in RFC-0016&#39;s own naming is therefore not incidental phrasing: recovery has to live on genuinely separate media (a different SD card or USB stick an operator brings to the device), not anywhere on the device&#39;s own storage.</p><h3 id="what-s-actually-missing-once-that-s-accepted" tabindex="-1">What&#39;s actually missing, once that&#39;s accepted <a class="header-anchor" href="#what-s-actually-missing-once-that-s-accepted" aria-label="Permalink to &quot;What&#39;s actually missing, once that&#39;s accepted&quot;">​</a></h3><p>Given recovery must be external, two separate questions remain:</p><ol><li>What <em>is</em> the external recovery image? Does this project need to design, build, and maintain a dedicated minimal recovery OS distinct from its normal distributable image?</li><li>Once a device is reflashed, can an operator get their data back, or is a full recovery always also a full data loss?</li></ol><p>On (2): <code>sovereign-update backup</code>/<code>restore</code> already exist (RFC-0014) and were re-examined for this ADR. A backup directory (<code>STATE_ROOT/backups/&lt;backup_id&gt;/</code>) is already fully self-contained — a handful of <code>.tar.zst</code> archives (Pi-hole state, Sovereign configuration, secrets, a release pointer) plus a <code>backup-manifest.json</code> — and <code>restore_backup</code>&#39;s validation (<code>validate_backup_manifest</code>, <code>verify_artifact</code>) checks purely by schema version, declared appliance version, Pi-hole digest, and per-artifact SHA-256, with <strong>no binding to device identity</strong> (no machine-id, no hostname, nothing host-specific) in either the archive contents or the validation logic. Nothing about <code>restore</code> as it exists today assumes the backup was ever on <em>this</em> device, or even that the device was never reflashed in between — it only insists the <em>appliance version</em> the backup came from is compatible with the currently-installed one (or <code>--force</code>, already qualified against a genuine version mismatch — see the <a href="./../research/production-signed-restore-qualification-report.html">production-signed restore qualification report</a>).</p><p>That means a backup taken before a failure, copied off-device (<code>scp</code> to an operator&#39;s own machine — already-existing, ordinary file transfer, nothing new to build), can in principle be copied back onto a freshly-reflashed device&#39;s <code>/data/sovereign/backups/&lt;backup_id&gt;/</code> and fed straight into the existing <code>sovereign-update restore &lt;backup_id&gt;</code> — no new subcommand, no new validation logic, no new trust boundary. This has never been exercised end-to-end, which is exactly the gap update-recovery-and-compatibility.md already names honestly.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Two decisions, addressing the two questions above:</strong></p><ol><li><strong>No dedicated recovery-only OS or image.</strong> The existing distributable Sovereign OS image, flashed via Raspberry Pi Imager — the same mechanism <a href="./../operations/raspberry-pi-imager-provisioning.html">docs/operations/raspberry-pi-imager-provisioning.md</a> already documents for first-time setup — <em>is</em> the external recovery image. &quot;External recovery-image path&quot; is satisfied by the artifact this project already builds and ships on every release; it does not name a new thing to design or maintain.</li><li><strong>Close the untested reflash-then-restore gap</strong> by formalizing and hardware-qualifying the off-device backup round trip described above: take a backup (<code>sovereign-update backup &lt;transaction-id&gt;</code>), copy the resulting <code>STATE_ROOT/backups/&lt;backup_id&gt;/</code> directory off the device to separate storage the operator controls, reflash, copy that directory back onto the fresh device at the same path, and run the existing <code>sovereign-update restore &lt;backup_id&gt;</code> unmodified. No code changes to <code>backup</code>/<code>restore</code> are anticipated; this decision is about proving and documenting a procedure that already-shipped machinery supports, not building new machinery.</li></ol><p>Implementation and hardware qualification of (2) are <strong>out of scope for this ADR</strong> — this document only resolves the design question RFC-0016 left open. A follow-up qualification pass (mirroring this project&#39;s existing discipline: real device, real reflash, real restore, a written report) is the next step once this decision is accepted.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="a-dedicated-minimal-recovery-only-os-image" tabindex="-1">A dedicated, minimal recovery-only OS image <a class="header-anchor" href="#a-dedicated-minimal-recovery-only-os-image" aria-label="Permalink to &quot;A dedicated, minimal recovery-only OS image&quot;">​</a></h3><p>A stripped-down image built solely for recovery (no Pi-hole, no Console, just enough to run recovery/restore tooling), separate from the normal distributable image.</p><ul><li><strong>Rejected.</strong> Doubles the image-build surface this single-maintainer, single-hardware-target project has to build, sign, and keep current forever, for no capability the existing distributable image doesn&#39;t already have — a fresh flash of the normal image already produces a fully working device, which is the actual recovery goal. This is the same disproportionate-maintenance-cost reasoning ADR-0006 applied to reject cloud KMS and ADR-0010 applied to reject a real SSO identity provider.</li></ul><h3 id="network-pxe-boot-recovery" tabindex="-1">Network (PXE) boot recovery <a class="header-anchor" href="#network-pxe-boot-recovery" aria-label="Permalink to &quot;Network (PXE) boot recovery&quot;">​</a></h3><p>Recover a bricked device by netbooting a recovery environment over the LAN instead of requiring physical media.</p><ul><li><strong>Rejected.</strong> Raspberry Pi 5&#39;s PXE/network boot path is a real EEPROM capability, but stands up new infrastructure (a DHCP/TFTP or PXE server) this appliance&#39;s single-household deployment model has no other use for, and conflicts with this project&#39;s offline-first, self-hosted values in the same way ADR-0004 already reasoned about external dependencies for the assistant/search milestone. Reflashing from an operator&#39;s own machine, already documented and already the supported path, needs no new server infrastructure at all.</li></ul><h3 id="an-on-device-recovery-partition" tabindex="-1">An on-device recovery partition <a class="header-anchor" href="#an-on-device-recovery-partition" aria-label="Permalink to &quot;An on-device recovery partition&quot;">​</a></h3><p>Reserve space at build time for a small recovery partition alongside <code>bootconfig</code>/<code>boot_a</code>/<code>boot_b</code>/<code>system_a</code>/<code>system_b</code>/<code>data</code>.</p><ul><li><strong>Rejected outright, not a judgment call.</strong> RFC-0016&#39;s Compatibility and Migration section already establishes that <code>data</code> consumes every byte left after boot and root at first boot — there is no space left for a seventh partition once any device has completed first boot, independent of this ADR&#39;s own reasoning. Reserving space for it at build time would mean permanently shrinking every device&#39;s usable <code>data</code> capacity for a partition most devices will never need.</li></ul><h3 id="build-a-new-sovereign-update-export-backup-import-backup-convenience-wrapper-now" tabindex="-1">Build a new <code>sovereign-update export-backup</code>/<code>import-backup</code> convenience wrapper now <a class="header-anchor" href="#build-a-new-sovereign-update-export-backup-import-backup-convenience-wrapper-now" aria-label="Permalink to &quot;Build a new `sovereign-update export-backup`/`import-backup` convenience wrapper now&quot;">​</a></h3><p>Script the copy-off/copy-back steps instead of leaving them as plain <code>scp</code>/<code>rsync</code> in an operator runbook.</p><ul><li><strong>Deferred, not rejected.</strong> Worth doing once the manual procedure is actually qualified and its exact steps are known precisely — writing convenience tooling around an unproven procedure risks automating the wrong thing. Revisit after the qualification pass this ADR sets up.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Resolves RFC-0016&#39;s last named-but-undesigned requirement with no new artifact to build, sign, or maintain — the existing release pipeline already produces the &quot;external recovery image.&quot;</li><li>Turns &quot;reflash always means total data loss&quot; into &quot;reflash means data loss only if you skipped taking an off-device backup first&quot; — a real, meaningful improvement using machinery this project has already built and qualified for a different purpose (in-place restore).</li><li>No new trust boundary, validation logic, or attack surface: <code>restore</code>&#39;s existing content-only validation (no device-identity binding) already supports this use case without modification.</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li>Still fundamentally destructive: a full reflash always wipes root and DATA immediately; recovery only works if the operator took a backup <em>before</em> the failure and stored it somewhere the failed device&#39;s own wipe can&#39;t reach. A device that fails without ever having had an off-device backup taken is no better off than today.</li><li>The copy-off/copy-back steps are manual (<code>scp</code>/<code>rsync</code> by hand) until the deferred convenience-wrapper alternative above is revisited — real friction for an operator recovering from an actual failure, not just a qualification nicety.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li><strong>Resolved:</strong> this ADR&#39;s claim about <code>restore</code>&#39;s validation being device-identity-agnostic was based on reading source at the time of writing; the 2026-08-08/09 qualification pass (see Validation and Revisit Conditions) confirmed it directly against a real reflash and a real restored device, not just analysis.</li><li>If a future change to <code>backup</code>/<code>restore</code> ever <em>does</em> introduce device-identity binding (for a reason unrelated to this ADR), it would silently break this recovery path without necessarily being flagged as a regression against a &quot;supported&quot; feature, since none of today&#39;s tests exercise the cross-device case this ADR relies on.</li></ul><h2 id="validation-and-revisit-conditions" tabindex="-1">Validation and Revisit Conditions <a class="header-anchor" href="#validation-and-revisit-conditions" aria-label="Permalink to &quot;Validation and Revisit Conditions&quot;">​</a></h2><p><strong>Accepted (2026-08-07, project creator).</strong> Direction approved as written. <strong>Hardware-qualified (2026-08-08/09).</strong> The full backup-off/reflash/backup-back/restore round trip was exercised for real on the qualification device — a real backup, copied off-device, a real destructive reflash (via a newly-added CI artifact for the actual flashable A/B image, since none existed before), and a real <code>restore --force</code> against the version mismatch a genuine reflash produces. Every persisted file (Pi-hole state, secrets) was independently verified byte-for-byte against the original backup, not just trusted from the tool&#39;s own reported status, and the restored device&#39;s actual DNS service was confirmed working. Zero code changes to <code>backup</code>/<code>restore</code> were needed — this ADR&#39;s claim about their device-identity-agnostic validation held up under real conditions. See the <a href="./../research/external-recovery-backup-restore-qualification-report.html">external recovery backup/restore qualification report</a> for the full account.</p><p>Revisit this ADR if: the manual copy-off/copy-back friction proves unacceptable in practice (promote the deferred convenience-wrapper alternative), or if <code>restore</code>&#39;s validation logic ever needs to change in a way that would reintroduce device-identity binding this ADR currently relies on it not having.</p>',39)])])}const f=a(r,[["render",n]]);export{u as __pageData,f as default};
