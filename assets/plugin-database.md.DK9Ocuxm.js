import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"Plugin database guide","description":"","frontmatter":{"docSection":"app-developers","docType":"reference","audiences":["app-developer"],"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/plugin-database.md"},"headers":[],"relativePath":"plugin-database.md","filePath":"plugin-database.md"}'),n={name:"plugin-database.md"};function l(o,e,r,d,p,h){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="plugin-database-guide" tabindex="-1">Plugin database guide <a class="header-anchor" href="#plugin-database-guide" aria-label="Permalink to &quot;Plugin database guide&quot;">​</a></h1><p>Sovereign gives every plugin access to a Drizzle client through <code>sdk.db.getClient()</code>. Every plugin gets its own <strong>dedicated store</strong> — an sqld namespace or a Postgres schema. There is no per-plugin choice to make, and no <code>database</code> field left in the manifest at all — see <a href="#database">Database</a>.</p><p><code>type: &quot;platform&quot;</code> plugins (<code>account</code>, <code>console</code>, <code>launcher</code>) are the one exception — see <a href="#platform-type-plugins">Platform-type plugins</a>.</p><hr><h2 id="database" tabindex="-1">Database <a class="header-anchor" href="#database" aria-label="Permalink to &quot;Database&quot;">​</a></h2><h3 id="what-the-platform-does" tabindex="-1">What the platform does <a class="header-anchor" href="#what-the-platform-does" aria-label="Permalink to &quot;What the platform does&quot;">​</a></h3><p>On the <strong>first request</strong> where a plugin route calls <code>sdk.db.getClient()</code>, the runtime:</p><ol><li>Checks the plugin&#39;s manifest <code>type</code> — anything other than <code>platform</code> gets a dedicated store.</li><li>Provisions the store (idempotent — safe to call repeatedly).</li><li>Returns a dedicated Drizzle client.</li></ol><p><strong>Provisioning by dialect:</strong></p><table tabindex="0"><thead><tr><th>Dialect</th><th>What gets created</th></tr></thead><tbody><tr><td>SQLite</td><td>A dedicated <strong>sqld namespace</strong> (RFC 0091, workstream 0009 leg 3), created via sqld&#39;s admin API. Namespace name: same slug rules as the Postgres schema below — e.g. <code>io.example.tasks</code> → <code>plugin_io_example_tasks</code>. sqld is a required part of a SQLite deployment, not an optional extra — no plain-file fallback.</td></tr><tr><td>Postgres</td><td><code>CREATE SCHEMA IF NOT EXISTS &quot;plugin_&lt;slug&gt;&quot;</code> on the same server as the platform DB (<code>POSTGRES_DB_URL</code>). Schema name: plugin id with <code>.</code>/<code>-</code> → <code>_</code>, prefixed with <code>plugin_</code> — e.g. <code>io.example.tasks</code> → <code>plugin_io_example_tasks</code>. The runtime pool sets <code>search_path</code> on connection (startup option, not a <code>SET</code> after connecting).</td></tr></tbody></table><p>Subsequent <code>sdk.db.getClient()</code> calls return the same cached client — provisioning only runs once per process.</p><h3 id="sdk-usage" tabindex="-1">SDK usage <a class="header-anchor" href="#sdk-usage" aria-label="Permalink to &quot;SDK usage&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sdk } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sovereignfs/sdk&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> db</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>You do NOT need to pass the plugin ID — the runtime reads the <code>x-sovereign-plugin-id</code> header that the middleware injects on every plugin route.</p><p><strong>Outside a request context</strong> (e.g. background scripts, server startup hooks): the header is absent and the platform DB is returned as a fallback. If you need the dedicated client outside a request, provision it explicitly using the internal <code>packages/db</code> helpers — but this is an unusual case.</p><h3 id="no-slug-prefix-required" tabindex="-1">No slug prefix required <a class="header-anchor" href="#no-slug-prefix-required" aria-label="Permalink to &quot;No slug prefix required&quot;">​</a></h3><p>Table names don&#39;t need a slug prefix. Your schema can use simple names:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// db/schema.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sqliteTable, text, integer } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;drizzle-orm/sqlite-core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lists</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sqliteTable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;lists&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  id: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primaryKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  userId: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tenantId: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tenant_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// still required</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createdAt: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;created_at&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h3 id="rules" tabindex="-1">Rules <a class="header-anchor" href="#rules" aria-label="Permalink to &quot;Rules&quot;">​</a></h3><ul><li><strong><code>tenant_id</code> on every user-scoped table</strong> (multi-tenancy readiness). v1 is single-tenant, but the column is required from day one — tables without it will need a migration later.</li><li><strong>Application code queries through one dialect&#39;s schema — usually <code>sqlite-core</code> — and that&#39;s fine.</strong> <code>sdk.db.getClient()</code> returns a client whose query-builder dialect (<code>node-postgres</code> vs <code>better-sqlite3</code>) is bound to the connection, not to the table object passed to <code>.from()</code>/<code>.insert()</code>. A <code>sqliteTable()</code>-defined table works correctly against a Postgres-backed client at query time (verified empirically) <strong>as long as the physical Postgres columns use types that serialize identically</strong>: plain <code>integer</code> for booleans, IDs, and timestamps — never native Postgres <code>boolean</code> or <code>bigint</code> — matching what Drizzle&#39;s SQLite column mappers already produce. Reaching for a native Postgres type here breaks writes at runtime the first time a boolean or bigint column round-trips through the mismatched serializer.</li><li><strong>You still need a genuine, separate Postgres schema file to generate Postgres migrations from.</strong> <code>drizzle-kit generate --dialect postgresql</code> cannot read a <code>sqliteTable()</code>-based schema file — it silently reports zero tables found. Keep a second file (e.g. <code>db/schema.postgres.ts</code>, using <code>pgTable</code>/<code>integer</code>, a structural mirror of your real schema) whose only job is driving migration generation; application code never imports it. See <code>packages/db/src/schema/{sqlite,postgres}/platform.ts</code> for the platform&#39;s own reference pair — note the platform&#39;s own Postgres schema uses native <code>boolean</code>/<code>bigint</code> because the platform&#39;s <em>own</em> query code is dialect-aware (<code>packages/db/src/exec.ts</code>); a plugin whose application code is not dialect-aware must not copy that part of the pattern.</li></ul><p>There is currently no application-level at-rest encryption option for a plugin&#39;s store, on either dialect — a prior SQLite-only opt-in (<code>database.requireEncryption</code>, RFC 0071) For protecting sensitive <em>fields</em> against a database operator, use app-level field encryption (RFC 0092) — see <code>docs/plugin-development.md</code>&#39;s &quot;Adopting field encryption&quot; checklist and the <code>example-plugins/example-encrypted</code> reference. was retired along with the rest of the <code>database</code> manifest field. Rely on disk/volume-level encryption if this matters for your deployment.</p><h3 id="migrations" tabindex="-1">Migrations <a class="header-anchor" href="#migrations" aria-label="Permalink to &quot;Migrations&quot;">​</a></h3><p>Place migration files at:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/&lt;id&gt;/migrations/sqlite/    # SQLite migration SQL files</span></span>
<span class="line"><span>plugins/&lt;id&gt;/migrations/postgres/  # Postgres migration SQL files</span></span></code></pre></div><p>This is the same layout as <code>packages/db/migrations/</code>. The platform runs these at startup (before handling any requests) via Drizzle&#39;s migrator, routed to the plugin&#39;s dedicated store.</p><p><strong>Each store tracks its own applied migrations</strong> in a <code>__drizzle_migrations</code> table inside that store — completely independent of the platform DB&#39;s migration log. Version state is per-database.</p><p><strong>Generating migrations with drizzle-kit:</strong> run from your plugin&#39;s own repo, once per dialect, <strong>each against its own schema file</strong> — <code>drizzle-kit generate --dialect postgresql</code> cannot read a <code>sqliteTable()</code>-based schema (it silently reports zero tables found), so a genuine second <code>pgTable</code>-based file is required:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># SQLite — from db/schema.ts (the schema application code queries against):</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> drizzle-kit</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --schema</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db/schema.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrations/sqlite</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dialect</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sqlite</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Postgres — from a separate db/schema.postgres.ts, structurally mirroring</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># db/schema.ts but never using native Postgres boolean/bigint (see above):</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> drizzle-kit</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --schema</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db/schema.postgres.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrations/postgres</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dialect</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> postgresql</span></span></code></pre></div><p>Review the generated SQL before committing. Migration files are committed to your plugin&#39;s own source repository.</p><p><strong>Startup order:</strong> platform migrations run first, then per-plugin migrations in registry order. A failed plugin migration is logged but does not abort startup — the compatibility check still gates access to the plugin&#39;s routes.</p><hr><h2 id="lifecycle" tabindex="-1">Lifecycle <a class="header-anchor" href="#lifecycle" aria-label="Permalink to &quot;Lifecycle&quot;">​</a></h2><h3 id="provision" tabindex="-1">Provision <a class="header-anchor" href="#provision" aria-label="Permalink to &quot;Provision&quot;">​</a></h3><p>The store is provisioned <strong>lazily on first <code>sdk.db.getClient()</code> call</strong> from within a plugin route. Provisioning is also run explicitly at startup by <code>instrumentation.ts</code> for plugins with a <code>migrations/</code> folder (so migrations land before the first request).</p><h3 id="uninstall" tabindex="-1">Uninstall <a class="header-anchor" href="#uninstall" aria-label="Permalink to &quot;Uninstall&quot;">​</a></h3><p><code>sv plugin remove &lt;id&gt;</code> drops the plugin directory <strong>and</strong> its dedicated store:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plugin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remove</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> io.example.tasks</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # remove + drop store</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plugin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remove</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> io.example.tasks</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --keep-data</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # remove, keep the store</span></span></code></pre></div><p>What &quot;drop&quot; means by dialect:</p><ul><li><strong>SQLite</strong> — drops the plugin&#39;s sqld namespace via sqld&#39;s admin API (<code>DELETE /v1/namespaces/&lt;ns&gt;</code>). There is no file on disk to delete.</li><li><strong>Postgres</strong> — runs <code>DROP SCHEMA &quot;plugin_io_example_tasks&quot; CASCADE</code>, which deletes all tables and indexes in that schema.</li></ul><p><strong><code>--keep-data</code></strong> retains the store. Useful when you want to inspect the data before deleting, migrate it elsewhere, or reinstall the plugin with its history intact. The namespace/schema itself is retained on the server (see <a href="./self-hosting.html#sqld-libsql-server-rfc-0091">self-hosting.md&#39;s sqld section</a> for how sqld&#39;s data is backed up).</p><h3 id="reinstall" tabindex="-1">Reinstall <a class="header-anchor" href="#reinstall" aria-label="Permalink to &quot;Reinstall&quot;">​</a></h3><p>If you reinstall a plugin after removing it <strong>with <code>--keep-data</code></strong>, the existing store is reused automatically. Migrations that have already been applied are skipped (tracked in the store&#39;s <code>__drizzle_migrations</code>).</p><hr><h2 id="backup-and-restore" tabindex="-1">Backup and restore <a class="header-anchor" href="#backup-and-restore" aria-label="Permalink to &quot;Backup and restore&quot;">​</a></h2><h3 id="sqlite" tabindex="-1">SQLite <a class="header-anchor" href="#sqlite" aria-label="Permalink to &quot;SQLite&quot;">​</a></h3><p>Every plugin&#39;s data lives in sqld, backed up and restored as part of its own named volume (<code>sovereign_sqld_data</code>), not per-namespace — see <a href="./self-hosting.html#sqld-libsql-server-rfc-0091">self-hosting.md&#39;s sqld section</a>. <code>data/</code> now holds only uploaded files (avatars), no databases.</p><h3 id="postgres" tabindex="-1">Postgres <a class="header-anchor" href="#postgres" aria-label="Permalink to &quot;Postgres&quot;">​</a></h3><p>Plugin schemas (<code>plugin_*</code>) live in the <strong>same Postgres database</strong> as the platform. A full <code>pg_dump</code> of the database captures them. To dump only one plugin&#39;s schema:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pg_dump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --schema</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;plugin_io_example_tasks&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$POSTGRES_DB_URL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> io_example_tasks_backup.sql</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Restore</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">psql</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$POSTGRES_DB_URL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> io_example_tasks_backup.sql</span></span></code></pre></div><hr><h2 id="cross-plugin-data" tabindex="-1">Cross-plugin data <a class="header-anchor" href="#cross-plugin-data" aria-label="Permalink to &quot;Cross-plugin data&quot;">​</a></h2><p>Dedicated stores make SQL joins across plugins <strong>impossible</strong> — each plugin has its own database or schema and cannot reference another plugin&#39;s tables directly. This reinforces the SDK boundary.</p><p>If one plugin needs data from another, use the consent-gated <code>sdk.data</code> mechanism (RFC 0002):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Provider plugin registers a resolver:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.data.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provide</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;io.example.tasks:summary&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Consumer plugin queries it (user must have granted consent):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.data.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { providerId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;io.example.tasks&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, contract: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;summary&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, version: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  params,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>See <a href="./plugin-development.html"><code>docs/plugin-development.md</code></a> → Cross-plugin data sharing.</p><hr><h2 id="platform-type-plugins" tabindex="-1">Platform-type plugins <a class="header-anchor" href="#platform-type-plugins" aria-label="Permalink to &quot;Platform-type plugins&quot;">​</a></h2><p><code>account</code>, <code>console</code>, and <code>launcher</code> (<code>type: &quot;platform&quot;</code>) are the one exception to everything above. They administer the platform&#39;s own core data directly — the same as <code>apps/auth</code> — rather than owning data of their own: Console owns no tables at all (it reads and writes existing platform tables like <code>users</code>/<code>plugin_status</code> through <code>@sovereignfs/db</code> directly, not <code>sdk.db.getClient()</code>); Account&#39;s <code>account_prefs</code> table is defined in the platform&#39;s own schema file (<code>packages/db/src/schema/*/platform.ts</code>), not a plugin-owned one; Launcher has no database code at all. <code>manifestDatabaseIsolation()</code> resolves <code>type: &quot;platform&quot;</code> to <code>&quot;shared&quot;</code> — the platform DB is returned instead of a dedicated store — but in practice this branch is unreachable today, since none of the three declare a <code>migrations/</code> folder. This isn&#39;t something a third-party plugin author configures; it only applies to first-party plugins in this monorepo.</p><hr><h2 id="internals" tabindex="-1">Internals <a class="header-anchor" href="#internals" aria-label="Permalink to &quot;Internals&quot;">​</a></h2><p>This section is for contributors and advanced plugin authors who want to understand how the plumbing works.</p><h3 id="how-sdk-db-getclient-routes-requests" tabindex="-1">How <code>sdk.db.getClient()</code> routes requests <a class="header-anchor" href="#how-sdk-db-getclient-routes-requests" aria-label="Permalink to &quot;How \`sdk.db.getClient()\` routes requests&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Plugin route handler calls sdk.db.getClient()</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>packages/sdk/src/db.ts reads x-sovereign-plugin-id from Next.js headers()</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>Passes pluginId to SdkHost.db.getClient(pluginId)</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>runtime/src/sdk-host.ts:</span></span>
<span class="line"><span>  - pluginId is null? → return platform DB</span></span>
<span class="line"><span>  - manifest.type !== &#39;platform&#39;? → provisionPluginDb() + return getPluginDb().db</span></span>
<span class="line"><span>  - otherwise (type: &quot;platform&quot;) → return platform DB</span></span></code></pre></div><h3 id="per-plugin-client-registry" tabindex="-1">Per-plugin client registry <a class="header-anchor" href="#per-plugin-client-registry" aria-label="Permalink to &quot;Per-plugin client registry&quot;">​</a></h3><p><code>packages/db/src/plugin-client.ts</code> maintains an in-process Map keyed by plugin ID. <code>getPluginDb(pluginId)</code> creates the client on first call and returns the cached instance on subsequent calls. The registry is reset on server restart (which is fine — the store persists on disk; the client is reconstructed on next access).</p><h3 id="postgres-search-path" tabindex="-1">Postgres <code>search_path</code> <a class="header-anchor" href="#postgres-search-path" aria-label="Permalink to &quot;Postgres \`search_path\`&quot;">​</a></h3><p>For a plugin&#39;s Postgres store, the runtime creates a separate <code>pg.Pool</code> pointing at the same <code>POSTGRES_DB_URL</code> but with <code>search_path</code> pinned to <code>&quot;plugin_&lt;slug&gt;&quot;</code> via the connection&#39;s startup options (<code>-c search_path=...</code>) — part of the connection handshake itself, not a <code>SET</code> issued after connecting from a <code>pool.on(&#39;connect&#39;, ...)</code> handler (that pattern has a real race: the handler fires when the socket connects, but the pool doesn&#39;t wait for its callback to finish before handing the connection to whichever query is waiting). This means all unqualified table names in queries resolve to the plugin&#39;s schema from the first statement. The platform pool is unaffected.</p><h3 id="migration-runner" tabindex="-1">Migration runner <a class="header-anchor" href="#migration-runner" aria-label="Permalink to &quot;Migration runner&quot;">​</a></h3><p><code>runtime/src/plugin-migrations.ts</code> (called from <code>instrumentation.ts</code>) iterates the registry, finds every plugin with a <code>migrations/&lt;dialect&gt;/</code> folder, and calls <code>runPluginMigrations(pluginDb, folder, migrationsTable?)</code> from <code>packages/db/src/migrate.ts</code>. That function calls Drizzle&#39;s built-in <code>migrate()</code> for the appropriate dialect, which creates the migrations-tracking table on first run and tracks applied files.</p><p>For SQLite plugins, <code>migrationsTable</code> is omitted — Drizzle&#39;s default <code>__drizzle_migrations</code> is fine since each store is already its own dedicated database file, with no collision risk. For <strong>Postgres</strong> plugins, the runner also passes <code>pluginMigrationsTableName(manifest.id)</code> — Drizzle&#39;s node-postgres migrator tracks applied migrations in a table living in a <em>fixed</em> <code>drizzle</code> schema regardless of the connecting pool&#39;s <code>search_path</code>, so every Postgres plugin left on the default table name would otherwise share one tracking table across <em>all</em> of them (see task 8.26 — a real incident where a second plugin&#39;s migrations were silently skipped as &quot;already applied&quot;). This matters because Drizzle&#39;s migrator (<code>drizzle-orm/*/migrator</code>) tracks &quot;already applied&quot; by comparing a migration&#39;s own timestamp against only the single most recent <code>created_at</code> row in the table — not a per-migration or per-plugin hash lookup. Two independent migration histories sharing one table would let whichever has later timestamps make the other look already-applied and skip silently, forever.</p><h3 id="foreign-keys-in-a-postgres-schema" tabindex="-1">Foreign keys in a Postgres schema <a class="header-anchor" href="#foreign-keys-in-a-postgres-schema" aria-label="Permalink to &quot;Foreign keys in a Postgres schema&quot;">​</a></h3><p><code>drizzle-kit generate --dialect postgresql</code> always qualifies a generated <code>FOREIGN KEY</code> constraint&#39;s target table with the schema the <code>pgTable()</code> was declared in — which is <code>public</code> by default, since <code>db/schema.postgres.ts</code> never declares an explicit <code>pgSchema()</code> (see &quot;You still need a genuine, separate Postgres schema file&quot; above). At runtime a plugin&#39;s tables never live in <code>public</code> — they live in <code>plugin_&lt;slug&gt;</code>, reached only via the connection&#39;s <code>search_path</code> (see above) — so a generated <code>ALTER TABLE ... REFERENCES &quot;public&quot;.&quot;other_table&quot;(...)</code> will always fail with <code>relation &quot;public.other_table&quot; does not exist</code> the first time a plugin&#39;s schema has a foreign key between two of its own tables. Drizzle wraps each migration file in one transaction, so this failure rolls back every statement in the same file, including any <code>CREATE TABLE</code>s that came before it — no partial state, but no tables either. Fix: after generating, manually strip the schema qualifier from any <code>REFERENCES &quot;public&quot;.&quot;...&quot;</code> down to an unqualified <code>REFERENCES &quot;...&quot;</code> — the unqualified name resolves correctly through <code>search_path</code> for both the plugin&#39;s own runtime queries and the migration itself. Re-check this by hand after every <code>drizzle-kit generate</code> that touches a foreign key; the generator has no schema awareness and will keep re-adding the qualifier.</p>`,72)])])}const k=s(n,[["render",l]]);export{g as __pageData,k as default};
