import{_ as o,o as t,c as a,a2 as i}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0079 — Mobile PWA layout, overlay, and gesture consistency","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0079-mobile-pwa-layout-overlay-gesture-consistency.md","filePath":"rfcs/0079-mobile-pwa-layout-overlay-gesture-consistency.md"}'),n={name:"rfcs/0079-mobile-pwa-layout-overlay-gesture-consistency.md"};function s(d,e,c,l,r,p){return t(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="rfc-0079-—-mobile-pwa-layout-overlay-and-gesture-consistency" tabindex="-1">RFC 0079 — Mobile PWA layout, overlay, and gesture consistency <a class="header-anchor" href="#rfc-0079-—-mobile-pwa-layout-overlay-and-gesture-consistency" aria-label="Permalink to &quot;RFC 0079 — Mobile PWA layout, overlay, and gesture consistency&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> Claude Code<br><strong>Scope:</strong> <code>packages/ui</code> (new <code>PageContainer</code> component, internal refactor of <code>Dialog</code>/<code>Drawer</code>/<code>Sheet</code>/<code>ConfirmDialog</code>, new <code>useSwipeReveal</code>/<code>useSnapCarousel</code> hooks), <code>plugins/account</code>, <code>plugins/console</code> (the only plugins that live in this monorepo), <code>docs/design-system.md</code>, <code>docs/plugin-development.md</code>, <code>docs/architecture-rules.md</code>. Builds on RFC 0013 (mobile responsiveness &amp; PWA), RFC 0075 (mobile chrome toggle), RFC 0076 (DS sizing alignment), and the <code>docs/design-system-stabilization-proposal.md</code> review.<br><strong>Cross-repo note:</strong> <code>sovereign-tasks</code>, <code>sovereign-shopper</code>, and <code>sovereign-plainwrite</code> are externally-maintained plugins, each in its own repository and excluded from this monorepo by <code>.gitignore</code> (only <code>account/</code>, <code>console/</code>, <code>launcher/</code> are tracked here). This RFC&#39;s <code>packages/ui</code> deliverables (the hooks and the ConfirmDialog consolidation) are fully in scope and shippable from here; the plugin-side migrations onto them are out of scope for any branch in this repo and require separate PRs in each plugin&#39;s own repository once a new <code>@sovereignfs/ui</code> version ships.<br><strong>Incorporated into plan:</strong> Yes — epic tasks 9.18, 9.19, 9.20.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Three independent, non-breaking additions to <code>@sovereignfs/ui</code> to close gaps found in a design-system consistency review, each addressing a different symptom of the same root cause (reusable UI/UX capability implemented ad hoc per plugin instead of once in the design system):</p><ol><li>A <code>PageContainer</code> primitive that gives first-party plugins one documented way to constrain and pad their own main content, composing predictably with the shell&#39;s existing auto-padding instead of stacking on top of it.</li><li>An internal refactor consolidating <code>Dialog</code>, <code>Drawer</code>, and <code>Sheet</code>&#39;s duplicated scrim/focus-trap/Escape/scroll-lock code onto one shared internal primitive, with no public API change, plus migrating <code>sovereign-plainwrite</code>&#39;s stray local <code>ConfirmDialog</code> onto the shared one.</li><li>Two new gesture hooks, <code>useSwipeReveal</code> and <code>useSnapCarousel</code>, extracted from <code>sovereign-tasks</code>&#39;s and <code>sovereign-shopper</code>&#39;s independent, duplicated swipe-to-reveal and carousel-settle-detection implementations.</li></ol><p>None of these change a single existing public prop, token, or manifest field. All three are additive <code>packages/ui</code> exports plus first-party plugin migrations onto them.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>A design-system review (this session, see current-state citations below) found the same pattern three times: a capability that should live once in <code>packages/ui</code> instead exists as multiple independent, drifting implementations in plugin code or in supposedly-shared components. This directly contradicts the DS-first rule already written into <code>docs/architecture-rules.md</code> (&quot;Reusable UI/UX capability... is implemented in <code>packages/ui</code>... and consumed by plugins, never implemented plugin-locally &#39;to be promoted later&#39;&quot;), and <code>sovereign-tasks/CLAUDE.md</code> confirms this rule was already enforced once before for other primitives (an older local overlay/double-tap implementation was removed in favor of DS <code>Sheet</code>/ <code>useDoubleTap</code>) — the three items below are the same enforcement gap recurring in three new places.</p><p>Concretely, the developer-facing symptoms are:</p><ul><li>Plugin main-content padding/margin/max-width is different in every first-party plugin, with no documented convention for what a plugin should add on top of the shell&#39;s own padding — some plugins end up double-padded, some have none of their own, values range across five different max-widths and four different padding combinations.</li><li><code>Dialog</code>, <code>Drawer</code>, and <code>Sheet</code> each hand-roll their own scrim, focus trap, Escape handling, and scroll-lock wiring, with comments in the source acknowledging the duplication (e.g. &quot;see Dialog.tsx&#39;s identical comment for why&quot;). This isn&#39;t visibly broken today, but it&#39;s the kind of duplication that drifts: a fix to one&#39;s focus-trap edge case doesn&#39;t propagate to the other two.</li><li>Mobile swipe interaction has no shared hook, so <code>sovereign-tasks</code> has two separate hand-rolled swipe-to-reveal implementations in the same plugin, and <code>sovereign-tasks</code>/<code>sovereign-shopper</code> each independently reimplement the same scroll-snap carousel settle-detection.</li></ul><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><h3 id="layout" tabindex="-1">Layout <a class="header-anchor" href="#layout" aria-label="Permalink to &quot;Layout&quot;">​</a></h3><ul><li>The runtime shell already auto-applies outer content padding to every plugin: <code>runtime/app/(platform)/shell.module.css:144-154</code> — <code>.content { padding: var(--sv-space-8); }</code>, zeroed via <code>.content:has([data-plugin-fullbleed]) { padding: 0; }</code>; mobile override at <code>shell.module.css:270-278</code> drops this to <code>var(--sv-space-4)</code> plus a footer-height-aware <code>padding-bottom</code>.</li><li>No plugin imports a shared page-container component — every plugin layout (<code>plugins/*/app/layout.tsx</code> + its CSS module) is ad hoc local CSS. Observed values: 960px max-width + <code>var(--sv-space-8) var(--sv-space-6)</code> padding (ledger, wallet, healthlog — inconsistent on <code>margin: 0 auto</code>, wallet lacks it); 640px max-width (tritext); 1040px max-width stacked on top of the shell&#39;s own padding plus a second local padding layer (plainwrite — double-padded); no max-width, <code>var(--sv-space-6)</code> padding only (docs, sheets.local); no padding or max-width at all, relying entirely on shell padding (account, console, tally); a hardcoded <code>28rem</code> instead of a <code>--sv-space-*</code> token (sheets.local&#39;s inline form).</li><li><code>PageHeader</code> (<code>packages/ui/src/components/PageHeader/PageHeader.tsx</code>) only lays out a title/description/action row (<code>margin-bottom</code> only); it is not, and was never meant to be, a page wrapper. No <code>PageContainer</code>/<code>AppShell</code>/ <code>ContentLayout</code>-shaped component exists in <code>packages/ui</code> today (confirmed via directory listing and repo-wide grep).</li><li>Two plugins (<code>sovereign-tasks</code>, <code>sovereign-shopper</code>) opt out of shell padding entirely via <code>data-plugin-fullbleed</code> and manage their own full-bleed shell/sidebar/content grid — this mechanism is intentional and out of scope here; <code>PageContainer</code> is for the common case of a plugin that wants shell padding plus a readable content width, not for fullbleed plugins.</li></ul><h3 id="overlays" tabindex="-1">Overlays <a class="header-anchor" href="#overlays" aria-label="Permalink to &quot;Overlays&quot;">​</a></h3><ul><li><code>docs/design-system.md</code>&#39;s existing &quot;Overlay surfaces&quot; decision table (documented already, ~line 889) correctly assigns Dialog / ConfirmDialog / Drawer / Sheet / Menu to their jobs — this RFC does not change that table or any component&#39;s public API, only what happens inside each component.</li><li><code>Dialog</code> (<code>packages/ui/src/components/Dialog/Dialog.tsx</code>), <code>Drawer</code> (<code>.../Drawer/Drawer.tsx</code>), and <code>Sheet</code> (<code>.../Sheet/Sheet.tsx</code>) each independently implement: an in-tree (non-portal) scrim/panel, manual Tab/Shift-Tab focus-trap cycling with restore-on-close, a <code>keydown</code> Escape handler, and <code>lockBodyScroll</code>/<code>unlockBodyScroll</code> (<code>packages/ui/src/scroll-lock.ts</code>) inside an identical <code>useEffect</code> keyed on a <code>mounted</code> flag (to cover the exit-animation window). The three implementations are functionally equivalent but textually separate.</li><li><code>ConfirmDialog</code> (<code>.../ConfirmDialog/ConfirmDialog.tsx</code>) is deliberately built on the native <code>&lt;dialog&gt;</code> element instead (<code>showModal()</code>/<code>.close()</code>) and is explicitly out of scope for consolidation — native <code>&lt;dialog&gt;</code> already provides backdrop, top-layer stacking, and focus-trap for free, and its doc comment already explains why it isn&#39;t built on <code>Dialog</code>. It does not currently participate in the shared <code>scroll-lock.ts</code> ref-counter, which is consistent with relying on the browser&#39;s own modal semantics rather than a gap to close.</li><li><code>sovereign-plainwrite</code> has its own local <code>plugins/sovereign-plainwrite/app/_components/ConfirmDialog.tsx</code> — a hand-rolled native-<code>&lt;dialog&gt;</code> component with a different prop shape (<code>onCancel</code> instead of <code>onClose</code>, no <code>destructive</code>/<code>pending</code>/<code>error</code> props). Its own doc comment says it exists &quot;matching the pattern used elsewhere in the platform... until the design system ships a ConfirmDialog component&quot; — that component (<code>@sovereignfs/ui</code>&#39;s <code>ConfirmDialog</code>, exported from <code>packages/ui/src/index.ts:42</code>) has since shipped (Task 9.12), so this is an unmigrated leftover, not a deliberate divergence. All ~20 other <code>ConfirmDialog</code> call sites across first-party plugins already use the shared component consistently.</li></ul><h3 id="gestures" tabindex="-1">Gestures <a class="header-anchor" href="#gestures" aria-label="Permalink to &quot;Gestures&quot;">​</a></h3><ul><li><code>packages/ui/src/hooks/</code> has <code>useDoubleTap.ts</code>, <code>useLongPress.ts</code>, <code>useIsMobile.ts</code>, <code>useCommitOnEnterOrBlur.ts</code> — all consumed correctly everywhere they&#39;re used (<code>ContextMenu.tsx:6,46</code>, <code>Combobox.tsx:57</code>, <code>DatePicker.tsx:47</code>, plus first-party plugins). There is no shared swipe hook.</li><li><code>plugins/sovereign-tasks/app/_components/TaskItem.tsx:178-210</code> (swipe reveal on task rows, <code>SWIPE_REVEAL_WIDTH=128px</code>) and <code>plugins/sovereign-tasks/app/ListSidebar.tsx:544-589</code> (swipe reveal on list rows, <code>SWIPE_REVEAL_WIDTH=72px</code>) are two separate, hand-written pointer-swipe implementations in the same plugin — identical axis-lock and open/close-at-half-width math, not shared even locally.</li><li><code>plugins/sovereign-tasks/app/_components/MobileTasksCarousel.tsx</code> and <code>plugins/sovereign-shopper/app/_components/MobileShopperCarousel.tsx</code> each independently implement the same debounced-<code>scroll</code>-event &quot;settled slide&quot; detection over a native <code>scroll-snap-type: x</code> carousel, with near-identical comments citing the same iOS <code>scrollend</code>-support rationale.</li><li>Every other first-party plugin (ledger, plainwrite, tritext, healthlog, docs, wallet, tally, console, launcher) has zero gesture code of its own — this RFC&#39;s gesture scope is exactly <code>sovereign-tasks</code> and <code>sovereign-shopper</code>, not a platform-wide rollout.</li><li><code>docs/architecture-rules.md</code>&#39;s existing <code>touch-action</code> rule (nested <code>pan-x</code>/<code>pan-y</code> intersection) and DS-first rule are already in place and unchanged by this RFC — the new hooks are additive implementations of rules the docs already state.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-pagecontainer-packages-ui" tabindex="-1">1. <code>PageContainer</code> (<code>packages/ui</code>) <a class="header-anchor" href="#_1-pagecontainer-packages-ui" aria-label="Permalink to &quot;1. `PageContainer` (`packages/ui`)&quot;">​</a></h3><p>A single new primitive that a plugin&#39;s <code>app/layout.tsx</code> (or top-level page) wraps its own content in, replacing local CSS-module container rules:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PageContainer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> maxWidth</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;{children}&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PageContainer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><ul><li><code>maxWidth</code>: <code>&#39;sm&#39; | &#39;md&#39; | &#39;lg&#39; | &#39;full&#39;</code>, mapping to a small fixed set of documented pixel values (not a new token tier — reuses existing <code>--sv-space-*</code>/breakpoint conventions for the internal implementation). <code>&#39;full&#39;</code> applies no max-width (for plugins like <code>docs</code>/<code>sheets.local</code> that want full shell width). Default <code>&#39;md&#39;</code>.</li><li><code>PageContainer</code> does <strong>not</strong> add its own padding — the shell already pads every non-fullbleed plugin (<code>--sv-space-8</code> desktop / <code>--sv-space-4</code> mobile). It only centers (<code>margin-inline: auto</code>) and constrains width. This is the fix for plainwrite&#39;s double-padding: a plugin composing <code>PageContainer</code> should stop adding its own <code>padding</code> in a wrapping layout div.</li><li>Fullbleed plugins (<code>sovereign-tasks</code>, <code>sovereign-shopper</code>) are unaffected — <code>PageContainer</code> is opt-in per plugin, not a replacement for <code>data-plugin-fullbleed</code>.</li><li><code>docs/design-system.md</code> and <code>docs/plugin-development.md</code> gain a short &quot;Page layout&quot; section documenting: the shell already pads your content; use <code>PageContainer</code> to additionally constrain width; do not add your own outer <code>padding</code>/<code>max-width</code> in plugin-local CSS.</li></ul><p>This does not touch <code>runtime/app/(platform)/shell.module.css</code> — the shell&#39;s padding behavior is unchanged, only documented more explicitly and now has a composable component sitting inside it.</p><h3 id="_2-overlay-internal-consolidation" tabindex="-1">2. Overlay internal consolidation <a class="header-anchor" href="#_2-overlay-internal-consolidation" aria-label="Permalink to &quot;2. Overlay internal consolidation&quot;">​</a></h3><p>Extract the scrim + focus-trap + Escape + scroll-lock logic shared by <code>Dialog</code>, <code>Drawer</code>, and <code>Sheet</code> into one internal (non-exported) hook/helper inside <code>packages/ui/src/components</code> — e.g. <code>useOverlayShell({ open, onClose, lockScroll })</code> returning the ref, keydown handling, and mount/exit-animation bookkeeping each component currently duplicates. <code>Dialog</code>, <code>Drawer</code>, and <code>Sheet</code> call it internally; their public props (<code>open</code>, <code>onClose</code>, <code>size</code>, <code>snapHeight</code>, <code>slideFrom</code>, <code>title</code>, etc.) are unchanged. <code>ConfirmDialog</code> stays on native <code>&lt;dialog&gt;</code> and is explicitly not migrated onto this helper — consolidating three duplicated implementations into one is the goal here, not forcing a fourth, already-working approach to match.</p><p>Separately, migrate <code>sovereign-plainwrite</code>&#39;s local <code>ConfirmDialog</code> (<code>plugins/sovereign-plainwrite/app/_components/ConfirmDialog.tsx</code>) and its three call sites (<code>MarkdownEditor.tsx</code>, <code>NewPostDialog.tsx</code>, <code>NewProjectDialog.tsx</code>) onto <code>@sovereignfs/ui</code>&#39;s <code>ConfirmDialog</code>, then delete the local component. This is a plugin-local change with no <code>packages/ui</code> API impact — it&#39;s included here because it&#39;s the concrete, actionable instance of the &quot;adoption is inconsistent&quot; finding, not because it needs new design-system work.</p><h3 id="_3-shared-gesture-hooks" tabindex="-1">3. Shared gesture hooks <a class="header-anchor" href="#_3-shared-gesture-hooks" aria-label="Permalink to &quot;3. Shared gesture hooks&quot;">​</a></h3><ul><li><strong><code>useSwipeReveal({ revealWidth, onReveal?, disabled? })</code></strong> — extracted from <code>TaskItem.tsx</code>&#39;s and <code>ListSidebar.tsx</code>&#39;s existing pointer-swipe implementations (same axis-lock tolerance, same open/close-at-half-width behavior), parameterized by <code>revealWidth</code> so both call sites (128px and 72px today) configure the same hook instead of hand-rolling it twice. Returns pointer handlers plus the current reveal offset for the caller to apply as a transform.</li><li><strong><code>useSnapCarousel({ itemCount, onSettle? })</code></strong> — extracted from <code>MobileTasksCarousel.tsx</code>&#39;s and <code>MobileShopperCarousel.tsx</code>&#39;s existing debounced-scroll settle-detection over a <code>scroll-snap-type: x</code> container. Returns the active index and a ref to attach to the scroll container.</li><li>Both plugins migrate their existing call sites onto the new hooks; no behavior change from the user&#39;s perspective (same thresholds, same timing) — this is a duplication removal, not a UX change.</li><li><code>touch-action</code> usage in <code>ListSidebar.module.css</code>, <code>TaskItem.module.css</code>, and <code>ItemRow.module.css</code> is unchanged — the existing <code>docs/architecture-rules.md</code> guidance on nested <code>pan-x</code>/<code>pan-y</code> still applies and isn&#39;t affected by moving the JS into a shared hook.</li></ul><h3 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h3><ul><li>No new overlay component and no change to the Dialog/Drawer/Sheet/ ConfirmDialog decision table.</li><li>No swipe-to-reveal or carousel gestures added to any plugin that doesn&#39;t already have them — this is consolidation of existing gestures, not new gesture rollout.</li><li>No change to <code>data-plugin-fullbleed</code>, <code>shellConfig.mobileHeader</code>/ <code>mobileFooter</code> (RFC 0075), or any manifest field.</li><li>No cross-plugin (app-switching) swipe navigation — confirmed not to exist today, and not proposed here.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>A single <code>AppShell</code>/<code>ContentLayout</code> component owning both padding and max-width</strong> (rather than <code>PageContainer</code> composing with the shell&#39;s existing padding): rejected — the shell already owns padding correctly today; duplicating that responsibility in a <code>packages/ui</code> component would create exactly the double-padding bug this RFC fixes for plainwrite, just moved one level.</li><li><strong>Unify Dialog/Drawer/Sheet into one component with a <code>variant</code> prop</strong>: rejected — breaking change to a public contract plugin developers already use via three distinct, already-documented semantic components; the existing decision table exists precisely so callers pick the right one by name. Internal code-sharing gets the deduplication benefit without the breakage.</li><li><strong>Migrate <code>ConfirmDialog</code> onto the shared overlay helper too</strong>: rejected for now — it works today via native <code>&lt;dialog&gt;</code> semantics, and forcing it onto the manual focus-trap path used by the other three would be a regression (native focus-trapping is more reliable), not a consolidation.</li><li><strong>Leave gesture duplication alone (it isn&#39;t visibly broken)</strong>: rejected — it&#39;s the direct, current-code instance of a rule already written into <code>docs/architecture-rules.md</code>, and <code>sovereign-tasks/CLAUDE.md</code> shows this same class of debt was worth paying down once already.</li><li><strong>Do all three as one combined task</strong>: rejected in favor of three separate epic tasks — they touch different files, have different risk profiles (layout is additive/net-new, overlay is an internal refactor, gestures are a plugin migration), and reviewing them together would obscure which regression, if any, came from which change.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Exact pixel values for <code>PageContainer</code>&#39;s <code>sm</code>/<code>md</code>/<code>lg</code> max-widths — the implementing task should reconcile the five values currently observed (960px, 1040px, 640px, plus <code>account</code>&#39;s local 680px section width) rather than inventing new numbers; likely <code>sm</code> ≈ 640px, <code>md</code> ≈ 960px, <code>lg</code> ≈ 1040–1200px, but this is a decision for Task 9.18, not fixed here.</li><li>Whether <code>sovereign-tasks</code>/<code>sovereign-shopper</code>&#39;s existing <code>SWIPE_REVEAL_WIDTH</code> constants (128px, 72px) should be normalized to one shared default once both go through <code>useSwipeReveal</code>, or remain per-call-site configuration. Leaning toward keeping them per-call-site-configurable (the hook takes <code>revealWidth</code> as a prop) since the two rows have legitimately different content widths.</li></ul><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Three separate epic tasks, each independently shippable and independently reviewable:</p><ul><li><strong>Task 9.18</strong> — <code>PageContainer</code> + docs + first-party plugin migration.</li><li><strong>Task 9.19</strong> — Overlay internal consolidation + <code>sovereign-plainwrite</code><code>ConfirmDialog</code> migration.</li><li><strong>Task 9.20</strong> — <code>useSwipeReveal</code>/<code>useSnapCarousel</code> + <code>sovereign-tasks</code>/ <code>sovereign-shopper</code> migration.</li></ul><p>All three are additive to <code>@sovereignfs/ui</code>&#39;s public surface (new exports only, no existing prop/token removed or renamed) — per NFR-04 this ships as a <strong>minor</strong> version bump of <code>@sovereignfs/ui</code>, not a major one, whichever task lands first.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr></tbody></table>',41)])])}const g=o(n,[["render",s]]);export{u as __pageData,g as default};
