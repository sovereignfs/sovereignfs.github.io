import{_ as t,o,c as n,a2 as a}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Connector Framework","description":"","frontmatter":{"epic":2,"title":"Connector Framework","status":"⏳ In Progress","scope":"mobile","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/epics/mobile/connector-framework.md"},"headers":[],"relativePath":"sovereign-edge/epics/mobile/connector-framework.md","filePath":"sovereign-edge/epics/mobile/connector-framework.md"}'),s={name:"sovereign-edge/epics/mobile/connector-framework.md"};function r(i,e,c,d,l,h){return o(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="epic-connector-framework" tabindex="-1">Epic: Connector Framework <a class="header-anchor" href="#epic-connector-framework" aria-label="Permalink to &quot;Epic: Connector Framework&quot;">​</a></h1><blockquote><p>The manifest schema, permission/consent model, tool-routing, and tiered trust runtime that every connector — first-party or, eventually, third-party — is built on.</p></blockquote><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>This is the foundation research 0001 calls out as needing to be right from Phase 1: even though only one connector (Search) exists at first and it&#39;s hardcoded, it must already be expressed in the same shape a downloaded, third-party connector will use in Phase 3. Getting this epic right is what lets Phase 2 and Phase 3 be purely additive.</p><p>Three trust tiers, per CONCEPT.md:</p><ul><li><strong>Tier 1</strong> — declarative manifest only (tool schema, endpoint, auth, request/response templates). No code. Open to third parties in Phase 3.</li><li><strong>Tier 2</strong> — a sandboxed transform script on top of Tier 1. Open to third parties in Phase 3, once a real use case justifies building it.</li><li><strong>Tier 3</strong> — first-party native OS integration. Not opened to third parties in this project&#39;s current scope.</li></ul><h2 id="tasks" tabindex="-1">Tasks <a class="header-anchor" href="#tasks" aria-label="Permalink to &quot;Tasks&quot;">​</a></h2><h4 id="✅-2-1-—-connector-manifest-schema-tier-1" tabindex="-1">✅ 2.1 — Connector manifest schema (Tier 1) <a class="header-anchor" href="#✅-2-1-—-connector-manifest-schema-tier-1" aria-label="Permalink to &quot;✅ 2.1 — Connector manifest schema (Tier 1)&quot;">​</a></h4><p><strong>Goal:</strong> Define the declarative shape every Tier 1 connector — first-party or third-party — conforms to.</p><p><strong>Deliverables:</strong></p><ul><li>Schema fields: <code>id</code>, <code>name</code>, <code>version</code>, <code>tier</code>, <code>tool</code> (name + JSON schema for the LLM&#39;s function-call), <code>permissions</code> (declared network domain, credential storage), <code>endpoint</code>/<code>request</code>/<code>response</code> templates, <code>pricing</code>, <code>platforms</code>.</li><li>A validator (used both at connector-author time and at load time).</li><li>Written and versioned even though only one connector uses it in Phase 1 — this is the piece Phase 3 reuses without a rewrite.</li></ul><p><strong>Dependencies:</strong> none within this epic — first task.</p><p><strong>Review checklist:</strong></p><ul><li>✅ The Search connector (epic 3) validates against this schema with no special-casing. <code>fixtures/search.manifest.json</code> is a realistic connector — query slot, language slot, literal <code>format=json</code>, and a bearer credential — and it passes unmodified.</li></ul><p><strong>Design decisions are in <a href="./../../research/0004-connector-manifest-schema.html">research 0004</a>.</strong> Two shape everything else:</p><ul><li><strong>No expression language, no string interpolation.</strong> A request is literal parts plus named slots, and the runtime encodes each slot for the position it occupies. Encoding is decided by position rather than declared, so the two cannot disagree. The values filling slots come from a language model steered by whatever the user pasted into chat, and a format permitting interpolation could not later forbid it without breaking every connector written against it.</li><li><strong>A credential may never appear in a URL</strong> — not in the origin, a path segment, or a query value. URLs reach proxy logs, <code>Referer</code> headers, and crash reports.</li></ul><p><strong>What the validator rejects</strong>, each with a test that fails for that reason:</p><table tabindex="0"><thead><tr><th>Rejected</th><th>Attack closed</th></tr></thead><tbody><tr><td>Credential in query or path</td><td>Token leaked through logs</td></tr><tr><td>Userinfo in an origin</td><td><code>https://real.example.org@evil.com</code> resolves to <code>evil.com</code></td></tr><tr><td>Origin carrying a path, query, or fragment</td><td>Author-controlled string joining</td></tr><tr><td>Literal path segment containing <code>/</code>, <code>?</code>, <code>#</code></td><td>Path traversal, invented structure</td></tr><tr><td>Cleartext <code>http</code></td><td>Refused by iOS ATS on device anyway</td></tr><tr><td>Request origin outside <code>permissions.network.origins</code></td><td>Undeclared network access</td></tr><tr><td>Slot naming a parameter the tool does not declare</td><td>Silently empty value at runtime</td></tr><tr><td>Credential the user is never asked for</td><td>Grant that cannot be honoured</td></tr><tr><td>Unknown <code>manifestVersion</code></td><td>Loading a manifest only partly understood</td></tr><tr><td>Unknown top-level field</td><td>A typo indistinguishable from an unimplemented field</td></tr></tbody></table><p>Validation is two passes: Zod for shape, then cross-field rules for everything that only makes sense in combination — which is where the security properties actually live, and none of it expressible as a per-field type. All issues are reported at once, because a connector author fixing one error per device round-trip is the slowest possible loop.</p><p><strong>Chosen against Ajv deliberately.</strong> Ajv compiles schemas by generating JavaScript at runtime; &quot;we generate and execute code derived from a third-party manifest&quot; is the wrong sentence for this product, independent of Hermes&#39; restrictions on runtime codegen.</p><p><strong>Left to later tasks.</strong> Response mapping is deliberately minimal — a source path and a size cap — because turning a response back into model context is the mirror of the request problem and deserves its own pass. Redirect handling, timeouts, and retries belong to the runtime host (2.4).</p><hr><h4 id="✅-2-2-—-permission-and-consent-model" tabindex="-1">✅ 2.2 — Permission and consent model <a class="header-anchor" href="#✅-2-2-—-permission-and-consent-model" aria-label="Permalink to &quot;✅ 2.2 — Permission and consent model&quot;">​</a></h4><p><strong>Goal:</strong> Per-connector, explicit, revocable permission grants — never a blanket &quot;this app can use the network&quot; toggle.</p><p><strong>Deliverables:</strong></p><ul><li>Grant/revoke UI per connector.</li><li>Per-connector scoped credential storage (OS keychain), isolated so one connector&#39;s token is never visible to another.</li><li>A settings surface listing every installed connector and its current permission state.</li></ul><p><strong>Dependencies:</strong> Task 2.1.</p><p><strong>Review checklist:</strong></p><ul><li>✅ Revoking one connector&#39;s permission does not affect any other connector&#39;s access or stored credentials. Two connectors sharing the same credential <em>key</em> (<code>apiToken</code>) — the case a naive key scheme collides on — and revoking one leaves the other&#39;s grant and secret intact. Verified by breaking it: removing the connector id from the vault&#39;s key template failed exactly that test and no other.</li></ul><p><strong>Isolation holds by construction, not by discipline.</strong> No exported function takes a connector id and a key. The only route to a credential is a <code>ConnectorVault</code> handle that closes over its own id and builds every key itself, so code holding one connector&#39;s vault <em>cannot name</em> another&#39;s token — not &quot;should not&quot;, cannot, without bypassing the module in a way that is visible in review.</p><p>Credentials live in the OS keychain via <code>expo-secure-store</code> (iOS keychain, Android EncryptedSharedPreferences), so they are not readable from a filesystem dump the way <code>AsyncStorage</code> would be. Grants live in plain JSON instead: they are not secrets, and keeping them readable means a user can inspect what they agreed to without the app mediating.</p><p><strong>Three decisions worth keeping:</strong></p><ul><li><strong><code>not-asked</code> is distinct from <code>denied</code>.</strong> Absence of a decision is not refusal, and the distinction is what lets the UI avoid re-prompting for something already turned down. A dialog that reappears until answered &quot;correctly&quot; is coercion, not consent.</li><li><strong>Revoking destroys credentials, not just the grant.</strong> Otherwise &quot;revoked&quot; describes the UI rather than the device, and a later re-grant silently reuses a secret the user believed was gone.</li><li><strong>Granted origins are copied at grant time, not referenced.</strong> A connector update that widens <code>permissions.network.origins</code> cannot inherit old consent; it surfaces as <code>NEEDS REVIEW</code>. An update is the natural moment for scope to creep, and consent for one set of destinations is not consent for a larger one.</li></ul><p>Corrupt grant state fails closed — no grants rather than stale ones, so nothing reaches the network until the user decides again.</p><p><strong>Verified on device, and the check earned its keep.</strong> <code>expo-secure-store</code> is a native module, so this needed a rebuild rather than a Metro reload. The rebuild silently added <code>USE_BIOMETRIC</code> and <code>USE_FINGERPRINT</code> to the Release manifest — invalidating <a href="./../../network-audit.html">docs/network-audit.md</a>&#39;s &quot;exactly one permission&quot; claim within hours of it being written. Both are blocked now, and the audit carries a standing instruction to re-check the merged manifest whenever a native dependency is added.</p><p><strong>Not yet exercised through the UI.</strong> The screen renders its empty state because no connector exists until task 3.1. Grant and revoke are covered by tests; the on-device run confirms the app boots with the new native module and the surface renders. The interactive path gets a real device run when the Search connector lands.</p><hr><h4 id="✅-2-3-—-tool-routing-intent-detection-layer" tabindex="-1">✅ 2.3 — Tool-routing / intent-detection layer <a class="header-anchor" href="#✅-2-3-—-tool-routing-intent-detection-layer" aria-label="Permalink to &quot;✅ 2.3 — Tool-routing / intent-detection layer&quot;">​</a></h4><p><strong>Goal:</strong> Let the local model decide &quot;this needs a connector&quot; vs. &quot;just answer in chat,&quot; and pick the right one.</p><p><strong>Deliverables:</strong></p><ul><li>Prompt/grammar-constrained decoding (e.g. GBNF-style grammar via <code>llama.cpp</code>) so tool-call output is reliably valid JSON even from a small on-device model — small models are not reliable free-form tool-callers without this constraint.</li><li>Fallback behavior when no connector matches or the matching connector lacks permission (explain to the user what&#39;s needed, don&#39;t silently fail).</li></ul><p><strong>Dependencies:</strong> Task 2.1, Task 1.1 (inference engine).</p><p><strong>Review checklist:</strong></p><ul><li>✅ A request that should trigger a connector call produces valid, schema- conformant tool-call output in a controlled test set, not just &quot;it usually works.&quot;</li></ul><p><code>routeMessage()</code> (<code>src/connectors/routing/</code>) turns one completion into a <code>RoutingDecision</code>: <code>answered</code> (no tool needed), <code>tool-call</code> (a permitted connector matched), <code>blocked</code> (a tool was called but can&#39;t be honoured — <code>no-connector</code>, <code>not-permitted</code>, or <code>malformed</code>), or <code>unsupported</code> (the loaded model&#39;s chat template can&#39;t emit tool calls at all — <code>EngineInfo.toolCapable</code>, read from <code>chatTemplates.jinja.defaultCaps.tools</code> per research 0004). One completion call does double duty: the same request that offers tools either returns a chat answer or a tool call, rather than a separate classifier guessing intent ahead of generation.</p><p><strong>No argument validator, by design.</strong> <code>tool.parameters</code> is JSON Schema specifically so <code>llama.rn</code> can convert it into a decoding grammar — the model&#39;s output is constrained to be valid rather than merely likely to be. <code>routeMessage</code> trusts that guarantee and only <code>JSON.parse</code>s the arguments, rather than re-validating them against the schema. The <code>malformed</code> outcome exists for the same reason 2.1&#39;s validator exists at all: the arguments are still model output steered by whatever the user pasted into chat, and this repo treats that as untrusted regardless of what the decoding grammar is supposed to guarantee.</p><p><strong>Capability gate is silent, not user-facing.</strong> When <code>toolCapable</code> is false, no <code>tools</code> are offered and the model just answers normally — there is no <code>blocked</code> case to report, because nothing was ever offered. The <code>blocked</code> cases are for the two failures that can only happen once a tool <em>was</em> offered and something after that failed. Whether <code>unsupported</code> gets any UI treatment is left to whichever task wires this into <code>ChatScreen</code> (2.5, most likely) — 2.3 has no chat-surface deliverable.</p><p><strong>Verified on-device against a real model, and it surfaced a real limitation.</strong> Qwen2.5-0.5B-Instruct correctly emitted a grammar-constrained call naming the Search fixture&#39;s actual tool (<code>web_search</code>) for a search-shaped prompt — confirming the mechanism this task depends on. But the same model also called <code>web_search</code> for &quot;What is 12 plus 30?&quot;, which needs no connector at all. <code>tool_choice: &#39;auto&#39;</code> is not reliably choosing &quot;just answer&quot; on a model this small — a routing-layer concern in the sense that whoever executes tool calls (2.4) or exposes a real connector (3.1) should not assume &quot;the model called a tool&quot; means &quot;the model needed to.&quot; Worth a closer look — a different <code>tool_choice</code>, an explicit intent gate before offering tools, or simply not offering tools on models this size — when a connector is actually reachable and an unnecessary network round-trip stops being a hypothetical.</p><hr><h4 id="✅-2-4-—-connector-runtime-host" tabindex="-1">✅ 2.4 — Connector runtime host <a class="header-anchor" href="#✅-2-4-—-connector-runtime-host" aria-label="Permalink to &quot;✅ 2.4 — Connector runtime host&quot;">​</a></h4><p><strong>Goal:</strong> Execute a validated tool call against a Tier 1 connector&#39;s manifest.</p><p><strong>Deliverables:</strong></p><ul><li>Request/response template execution (map the LLM&#39;s structured call into an HTTP request per the manifest, map the HTTP response back into text/ data for the model).</li><li>Reserved extension points for Tier 2 (sandboxed script execution) and Tier 3 (native module dispatch), even if unimplemented until needed. (Originally written expecting epic 5/9 to need Tier 3 first — that turned out wrong. It&#39;s task 2.6, consumed by the Calendar and Device connectors, epics 10 and 11.)</li></ul><p><strong>Dependencies:</strong> Task 2.1, Task 2.2, Task 2.3.</p><p><strong>Review checklist:</strong></p><ul><li>✅ A Tier 1 connector&#39;s manifest alone (no connector-specific code) is enough to execute a real request/response round trip.</li></ul><p><code>executeConnectorCall()</code> (<code>src/connectors/runtime/</code>) maps a <code>RoutingDecision</code> of kind <code>tool-call</code> into an HTTP request per <code>request</code>, and the response back into <code>text</code> per <code>response</code>, returning a typed <code>ExecutionResult</code> — <code>ok: true</code> or <code>ok: false</code> with a specific <code>reason</code> (<code>not-permitted</code>, <code>missing-credential</code>, <code>invalid-arguments</code>, <code>network-error</code>, <code>redirected</code>, <code>http-error</code>, <code>response-too-large</code>, <code>malformed-response</code>), same shape task 2.3 established for <code>RoutingDecision</code>: a switch for the caller, not a try/catch.</p><p><strong><code>isAllowed()</code> is re-checked here, not just trusted from routing.</strong> 2.2&#39;s own doc comment already called this &quot;the single question the runtime asks before any request&quot; — this function does not assume its caller already asked it.</p><p><strong>Redirects are never followed.</strong> <code>fetch(url, { redirect: &#39;manual&#39; })</code>, and any 3xx (or an opaque redirect response) is a hard <code>redirected</code> failure. Research 0004 flagged this as an open question — a redirect to another origin defeats the origin allowlist that makes 2.2&#39;s grant enforceable — and this task is where it got settled rather than deferred again.</p><p><strong>No manifest field for timeout</strong>, so it&#39;s a runtime constant (15s via <code>AbortController</code>) rather than connector-configurable, matching the epic&#39;s own field list. No retries: one clean failure is more honest than silently repeating a request the user never saw happen once.</p><p><strong><code>response.maxBytes</code> is enforced against the actual decoded byte length, not just <code>Content-Length</code></strong> — a header that can be absent or simply wrong. A response is rejected before <code>JSON.parse</code> either way it fails the cap.</p><p><strong>Verified on-device against a real endpoint, not just mocks</strong> — the round trip the review checklist asks for is a device claim, same as 2.3&#39;s. Hitting httpbin.org from the iOS simulator: a GET request built from a manifest template and args (<code>?q=hello-from-sovereign-edge</code>) echoed back and mapped through <code>textFrom</code> correctly; a POST with a JSON body round-tripped the same way; and a real 302 from <code>httpbin.org/redirect-to</code> was refused exactly as designed (<code>{ ok: false, reason: &#39;redirected&#39; }</code>) — resolving, on a real device rather than by reading the spec, whether Expo&#39;s <code>fetch</code> reports a manual redirect as an opaque response or a plain 3xx status (it can do either depending on path, which is why the runtime checks both).</p><p><strong>A pre-existing conflict with task 1.5&#39;s offline tripwire, found by that same device run.</strong> <code>src/connectors/runtime/execute.ts</code> is the first code in this repo to call <code>fetch</code> directly, and <code>armOfflineTripwire()</code> (<code>src/chat/session/offlineTripwire.ts</code>) replaced the global for the entire process in development builds — not scoped to <code>src/chat/</code> — so every call from the runtime host threw the tripwire&#39;s own violation error, whose message already (and, until now, only aspirationally) said &quot;network access belongs in <code>src/connectors/</code>, behind an explicit per-connector grant.&quot; Nothing had exercised that claim before a connector runtime existed to call <code>fetch</code>. Fixed with <code>allowNetworkForConnector()</code>, a narrow, named, importable escape hatch added to the same file: the only legitimate call site has to name itself explicitly, which is a stronger guarantee than the blanket replacement it replaces, not a weaker one — the same &quot;not &#39;should not&#39;, cannot without it being visible in review&quot; property 2.2 gives credential isolation. <code>jest.setup.js</code>&#39;s own network-in-tests guard is untouched: the escape hatch reaches through to whatever <code>fetch</code> was originally there, which in Jest is still that throwing stub.</p><hr><h4 id="✅-2-5-—-in-chat-connector-provenance" tabindex="-1">✅ 2.5 — In-chat connector provenance <a class="header-anchor" href="#✅-2-5-—-in-chat-connector-provenance" aria-label="Permalink to &quot;✅ 2.5 — In-chat connector provenance&quot;">​</a></h4><p><strong>Goal:</strong> Make it visible in the UI which connector (if any) answered a given message — per CONCEPT.md&#39;s &quot;always show which trust tier is active.&quot;</p><p><strong>Deliverables:</strong></p><ul><li>A visible marker in chat distinguishing a fully-offline reply from one that used a specific named connector.</li></ul><p><strong>Dependencies:</strong> Task 2.4, Task 1.3 (chat UI).</p><p><strong>Review checklist:</strong></p><ul><li>✅ A user can tell, without opening settings, whether a given reply touched the network and which connector did it.</li></ul><p>The marker itself needed no new UI work — <code>ChatBubble</code>&#39;s <code>connector?: string</code> prop already existed, fully implemented and tested, from earlier design-system work; it had simply never been passed from <code>ChatScreen</code>. The actual task was the orchestration underneath: <code>generateWithConnectors()</code> (<code>src/settings/connectorOrchestration.ts</code>) is the one place <code>RoutingDecision</code> (2.3) and <code>ExecutionResult</code> (2.4) meet a user-facing reply — routes a message, executes a connector if one was called, maps every <code>blocked</code> / <code>ok: false</code> outcome to a short, specific, honest fallback string (never a generic &quot;something went wrong&quot;), and returns <code>{ text, connector }</code> for <code>ChatScreen</code> to render.</p><p><strong>Gated to plain Chat mode only.</strong> <code>GenerateRequest</code> gained <code>allowConnectors</code>, set by <code>ChatScreen</code> to <code>modeId === &#39;plain&#39;</code>. The writing-assist modes are documented as transformations of the text handed to them, not conversations — offering a connector to &quot;Fix grammar&quot; is a category error before even asking whether one is installed, and it also sidesteps 2.3&#39;s own device finding that a small model offered a tool will sometimes call it when it plainly should not have.</p><p><strong>No shared connector registry.</strong> <code>INSTALLED: ConnectorManifest[] = []</code> in <code>ModelSessionProvider.tsx</code> follows the exact placeholder pattern <code>ConnectorsScreen.tsx</code> already established for task 2.2 — empty until 3.1, local rather than promoted to a shared module ahead of the one connector that would need it.</p><p><strong>Three real bugs, all found on-device, none caught by unit tests</strong> — every suite was green before any of these were discovered, which is the specific thing this project&#39;s own verification discipline exists to catch:</p><ol><li><strong><code>routeMessage</code>&#39;s <code>unsupported</code> branch generated nothing.</strong> It returned immediately without ever calling <code>engine.generate()</code> — a tool-incapable model produced no reply at all, not a silent-but-normal one as designed. Fixed to always generate when nothing can be offered, whether that&#39;s because no connector exists (<code>answered</code>) or the model can&#39;t call tools (<code>unsupported</code>); <code>RoutingDecision</code>&#39;s <code>unsupported</code> variant now carries <code>text</code>.</li><li><strong>Tool-call syntax leaked into the visible chat.</strong> <code>routeMessage</code> streamed the tool-decision completion&#39;s raw tokens live via <code>onToken</code>. On a real device, Qwen2.5-0.5B emitted its literal <code>&lt;tool_call&gt;{&quot;name&quot;:&quot;web_search&quot;, ...}&lt;/tool_call&gt;</code> block as part of its raw text stream before that syntax was recognised as a tool call — it appeared in the chat bubble ahead of the real answer. Fixed by no longer forwarding <code>onToken</code> into that one completion; its <code>result.text</code> is flushed to the caller in a single call, and only once the outcome is known to be a plain answer — never for a <code>tool-call</code> outcome, whose raw text was never meant to be seen at all.</li><li><strong>A <code>blocked</code> fallback rendered as a permanently empty bubble.</strong><code>ChatScreen</code> built visible message content purely by accumulating <code>onToken</code> calls. A <code>blocked</code> reply resolves with real, specific text (e.g. &quot;hasn&#39;t been granted access&quot;) but streams zero tokens, since it is app-generated text, not model output. Fixed by also setting content from the resolved <code>generate()</code> result, not only from streamed tokens.</li></ol><p>All three were verified fixed against a real device, using a temporary synthetic connector (an httpbin.org-backed manifest, fully reverted after use) — the same device-first pattern 2.3 and 2.4 already established, and this task&#39;s own reminder that a green suite is not the same claim as &quot;the feature works.&quot;</p><hr><h4 id="✅-2-6-—-tier-3-connector-scaffolding" tabindex="-1">✅ 2.6 — Tier 3 connector scaffolding <a class="header-anchor" href="#✅-2-6-—-tier-3-connector-scaffolding" aria-label="Permalink to &quot;✅ 2.6 — Tier 3 connector scaffolding&quot;">​</a></h4><p><strong>Goal:</strong> Build the reserved-but-unimplemented Tier 3 extension point — first-party native OS integration — that tasks 2.1 and 2.4 always left room for, and that the Calendar and Device connectors (epics 10, 11) are the first to actually need.</p><p><strong>Deliverables:</strong></p><ul><li>A Tier 3 manifest schema variant (<code>tier: 3</code>), replacing Tier 1&#39;s HTTP <code>request</code>/<code>response</code> templates with a reference to a registered native handler. No expression language here either, for the same reason task 2.1 forbade one in Tier 1: arguments still originate from a language model steered by untrusted chat input.</li><li>A generalized grant-scope concept in <code>src/connectors/permissions/</code>. The grant <em>state machine</em> (<code>not-asked</code>/<code>granted</code>/<code>denied</code>, one record per connector ID, no blanket toggle) is already tier-agnostic and needs no change; only <code>ConnectorGrant.grantedOrigins</code> and the two functions that read/write it (<code>grant()</code>, <code>needsRedecision()</code>) are hardwired to <code>permissions.network.origins</code>. This task generalizes that one field to a granted-scope concept Tier 1 fills with origins and Tier 3 fills with named OS capabilities (e.g. <code>calendar.write</code>, <code>device.torch</code>), without touching the state machine itself.</li><li>A <code>case 3</code> in <code>executeConnectorCall()</code> dispatching to a small first-party registry of native handlers (one per capability, e.g. backed by <code>expo-calendar</code>, <code>expo-camera</code>, <code>expo-sensors</code>), gated by <code>isAllowed()</code> exactly as Tier 1 is today.</li></ul><p><strong>Dependencies:</strong> Tasks 2.1, 2.2, 2.4. Unblocked immediately — this is scaffolding, not a feature with its own product surface.</p><p><strong>Review checklist:</strong></p><ul><li>A Tier 3 connector&#39;s manifest plus one registered native handler function is enough to execute a permitted on-device action, with no other connector-specific code in the runtime.</li><li>Revoking a Tier 3 connector&#39;s grant blocks its native handler from running, the same way revoking a Tier 1 connector&#39;s grant blocks its <code>fetch</code> call.</li><li>A Tier 1 connector&#39;s existing behavior (manifest shape, grant shape, <code>case 1</code> dispatch) is unchanged — this is additive, not a migration.</li></ul><p><code>connectorManifest</code> (<code>src/connectors/manifest/schema.ts</code>) is now a <code>z.discriminatedUnion(&#39;tier&#39;, …)</code> of <code>connectorManifestTier1</code> (unchanged) and the new <code>connectorManifestTier3</code> — <code>manifestCommon</code> factors out the fields every tier shares (<code>id</code>, <code>name</code>, <code>tool</code>, <code>pricing</code>, …) so the two variants can&#39;t drift on those by accident. Tier 3 replaces <code>request</code>/<code>response</code> with a single <code>handler: { capability }</code> reference, and <code>permissions</code> with <code>tier3Permissions</code> (<code>{ device: { capabilities: string[] } }</code>) — the capability-scoped mirror of Tier 1&#39;s <code>{ network: { origins } }</code>. <code>validateManifest</code>&#39;s cross-field pass dispatches on tier the same way: Tier 3&#39;s own check is one rule, that <code>handler.capability</code> is a member of <code>permissions.device.capabilities</code>, the exact shape Tier 1&#39;s &quot;request origin must be in the allowlist&quot; check already had.</p><p><strong>The grant-scope generalization is a rename, not a new field.</strong><code>ConnectorGrant.grantedOrigins</code> is now <code>grantedScope: string[]</code>, and <code>grants.ts</code> gained one small function, <code>connectorScope(manifest)</code>, that switches on tier to produce it — origins for Tier 1, capabilities for Tier 3. <code>grant()</code> and <code>needsRedecision()</code> are now one line each, built on top of it, instead of reading <code>permissions.network.origins</code> directly. No migration for existing on-device grant files: this app has no shipped release yet (task 0.1.20 is still open), so there is no stored <code>grantedOrigins</code> to carry forward — the honest fix was renaming the field, not keeping both.</p><p><strong><code>revoke()</code> only clears vault credentials for Tier 1.</strong> A Tier 3 connector has no app-managed secret — the &quot;credential&quot; is an OS permission grant, not a token this app stores — so <code>manifest.tier === 1</code> gates the <code>openVault(...).clear(...)</code> call; a Tier 3 revoke still flips the grant to <code>denied</code>, same as Tier 1, just with nothing to clear.</p><p><strong><code>case 3</code> in <code>executeConnectorCall()</code></strong> (<code>src/connectors/runtime/execute.ts</code>) re-checks <code>isAllowed()</code> exactly like <code>case 1</code> does, then looks up <code>manifest.handler.capability</code> in a small registry (<code>src/connectors/runtime/nativeHandlers.ts</code>) and calls whatever it finds. A handler returns an <code>ExecutionResult</code> directly — the same union <code>executeTier1</code> returns — so a native failure and an HTTP failure are both just &quot;the connector didn&#39;t answer&quot; to <code>connectorOrchestration.ts</code>, which gained one more <code>case</code> (<code>&#39;handler-error&#39;</code>) in its exhaustive switch over failure reasons. No capability, or a handler that throws, both map to <code>handler-error</code>; there is no separate &quot;not registered&quot; reason, since neither is something a user&#39;s retry can fix.</p><p><strong><code>device.info</code> is the one registered handler</strong>, backed by <code>expo-device</code> (already a dependency — no new native module pulled in for scaffolding that has no product surface of its own). It exists to prove the extension point end to end, the same way task 2.4&#39;s <code>httpbin.org</code> round trip proved Tier 1&#39;s — not as a connector this task ships. The real capabilities (<code>calendar.write</code>, <code>device.torch</code>, …) arrive with the connectors that need them, tasks 0.2.2 and 0.2.3.</p><p><strong>Verified on-device</strong>, iOS Simulator (iPhone 17, iOS 26.5), via <code>expo run:ios</code> — a real native build, not Metro&#39;s cached bundle. A temporary hook in <code>App.tsx</code> (reverted before commit, never merged) ran <code>executeConnectorCall</code> against the <code>device.info</code> fixture manifest three times — before granting, after granting, after revoking — and surfaced the result in an <code>Alert</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;beforeGrant&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;reason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;not-permitted&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;afterGrant&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;text&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Simulator iOS iOS (26.5)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;afterRevoke&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;reason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;not-permitted&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>afterGrant.text</code> is the real <code>expo-device</code> native module&#39;s own values (<code>Device.modelName</code> / <code>osName</code> / <code>osVersion</code> for the actual simulator), not Jest&#39;s mocked <code>&quot;Pixel 9&quot; &quot;Android&quot; &quot;15&quot;</code> — the on-device round trip this project&#39;s own history says a mock cannot stand in for. <code>isAllowed()</code> gates <code>case 3</code> correctly in both directions: refused before the grant existed, and refused again the instant it was revoked, with no code path in between that could serve a result outside that window.</p><hr><h2 id="related-docs" tabindex="-1">Related Docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related Docs&quot;">​</a></h2><ul><li><a href="./../../../CONCEPT.html">CONCEPT.md</a></li><li><a href="./../../research/0001-concept-and-connector-architecture.html">research 0001</a></li><li><a href="./../../research/0005-calendar-connector.html">research 0005</a> (the Tier 3 classification finding that motivated task 2.6)</li></ul><h2 id="cross-references" tabindex="-1">Cross-references <a class="header-anchor" href="#cross-references" aria-label="Permalink to &quot;Cross-references&quot;">​</a></h2><ul><li>The Search connector (epic 3) and Sovereign Tasks connector (epic 4) are the first consumers of this framework&#39;s Tier 1 shape.</li><li>The Calendar connector (epic 10) and Device connector (epic 11) are the first consumers of task 2.6&#39;s Tier 3 shape.</li><li>Epic 5 (Connector Store &amp; SDK) reuses this framework&#39;s manifest schema unchanged when opening it to third parties.</li></ul>`,98)])])}const g=t(s,[["render",r]]);export{u as __pageData,g as default};
