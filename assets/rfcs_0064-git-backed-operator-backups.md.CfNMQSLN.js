import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.BWuWLRhz.js";const k=JSON.parse('{"title":"RFC 0064 — Git-backed operator backups","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0064-git-backed-operator-backups.md","filePath":"rfcs/0064-git-backed-operator-backups.md"}'),n={name:"rfcs/0064-git-backed-operator-backups.md"};function l(r,e,o,p,d,h){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="rfc-0064-—-git-backed-operator-backups" tabindex="-1">RFC 0064 — Git-backed operator backups <a class="header-anchor" href="#rfc-0064-—-git-backed-operator-backups" aria-label="Permalink to &quot;RFC 0064 — Git-backed operator backups&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>bin/sv</code>, <code>bin/helpers.ts</code>, backup/restore docs, <code>.env.example</code>, Docker/Compose operator workflows; amends RFC 0006 and RFC 0008<br><strong>Incorporated into plan:</strong> Yes — epic tasks 8.10, 8.11, and 8.12. This RFC specifies the desired default backup backend and restore semantics; roadmap slot versions are deferred.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Extend <code>sv backup</code> and <code>sv restore</code> with a Git-backed remote backup flow. The default operator backup remains a full-instance disaster-recovery snapshot, not a user portability export. The new default remote backend is any Git server that can accept pushes to an empty backup repository.</p><p>Backups are encrypted before they leave the host. The root <code>.env</code> is never stored as plaintext in a backup bundle; when included, it is written only as <code>.env.enc</code>. Operators may explicitly allow plaintext remote backup payloads with <code>--allow-plaintext-remote</code>, but that escape hatch still does not permit a plaintext <code>.env</code>.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign already has local <code>sv backup</code> / <code>sv restore</code> commands, but operators still need to move backup archives off-host, retain a useful history, and restore the latest or a specific known-good snapshot without manually copying files. Security docs also state that raw v1 backups expose data if leaked, so any remote-first flow must encrypt before upload.</p><p>Git is a pragmatic first remote backend because it is widely available, self-hostable, auditable, and works with simple token or SSH credentials. It is not a perfect large-object backup store, so the design constrains bundle size, retention, and deletion semantics instead of pretending Git is object storage.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><code>sv backup</code> currently creates a local <code>.tar.gz</code> archive. SQLite mode tars the whole data directory so WAL/SHM sidecars, <code>auth.db</code>, <code>sovereign.db</code>, <code>avatars/</code>, and isolated plugin DB files are captured together. Postgres mode runs <code>pg_dump</code> for platform/auth DBs and includes avatars.</li><li><code>sv restore</code> currently restores from a local archive path. It warns operators to stop the server first, but it does not fetch remote backups, decrypt bundles, verify a backup manifest, or select <code>latest</code>.</li><li>RFC 0006 planned first-class backups, automatic pre-upgrade snapshots, labeled restore points, and tunable retention.</li><li>RFC 0008 already amends RFC 0006 toward encrypted backups. It also states that losing the operator-held key loses the data and that the key must be backed up separately from encrypted backups.</li><li><code>docs/security.md</code> states plainly that raw backups are readable in v1 and tells operators to keep backups encrypted and off-host.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="command-model" tabindex="-1">Command model <a class="header-anchor" href="#command-model" aria-label="Permalink to &quot;Command model&quot;">​</a></h3><p>Keep local archives supported, but make the remote-capable command shape explicit:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> delete</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --older-than</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 60d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prune</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --older-than</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 60d</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> latest</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backup-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./backups/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">archiv</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plugin</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">plugin-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --from</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backup-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p><code>pnpm sv backup</code> is shorthand for <code>backup create</code>; when <code>SV_BACKUP_BACKEND=git</code> is configured it may become shorthand for <code>backup create &amp;&amp; backup push</code>.</p><p><code>restore latest</code> resolves the newest valid remote backup tag, fetches it to a temporary local directory, verifies its manifest and checksums, decrypts it, then applies it. <code>restore &lt;backup-tag&gt;</code> restores a specific tag. Local archive restore continues to work for air-gapped and manual workflows.</p><p><code>restore plugin &lt;plugin-id&gt;</code> restores only one plugin&#39;s data from a full-instance backup. It is a surgical recovery tool for operator mistakes, failed plugin upgrades, or accidental plugin-data deletion; it is not a user portability flow.</p><h3 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h3><p>Proposed environment variables:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_BACKEND</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">git</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_GIT_REPOSITORY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://git.example.com/org/sovereign-backups.git</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_GIT_BRANCH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backups</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_GIT_TOKEN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_ENCRYPTION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">required</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_PASSPHRASE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_KEY_FILE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_RETENTION_DAYS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">60</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SV_BACKUP_MAX_BUNDLE_BYTES</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1073741824</span></span></code></pre></div><p><code>SV_BACKUP_GIT_REPOSITORY</code> supports any Git server. HTTPS token auth is configured with <code>SV_BACKUP_GIT_TOKEN</code>; SSH URLs use the operator&#39;s existing SSH agent or key configuration. Tokens must be scoped to one empty private backup repository with contents read/write access only.</p><p><code>SV_BACKUP_ENCRYPTION=required</code> is the default for remote backups. Operators may pass <code>--allow-plaintext-remote</code> for emergency cases, but the command must print a high-friction warning and still write <code>.env</code> only as <code>.env.enc</code>.</p><h3 id="bundle-layout" tabindex="-1">Bundle layout <a class="header-anchor" href="#bundle-layout" aria-label="Permalink to &quot;Bundle layout&quot;">​</a></h3><p>Each backup is assembled in a temporary staging directory:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>backup-manifest.json</span></span>
<span class="line"><span>data/</span></span>
<span class="line"><span>  sqlite/...</span></span>
<span class="line"><span>  postgres/...</span></span>
<span class="line"><span>  avatars/...</span></span>
<span class="line"><span>  plugins/</span></span>
<span class="line"><span>    &lt;plugin-id&gt;/</span></span>
<span class="line"><span>      db/...</span></span>
<span class="line"><span>      storage/...</span></span>
<span class="line"><span>      manifest.json</span></span>
<span class="line"><span>config/</span></span>
<span class="line"><span>  env.required.json</span></span>
<span class="line"><span>  env.public.json</span></span>
<span class="line"><span>  .env.enc</span></span>
<span class="line"><span>checksums/</span></span>
<span class="line"><span>  sha256sums.txt</span></span></code></pre></div><p><code>backup-manifest.json</code> records:</p><ul><li>backup format version;</li><li>backup ID and Git tag;</li><li>created-at timestamp;</li><li>source instance ID when available;</li><li>platform version and schema/migration version;</li><li>DB dialect and artifact inventory;</li><li>installed plugin inventory: plugin ID, version, route prefix, manifest checksum, database isolation/dialect, storage roots, and enabled/disabled status;</li><li>encryption metadata, never the key or passphrase;</li><li>checksums for the encrypted payload and plaintext staging files where useful;</li><li>whether plaintext remote upload was explicitly allowed.</li></ul><p>The final remote artifact is encrypted as one payload before upload. <code>.env.enc</code> is also encrypted before bundle assembly so a plaintext local staging directory never contains <code>.env</code> in recoverable form after the config-capture step.</p><h3 id="plugin-data-coverage" tabindex="-1">Plugin data coverage <a class="header-anchor" href="#plugin-data-coverage" aria-label="Permalink to &quot;Plugin data coverage&quot;">​</a></h3><p>Operator backups include plugin data by capturing the storage substrate, not by calling user-scoped <code>sdk.portability</code> export hooks.</p><ul><li><strong>Shared database plugins:</strong> plugin tables live in the platform DB, so they are included in the platform SQLite backup or Postgres dump.</li><li><strong>Isolated SQLite plugins:</strong> each provisioned plugin DB under <code>data/plugins/&lt;plugin-id&gt;.db</code> is captured through the same SQLite online backup path as the platform/auth DBs, including sidecar or generated backup files as needed.</li><li><strong>Isolated Postgres plugins:</strong> each provisioned plugin database or schema is dumped as its own plugin artifact and listed separately in <code>backup-manifest.json</code>.</li><li><strong>Plugin file storage:</strong> once <code>sdk.storage</code> lands, local plugin-owned files are included under the plugin&#39;s storage root and inventoried by plugin ID with file counts, byte totals, and checksums.</li><li><strong>Plugin secret vault data:</strong> encrypted vault ciphertext and metadata are included through the platform DB. Plaintext secret values are never exported.</li><li><strong>Plugin install state:</strong> installed/enabled status, manifest metadata, and compatibility information are included so restore can detect missing or incompatible plugins before writing data.</li></ul><p>The bundle should not treat plugin source code as the primary backup artifact. Restore should reinstall or verify the matching plugin from its manifest and repository metadata where possible, then restore that plugin&#39;s data. For operator-owned local plugins, docs should explain that source control remains the source-of-truth for code while <code>sv backup</code> covers runtime data.</p><h3 id="env-handling" tabindex="-1"><code>.env</code> handling <a class="header-anchor" href="#env-handling" aria-label="Permalink to &quot;\`.env\` handling&quot;">​</a></h3><p>The root <code>.env</code> is sensitive because it contains auth secrets, admin keys, database credentials, SMTP credentials, VAPID private keys, and backup keys.</p><p>Rules:</p><ul><li>never include plaintext <code>.env</code> in a backup bundle;</li><li>create <code>config/.env.enc</code> only when a backup key/passphrase is present;</li><li>include <code>config/env.required.json</code> with the names of required variables and restore notes, not secret values;</li><li>include <code>config/env.public.json</code> only for non-secret deployment metadata that helps operators reconstruct a restore environment;</li><li>never store the backup passphrase or key file inside the backup.</li></ul><p>Restoring <code>.env.enc</code> is a separate explicit step:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> env</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backup-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .env.restored</span></span></code></pre></div><p>This avoids silently overwriting the current host&#39;s runtime secrets during a data restore.</p><h3 id="backup-consistency" tabindex="-1">Backup consistency <a class="header-anchor" href="#backup-consistency" aria-label="Permalink to &quot;Backup consistency&quot;">​</a></h3><p>SQLite backups should use SQLite&#39;s online backup API or <code>VACUUM INTO</code> for each database file instead of tarring live <code>.db</code> files directly. During backup, <code>sv</code> should briefly pause writes or acquire a platform backup lock so auth DB, platform DB, isolated plugin DBs, and files represent one restore point. If a fully consistent cross-store lock is not available yet, the manifest must say so and mark the backup as <code>consistency: &quot;best-effort&quot;</code>.</p><p>Postgres backups use <code>pg_dump --format=custom</code> for platform and auth databases. For a same-server multi-database deployment, exact cross-database snapshot isolation is not portable with plain <code>pg_dump</code>; the backup should be documented as near-consistent unless a future coordinated lock is added.</p><h3 id="scoped-plugin-restore" tabindex="-1">Scoped plugin restore <a class="header-anchor" href="#scoped-plugin-restore" aria-label="Permalink to &quot;Scoped plugin restore&quot;">​</a></h3><p>Operators sometimes need to restore one plugin without rolling the whole instance back. <code>sv restore plugin &lt;plugin-id&gt; --from &lt;backup-tag&gt;</code> restores only that plugin&#39;s data artifacts:</p><ul><li>shared DB plugin tables matching the plugin&#39;s slug/table prefix;</li><li>isolated plugin DB files or Postgres database/schema dumps;</li><li>plugin-owned file storage;</li><li>plugin-scoped vault ciphertext/metadata when the vault supports scoped restore;</li><li>plugin install status only when <code>--include-install-state</code> is passed.</li></ul><p>Scoped restore is destructive for that plugin&#39;s current data by default and must require explicit confirmation. It should support a safer staging mode:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plugin</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">plugin-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --from</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backup-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dry-run</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plugin</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">plugin-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --from</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backup-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --staging-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./restore-check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restore</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plugin</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">plugin-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --from</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">backup-ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --yes</span></span></code></pre></div><p>The manifest must identify whether a plugin is safe to restore independently. If the plugin has cross-plugin references, queued jobs, external connection state, or schema migrations newer than the selected backup, the CLI must either block or require <code>--force</code> with a warning that references may need repair. Platform-owned identity rows are never rolled back by a plugin-scoped restore.</p><h3 id="size-policy" tabindex="-1">Size policy <a class="header-anchor" href="#size-policy" aria-label="Permalink to &quot;Size policy&quot;">​</a></h3><p>Git is the default remote backend for small and medium self-hosted instances, not large binary archives. Default policy:</p><ul><li>warn above 250 MiB encrypted payload size;</li><li>require <code>--large-backup-ok</code> above 1 GiB;</li><li>refuse above <code>SV_BACKUP_MAX_BUNDLE_BYTES</code> unless the operator increases the limit explicitly;</li><li>document that deployments with repeated multi-GB backups should use a future object-storage backend instead of Git.</li></ul><p>This keeps the first backend practical while leaving room for later S3, restic/borg, or filesystem remotes.</p><h3 id="git-storage-model" tabindex="-1">Git storage model <a class="header-anchor" href="#git-storage-model" aria-label="Permalink to &quot;Git storage model&quot;">​</a></h3><p>Use one orphan commit per backup, tagged by backup ID:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sv-backup/2026-07-06T12-30-00Z/v0.15.0</span></span></code></pre></div><p>Each backup commit contains the encrypted backup payload and a small plaintext remote manifest with non-secret metadata needed for listing. An optional <code>backups</code> branch stores an index file for fast listing, but the large backup payloads are not kept in branch history.</p><p>This model makes deletion viable: deleting the tag makes the large backup commit unreachable. Actual object reclamation still depends on the Git server&#39;s garbage collection policy, which Sovereign cannot force across arbitrary Git servers.</p><h3 id="retention-and-deletion" tabindex="-1">Retention and deletion <a class="header-anchor" href="#retention-and-deletion" aria-label="Permalink to &quot;Retention and deletion&quot;">​</a></h3><p>Operators can delete by age or keep count:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> delete</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --older-than</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 30d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> delete</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --older-than</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 60d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> delete</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --keep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prune</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --older-than</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 60d</span></span></code></pre></div><p><code>delete</code> removes matching remote backup tags and updates the optional index. <code>prune</code> performs the same deletion and then runs local Git cleanup. Remote disk space may not shrink until the Git server performs garbage collection.</p><p>Deletion must be conservative:</p><ul><li>dry-run by default unless <code>--yes</code> is passed;</li><li>never delete the newest successful backup;</li><li>never delete a backup referenced by a configured protected tag pattern;</li><li>print the exact tags selected for deletion;</li><li>verify after deletion that <code>restore latest</code> still resolves to a valid backup.</li></ul><h3 id="restore-guards" tabindex="-1">Restore guards <a class="header-anchor" href="#restore-guards" aria-label="Permalink to &quot;Restore guards&quot;">​</a></h3><p>Restore validates before writing:</p><ul><li>backup manifest checksum and encrypted payload authentication;</li><li>required key/passphrase presence;</li><li>platform version compatibility;</li><li>DB dialect compatibility or a documented migration path;</li><li>expected artifact set for the selected dialect;</li><li>plugin manifest/version compatibility for full and plugin-scoped restores;</li><li>scoped-restore safety markers for plugin-only restore;</li><li>enough free disk space for staging and restore.</li></ul><p>By default, restore refuses a backup created by a newer platform version than the currently running code supports. Operators can override with <code>--force</code>, but the CLI should explain that the safer path is to run the matching or newer platform version first.</p><h3 id="security-properties" tabindex="-1">Security properties <a class="header-anchor" href="#security-properties" aria-label="Permalink to &quot;Security properties&quot;">​</a></h3><p>The Git remote receives only ciphertext by default. The Git token grants access to the backup repository but does not grant decrypt capability. The backup key or passphrase is operator-held and must be stored separately from the Git repo.</p><p>This protects against a leaked Git repo, stolen Git token, or compromised Git server storage. It does not protect against an attacker who controls the running Sovereign host while the backup key is available in process environment or on disk. That remains consistent with RFC 0008&#39;s at-rest encryption model.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Commit plaintext tarballs to Git.</strong> Simple, but contradicts the security guidance that raw backups expose all data. Rejected as the default. An explicit <code>--allow-plaintext-remote</code> escape hatch exists only for emergency operator-controlled cases and still encrypts <code>.env</code>.</li><li><strong>One linear branch with one backup commit after another.</strong> Easy to inspect, but deleting old backups does not remove their large blobs because branch history keeps them reachable. Rejected.</li><li><strong>Git LFS.</strong> Better large-file ergonomics, but not available on every Git server and adds another service contract. Deferred.</li><li><strong>Object storage as the first backend.</strong> Technically better for large backup objects, but Git is simpler for self-hosters and private infrastructure. Object storage should be the second backend if backup sizes outgrow Git.</li><li><strong>Automatically restore <code>.env</code> during data restore.</strong> Convenient, but risky because it can overwrite host-specific secrets, URLs, and credentials. Rejected in favor of an explicit <code>restore env</code> command.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li>Which encryption implementation should v1 use: age recipients, passphrase AES-GCM via Node crypto, or both?</li><li>Should backup locking be implemented as a platform DB advisory lock, an application maintenance mode, or a shorter per-artifact consistency marker?</li><li>Should remote backup scheduling live in <code>sv</code> only, or should Console later expose schedule/retention status for operators?</li><li>How should backup success/failure events be surfaced in activity logs once operator audit events exist?</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li>Add backup manifest and encrypted local archive support.</li><li>Add <code>.env.enc</code> capture and explicit <code>restore env</code>.</li><li>Add Git remote push/list/fetch by tag.</li><li>Add <code>restore latest</code> and restore guards.</li><li>Add scoped plugin restore with dry-run/staging support.</li><li>Add retention deletion/prune.</li><li>Update <code>.env.example</code>, <code>docs/self-hosting.md</code>, <code>docs/upgrade.md</code>, and <code>docs/security.md</code>.</li><li>Add tests for manifest validation, plugin artifact inventory, scoped plugin restore planning, encryption-required remote push, plaintext escape hatch behavior, tag selection, and retention deletion.</li></ol><p>No public SDK or UI package semver impact is expected. CLI behavior changes are operator-facing and should be covered by upgrade notes when scheduled.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr></tbody></table>`,80)])])}const u=s(n,[["render",l]]);export{k as __pageData,u as default};
