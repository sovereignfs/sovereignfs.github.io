import{_ as s,o as a,c as t,a2 as i}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"RFC 0002 — Cross-plugin data sharing","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0002-cross-plugin-data-sharing.md","filePath":"rfcs/0002-cross-plugin-data-sharing.md"}'),n={name:"rfcs/0002-cross-plugin-data-sharing.md"};function r(o,e,d,l,c,h){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0002-—-cross-plugin-data-sharing" tabindex="-1">RFC 0002 — Cross-plugin data sharing <a class="header-anchor" href="#rfc-0002-—-cross-plugin-data-sharing" aria-label="Permalink to &quot;RFC 0002 — Cross-plugin data sharing&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> SDK (<code>packages/sdk</code>), manifest schema (<code>packages/manifest</code>), runtime, <code>packages/ui</code>, Console/Account<br><strong>Incorporated into plan:</strong> Yes — scheduled as roadmap Task 0.5.11. The <strong>reserved <code>sdk.data</code> surface</strong> (stub) and the <code>data:provide</code> / <code>data:consume</code> permissions already landed (they throw <code>NotImplementedError</code>, like the other reserved surfaces); the full mechanism — consent model, manifest data-contract declarations, runtime resolution, audit log, and consent UI — lands in that task.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Define a <strong>consent-gated, pull-based, read-only</strong> mechanism that lets one plugin (the <strong>consumer</strong>) read another plugin&#39;s data (the <strong>provider</strong>) on behalf of the current user. A provider exposes one or more named, versioned <strong>data contracts</strong>; a consumer requests a contract and receives data only when the user has granted an explicit, revocable <strong>consent grant</strong>. The platform mediates every access — no plugin reads another plugin&#39;s tables directly.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// consumer</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.data.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ providerId, contract: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;things&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, version: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, params);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// provider</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.data.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provide</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;things&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> resolveThings</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(params));</span></span></code></pre></div><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign&#39;s plugins each own a slice of a user&#39;s data behind the SDK boundary (plugins must not import <code>runtime/src</code> or another plugin&#39;s internals; data lives in slug-prefixed tables in the shared schema). That isolation is correct, but it blocks legitimate, user-desired flows where one plugin could enrich itself from another&#39;s data — e.g. a plugin that aggregates or reconciles records another plugin already maintains, so the user doesn&#39;t re-enter them.</p><p>Today the only options are bad ones: duplicate data entry, or break the boundary. A first-class, <strong>consent-first</strong> sharing mechanism lets plugins compose around a user&#39;s data while keeping isolation, privacy, and user control intact — and keeps the principle that capabilities are explicit and platform-mediated.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>SDK boundary:</strong> plugins use <code>@sovereignfs/sdk</code> only; no cross-plugin imports (CLAUDE.md hard rules, SRS §3.6). There is no sanctioned way for plugin B to read plugin A&#39;s data.</li><li><strong>Reserved-surface pattern:</strong> the SDK already declares post-v1 surfaces (<code>storage</code>, <code>notifications</code>, <code>events</code>) as stubs that throw <code>NotImplementedError</code> (<code>packages/sdk/src/unimplemented.ts</code>), and the manifest permission enum reserves the matching capabilities (<code>packages/manifest/src/schema.ts</code>).</li><li><strong>Permissions:</strong> plugins declare capabilities in their manifest; the runtime is the enforcement point.</li><li><strong>Data scoping:</strong> user-scoped tables carry <code>tenant_id</code> + <code>user_id</code>; all access is already per-user, per-tenant.</li></ul><p>This RFC reuses that reserved-surface pattern: <code>sdk.data</code> and <code>data:*</code> permissions land now as stubs; the mechanism behind them is specified here for later.</p><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-data-contracts" tabindex="-1">1. Data contracts <a class="header-anchor" href="#_1-data-contracts" aria-label="Permalink to &quot;1. Data contracts&quot;">​</a></h3><p>A provider exposes <strong>named, versioned, read-only</strong> datasets (&quot;contracts&quot;). A contract is a stable shape (e.g. <code>things@1</code>) decoupled from the provider&#39;s internal tables — the provider may refactor storage without breaking consumers, as long as the contract shape holds. Versioning is by major integer; a provider may serve multiple versions during migration.</p><h3 id="_2-manifest-declarations" tabindex="-1">2. Manifest declarations <a class="header-anchor" href="#_2-manifest-declarations" aria-label="Permalink to &quot;2. Manifest declarations&quot;">​</a></h3><p>Plugins declare the contracts they expose and consume (deferred — added when accepted):</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;provides&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;contract&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;things&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;scope&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;consumes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;provider&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;pluginId&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;contract&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;things&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>provides</code> registers the contracts a provider serves.</li><li><code>consumes</code> is <strong>informational</strong> (powers consent UI, discovery, and build-time checks) — it is <strong>not</strong> the access gate. The gate is the runtime consent grant.</li><li>Validated at build time against the manifest schema.</li></ul><h3 id="_3-permissions" tabindex="-1">3. Permissions <a class="header-anchor" href="#_3-permissions" aria-label="Permalink to &quot;3. Permissions&quot;">​</a></h3><p>Two reserved permissions gate participation (added now, enforced later):</p><ul><li><code>data:provide</code> — the plugin may expose contracts.</li><li><code>data:consume</code> — the plugin may request other plugins&#39; contracts.</li></ul><h3 id="_4-consent" tabindex="-1">4. Consent <a class="header-anchor" href="#_4-consent" aria-label="Permalink to &quot;4. Consent&quot;">​</a></h3><p>Cross-plugin reads require an <strong>explicit, recorded, revocable user consent grant</strong> keyed on <code>(consumer plugin, provider plugin, contract, user)</code>:</p><ul><li>On a consumer&#39;s first <code>query</code> without a grant, the runtime raises <code>ConsentRequiredError</code> and surfaces a <strong>consent prompt</strong> describing exactly what the consumer wants to read from which provider, for this user.</li><li>Grants are stored in a platform table (consumer, provider, contract, user, <code>granted_at</code>, <code>revoked_at</code>, scope) and are <strong>revocable</strong> from Account (the user&#39;s own grants) and visible/auditable in Console.</li><li>Revoking a grant blocks subsequent reads immediately.</li></ul><h3 id="_5-sdk-surface" tabindex="-1">5. SDK surface <a class="header-anchor" href="#_5-sdk-surface" aria-label="Permalink to &quot;5. SDK surface&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// consumer — resolves only with an active consent grant, else ConsentRequiredError</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.data.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ref: DataContractRef, params</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TRow[]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// provider — register a resolver for a contract it exposes</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.data.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provide</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(contract: string, resolver: DataContractResolver): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>query</code> is <strong>read-only</strong> and the runtime scopes it to the current user + tenant before invoking the provider&#39;s resolver. The reserved stub (<code>packages/sdk/src/data.ts</code>) throws <code>NotImplementedError</code> until the mechanism lands; <code>ConsentRequiredError</code> is reserved in <code>packages/sdk/src/errors.ts</code>.</p><h3 id="_6-security-and-scoping" tabindex="-1">6. Security and scoping <a class="header-anchor" href="#_6-security-and-scoping" aria-label="Permalink to &quot;6. Security and scoping&quot;">​</a></h3><ul><li><strong>Read-only.</strong> No write-back across plugins in this RFC.</li><li><strong>Platform-mediated.</strong> Consumers never touch provider tables; the runtime routes <code>query</code> to the provider&#39;s registered resolver. The SDK boundary is preserved.</li><li><strong>User- and tenant-scoped.</strong> The runtime injects the requesting user/tenant; a provider resolver cannot read another user&#39;s data.</li><li><strong>Audited.</strong> Every cross-plugin read is logged (consumer, provider, contract, user, timestamp) for transparency.</li><li><strong>Sync is the consumer&#39;s job.</strong> The mechanism is query/resolve; mapping fetched data into the consumer&#39;s own records (and any scheduling) is plugin logic.</li></ul><h3 id="example-abstract" tabindex="-1">Example (abstract) <a class="header-anchor" href="#example-abstract" aria-label="Permalink to &quot;Example (abstract)&quot;">​</a></h3><blockquote><p><em>Plugin A</em> exposes a <code>things</code> contract (its records, as a stable read-only shape). <em>Plugin B</em> declares <code>consumes</code> of <code>A/things@1</code> and holds <code>data:consume</code>. With the user&#39;s consent, <em>Plugin B</em> calls <code>sdk.data.query({ providerId: &#39;A&#39;, contract: &#39;things&#39;, version: 1 })</code> and maps the result into its own records, re-syncing periodically. Without consent, the call raises <code>ConsentRequiredError</code>.</p></blockquote><p>No real plugin is named here intentionally — the mechanism is generic and any plugin may be a provider, a consumer, or both.</p><h2 id="impact-when-accepted-deferred-—-beyond-the-reserved-stub-already-landed" tabindex="-1">Impact when accepted (deferred — beyond the reserved stub already landed) <a class="header-anchor" href="#impact-when-accepted-deferred-—-beyond-the-reserved-stub-already-landed" aria-label="Permalink to &quot;Impact when accepted (deferred — beyond the reserved stub already landed)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Where</th><th>Change</th></tr></thead><tbody><tr><td><code>packages/manifest</code></td><td>Add the optional <code>data.provides[]</code> / <code>data.consumes[]</code> declarations; tests; <strong>minor</strong> bump.</td></tr><tr><td><code>packages/sdk</code></td><td>Implement <code>sdk.data.query</code>/<code>provide</code> against the runtime (replace the stub).</td></tr><tr><td>Runtime</td><td>Provider-resolver registry, consent enforcement, user/tenant scoping, audit log.</td></tr><tr><td>Platform DB</td><td><code>consent_grants</code> + <code>data_access_log</code> tables (with <code>tenant_id</code>).</td></tr><tr><td><code>packages/ui</code></td><td>Consent-prompt dialog primitive.</td></tr><tr><td>Console / Account</td><td>Manage/revoke grants (Account: own grants; Console: audit/oversight).</td></tr><tr><td>SRS §3 / §5</td><td>Promote the mechanism from &quot;post-v1 plan&quot; to specified; manifest reference for <code>data.*</code>.</td></tr><tr><td><code>ROADMAP.md</code></td><td>The implementation task (already stubbed as a future task entry).</td></tr></tbody></table><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Direct cross-plugin table reads.</strong> Breaks the SDK boundary and isolation; a consumer would couple to a provider&#39;s storage. Rejected.</li><li><strong>Push-only via <code>events</code>.</strong> A provider could publish change events a consumer subscribes to. Useful later, but it does not cover &quot;read current state on demand with consent,&quot; and pushes data without a per-contract consent gate. Complementary, not a replacement — out of scope here.</li><li><strong>Per-call consent prompts (no stored grant).</strong> Prompting on every read is hostile to sync/automation. A stored, revocable grant is the right model.</li><li><strong>Write-through sharing.</strong> Letting a consumer mutate a provider&#39;s data is a far larger surface (conflict, ownership, audit). Explicitly out of scope; this RFC is read-only.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Grant granularity.</strong> Per <code>(consumer, provider, contract)</code> (proposed) vs. per-provider blanket consent. Proposal: per-contract — least surprise.</li><li><strong>Contract discovery.</strong> Do consumers bind to a fixed <code>providerId</code>, or to a contract &quot;kind&quot; any provider can satisfy (e.g. several plugins exposing a <code>things</code> contract)? Proposal: fixed <code>providerId</code> in v1; capability-style discovery later.</li><li><strong>Caching/staleness.</strong> Does the runtime cache <code>query</code> results, or always hit the provider resolver? Proposal: no platform cache in v1; consumers cache if needed.</li><li><strong>Versioning negotiation.</strong> Behaviour when a consumer requests a version the provider no longer serves — hard error vs. negotiated downgrade. Proposal: hard <code>error</code>; providers keep old majors during migration windows.</li><li><strong>Consent expiry.</strong> Should grants expire or require periodic re-confirmation? Proposal: no expiry in v1; revocation is always available.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Now (this change):</strong> reserved <code>sdk.data</code> stub + <code>data:provide</code>/<code>data:consume</code> permissions + <code>ConsentRequiredError</code>, all additive (SDK/manifest <strong>minor</strong> bumps). No behaviour change for existing plugins.</li><li><strong>On acceptance:</strong> apply the &quot;Impact when accepted&quot; table — manifest <code>data.*</code>, consent tables + UI, runtime resolution, then SDK implementation.</li><li>The mechanism becomes part of the public manifest + SDK contract from the <code>@sovereignfs/sdk</code> / <code>@sovereignfs/manifest</code> minor release that ships it.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft; reserved <code>sdk.data</code> stub + <code>data:*</code> permissions.</td></tr><tr><td>0.2</td><td>Jun 2026</td><td>Accepted; scheduled in the roadmap as Task 0.5.11.</td></tr></tbody></table>`,43)])])}const u=s(n,[["render",r]]);export{g as __pageData,u as default};
