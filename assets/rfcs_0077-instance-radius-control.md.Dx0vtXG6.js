import{_ as s,o as t,c as i,a2 as a}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"RFC 0077 — Instance-level corner radius control","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0077-instance-radius-control.md"},"headers":[],"relativePath":"rfcs/0077-instance-radius-control.md","filePath":"rfcs/0077-instance-radius-control.md"}'),n={name:"rfcs/0077-instance-radius-control.md"};function o(l,e,r,d,h,c){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0077-—-instance-level-corner-radius-control" tabindex="-1">RFC 0077 — Instance-level corner radius control <a class="header-anchor" href="#rfc-0077-—-instance-level-corner-radius-control" aria-label="Permalink to &quot;RFC 0077 — Instance-level corner radius control&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/ui</code>, <code>packages/db</code>, <code>runtime</code>, <code>plugins/console</code>, <code>.env.example</code>, docs; extends RFC 0027 / RFC 0032 (instance identity theming)<br><strong>Incorporated into plan:</strong> Yes — every step in this RFC&#39;s Adoption path has shipped: <code>--sv-radius-scale</code> and the <code>calc()</code>-based radius tokens in <code>packages/ui/src/tokens/primitives.css</code>; the <code>brand_radius</code>/<code>instanceRadius</code> column and migration in both <code>packages/db</code> schemas (sqlite <code>migrations/sqlite/0018_odd_jigsaw.sql</code>, postgres <code>migrations/postgres/0018_melodic_leopardon.sql</code>); the <code>RADIUS_SCALE</code> lookup and emission in <code>runtime/src/instance-style.ts</code>; the Console <code>Select</code> control in <code>plugins/console/app/settings/SettingsForms.tsx</code>; the <code>INSTANCE_RADIUS</code> env var and its <code>docs/self-hosting.md</code> entry; the <code>docs/design-system.md</code> token-architecture exception note; and all five presets in <code>packages/ui/src/stories/TokenGallery.stories.tsx</code>. Open question #2 (SegmentedControl vs. Select) resolved as <code>Select</code>. Open question #3 (a live-updating preview in Console) was never built — it was filed as a non-blocking nice-to-have, not an adoption-path step, so its absence doesn&#39;t change this RFC&#39;s status.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Add a five-step, instance-level &quot;corner radius&quot; setting — <strong>None / XS / S / M / L</strong> — that an operator picks once (Console → Instance identity, or an <code>INSTANCE_RADIUS</code> env var for the single-tenant case) and which then scales every rounded corner across the whole design system. <strong>None</strong> produces fully square corners everywhere; <strong>M</strong> is today&#39;s shipped look, byte-for-byte unchanged, so existing instances see zero visual difference until an operator opts in; <strong>L</strong> pushes past today&#39;s values enough that short elements — buttons, badges, small popovers — render as full pills/stadiums (see &quot;Why L doesn&#39;t need a separate mechanism&quot; below).</p><p>The five presets are one proportional scale factor (<code>--sv-radius-scale</code>) applied to today&#39;s six corner-radius tokens (<code>--sv-radius-sm</code> through <code>-3xl</code>) via <code>calc()</code>. Delivered through the <strong>exact same</strong> per-tenant <code>instance_config</code> → <code>InstanceProvider</code> → injected <code>&lt;style&gt;</code> pipeline that already powers accent-colour theming (RFC 0027/0032) — no new delivery mechanism, and <strong>no component changes anywhere</strong>, because every component already reads <code>--sv-radius-sm</code>/<code>-md</code>/etc. by name and those names don&#39;t change.</p><p><code>--sv-radius-full</code> (avatars, pill badges, the toggle switch knob) is deliberately <strong>not</strong> touched by this setting — see &quot;Alternatives considered.&quot;</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Some operators want a stricter, more utilitarian visual language (square corners read as &quot;enterprise tool,&quot; not &quot;consumer app&quot;) without hand-overriding dozens of individual component styles one at a time. Today radius is documented as a &quot;theme-stable&quot; scale token (<code>docs/design-system.md</code> — Token architecture: &quot;The scale tokens (<code>--sv-space-*</code>, <code>--sv-radius-*</code>, <code>--sv-font-size-*</code>) have no separate semantic tier — they are theme-stable and used directly&quot;). This RFC carves out one deliberate exception to that rule, on the same precedent RFC 0027 already set for <code>--sv-color-accent</code>: a token that was nominally &quot;fixed&quot; until an instance-level control made it operator-configurable.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>Radius primitives</strong> — six corner tokens plus one shape token, defined once at <code>packages/ui/src/tokens/primitives.css:87-93</code>: <code>--sv-radius-sm</code> (6px), <code>-md</code> (8px), <code>-lg</code> (11px), <code>-xl</code> (12px), <code>-2xl</code> (14px), <code>-3xl</code> (20px), <code>-full</code> (9999px). Every component references these by name (e.g. <code>packages/ui/src/components/Menu/Menu.module.css:30</code> — <code>border-radius: var(--sv-radius-sm)</code>); none hardcode a literal radius (enforced by <code>pnpm design:tokens:check</code>).</li><li><strong>Instance identity storage</strong> — <code>instance_config</code> table (<code>packages/db/src/schema/sqlite/platform.ts:552-569</code>), one row per tenant, columns named <code>brand_*</code> even after RFC 0032 renamed the <em>application-level</em> field names to <code>instance*</code> (the DB columns were left as-is; see the <code>instanceName: text(&#39;brand_name&#39;)</code> mapping at line 555). <code>InstanceConfig</code> (<code>packages/db/src/platform-db.ts:2895-2903</code>), <code>getInstanceConfig</code> (line 2920) and <code>setInstanceConfig</code> (line 2964) read/write it; <code>instancePrimary</code> is validated against <code>HEX_COLOR_RE</code> before it can reach a <code>&lt;style&gt;</code> block (line 2969).</li><li><strong>Delivery mechanism</strong> — <code>runtime/src/instance-provider.tsx</code>&#39;s <code>buildInstanceStyle()</code> (line 35) builds a <code>:root { ... }</code> string from the config and <code>InstanceProvider</code> renders it as an inline <code>&lt;style&gt;</code> tag ahead of its render-prop <code>children</code> (line 108-111). <strong>This is the only place instance config becomes CSS.</strong> It matters where: <code>InstanceProvider</code> is used exactly once, inside <code>runtime/app/(platform)/layout.tsx:182</code>, wrapping the authenticated shell. It is <strong>not</strong> used in the synchronous root <code>runtime/app/layout.tsx</code> (which has no DB access — it doesn&#39;t <code>async</code>/await anything) and <strong>not</strong> in <code>apps/auth</code>. Accent-colour theming today therefore only reaches the authenticated platform shell, not the standalone login/register pages — a known, pre-existing gap this RFC inherits rather than solves (see Open questions).</li><li><strong>Console UI precedent</strong> — <code>InstanceForm</code> in <code>plugins/console/app/settings/SettingsForms.tsx:240-293</code> renders the existing instance-identity fields (name, primary colour, logos) as a <code>useActionState</code> form; <code>plugins/console/app/identity/page.tsx</code> loads current values via <code>/api/admin/instance-config</code> and renders the form. <code>SegmentedControl</code> (<code>packages/ui/src/components/SegmentedControl/</code>) is the existing DS component for a 2–3 option picker; at 5 options this preset picker is a stretch for it (see §4 below).</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-token-layer-—-one-scale-factor-six-calc-tokens" tabindex="-1">1. Token layer — one scale factor, six <code>calc()</code> tokens <a class="header-anchor" href="#_1-token-layer-—-one-scale-factor-six-calc-tokens" aria-label="Permalink to &quot;1. Token layer — one scale factor, six \`calc()\` tokens&quot;">​</a></h3><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* packages/ui/src/tokens/primitives.css */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">:root</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* M — today&#39;s look. Overridden per-instance below. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-sm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">calc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.375</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">rem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-md</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">calc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.5</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">rem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-lg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">calc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.6875</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">rem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-xl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">calc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.75</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">rem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-2xl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">calc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.875</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">rem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-3xl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">calc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-radius-scale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1.25</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">rem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --sv-radius-full</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9999</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">px</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* unaffected — see Alternatives considered */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>No component changes: <code>--sv-radius-sm</code> etc. keep their names and their default values at <code>--sv-radius-scale: 1</code>, so a package consumer who never sets the scale sees pixel-identical output to today.</p><p>Proposed scale factors per preset (open to visual-QA adjustment — see Open questions):</p><table tabindex="0"><thead><tr><th>Preset</th><th style="text-align:right;"><code>--sv-radius-scale</code></th><th style="text-align:right;">sm (6px)</th><th style="text-align:right;">md (8px)</th><th style="text-align:right;">lg (11px)</th><th style="text-align:right;">xl (12px)</th><th style="text-align:right;">2xl (14px)</th><th style="text-align:right;">3xl (20px)</th></tr></thead><tbody><tr><td>None</td><td style="text-align:right;">0</td><td style="text-align:right;">0</td><td style="text-align:right;">0</td><td style="text-align:right;">0</td><td style="text-align:right;">0</td><td style="text-align:right;">0</td><td style="text-align:right;">0</td></tr><tr><td>XS</td><td style="text-align:right;">0.35</td><td style="text-align:right;">~2px</td><td style="text-align:right;">~3px</td><td style="text-align:right;">~4px</td><td style="text-align:right;">~4px</td><td style="text-align:right;">~5px</td><td style="text-align:right;">~7px</td></tr><tr><td>S</td><td style="text-align:right;">0.65</td><td style="text-align:right;">~4px</td><td style="text-align:right;">~5px</td><td style="text-align:right;">~7px</td><td style="text-align:right;">~8px</td><td style="text-align:right;">~9px</td><td style="text-align:right;">~13px</td></tr><tr><td>M</td><td style="text-align:right;">1</td><td style="text-align:right;">6px</td><td style="text-align:right;">8px</td><td style="text-align:right;">11px</td><td style="text-align:right;">12px</td><td style="text-align:right;">14px</td><td style="text-align:right;">20px</td></tr><tr><td>L</td><td style="text-align:right;">2.75</td><td style="text-align:right;">~17px</td><td style="text-align:right;">~22px</td><td style="text-align:right;">~30px</td><td style="text-align:right;">~33px</td><td style="text-align:right;">~39px</td><td style="text-align:right;">~55px</td></tr></tbody></table><h4 id="why-l-doesn-t-need-a-separate-mechanism" tabindex="-1">Why L doesn&#39;t need a separate mechanism <a class="header-anchor" href="#why-l-doesn-t-need-a-separate-mechanism" aria-label="Permalink to &quot;Why L doesn&#39;t need a separate mechanism&quot;">​</a></h4><p><code>border-radius</code> is spec-clamped: the UA caps a box&#39;s used radius at half its own height (and, for adjacent corners, proportionally scales all four down together so they never overlap — <a href="https://www.w3.org/TR/css-backgrounds-3/#corner-overlap" target="_blank" rel="noreferrer">CSS Backgrounds §5.5</a>). That&#39;s the standard trick behind every hand-written &quot;pill button&quot; (<code>border-radius: 9999px</code> on a 40px-tall element clips to 20px automatically).</p><p>Button heights are 32/36/40px (<code>Button.module.css:39</code> — sm/md/lg); at L&#39;s <code>--sv-radius-md ≈ 22px</code>, even the tallest (40px, half-height 20px) clips to a full pill. Badges (~18px tall) clip at L&#39;s <code>--sv-radius-sm ≈ 17px</code> too. Cards, dialogs, and drawers are large enough that the same scale factor just reads as &quot;very rounded,&quot; not literally circular — which matches &quot;full curvy UI&quot; without needing a second mechanism, a different token set, or per-component overrides. <code>--sv-radius-full</code> still doesn&#39;t move (it&#39;s already maximally round) — L doesn&#39;t change that story, it just makes the <em>other</em> six tokens big enough to hit the same visual result on short elements.</p><h3 id="_2-storage-—-extend-instance-config" tabindex="-1">2. Storage — extend <code>instance_config</code> <a class="header-anchor" href="#_2-storage-—-extend-instance-config" aria-label="Permalink to &quot;2. Storage — extend \`instance_config\`&quot;">​</a></h3><p>Add one nullable column, following the existing <code>brand_*</code> naming:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/db/src/schema/{sqlite,postgres}/platform.ts</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** Corner-radius intensity preset overriding --sv-radius-scale. Null = default (M). */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">instanceRadius</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;brand_radius&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;none&#39; | &#39;xs&#39; | &#39;s&#39; | &#39;m&#39; | &#39;l&#39;</span></span></code></pre></div><p>plus a Drizzle migration (mirrors <code>migrations/sqlite/0005_rename_tenant_branding.sql</code>&#39;s scope for the earlier rename).</p><p><code>InstanceConfig</code> (<code>platform-db.ts:2895</code>) gains:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">instanceRadius</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;none&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;xs&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;m&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;l&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>getInstanceConfig</code>/<code>setInstanceConfig</code> extend in parallel with the existing <code>instancePrimary</code> handling — <code>setInstanceConfig</code> validates against the 5-value literal union the same way it validates <code>HEX_COLOR_RE</code> today (line 2969), rejecting anything else before it can reach persistence.</p><p>Env var fallback for the single-tenant case, matching <code>INSTANCE_PRIMARY_COLOR</code>: <code>INSTANCE_RADIUS</code> (<code>.env.example</code>, <code>docs/self-hosting.md</code> — required by CLAUDE.md&#39;s env-var doc-parity convention).</p><h3 id="_3-delivery-—-one-more-line-in-buildinstancestyle" tabindex="-1">3. Delivery — one more line in <code>buildInstanceStyle()</code> <a class="header-anchor" href="#_3-delivery-—-one-more-line-in-buildinstancestyle" aria-label="Permalink to &quot;3. Delivery — one more line in \`buildInstanceStyle()\`&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// runtime/src/instance-provider.tsx</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> RADIUS_SCALE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;none&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;xs&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;m&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;l&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  none: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  xs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.35</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  s: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.65</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  m: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  l: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.75</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (config.instanceRadius) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  lines.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`  --sv-radius-scale: \${</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RADIUS_SCALE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">[</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">instanceRadius</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">]</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">};\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This reuses the existing <code>:root { ... }</code> block and <code>&lt;style&gt;</code> tag verbatim — no new injection surface, no new CSP consideration, no math (a plain lookup table, safer than the accent-colour path&#39;s HSL conversion since there&#39;s nothing to compute from user input).</p><p><strong>Course correction from our chat:</strong> I&#39;d floated a <code>data-radius=&quot;none|xs|s|m&quot;</code> attribute on <code>&lt;html&gt;</code> (the same mechanism as <code>data-theme</code>) as the cleaner option, and that&#39;s genuinely the nicer shape <em>in isolation</em>. But <code>runtime/app/layout.tsx</code>&#39;s <code>&lt;html&gt;</code> is rendered by a synchronous root layout with no DB access — <code>InstanceProvider</code> only exists three layouts deeper, at <code>(platform)/layout.tsx</code>, and renders a <code>&lt;style&gt;</code> tag rather than wrapping a DOM element it could stamp an attribute onto. <code>:root { }</code> CSS injected from a <code>&lt;style&gt;</code> tag applies document-wide regardless of where in the tree that tag sits, which is <em>why</em> accent-colour used this path in the first place — it&#39;s the only channel that reaches document scope from where <code>InstanceProvider</code> actually renders. Given that constraint, extending the same pipe is simpler than restructuring <code>RootLayout</code> to be async just to host one more data attribute.</p><h3 id="_4-console-ui" tabindex="-1">4. Console UI <a class="header-anchor" href="#_4-console-ui" aria-label="Permalink to &quot;4. Console UI&quot;">​</a></h3><p>Extend <code>InstanceForm</code> (<code>SettingsForms.tsx:240</code>) with a control offering None/XS/S/M/L below the existing &quot;Primary colour&quot; field, wired through the same <code>useActionState</code> action and <code>/api/admin/instance-config</code> route as the rest of the form — same gating, same save/feedback pattern, no new permission surface. <code>SegmentedControl</code> is documented for &quot;2–3 option&quot; use (<code>docs/design-system.md</code> — Component gallery); five options is a stretch for that visual style, so this may want a <code>Select</code> instead — a UI-fit call best made once the control is actually laid out next to the other fields, not decided in the abstract here.</p><h3 id="_5-design-system-doc" tabindex="-1">5. Design-system doc <a class="header-anchor" href="#_5-design-system-doc" aria-label="Permalink to &quot;5. Design-system doc&quot;">​</a></h3><p><code>docs/design-system.md</code>&#39;s Token architecture section currently states scale tokens are &quot;theme-stable&quot; without qualification. This needs a one-line exception carved out for radius specifically, pointing at this RFC — &quot;Docs are part of the change&quot; per CLAUDE.md.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Also flatten <code>--sv-radius-full</code> under &quot;None.&quot;</strong> Rejected (per our discussion) — <code>-full</code> produces circles and pills, a <em>shape</em> decision (avatar, toggle knob, pagination current-page indicator), not a &quot;how rounded are corners&quot; one. Flattening it would be a much bigger, more surprising visual change than &quot;square corners&quot; implies, and nothing in the request asked for square avatars.</li><li><strong>Hand-tuned pixel table per preset</strong> (24 hand-picked numbers instead of one scale factor). Rejected (per our discussion) — loses the guarantee that &quot;M&quot; equals today&#39;s tokens exactly, loses proportionality between steps, and is more to maintain for no clear benefit. The one real cost of the scale-factor approach is sub-pixel <code>calc()</code> output at XS/S (e.g. ~2.1px) — accepted, since browsers render fractional pixel radii fine.</li><li><strong><code>data-radius</code> attribute mirroring <code>data-theme</code>.</strong> Rejected once the actual render tree was checked — see &quot;Course correction&quot; above.</li><li><strong>Per-component/per-plugin override instead of a single theme-level control.</strong> Rejected — explicitly out of scope; the ask was one instance-wide knob, not per-surface control.</li><li><strong>Continuous slider (0–100%) instead of fixed presets.</strong> Rejected — the ask was fixed, named states matching a deliberately designed visual language, not open-ended variation.</li><li><strong>A separate &quot;pill mode&quot; flag for L instead of a bigger scale factor.</strong> Rejected — unnecessary. <code>border-radius</code>&#39;s own spec-mandated clamping to half the box height already turns a large-enough radius into a pill on short elements (see &quot;Why L doesn&#39;t need a separate mechanism&quot; above); a second flag would duplicate behavior the platform gets for free.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Exact scale factors for XS/S/L.</strong> 0.35/0.65/2.75 are reasonable starting points (L&#39;s is grounded in actual Button/Badge heights — see above — the other two are an even proportional split) but untested against every component — worth a visual pass in Storybook (Token Gallery / Design System Overview) before finalizing, since these are trivial to retune (one number each). L is the one preset that <em>increases</em> radius past today&#39;s baseline rather than reducing it, so it&#39;s worth specifically eyeballing the large panels (Dialog, Drawer&#39;s top corners at <code>--sv-radius-3xl ≈ 55px</code>) to confirm &quot;very curvy&quot; doesn&#39;t tip into &quot;looks broken&quot; at that scale — the small/short components (Button, Badge) are the ones grounded in exact math above; the large ones are the ones most likely to need retuning.</li><li><strong>SegmentedControl vs Select for 5 options in Console</strong> (see §4 above) — pick once the form is actually laid out.</li><li><strong>Live preview in Console.</strong> A static control tells the operator the <em>name</em> of what they&#39;re picking but not the look. A small live-updating swatch (a couple of components re-rendered with the pending value) would be a nicer admin experience — flagging as a nice-to-have, not blocking.</li><li><strong>Auth-app propagation.</strong> <code>apps/auth</code>&#39;s login/register pages and the synchronous runtime root layout don&#39;t consume <code>InstanceProvider</code> at all today, so neither accent colour nor this new radius setting reaches them. Pre-existing gap (tracked implicitly by RFC 0027&#39;s deferred &quot;Phase 2: email <ul><li>auth login&quot;), not something this RFC takes on.</li></ul></li><li><strong>Field/column naming</strong> — <code>instanceRadius</code> / <code>INSTANCE_RADIUS</code> / <code>brand_radius</code> column, <code>--sv-radius-scale</code> CSS var, <code>&#39;none&#39; | &#39;xs&#39; | &#39;s&#39; | &#39;m&#39; | &#39;l&#39;</code> values — all provisional pending sign-off before implementation starts.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Single PR is feasible — the surface area is small and, unlike RFC 0027, there is no auth-app work to phase separately (auth doesn&#39;t consume <code>InstanceProvider</code> today for anything, radius included). Suggested order: schema + migration → <code>InstanceConfig</code> plumbing → <code>primitives.css</code> scale conversion → <code>instance-provider.tsx</code> emission → Console form control → env var + doc parity (<code>.env.example</code>, <code>self-hosting.md</code>) → <code>design-system.md</code> exception note → Storybook Token Gallery update to show all five presets.</p><p><strong>Semver:</strong> <code>packages/ui</code> token changes are additive — <code>--sv-radius-scale</code> is new, and every existing <code>--sv-radius-*</code> token keeps its name and its default computed value. Not a breaking change to the published <code>@sovereignfs/ui</code> contract; ships as a minor bump per the repo&#39;s <code>feat/</code> convention.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr><tr><td>0.2</td><td>August 2026</td><td>Status corrected from Draft to Implemented — the feature shipped without this RFC&#39;s status line being updated at the time. Verified against the live code (token layer, both DB migrations, Console form, env var/doc parity, Storybook Token Gallery) rather than assumed from the design; no functional changes made.</td></tr></tbody></table>`,45)])])}const k=s(n,[["render",o]]);export{g as __pageData,k as default};
