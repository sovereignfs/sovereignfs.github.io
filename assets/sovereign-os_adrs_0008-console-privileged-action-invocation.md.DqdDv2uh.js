import{_ as o,o as t,c as a,a2 as i}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"ADR-0008: Console Privileged Action Invocation","description":"","frontmatter":{},"headers":[],"relativePath":"sovereign-os/adrs/0008-console-privileged-action-invocation.md","filePath":"sovereign-os/adrs/0008-console-privileged-action-invocation.md"}'),n={name:"sovereign-os/adrs/0008-console-privileged-action-invocation.md"};function s(r,e,c,d,l,h){return t(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="adr-0008-console-privileged-action-invocation" tabindex="-1">ADR-0008: Console Privileged Action Invocation <a class="header-anchor" href="#adr-0008-console-privileged-action-invocation" aria-label="Permalink to &quot;ADR-0008: Console Privileged Action Invocation&quot;">​</a></h1><p><strong>Status:</strong> Accepted <strong>Date:</strong> 2026-08-01 <strong>Decision owner:</strong> Project creator (delegated to the assistant for this decision — see Decision below) <strong>Related ADR:</strong> <a href="./0007-console-authentication.html">ADR-0007</a><strong>Related RFCs:</strong> <a href="./../rfcs/0014-appliance-update-system.html">RFC-0014</a>, <a href="./../rfcs/0015-update-discovery.html">RFC-0015</a><strong>Related milestone:</strong> <a href="./../../ROADMAP.html">Update Discovery and Console Controls</a><strong>Supersedes:</strong> None</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR-0007 gave Console a real login/session mechanism but explicitly left one problem unsolved: <em>&quot;this ADR addresses who&#39;s allowed to ask, not how the ask reaches root.&quot;</em> <code>console-auth</code> runs as an unprivileged, <code>DynamicUser</code>, <code>NoNewPrivileges=yes</code>, <code>ProtectSystem=strict</code> service — by design, the same hardening every other Console-adjacent process gets. It has no path today to actually cause <code>sovereign-update</code> (which requires root — <code>fail(os.geteuid() == 0, &quot;ROOT_REQUIRED&quot;, ...)</code> guards every mutating subcommand) to do anything.</p><p>RFC-0015 already deferred Console-triggered update actions pending exactly this ADR. This decision covers two things together, since the mechanism chosen constrains what can safely be exposed:</p><ol><li><strong>How</strong> an authenticated Console session gets a privileged action to actually happen, without widening <code>console-auth</code>&#39;s own privilege.</li><li><strong>What</strong> the first action exposed this way should be.</li></ol><h3 id="what-this-actually-protects" tabindex="-1">What this actually protects <a class="header-anchor" href="#what-this-actually-protects" aria-label="Permalink to &quot;What this actually protects&quot;">​</a></h3><p>Once any authenticated session can cause a root action, the question is the same shape as ADR-0007&#39;s: not protecting a secret, but gating <em>who is allowed to cause state change</em>, now one layer deeper — from &quot;who can open an authenticated session&quot; to &quot;what can an authenticated session actually make root do.&quot; A bug in the invocation mechanism itself could let a compromised or buggy <code>console-auth</code> process (still just a web server parsing untrusted HTTP requests) escalate arbitrary root execution, which would undo everything <code>NoNewPrivileges</code>/<code>DynamicUser</code>/ <code>ProtectSystem=strict</code> are there to contain.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>File-trigger + a <code>systemd</code> path-activated, root-owned oneshot runner</strong>, scoped for now to exactly one action: <code>sovereign-update check</code>. This decision was delegated to the assistant by the project owner (&quot;write ADRs and start working in them after&quot;) rather than reviewed line-by-line before acceptance, unlike ADR-0006/0007 — recorded here for anyone auditing this decision&#39;s provenance later.</p><ul><li><code>console-auth</code>, after verifying session + CSRF exactly like <code>logout</code> already does, writes an empty trigger file to <code>/data/sovereign/console/actions/check.request</code>. It has group-write access to that directory (<code>sovereign-console-secrets</code>, mode <code>0730</code> — write and traverse, no read) and nothing more; it cannot read the directory&#39;s contents, cannot invoke anything else. <code>ReadWritePaths=</code> for that one directory was added to its otherwise-unchanged hardening.</li><li>A <code>systemd</code> <code>.path</code> unit (<code>sovereign-console-check-trigger.path</code>, <code>PathExists=</code>) watches for that file and activates a oneshot <code>.service</code> (<code>sovereign-console-check-trigger.service</code>) running as root. The service&#39;s only job: remove the trigger file, then run <code>sovereign-update check</code>. Nothing about the trigger file&#39;s content is trusted or parsed — its mere existence is the only signal, so there is no request format for a compromised writer to abuse beyond &quot;cause <code>check</code> to run,&quot; which is already something <code>sovereign-update-check.timer</code> does unattended, daily, for every device.</li><li>Reading the result stays fully separate from triggering it, and stays unauthenticated: <code>/data/sovereign/update-check.json</code> was already designed world-readable, non-sensitive output (RFC-0015). <code>console-health</code> (already unprivileged, already loopback-only, already the read-only status surface) now also serves it, unauthenticated, exactly like every other health field. Only <em>causing a new check to run</em> costs anything (a live GitHub API call) and is worth gating; reading the last result costs nothing and was never meant to require a login.</li><li>Client-side triggering is additionally rate-limited (one trigger per 60 seconds, enforced in <code>console-auth</code>&#39;s existing in-memory rate-limit style) to bound how often an authenticated session can force a live GitHub API call.</li></ul><p>Scope, deliberately narrow: <strong>only <code>check</code> is wired this way in this pass.</strong> <code>prepare</code>/<code>backup</code>/<code>stage</code>/<code>activate</code> (an actual Console-triggered <em>install</em>) are explicitly out of scope here — they have a materially larger blast radius (data migration risk, service interruption, the whole health-gated rollback machinery), deserve their own confirmation UX and progress-reporting design, and should not inherit this decision by default just because the plumbing now exists. Extending this mechanism to install-triggering needs its own revisit (see Validation and Revisit Conditions).</p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><h3 id="option-a-—-sudo-with-a-narrow-nopasswd-rule" tabindex="-1">Option A — <code>sudo</code> with a narrow <code>NOPASSWD</code> rule <a class="header-anchor" href="#option-a-—-sudo-with-a-narrow-nopasswd-rule" aria-label="Permalink to &quot;Option A — `sudo` with a narrow `NOPASSWD` rule&quot;">​</a></h3><p><code>console-auth</code> execs <code>sudo -n /usr/sbin/sovereign-update check</code> under a <code>/etc/sudoers.d/</code> rule scoped to exactly that command.</p><ul><li><strong>Rejected outright, not just deprioritized:</strong> <code>console-auth</code> runs under <code>DynamicUser=yes</code>, which assigns a <em>new random username</em> on every service start. A static <code>sudoers</code> rule cannot reference a username that doesn&#39;t exist yet at write time and changes on every restart. Using this option would require first moving <code>console-auth</code> off <code>DynamicUser</code> onto a static system account — a real hardening regression (losing UID/GID randomization and the extra isolation <code>DynamicUser</code> provides) paid just to make <code>sudo</code> work, not because a static account is otherwise desirable here.</li></ul><h3 id="option-b-—-a-socket-listening-privileged-helper-daemon" tabindex="-1">Option B — a socket-listening privileged helper daemon <a class="header-anchor" href="#option-b-—-a-socket-listening-privileged-helper-daemon" aria-label="Permalink to &quot;Option B — a socket-listening privileged helper daemon&quot;">​</a></h3><p>A new root-owned service listens on a Unix domain socket (or loopback TCP, like <code>console-health</code>/<code>console-auth</code> already do); <code>console-auth</code> connects and sends a small authenticated-locally request.</p><ul><li>Workable, and closer to conventional service-to-service design.</li><li><strong>Not chosen:</strong> needs a request protocol (parsing, versioning, a second place a malformed message can be mishandled), a persistently-running root process idle most of the time, and its own socket-permission hardening — meaningfully more surface than &quot;does a specific file exist&quot; for a feature whose entire initial scope is &quot;run one specific, argument-free command.&quot; Worth reconsidering if/when this needs to carry real parameters (see Revisit Conditions).</li></ul><h3 id="option-c-—-policykit-polkit" tabindex="-1">Option C — PolicyKit (<code>polkit</code>) <a class="header-anchor" href="#option-c-—-policykit-polkit" aria-label="Permalink to &quot;Option C — PolicyKit (`polkit`)&quot;">​</a></h3><p>Define a <code>polkit</code> action and policy; <code>console-auth</code> requests it via <code>pkexec</code> or D-Bus.</p><ul><li>The standard Linux-native answer to exactly this class of problem.</li><li><strong>Not chosen:</strong> introduces a <code>polkit</code>/D-Bus dependency this project has deliberately avoided everywhere else — <code>sovereign-update</code>, <code>console-health</code>, and <code>console-auth</code> are all dependency-free stdlib Python by explicit, consistent choice throughout this codebase. Adding the first non-stdlib runtime dependency for one narrow trigger is disproportionate, and a minimal appliance image is exactly the kind of project where every added always-running system service has a real, ongoing image-size and attack-surface cost.</li></ul><h3 id="option-d-—-file-trigger-systemd-path-activated-oneshot-runner-chosen" tabindex="-1">Option D — file-trigger + <code>systemd</code> path-activated oneshot runner (chosen) <a class="header-anchor" href="#option-d-—-file-trigger-systemd-path-activated-oneshot-runner-chosen" aria-label="Permalink to &quot;Option D — file-trigger + `systemd` path-activated oneshot runner (chosen)&quot;">​</a></h3><p>Described in Decision above.</p><ul><li>No listening socket, no persistently-running privileged process (the oneshot only exists while handling one request), no request protocol to parse or version — the trigger file&#39;s <em>existence</em>, not its content, is the entire signal.</li><li>Matches this codebase&#39;s existing durable-file-state idioms closely (<code>sovereign-update</code>&#39;s own atomic transaction journal, the <code>.timer</code> units already running <code>sovereign-update check</code>/<code>prune</code> as root on a schedule — this is the same pattern, just event-triggered instead of time-triggered).</li><li><strong>Downside:</strong> doesn&#39;t generalize cleanly to actions that need real parameters (e.g., &quot;restore <em>this specific</em> backup ID&quot;) without adding a request format later — acceptable now because the one action in scope takes none, but a real constraint to weigh before extending this to more than <code>check</code>.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li><code>console-auth</code> gains zero new privilege — it can cause exactly one specific root command to run, with a mechanism where the untrusted input surface (an HTTP request handled by unprivileged code) is separated from the trusted action (a root process that trusts nothing but a file&#39;s existence) by a boundary neither side can widen from where it sits.</li><li>Unblocks the first genuinely useful thing an authenticated Console session can do, closing part of the &quot;Update Discovery and Console Controls&quot; milestone.</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li>Two new systemd units, a new directory with its own permission model, and a new endpoint pair (trigger + read) are real new surface, on top of everything ADR-0007 already added.</li><li>The file-existence-only protocol is deliberately inflexible; extending it to parameterized actions is a real, separate design cost later, not a small addition.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li>If the <code>.path</code>/<code>.service</code> pairing is misconfigured, the realistic failure mode is &quot;trigger silently does nothing&quot; (fails safe) rather than an unintended privilege escalation — worth confirming directly in hardware qualification rather than assuming from the unit file alone.</li><li>A compromised <code>console-auth</code> process, even though it cannot itself gain privilege, <em>can</em> still repeatedly write the trigger file — bounded by the 60-second client-side rate limit, but that limit lives in the same process being assumed compromised in this threat scenario. The real backstop is that the only consequence of unlimited triggering is unlimited <code>sovereign-update check</code> runs — already a command safe to run arbitrarily often (RFC-0015: read-only discovery, no mutation).</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="skip-the-mechanism-question-hardcode-a-one-off-for-check-only" tabindex="-1">Skip the mechanism question; hardcode a one-off for <code>check</code> only <a class="header-anchor" href="#skip-the-mechanism-question-hardcode-a-one-off-for-check-only" aria-label="Permalink to &quot;Skip the mechanism question; hardcode a one-off for `check` only&quot;">​</a></h3><p>Rejected — the actual hard problem (how does unprivileged Console code ever safely reach root) doesn&#39;t go away by special-casing one command; it would just resurface, unexamined, the moment a second action is needed.</p><h2 id="hardware-qualification" tabindex="-1">Hardware Qualification <a class="header-anchor" href="#hardware-qualification" aria-label="Permalink to &quot;Hardware Qualification&quot;">​</a></h2><p>Hardware-qualified on Raspberry Pi 5, deployed permanently alongside the already-live Console authentication — see the <a href="./../research/console-check-trigger-hardware-qualification-report.html">qualification report</a>. The full real chain (unauthenticated rejection, CSRF enforcement, trigger, the <code>.path</code>/<code>.service</code> pair actually firing and running <code>sovereign-update check</code> as root, the result appearing in <code>/api/v1/update/check</code>, cooldown enforcement) verified correctly — after fixing two real defects design review alone didn&#39;t catch: the auth service&#39;s unit was missing <code>ReadWritePaths=</code> for the new trigger directory (silent <code>503</code> on every write), and the static group backing it was named <code>sovereign-console</code> — colliding with <code>sovereign-console.service</code>&#39;s (unrelated <code>console-health</code>) own <code>DynamicUser</code>-derived identity, breaking that pre-existing service&#39;s ability to restart at all. Renamed to <code>sovereign-console-secrets</code> throughout.</p><h2 id="validation-and-revisit-conditions" tabindex="-1">Validation and Revisit Conditions <a class="header-anchor" href="#validation-and-revisit-conditions" aria-label="Permalink to &quot;Validation and Revisit Conditions&quot;">​</a></h2><p>Revisit this ADR before extending this mechanism to any action that: takes parameters (a specific backup ID, a specific target version); has a blast radius beyond &quot;runs a read-only discovery check&quot; (anything in the <code>prepare</code>/<code>backup</code>/<code>stage</code>/<code>activate</code>/<code>restore</code>/<code>rotate-trust</code> family); or needs to report granular in-progress status back to Console rather than a single before/after result file. Any of those likely favors Option B (a real request protocol) over continuing to stretch Option D&#39;s file-existence-only signal.</p>',39)])])}const p=o(n,[["render",s]]);export{g as __pageData,p as default};
