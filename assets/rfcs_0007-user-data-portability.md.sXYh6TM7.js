import{_ as e,o as t,c as a,a2 as i}from"./chunks/framework.CHeM0PsO.js";const k=JSON.parse('{"title":"RFC 0007 — User data export & portability","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0007-user-data-portability.md","filePath":"rfcs/0007-user-data-portability.md"}'),n={name:"rfcs/0007-user-data-portability.md"};function r(o,s,d,l,p,h){return t(),a("div",null,[...s[0]||(s[0]=[i(`<h1 id="rfc-0007-—-user-data-export-portability" tabindex="-1">RFC 0007 — User data export &amp; portability <a class="header-anchor" href="#rfc-0007-—-user-data-export-portability" aria-label="Permalink to &quot;RFC 0007 — User data export &amp; portability&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> SDK (<code>packages/sdk</code>), manifest schema (<code>packages/manifest</code>), runtime, Account, reference plugins<br><strong>Incorporated into plan:</strong> Yes — SRS §3.16, §5 (<code>data:export</code> / <code>data:import</code>), and <strong>Task 0.5.15</strong>. The reserved <code>sdk.portability</code> surface + permissions and the export/import/cross-instance-migration mechanism land in that task (the reserved stub is applied there, after RFC 0005&#39;s SDK/manifest stubs).</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Give every user a <strong>self-service &quot;data takeout&quot;</strong>: export their own data to a <strong>versioned ZIP</strong>, <strong>restore/import</strong> it (recovery on the same instance), and <strong>migrate</strong> it to a different Sovereign instance. Plugins contribute their slice of a user&#39;s data through a reserved <code>sdk.portability</code> participation surface; the platform assembles, validates, and (on import) re-keys the bundle.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// a plugin opts into portability (reserved — throws NotImplementedError for now)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.portability.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideExport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collectMyRowsFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx.userId));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.portability.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideImport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">section</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> restoreMyRowsFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(section, ctx.userId));</span></span></code></pre></div><p>This is <strong>user-facing data ownership</strong>, the flagship expression of the project&#39;s name — and deliberately <strong>distinct from RFC 0006</strong> (operator/whole-instance backup for disaster recovery and rollback).</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign&#39;s premise is that users own their data, yet today there is <strong>no way to get it out</strong>: no export, no portability, no migration path off an instance. That is both a trust gap and, for many operators, a compliance one (GDPR-style data portability / right to access).</p><p>The hard part is that a user&#39;s data is <strong>scattered and heterogeneously owned</strong>. Platform tables key on <code>user_id</code>; avatars live on disk; plugin tables are slug-prefixed and express ownership differently (<code>owner_id</code>, <code>created_by</code>, <code>user_id</code>) with membership join tables for shared resources. There is <strong>no generic &quot;select this user&#39;s rows&quot;</strong> — so a faithful export requires each plugin to describe and produce its own slice. This RFC defines that participation model and the bundle that carries the result.</p><p>No export/portability code exists yet — this is greenfield.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>Reserved-surface pattern:</strong> the SDK declares post-v1 surfaces as stubs that throw <code>NotImplementedError</code> (<code>packages/sdk/src/unimplemented.ts</code>, <code>packages/sdk/src/data.ts</code>), and the manifest permission enum reserves the matching capabilities (<code>packages/manifest/src/schema.ts</code>). This RFC reuses that pattern for <code>sdk.portability</code> + <code>data:export</code> / <code>data:import</code>.</li><li><strong>Account self-service surface:</strong> tabs (Profile / Preferences / Security) in <code>plugins/account/app/layout.tsx</code>, server actions in <code>plugins/account/app/actions.ts</code> — the natural home for a new <strong>&quot;Data&quot;</strong> tab.</li><li><strong>Per-user data is scattered:</strong> platform <code>users</code> / <code>account_prefs</code> (keyed by <code>user_id</code>) + avatars on disk at <code>data/avatars/&lt;userId&gt;.&lt;ext&gt;</code>; plugin tables carry <code>tenant_id</code> with varied ownership columns (<code>packages/db/src/schema/{sqlite,postgres}/platform.ts</code>; <code>docs/plugins/tasks.md</code>, <code>docs/plugins/splitify.md</code>).</li><li><strong>File download/upload patterns to reuse:</strong> the avatar routes (<code>runtime/app/api/account/avatar/[userId]/route.ts</code>, <code>runtime/app/api/account/avatar/route.ts</code>) — <code>Content-Disposition</code> attachment, MIME/size validation, owner gating via the <code>x-sovereign-user-id</code> header.</li><li><strong><code>sdk.db.getClient()</code></strong> returns the live Drizzle client for platform/account reads.</li><li><strong>Related RFCs:</strong> 0002 (<code>sdk.data</code> cross-plugin sharing — namespace relationship, see Open questions), 0004 (per-plugin DB — export must iterate per-plugin databases), 0005 (<code>sdk.activity</code> — audit export/import), 0006 (operator backup — clearly delineated from this).</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><p>All of this is <strong>deferred</strong> (specified for the post-acceptance task) except the reserved stub noted under &quot;Adoption path&quot;.</p><h3 id="_1-plugin-participation-—-reserved-sdk-portability" tabindex="-1">1. Plugin participation — reserved <code>sdk.portability</code> <a class="header-anchor" href="#_1-plugin-participation-—-reserved-sdk-portability" aria-label="Permalink to &quot;1. Plugin participation — reserved \`sdk.portability\`&quot;">​</a></h3><p>A plugin opts into portability by registering an <strong>export resolver</strong> and an <strong>import handler</strong>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.portability.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideExport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  resolver: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ExportContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PluginExportSection</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.portability.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideImport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">section</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PluginExportSection</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ImportContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;void&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ExportContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ImportContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Maps an id from the source bundle to the id minted on this instance. */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  remapId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">originalId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PluginExportSection</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** The plugin&#39;s own data-format version (for forward/backward compatibility). */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  schemaVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Plugin-defined JSON payload (rows, references). */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  data</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Optional binary attachments, keyed by relative path within the section. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  blobs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Uint8Array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Runtime-mediated.</strong> The runtime supplies <code>ctx.userId</code> / <code>ctx.tenantId</code>; a plugin only ever reads or writes the <strong>current user&#39;s own slice</strong> and cannot reach another user&#39;s data, another plugin&#39;s data, or another tenant. This mirrors RFC 0002&#39;s resolver model (<code>sdk.data.provide</code>) but for portability rather than consent-gated cross-plugin reads.</p><h3 id="_2-bundle-format-—-versioned-zip" tabindex="-1">2. Bundle format — versioned ZIP <a class="header-anchor" href="#_2-bundle-format-—-versioned-zip" aria-label="Permalink to &quot;2. Bundle format — versioned ZIP&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>export.zip</span></span>
<span class="line"><span>├── manifest.json          # formatVersion, exportedAt, source instance + platform version,</span></span>
<span class="line"><span>│                          # subject user (minimal identity), and per-section</span></span>
<span class="line"><span>│                          # { pluginId, schemaVersion, checksum }</span></span>
<span class="line"><span>├── platform/</span></span>
<span class="line"><span>│   ├── account.json       # profile (name, email), preferences (timezone, theme)</span></span>
<span class="line"><span>│   └── avatar.&lt;ext&gt;        # the avatar file, if any</span></span>
<span class="line"><span>└── plugins/</span></span>
<span class="line"><span>    └── &lt;pluginId&gt;/</span></span>
<span class="line"><span>        ├── data.json       # the plugin&#39;s PluginExportSection.data</span></span>
<span class="line"><span>        └── blobs/...        # the plugin&#39;s PluginExportSection.blobs</span></span></code></pre></div><p><code>formatVersion</code> gates overall compatibility; each section&#39;s <code>schemaVersion</code> gates that plugin&#39;s payload. Checksums detect tampering/corruption.</p><h3 id="_3-export-flow" tabindex="-1">3. Export flow <a class="header-anchor" href="#_3-export-flow" aria-label="Permalink to &quot;3. Export flow&quot;">​</a></h3><p>User triggers export from the Account <strong>&quot;Data&quot;</strong> tab → the runtime collects platform/account data, then invokes each <strong>installed, opted-in</strong> plugin&#39;s export resolver (scoped to the user) → assembles the ZIP → streams it back as an attachment. <strong>Synchronous with a size cap in v1</strong>; large/async export (job + notification) is an Open question (it would lean on the reserved <code>sdk.notifications</code> / <code>events</code>).</p><h3 id="_4-import-restore-flow" tabindex="-1">4. Import / restore flow <a class="header-anchor" href="#_4-import-restore-flow" aria-label="Permalink to &quot;4. Import / restore flow&quot;">​</a></h3><p>Upload a bundle → <strong>validate</strong> <code>formatVersion</code> and each section&#39;s <code>schemaVersion</code> → <strong>ID remap</strong> (mint fresh ULIDs on this instance, preserving internal references via <code>ctx.remapId</code>) → invoke each plugin&#39;s import handler <strong>transactionally</strong> (runtime injects user/tenant) → default <strong>additive merge</strong> (create new records; never overwrite another user&#39;s data) → <strong>unknown/absent plugins are skipped with a warning</strong>. Conflict policy beyond &quot;additive&quot; is an Open question.</p><h3 id="_5-cross-instance-migration" tabindex="-1">5. Cross-instance migration <a class="header-anchor" href="#_5-cross-instance-migration" aria-label="Permalink to &quot;5. Cross-instance migration&quot;">​</a></h3><p>The same bundle moves between instances. Compatibility is checked via <code>formatVersion</code>, each section&#39;s <code>schemaVersion</code>, and the plugin manifest&#39;s <code>compatibility.minPlatformVersion</code>. The subject user is <strong>remapped to the target instance&#39;s current user</strong> (you import <em>into</em> your account on instance B); source instance identity is recorded in <code>manifest.json</code> for provenance.</p><h3 id="_6-permissions-reserved" tabindex="-1">6. Permissions (reserved) <a class="header-anchor" href="#_6-permissions-reserved" aria-label="Permalink to &quot;6. Permissions (reserved)&quot;">​</a></h3><ul><li><code>data:export</code> — the plugin may contribute its slice to a user&#39;s export.</li><li><code>data:import</code> — the plugin may accept imported user data.</li></ul><h3 id="_7-security-privacy" tabindex="-1">7. Security &amp; privacy <a class="header-anchor" href="#_7-security-privacy" aria-label="Permalink to &quot;7. Security &amp; privacy&quot;">​</a></h3><ul><li><strong>Owner-only.</strong> Export/import act on the requesting user (<code>x-sovereign-user-id</code>); no cross-user access. Optional re-auth before export of sensitive data.</li><li><strong>Untrusted input on import.</strong> Validate format/schema versions, enforce size limits, reject unknown shapes, and run each plugin&#39;s import in its own user/tenant-scoped context — a malicious bundle cannot write outside the importing user.</li><li><strong>Transport.</strong> If async export lands, downloads use signed, expiring links.</li><li><strong>Audited.</strong> Every export and import is recorded via <code>sdk.activity</code> (RFC 0005) — a security-relevant, user-visible event.</li></ul><h3 id="_8-ui-deferred" tabindex="-1">8. UI (deferred) <a class="header-anchor" href="#_8-ui-deferred" aria-label="Permalink to &quot;8. UI (deferred)&quot;">​</a></h3><p>A new Account <strong>&quot;Data&quot;</strong> tab: <strong>Export my data</strong> (download) and <strong>Import / restore</strong> (upload), with progress and a per-section result summary (imported / skipped / warnings).</p><h2 id="impact-when-accepted-deferred-—-beyond-the-reserved-stub" tabindex="-1">Impact when accepted (deferred — beyond the reserved stub) <a class="header-anchor" href="#impact-when-accepted-deferred-—-beyond-the-reserved-stub" aria-label="Permalink to &quot;Impact when accepted (deferred — beyond the reserved stub)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Where</th><th>Change</th></tr></thead><tbody><tr><td><code>packages/sdk</code></td><td>Implement <code>sdk.portability</code> against the runtime (replace the stub).</td></tr><tr><td><code>packages/manifest</code></td><td>Enforce <code>data:export</code> / <code>data:import</code>.</td></tr><tr><td>Runtime</td><td>Export assembler, import validator + ID remap, plugin-resolver registry, ZIP streaming, owner gating.</td></tr><tr><td>Account</td><td>&quot;Data&quot; tab + actions/routes.</td></tr><tr><td>Reference plugins</td><td>Implement export/import resolvers (Tasks, Splitify, …).</td></tr><tr><td>SRS §3 / §5</td><td>Promote to specified; new <code>POR-xx</code> requirement IDs + decision-log entry; manifest reference for <code>data:*</code>.</td></tr><tr><td><code>ROADMAP.md</code></td><td>The sequenced implementation task.</td></tr></tbody></table><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Declarative manifest annotations</strong> (plugin lists user-owned tables/columns; platform auto-queries) instead of SDK hooks. Cannot express membership graphs, shared resources, derived data, or blobs. Rejected.</li><li><strong>Platform + account data only (v1)</strong> — export profile/prefs/avatar and defer plugin participation. Ships fast but omits the plugin data users actually care about. Rejected as the target (though it is the natural first delivery slice).</li><li><strong>JSON-only bundle</strong> — a single JSON file with avatars as URLs/base64. Awkward for files and large blobs; rejected in favour of a versioned ZIP.</li><li><strong>Admin full-instance GUI</strong> — a Console wrapper over RFC 0006&#39;s <code>sv backup</code>. Admin-facing, not end-user; belongs with the ops RFC. Excluded here.</li><li><strong>Fold into RFC 0002&#39;s <code>sdk.data</code></strong> — conflates consent-gated cross-plugin reads with user portability. Kept as a separate namespace (see Open questions).</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Namespace placement.</strong> <code>sdk.portability</code> (proposed) vs folding export/import into RFC 0002&#39;s <code>sdk.data</code>. Proposal: separate, to decouple from the consent model.</li><li><strong>Sync vs async export.</strong> v1 sync with a size cap; large exports may need a job <ul><li>notification (reserved <code>sdk.notifications</code>/<code>events</code>) and signed links.</li></ul></li><li><strong>Import conflict policy.</strong> Additive-only (proposed default) vs replace vs interactive merge.</li><li><strong>Bundle encryption.</strong> Optional password/age-encryption of the ZIP, given it contains personal data at rest after download.</li><li><strong>ID remap &amp; referential integrity.</strong> Cross-plugin references and the <code>remapId</code> contract — how far the platform helps vs the plugin owns.</li><li><strong>Shared / membership data.</strong> Export owned rows only, or also include shared-resource membership (read-only)? Avoids duplicating other users&#39; data.</li><li><strong>Selective export.</strong> Per-plugin / per-category selection vs all-or-nothing.</li><li><strong>Avatars via <code>sdk.storage</code>.</strong> Once <code>sdk.storage</code> lands, source the avatar blob through it rather than reading disk directly.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Now (proposed, deferred for sequencing):</strong> reserved <code>sdk.portability</code> stub (<code>provideExport</code>/<code>provideImport</code> throw <code>NotImplementedError</code>) + <code>data:export</code> / <code>data:import</code> permissions — additive (SDK + manifest <strong>minor</strong> bumps). No behaviour change for existing plugins.</li><li><strong>On acceptance:</strong> apply the &quot;Impact when accepted&quot; table — bundle format + runtime assembler/importer, the Account &quot;Data&quot; tab, then reference-plugin resolvers.</li><li>The mechanism becomes part of the public manifest + SDK contract from the <code>@sovereignfs/sdk</code> / <code>@sovereignfs/manifest</code> minor release that ships it.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft; proposes reserved <code>sdk.portability</code> + <code>data:export</code>/<code>data:import</code>.</td></tr><tr><td>1.0</td><td>Jun 2026</td><td>Accepted; incorporated into SRS §3.16, §5 (<code>data:export</code>/<code>data:import</code>), and Task 0.5.15.</td></tr></tbody></table>`,45)])])}const g=e(n,[["render",r]]);export{k as __pageData,g as default};
