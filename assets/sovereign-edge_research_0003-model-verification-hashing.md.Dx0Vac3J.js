import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"Model download verification: hashing strategy","description":"","frontmatter":{"id":3,"title":"Model download verification: hashing strategy","status":"Superseded in part — see Update: native SHA-256","date":"July 2026","author":"Claude Code (session with the developer)","scope":"mobile","summary":"Task 0.1.4 / epic 0.4 — how downloaded GGUF weights are verified on device","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/research/0003-model-verification-hashing.md"},"headers":[],"relativePath":"sovereign-edge/research/0003-model-verification-hashing.md","filePath":"sovereign-edge/research/0003-model-verification-hashing.md"}'),i={name:"sovereign-edge/research/0003-model-verification-hashing.md"};function r(n,e,d,h,l,c){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="research-0003-—-model-download-verification-hashing-strategy" tabindex="-1">Research 0003 — Model download verification: hashing strategy <a class="header-anchor" href="#research-0003-—-model-download-verification-hashing-strategy" aria-label="Permalink to &quot;Research 0003 — Model download verification: hashing strategy&quot;">​</a></h1><p>See <a href="#update-native-sha-256-supersedes-the-md5-default">Update: native SHA-256</a> for the superseding decision.</p><p><strong>Related:</strong> <a href="./0001-concept-and-connector-architecture.html">0001</a> (model assets never bundled), <a href="./0002-react-native-framework-choice.html">0002</a> (Expo chosen partly <em>because</em> <code>expo-file-system</code> provides resumable downloads).</p><hr><h2 id="question" tabindex="-1">Question <a class="header-anchor" href="#question" aria-label="Permalink to &quot;Question&quot;">​</a></h2><p>Epic 0.4 requires &quot;checksum verification&quot; of downloaded GGUF files. SHA-256 is the obvious default, and it is what model hosts publish. Is it actually usable on a phone for a multi-gigabyte file?</p><h2 id="findings" tabindex="-1">Findings <a class="header-anchor" href="#findings" aria-label="Permalink to &quot;Findings&quot;">​</a></h2><h3 id="expo-file-system-hashes-natively-for-md5-only" tabindex="-1"><code>expo-file-system</code> hashes natively for MD5 only <a class="header-anchor" href="#expo-file-system-hashes-natively-for-md5-only" aria-label="Permalink to &quot;`expo-file-system` hashes natively for MD5 only&quot;">​</a></h3><p><code>File.info({ md5: true })</code> is computed in native code. There is no native SHA-256 for files, and <code>expo-crypto</code>&#39;s <code>digest()</code> takes a whole <code>BufferSource</code> — which for a 4 GB model would mean holding it in memory. So SHA-256 has to be streamed and hashed in JavaScript.</p><h3 id="measured-not-assumed" tabindex="-1">Measured, not assumed <a class="header-anchor" href="#measured-not-assumed" aria-label="Permalink to &quot;Measured, not assumed&quot;">​</a></h3><p>The first implementation streamed SHA-256 via <code>@noble/hashes</code>. Benchmarked on an Android emulator (Pixel-class AVD, arm64, API 36) against a 128 MB file:</p><table tabindex="0"><thead><tr><th>Operation</th><th>Throughput</th><th>Extrapolated to 4 GB</th></tr></thead><tbody><tr><td>Native MD5</td><td>74.5 MB/s</td><td>~1 min</td></tr><tr><td>Stream read, no hashing</td><td>6.2 MB/s</td><td>~11 min</td></tr><tr><td>Stream read + SHA-256 in JS</td><td>1.1 MB/s</td><td><strong>~61 min</strong></td></tr></tbody></table><p>Two independent problems, and it matters that they are separate:</p><ol><li><strong>The stream arrives in 1 KB chunks</strong> — 131,072 of them for 128 MB. That bridge crossing alone costs ~20s per 128 MB before any hashing.</li><li><strong>SHA-256 in Hermes runs at roughly 1.4 MB/s.</strong> Even with chunking fixed, hashing alone still puts 4 GB at ~48 minutes.</li></ol><p>So this is not a tuning problem. An hour of verification on top of an hour of downloading is not a shippable experience, and a foregrounded hour-long CPU burn on a phone will be killed by the OS or destroy the battery first.</p><p>A later measurement of native MD5 on an already-read file reported 256 MB/s against 74.5 MB/s for the first run — almost certainly OS page cache. The conservative 74.5 MB/s figure is the one to plan against.</p><h3 id="md5-s-weakness-is-the-wrong-weakness-to-worry-about-here" tabindex="-1">MD5&#39;s weakness is the wrong weakness to worry about here <a class="header-anchor" href="#md5-s-weakness-is-the-wrong-weakness-to-worry-about-here" aria-label="Permalink to &quot;MD5&#39;s weakness is the wrong weakness to worry about here&quot;">​</a></h3><p>MD5 is widely described as &quot;broken&quot;, but the broken property is <strong>collision resistance</strong>: an attacker crafts <em>two</em> files with the same digest. That helps an attacker who controls what digest gets published — i.e. a hostile model publisher — and against that threat a stronger hash in this app changes nothing, because the descriptor would carry the attacker&#39;s digest either way.</p><p>The property this pipeline actually relies on is <strong>second-preimage resistance</strong>: given the publisher&#39;s file and its published digest, produce a <em>different</em> file with the same digest. MD5 has no practical second-preimage attack. Combined with an exact size check and HTTPS transport, MD5 is sufficient for what verification is really defending against here — a corrupt or truncated download, or a mirror serving the wrong file.</p><h2 id="options-considered" tabindex="-1">Options considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options considered&quot;">​</a></h2><h3 id="a-native-md5-by-default-sha-256-available-but-opt-in-recommended" tabindex="-1">A. Native MD5 by default, SHA-256 available but opt-in (recommended) <a class="header-anchor" href="#a-native-md5-by-default-sha-256-available-but-opt-in-recommended" aria-label="Permalink to &quot;A. Native MD5 by default, SHA-256 available but opt-in (recommended)&quot;">​</a></h3><p>Size and MD5 on every download (~1 min per 4 GB). <code>sha256</code> stays in the descriptor and is checked only under <code>deep: true</code>, at the measured cost.</p><h3 id="b-ship-a-native-sha-256-module-now" tabindex="-1">B. Ship a native SHA-256 module now <a class="header-anchor" href="#b-ship-a-native-sha-256-module-now" aria-label="Permalink to &quot;B. Ship a native SHA-256 module now&quot;">​</a></h3><p>A small Expo native module over <code>CommonCrypto</code> (iOS) and <code>MessageDigest</code> (Android) would give SHA-256 at native speed. It is the strongest outcome and the eventual destination — but it pulls native-module work into a task scoped as pure infrastructure, and needs a build/verify cycle on both platforms. Deferred, not rejected.</p><h3 id="c-keep-sha-256-in-javascript" tabindex="-1">C. Keep SHA-256 in JavaScript <a class="header-anchor" href="#c-keep-sha-256-in-javascript" aria-label="Permalink to &quot;C. Keep SHA-256 in JavaScript&quot;">​</a></h3><p>Rejected on the measurements above.</p><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><ul><li><strong>Default verification is size + native MD5.</strong> Fast enough to run on every download, and sound for the threat it actually addresses.</li><li><strong><code>ModelDescriptor.md5</code> is required; <code>sha256</code> is optional.</strong> Descriptors carry SHA-256 when the publisher offers one, so nothing is lost when a native implementation lands.</li><li><strong><code>deep: true</code> verifies SHA-256</strong>, with the cost documented at the call site rather than buried.</li><li><strong>Verification order is cheapest-first</strong>: size, then MD5, then SHA-256. A truncated file fails instantly instead of after minutes of hashing.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><strong>Native SHA-256 module.</strong> Roughly 40 lines per platform. Once it exists, SHA-256 becomes the default and this document&#39;s trade-off goes away. Worth scheduling before the connector SDK opens the app to third-party content.</li><li><strong>The 1 KB stream chunk size.</strong> Costs ~11 min per 4 GB just to read a file. Irrelevant while MD5 is native and needs no stream, but it will matter for any future feature that reads model files from JavaScript.</li><li><strong>Real-device throughput.</strong> All figures above are from an emulator sharing a busy host. A physical device should be measured before these numbers are quoted anywhere user-facing.</li></ul><h2 id="notes-from-verification" tabindex="-1">Notes from verification <a class="header-anchor" href="#notes-from-verification" aria-label="Permalink to &quot;Notes from verification&quot;">​</a></h2><p>Testing this end-to-end required an HTTP origin supporting <strong>Range requests</strong> — without them resume cannot work at all — plus bandwidth throttling and a &quot;go quiet without closing the socket&quot; mode. <code>python3 -m http.server</code> supports none of these.</p><p>That exercise found a real bug that unit tests could not: calling <code>pauseAsync()</code> on a stalled download causes the in-flight <code>downloadAsync()</code> to resolve with <code>null</code>, which raced ahead of the stall handler and surfaced as <code>code: &#39;network&#39;</code> — &quot;the download ended without producing a file&quot; — hiding both the true cause and the fact that the transfer was resumable. The unit test mocked the download as never-resolving, so the race could not occur there. It is now covered by an explicit regression test.</p><hr><h2 id="update-native-sha-256-supersedes-the-md5-default" tabindex="-1">Update: native SHA-256 supersedes the MD5 default <a class="header-anchor" href="#update-native-sha-256-supersedes-the-md5-default" aria-label="Permalink to &quot;Update: native SHA-256 supersedes the MD5 default&quot;">​</a></h2><p><strong>Date:</strong> July 2026 · <strong>Epic task:</strong> <a href="./../epics/mobile/infrastructure.html#-05--native-sha-256-hashing">0.5</a></p><p>The MD5 default was correct given what was measurable at the time, and wrong about the thing it did not check: <strong>where catalog digests come from.</strong></p><p>Building the model catalog (task 1.2) made it obvious. Model publishers publish SHA-256 — Hugging Face exposes it as <code>lfs.oid</code> on every GGUF — and never MD5. So an MD5 in a catalog entry can only come from a maintainer downloading the file and computing it, which certifies <em>&quot;this matches what we downloaded&quot;</em> rather than <em>&quot;this matches what the publisher published&quot;</em>. That is strictly weaker than checking the publisher&#39;s own digest, however fast it runs. Of four catalog entries, exactly one had an MD5, and only because it had been downloaded during task 1.1.</p><p>So the constraint was never really MD5-versus-SHA-256. It was native-versus-JavaScript.</p><h3 id="measured-after-implementing-a-native-module" tabindex="-1">Measured after implementing a native module <a class="header-anchor" href="#measured-after-implementing-a-native-module" aria-label="Permalink to &quot;Measured after implementing a native module&quot;">​</a></h3><p><code>modules/sovereign-hashing</code>, roughly 50 lines per platform over <code>CryptoKit</code> and <code>MessageDigest</code>, on the same emulator as the original benchmarks:</p><table tabindex="0"><thead><tr><th>Implementation</th><th>Throughput</th><th>4 GB extrapolated</th></tr></thead><tbody><tr><td><strong>Native SHA-256, Android</strong> (this module)</td><td><strong>762–932 MB/s</strong></td><td><strong>~5 s</strong></td></tr><tr><td><strong>Native SHA-256, iOS</strong> (this module)</td><td><strong>599 MB/s</strong></td><td><strong>~7 s</strong></td></tr><tr><td>Native MD5 (<code>expo-file-system</code>)</td><td>74.5 MB/s</td><td>~1 min</td></tr><tr><td>SHA-256 in JavaScript (<code>@noble</code>)</td><td>0.9–1.1 MB/s</td><td>~61 min</td></tr></tbody></table><p>The JavaScript figure reproduced the original 1.1 MB/s measurement, which is reassuring about both runs.</p><p>Two things worth noting. Native SHA-256 is roughly <strong>10× faster than <code>expo-file-system</code>&#39;s native MD5</strong>, so the stronger, publisher-authoritative digest is now also the <em>cheapest</em> one — the trade-off the original decision managed simply no longer exists. And the speedup over JavaScript is ~700×, which is larger than a language gap alone explains: the JS path also pays for a stream that arrives in 1 KB chunks across the JSI bridge.</p><h3 id="verified-not-assumed" tabindex="-1">Verified, not assumed <a class="header-anchor" href="#verified-not-assumed" aria-label="Permalink to &quot;Verified, not assumed&quot;">​</a></h3><ul><li><strong>Known-answer test:</strong> the module returns the published SHA-256 of <code>&quot;abc&quot;</code> exactly.</li><li><strong>Cross-check:</strong> for the same 25 MB file, <code>CryptoKit</code> (iOS), <code>MessageDigest</code> (Android), and <code>@noble/hashes</code> (JavaScript) all produce the identical digest <code>1aa7c925…</code>. Three independent implementations agreeing is the evidence worth having: a fast-but-wrong hasher would be worse than none, so they check each other rather than the fast one being trusted alone.</li><li><strong>Error path:</strong> a missing file rejects cleanly instead of crashing.</li></ul><h3 id="revised-decisions" tabindex="-1">Revised decisions <a class="header-anchor" href="#revised-decisions" aria-label="Permalink to &quot;Revised decisions&quot;">​</a></h3><ul><li><strong>SHA-256 is the default digest</strong>, checked on every download, against the value the publisher published.</li><li><strong>MD5 is retained but optional.</strong> When a descriptor carries one it is still checked — it costs almost nothing and catches corruption early.</li><li><strong><code>deep</code> now means &quot;permit the slow JavaScript path&quot;</strong>, not &quot;check SHA-256&quot;. It matters only where the native module is unavailable.</li><li><strong>The JavaScript implementation stays</strong>, as the fallback for such builds and as the cross-check above.</li></ul><h3 id="what-this-does-not-change" tabindex="-1">What this does not change <a class="header-anchor" href="#what-this-does-not-change" aria-label="Permalink to &quot;What this does not change&quot;">​</a></h3><p>The reasoning about MD5&#39;s second-preimage resistance still stands — it was never the weak point. What changed is that a stronger, publisher-authoritative digest became free, and there is no longer a reason to reason about MD5&#39;s properties at all.</p><hr><h2 id="update-measured-on-real-hardware" tabindex="-1">Update: measured on real hardware <a class="header-anchor" href="#update-measured-on-real-hardware" aria-label="Permalink to &quot;Update: measured on real hardware&quot;">​</a></h2><p><strong>Date:</strong> August 2026 · <strong>Device:</strong> iPhone 15 Pro (A17 Pro, 7.50 GB)</p><p>The figures above were all taken on an emulator, and this document said so — &quot;a physical device should be measured before these numbers are quoted anywhere user-facing&quot;. That is now done, against the 491 MB Qwen2.5 0.5B model rather than a 25 MB synthetic file.</p><table tabindex="0"><thead><tr><th>Implementation</th><th>Emulator</th><th>iPhone 15 Pro</th></tr></thead><tbody><tr><td>Native SHA-256 (this module)</td><td>599 MB/s</td><td><strong>2204 MB/s</strong></td></tr><tr><td>Native MD5 (<code>expo-file-system</code>)</td><td>74.5 MB/s</td><td>~512 MB/s*</td></tr></tbody></table><p>* Inferred: verifying the model with both digests took 960ms where SHA-256 alone took 223ms, so the MD5 pass accounts for roughly 737ms of 491 MB.</p><p>Two consequences.</p><p><strong>Native SHA-256 is ~4x faster than the native MD5 it replaced</strong>, not merely comparable. The gap is wider on real silicon than on the emulator, so the task 0.5 decision is more clearly right than the original measurements suggested.</p><p><strong>The catalog no longer carries MD5 at all.</strong> One entry still had one, left from when native MD5 was the only fast digest. Keeping it meant <code>verifyFile</code> made two full passes over the file for no gain — the MD5 was both slower and the weaker claim, being maintainer-computed rather than publisher-published. Removing it roughly halves verification time and makes every catalog entry uniform.</p><h3 id="still-unmeasured" tabindex="-1">Still unmeasured <a class="header-anchor" href="#still-unmeasured" aria-label="Permalink to &quot;Still unmeasured&quot;">​</a></h3><p>Both devices available for testing have 8 GB of RAM, so the memory-fit heuristic has still never been exercised near its boundary — <code>unsupported</code> has only ever fired on a 3.8 GB emulator. That remains a guess.</p>',61)])])}const m=t(i,[["render",r]]);export{p as __pageData,m as default};
