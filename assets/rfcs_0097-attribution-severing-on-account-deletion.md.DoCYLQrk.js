import{_ as t,o as s,c as n,a2 as i}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0097 — Severing attribution on account deletion","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0097-attribution-severing-on-account-deletion.md"},"headers":[],"relativePath":"rfcs/0097-attribution-severing-on-account-deletion.md","filePath":"rfcs/0097-attribution-severing-on-account-deletion.md"}'),a={name:"rfcs/0097-attribution-severing-on-account-deletion.md"};function o(r,e,d,l,h,c){return s(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0097-—-severing-attribution-on-account-deletion" tabindex="-1">RFC 0097 — Severing attribution on account deletion <a class="header-anchor" href="#rfc-0097-—-severing-attribution-on-account-deletion" aria-label="Permalink to &quot;RFC 0097 — Severing attribution on account deletion&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> September 2026<br><strong>Author:</strong> Claude Code<br><strong>Scope:</strong> <code>packages/sdk</code> (<code>DeletionResult.anonymized</code>), <code>docs/plugin-development.md</code>, <code>docs/architecture-rules.md</code>, <code>CLAUDE.md</code>, and the schema/migrations/deletion handler of every plugin holding cross-user attribution columns (<code>sovereign-shopper</code>, <code>sovereign-tasks</code> first); <strong>amends <a href="./0033-user-data-deletion.html">RFC 0033</a></strong> — it fills in the answer RFC 0033&#39;s &quot;What is NOT deleted&quot; section deliberately left to each plugin, without changing RFC 0033&#39;s rule that the platform does not impose one. RFC 0033 stays &quot;Implemented&quot; for everything it already covers.<br><strong>Incorporated into plan:</strong> No — documentation-first. Design plus the SDK/doc surface it needs; per-plugin adoption is scheduled separately.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>When an account is deleted, a plugin&#39;s <code>sdk.portability.provideDelete</code> handler removes the rows that user owned. Rows the departing user authored <strong>on another user&#39;s data</strong> are a different case: they must survive — the row belongs to the other user&#39;s list, board, or ledger — but they keep the deleted account&#39;s id in an attribution column, pointing at a user who no longer exists.</p><p>This RFC sets one platform-wide answer: a cross-user attribution column is <strong>nullable</strong>, and a deletion handler <strong>sets it to <code>NULL</code></strong> — severing the attribution instead of deleting the row. Handlers report those rows through a new optional <code>DeletionResult.anonymized</code> count so the cascade never reports a severed row as an erased one. It explicitly rejects the alternative of a platform-wide &quot;deleted user&quot; sentinel id.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Two plugins have independently hit the identical gap and both, correctly, declined to invent an answer:</p><ul><li><strong>Shopper</strong> — <code>shopper_list_items.added_by</code> and <code>shopper_purchases.purchased_by</code> on lists the departing user only had editor access to are never touched, because <code>deleteAllShopperData</code> scopes every statement by <code>owner_user_id</code>. The handler&#39;s own doc comment records the gap and names the two candidate designs, deferring both as &quot;decisions bigger than this plugin.&quot;</li><li><strong>Tasks</strong> — <code>assignee_id</code> has the same shape, noted in Shopper&#39;s <code>portability.ts</code> import comment as the reason not to decide unilaterally.</li></ul><p><a href="./0096-plugin-deletion-veto-hook.html">RFC 0096</a> surveyed the same territory from the other end and found the pattern is not confined to those two: <strong>Tally</strong>, <strong>Docs</strong>, <strong>Sheets</strong>, and <strong>Kanban</strong> all hold rows where one user&#39;s record references another user. RFC 0096&#39;s answer is to let a plugin <em>block</em> a deletion before it starts. This RFC is its complement: what a plugin does with attribution <em>after</em> a deletion it did not block. The two are independent — a plugin may need either, both, or neither.</p><p>RFC 0033 already anticipated this case and ruled on it correctly:</p><blockquote><p><strong>Shared / aggregate data</strong> that references the user only by attribution (e.g. plugin content visible to other users): plugin handlers decide; the platform does not impose.</p></blockquote><p>That is right about <em>enforcement</em> — the platform cannot know which of a plugin&#39;s rows are shared. But &quot;plugins decide&quot; was read as &quot;no guidance exists,&quot; and the result is two plugins sitting on the same unresolved question waiting for a convention that was never written. This RFC writes the convention. It does not make the platform impose it.</p><p><strong>This is not urgent as a leak.</strong> Nothing in either plugin&#39;s UI renders these columns today, so what exists is a dangling id in a table, not a visible disclosure. The reason to fix it now is that the cost only rises: the migration is cheap while the columns are write-only, and expensive once a UI, an export consumer, or Shopper&#39;s v0.7 price history starts reading them.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>The gap, as written down.</strong><code>plugins/sovereign-plugin-shopper.local/app/_lib/portability.ts:380-397</code> documents it in full, including that deleting the rows would be wrong. <code>added_by</code> and <code>purchased_by</code> are <code>.notNull()</code> (<code>app/_db/schema.ts:93,111</code>; <code>app/_db/schema.postgres.ts:79,97</code>).</li><li><strong>These columns are already soft references.</strong> Neither has a foreign key, and neither can: the <code>user</code> table lives in <code>apps/auth</code>&#39;s own <code>sovereign_auth</code> schema/database, not the plugin&#39;s. An id in one of these columns has <em>always</em> been &quot;a user who may or may not still exist&quot; — an admin deleting a user has left dangling ids here since the column was created. Any read path must already handle &quot;resolves to nobody.&quot;</li><li><strong>The platform already models this exact concept, with NULL.</strong> <code>activity_log.actor_id</code> is nullable, paired with a non-null <code>actor_type</code> discriminator (<code>&#39;user&#39; | &#39;system&#39; | &#39;plugin&#39;</code>) — <code>packages/db/src/schema/sqlite/platform.ts:200-201</code>. Workstream 0025 leg 1 (<code>GDPR-1</code>) used precisely this to make a self-deletion&#39;s own audit record survive the cascade: the entry is written with <code>actorType: &#39;system&#39;</code> and <code>actorId: null</code> rather than the deleted user&#39;s id.</li><li><strong>The rendering fallback already exists.</strong> <code>plugins/console/app/activity/page.tsx:68-71</code> renders <code>actorType === &#39;system&#39;</code> as <code>&quot;System&quot;</code> and a null <code>actorId</code> as no actor label at all.</li><li><strong><code>sdk.directory.resolveUsers()</code> resolves only active users.</strong> Ids with no live user are simply absent from the result (<code>packages/sdk/src/directory.ts</code>, <code>runtime/src/sdk-host.ts:732-745</code>) — it does not throw and does not return a placeholder. A UI rendering any stored user id therefore already needs an unresolved-id branch.</li><li><strong>Nullable soft links are already the documented idiom.</strong> <code>docs/plugin-development.md</code>&#39;s cross-plugin reference section tells plugins to &quot;treat a stored reference as a nullable link and handle the provider being uninstalled, disabled, revoked, or the resource deleted.&quot;</li><li><strong><code>DeletionResult</code> is <code>{ deleted: number; errors?: string[] }</code></strong> (<code>packages/sdk/src/portability.ts:100-106</code>), surfaced per plugin in <code>DeletionSummary. pluginResults</code> (<code>runtime/src/user-deletion.ts:21-34</code>). Nothing in the runtime or Console aggregates or renders <code>deleted</code> today; it is carried for the operator-facing summary.</li><li><strong>drizzle-kit generates the SQLite relaxation itself.</strong> SQLite has no <code>ALTER COLUMN</code>, but <code>drizzle-kit generate</code> emits the standard <code>PRAGMA foreign_keys=OFF</code> → create <code>__new_*</code> → copy → drop → rename rebuild. This repo already ships that exact generated pattern in <code>plugins/sovereign-plugin-travellog.local/migrations/sqlite/0002_vengeful_wind_dancer.sql:76-100</code>. The hand-authoring rule in <code>CLAUDE.md</code> applies to <em>renames</em>, which drizzle-kit cannot do non-interactively; a nullability relaxation is not a rename and needs no hand-authoring.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-the-three-way-classification" tabindex="-1">1. The three-way classification <a class="header-anchor" href="#_1-the-three-way-classification" aria-label="Permalink to &quot;1. The three-way classification&quot;">​</a></h3><p>Every row a plugin holds falls into exactly one of three buckets at deletion time. Stating them explicitly is most of the value of this RFC — the Shopper and Tasks gaps are both bucket 2 rows that were handled as though only buckets 1 and 3 existed.</p><table tabindex="0"><thead><tr><th>#</th><th>Row</th><th>Action</th></tr></thead><tbody><tr><td>1</td><td>Owned by the departing user</td><td><strong>Delete</strong></td></tr><tr><td>2</td><td>Owned by another user, <strong>attributed to</strong> the departing user</td><td><strong>Sever the attribution, keep the row</strong></td></tr><tr><td>3</td><td>Owned by another user, not attributed to the departing user</td><td>Leave untouched</td></tr></tbody></table><p>The distinction that matters is <strong>ownership vs. attribution</strong>:</p><ul><li>An <strong>ownership</strong> column (<code>owner_user_id</code>, <code>user_id</code>, <code>tenant_id</code>) says whose data this is. It stays <code>NOT NULL</code>. Its rows are bucket 1 — deleted outright. An ownership column is never severed, because a row with no owner is unreachable garbage.</li><li>An <strong>attribution</strong> column (<code>added_by</code>, <code>purchased_by</code>, <code>assignee_id</code>, <code>created_by</code>, <code>checked_by</code>) says who <em>did something</em> to a row someone else owns. It is nullable, and it is what this RFC severs.</li></ul><p>A column can be both, in different tables. <code>shopper_lists.created_by</code> is attribution on a list whose <code>owner_user_id</code> is someone else, and co-owned with the owner otherwise — so it follows the row: severed when the list survives, deleted with the list when it does not.</p><h3 id="_2-sever-means-null" tabindex="-1">2. Sever means <code>NULL</code> <a class="header-anchor" href="#_2-sever-means-null" aria-label="Permalink to &quot;2. Sever means \`NULL\`&quot;">​</a></h3><p>A cross-user attribution column is declared nullable, and the deletion handler sets it to <code>NULL</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> severed</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(shopperListItems)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ addedBy: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(shopperListItems.tenantId, ctx.tenantId), </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(shopperListItems.addedBy, ctx.userId)),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span></code></pre></div><p>Why <code>NULL</code> rather than a sentinel:</p><ul><li><strong>It is what the platform already does.</strong> <code>activity_log.actor_id</code> is nullable for exactly this reason, and <code>GDPR-1</code> chose <code>actorId: null</code> over a synthetic actor id when it faced the same question three legs ago. A second, different convention for one concept is drift.</li><li><strong>The type system enforces the read side.</strong> Relaxing the column changes the inferred type to <code>string | null</code>, so <code>tsc</code> names every site that reads it. A sentinel leaves the type as <code>string</code>; every existing read site keeps compiling and silently renders a meaningless token. The failure mode a sentinel is meant to prevent is the one it quietly preserves.</li><li><strong>SQL treats it correctly for free.</strong> <code>WHERE added_by = ?</code> never matches, <code>GROUP BY</code> does not invent a phantom contributor, <code>COUNT(added_by)</code> excludes it. A sentinel must be excluded by hand in every aggregate, filter, and join, forever, in every plugin — an allowlist with no compiler backing and no test that fails when someone forgets.</li><li><strong>It cannot be mistaken for a real id.</strong> A sentinel is a string shaped exactly like a user id that resolves to no user. Anything that treats it as an id — a directory lookup, an &quot;is this me?&quot; check, an export, a cross-plugin reference — does the wrong thing unless it has been taught the exception.</li></ul><h3 id="_3-the-sentinel-buys-nothing-on-the-read-path" tabindex="-1">3. The sentinel buys nothing on the read path <a class="header-anchor" href="#_3-the-sentinel-buys-nothing-on-the-read-path" aria-label="Permalink to &quot;3. The sentinel buys nothing on the read path&quot;">​</a></h3><p>The strongest argument for a sentinel is that it gives the UI a value to recognise, so it can render &quot;Deleted user&quot; instead of a blank. That argument does not survive contact with <code>resolveUsers</code>.</p><p><code>sdk.directory.resolveUsers()</code> returns rows for active users only. A deleted user&#39;s id is absent from the result whether that id is the original one, a sentinel, or nothing at all. The UI&#39;s branch is &quot;this id resolved to no user&quot; in every case — <strong>and it needs that branch regardless of this RFC</strong>, because an admin-deleted user has always left ids behind. Since the rendering rule is identical under both designs, the sentinel&#39;s only claimed advantage is not an advantage, and its costs (above) are unmatched.</p><p>The remaining honest argument for the sentinel is that it needs no migration. Section 6 addresses that: for SQLite drizzle-kit generates the rebuild, and this repo already ships one.</p><h3 id="_4-the-rendering-rule" tabindex="-1">4. The rendering rule <a class="header-anchor" href="#_4-the-rendering-rule" aria-label="Permalink to &quot;4. The rendering rule&quot;">​</a></h3><p>A plugin rendering an attribution column resolves it through <code>sdk.directory.resolveUsers()</code> and handles two cases, which it must not conflate:</p><table tabindex="0"><thead><tr><th>Value</th><th>Meaning</th><th>Render</th></tr></thead><tbody><tr><td><code>null</code></td><td>Attribution deliberately severed, or never set</td><td>Omit the byline entirely</td></tr><tr><td>non-null, absent from <code>resolveUsers</code></td><td>Points at a user who is gone or not visible</td><td>A neutral label, e.g. &quot;Someone&quot;</td></tr></tbody></table><p><strong>Omit, do not invent.</strong> A severed row should read &quot;Milk&quot;, not &quot;Milk — added by Deleted User&quot;. The point of erasure is that the departed user is not a lingering presence in someone else&#39;s UI, and a &quot;Deleted user&quot; byline on every row is a worse outcome for both parties than no byline. This is the same call <code>plugins/console/app/activity/page.tsx:71</code> already makes for a null <code>actorId</code>.</p><p>The non-null-but-unresolved case is not created by this RFC and is not fixed by it; it is listed so plugins do not collapse the two into one branch and start rendering &quot;Someone&quot; on rows whose attribution was deliberately removed.</p><h3 id="_5-reporting-deletionresult-anonymized" tabindex="-1">5. Reporting: <code>DeletionResult.anonymized</code> <a class="header-anchor" href="#_5-reporting-deletionresult-anonymized" aria-label="Permalink to &quot;5. Reporting: \`DeletionResult.anonymized\`&quot;">​</a></h3><p><code>DeletionResult</code> gains one optional field:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeletionResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Number of rows (or objects) deleted. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  deleted</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * Rows kept but stripped of their attribution to the deleted user (RFC 0097) —</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * counted separately because they were not erased.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  anonymized</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Non-fatal errors encountered during cleanup. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  errors</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Severed rows must <strong>not</strong> be folded into <code>deleted</code>. <code>deleted</code> is the count an operator would cite to evidence an erasure request; a severed row still exists, and reporting it as deleted overstates what happened. This is the same accountability distinction workstream 0025 drew for <code>GDPR-1</code> (Art. 5(2)/Art. 30 accountability, separate from erasure completeness).</p><p>The field is optional and additive: existing handlers are unchanged and existing readers see <code>undefined</code>. It flows to the operator with no runtime change, since <code>DeletionSummary. pluginResults[].result</code> already carries the whole <code>DeletionResult</code>.</p><h3 id="_6-migration-path-for-an-already-shipped-not-null-column" tabindex="-1">6. Migration path for an already-shipped <code>NOT NULL</code> column <a class="header-anchor" href="#_6-migration-path-for-an-already-shipped-not-null-column" aria-label="Permalink to &quot;6. Migration path for an already-shipped \`NOT NULL\` column&quot;">​</a></h3><p>Per dialect, generated not hand-authored:</p><ul><li><strong>Postgres</strong> — <code>ALTER TABLE ... ALTER COLUMN ... DROP NOT NULL</code>.</li><li><strong>SQLite</strong> — the standard drizzle-kit table rebuild, generated automatically.</li></ul><p>The two <code>drizzle-kit generate</code> runs are the plugin&#39;s existing <code>db:generate:sqlite</code> / <code>db:generate:pg</code> scripts. This is a widening change: every existing row stays valid, nothing is backfilled, and no data is rewritten at migration time.</p><h3 id="_7-what-this-does-not-do" tabindex="-1">7. What this does not do <a class="header-anchor" href="#_7-what-this-does-not-do" aria-label="Permalink to &quot;7. What this does not do&quot;">​</a></h3><ul><li><strong>It does not make the platform enforce anything.</strong> RFC 0033&#39;s &quot;plugin handlers decide&quot; stands. There is no runtime check that a handler severed anything, and none is proposed: the platform cannot tell an ownership column from an attribution column in a plugin&#39;s schema.</li><li><strong>It does not retro-clean existing dangling ids.</strong> Rows already carrying a deleted user&#39;s id keep it. A plugin that wants them cleaned needs its own one-off migration, which it can only write if it can still enumerate deleted user ids — generally it cannot. Adopting this RFC fixes deletions from adoption onward.</li><li><strong>It does not touch export/import.</strong> A severed <code>null</code> exports as <code>null</code>; import already rewrites attribution to the importing user.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><p><strong>A platform-wide &quot;deleted user&quot; sentinel id</strong> (e.g. <code>SOVEREIGN_DELETED_USER_ID</code> exported from <code>@sovereignfs/sdk</code>, written into <code>NOT NULL</code> attribution columns). <strong>Rejected.</strong> It needs no migration, which is its only real advantage. Against that: it is invisible to the type system, so no read site is forced to handle it and every existing one keeps compiling while rendering a meaningless token; it must be excluded by hand from every filter, aggregate, and lookup in every plugin forever, with nothing that fails when someone forgets; it introduces a value shaped like a user id that resolves to no user, which anything treating it as an id will mishandle; and it diverges from <code>activity_log.actor_id</code>, where the platform already answered this question with <code>NULL</code>. Its headline benefit — &quot;a value the UI can recognise&quot; — is not real: <code>resolveUsers()</code> returns nothing for it just as for any other id, so the UI&#39;s branch is unchanged (§3).</p><p><strong>Deleting the rows outright.</strong> Rejected, and already rejected in Shopper&#39;s own handler comment: the item belongs to the list owner&#39;s shopping list and the purchase to their ledger (Shopper v0.7 price history reads it). Deleting another user&#39;s row to erase a third party&#39;s attribution corrupts data the platform has no claim to — the corruption case RFC 0096 was written about.</p><p><strong>Leaving it to each plugin</strong> (status quo). Rejected: it is exactly what produced two plugins independently blocked on the same question, each declining to set a precedent unilaterally.</p><p><strong>A platform-enforced sweep</strong> — the runtime nulling attribution columns itself after the cascade. Rejected: the platform cannot distinguish an ownership column from an attribution column in a plugin&#39;s schema, and guessing by column name across isolated per-plugin databases would be both unreliable and a direct violation of RFC 0033&#39;s &quot;the platform does not impose.&quot;</p><h2 id="semver-impact" tabindex="-1">Semver impact <a class="header-anchor" href="#semver-impact" aria-label="Permalink to &quot;Semver impact&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Package</th><th>Bump</th><th>Version</th><th>Reason</th></tr></thead><tbody><tr><td><code>@sovereignfs/sdk</code></td><td>minor</td><td><code>1.51.0</code> → <code>1.52.0</code></td><td>Additive optional <code>DeletionResult.anonymized</code>; no member added to <code>SdkHost</code>, so no mock-host stubs needed</td></tr><tr><td><code>sovereign-shopper</code> manifest</td><td>minor</td><td><code>0.5.0</code> → <code>0.6.0</code></td><td>Nullable <code>added_by</code>/<code>purchased_by</code> + migrations + handler</td></tr><tr><td><code>sovereign-tasks</code> manifest</td><td>minor</td><td>—</td><td>Same change for <code>assignee_id</code>, in its own repo</td></tr><tr><td>Root <code>package.json</code></td><td>minor</td><td><code>0.138.3</code> → <code>0.139.0</code></td><td>One completed task</td></tr></tbody></table><p><code>DeletionResult</code> is a return type a plugin <em>produces</em>, never one it consumes, so adding an optional field cannot break an existing handler. It is a minor, not a patch, because it widens a published contract (NFR-04).</p><h2 id="security-considerations" tabindex="-1">Security considerations <a class="header-anchor" href="#security-considerations" aria-label="Permalink to &quot;Security considerations&quot;">​</a></h2><ul><li><strong>Severing is not erasure, and must not be reported as such.</strong> §5 is the security-relevant part of this design: an operator answering an erasure request needs to know a row survived. Folding severed rows into <code>deleted</code> would make the cascade&#39;s own summary misleading.</li><li><strong>A severed row can still identify its author indirectly.</strong> Free-text content, timestamps, and co-occurrence can re-identify a contributor to a small shared list regardless of the id being gone. This RFC removes the direct identifier only; it does not claim anonymity in the GDPR Recital 26 sense, and no plugin should describe it to users as though it did.</li><li><strong>No new authorization surface.</strong> Severing happens inside an existing <code>provideDelete</code> handler, already scoped to <code>ctx.userId</code>/<code>ctx.tenantId</code>, already invoked only by the runtime&#39;s cascade. A handler must still scope its <code>UPDATE</code> by <code>tenant_id</code> exactly as it scopes its <code>DELETE</code>s — an unscoped <code>WHERE added_by = ?</code> would cross tenants.</li></ul><h2 id="review-checklist" tabindex="-1">Review checklist <a class="header-anchor" href="#review-checklist" aria-label="Permalink to &quot;Review checklist&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Deletion handler: rows owned by the user are deleted</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Deletion handler: rows on OTHER users&#39; lists attributed to the user are kept, attribution NULL</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Deletion handler: rows on other users&#39; lists NOT attributed to the user are untouched</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Deletion handler: severed rows counted in \`anonymized\`, never in \`deleted\`</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Deletion handler: the severing UPDATE is scoped by tenant_id</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Migration (sqlite): drizzle-kit rebuild applies; existing rows survive with values intact</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Migration (postgres): DROP NOT NULL applies; existing rows survive</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># A regression test fails against the pre-fix handler</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> format:check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><ul><li><strong>v0.1</strong> (September 2026) — Initial draft.</li></ul>`,62)])])}const g=t(a,[["render",o]]);export{u as __pageData,g as default};
