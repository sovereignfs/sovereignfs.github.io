import{_ as a,o as n,c as i,a2 as s}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"RFC 0065 — User groups and plugin access policy","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0065-user-groups-plugin-access.md","filePath":"rfcs/0065-user-groups-plugin-access.md"}'),t={name:"rfcs/0065-user-groups-plugin-access.md"};function o(l,e,r,d,c,p){return n(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="rfc-0065-—-user-groups-and-plugin-access-policy" tabindex="-1">RFC 0065 — User groups and plugin access policy <a class="header-anchor" href="#rfc-0065-—-user-groups-and-plugin-access-policy" aria-label="Permalink to &quot;RFC 0065 — User groups and plugin access policy&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> July 2026 (revised July 2026)<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/db</code>, <code>packages/manifest</code>, <code>apps/auth</code>, <code>runtime</code>, <code>plugins/console</code>, <code>plugins/launcher</code>, <code>scripts/install-plugins.ts</code>, docs; builds on RFC 0021, RFC 0022, RFC 0041, RFC 0054, RFC 0070, and existing <code>plugin_status</code> behavior<br><strong>Incorporated into plan:</strong> Yes — epic tasks 1.15, 1.16, 1.17, 2.21, 2.23, 3.28, 13.7, 13.8, and 15.3. Roadmap slot versions are deferred; prioritized within plugins-runtime.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Installed plugins are currently platform-wide: once a plugin is installed and not globally disabled, it is visible and openable for all active users unless the plugin has bespoke checks such as admin-only handling. Sovereign needs a platform-owned access layer so an operator can decide which users may open each installed plugin.</p><p>This RFC adds four foundations:</p><ul><li>A <strong>plugin catalog and install-time activation model</strong>: every plugin declared in <code>sovereign.plugins.json</code> ships bundled inside the platform image; &quot;installing&quot; a plugin is a DB-driven activation (not a filesystem/rebuild event), and a newly activated plugin starts unavailable to end users until an admin sets its access policy.</li><li>User groups, managed by admins/owners, as reusable audience sets.</li><li>Plugin access policies: <code>everyone</code>, <code>admins</code>, <code>selected_users</code>, <code>selected_groups</code>, and <code>disabled</code> — including a self-service opt-in mode for <code>selected_users</code>/<code>selected_groups</code> policies, gated by a capability defined in RFC 0070.</li><li>Invite-scoped plugin entitlement, so an admin can grant access to specific plugins as part of inviting a new user.</li></ul><p><code>disabled</code> is the strongest state: the plugin remains installed and manageable from Console, but no user can open the app until an admin changes the policy. Existing enabled plugins keep working unchanged under <code>everyone</code>; only newly activated plugins default to the stricter posture.</p><p><strong>Explicitly out of scope for this RFC:</strong> true dynamic runtime installation of plugin code not already bundled in the image (fetching/cloning a plugin into a running instance without a rebuild/redeploy). That remains a real long-term goal — see &quot;Dynamic runtime install (deferred)&quot; under Alternatives considered — but it is a build-pipeline and deployment-model change of a different order of complexity than access control, and is intentionally deferred to a future RFC.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign is single-tenant and multi-user in v1. Operators need to install one plugin once and expose it only to the users who should use it. Examples:</p><ul><li>A finance plugin should be visible only to the finance group.</li><li>A support plugin should be visible to selected staff users during rollout.</li><li>An experimental plugin should be installed for validation without appearing for everyone.</li><li>A sensitive plugin should be disabled immediately without uninstalling it or deleting data.</li><li>An operator should be able to browse everything available to install (not just what&#39;s already cloned into the repo) and activate a plugin for the instance in one action, without a redeploy.</li><li>A new hire&#39;s invite should be able to say &quot;this person gets Finance and Tasks&quot; up front, instead of an admin remembering to grant access after the account exists.</li><li>A trusted user should be able to turn on a plugin they&#39;ve been made eligible for, without filing an admin request, when the operator has decided that&#39;s an acceptable self-service posture for that plugin.</li></ul><p>Launcher-only filtering is not enough. A user who cannot see a plugin must also be denied direct routes, public API entry points, and generated runtime routes for that plugin.</p><h2 id="current-state" tabindex="-1">Current state <a class="header-anchor" href="#current-state" aria-label="Permalink to &quot;Current state&quot;">​</a></h2><ul><li><code>plugin_status</code> provides install/enable state and some admin-only behavior.</li><li>Launcher, sidebar, and mobile nav are built from installed plugin metadata and user preferences, but preferences are not authorization.</li><li>RFC 0054 covers plugin-scoped roles and resource grants inside a plugin after a user has opened it. It does not answer whether the platform should let that user open the plugin at all.</li><li>RFC 0041 provides user directory and member selection primitives, but there is no reusable group/audience model yet.</li><li>&quot;Installed&quot; is purely a build-time/filesystem concept today: <code>scripts/install-plugins.ts</code> clones a plugin declared in <code>sovereign.plugins.json</code> into <code>plugins/&lt;id&gt;/</code>, and <code>scripts/generate-registry.ts</code> composes every directory found under <code>plugins/</code> into <code>runtime/generated/registry.ts</code> and the route tree — unconditionally, with no <code>enabled</code> check at generate time. A plugin&#39;s code is already part of the production image as soon as it&#39;s present under <code>plugins/</code>, regardless of whether an admin has ever enabled it.</li><li>There is no runtime &quot;install&quot; action. An admin cannot make a new plugin available to an instance without someone running <code>sv plugin add</code> / <code>install-plugins.ts</code> against the repository and rebuilding/redeploying.</li><li><code>capabilities.ts</code> capabilities are role-derived only (RFC 0021) — there is no per-user capability grant, so &quot;let this one user self-manage plugin access&quot; cannot be expressed today. RFC 0070 adds that mechanism; this RFC depends on it for the self-service policy variant.</li><li>Invites (<code>apps/auth/src/db.ts</code>) carry only <code>{token, email, created_at, expires_at, consumed_at, invited_by}</code> — no role or scope field. An invited user&#39;s plugin access is decided entirely after account creation, by an admin performing a separate grant.</li></ul><h2 id="proposal" tabindex="-1">Proposal <a class="header-anchor" href="#proposal" aria-label="Permalink to &quot;Proposal&quot;">​</a></h2><h3 id="plugin-catalog-and-install-time-activation" tabindex="-1">Plugin catalog and install-time activation <a class="header-anchor" href="#plugin-catalog-and-install-time-activation" aria-label="Permalink to &quot;Plugin catalog and install-time activation&quot;">​</a></h3><p>Today, &quot;which plugins exist in the image&quot; and &quot;which plugins an admin has turned on&quot; are the same question — a plugin is in the image iff someone ran <code>sv plugin add</code>/<code>install-plugins.ts</code> and the platform was rebuilt. This RFC splits that into two questions:</p><ol><li><strong>What&#39;s in the image?</strong> Every plugin declared in <code>sovereign.plugins.json</code> — the full catalog of plugins the operator&#39;s build is willing to offer, not just the ones currently active — is cloned and composed at build time, exactly as <code>install-plugins.ts</code> and <code>generate-registry.ts</code> do today. Nothing changes about the build pipeline. Bundling more plugins this way adds low-single-digit megabytes per plugin to the standalone trace output (existing plugins range roughly 8 KB–570 KB of <code>app/</code> source; unique third-party npm dependencies not already shared are the larger variable, not plugin source size) — negligible next to typical Next.js standalone image sizes, and this is already how disabled plugins behave today: shipping in the image is already decoupled from being enabled.</li><li><strong>What has an admin turned on for this instance?</strong> A plugin present in the image is <strong>inactive</strong> until an admin activates it from Console&#39;s catalog browser. Activation is a DB action: create the <code>plugin_status</code> row (if absent) and run the plugin&#39;s migrations (<code>sv plugin migrate</code> today; the same runner triggered from Console). No rebuild, no restart, no filesystem write.</li></ol><p>A newly activated plugin&#39;s <code>access_policy</code> defaults to <strong><code>disabled</code></strong>: it is installed and manageable from Console, but not visible or openable to anyone until an admin sets a policy. This is the &quot;enabled/disabled/open-to-everyone/open-to-selected&quot; configurability the operator needs — expressed as choosing an <code>access_policy</code> at (or shortly after) activation time, not as a separate on/off flag layered on top of the policy. See &quot;Storage model&quot; below.</p><p>Existing plugins that are already active as of this RFC keep their current effective state: already-<code>enabled</code> plugins migrate to <code>access_policy = everyone</code> (unchanged behavior), and already-<code>disabled</code> plugins migrate to <code>access_policy = disabled</code>. Only plugins activated after this ships get the disabled-by-default posture.</p><blockquote><p><strong>Scope note (added 2026-07-19):</strong> &quot;existing plugins... as of this RFC&quot; means plugins that already had an explicit <code>plugin_status</code> row, or were accessible via the pre-existing &quot;absence of row = enabled&quot; convention, on an instance that was <strong>already running</strong> before this RFC&#39;s schema shipped. It does not mean &quot;every plugin gets activated on an instance&#39;s first boot&quot; — a genuinely new instance booting for the first time after this RFC ships has no existing state to grandfather, so it should land in the disabled-by-default posture described above for every non-chrome, non-example plugin (and the hidden-by-default posture from Task 12.3/RFC&#39;s own example-plugins model for examples). The implementation initially conflated these two cases — see the changelog entry below.</p></blockquote><p><strong>Deferred:</strong> installing a plugin <em>not</em> already bundled in the image — i.e., fetching arbitrary plugin code into a running instance without a rebuild — is a materially larger change (build pipeline, route composition, dependency installation, and deployment-model implications) and is intentionally out of scope here. See &quot;Dynamic runtime install (deferred)&quot; below.</p><h3 id="two-enforcement-layers" tabindex="-1">Two enforcement layers <a class="header-anchor" href="#two-enforcement-layers" aria-label="Permalink to &quot;Two enforcement layers&quot;">​</a></h3><p>Plugin availability remains platform-owned and is evaluated before any plugin route or SDK surface is exposed:</p><ol><li>The plugin must be installed and enabled.</li><li>The plugin access policy must allow the current user.</li></ol><p>Plugin-local authorization remains plugin-owned. Once the platform allows a user to open a plugin, RFC 0054-style plugin roles and grants decide what that user may do inside the plugin.</p><h3 id="user-groups" tabindex="-1">User groups <a class="header-anchor" href="#user-groups" aria-label="Permalink to &quot;User groups&quot;">​</a></h3><p>Add platform-managed user groups as a prerequisite for group-targeted plugin access.</p><p>Proposed tables:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>user_groups(</span></span>
<span class="line"><span>  id,</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  name,</span></span>
<span class="line"><span>  slug,</span></span>
<span class="line"><span>  description,</span></span>
<span class="line"><span>  created_by_user_id,</span></span>
<span class="line"><span>  created_at,</span></span>
<span class="line"><span>  updated_at</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>user_group_members(</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  group_id,</span></span>
<span class="line"><span>  user_id,</span></span>
<span class="line"><span>  added_by_user_id,</span></span>
<span class="line"><span>  added_at</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>Groups are administrative audiences, not plugin-domain roles. They should be reusable across plugin access policies, notification audiences, and future operator workflows.</p><h3 id="plugin-access-policies" tabindex="-1">Plugin access policies <a class="header-anchor" href="#plugin-access-policies" aria-label="Permalink to &quot;Plugin access policies&quot;">​</a></h3><p>Add a policy field to plugin status/configuration:</p><table tabindex="0"><thead><tr><th>Policy</th><th>Meaning</th></tr></thead><tbody><tr><td><code>everyone</code></td><td>Any active authenticated user can open the plugin. Default for existing behavior.</td></tr><tr><td><code>admins</code></td><td>Only platform admins and owners can open the plugin.</td></tr><tr><td><code>selected_users</code></td><td>Only explicitly granted users can open the plugin.</td></tr><tr><td><code>selected_groups</code></td><td>Only members of explicitly granted groups can open the plugin.</td></tr><tr><td><code>disabled</code></td><td>No user can open the plugin. Console can still manage the installed plugin and its data.</td></tr></tbody></table><p><code>disabled</code> is stronger than every other grant. A disabled plugin does not appear in the Launcher or shell navigation, and direct routes return 404.</p><p>For <code>selected_users</code> and <code>selected_groups</code>, admins and owners are not automatically granted app access. They can grant themselves access in Console, and that action should be audited. Console management access is separate from plugin app access.</p><h3 id="self-service-opt-in" tabindex="-1">Self-service opt-in <a class="header-anchor" href="#self-service-opt-in" aria-label="Permalink to &quot;Self-service opt-in&quot;">​</a></h3><p>Some <code>selected_users</code>/<code>selected_groups</code> plugins should let eligible users turn the plugin on for themselves — a support plugin during staff rollout, for example — without filing an admin request. Rather than adding a third per-user on/off flag alongside install-activation and access policy, self-service is modeled as the user granting themselves the same <code>plugin_access_users</code> row an admin would otherwise create:</p><ul><li>A plugin&#39;s access policy gets an additional <code>self_service: boolean</code> flag (default <code>false</code>), settable by an admin alongside the policy itself.</li><li>When <code>self_service</code> is true and the plugin&#39;s policy is <code>selected_users</code> or <code>selected_groups</code>, an eligible user (any active user for <code>selected_users</code>-self-service; any member of a group the admin has flagged as self-joinable for <code>selected_groups</code>-self-service) may add themselves to the grant table directly, from a plugin-directory browsing surface.</li><li>Performing this self-grant requires the RFC 0070 <code>plugins:self-manage</code> capability. A user without that capability sees the plugin (if eligible) but has no enable/disable affordance — the feature does not exist for them, it is not merely disabled.</li><li>Self-grants are audited identically to admin grants, with the granting actor recorded as the user themselves.</li><li>Revoking a self-granted plugin is the same self-service action in reverse (remove the user&#39;s own <code>plugin_access_users</code> row); an admin can also revoke it directly at any time regardless of who granted it.</li></ul><p>This keeps a single entitlement model (one grants table, one resolver) instead of a second per-user &quot;is it currently on&quot; layer that would have to be reconciled against entitlement.</p><h3 id="invite-scoped-plugin-entitlement" tabindex="-1">Invite-scoped plugin entitlement <a class="header-anchor" href="#invite-scoped-plugin-entitlement" aria-label="Permalink to &quot;Invite-scoped plugin entitlement&quot;">​</a></h3><p>An admin creating an invite (<code>POST /api/admin/invites</code>, <code>apps/auth/app/api/admin/invites/route.ts</code>) may optionally include a <code>plugins</code> scope: a list of plugin IDs to grant the invited user <code>selected_users</code> access to at account-creation time, resolved via the same grant tables this RFC introduces.</p><ul><li><code>invites</code> gains a nullable <code>plugins</code> column (JSON array of plugin IDs). Absent/empty means &quot;no invite-scoped grants&quot; — entirely backward compatible with today&#39;s <code>{email}</code>-only invites.</li><li>The register flow (<code>runtime/app/register/page.tsx</code> and its consuming action) reads the invite&#39;s <code>plugins</code> scope on successful registration and inserts a <code>plugin_access_users</code> row per listed plugin, with <code>granted_by_user_id</code> set to the inviter.</li><li>A plugin ID in the invite scope that is not currently <code>selected_users</code>/<code>selected_groups</code> (e.g. it&#39;s <code>everyone</code> or <code>disabled</code>) is a no-op grant for that plugin — the invite scope grants eligibility, it does not override the plugin&#39;s own access policy.</li><li>The invite-creation UI (Console &quot;invite user&quot; flow) gets a plugin multi-select, backed by the same catalog/access surfaces as the per-plugin Access section.</li></ul><h3 id="storage-model" tabindex="-1">Storage model <a class="header-anchor" href="#storage-model" aria-label="Permalink to &quot;Storage model&quot;">​</a></h3><p>Extend the existing plugin status/configuration persistence instead of introducing a separate installation concept.</p><p>Proposed additions:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin_status.access_policy</span></span>
<span class="line"><span>plugin_status.self_service   -- boolean, default false; only meaningful for selected_users/selected_groups</span></span>
<span class="line"><span></span></span>
<span class="line"><span>plugin_access_users(</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  plugin_id,</span></span>
<span class="line"><span>  user_id,</span></span>
<span class="line"><span>  granted_by_user_id,   -- the user themselves for self-service grants</span></span>
<span class="line"><span>  granted_at</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>plugin_access_groups(</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  plugin_id,</span></span>
<span class="line"><span>  group_id,</span></span>
<span class="line"><span>  granted_by_user_id,</span></span>
<span class="line"><span>  granted_at</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>invites.plugins   -- nullable JSON array of plugin IDs; resolved into plugin_access_users at registration</span></span></code></pre></div><p>The current enabled/disabled flag should migrate cleanly:</p><ul><li>Existing enabled plugins use <code>access_policy = everyone</code>.</li><li>Existing disabled plugins either remain <code>enabled = false</code> with an effective disabled state or migrate to <code>access_policy = disabled</code>.</li><li>The admin UI should present one coherent disabled concept to operators.</li><li>Plugins activated after this ships default to <code>access_policy = disabled</code> (see &quot;Plugin catalog and install-time activation&quot; above) rather than <code>everyone</code> — this is a deliberate behavior change for newly activated plugins only, not a retroactive change to what&#39;s already running.</li></ul><h3 id="authorization-resolver" tabindex="-1">Authorization resolver <a class="header-anchor" href="#authorization-resolver" aria-label="Permalink to &quot;Authorization resolver&quot;">​</a></h3><p>Centralize the check so Launcher, middleware, API routes, and Console summaries all agree.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>canOpenPlugin(user, plugin):</span></span>
<span class="line"><span>  if plugin is not installed or not enabled:</span></span>
<span class="line"><span>    return false</span></span>
<span class="line"><span>  if plugin.access_policy == disabled:</span></span>
<span class="line"><span>    return false</span></span>
<span class="line"><span>  if plugin.access_policy == everyone:</span></span>
<span class="line"><span>    return user is active</span></span>
<span class="line"><span>  if plugin.access_policy == admins:</span></span>
<span class="line"><span>    return user is platform admin or owner</span></span>
<span class="line"><span>  if plugin.access_policy == selected_users:</span></span>
<span class="line"><span>    return user has direct plugin access grant</span></span>
<span class="line"><span>  if plugin.access_policy == selected_groups:</span></span>
<span class="line"><span>    return user belongs to a granted group</span></span>
<span class="line"><span>  return false</span></span></code></pre></div><p>The resolver should return enough metadata for Console to explain why a plugin is or is not available, but route guards should not expose private policy details to unauthorized users.</p><h3 id="runtime-enforcement" tabindex="-1">Runtime enforcement <a class="header-anchor" href="#runtime-enforcement" aria-label="Permalink to &quot;Runtime enforcement&quot;">​</a></h3><p>Apply the resolver to every platform-owned entry point:</p><ul><li>Launcher app grid.</li><li>Sidebar and mobile navigation.</li><li><code>/api/plugins</code> and any plugin registry endpoint exposed to authenticated users.</li><li>Root plugin resolution and fallback.</li><li>Generated plugin route eligibility in runtime middleware.</li><li>Public API namespace delegation where authenticated plugin APIs are routed.</li></ul><p>Denied direct plugin app routes should return 404. This keeps restricted plugin existence from being disclosed to users who are not allowed to open it.</p><h3 id="console-ux" tabindex="-1">Console UX <a class="header-anchor" href="#console-ux" aria-label="Permalink to &quot;Console UX&quot;">​</a></h3><p>Console gets a <strong>plugin catalog browser</strong>, separate from the existing per-plugin management list: every plugin declared in <code>sovereign.plugins.json</code> (bundled in the image, per &quot;Plugin catalog and install-time activation&quot; above), showing which are already active and which are not yet. Activating a plugin runs its migrations and immediately prompts the admin for an initial access policy (defaulting the selector to <code>disabled</code>, matching the storage default) instead of leaving that as a follow-up step.</p><p>Console plugin management gets an Access section for each installed plugin:</p><ul><li>Policy selector: Everyone, Admins and owners, Selected users, Selected groups, Disabled.</li><li>A self-service toggle, enabled only for Selected users/Selected groups policies, with copy explaining it requires the acting user to hold the plugins:self-manage capability (RFC 0070).</li><li>User picker for <code>selected_users</code>, backed by the user directory primitives.</li><li>Group picker for <code>selected_groups</code>.</li><li>Effective access summary and empty-state warnings.</li><li>Clear copy that managing a plugin from Console does not automatically grant app access.</li><li>Audit events for policy changes and user/group grant changes, including self-service grants/revocations (attributed to the acting user, distinguishable from admin-initiated grants).</li></ul><p>The Console &quot;invite user&quot; flow gets an optional plugin multi-select, applied as the invite&#39;s <code>plugins</code> scope (see &quot;Invite-scoped plugin entitlement&quot; above).</p><p>Console also gets user group management:</p><ul><li>Create, rename, and describe groups.</li><li>Add/remove users.</li><li>Show where a group is used by plugin access policies.</li><li>Prevent accidental deletion of an in-use group unless the admin explicitly confirms the impact.</li></ul><h3 id="root-plugin-behavior" tabindex="-1">Root plugin behavior <a class="header-anchor" href="#root-plugin-behavior" aria-label="Permalink to &quot;Root plugin behavior&quot;">​</a></h3><p>If the configured root plugin is not openable by the current user, runtime should fall back to Launcher. If Launcher is also inaccessible or no plugins are available, show a platform owned &quot;No apps available&quot; state rather than leaking inaccessible plugin names.</p><h3 id="manifest-defaults" tabindex="-1">Manifest defaults <a class="header-anchor" href="#manifest-defaults" aria-label="Permalink to &quot;Manifest defaults&quot;">​</a></h3><p>No manifest-level default access policy is required for v1. Access is operator policy, not a developer guarantee. A future manifest hint may be considered later for install-time Console recommendations, but the platform must treat operator configuration as authoritative.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="per-user-only" tabindex="-1">Per-user only <a class="header-anchor" href="#per-user-only" aria-label="Permalink to &quot;Per-user only&quot;">​</a></h3><p>Direct per-user grants are simple but do not scale for real teams. They are useful for exceptions and rollout, but groups should be the primary reusable audience primitive.</p><h3 id="groups-only" tabindex="-1">Groups only <a class="header-anchor" href="#groups-only" aria-label="Permalink to &quot;Groups only&quot;">​</a></h3><p>Groups-only access is clean but too rigid. Operators often need to test a plugin with one user or grant temporary access without creating a permanent group.</p><h3 id="deny-overrides" tabindex="-1">Deny overrides <a class="header-anchor" href="#deny-overrides" aria-label="Permalink to &quot;Deny overrides&quot;">​</a></h3><p>Allow/deny matrices add complexity and make effective access harder to reason about. This RFC intentionally starts with one positive policy plus the hard <code>disabled</code> state. A &quot;block specific users from an otherwise-open plugin&quot; requirement is satisfiable without a deny list: move the plugin from <code>everyone</code> to <code>selected_users</code>/<code>selected_groups</code> and grant everyone except the blocked user(s). That&#39;s more admin effort for a large allow-list against a small block-list, but it keeps the resolver a single positive check with no precedence rules between allow and deny. If real usage shows the block-list case is common and the allow-list workaround is unworkable at scale, revisit as a follow-up amendment rather than complicating the v1 resolver.</p><h3 id="dynamic-runtime-install-deferred" tabindex="-1">Dynamic runtime install (deferred) <a class="header-anchor" href="#dynamic-runtime-install-deferred" aria-label="Permalink to &quot;Dynamic runtime install (deferred)&quot;">​</a></h3><p>An admin could install a plugin not already bundled in the image — the running instance fetches the plugin&#39;s source (from a registry entry or arbitrary repository), installs its dependencies, regenerates routes, and restarts/rebuilds — all without a human running <code>sv plugin add</code> and redeploying. This is the more expansive interpretation of &quot;browse and install any available plugin,&quot; and it&#39;s a real long-term goal for Sovereign&#39;s plugin ecosystem.</p><p>It&#39;s deferred here because it&#39;s a different kind of change: today plugin routes are a <strong>build artifact</strong> (<code>scripts/generate-registry.ts</code> composes <code>plugins/*/app/</code> into route groups before <code>next build</code>; the Docker image is otherwise immutable — see Dockerfile:44-46&#39;s explicit choice not to ship plugin source/<code>node_modules</code> into the runner stage). Making install-without-redeploy work means either writable plugin code inside a running production container (in tension with the immutable-image deployment model) or an admin-triggered CI rebuild+redeploy with a &quot;pending restart&quot; UI state — both are build-pipeline/deployment-model decisions with their own security, versioning, and rollback questions, not access-control questions. This RFC&#39;s DB-activation model (bundle everything cataloged, activate via DB) delivers the &quot;browse and install&quot; <em>experience</em> the operator wants today without touching the build pipeline; true dynamic code-fetching install is left for a future RFC once there&#39;s a concrete need it can&#39;t cover.</p><h3 id="launcher-only-filtering" tabindex="-1">Launcher-only filtering <a class="header-anchor" href="#launcher-only-filtering" aria-label="Permalink to &quot;Launcher-only filtering&quot;">​</a></h3><p>Hiding apps from Launcher would improve ergonomics but would not be authorization. Direct routes and plugin APIs must enforce the same policy.</p><h3 id="per-user-installation" tabindex="-1">Per-user installation <a class="header-anchor" href="#per-user-installation" aria-label="Permalink to &quot;Per-user installation&quot;">​</a></h3><p>Installing separate plugin copies per user conflicts with Sovereign&#39;s platform-plugin model and creates avoidable upgrade/data duplication problems.</p><h3 id="use-plugin-scoped-roles-only" tabindex="-1">Use plugin-scoped roles only <a class="header-anchor" href="#use-plugin-scoped-roles-only" aria-label="Permalink to &quot;Use plugin-scoped roles only&quot;">​</a></h3><p>RFC 0054 is still needed for inside-plugin permissions, but platform plugin access must be enforced before plugin code runs.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><s>Should <code>selected_users</code> and <code>selected_groups</code> stay separate policy values, or should the final schema use one <code>selected</code> policy that supports both grant types?</s> <strong>Resolved:</strong> keep them separate. Merging adds a code path that always has to check both grant tables regardless of which policy is active, for a scenario (mixing individual users into a group-based policy) that&#39;s already reachable today by adding the individual to a group.</li><li>Should plugin manifests support an optional access-policy hint for install-time defaults, while keeping operator policy authoritative? Still open; not required for v1 (see &quot;Manifest defaults&quot; above).</li><li><s>When an admin/owner can manage but not open a plugin, should Console show a disabled &quot;Open&quot; button with the reason, or hide it entirely?</s> <strong>Resolved:</strong> show it disabled, with the reason. An admin managing a plugin already knows it exists (they&#39;re looking at its Console page); a disabled button with an explanation is more useful than hiding an affordance the admin otherwise expects to be there, and it&#39;s consistent with the 404-to-avoid-disclosure principle applying only to users who don&#39;t already have management visibility.</li><li>What cache invalidation mechanism should be used so policy changes take effect immediately across middleware, shell navigation, and API routing? Still open — resolve by reusing whichever mechanism <code>plugin_status</code>&#39;s existing enabled/disabled flag already relies on (<code>runtime/src/plugin-status.ts</code>) rather than introducing a second invalidation path for the new <code>access_policy</code>/grant tables.</li></ul><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li>Add user groups and Console group management.</li><li>Add the plugin access schema (including <code>self_service</code> and <code>invites.plugins</code>), migration, and centralized resolver.</li><li>Add the plugin catalog browser and DB-driven activation flow (migrations-on-activate, disabled-by-default policy).</li><li>Enforce access in runtime routing, Launcher, shell navigation, and plugin API delegation.</li><li>Add Console plugin Access management, including the self-service toggle and the invite plugin scope.</li><li>Land RFC 0070 (per-user capability grants) far enough to gate <code>plugins:self-manage</code>, or ship self-service disabled-by-default until it lands.</li><li>Add regression tests for every policy, including direct-route denial, root-plugin fallback, self-service grant/revoke, and invite-scope resolution at registration.</li><li>Update operator and plugin developer docs to distinguish platform app access from plugin-scoped authorization, and to document the catalog/activation model.</li></ol><p>This RFC has no <code>@sovereignfs/sdk</code> or <code>@sovereignfs/ui</code> surface changes and no manifest schema change (see &quot;Manifest defaults&quot;); it is an internal <code>packages/db</code>/<code>runtime</code>/<code>apps/auth</code>/Console change.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><ul><li>2026-07 — Drafted after deciding that plugin visibility must become platform-enforced access control, backed by user groups and explicit plugin policies.</li><li>2026-07 (revision) — Added the plugin catalog/install-time activation model (disabled-by-default for newly activated plugins, addressing the &quot;browse and install&quot; and &quot;configurable default scope&quot; requirements without a deny-override matrix); added self-service opt-in for selected_users/selected_groups gated by the new RFC 0070 capability-grant mechanism; added invite-scoped plugin entitlement; resolved three of the four open questions; documented dynamic runtime install as an explicitly deferred future RFC.</li><li>2026-07-19 (correction) — The initial implementation (epic task 3.28) read the &quot;existing plugins... keep their current effective state&quot; grandfathering language above as license to eagerly create an explicit <code>enabled: true, accessPolicy: &#39;everyone&#39;</code> <code>plugin_status</code> row for <strong>every</strong> non-chrome plugin — including example plugins — on an instance&#39;s first-ever boot, via a <code>backfillPluginCatalogOnce()</code> function gated only by a one-time flag, with no way to distinguish &quot;an instance that predates this RFC&quot; from &quot;a brand-new instance booting for the first time after this RFC shipped.&quot; Two consequences, both bugs relative to this RFC&#39;s and Task 12.3&#39;s actual intent: (1) ordinary plugins were active by default on every fresh instance instead of requiring explicit admin activation; (2) because &quot;an explicit row always wins&quot; over the Settings → Example plugins bulk toggle, every example plugin became permanently immune to that toggle the moment the backfill touched it. Separately, <code>resolveAccessPolicy</code> (<code>runtime/src/plugin-access-server.ts</code>) defaulted a row-less plugin to <code>accessPolicy: &#39;disabled&#39;</code> with no awareness of the examples bulk toggle at all, so even a correctly-row-less example would have stayed access-restricted after turning the bulk toggle on. Fixed by: removing <code>backfillPluginCatalogOnce</code> entirely (safe — see the scope note above the &quot;Existing plugins...&quot; paragraph, and the fact that any instance which had already booted past 3.28&#39;s landing commit had already run the backfill and set its one-time flag, so removing the function has no effect on it); and making <code>resolveAccessPolicy</code> example-aware, so a row-less example plugin resolves to <code>everyone</code> when the bulk toggle is on and <code>disabled</code> otherwise, matching the resolution already used by <code>runtime/src/plugin-status.ts</code>&#39;s <code>computeDisabledPluginIds</code>. Also fixed a related gap: <code>runtime/app/api/account/sidebar-plugins/route.ts</code> (Account → Preferences → Sidebar) was computing its plugin list without this RFC&#39;s <code>restrictedIds</code> gate at all, unlike every other consumer (<code>(platform)/layout.tsx</code>, <code>/api/plugins</code>) — a user could reorder or &quot;show&quot; a plugin there that access policy actually denied them everywhere else.</li></ul>`,91)])])}const h=a(t,[["render",o]]);export{g as __pageData,h as default};
