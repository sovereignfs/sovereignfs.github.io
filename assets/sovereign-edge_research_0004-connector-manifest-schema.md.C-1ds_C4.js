import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.CHeM0PsO.js";const m=JSON.parse('{"title":"Connector manifest schema: format, validation, and the templating surface","description":"","frontmatter":{"id":4,"title":"Connector manifest schema: format, validation, and the templating surface","status":"Decided — implemented in task 2.1","date":"August 2026","author":"Claude Code (session with the developer)","scope":"shared","summary":"Task 0.1.13 / epic 2.1 — the declarative shape every Tier 1 connector conforms to","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/research/0004-connector-manifest-schema.md"},"headers":[],"relativePath":"sovereign-edge/research/0004-connector-manifest-schema.md","filePath":"sovereign-edge/research/0004-connector-manifest-schema.md"}'),i={name:"sovereign-edge/research/0004-connector-manifest-schema.md"};function s(r,e,l,d,c,h){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="research-0004-—-connector-manifest-schema-format-validation-and-the-templating-surface" tabindex="-1">Research 0004 — Connector manifest schema: format, validation, and the templating surface <a class="header-anchor" href="#research-0004-—-connector-manifest-schema-format-validation-and-the-templating-surface" aria-label="Permalink to &quot;Research 0004 — Connector manifest schema: format, validation, and the templating surface&quot;">​</a></h1><p><strong>Related:</strong> <a href="./0001-concept-and-connector-architecture.html">0001</a> (tiered connector model, per-connector permissions), <a href="./../network-audit.html">network-audit</a> (the boundary this framework is the sanctioned exception to)</p><hr><h2 id="question" tabindex="-1">Question <a class="header-anchor" href="#question" aria-label="Permalink to &quot;Question&quot;">​</a></h2><p>Epic 2.1 lists the fields a Tier 1 connector manifest carries. It does not say what format the schema is written in, what validates it, how it survives version changes, or — the part with teeth — <strong>how values produced by a language model get turned into an HTTP request without becoming an injection vector.</strong></p><p>This is the piece epic 5 reuses unchanged when third parties start writing connectors. A field can be added later; a templating design cannot be retrofitted once manifests exist in the wild.</p><h2 id="findings" tabindex="-1">Findings <a class="header-anchor" href="#findings" aria-label="Permalink to &quot;Findings&quot;">​</a></h2><h3 id="llama-rn-already-converts-json-schema-to-a-decoding-grammar" tabindex="-1"><code>llama.rn</code> already converts JSON Schema to a decoding grammar <a class="header-anchor" href="#llama-rn-already-converts-json-schema-to-a-decoding-grammar" aria-label="Permalink to &quot;`llama.rn` already converts JSON Schema to a decoding grammar&quot;">​</a></h3><p>Task 2.3 requires that a small on-device model emit &quot;reliably valid JSON even from a small on-device model&quot;. That capability is already present in the dependency this project uses, and it is shaped around JSON Schema specifically:</p><ul><li><code>completion()</code> accepts <code>json_schema</code>, documented in <code>src/types.ts</code> as &quot;JSON schema for convert to grammar for structured JSON output&quot;.</li><li><code>response_format: { type: &#39;json_schema&#39; }</code> is supported.</li><li>The native side bundles <code>cpp/common/json-schema-to-grammar.cpp</code>.</li><li><code>tools</code> and <code>tool_choice</code> are accepted directly.</li></ul><p><strong>Consequence:</strong> if a manifest&#39;s tool parameters are written as JSON Schema, grammar-constrained decoding in task 2.3 costs nothing to add. Any other format means writing and maintaining a converter to get the same guarantee.</p><h3 id="tool-calling-support-is-a-property-of-the-loaded-model-not-the-connector" tabindex="-1">Tool-calling support is a property of the loaded model, not the connector <a class="header-anchor" href="#tool-calling-support-is-a-property-of-the-loaded-model-not-the-connector" aria-label="Permalink to &quot;Tool-calling support is a property of the loaded model, not the connector&quot;">​</a></h3><p><code>llama.rn</code> reports per-model capability at load time:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>chatTemplates.jinja.defaultCaps.{ tools, toolCalls, systemRole, parallelToolCalls }</span></span>\n<span class="line"><span>chatTemplates.jinja.toolUse</span></span></code></pre></div><p>So &quot;can this model call tools at all&quot; is answerable at runtime from the model, and does not belong in the manifest. A connector manifest describes the tool; whether the current model can invoke it is a separate, detectable fact. This matters for task 2.3&#39;s required fallback behaviour — the honest message when tool-calling is unavailable is about the <em>model</em>, not the connector.</p><h3 id="there-is-no-validation-library-in-the-project-today" tabindex="-1">There is no validation library in the project today <a class="header-anchor" href="#there-is-no-validation-library-in-the-project-today" aria-label="Permalink to &quot;There is no validation library in the project today&quot;">​</a></h3><p>Dependencies are deliberately few: <code>@noble/hashes</code>, four Expo packages, <code>llama.rn</code>, React Native and navigation. Nothing does runtime schema validation. Whatever 2.1 chooses is a new dependency in a project that has just published an audit of its dependency surface — so the choice needs a reason beyond familiarity.</p><p>Ajv, the usual JSON Schema validator, compiles schemas by generating JavaScript at runtime. That is a poor fit here on two counts: Hermes restricts runtime code generation, and &quot;we generate and execute code derived from a third-party manifest&quot; is precisely the sentence a security reviewer will stop on. It should not be the default choice for this project.</p><h3 id="the-templating-step-is-the-real-security-surface" tabindex="-1">The templating step is the real security surface <a class="header-anchor" href="#the-templating-step-is-the-real-security-surface" aria-label="Permalink to &quot;The templating step is the real security surface&quot;">​</a></h3><p>A Tier 1 connector maps a model-produced tool call into an HTTP request. The model&#39;s output is <em>untrusted input</em> — it is influenced by whatever the user pasted into chat, and small models are easy to steer. If a manifest can express &quot;put this argument here&quot; as free string interpolation, the following all become reachable:</p><ul><li><strong>Origin escape</strong> — an argument that changes host, port, or scheme (<code>https://api.example.com/@evil.com/</code>, or a leading <code>//</code>).</li><li><strong>Path traversal</strong> — <code>../../admin</code> in a segment.</li><li><strong>Header injection</strong> — CRLF in a value.</li><li><strong>Credential leakage</strong> — a token interpolated into a URL, which then lands in a proxy log or a <code>Referer</code>.</li><li><strong>SSRF onto the user&#39;s own LAN</strong> — especially sharp here, because <code>NSAllowsLocalNetworking</code> is deliberately enabled for the self-hosted Sovereign Tasks connector.</li></ul><p>This is not hypothetical for third-party manifests in Phase 3, and the mitigation has to exist in the schema from the start, because a manifest format that permits free interpolation cannot later forbid it without breaking every connector written against it.</p><h2 id="options-considered" tabindex="-1">Options considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options considered&quot;">​</a></h2><h3 id="manifest-validation" tabindex="-1">Manifest validation <a class="header-anchor" href="#manifest-validation" aria-label="Permalink to &quot;Manifest validation&quot;">​</a></h3><p><strong>A. Zod, with tool parameters kept as opaque JSON Schema (recommended).</strong> Pure TypeScript, no runtime code generation, types derive from the schema so the validator and the TS type cannot drift. The <code>tool.parameters</code> field stays raw JSON Schema — validated structurally (is it an object, does it declare <code>type</code>/<code>properties</code>) but not interpreted, then handed to <code>llama.rn</code> as-is. Cost: one dependency, and two schema dialects in one file.</p><p><strong>B. Ajv over a JSON Schema definition of the manifest.</strong> One dialect throughout, and the manifest schema itself becomes publishable for third-party authors. Rejected as the default: runtime code generation against third-party-supplied input, and Hermes constraints.</p><p><strong>C. Hand-rolled validator.</strong> No dependency, full control, and every error message can be written for a connector author. Realistically a few hundred lines that must be kept in step with the TS types by hand — the same duplication that <code>app-info.test.ts</code> exists to catch elsewhere in this repo, but larger and without a cheap equality check to lock it.</p><h3 id="templating" tabindex="-1">Templating <a class="header-anchor" href="#templating" aria-label="Permalink to &quot;Templating&quot;">​</a></h3><p><strong>D. Typed slots with per-position encoding (recommended).</strong> The manifest declares a fixed <code>origin</code> and a path <em>template made of literal segments and named slots</em>. Each slot states where it goes — path segment, query value, JSON body field, header value — and the runtime encodes per position. No expression language, no string concatenation, and the origin is not substitutable at all.</p><p><strong>E. A restricted expression language</strong> (<code>{{ args.query | urlencode }}</code>). Familiar and flexible. It also means shipping an evaluator, and every filter is a place to get encoding wrong. Flexibility here buys the connector author convenience and buys the user risk.</p><p><strong>F. Full string interpolation.</strong> Simplest to implement, and unfixable later. Rejected outright.</p><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><ul><li><strong>Tool parameters are JSON Schema</strong>, so grammar-constrained decoding in 2.3 is free rather than a converter to maintain.</li><li><strong>The manifest is validated with Zod</strong>; <code>tool.parameters</code> is validated shallowly and passed through untouched.</li><li><strong>No expression language.</strong> Typed slots, encoded per position, origin fixed and non-substitutable.</li><li><strong>Credentials are never expressible in a URL</strong> — the schema allows them in a header or a body field only, and the validator rejects a manifest that places one in <code>origin</code>, path, or query.</li><li><strong><code>manifestVersion</code> is separate from the connector&#39;s own <code>version</code></strong>, and an unknown <code>manifestVersion</code> is rejected rather than parsed best-effort. A connector that will not load is a better outcome than one that loads with a field silently ignored.</li><li><strong>Signatures stay detached</strong> (Phase 3), so the signed bytes are exactly the manifest file and adding signing later changes nothing about the format.</li><li><strong>Declared network access is an allowlist of origins</strong>, and the runtime refuses any request outside it. This is what makes epic 2.2&#39;s per-connector grant enforceable rather than advisory.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><strong>Response mapping.</strong> Turning an HTTP response back into text for the model is the mirror of the request problem, and is deliberately not designed here. It needs its own pass, including how much of a response body is allowed to reach the model&#39;s context.</li><li><strong>Whether the manifest schema is published as JSON Schema for third-party authors</strong> even though Zod validates internally. Two artifacts that must agree is a drift risk of the kind this repo has already been bitten by.</li><li><strong>Redirect handling.</strong> A 302 to another origin defeats an origin allowlist unless redirects are disabled or re-checked per hop.</li><li><strong>Timeouts, retries, and response size caps</strong> — absent from the epic&#39;s field list, and each is a way for a connector to misbehave without ever leaving its allowlist.</li><li><strong>Where manifests live on disk</strong> and whether first-party connectors are compiled in or loaded from a file like everything else. Loading them the same way is the honest test that the format is genuinely sufficient.</li></ul><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><p>The recommendation deliberately trades connector-author convenience for user safety at every fork. That is the correct default for a product whose entire pitch is that the network boundary is real — but it is a trade, and Phase 3 will produce authors who find it restrictive. The place to revisit it is a specific connector that cannot be expressed, not a general complaint that templating is limited.</p>',37)])])}const u=t(i,[["render",s]]);export{m as __pageData,u as default};
