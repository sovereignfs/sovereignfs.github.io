import{_ as o,o as t,c as a,a2 as s}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse(`{"title":"Desktop shell technology: Tauri vs. Electron vs. React Native desktop","description":"","frontmatter":{"id":10,"title":"Desktop shell technology: Tauri vs. Electron vs. React Native desktop","status":"decided","date":"August 2026","author":"Claude Code (session with the developer)","scope":"desktop","summary":"Epic 9.1's spike — which shell desktop builds on, weighed against native llama.cpp bindings, Tier 3 OS integration, and install size","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/research/0010-desktop-shell-technology.md"},"headers":[],"relativePath":"sovereign-edge/research/0010-desktop-shell-technology.md","filePath":"sovereign-edge/research/0010-desktop-shell-technology.md"}`),i={name:"sovereign-edge/research/0010-desktop-shell-technology.md"};function n(r,e,c,d,l,p){return t(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="research-0010-—-desktop-shell-technology" tabindex="-1">Research 0010 — Desktop shell technology <a class="header-anchor" href="#research-0010-—-desktop-shell-technology" aria-label="Permalink to &quot;Research 0010 — Desktop shell technology&quot;">​</a></h1><p><strong>Related:</strong> <a href="./../epics/desktop/shell.html">Epic 9 — Desktop Shell</a> (task 9.1, this doc&#39;s own subject), <a href="./../epics/desktop/core-port.html">Epic 12 — Desktop Core Port</a> (depends on the decision here), <a href="./0001-concept-and-connector-architecture.html#open-questions">research 0001</a> (first flagged this as an open question), <a href="./0002-react-native-framework-choice.html">research 0002</a> (the mobile equivalent of this decision, same shape of question one layer down the stack)</p><hr><h2 id="note-on-sequencing" tabindex="-1">Note on sequencing <a class="header-anchor" href="#note-on-sequencing" aria-label="Permalink to &quot;Note on sequencing&quot;">​</a></h2><p><code>ROADMAP.md</code>&#39;s Desktop section and epic 9.1 itself both gate this on &quot;Mobile MVP (Phase 1) shipped.&quot; Phase 1 is functionally complete — chat, model manager, and the Search connector all ship — but task 0.1.20 (store release setup) is still open. This spike was deliberately pulled forward ahead of that one packaging/submission task, on the developer&#39;s explicit instruction, because it answers an architecture question (which native <code>llama.cpp</code> binding, which package layout) that has no bearing on whether the mobile store listing is live. <strong>Epic 12 (the actual desktop port) still is not started</strong> — this doc and epic 9.1&#39;s status are the only things moving early.</p><h2 id="question" tabindex="-1">Question <a class="header-anchor" href="#question" aria-label="Permalink to &quot;Question&quot;">​</a></h2><p>Which shell technology does <code>apps/desktop</code> build on? The three candidates on the table since research 0001: Tauri, Electron, or a React Native desktop renderer (<code>react-native-macos</code> / <code>react-native-windows</code>, reusing <code>apps/mobile</code>&#39;s own framework). Decided against this project&#39;s specific needs, not shell technology in the abstract:</p><ol><li>A working path to run <code>llama.cpp</code> GGUF inference on the desktop, since <code>llama.rn</code> (<code>apps/mobile</code>&#39;s binding) is mobile-only.</li><li>A Tier 3 native-OS-integration story as strong as the permission model <code>apps/mobile</code> already ships (per-capability grants, revocable, enforced structurally — see <a href="./../epics/mobile/connector-framework.html">connector-framework.md</a> task 2.6).</li><li>Install size — this product&#39;s whole pitch is a lean, trustworthy local companion, not another Electron-sized download.</li><li>How much of <code>packages/mobile-ui</code> / <code>packages/design-tokens</code> a choice lets this app reuse, per both packages&#39; own READMEs.</li></ol><h2 id="findings" tabindex="-1">Findings <a class="header-anchor" href="#findings" aria-label="Permalink to &quot;Findings&quot;">​</a></h2><p><strong><code>llama.rn</code> does not run outside React Native, and does not extend to <code>react-native-macos</code>/<code>react-native-windows</code> today.</strong> Its own repository documents iOS and Android only — no mention of macOS or Windows, no <code>react-native-macos</code>/<code>-windows</code> compatibility notes. Worse, <code>llama.rn</code> v0.10+ hard-requires React Native&#39;s New Architecture (Fabric/TurboModules); the out-of-tree Windows and macOS platforms have historically lagged core React Native&#39;s New Architecture rollout by a wide margin, and there is no evidence either has caught up enough to run <code>llama.rn</code> unmodified. Picking RN-desktop would not mean reusing <code>llama.rn</code> — it would mean writing a brand-new native <code>llama.cpp</code> binding per desktop OS from scratch, on a platform (<code>react-native-windows</code> in particular) with a materially smaller contributor base than either Tauri or Electron to lean on when that binding breaks.</p><p><strong>Electron has a real, actively-maintained <code>llama.cpp</code> binding.</strong><a href="https://github.com/withcatai/node-llama-cpp" target="_blank" rel="noreferrer"><code>node-llama-cpp</code></a> wraps <code>llama.cpp</code> for Node, is used in shipped Electron apps today (demonstrated in public examples), and had community activity as recent as this year. Bundle size is the well-known tradeoff: Electron installers commonly run 50–150MB+ because every app ships its own Chromium and Node runtime, regardless of what the app itself needs.</p><p><strong>Tauri has an equally real path via Rust bindings</strong> (<code>llama-cpp-2</code>, <code>llama_cpp-rs</code>), demonstrated in multiple shipped and in-progress projects running <code>llama.cpp</code>/GGUF models fully on-device through a Tauri shell. Tauri uses the OS&#39;s own WebView instead of bundling a browser engine, so typical app sizes land in the 3–15MB range — roughly an order of magnitude smaller than Electron&#39;s — with correspondingly lower idle memory use. Tauri v2 has been stable since October 2024.</p><p><strong>Tauri v2&#39;s security model is a closer structural match to this project&#39;s own.</strong> It replaced Tauri 1&#39;s allowlist with a default-deny <strong>capabilities / permissions / scopes</strong> system: every window&#39;s access to native commands is explicitly granted per capability, nothing is reachable by default. That is the same shape <code>apps/mobile</code>&#39;s connector framework already enforces — per-connector, per-capability, revocable, denied unless explicitly granted (see <code>permissions/grants.ts</code>&#39;s <code>isAllowed()</code> and task 2.6&#39;s generalized granted-scope concept). Electron&#39;s main process has full Node access by default; matching Tauri&#39;s default-deny posture there is possible but is discipline the developer has to maintain, not something the shell enforces structurally — the same &quot;cannot, not should not&quot; distinction this repo&#39;s own <code>ConnectorVault</code> design already leans on elsewhere.</p><p><strong>Skillset cost is real and goes the other way.</strong> Tauri&#39;s backend is Rust. Nothing in this repo is Rust today; the mobile app and its tooling are TypeScript/Swift/Kotlin (via Expo modules). Electron would let the existing TypeScript skillset carry over directly, with <code>node-llama-cpp</code> as a same-language dependency rather than a new one. This is the strongest point in Electron&#39;s favor and the main cost on Tauri&#39;s side of the ledger.</p><p><strong>Package reuse is a wash, not a tiebreaker.</strong> Neither Tauri nor Electron lets <code>apps/desktop</code> consume <code>packages/mobile-ui</code> directly — both render a web frontend (React DOM or similar), not React Native primitives. Either choice means <code>packages/desktop-ui</code> gets real content, built against <code>packages/design-tokens</code>, exactly the second branch both packages&#39; READMEs already anticipated. Only RN-desktop would have avoided this, and RN-desktop is ruled out on the inference-binding finding above regardless.</p><h2 id="options-considered" tabindex="-1">Options considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options considered&quot;">​</a></h2><p><strong>A. Tauri.</strong> Smallest install size and lowest idle memory by a wide margin; default-deny capability model matches this project&#39;s existing trust architecture; real Rust <code>llama.cpp</code> bindings exist and are used in shipped apps. Cost: Rust is a new language for this codebase, and Tauri&#39;s tooling/ ecosystem, while stable, is smaller than Electron&#39;s. <strong>Recommended.</strong></p><p><strong>B. Electron.</strong> No new language — the existing TypeScript skillset carries over directly; <code>node-llama-cpp</code> is mature and actively maintained; by far the largest packaging/signing/auto-update ecosystem. Cost: 50–150MB+ installs and proportionally higher memory use work against this product&#39;s own &quot;lean, trustworthy local companion&quot; pitch, and matching Tauri&#39;s default-deny security posture takes deliberate main-process discipline rather than a shell-enforced default.</p><p><strong>C. React Native desktop</strong> (<code>react-native-macos</code> / <code>react-native-windows</code>). Would in principle let <code>apps/desktop</code> consume <code>packages/mobile-ui</code> (or even <code>apps/mobile/src/design-system/</code>) directly, and keep the whole codebase in one framework. Ruled out: <code>llama.rn</code> has no desktop support today and no signaled path to it, <code>react-native-windows</code>&#39;s New Architecture support lags enough to make the New-Architecture-only <code>llama.rn</code> v0.10+ a further risk even if a binding existed, and the actual engineering lift — writing a new native <code>llama.cpp</code> binding per OS — is the same order of effort Tauri requires, without Tauri&#39;s install-size or security-model advantages to show for it.</p><h2 id="recommendation" tabindex="-1">Recommendation <a class="header-anchor" href="#recommendation" aria-label="Permalink to &quot;Recommendation&quot;">​</a></h2><p><strong>Tauri v2.</strong> It wins on two of this doc&#39;s four stated criteria outright (install size, Tier 3 security-model fit), ties on the third (a real, maintained <code>llama.cpp</code> binding exists, same as Electron), and only RN-desktop — already ruled out — would have won the fourth (package reuse). The Rust skillset cost is real but bounded: the surface Rust actually needs to cover (an <code>EngineAdapter</code> around a <code>llama.cpp</code> binding, a <code>SecureStorageAdapter</code> over each OS&#39;s credential store, a Tier 3 native-handler registry) is small and mirrors <code>apps/mobile/src/models/</code>, <code>permissions/vault.ts</code>, and <code>nativeHandlers.ts</code> closely enough to port by pattern rather than design from scratch.</p><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><ul><li><strong>Shell: Tauri v2.</strong> <code>apps/desktop</code> builds its UI as a web frontend (React DOM) inside a Tauri shell, not Electron and not <code>react-native-macos</code>/<code>-windows</code>.</li><li><strong>Inference:</strong> a Rust <code>llama.cpp</code> binding (<code>llama-cpp-2</code> or equivalent, final crate choice deferred to task 12.2&#39;s implementation) behind an <code>EngineAdapter</code>, mirroring <code>apps/mobile/src/chat/inference</code>&#39;s own adapter shape once <code>packages/core</code>&#39;s extraction happens.</li><li><strong>Credentials:</strong> a <code>SecureStorageAdapter</code> over the OS&#39;s own credential store — macOS Keychain, Windows Credential Manager, Linux Secret Service — via a Tauri plugin, mirroring <code>permissions/vault.ts</code>&#39;s per-connector namespacing (task 12.3&#39;s job to implement, not this doc&#39;s).</li><li><strong>Tier 3 native handlers:</strong> Tauri commands gated by its own capabilities system, registered the same &quot;manifest names a capability, runtime looks it up&quot; way <code>apps/mobile/src/connectors/runtime/nativeHandlers.ts</code> already does — the manifest schema and permission model (task 2.6) carry over unchanged, only the native-handler registry&#39;s implementation differs per platform.</li><li><strong><code>packages/desktop-ui</code> is now real work</strong>, not a maybe: it needs a component set matching <code>apps/mobile/src/design-system</code>&#39;s (<code>ThemeProvider</code>, <code>Button</code>, <code>ChatBubble</code>, <code>ListItem</code>, <code>TextField</code>, <code>Toggle</code>) built against <code>packages/design-tokens</code>, for React DOM rather than React Native primitives. <code>packages/mobile-ui</code> is unaffected — still not worth populating, since desktop cannot consume it regardless.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Final Rust <code>llama.cpp</code> crate choice (<code>llama-cpp-2</code> vs. <code>llama_cpp-rs</code> vs. shelling out to a bundled <code>llama-server</code> binary, as some surveyed projects do) — task 12.2&#39;s implementation decision, not this doc&#39;s to settle.</li><li>Auto-update story: Tauri has a first-party updater plugin, but this product&#39;s own model — direct sale, no store review gate — means the update mechanism and code-signing/notarization requirements need their own pass before task 12.1 (build tooling) or any later release-packaging task ships anything.</li><li>WebView rendering parity across macOS (WebKit), Windows (WebView2), and Linux (WebKitGTK) for the specific <code>packages/design-tokens</code> output — not measured here; task 12.6&#39;s own review checklist is where this gets checked, not assumed.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><p>Epic 9 (Desktop Shell) is done — recorded here. Epic 12 (Desktop Core Port, now broken into tasks 12.1–12.7 — see <a href="./../epics/desktop/core-port.html">core-port.md</a>) can now start in principle, but per the note on sequencing above, this repo&#39;s own convention is still &quot;one task at a time, sequenced&quot; — whether 12.1 actually starts next, or waits for task 0.1.20 to close out Phase 1 properly first, is a scheduling call for whoever picks this up next, not a technical blocker this doc leaves behind.</p>',27)])])}const m=o(i,[["render",n]]);export{u as __pageData,m as default};
