import{_ as t,o,c as n,a2 as s}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0074 — Offline-capable plugin routes","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0074-offline-capable-plugins.md","filePath":"rfcs/0074-offline-capable-plugins.md"}'),i={name:"rfcs/0074-offline-capable-plugins.md"};function a(r,e,l,d,c,h){return o(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="rfc-0074-—-offline-capable-plugin-routes" tabindex="-1">RFC 0074 — Offline-capable plugin routes <a class="header-anchor" href="#rfc-0074-—-offline-capable-plugin-routes" aria-label="Permalink to &quot;RFC 0074 — Offline-capable plugin routes&quot;">​</a></h1><p><strong>Status:</strong> Implemented — platform plumbing (manifest fields <code>offline.routes</code>/ <code>offline.root</code>, SDK surface, SW precaching, logout+login purge, shared-shell neutrality, Launcher as the PWA cold-start entry point) is in place; Shopper and Wallet have adopted <code>offline.routes</code> in their own repositories, Launcher adopts <code>offline.root</code><br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/manifest</code> (new <code>offline</code> manifest field), <code>packages/sdk</code> (new client-side <code>sdk.offline.*</code> surface), <code>runtime</code> (route composition, service worker generation in <code>next.config.ts</code> / <code>scripts/</code>, logout purge), Console (offline-plugin surfacing), <code>docs/plugin-development.md</code>; builds on RFC 0013 (mobile responsiveness &amp; PWA), RFC 0042 (public plugin routes — manifest route-list precedent), and the per-user SSR caching rule in CLAUDE.md<br><strong>Incorporated into plan:</strong> No roadmap slot or epic task ID assigned. Platform plumbing shipped directly on <code>feat/offline-capable-plugins</code> (manifest fields, SDK surface, SW precaching, logout+login purge, shared-shell neutrality, Launcher&#39;s <code>offline.root</code> adoption — §§1–6 of Proposed design, all landed). Console surfacing (open question 5) remains optional, not built. Offline <strong>writes</strong> (outbox + sync) remain explicitly out of scope, left to a future RFC.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Let a plugin declare, in its <code>manifest.json</code>, a small set of <strong>mission-critical routes that must keep working with no network</strong>. When the device is offline, the app opens, those declared routes render from cache, and any other path shows the existing &quot;no internet connection&quot; fallback. Everything is <strong>opt-in per plugin and per route</strong> — nothing is cached offline unless a plugin asks for it.</p><p>The v1 scope is <strong>read-only</strong>: a user can open the app and <em>view</em> cached data on an offline-enabled route (e.g. Wallet → Cards, Tasks → today&#39;s list, Shopper → the current list). Creating or editing while offline (queued writes, background sync, conflict resolution) is deliberately deferred to a later RFC.</p><p>The design threads a needle that the current architecture makes narrow: Sovereign pages are <strong>per-user SSR</strong> and must never be stale-served from cache (a hard rule in CLAUDE.md — a replayed authenticated shell could show a previous user&#39;s content after logout/login on a shared device). Offline-enabled routes therefore render a <strong>user-neutral shell</strong> and hydrate their data <strong>client-side from IndexedDB</strong>, so nothing per-user is ever cached as HTML.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Users expect an installed app to open and do <em>something</em> useful without a connection — glance at a boarding-pass-style card, check a shopping list in a shop with no signal, tick off a task on the subway. Today Sovereign is installable (RFC 0013) but is entirely online-dependent past the static <code>/offline</code> fallback: every page is per-user SSR fetched <code>NetworkFirst</code>, and plugin data is only reachable through server-side code. Offline, the app can show a connectivity banner and a fallback page — and nothing else.</p><p>Caching <em>everything</em> is the wrong answer: it bloats storage, risks staleness across the whole app, and — most importantly — collides head-on with the per-user SSR rule. The right answer is a <strong>narrow, declared allowlist</strong>: a plugin nominates the few paths that matter offline and takes on the constraints that come with it. This keeps offline exposure explicit and reviewable, exactly as RFC 0042 did for public routes.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>PWA + Workbox service worker.</strong> <code>runtime/next.config.ts:52</code> wires <code>@ducanh2912/next-pwa</code>; the SW is generated into <code>public/</code> at build time and disabled in dev.</li><li><strong>The <code>pages</code> cache is <code>NetworkFirst</code>, deliberately.</strong><code>runtime/next.config.ts:69-84</code> caches same-origin non-<code>/api/</code> GETs with a 4s <code>networkTimeoutSeconds</code>, falling back to <code>/offline</code>. The comment there and the hard rule in <code>CLAUDE.md</code> both state why it is <strong>not</strong> stale-while-revalidate: Sovereign pages are per-user SSR, and replaying a cached authenticated shell risks showing a stale/different user&#39;s content after logout/login. <strong>Any offline design must preserve this rule.</strong></li><li><strong>Offline fallback route.</strong> <code>runtime/app/offline/page.tsx</code> is a self-contained, auth-free &quot;You&#39;re offline&quot; shell that next-pwa precaches and serves for failed navigations (<code>fallbacks.document: &#39;/offline&#39;</code>).</li><li><strong>Connectivity banner.</strong> <code>runtime/app/(platform)/_components/OfflineBanner.tsx</code> already surfaces soft-offline/reconnect state, wired into both platform and minimal layouts.</li><li><strong>Plugin data is server-side only.</strong> <code>sdk.db.getClient()</code> (<code>packages/sdk/src/db.ts:25</code>) and <code>sdk.storage</code> (<code>packages/sdk/src/storage.ts:7</code>) both read <code>next/headers</code> and only run in a server request context. Plugin pages are SSR server components that query the DB during render. <strong>There is no client-side read path for plugin data today</strong> — so caching a plugin&#39;s HTML alone would render an empty page offline; the data must also be on the device.</li><li><strong>Manifest route-list precedent.</strong> <code>publicRoutes</code> (<code>packages/manifest/src/schema.ts:156-186</code>) already validates a per-plugin array of route prefixes relative to <code>routePrefix</code>, with strict rules (must start with <code>/</code>, must not be <code>/</code>, no <code>..</code>, no route-group/interception markers, unique). The <code>offline</code> field mirrors this shape and validation.</li><li><strong>Client IndexedDB precedent.</strong> The e2ee SDK already uses IndexedDB from the client (<code>packages/sdk/src/e2ee-device.ts</code>), exposed via a dedicated <code>@sovereignfs/sdk/e2ee-device</code> subpath (not the main barrel) because the barrel also reaches server-only modules — <code>sdk.offline</code> follows the same subpath pattern.</li><li><strong>The parity test.</strong> <code>runtime/src/docs-parity.test.ts</code> requires every new manifest field and SDK key to appear in its doc; a new <code>offline</code> field and <code>sdk.offline.*</code> surface must be documented in <code>docs/plugin-development.md</code> in the same change.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><p>Four parts: a manifest opt-in, an app-shell rendering rule, a client-side data surface, and scoped service-worker precaching — plus a logout purge that keeps the whole thing safe on shared devices.</p><h3 id="_1-manifest-field-—-offline" tabindex="-1">1. Manifest field — <code>offline</code> <a class="header-anchor" href="#_1-manifest-field-—-offline" aria-label="Permalink to &quot;1. Manifest field — \`offline\`&quot;">​</a></h3><p>Add an optional <code>offline</code> object to <code>manifest.json</code>, mirroring <code>publicRoutes</code>:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;routes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Relative to this plugin&#39;s routePrefix. Same validation as publicRoutes:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // must start with &quot;/&quot;, must not be &quot;/&quot;, no &quot;..&quot;, no &quot;(&quot; / &quot;)&quot; markers,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // unique within the plugin.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;prefix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/cards&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;View saved cards without a connection.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Rules:</p><ul><li><code>offline.routes[].prefix</code> is relative to the plugin <code>routePrefix</code>; resolves to <code>&lt;routePrefix&gt;&lt;prefix&gt;/*</code> (e.g. <code>/wallet/cards/*</code>).</li><li>Same prefix-safety validation as <code>publicRoutes</code> (reuse the refinement).</li><li>Offline routes are <strong>never inherited</strong> by child plugins or API routes.</li><li>Declaring <code>offline</code> requires <strong>no new permission</strong> — it grants no additional server capability; it only changes caching/rendering behavior for the plugin&#39;s own routes. (Open question 1 revisits whether an explicit <code>offline</code> permission is worth it for reviewability.)</li><li>Absent field ⇒ the plugin is fully online-only (today&#39;s behavior). No plugin is affected until it opts in.</li></ul><h3 id="_2-rendering-rule-—-offline-routes-are-a-user-neutral-shell" tabindex="-1">2. Rendering rule — offline routes are a <strong>user-neutral shell</strong> <a class="header-anchor" href="#_2-rendering-rule-—-offline-routes-are-a-user-neutral-shell" aria-label="Permalink to &quot;2. Rendering rule — offline routes are a **user-neutral shell**&quot;">​</a></h3><p>This is the load-bearing constraint that keeps the per-user-SSR rule intact.</p><p>An offline-declared route <strong>must not</strong> emit per-user content in its server-rendered HTML. Its SSR output is a <strong>user-neutral app shell</strong> (layout, static chrome, empty states, skeletons); the actual user data is fetched <strong>client-side</strong> and rendered after hydration. Because the cached HTML contains nothing user-specific, precaching and replaying it offline cannot leak one user&#39;s data to another — the shell is the same for everybody, and the data comes from a per-user client store that logout clears (§5).</p><p>Concretely, an offline route is a <code>&#39;use client&#39;</code>-driven page (or a server shell whose data region is a client component) that:</p><ol><li>On load, reads from the client offline store (§3) and renders immediately (works with no network).</li><li>If online, fetches fresh data from the plugin&#39;s read API, renders it, and <strong>writes it back</strong> into the offline store for next time.</li><li>If offline and the store is empty (never visited online), shows a plugin-owned &quot;not available offline yet&quot; empty state — not a blank page.</li></ol><p>The runtime enforces the shell rule at build/validation time as far as it can (see Security requirements); the rest is plugin responsibility, documented in <code>docs/plugin-development.md</code>, exactly as <code>publicRoutes</code> makes token validation the plugin&#39;s job.</p><p><strong>The shared platform shell had to become neutral too (Changelog 0.4).</strong> A plugin&#39;s own offline-route page can be perfectly neutral and still ship inside a document that isn&#39;t: every default-shell plugin (Shopper and Wallet included) composes under <code>runtime/app/(platform)/layout.tsx</code>, one shared Server Component every route renders through — and that layout reads the authenticated user&#39;s name, email, and avatar image straight into its SSR output, plus a personalized, access-policy-filtered, user-reordered sidebar plugin list. A full-document navigation to an offline route returns <em>one</em> HTML response containing both the plugin&#39;s neutral page and this decidedly non-neutral shell, and that whole response is what the service worker precaches. Found after the fact, not by design — the neutrality rule above was written (and reviewed, and CI-scanned by <code>offline-route-neutrality.test.ts</code>) with only a plugin&#39;s own route files in view, never the shell wrapping them.</p><p>Fixed by flagging the request in <code>middleware.ts</code> — <code>getOfflineRoutePrefixes()</code> against the request path sets <code>x-sovereign-offline-route: &#39;1&#39;</code> — which <code>PlatformLayout</code> reads to render a single, fixed, identical-for-every-user shell for these routes: no name/email/avatar, no admin Console icon, no personalized/restricted/reordered plugin list (only the Launcher icon, which was already unfiltered by any of that), and no user id threaded to <code>ClientShell</code> (a value frozen into a precached document reflects whoever&#39;s visit populated the cache, not whoever it&#39;s later replayed to — passing it through at all would make <code>ClientShell</code>&#39;s own mismatch-purge check, added in Changelog 0.3, compare against a stale identity instead of skipping safely). Every other route is completely unaffected — this only branches for paths under a manifest-declared <code>offline.routes</code> prefix.</p><h3 id="_3-client-data-surface-—-sdk-offline" tabindex="-1">3. Client data surface — <code>sdk.offline.*</code> <a class="header-anchor" href="#_3-client-data-surface-—-sdk-offline" aria-label="Permalink to &quot;3. Client data surface — \`sdk.offline.*\`&quot;">​</a></h3><p>Add a <strong>client-side</strong> SDK surface (<code>packages/sdk/src/offline.ts</code>) backed by IndexedDB, exposed via the dedicated <code>@sovereignfs/sdk/offline</code> subpath — not the main barrel, for the same reason as the existing <code>e2ee-*</code> modules: the barrel also reaches server-only code (e.g. <code>activity.ts</code>&#39;s <code>next/headers</code> import), and Next&#39;s client/server boundary check flags the whole reachable module graph, so a <code>&#39;use client&#39;</code> component importing from the barrel would fail to build.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Client component on an offline-enabled route.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { offline } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sovereignfs/sdk/offline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PLUGIN_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;fs.sovereign.wallet&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// your own manifest id, known statically</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Read cached data (works with no network).</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cards</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> offline.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Card</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]&gt;(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PLUGIN_ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;cards&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// After a successful online fetch, mirror it for offline use.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> offline.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PLUGIN_ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;cards&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, cards);</span></span></code></pre></div><p>Shape (v1, read/write to the <em>local</em> store only — no server sync):</p><ul><li><code>offline.get&lt;T&gt;(pluginId, key): Promise&lt;T | null&gt;</code> — read this plugin&#39;s cached value.</li><li><code>offline.set&lt;T&gt;(pluginId, key, value): Promise&lt;void&gt;</code> — write/replace it.</li><li><code>offline.remove(pluginId, key): Promise&lt;void&gt;</code> / <code>offline.keys(pluginId): Promise&lt;string[]&gt;</code>.</li><li><code>offline.clear(pluginId): Promise&lt;void&gt;</code> — wipe one plugin&#39;s cache.</li><li><code>offline.clearAll(): Promise&lt;void&gt;</code> — wipe every plugin&#39;s cache (used by the runtime&#39;s logout purge, §5).</li></ul><p><strong>Isolation is scoped by plugin id only — deliberately not by user id</strong>, which revises this RFC&#39;s original design (see Changelog 0.2). The original draft called for <code>(pluginId, userId)</code> keying via a client-readable session hint (Open question 2, below). Implementation surfaced a sharper argument against it: an offline route&#39;s own SSR output must never carry per-user data in the first place (§2) — that&#39;s what makes it safe to precache and replay on a shared device. But that means there is no safe place left to <em>read</em> a client-side user id from either: embedding it anywhere in the offline route&#39;s own document reintroduces exactly the leak §2 exists to prevent, and any mechanism that fetches it out-of-band (a session-hint endpoint, a global inline script on the shared layout) adds real complexity for a signal this design doesn&#39;t actually need. Plugin-only scoping plus an unconditional wipe on every logout/user-switch (§5) achieves the identical safety property — nothing cached ever survives past the session that wrote it — without inventing a new way to move user identity into client JS. Open question 2 is resolved by this: no <code>sdk.session</code> client getter, no session-hint plumbing.</p><p>This is a <strong>published-package (<code>@sovereignfs/sdk</code>) addition ⇒ minor bump</strong>, and per NFR-04 it must be additive (it is). It must be documented in <code>docs/plugin-development.md</code> for the parity test.</p><p>Rationale for putting <strong>data in IndexedDB rather than SW-caching API responses:</strong> SW response caches are origin-global and would re-introduce the exact per-user leak the <code>pages</code> rule guards against. A client-owned IndexedDB store that logout wipes keeps user data isolation explicit and auditable. The SW therefore caches only <strong>user-neutral assets</strong> (§4), never per-user API responses.</p><h3 id="_4-service-worker-precaching-—-scoped-allowlist" tabindex="-1">4. Service-worker precaching — scoped allowlist <a class="header-anchor" href="#_4-service-worker-precaching-—-scoped-allowlist" aria-label="Permalink to &quot;4. Service-worker precaching — scoped allowlist&quot;">​</a></h3><p><code>runtime/src/registry.ts</code>&#39;s <code>getOfflineRoutePrefixes()</code> resolves every manifest-declared offline route to its full path (<code>&lt;routePrefix&gt;&lt;offline.routes[].prefix&gt;</code>) from the generated plugin registry — <code>runtime/generated/registry.ts</code> already serializes the entire manifest (including <code>offline</code>) via <code>JSON.stringify</code>, so no change to the generate script itself was needed; the registry was already the single source of truth. Fed into <code>next.config.ts</code>&#39;s Workbox <code>runtimeCaching</code>:</p><ul><li>A dedicated <code>offline-shells</code> matcher, listed <strong>before</strong> the general <code>pages</code> matcher (Workbox picks the first match), catches same-origin requests under any declared offline prefix and caches them <code>StaleWhileRevalidate</code> — safe here, and only here, because these documents are declared user-neutral shells (§2): the cached response serves instantly (works with no network), while a background fetch refreshes the cache for next time. (Originally <code>CacheFirst</code>; revised during hardening — see Changelog 0.3. <code>CacheFirst</code> never revalidates against network while an entry is within <code>maxAgeSeconds</code>, so a deployed shell change, including a content-hashed JS chunk the stale HTML still references, would stay invisible to a returning <em>online</em> user for up to 30 days.)</li><li><strong>Everything else stays <code>NetworkFirst</code> → <code>/offline</code>.</strong> The general <code>pages</code> matcher gained one exclusion (skip paths already claimed by the offline matcher) but is otherwise unchanged.</li><li><strong>No per-user API GET is added to the SW runtime cache.</strong> Data comes from IndexedDB (§3), not a replayed API response.</li><li>With zero plugins currently declaring <code>offline</code>, this is a verified no-op: a production build was run to confirm <code>next.config.ts</code> loads correctly and the generated service worker includes the <code>offline-shells</code> cache entry (currently matching nothing).</li></ul><p>This does <strong>not</strong> precache the route&#39;s HTML at build time the way static assets are precached — dynamic SSR documents aren&#39;t statically known to Workbox&#39;s precache manifest. In practice this means the <em>first</em> visit to an offline route must happen online (populating the runtime cache); every visit after that — online or offline — is served <code>CacheFirst</code>. The RFC&#39;s &quot;precache&quot; language is satisfied by this runtime-populated cache, not a build-time one; this is a scope narrowing from the original draft, not a behavior gap (a route can&#39;t render meaningful cached data before it&#39;s ever been fetched once anyway — see the UI flows below).</p><h3 id="_5-logout-purge-—-the-shared-device-safeguard" tabindex="-1">5. Logout purge — the shared-device safeguard <a class="header-anchor" href="#_5-logout-purge-—-the-shared-device-safeguard" aria-label="Permalink to &quot;5. Logout purge — the shared-device safeguard&quot;">​</a></h3><p><code>runtime/app/(platform)/_components/AccountMenu.tsx</code>&#39;s sign-out form (the sole UI entry point to <code>/api/account/logout</code> — confirmed by a repo-wide search) now runs an <code>onSubmit</code> handler that awaits <code>offline.clearAll()</code> before letting the native submission proceed (<code>form.submit()</code> runs in a <code>finally</code>, so sign-out still completes normally if IndexedDB is unavailable or clearing errors). This wipes every plugin&#39;s offline cache, not just the outgoing user&#39;s — the mechanism that makes §3&#39;s plugin-only (not per-user) key scoping safe on a shared device: nothing cached survives past the session that wrote it, so the next login starts from an empty cache regardless of who logs in.</p><p>This mirrors the existing discipline of clearing both <code>session_data</code> cookie variants on profile self-mutation (CLAUDE.md) — offline data gets the same &quot;nothing survives a session boundary&quot; treatment. Without it, a shared device is exactly the leak the per-user-SSR rule (and, now, plugin-only cache scoping) was written to prevent.</p><p><code>runtime/src/complete-sign-in.ts</code>&#39;s <code>completeSignIn()</code> extends this to the <strong>sign-in</strong> side too — called by every successful auth path (password, passkey, 2FA challenge, registration), purging unconditionally before navigating into the app. Sign-out-only purging couldn&#39;t catch a prior session that ended by expiry or tab-close rather than an explicit sign-out; purging again on the <em>next</em> login closes that gap regardless of how the previous session ended (see Changelog 0.3).</p><p>Two further gaps in the purge mechanism itself, found during hardening and fixed on this branch (Changelog 0.3): the write-epoch race guard between a <code>set()</code> and a <code>clearAll()</code> was originally a same-tab-only in-memory variable, invisible to a second open tab — now propagated cross-tab via <code>BroadcastChannel</code>. And <code>clearAll()</code> originally cleared only the IndexedDB store, leaving the service worker&#39;s precached <code>offline-shells</code> documents (a separate Cache Storage bucket) untouched — now purged too, as defense in depth against §2&#39;s user-neutral-shell rule not holding for some plugin.</p><h3 id="known-limitation-—-the-purge-timing-gap-cannot-be-fully-closed" tabindex="-1">Known limitation — the purge-timing gap cannot be fully closed <a class="header-anchor" href="#known-limitation-—-the-purge-timing-gap-cannot-be-fully-closed" aria-label="Permalink to &quot;Known limitation — the purge-timing gap cannot be fully closed&quot;">​</a></h3><p>The purge mechanism above (<code>clearAll()</code> on both sign-out and sign-in, plus a mount-time user-id mismatch check in <code>ClientShell.tsx</code> that re-purges if a persisted session belongs to a different user than this device last purged for) narrows, but cannot fully close, the window between one session ending and the next login.</p><p>Concretely: if a tab is closed (or the session simply expires) without an explicit sign-out, and the device goes offline <em>before</em> anyone opens the app again while online, the previous user&#39;s cached data sits in IndexedDB with nothing able to purge it — because the offline routes&#39; entire premise is that they render with <strong>zero network</strong>, there is no point in that window where any code can run a session check at all. This is not a gap in implementation; it is the structural cost of &quot;works with no connection.&quot; Fully closing it would require per-user, device-bound encryption of the offline cache (so a different user&#39;s session literally cannot decrypt leftover ciphertext) — a materially larger feature than this RFC&#39;s scope, and a candidate for a future RFC alongside offline writes. Accepted as a residual risk for v1: the exposure is bounded to a shared, unattended, never-reconnected device between one user&#39;s session ending and the next sign-in, and only reaches data the offline route itself already puts in a user-neutral, precache-safe shell.</p><h3 id="_6-launcher-—-the-pwa-cold-start-entry-point-offline-root-changelog-0-5" tabindex="-1">6. Launcher — the PWA cold-start entry point (<code>offline.root</code>, Changelog 0.5) <a class="header-anchor" href="#_6-launcher-—-the-pwa-cold-start-entry-point-offline-root-changelog-0-5" aria-label="Permalink to &quot;6. Launcher — the PWA cold-start entry point (\`offline.root\`, Changelog 0.5)&quot;">​</a></h3><p>The PWA&#39;s <code>start_url</code> is <code>/</code>, not any plugin&#39;s own <code>routePrefix</code> — a fully cold relaunch (process killed, zero connectivity for the whole session) hits <code>/</code> before the user ever sees anything to navigate from. Until this addition, <code>/</code> was ordinary per-user SSR (middleware rewrites it server-side to whichever plugin is configured as the platform root — Launcher by default, RFC PLT-14), so a cold offline relaunch dead-ended at the generic <code>/offline</code> fallback with no way to reach an already-cached offline route like Shopper&#39;s list.</p><p><strong>New manifest field <code>offline.root: boolean</code></strong> — orthogonal to <code>routes[]</code>, which keeps its existing restriction that a sub-path entry can&#39;t be <code>/</code> (that restriction forces explicit enumeration of sub-paths; <code>root</code> is a separate, explicit opt-in for a single-page plugin&#39;s own top-level route, generalizing beyond Launcher to any plugin whose whole UI lives at its bare <code>routePrefix</code>). <code>getOfflineRoutePrefixes()</code> includes the bare <code>routePrefix</code> for any manifest declaring it.</p><p><strong>Launcher (<code>plugins/launcher/</code>) adopted it</strong>: <code>manifest.json</code> now declares <code>offline: { root: true }</code>; <code>app/page.tsx</code> was rewritten to the same neutral-shell + client-hydration pattern as Shopper/Wallet — all data (installed-plugin tiles, the self-service directory) now fetched client-side in a new <code>LauncherOfflineView.tsx</code> via <code>/api/plugins</code> (already server-filtered per user/role) and a new <code>GET /api/plugins/directory</code> route (added so the self-service directory section — previously only computed server-side via <code>getSelfServiceDirectory</code> — survives the move to client fetching, preserving full feature parity rather than dropping it).</p><p><strong><code>/</code> itself needed no new service-worker cache entry.</strong> <code>@ducanh2912/next-pwa</code>&#39;s <code>dynamicStartUrl</code>/<code>cacheStartUrl</code> options (both default <code>true</code>) already <code>unshift</code> an automatic <code>NetworkFirst</code> route for <code>/</code> (&quot;start-url&quot; cache) ahead of <code>next.config.ts</code>&#39;s entire <code>runtimeCaching</code> array — a custom match for <code>/</code> in the <code>offline-shells</code> matcher would never actually be reached by Workbox regardless of array order, so <code>underOfflineRoutePrefix()</code> deliberately has no special case for it. That built-in route already does what&#39;s needed (serve network, fall back to cache when offline); what was missing was making the <em>content</em> it caches for <code>/</code> safe. <code>middleware.ts</code> now flags <code>/</code> with <code>x-sovereign-offline-route</code> unconditionally, alongside the manifest-driven list, so <code>PlatformLayout</code> (§2&#39;s &quot;shared platform shell&quot; fix) renders the same neutral shell for it as for any other offline route — and since Launcher&#39;s own page is now neutral too, the response next-pwa&#39;s built-in caching captures for <code>/</code> is neutral by construction, <strong>for the default configuration</strong>. An admin who configures a <em>different</em>, non-offline-aware plugin as the platform root could still leave a stale/per-user document cached at <code>/</code> from whenever it was last visited — a narrow, accepted edge case (non-default config + that plugin not adopting offline), not solved here, same category of trade-off as the Known Limitation above.</p><h3 id="docker-config-impact" tabindex="-1">Docker / config impact <a class="header-anchor" href="#docker-config-impact" aria-label="Permalink to &quot;Docker / config impact&quot;">​</a></h3><p>None expected: no new env var, port, on-disk path, or native dep. The SW is still generated into <code>public/</code> at build time. (Flagging per the CLAUDE.md rule — re-check during implementation if the compose step gains a generated artifact that needs a Dockerfile <code>COPY</code>.)</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><p><strong>First online visit to an offline route</strong> (e.g. Wallet → Cards):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>online → route renders shell → client fetches cards → renders + offline.set(&#39;cards&#39;)</span></span></code></pre></div><p><strong>Later, offline:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>offline → app opens → route shell renders from precache</span></span>
<span class="line"><span>        → client offline.get(&#39;cards&#39;) → renders cached cards</span></span>
<span class="line"><span>        → OfflineBanner shows &quot;No internet connection&quot;</span></span></code></pre></div><p><strong>Offline, navigating to a non-offline route:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>offline → NetworkFirst times out → /offline &quot;no internet connection&quot; fallback</span></span></code></pre></div><p><strong>Offline route never visited online (empty store):</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>offline → shell renders → offline.get returns null</span></span>
<span class="line"><span>        → plugin&#39;s &quot;Not available offline yet — open once online&quot; empty state</span></span></code></pre></div><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><p><strong>Cache the per-user SSR HTML and partition the SW cache by user.</strong> Rejected. It fights the hard per-user-SSR rule directly, and correct cache partitioning + eviction on logout across the SW is far more error-prone than an IndexedDB store the client owns and unconditionally clears on every logout. One mistake leaks another user&#39;s shell.</p><p><strong>SW-cache the plugins&#39; read API GET responses.</strong> Rejected for the same reason — SW response caches are origin-global; a cached <code>/api/wallet/cards</code> response would be replayable for the wrong user. Keeping data in a client-owned IndexedDB store that&#39;s wiped on every logout makes isolation explicit and auditable.</p><p><strong>Key <code>sdk.offline</code> by <code>(pluginId, userId)</code>, resolving <code>userId</code> via a client-readable session hint.</strong> This was the original design (see Changelog 0.1). Rejected during implementation: there is no place to source a client-readable <code>userId</code> that doesn&#39;t either (a) get embedded in an offline route&#39;s own precached, replayable document — the exact leak §2 exists to prevent — or (b) require new out-of-band plumbing (a session-hint endpoint, a global inline script) for a signal the design turns out not to need. Plugin- only scoping plus an unconditional <code>clearAll()</code> on every logout achieves the same safety property with less mechanism: nothing survives past the session that wrote it, so there&#39;s nothing to key by user identity in the first place.</p><p><strong>A generic platform &quot;offline everything&quot; toggle.</strong> Rejected. Plugins have very different data models and freshness needs; a blanket cache bloats storage and maximizes staleness. Per-route opt-in keeps the surface small and reviewable (same reasoning as RFC 0042 for public routes).</p><p><strong>Ship read + write (outbox/sync) in v1.</strong> Deferred. Queued mutations need a sync engine, conflict resolution, and background sync — a much larger surface. Read-only delivers the headline value (open the app, see your stuff) and is independently shippable. Writes get their own RFC building on this one.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li>Should opting into <code>offline</code> require an explicit <code>offline</code> <strong>permission</strong> in <code>permissions[]</code> (for install-review visibility), or is the manifest field alone enough? <code>publicRoutes</code> chose field-only; offline exposes cached data on-device, which may warrant the extra signal. <strong>Still open</strong> — implementation shipped field-only, matching <code>publicRoutes</code>; revisit if reviewers want the extra signal.</li><li><s>How is the client-readable <code>userId</code> for store keying exposed without leaking anything sensitive?</s> <strong>Resolved</strong> — it isn&#39;t. <code>sdk.offline</code> scopes by plugin id only; safety comes from an unconditional <code>clearAll()</code> on every logout (§3, §5). No <code>sdk.session</code> client getter was added.</li><li>Should the runtime <strong>enforce</strong> the user-neutral-shell rule (e.g. a dev-time check that an offline route&#39;s SSR output carries no session headers / no per-user markers), or is it documentation + review only? <strong>Resolved</strong> — <code>runtime/src/__tests__/offline-route-neutrality.test.ts</code> (Changelog 0.3) statically scans every manifest-declared offline route&#39;s server-component source for identity-reading APIs (<code>headers()</code>, <code>cookies()</code>, a session helper, the <code>x-sovereign-user-id</code> header) and fails CI if any are found. A source scan, not a rendered-output diff — it can&#39;t catch every possible per-user leak, only the direct ones, so documentation + review remain the primary line of defense; this is a backstop, not a replacement.</li><li>Eviction policy and quota: per-plugin IndexedDB size caps? LRU across offline plugins when the browser signals storage pressure? <strong>Partially resolved</strong> — <code>sdk.offline.set()</code> now enforces a 5 MB soft per-entry cap and throws a typed <code>OfflineQuotaExceededError</code> on both that cap and a real IndexedDB <code>QuotaExceededError</code> (Changelog 0.3), so a caller gets an actionable failure instead of an opaque <code>DOMException</code>. No LRU/automatic eviction — that needs a metadata envelope around stored values and a changed <code>get()</code> contract, deferred alongside offline writes/outbox.</li><li>Should Console show which plugins declare offline routes and let an operator <strong>disable</strong> offline for a plugin instance-wide (parallel to plugin enable/disable)? <strong>Still open</strong> — not built (adoption path step 6, below).</li><li>Does the paywall gate interact with offline routes — can a cached offline route render after an entitlement lapses? <strong>Still open</strong> — not addressed; no installed plugin combines <code>monetization</code> and <code>offline</code> yet, so this hasn&#39;t been forced.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Platform plumbing (steps 1–4) shipped directly on <code>feat/offline-capable-plugins</code>, without a roadmap slot — the developer chose to build ahead of scheduling. Status per step:</p><ol><li><strong>Manifest + validation.</strong> ✅ <code>offline</code> added to <code>packages/manifest</code> (reused the <code>publicRoutes</code> prefix refinement); documented in <code>docs/plugin-development.md</code>; parity test passes; <code>@sovereignfs/manifest</code> minor-bumped.</li><li><strong>Client SDK surface.</strong> ✅ <code>packages/sdk/src/offline.ts</code> (<code>get</code>/<code>set</code>/<code>remove</code>/<code>keys</code>/<code>clear</code>/<code>clearAll</code>), plugin-id-scoped IndexedDB store (not per-user — see §3&#39;s revised design), exposed via the <code>@sovereignfs/sdk/offline</code> subpath; documented; <code>@sovereignfs/sdk</code> minor-bumped.</li><li><strong>SW generation + precache.</strong> ✅ <code>runtime/src/registry.ts</code>&#39;s <code>getOfflineRoutePrefixes()</code> feeds a dedicated <code>offline-shells</code> <code>CacheFirst</code> matcher in <code>next.config.ts</code>, ahead of the general <code>pages</code> matcher; verified with a full production build. Runtime-populated (first online visit populates the cache), not build-time precached — see §4&#39;s narrowed scope.</li><li><strong>Logout purge.</strong> ✅ <code>AccountMenu.tsx</code>&#39;s sign-out form awaits <code>offline.clearAll()</code> before submitting.</li><li><strong>First adopters.</strong> ✅ Shopper (<code>/lists/[listId]</code>) and Wallet (<code>/cards</code>, <code>/cards/[cardId]</code>) have both adopted the app-shell + <code>sdk.offline</code> pattern in their own repositories (community plugins, cloned as gitignored <code>.local</code> directories in this workspace) — found during Changelog 0.4&#39;s investigation. Tasks has not yet.</li><li><strong>Console surfacing.</strong> ⬜ Not started (optional, per open question 5).</li><li><strong>Launcher — PWA cold-start entry point.</strong> ✅ <code>offline.root: true</code> (Changelog 0.5) — see §6.</li></ol><p>Offline <strong>writes</strong> (outbox, background sync, conflict resolution) are a separate future RFC that builds on the <code>sdk.offline</code> store defined here — see RFC 0078, which also removes this RFC&#39;s <code>offline.routes[]</code> array in favor of a single plugin-level <code>offline</code> boolean, generalizing the <code>offline.root: true</code> shape Launcher already uses (§6) into the only model.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr><tr><td>0.2</td><td>July 2026</td><td>Steps 1–4 implemented on <code>feat/offline-capable-plugins</code>. Revised <code>sdk.offline</code> from <code>(pluginId, userId)</code> keying to plugin-id-only keying with an unconditional <code>clearAll()</code> on logout (open question 2 resolved); narrowed SW precaching from build-time to runtime-populated <code>CacheFirst</code> (§4); added <code>@sovereignfs/sdk/offline</code> as the import subpath (matching the <code>e2ee-*</code> pattern, not the main barrel as originally sketched).</td></tr><tr><td>0.3</td><td>July 2026</td><td>Three hardening passes on the same branch, ahead of merge. <strong>Purge coverage:</strong> added <code>completeSignIn()</code> to purge on every sign-in, not just sign-out (§5); closed a cross-tab write/clear race with <code>BroadcastChannel</code>; <code>clearAll()</code> now also purges the SW&#39;s <code>offline-shells</code> Cache Storage entries; added a <code>ClientShell</code> mount-time check that re-purges on a user-id mismatch. Documented the remaining structural gap as a known limitation (new §5 subsection) rather than a bug — a fully offline, never-reconnected device between one session ending and the next login cannot be purged by any code path, by construction. <strong>SW caching:</strong> <code>offline-shells</code> switched <code>CacheFirst</code> → <code>StaleWhileRevalidate</code> (§4) so a deployed shell change isn&#39;t invisible to an online user for up to 30 days. <strong>Enforcement:</strong> open question 3 resolved — <code>offline-route-neutrality.test.ts</code> statically scans declared offline routes&#39; SSR source for identity-reading APIs, CI-enforced. <strong>Quota:</strong> open question 4 partially resolved — <code>offline.set()</code> gained a 5 MB soft cap and a typed <code>OfflineQuotaExceededError</code>; LRU eviction remains open, deferred with offline writes.</td></tr><tr><td>0.4</td><td>July 2026</td><td>Found and fixed a leak the §2 neutrality rule (and its CI scanner) never covered: <code>runtime/app/(platform)/layout.tsx</code>, the shared Server Component every default-shell plugin route composes under — including Shopper&#39;s and Wallet&#39;s now-real offline routes — embeds the authenticated user&#39;s name/email/avatar and a personalized, access-policy-filtered, reordered sidebar plugin list directly into SSR output. A full-document response for an offline route bundles this non-neutral shell with the plugin&#39;s own (correctly neutral) page into one precached document. Fixed with a <code>middleware.ts</code> flag (<code>x-sovereign-offline-route</code>, set from <code>getOfflineRoutePrefixes()</code>) that <code>PlatformLayout</code> reads to render a fixed, identical-for-everyone shell for these routes only — see §2&#39;s &quot;shared platform shell&quot; subsection. Every non-offline route is unaffected.</td></tr><tr><td>0.5</td><td>July 2026</td><td>Made the Launcher itself offline-capable, so a cold PWA relaunch (<code>start_url: &quot;/&quot;</code>, zero connectivity for the whole session) lands somewhere useful instead of the generic <code>/offline</code> fallback. Added manifest field <code>offline.root: boolean</code> (packages/manifest, orthogonal to <code>routes[]</code>) — Launcher declares it, and its <code>page.tsx</code> was rewritten to the neutral-shell + client-hydration pattern (new <code>LauncherOfflineView.tsx</code>, new <code>GET /api/plugins/directory</code> route so the self-service directory section keeps working). <code>/</code> needed no new service-worker entry — <code>@ducanh2912/next-pwa</code>&#39;s built-in <code>dynamicStartUrl</code> <code>NetworkFirst</code> route for <code>/</code> already wins ahead of any custom match — but <code>middleware.ts</code> now flags <code>/</code> with <code>x-sovereign-offline-route</code> unconditionally so the <em>content</em> cached there is neutral (§6, §2). <code>offline-route-neutrality.test.ts</code> extended to scan <code>offline.root</code> declarations (a plugin&#39;s top-level route files, not just <code>routes[]</code> sub-paths). Accepted limitation: only guaranteed neutral for the <em>default</em> root-plugin configuration (Launcher); see §6.</td></tr></tbody></table>`,77)])])}const g=t(i,[["render",a]]);export{u as __pageData,g as default};
