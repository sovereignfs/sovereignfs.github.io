import{_ as a,o as t,c as i,a2 as s}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"RFC 0027 — White-labeling (tenant branding)","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0027-white-labeling.md","filePath":"rfcs/0027-white-labeling.md"}'),n={name:"rfcs/0027-white-labeling.md"};function o(d,e,r,l,c,h){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="rfc-0027-—-white-labeling-tenant-branding" tabindex="-1">RFC 0027 — White-labeling (tenant branding) <a class="header-anchor" href="#rfc-0027-—-white-labeling-tenant-branding" aria-label="Permalink to &quot;RFC 0027 — White-labeling (tenant branding)&quot;">​</a></h1><p><strong>Status:</strong> Accepted<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> Whole platform — <code>packages/ui</code>, <code>packages/db</code>, <code>runtime</code>, <code>apps/auth</code>, <code>packages/mailer</code>, <code>bin/sv</code>, <code>.env.example</code>, docs, Docker/compose, SRS<br><strong>Incorporated into plan:</strong> Yes — phased pre-v1. SRS §3.18 documents the architecture. Task 1.0.03 (Phase 1: DB + shell + Console form + SDK) is ✅ shipped; epic tasks 9.9 (Phase 2: email + auth login) and 9.10 (Phase 3: dynamic PWA manifest + favicon) are in the pre-release schedule. Epic task 9.8 (RFC 0032) renames <code>BRAND_*</code> → <code>INSTANCE_*</code> ahead of Phase 2.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Let any Sovereign operator — individual, organisation, or company — replace Sovereign&#39;s visual identity with their own brand. An operator deploys Sovereign as the core and customises the logo, app name, favicon, colours, and email sender identity per tenant, with env-var defaults for the single-tenant case. The same infrastructure enables the dual-licensing model (AGPL + commercial) where a closed-source derivative ships under an operator&#39;s own brand.</p><p>Instance identity values are injected as CSS custom properties at <code>:root</code> — the same mechanism as dark mode and tenant theming — so every component inherits the configured identity without code changes. A new <code>--sv-instance-*</code> token namespace carries non-colour identity (logo URLs, favicon URL). Colour overrides go into the existing <code>--sv-color-*</code> semantic layer. The instance name is delivered as a React prop by <code>InstanceProvider</code> and may be mirrored as a CSS custom property for decorative uses only.</p><blockquote><p><strong>RFC 0032 note:</strong> The identifiers in this RFC were originally defined with a <code>brand/BRAND_</code> prefix (Phase 1 shipped). RFC 0032 (epic task 9.8) renames them to <code>instance/INSTANCE_</code> before Phase 2. This document reflects the post-rename naming throughout.</p></blockquote><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign is positioned as a &quot;modular, self-hostable workspace runtime&quot; that anyone can own. Today, every instance looks like Sovereign — grey-scale, the Sovereign logo in the shell header, the same name on the login page. That works for the developer running their own instance, but three groups need to make it their own:</p><ul><li><strong>Individuals</strong> who want their instance to feel like <em>their</em> workspace, not someone else&#39;s product.</li><li><strong>Organisations</strong> deploying Sovereign internally — they want their own logo, colours, and app name so their team sees a familiar corporate tool, not an open-source project.</li><li><strong>Companies</strong> embedding Sovereign as the core of a commercial product (the dual-licensing model). They need full visual OEM — their logo, their name, their URL — so their customers never see &quot;Sovereign.&quot;</li></ul><p>The existing infrastructure — <code>tenant_id</code> on every user-scoped table, the <code>tenants</code> table with tenant name, the <code>platform_settings</code> key-value store, and the design-system token architecture — already points toward this capability. What is missing is a structured model for per-tenant brand values, a dedicated token namespace, and the injection mechanism to deliver them across the runtime, auth server, email, and PWA.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><h3 id="platform-db-packages-db" tabindex="-1">Platform DB (<code>packages/db</code>) <a class="header-anchor" href="#platform-db-packages-db" aria-label="Permalink to &quot;Platform DB (\`packages/db\`)&quot;">​</a></h3><ul><li><code>tenants</code> table with <code>tenant_id</code> PK and <code>name</code> column (<code>docs/sovereign-proposal-plan-srs.md</code> §3.7, <code>packages/db/src/schema/</code>)</li><li><code>platform_settings</code> key-value store scoped by <code>tenant_id</code> (<code>PLT-15</code>, <code>packages/db/src/schema/</code>)</li><li><code>sdk.platform.getConfig()</code> returns <code>{ tenantName, inviteOnly, version }</code> (<code>packages/sdk/src/platform.ts</code>, <code>plugins/console/app/settings/</code>)</li><li><code>DEFAULT_TENANT_ID = &#39;default&#39;</code> for single-tenant v1 (<code>packages/db/src/constants.ts</code>)</li></ul><h3 id="design-system-packages-ui" tabindex="-1">Design system (<code>packages/ui</code>) <a class="header-anchor" href="#design-system-packages-ui" aria-label="Permalink to &quot;Design system (\`packages/ui\`)&quot;">​</a></h3><ul><li>Two-tier <code>--sv-*</code> token architecture: primitives (fixed) + semantic (theme surface) (<code>docs/design-system.md</code> §Token architecture)</li><li>Dark mode and tenant theming already swap semantic colour tokens at <code>:root</code> / <code>[data-theme]</code> (<code>docs/design-system.md:120-137</code>, <code>packages/ui/src/tokens/semantic.css</code>)</li><li>Components reference only semantic tokens — a theme is purely CSS variable values, no component changes</li><li><code>@sovereignfs/ui</code> v0.4.0 — published to npm, versioned with semver discipline</li></ul><h3 id="runtime-runtime" tabindex="-1">Runtime (<code>runtime/</code>) <a class="header-anchor" href="#runtime-runtime" aria-label="Permalink to &quot;Runtime (\`runtime/\`)&quot;">​</a></h3><ul><li>Shell sidebar has a branding header (currently hardcoded &quot;Sovereign&quot; + logo) (<code>runtime/app/(platform)/</code>, <code>plugins/launcher/</code>)</li><li>Middleware injects <code>x-sovereign-user-*</code> headers from verified session (<code>runtime/middleware.ts</code>)</li><li><code>(platform)/layout.tsx</code> renders the shared chrome</li><li>PWA manifest at <code>runtime/public/manifest.json</code> is a static committed file (<code>Task 0.5.1</code>)</li><li>CSP nonce + hash for inline scripts (<code>runtime/src/security.ts</code>, <code>runtime/src/theme-script.ts</code>)</li><li>Current CSP: <code>img-src &#39;self&#39; data: blob:</code> — external origin images are blocked by default</li></ul><h3 id="auth-server-apps-auth" tabindex="-1">Auth server (<code>apps/auth</code>) <a class="header-anchor" href="#auth-server-apps-auth" aria-label="Permalink to &quot;Auth server (\`apps/auth\`)&quot;">​</a></h3><ul><li>Standalone Next.js app with its own DB and login UI (<code>apps/auth/</code>)</li><li>Login page currently hardcodes Sovereign branding</li><li>Server-to-server calls use <code>Origin</code> header matching <code>SOVEREIGN_AUTH_URL</code> (<code>apps/auth/src/auth.ts</code>)</li><li>Email/password auth, invite-only registration</li></ul><h3 id="email-packages-mailer" tabindex="-1">Email (<code>packages/mailer</code>) <a class="header-anchor" href="#email-packages-mailer" aria-label="Permalink to &quot;Email (\`packages/mailer\`)&quot;">​</a></h3><ul><li>Thin SMTP wrapper around nodemailer (<code>packages/mailer/</code>)</li><li><code>SMTP_FROM</code> env var for the sender address (<code>packages/mailer/src/index.ts</code>)</li><li>HTML templates use a hardcoded Sovereign logo and name</li><li>No-op when <code>SMTP_HOST</code> is unset</li></ul><h3 id="existing-tenant-theming-notes" tabindex="-1">Existing tenant-theming notes <a class="header-anchor" href="#existing-tenant-theming-notes" aria-label="Permalink to &quot;Existing tenant-theming notes&quot;">​</a></h3><p>The SRS and decision log already acknowledge that tenant theming swaps semantic tokens at <code>:root</code> (<code>docs/design-system.md:133-137</code>), and the Console has a tenant-name field (<code>CON-08</code>). The concept is validated; this RFC formalises the extension into a full white-label model.</p><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="config-layering" tabindex="-1">Config layering <a class="header-anchor" href="#config-layering" aria-label="Permalink to &quot;Config layering&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>env var default → instance_config (DB table per tenant)</span></span></code></pre></div><p>Env vars define the baseline at deploy time; the <code>instance_config</code> table stores per-tenant overrides. The runtime reads the most specific value. Single-tenant operators who never touch the Console get a pure env-var experience; multi-tenant operators configure each tenant&#39;s identity in the Console.</p><p>The existing <code>platform_settings</code> key-value store is <strong>not</strong> used as a middle layer for instance identity. It is untyped and unstructured; identity values benefit from explicit columns (clear schema, easier migration, typed helpers). The dedicated <code>instance_config</code> table gives the same two-layer pattern (env defaults + DB overrides) with structured access.</p><h3 id="new-env-vars" tabindex="-1">New env vars <a class="header-anchor" href="#new-env-vars" aria-label="Permalink to &quot;New env vars&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>INSTANCE_NAME</code></td><td><code>&quot;Sovereign&quot;</code></td><td>The display name of the instance/tenant</td></tr><tr><td><code>INSTANCE_LOGO</code></td><td><em>(empty)</em></td><td>URL or path to the instance logo (light theme)</td></tr><tr><td><code>INSTANCE_LOGO_DARK</code></td><td><em>(empty)</em></td><td>URL or path to the instance logo (dark theme)</td></tr><tr><td><code>INSTANCE_FAVICON</code></td><td><em>(empty)</em></td><td>URL or path to the favicon</td></tr><tr><td><code>INSTANCE_PRIMARY_COLOR</code></td><td><em>(empty)</em></td><td>Hex colour overriding <code>--sv-color-accent</code></td></tr><tr><td><code>INSTANCE_EMAIL_FROM_NAME</code></td><td><em>(empty)</em></td><td>Sender name for outbound email</td></tr><tr><td><code>INSTANCE_EMAIL_LOGO</code></td><td><em>(empty)</em></td><td>Logo URL to include in HTML email templates</td></tr></tbody></table><p>All are optional. When unset, Sovereign defaults apply (the current behaviour). <code>INSTANCE_PRIMARY_COLOR</code> sets <code>--sv-color-accent</code> so the accent token reflects the instance primary colour; this is the minimum viable colour override — full palette customisation is a future extension.</p><h3 id="instance-config-table" tabindex="-1"><code>instance_config</code> table <a class="header-anchor" href="#instance-config-table" aria-label="Permalink to &quot;\`instance_config\` table&quot;">​</a></h3><p>A new table in <code>packages/db</code>. The DDL uses the dialect-aware bootstrap helper (same pattern as <code>platform_settings</code> and <code>plugin_status</code>), not raw <code>strftime</code> which is SQLite-only:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/db/src/schema/sqlite/index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> instanceConfig</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sqliteTable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;instance_config&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tenantId: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tenant_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primaryKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  instanceName: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;instance_name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  instanceLogo: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;instance_logo&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  instanceLogoDark: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;instance_logo_dark&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  instanceFavicon: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;instance_favicon&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  instancePrimary: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;instance_primary&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// validated hex colour</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  emailFromName: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;email_from_name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  emailLogo: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;email_logo&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  updatedAt: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;updated_at&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { mode: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;timestamp&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$defaultFn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/db/src/schema/postgres/index.ts — bigint timestamps, same columns</span></span></code></pre></div><p>Read via <code>getInstanceConfig(pdb, tenantId): Promise&lt;InstanceConfig | null&gt;</code>. The returned object merges DB values over env defaults so callers always get a complete, typed set. Written via <code>setInstanceConfig(pdb, tenantId, partial)</code> which upserts.</p><p><code>instance_primary</code> is stored pre-validated: the write helper rejects any value that does not match <code>/^#[0-9a-fA-F]{6}$/</code>. Raw user input must never be injected into a <code>&lt;style&gt;</code> block without this gate — a malformed or adversarial value (e.g. <code>red; } * { visibility:hidden } /*</code>) would be a CSS injection.</p><p>Bootstrapped by <code>bootstrapPlatformDb()</code> with an <code>INSTANCE_NAME</code> env-var seed row alongside the existing default-tenant seed.</p><h3 id="sv-instance-token-namespace" tabindex="-1"><code>--sv-instance-*</code> token namespace <a class="header-anchor" href="#sv-instance-token-namespace" aria-label="Permalink to &quot;\`--sv-instance-*\` token namespace&quot;">​</a></h3><p>New CSS custom properties injected at <code>:root</code>, separate from the existing <code>--sv-color-*</code> semantic tokens:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>--sv-instance-logo         URL of the instance logo (light), path-relative to the runtime origin</span></span>
<span class="line"><span>--sv-instance-logo-dark    URL of the instance logo (dark), falls back to --sv-instance-logo</span></span>
<span class="line"><span>--sv-instance-favicon      URL of the favicon</span></span></code></pre></div><p>Why a separate namespace rather than extending <code>--sv-color-*</code>:</p><ul><li><strong>Semantic distinction.</strong> <code>--sv-color-accent</code> is a theme token — it can change with dark mode, user preference, or seasonal decoration. <code>--sv-instance-logo</code> is an identity token — it stays the same regardless of theme variant. Mixing them conflates two concerns.</li><li><strong>Value types.</strong> Colour tokens hold hex/rgb values. Instance identity tokens hold URLs. A CSS custom property for a URL needs different handling (<code>url()</code> wrapping, <code>content</code> vs <code>background-image</code> usage).</li><li><strong>Clear override boundary.</strong> Instance identity tokens are set by the operator once; colour tokens may be swapped dynamically (dark mode, user theme).</li></ul><p><strong>The instance name is not a <code>--sv-instance-*</code> CSS token.</strong> CSS custom properties cannot supply rendered text content — they work with <code>content:</code> on pseudo-elements, which is too narrow for the shell chrome. The instance name is passed as a React prop by <code>InstanceProvider</code> and rendered in the sidebar header and login page as normal React children. It may additionally be mirrored as a <code>data-instance-name</code> attribute on <code>&lt;html&gt;</code> for CSS <code>attr()</code> edge cases, but this is never the primary mechanism.</p><p>When an instance primary colour is set, <code>InstanceProvider</code> also sets <code>--sv-color-accent</code> and derives <code>--sv-color-accent-hover</code>. Derivation uses an HSL lightness delta: the hover colour is the accent colour shifted toward its background (darker on light theme, lighter on dark theme) by a fixed <code>8%</code> lightness step. <code>InstanceProvider</code> computes the HSL triple from the validated hex value at render time (pure arithmetic, no external dependency). The algorithm is a named constant (<code>ACCENT_HOVER_LIGHTNESS_DELTA = 8</code>) so it can be adjusted without hunting the code.</p><h3 id="runtime-injection" tabindex="-1">Runtime injection <a class="header-anchor" href="#runtime-injection" aria-label="Permalink to &quot;Runtime injection&quot;">​</a></h3><p>A new React server component <code>InstanceProvider</code> at <code>runtime/src/instance-provider.tsx</code>:</p><ol><li>Reads <code>instance_config</code> from <code>getPlatformDb()</code> for the current tenant.</li><li>Merges with env defaults (<code>INSTANCE_*</code> vars).</li><li>Renders a <code>&lt;style&gt;</code> block setting <code>--sv-instance-*</code> tokens and (if <code>instancePrimary</code> is set) <code>--sv-color-accent</code> / <code>--sv-color-accent-hover</code>.</li><li>Passes <code>instanceName</code> as a named prop to its children so the shell chrome can render it without a CSS variable.</li></ol><p>Called from <code>(platform)/layout.tsx</code> (the authenticated shell) and the auth server&#39;s root layout (login page). Both call sites pass the instance name down as a prop to the header component — no prop drilling through intermediate layers.</p><p><code>InstanceProvider</code> is a server component; it runs per-request and always reflects the current DB state without an additional client-side fetch. There is no persistent in-process cache in the server component: Next.js&#39;s per-request render boundary provides sufficient isolation, and instance identity values change rarely enough that a DB read per render is acceptable.</p><h3 id="logo-serving-and-csp" tabindex="-1">Logo serving and CSP <a class="header-anchor" href="#logo-serving-and-csp" aria-label="Permalink to &quot;Logo serving and CSP&quot;">​</a></h3><p>Uploaded logos are stored at <code>data/instance/&lt;tenant_id&gt;.&lt;ext&gt;</code> (same volume as <code>data/avatars/</code>) and served by a dedicated runtime route:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /api/instance/logo[?dark=1]      light or dark variant</span></span>
<span class="line"><span>GET /api/instance/favicon</span></span></code></pre></div><p>These routes are <strong>excluded from the middleware session gate</strong> — they must load on the login page before any session exists. The exclusion list in <code>runtime/middleware.ts</code> already has entries for <code>/api/health</code> and PWA assets; <code>/api/instance/*</code> joins that list.</p><p>Uploaded files are validated for MIME type (<code>image/png</code>, <code>image/svg+xml</code>, <code>image/jpeg</code>, <code>image/webp</code>, <code>image/x-icon</code>) and capped at 2 MB at the upload handler — the same constraints as the avatar upload route.</p><p>The DB stores a <strong>path-relative URL</strong> for uploaded files (<code>/api/instance/logo</code>), not an absolute URL. An absolute URL baked at upload time breaks when the operator&#39;s hostname changes (different environments, VPN, local dev). The serving layer resolves the origin at response time. External URLs entered directly by the operator are stored as-is; the frontend renders them, not the server.</p><p><strong>CSP impact:</strong> the current <code>img-src &#39;self&#39; data: blob:</code> allows path-relative logo URLs served from the runtime&#39;s own origin. External logo URLs entered directly by the operator would be blocked by this policy. The implementation must handle this in one of two ways:</p><ul><li><strong>Recommended:</strong> proxy external URLs through <code>/api/instance/proxy?url=…</code> (validates against an allowlist, caches on disk) so the browser always fetches from <code>&#39;self&#39;</code>. This avoids any CSP relaxation.</li><li><strong>Alternative:</strong> widen <code>img-src</code> to include the configured instance domain when set, injected dynamically by the CSP middleware. Acceptable if the proxy approach is deferred.</li></ul><p>The proxy approach is preferred — it keeps the CSP maximally strict and avoids leaking referer information to third-party logo hosts. The decision is recorded here; the implementation task chooses which to ship first.</p><h3 id="login-page-apps-auth" tabindex="-1">Login page (<code>apps/auth</code>) <a class="header-anchor" href="#login-page-apps-auth" aria-label="Permalink to &quot;Login page (\`apps/auth\`)&quot;">​</a></h3><p>The auth server renders instance identity on the login page. Since the auth server is a separate Next.js app with no access to the platform DB, it must obtain identity values via one of two paths:</p><ul><li><strong>Proxy through runtime (recommended):</strong> the auth server&#39;s root layout fetches <code>GET &lt;SOVEREIGN_AUTH_URL replaced with runtime URL&gt;/api/admin/instance-config</code> at render time. The runtime route returns the merged instance config (DB + env defaults). This keeps the auth server stateless with respect to instance identity — no extra table, no dual-write, no desync surface. The tradeoff is a render-time dependency on the runtime being reachable; if the runtime is down the login page falls back to Sovereign defaults (graceful degradation).</li><li><strong>Dual-write (fallback):</strong> the Console PATCH writes branding to both the platform DB and a structured key in <code>apps/auth</code>&#39;s <code>auth_settings</code> table. Matches the invite-only precedent. More resilient to runtime downtime, but requires keeping two stores in sync across 7 fields. Invite-only is one boolean — branding is substantially more state, making desync more likely and harder to detect.</li></ul><p><strong>Decision:</strong> proxy through runtime. The dual-write approach was appropriate for invite-only (one boolean, safety-critical to keep in sync because registration enforcement reads the auth copy exclusively). Instance identity is cosmetic — a stale value shows the wrong logo for one render cycle, not a security failure. The simpler path (one store, one source of truth) is the right tradeoff here.</p><p>The fetch is wrapped in a try/catch; on failure <code>InstanceProvider</code> renders with Sovereign defaults. An in-memory TTL cache (60 s, same order as the session cookie cache) prevents an identity fetch on every login page render.</p><h3 id="email-templates" tabindex="-1">Email templates <a class="header-anchor" href="#email-templates" aria-label="Permalink to &quot;Email templates&quot;">​</a></h3><p><code>packages/mailer</code> gains an optional <code>instanceIdentity</code> parameter in <code>MailOptions</code> (added in RFC 0031, epic task 9.9):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MailOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  to</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  subject</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  html</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Override the configured default sender for this message. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  from</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  instanceIdentity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /** Displayed in the email header and footer in place of &quot;Sovereign&quot;. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * Publicly reachable URL for the instance logo.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * The template renders it as &lt;img src=&quot;…&quot; alt=&quot;name&quot;&gt; with a text fallback.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * See &quot;Email logo&quot; below for the rationale against data: URIs.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    logoUrl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The runtime passes instance identity from <code>getInstanceConfig()</code> when constructing mail options for invite and password-reset emails.</p><p><strong>Email logo approach — hosted URL, not <code>data:</code> URI.</strong> Embedding logos as base64 <code>data:</code> URIs is not viable: Gmail, Outlook, and most modern webmail clients strip or refuse to render inline <code>data:</code> URIs as a security policy. The two technically sound alternatives are:</p><ol><li><strong>Hosted URL</strong> (chosen): the <code>&lt;img&gt;</code> tag references the operator&#39;s logo URL. Some clients block external images by default; the template is designed to be readable without images — the instance name appears as text in the header and <code>alt</code> attribute so the email makes sense either way. This is the industry standard for transactional email.</li><li><strong>CID attachment</strong> (<code>multipart/related</code>, <code>Content-ID</code>): embeds the image in the MIME envelope without an external request. Works in desktop clients (Outlook, Apple Mail) but not in most webmail (Gmail renders CID attachments as broken images). Adds complexity to the mailer without reliable cross-client coverage; deferred as a future enhancement.</li></ol><p>The template must degrade gracefully — instance name in the sender field (<code>&quot;Acme Corp &lt;noreply@acme.com&gt;&quot;</code>), instance name as <code>alt</code> text on the logo <code>&lt;img&gt;</code>, instance name in the plain-text version.</p><h3 id="pwa-manifest" tabindex="-1">PWA manifest <a class="header-anchor" href="#pwa-manifest" aria-label="Permalink to &quot;PWA manifest&quot;">​</a></h3><p>The <code>manifest.json</code> at <code>runtime/public/manifest.json</code> becomes a <strong>served</strong> route (<code>GET /manifest.webmanifest</code>) when instance identity is configured. The route reads the tenant&#39;s instance name and icon URLs and returns a dynamic manifest. When no identity is configured, the static file continues to be served directly.</p><p>The service worker caches <code>manifest.json</code> on first load. When branding changes, users on the cached version see the old name/icons until the SW updates. This is a known limitation for v1 — document in <code>docs/self-hosting.md</code> under PWA notes.</p><h3 id="favicon" tabindex="-1">Favicon <a class="header-anchor" href="#favicon" aria-label="Permalink to &quot;Favicon&quot;">​</a></h3><p>A route <code>GET /favicon.ico</code> returns the tenant&#39;s configured favicon when set, falling back to the committed <code>runtime/public/favicon.ico</code>. The Next.js <code>&lt;head&gt;</code> metadata in <code>runtime/app/layout.tsx</code> points to this route unconditionally so the fallback is transparent.</p><h3 id="console-settings-page" tabindex="-1">Console settings page <a class="header-anchor" href="#console-settings-page" aria-label="Permalink to &quot;Console settings page&quot;">​</a></h3><p>A new <strong>Instance identity</strong> section under Console settings (<code>/console/settings/instance</code>), visible to <code>platform:admin</code>:</p><ul><li>Instance name (text input)</li><li>Logo upload (light + dark variants) or external URL entry</li><li>Primary colour (colour picker → hex, validated <code>/^#[0-9a-fA-F]{6}$/</code> client and server)</li><li>Favicon upload or external URL</li><li>Email sender name</li><li>Email logo URL</li><li>Preview panel showing the shell header and login page chrome with current identity values applied via client-side CSS variable swapping</li></ul><p>On save, PATCH writes to <code>instance_config</code>. No dual-write to the auth server (see Login page section).</p><h3 id="sdk-platform-getconfig-extension" tabindex="-1"><code>sdk.platform.getConfig()</code> extension <a class="header-anchor" href="#sdk-platform-getconfig-extension" aria-label="Permalink to &quot;\`sdk.platform.getConfig()\` extension&quot;">​</a></h3><p><code>getConfig()</code> currently returns <code>{ tenantName, inviteOnly, version }</code>. In Phase 1 it gains instance identity fields:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PlatformConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  inviteOnly</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // New in Phase 1 (renamed by RFC 0032):</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  instanceName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// falls back to tenantName</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  instancePrimaryColor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// validated hex, or undefined if unset</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Plugin developers use <code>sdk.platform.getConfig()</code> to display the instance name. Exposing <code>instanceName</code> and <code>instancePrimaryColor</code> lets plugins participate in the instance identity without reading CSS variables (useful in non-browser contexts like email-generating plugins). The <code>--sv-instance-*</code> CSS tokens are sufficient for purely visual plugin uses; <code>getConfig()</code> is the programmatic path.</p><p>Logo and favicon URLs are intentionally excluded from <code>getConfig()</code> — they are served via the <code>/api/instance/*</code> routes and consumed as CSS <code>background-image</code> values, not as props. Adding them to the SDK surface would couple the SDK to the runtime&#39;s URL structure.</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><h3 id="admin-flow-setting-up-instance-identity" tabindex="-1">Admin flow: setting up instance identity <a class="header-anchor" href="#admin-flow-setting-up-instance-identity" aria-label="Permalink to &quot;Admin flow: setting up instance identity&quot;">​</a></h3><ol><li>Admin navigates to Console → Settings → Instance identity.</li><li>Form is pre-filled from current <code>instance_config</code> (merged with env defaults).</li><li>Admin uploads a logo (stored at <code>data/instance/</code>, served as <code>/api/instance/logo</code>) or enters an external URL; enters instance name; picks a primary colour.</li><li>Preview panel updates live via client-side CSS variable swapping (no save needed to preview).</li><li>On save, PATCH validates and writes to <code>instance_config</code>.</li><li>Page reloads; <code>InstanceProvider</code> re-reads from DB and the new identity is visible immediately.</li></ol><h3 id="user-flow-seeing-the-configured-identity" tabindex="-1">User flow: seeing the configured identity <a class="header-anchor" href="#user-flow-seeing-the-configured-identity" aria-label="Permalink to &quot;User flow: seeing the configured identity&quot;">​</a></h3><ol><li>User visits the login page — sees the instance logo and name, not Sovereign&#39;s.</li><li>User logs in — shell header shows the instance logo and name.</li><li>User receives an email — sender name and logo reflect the instance identity (logo visible in clients that allow external images).</li><li>User installs the PWA — app name and icons show the instance identity.</li></ol><h3 id="reset-flow" tabindex="-1">Reset flow <a class="header-anchor" href="#reset-flow" aria-label="Permalink to &quot;Reset flow&quot;">​</a></h3><p>Admin clears all identity fields → PATCH writes nulls → env defaults apply → Sovereign&#39;s own identity is restored.</p><h2 id="security-considerations" tabindex="-1">Security considerations <a class="header-anchor" href="#security-considerations" aria-label="Permalink to &quot;Security considerations&quot;">​</a></h2><ul><li><strong>CSS injection:</strong> <code>instance_primary</code> is stored and rendered as a hex colour. Any non-hex value reaching the <code>&lt;style&gt;</code> block would be a CSS injection. Validated server-side at write time (<code>/^#[0-9a-fA-F]{6}$/</code>), never written raw.</li><li><strong>Logo URL injection:</strong> logo URLs stored in the DB are operator-supplied. The <code>/api/instance/logo</code> serving route restricts to uploaded files from <code>data/instance/</code>; external URLs are passed through to the browser and rendered by <code>&lt;img&gt;</code>, not fetched server-side, so there is no SSRF surface at the serving layer. If the proxy approach is implemented for CSP, the proxy must validate the URL against an allowlist or scheme check (<code>https:</code> only).</li><li><strong>Uploaded file validation:</strong> MIME type check + 2 MB cap at the upload handler. Files are stored under <code>data/instance/</code> with a sanitised filename (tenant ID + extension only — no path traversal).</li><li><strong>Pre-auth route exposure:</strong> <code>/api/instance/logo</code>, <code>/api/instance/favicon</code>, and <code>/manifest.webmanifest</code> are excluded from the session gate and publicly readable. They contain no user data; the only information exposed is the operator&#39;s own identity configuration.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="pure-env-var-approach" tabindex="-1">Pure env-var approach <a class="header-anchor" href="#pure-env-var-approach" aria-label="Permalink to &quot;Pure env-var approach&quot;">​</a></h3><p>Rejected. Env vars require a server restart to change and work only for single-tenant. The project has <code>tenant_id</code> on every table from day one — per-tenant branding is a direct use of that infrastructure. Env vars as the <em>baseline</em> (merged with DB overrides) gives the best of both: single-tenant ops set-and-forget, multi-tenant ops configure at runtime.</p><h3 id="build-time-approach-theme-at-docker-build" tabindex="-1">Build-time approach (theme at Docker build) <a class="header-anchor" href="#build-time-approach-theme-at-docker-build" aria-label="Permalink to &quot;Build-time approach (theme at Docker build)&quot;">​</a></h3><p>Rejected. Building a branded Docker image per tenant is operationally expensive, prevents runtime reconfiguration, and breaks auto-update. Sovereign already ships as a single image.</p><h3 id="template-overrides-full-login-page-template-per-tenant" tabindex="-1">Template overrides (full login page template per tenant) <a class="header-anchor" href="#template-overrides-full-login-page-template-per-tenant" aria-label="Permalink to &quot;Template overrides (full login page template per tenant)&quot;">​</a></h3><p>Rejected. Custom templates in <code>apps/auth</code> would fork from upstream, break on upgrade, and create a maintenance burden. CSS variable injection achieves the same visual result without forking a single file.</p><h3 id="extending-sv-color-instead-of-sv-instance" tabindex="-1">Extending <code>--sv-color-*</code> instead of <code>--sv-instance-*</code> <a class="header-anchor" href="#extending-sv-color-instead-of-sv-instance" aria-label="Permalink to &quot;Extending \`--sv-color-*\` instead of \`--sv-instance-*\`&quot;">​</a></h3><p>Considered but rejected. The core objection is mixing value types (colours vs URLs) and conflating theme identity with instance identity. A separate namespace is clearer for operators, plugin developers, and future maintainers.</p><h3 id="sv-instance-name-css-custom-property-for-the-instance-name" tabindex="-1"><code>--sv-instance-name</code> CSS custom property for the instance name <a class="header-anchor" href="#sv-instance-name-css-custom-property-for-the-instance-name" aria-label="Permalink to &quot;\`--sv-instance-name\` CSS custom property for the instance name&quot;">​</a></h3><p>Considered. CSS custom properties can expose a string via <code>content: var(--sv-instance-name)</code> on pseudo-elements, but this does not work for rendered text content in the shell chrome. The instance name is visible HTML text — it needs to be a React prop. A CSS custom property would be a secondary mirror at best, creating two sources of truth and a confusing API for plugin developers. Dropped in favour of a single mechanism: React prop from <code>InstanceProvider</code>.</p><h3 id="dual-write-for-auth-server-branding" tabindex="-1">Dual-write for auth server branding <a class="header-anchor" href="#dual-write-for-auth-server-branding" aria-label="Permalink to &quot;Dual-write for auth server branding&quot;">​</a></h3><p>Considered, matching the invite-only precedent. Rejected for instance identity because: (a) the scope is much larger (7 fields vs 1 boolean), increasing desync risk; (b) the failure mode is cosmetic (wrong logo) rather than a security failure (open registration); (c) the proxy approach (one store, one source of truth) is simpler and sufficient. See &quot;Login page&quot; section.</p><h3 id="data-uri-email-logo-embedding" tabindex="-1"><code>data:</code> URI email logo embedding <a class="header-anchor" href="#data-uri-email-logo-embedding" aria-label="Permalink to &quot;\`data:\` URI email logo embedding&quot;">​</a></h3><p>Considered to avoid external-image blocking in email clients. Rejected because Gmail, Outlook, and most webmail clients refuse to render inline <code>data:</code> URIs as a security measure — the technique would silently produce broken-image icons in the most common clients. Hosted URL with graceful text fallback is the correct pattern.</p><h3 id="per-role-identity" tabindex="-1">Per-role identity <a class="header-anchor" href="#per-role-identity" aria-label="Permalink to &quot;Per-role identity&quot;">​</a></h3><p>Considered and deferred. Showing different instance identity to admins vs users is a plausible requirement for OEM scenarios but adds complexity (role-scoped DB reads, multiple token sets). It can be layered on top of the per-tenant model without breaking changes by adding a <code>role</code> column to <code>instance_config</code>.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><p><strong>CSP for external logo URLs: proxy or widen?</strong> (See &quot;Logo serving and CSP&quot; above.) Proxy keeps the CSP strict; widening is simpler to ship. The proxy is preferred; the implementation task records the final choice.</p></li><li><p><strong>Logo storage: disk or external URL?</strong> The design supports both — uploaded files stored at <code>data/instance/</code> and served via <code>/api/instance/*</code>; external URLs stored as-is and rendered by the browser. The DB column stores a path-relative URL for uploaded files and an absolute URL for external entries. <strong>Resolved:</strong> both, as described above.</p></li><li><p><strong>PWA manifest caching:</strong> When instance identity changes, cached SW users see the old name/icons until the SW updates. <strong>Resolved:</strong> acceptable for v1. Document in <code>docs/self-hosting.md</code>.</p></li><li><p><strong><code>--sv-color-accent-hover</code> derivation algorithm:</strong> HSL lightness delta (<code>ACCENT_HOVER_LIGHTNESS_DELTA = 8%</code>) computed by <code>InstanceProvider</code> at render. If the accent colour is at an extreme lightness (&lt; 8% or &gt; 92%), the delta is clamped to keep the result in range. <strong>Resolved in design above; implementation validates the output is visually distinguishable.</strong></p></li><li><p><strong>What is the minimum viable surface for Phase 1?</strong> Recommended: <code>instance_config</code> table + env vars + <code>--sv-instance-*</code> injection in shell layout + <code>InstanceProvider</code> + Console identity form + <code>sdk.platform.getConfig()</code> extension. Phase 2: email templates + auth login page. Phase 3: dynamic PWA manifest + favicon route.</p></li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>This RFC is <strong>documentation-first</strong>. The design is recorded here for review and consensus; implementation scheduling is deferred.</p><table tabindex="0"><thead><tr><th>Phase</th><th>What ships</th><th>Packages affected</th><th>Semver</th></tr></thead><tbody><tr><td>0</td><td>Instance identity rename: <code>BRAND_*</code> → <code>INSTANCE_*</code>, <code>--sv-brand-*</code> → <code>--sv-instance-*</code>, <code>tenant_branding</code> → <code>instance_config</code> (RFC 0032)</td><td><code>packages/db</code>, <code>runtime</code>, <code>packages/ui</code>, <code>packages/sdk</code>, <code>plugins/console</code></td><td><code>ui</code> minor, <code>sdk</code> minor, <code>db</code> minor, <code>runtime</code> minor, <code>console</code> patch</td></tr><tr><td>1</td><td><code>instance_config</code> table + helpers, env vars, <code>--sv-instance-*</code> tokens, <code>InstanceProvider</code>, Console identity form, <code>getConfig()</code> extension</td><td><code>packages/db</code>, <code>runtime</code>, <code>plugins/console</code>, <code>packages/ui</code>, <code>packages/sdk</code></td><td><code>db</code> minor, <code>runtime</code> minor, <code>ui</code> minor, <code>console</code> minor, <code>sdk</code> minor</td></tr><tr><td>2</td><td>Email templates (logo + sender name, RFC 0031), auth login page identity via runtime proxy</td><td><code>packages/mailer</code>, <code>apps/auth</code></td><td><code>mailer</code> minor, <code>auth</code> minor</td></tr><tr><td>3</td><td>Dynamic PWA manifest route, dynamic favicon route</td><td><code>runtime</code></td><td><code>runtime</code> minor</td></tr></tbody></table><p>Phase 0 (RFC 0032) shipped as epic task 9.8; Phase 1 shipped as Task 1.0.03. Published packages (<code>@sovereignfs/ui</code>, <code>@sovereignfs/sdk</code>) treated the Phase 0 rename as a breaking minor bump per NFR-04 (no breaking patch releases).</p><h3 id="required-doc-updates" tabindex="-1">Required doc updates <a class="header-anchor" href="#required-doc-updates" aria-label="Permalink to &quot;Required doc updates&quot;">​</a></h3><ul><li><code>docs/design-system.md</code> — document <code>--sv-instance-*</code> token namespace and the instance-name-as-prop convention</li><li><code>docs/self-hosting.md</code> — document <code>INSTANCE_*</code> env vars, PWA manifest caching limitation</li><li><code>docs/plugin-development.md</code> — guide plugin developers on using <code>--sv-instance-*</code> tokens and <code>sdk.platform.getConfig()</code> identity fields</li><li><code>docs/sovereign-proposal-plan-srs.md</code> — add white-labeling to SRS §3 or §4</li><li><code>docs/upgrade.md</code> — migration notes: Phase 0 (RFC 0032) renames; Phase 1 <code>instance_config</code> DB table (bootstrapped automatically on first run)</li><li><code>.env.example</code> — <code>INSTANCE_*</code> vars</li></ul><h3 id="docker-config-impact" tabindex="-1">Docker/config impact <a class="header-anchor" href="#docker-config-impact" aria-label="Permalink to &quot;Docker/config impact&quot;">​</a></h3><ul><li>No new ports or Docker services</li><li>Logo storage reuses the <code>data/</code> directory (same named volume in prod, same <code>./data</code> bind mount in dev)</li><li><code>/api/instance/*</code> routes must be excluded from the middleware session gate (alongside existing exclusions for <code>/api/health</code> and PWA assets)</li><li><code>INSTANCE_*</code> env vars added to Docker Compose files and <code>.env.example</code></li></ul><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>June 2026</td><td>Initial draft</td></tr><tr><td>0.2</td><td>June 2026</td><td>Clarified config layering (removed phantom <code>platform_settings</code> middle tier); fixed <code>--sv-instance-name</code> approach (React prop, not CSS var); fixed email logo (hosted URL, not <code>data:</code> URI); specified CSS injection guard on <code>instance_primary</code>; specified dialect-agnostic DDL; added CSP/logo-serving analysis; resolved auth server approach (proxy, not dual-write); added <code>getConfig()</code> extension; specified <code>accent-hover</code> derivation algorithm; corrected Phase 3 semver to minor; added <code>docs/upgrade.md</code> to required doc updates; added security considerations section</td></tr><tr><td>0.3</td><td>June 2026</td><td>All <code>BRAND_*</code>/<code>--sv-brand-*</code>/<code>brandName</code>/<code>BrandProvider</code>/<code>tenant_branding</code> identifiers renamed to <code>INSTANCE_*</code>/<code>--sv-instance-*</code>/<code>instanceName</code>/<code>InstanceProvider</code>/<code>instance_config</code> per RFC 0032 (epic task 9.8); adoption path gains a Phase 0 row for the rename; incorporated-into-plan reference updated to reflect pre-v1 schedule</td></tr></tbody></table>`,123)])])}const u=a(n,[["render",o]]);export{g as __pageData,u as default};
