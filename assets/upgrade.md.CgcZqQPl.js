import{_ as o,o as s,c as t,a2 as i}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"Upgrade guide","description":"","frontmatter":{"docSection":"operators","docType":"guide","audiences":["operator","app-developer"],"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/upgrade.md"},"headers":[],"relativePath":"upgrade.md","filePath":"upgrade.md"}'),a={name:"upgrade.md"};function n(d,e,r,l,c,h){return s(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="upgrade-guide" tabindex="-1">Upgrade guide <a class="header-anchor" href="#upgrade-guide" aria-label="Permalink to &quot;Upgrade guide&quot;">​</a></h1><p>Migration notes for breaking changes and release-by-release upgrade steps.</p><p>Per NFR-04, any breaking change to a published package (<code>@sovereignfs/sdk</code>, <code>@sovereignfs/ui</code>) ships with at least a minor version bump and an entry here. Operators upgrading a self-hosted instance should read the Platform releases section; plugin developers should read the Published package migrations section.</p><hr><h2 id="upgrade-procedure-docker-compose" tabindex="-1">Upgrade procedure (Docker Compose) <a class="header-anchor" href="#upgrade-procedure-docker-compose" aria-label="Permalink to &quot;Upgrade procedure (Docker Compose)&quot;">​</a></h2><p>This is the standard path for all production upgrades.</p><h3 id="_1-back-up-data" tabindex="-1">1. Back up data <a class="header-anchor" href="#_1-back-up-data" aria-label="Permalink to &quot;1. Back up data&quot;">​</a></h3><p>Always snapshot before upgrading. On a running instance:</p><p><strong>Docker Compose (production) — back up the named volume.</strong> The prod stack stores data in the <code>sovereign_data</code> named volume, not on the host, so snapshot the volume directly:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sovereign_data:/data</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pwd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)/backups&quot;:/backup</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  alpine</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  tar</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> czf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /backup/sovereign-backup-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +%Y%m%dT%H%M%S</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.tar.gz</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /data</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre></div><p><strong>Source / host install — use the CLI.</strong> When the databases live in a host <code>./data</code> directory (dev, bind-mount, or a non-Docker host install), <code>sv backup</code> snapshots them (it reads <code>DATABASE_URL</code> from the environment):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backup</span></span></code></pre></div><p>Either way the archive contains all SQLite databases (with their <code>-wal</code>/<code>-shm</code> sidecars) and the <code>avatars/</code> directory, stored with paths relative to the data directory so it restores into any location.</p><h3 id="_2-apply-the-upgrade" tabindex="-1">2. Apply the upgrade <a class="header-anchor" href="#_2-apply-the-upgrade" aria-label="Permalink to &quot;2. Apply the upgrade&quot;">​</a></h3><p><strong>Published images</strong> (recommended — no build step):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Pin the exact release you are upgrading to.</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SOVEREIGN_VERSION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">v0.15.0</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SOVEREIGN_VERSION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">v0.15.0</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p><strong>Build from source</strong> (forks, air-gapped environments, custom patches):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>Database migrations run automatically on startup via <code>runMigrations()</code> in <code>packages/db</code>. The server is fail-fast: a migration error prevents the runtime from accepting requests, leaving the pre-upgrade snapshot intact.</p><h3 id="_3-verify" tabindex="-1">3. Verify <a class="header-anchor" href="#_3-verify" aria-label="Permalink to &quot;3. Verify&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Check health — includes platform version and any downgrade warning.</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SOVEREIGN_ADMIN_KEY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:4000/api/admin/health</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre></div><p>A successful upgrade shows the new <code>platformVersion</code>. A <code>downgradeWarning</code> field means the database was last written by a newer binary — stop, restore your backup, then re-apply the correct image.</p><h3 id="rollback" tabindex="-1">Rollback <a class="header-anchor" href="#rollback" aria-label="Permalink to &quot;Rollback&quot;">​</a></h3><p>If the upgraded instance is unhealthy:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. Stop the upgraded containers.</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> down</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. Restore the pre-upgrade backup into the named volume (mirror of the backup</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    command — overwrites the volume contents from the archive).</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sovereign_data:/data</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pwd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)/backups&quot;:/backup</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  alpine</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rm -rf /data/* &amp;&amp; tar xzf /backup/&lt;archive&gt;.tar.gz -C /data&#39;</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 3. Start the previous image.</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SOVEREIGN_VERSION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">previous-version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.prod.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>(For a source / host install, use <code>pnpm sv restore ./backups/&lt;archive&gt;.tar.gz</code> instead of the volume command.)</p><p>For published images, <code>SOVEREIGN_VERSION</code> pins the exact tag to restart from. For source builds, <code>git checkout &lt;previous-commit&gt;</code> before rebuilding.</p><hr><h2 id="platform-releases" tabindex="-1">Platform releases <a class="header-anchor" href="#platform-releases" aria-label="Permalink to &quot;Platform releases&quot;">​</a></h2><p>Version numbers in the sections below refer to the <strong><code>runtime</code> package version</strong> (<code>runtime/package.json</code>), not the root <code>package.json</code>. The root <code>package.json</code> tracks roadmap phase milestones and will jump to <code>1.0.0</code> at the public release. See the <a href="#runtime-version-map">Runtime version map</a> and <a href="#v100-release-checklist">v1.0.0 release checklist</a> at the end of this file.</p><p>Notes call out any required configuration changes, schema changes, or action required.</p><h3 id="v0-100-1-→-v0-101-0-root-0-133-0-apps-auth-2-5-0" tabindex="-1">v0.100.1 → v0.101.0 (root 0.133.0, <code>apps/auth</code> 2.5.0) <a class="header-anchor" href="#v0-100-1-→-v0-101-0-root-0-133-0-apps-auth-2-5-0" aria-label="Permalink to &quot;v0.100.1 → v0.101.0 (root 0.133.0, `apps/auth` 2.5.0)&quot;">​</a></h3><ul><li><strong><code>middleware.ts</code> → <code>proxy.ts</code> on both apps</strong> (Next 16&#39;s renamed convention; the codemod-equivalent rename plus the exported function). <code>next build</code> no longer prints the <code>middleware</code> deprecation warning. Behaviour is unchanged: same matcher, same 303 login redirect, same per-request CSP, same trust-header stripping and injection. <strong>The proxy now executes on the Node.js runtime</strong> (Next does not support the Edge runtime for <code>proxy</code>); by decision it keeps the Edge discipline it was written to — no Node built-ins, no DB access — so nothing about deployment changes: the standalone image runs the same <code>server.js</code>, and a reverse proxy in front needs no new rules. No operator action.</li></ul><h3 id="root-0-131-2-→-0-132-0-no-runtime-version-bump-sovereignfs-create-plugin-patch" tabindex="-1">Root 0.131.2 → 0.132.0 (no <code>runtime</code> version bump; <code>@sovereignfs/create-plugin</code> patch) <a class="header-anchor" href="#root-0-131-2-→-0-132-0-no-runtime-version-bump-sovereignfs-create-plugin-patch" aria-label="Permalink to &quot;Root 0.131.2 → 0.132.0 (no `runtime` version bump; `@sovereignfs/create-plugin` patch)&quot;">​</a></h3><ul><li><strong>TypeScript 5.9 → 6.0</strong> (pnpm catalog). No operator action; no change to any published package&#39;s emitted types (<code>@sovereignfs/sdk</code>, <code>ui</code>, <code>bridge</code>, <code>manifest</code> <code>.d.ts</code> output diffed byte-identical before/after, NFR-04). For plugin developers on the shared <code>@sovereignfs/tsconfig</code>: the base now sets <code>&quot;types&quot;: [&quot;node&quot;]</code> (TypeScript 6 no longer auto-includes every <code>@types/*</code> package), and <code>baseUrl</code> — deprecated in 6.0, removed in 7.0 — is gone from every tsconfig and from the <code>create-plugin</code> scaffold; <code>paths</code> needs none. A plugin repo that still sets <code>baseUrl</code> gets TS5101 on TypeScript 6 — delete the line. tsup&#39;s dts step injects its own <code>baseUrl</code> (tsup 8.5.1 predates TypeScript 6), so each package&#39;s <code>tsup.config.ts</code> scopes <code>ignoreDeprecations: &quot;6.0&quot;</code> to that pass only.</li></ul><h3 id="v0-99-1-→-v0-100-0-root-0-131-0-apps-auth-2-4-0-apps-relay-0-4-0-apps-harness-0-2-0" tabindex="-1">v0.99.1 → v0.100.0 (root 0.131.0, <code>apps/auth</code> 2.4.0, <code>apps/relay</code> 0.4.0, <code>apps/harness</code> 0.2.0) <a class="header-anchor" href="#v0-99-1-→-v0-100-0-root-0-131-0-apps-auth-2-4-0-apps-relay-0-4-0-apps-harness-0-2-0" aria-label="Permalink to &quot;v0.99.1 → v0.100.0 (root 0.131.0, `apps/auth` 2.4.0, `apps/relay` 0.4.0, `apps/harness` 0.2.0)&quot;">​</a></h3><ul><li><strong>Next.js 15.5 → 16.3</strong> on every app (pnpm catalog). Both <code>next dev</code> and <code>next build</code> now use Turbopack; nothing in the runtime or auth app needs the <code>--webpack</code> escape hatch. Node 24 (already required) satisfies Next 16&#39;s <code>&gt;=20.9</code> floor, so no host/Docker change is needed.</li><li><strong>The service worker toolchain changed.</strong> <code>@ducanh2912/next-pwa</code> (a webpack plugin, last released in 2024) is gone; the runtime&#39;s worker (<code>runtime/worker/index.ts</code> + <code>routes.ts</code>) is now built on <a href="https://serwist.pages.dev" target="_blank" rel="noreferrer">Serwist</a> by <code>runtime/scripts/build-sw.ts</code>, which <code>pnpm --filter @sovereignfs/runtime build</code> runs after <code>next build</code>. It emits one self-contained <code>runtime/public/sw.js</code> (the <code>workbox-*.js</code>, <code>fallback-*.js</code> and <code>worker-*.js</code> siblings no longer exist). <strong>No operator action:</strong> the published image runs the same build command, and a browser that still holds the old worker replaces it on its next visit (the new one calls <code>skipWaiting</code> + <code>clientsClaim</code>, and cleans up the old Workbox caches). Custom reverse-proxy rules that special-cased <code>/workbox-*.js</code>, <code>/fallback-*.js</code> or <code>/worker-*.js</code> can be dropped; <code>/sw.js</code>, <code>/offline</code> and <code>/manifest.json</code> still need to be reachable without a session.</li><li><strong>Caching guarantee unchanged, mechanism tightened.</strong> Per-user pages are served by Serwist&#39;s <code>NetworkOnly</code> (there is no cache for them at all, where next-pwa&#39;s build schema forced a <code>NetworkFirst</code> that refused writes); manifest-declared offline shells are still <code>StaleWhileRevalidate</code>; failed navigations still fall back to the precached <code>/offline</code>.</li><li><strong>Deprecation carried, not resolved:</strong> Next 16 deprecates the <code>middleware</code> file convention in favour of <code>proxy</code>, which runs on the Node.js runtime only. Sovereign&#39;s middleware deliberately runs on the Edge runtime (it is the session gate and CSP source for every request), so it stays <code>middleware.ts</code> for now — <code>next build</code> prints one deprecation warning. Tracked as epic task 0.29.</li></ul><h3 id="root-0-130-3-→-0-130-4-no-runtime-version-bump" tabindex="-1">Root 0.130.3 → 0.130.4 (no <code>runtime</code> version bump) <a class="header-anchor" href="#root-0-130-3-→-0-130-4-no-runtime-version-bump" aria-label="Permalink to &quot;Root 0.130.3 → 0.130.4 (no `runtime` version bump)&quot;">​</a></h3><ul><li><strong>Next.js 15.5.22 → 15.5.25 and vitest 4.1.10 → 4.1.11</strong> (pnpm catalog / root devDependency). No behavior change for operators, no action required. The Dependabot batch carrying these (#642) failed CI on a vitest-only module-duplication problem, fixed by aliasing <code>next</code> in <code>vitest.config.ts</code> — see <code>docs/architecture-rules.md</code>.</li></ul><h3 id="v0-99-0-→-v0-99-1-apps-auth-2-3-0-→-2-3-1-root-0-130-3" tabindex="-1">v0.99.0 → v0.99.1 (<code>apps/auth</code> 2.3.0 → 2.3.1, root 0.130.3) <a class="header-anchor" href="#v0-99-0-→-v0-99-1-apps-auth-2-3-0-→-2-3-1-root-0-130-3" aria-label="Permalink to &quot;v0.99.0 → v0.99.1 (`apps/auth` 2.3.0 → 2.3.1, root 0.130.3)&quot;">​</a></h3><ul><li><strong>better-auth 1.6.25 → 1.6.30</strong> (<code>better-auth</code>, <code>@better-auth/passkey</code>, <code>@better-auth/oauth-provider</code>). Same 1.6 line, no schema change, no action required.</li><li><strong>better-auth 1.7.0–1.7.2 are deliberately skipped</strong> (epic task 1.26). Those three releases add a required <code>account.issuer</code> column that better-auth&#39;s migrator refuses to add to any table with existing rows — the auth server would fail to boot on every real instance — and better-auth&#39;s own 1.7 upgrade guide states 1.7.3 removes the column again. <code>dependabot.yml</code> now ignores that window. <strong>If you upgraded an instance to 1.7.0–1.7.2 by hand</strong> and it booted (a fresh install with no accounts at the time), follow better-auth&#39;s &quot;Account identity keeps the provider key&quot; guide before moving to 1.7.3 or back to 1.6: on SQLite, <code>DROP INDEX account_issuer_accountId_uidx;</code> then <code>ALTER TABLE account DROP COLUMN issuer;</code>.</li><li>Two genuine <code>session_data</code> cookies (captured from real better-auth 1.6.25 and 1.7.2 instances) are now permanent fixtures in the runtime&#39;s session-verification tests, so a future bump that changes the cookie-cache format the middleware verifies offline fails CI instead of logging every user out.</li></ul><h3 id="v0-96-0-→-v0-97-0" tabindex="-1">v0.96.0 → v0.97.0 <a class="header-anchor" href="#v0-96-0-→-v0-97-0" aria-label="Permalink to &quot;v0.96.0 → v0.97.0&quot;">​</a></h3><ul><li><strong>Instance-scope async backups (epic task 8.16, <code>SOVEREIGN_BACKUP_WORKER_ENABLED</code>) now actually work in the documented Docker Compose production deployment — Postgres dialect only.</strong> Previously the worker&#39;s <code>sv backup</code> subprocess spawn failed with <code>ENOENT</code> in the <code>runner</code> image (no <code>bin/</code>/<code>scripts/</code>/<code>tsx</code> there); this is fixed by bundling a minimal backup/restore-only CLI into the image instead (no behavior change to <code>pnpm sv backup</code>/<code>sv restore</code> themselves). Two related, already-shipped-but-silently-broken things are also fixed by this same change: the <code>runner</code> image previously had no <code>git</code> binary at all, so the per-user git-push/restore-fetch backup destination feature (workstream 0023 legs 3–4) could not have worked in real production either; and <code>runtime</code>&#39;s own <code>./backups</code> bind mount was read-only, which would have failed the very first archive write for either backup scope. <ul><li><strong>Action required — instance operators who enable <code>SOVEREIGN_BACKUP_WORKER_ENABLED</code>:</strong> the <code>runner</code> container&#39;s non-root user now runs as a pinned UID/GID (<code>1001:1001</code>, previously auto-assigned) so the host <code>./backups</code> directory can be <code>chown</code>-ed to it. Before starting the upgraded image with the worker enabled for the first time:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backups</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chown</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1001:1001</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backups</span></span></code></pre></div>If you don&#39;t enable <code>SOVEREIGN_BACKUP_WORKER_ENABLED</code>, no action is needed — this only matters once the worker actually tries to write an archive.</li><li><strong>Action required — none for everyone else.</strong> No schema or data migration. The <code>runner</code>/<code>tools</code> image layers are modestly larger (added <code>git</code>, <code>postgresql16-client</code>, and a small bundled CLI artifact).</li><li><strong>Still not supported:</strong> SQLite (sqld) instance-scope backup/restore — <code>sv backup</code>/<code>sv restore</code> still explicitly refuse that dialect. Building it needs its own RFC first (see <code>docs/research/0017-sqld-backup-and-restore.md</code>); user-scope backups (Account → Data → Full backup) already work on both dialects and are unaffected either way.</li><li>See <code>docs/architecture-rules.md</code>&#39;s (now-resolved) entry on this gap and <code>bin/backup-restore.ts</code>&#39;s own doc comment for the implementation.</li></ul></li></ul><h3 id="v0-86-1-→-v0-87-0" tabindex="-1">v0.86.1 → v0.87.0 <a class="header-anchor" href="#v0-86-1-→-v0-87-0" aria-label="Permalink to &quot;v0.86.1 → v0.87.0&quot;">​</a></h3><ul><li><strong>Breaking for plugin developers: the runtime shell no longer pads plugin content.</strong> Previously <code>runtime/app/(platform)/shell.module.css</code> applied <code>var(--sv-space-8)</code> (32px desktop) / <code>var(--sv-space-4)</code> (16px mobile) to every non-<code>data-plugin-fullbleed</code> plugin&#39;s content region. That gutter is gone; the shell now contributes only clearance for chrome it alone can measure (offline banner height while visible, mobile footer height). A plugin&#39;s padding and max-width instead come from <code>@sovereignfs/ui</code>&#39;s <code>PageContainer</code>, wrapping the plugin&#39;s page or root layout — see <code>docs/design-system.md</code>&#39;s &quot;Page layout&quot; section and <code>docs/plugin-development.md</code>. <code>@sovereignfs/ui</code> bumped to <code>0.55.0</code> with this change: <code>PageContainer</code> gained a <code>padding</code> prop (<code>&#39;none&#39; | &#39;sm&#39; | &#39;md&#39; | &#39;lg&#39;</code>, default <code>&#39;md&#39;</code> — <code>&#39;md&#39;</code> reproduces the exact gutter the shell used to apply, so <code>&lt;PageContainer&gt;{children}&lt;/PageContainer&gt;</code> with no props is the equivalent replacement for a plugin that previously relied on the shell) and <code>maxWidth</code>&#39;s default flipped from <code>&#39;md&#39;</code> to <code>&#39;full&#39;</code> (all in-repo callers already passed <code>maxWidth</code> explicitly, so this default change was invisible in this repo, but is a behavior change for any external caller relying on the old default). <ul><li><strong>Action required — plugin developers:</strong> wrap every page (or your root <code>app/layout.tsx</code>) in <code>PageContainer</code>. Until you do, your plugin renders edge-to-edge. If your plugin already had its own <code>padding</code>/<code>max-width</code> in <code>app/layout.tsx</code> or a page CSS module, keep that CSS but additionally check <code>docs/design-system.md</code> — declaring padding in both places double-pads; move it onto <code>PageContainer</code>&#39;s props instead.</li><li><strong>Action required — instance operators:</strong> none. This is a plugin-authoring contract change, not a runtime configuration, schema, or data change. Every plugin shipped with the platform (<code>plugins/launcher</code>, <code>plugins/account</code>, <code>plugins/console</code>, and every <code>example-plugins/*</code> reference plugin) was migrated in the same change and renders correctly with no action needed.</li><li>See <code>docs/epics/design-system.md</code> task 9.25 and <code>docs/architecture-rules.md</code> for the full rationale, including how overlay-shell (<code>shell: &quot;overlay&quot;</code>) plugins avoid double-padding inside their <code>Dialog</code> automatically.</li></ul></li></ul><h3 id="v0-75-1-→-v0-75-2" tabindex="-1">v0.75.1 → v0.75.2 <a class="header-anchor" href="#v0-75-1-→-v0-75-2" aria-label="Permalink to &quot;v0.75.1 → v0.75.2&quot;">​</a></h3><ul><li><strong>Fix: offline navigation to a non-cached page now shows the <code>/offline</code> page instead of a raw browser error.</strong> Root cause: Next&#39;s <code>next.config.ts</code> loader globally retranspiles every module reached from that file through SWC (because the compiled config contains <code>require(</code>), which silently broke every <code>async</code> service-worker plugin hook in <code>runtimeCaching</code> — including <code>@ducanh2912/next-pwa</code>&#39;s own auto-injected <code>handlerDidError</code> — with a <code>ReferenceError: _async_to_generator is not defined</code> inside the generated <code>sw.js</code>. Chrome hid this behind a bare <code>net::ERR_FAILED</code>; only Safari/WebKit surfaced the real exception, which is how it was root-caused. Affected any offline request that fell through to a non-precached page (e.g. <code>/console</code> visited offline with nothing cached). <ul><li><strong>Action required:</strong> none. No config, schema, or data changes.</li><li>See <code>docs/workstreams/0008-offline-first-architecture.md</code>&#39;s leg 2 section and the comment above <code>runtimeCaching</code> in <code>runtime/next.config.ts</code>.</li></ul></li></ul><h3 id="v0-75-0-→-v0-75-1" tabindex="-1">v0.75.0 → v0.75.1 <a class="header-anchor" href="#v0-75-0-→-v0-75-1" aria-label="Permalink to &quot;v0.75.0 → v0.75.1&quot;">​</a></h3><ul><li><strong><code>SOVEREIGN_OFFLINE_SESSION_TTL_SECONDS</code> is removed.</strong> It configured the offline session assertion (research 0012, workstream 0008 legs 2a/2b), an offline-access mechanism found via live testing to have a real authentication bypass: <code>next-pwa</code>&#39;s own default caching of <code>/</code> never went through it at all, so a signed-out user&#39;s cached shell replayed offline regardless of session validity. Offline access to non-neutral pages (i.e. everything except manifest-declared <code>offline: &#39;offline-first&#39; | &#39;device-only&#39;</code> routes) no longer works — those pages now show the generic <code>/offline</code> page when the network is unreachable, rather than a stale, potentially wrong-user copy. Manifest-declared offline-first routes (Launcher, and <code>/</code> itself when Launcher is the platform root) are unaffected and still work offline, via the unrelated neutral-shell mechanism. <ul><li><strong>Action required:</strong> none. If you set this variable, remove it — it is now unread and has no effect. No data, session, or plugin is affected.</li><li>See <code>docs/architecture-rules.md</code>&#39;s &quot;cached authenticated document&quot; rule for the current mechanism.</li></ul></li></ul><h3 id="v0-74-→-v0-75-root-package-json-0-76-1-→-0-77-0" tabindex="-1">v0.74 → v0.75 (root <code>package.json</code> 0.76.1 → 0.77.0) <a class="header-anchor" href="#v0-74-→-v0-75-root-package-json-0-76-1-→-0-77-0" aria-label="Permalink to &quot;v0.74 → v0.75 (root `package.json` 0.76.1 → 0.77.0)&quot;">​</a></h3><p><strong>Breaking — database environment variables changed, and SQLite at-rest encryption was retired.</strong> No application code changes for operators, but every deployment&#39;s <code>.env</code>/compose config needs updating before this version will boot.</p><ul><li><strong><code>DATABASE_URL</code> and <code>AUTH_DATABASE_URL</code> are gone.</strong> <code>DB_DIALECT</code> is now the sole source of truth for the dialect — <strong>required</strong>, no default, no inference from a URL scheme. Postgres additionally requires the new <code>POSTGRES_DB_URL</code> (read identically by the runtime and the auth server — there is no longer a separate auth-specific database variable). SQLite needs nothing else here (see below). <ul><li><strong>Action required:</strong> set <code>DB_DIALECT=sqlite</code> or <code>DB_DIALECT=postgres</code> explicitly. On Postgres, rename <code>DATABASE_URL</code>/<code>AUTH_DATABASE_URL</code> to a single <code>POSTGRES_DB_URL</code>.</li></ul></li><li><strong>SQLite has no plain-file fallback anymore — sqld is now baked into the base Docker Compose files</strong> (<code>docker-compose.yml</code> / <code>docker-compose.prod.yml</code>), not a separate opt-in overlay (<code>docker-compose.sqld.yml</code> is deleted). A plain <code>docker compose up --build</code> now also starts the <code>sqld</code> service automatically. <ul><li><strong>Action required for a non-Docker SQLite deployment:</strong> run an sqld instance yourself and point <code>SQLD_URL</code>/<code>SQLD_ADMIN_URL</code> at it — there is no more &quot;just open <code>data/sovereign.db</code>&quot; fallback.</li></ul></li><li><strong>Auth now gets its own dedicated store</strong> instead of sharing the platform&#39;s — a Postgres schema (<code>sovereign_auth</code>) or an sqld namespace of the same name, mirroring how every isolated plugin already gets its own schema/namespace. On SQLite this matches existing behavior (auth already had its own <code>&#39;auth&#39;</code> sqld namespace, just renamed). <strong>On Postgres this is new</strong>: auth&#39;s tables move out of <code>public</code> into <code>sovereign_auth</code>. <ul><li><strong>Action required on an existing Postgres instance:</strong> auth&#39;s existing data (better-auth&#39;s <code>user</code>/<code>session</code>/<code>account</code>/<code>verification</code> tables, plus this platform&#39;s own <code>invites</code>/<code>auth_settings</code>/<code>auth_email_delivery_log</code>) needs migrating into the new schema before upgrading, or auth starts against a fresh, empty schema. There is no automated migration tool for this yet — take a <code>pg_dump</code> backup first, then move the tables manually (<code>ALTER TABLE ... SET SCHEMA sovereign_auth</code>) before starting this version.</li></ul></li><li><strong><code>SOVEREIGN_DB_ENCRYPTION_KEY</code> and SQLite at-rest encryption (RFC 0071) are retired</strong> — <code>sv db encrypt</code>/<code>sv db decrypt</code> are removed. Neither dialect has an application-level at-rest encryption option now; rely on disk/volume-level encryption if this matters for your deployment. A future resolution covering both dialects is tracked separately, not yet designed. <ul><li><strong>Action required if you had encryption enabled:</strong> decrypt your data with the previous version&#39;s <code>sv db decrypt</code> <strong>before</strong> upgrading — there is no decrypt path in this version.</li></ul></li><li>The manifest&#39;s <code>database</code> field is removed entirely (<code>database.isolation</code> was already retired; <code>database.requireEncryption</code> goes with this change) — a plugin manifest still declaring it fails validation. No first-party plugin in this repository declares it.</li><li><code>@sovereignfs/manifest</code> <strong>4.0.0 → 5.0.0</strong>, <code>@sovereignfs/db</code> <strong>3.4.1 → 4.0.0</strong>, <code>@sovereignfs/auth</code> <strong>1.1.0 → 2.0.0</strong> (all breaking — see above).</li></ul><h3 id="root-package-json-0-50-1-→-0-51-0-no-runtime-version-bump" tabindex="-1">Root <code>package.json</code> 0.50.1 → 0.51.0 (no <code>runtime</code> version bump) <a class="header-anchor" href="#root-package-json-0-50-1-→-0-51-0-no-runtime-version-bump" aria-label="Permalink to &quot;Root `package.json` 0.50.1 → 0.51.0 (no `runtime` version bump)&quot;">​</a></h3><ul><li><strong>Console&#39;s &quot;Add a plugin&quot; panel is removed.</strong> Console → Plugins no longer has the two-step install form (paste a Git repository URL → <strong>Check</strong> a fetched <code>manifest.json</code> → <strong>Install</strong>). Installing a plugin is now an operator action performed outside the web UI. <ul><li><strong>Action required if you installed plugins through Console:</strong> use the CLI instead — <code>pnpm sv plugin add &lt;repo-url&gt;</code> (add <code>--token-env &lt;VAR&gt;</code> for a private repository), or declare the plugin in <code>sovereign.plugins.json</code> and rebuild. Both paths already existed and are unchanged; see <code>docs/self-hosting.md</code> and <code>docs/plugin-development.md</code>.</li><li><strong>Removing</strong> a plugin from Console is unchanged — the per-row Remove action and its confirm dialog stay exactly as they were.</li><li><strong>No data or plugin is affected by upgrading.</strong> Plugins installed via the old panel were cloned into <code>plugins/&lt;id&gt;/</code> by the same <code>sv plugin add</code> the CLI runs, so they remain installed, enabled, and untouched.</li><li>Only <code>@sovereignfs/plugin-console</code> code changed (<strong>→ 0.26.0</strong>); no <code>runtime</code>, <code>@sovereignfs/db</code>, or <code>@sovereignfs/auth</code> code is touched, and no schema or configuration change is involved.</li></ul></li></ul><h3 id="root-package-json-0-47-0-→-0-48-0-no-runtime-version-bump" tabindex="-1">Root <code>package.json</code> 0.47.0 → 0.48.0 (no <code>runtime</code> version bump) <a class="header-anchor" href="#root-package-json-0-47-0-→-0-48-0-no-runtime-version-bump" aria-label="Permalink to &quot;Root `package.json` 0.47.0 → 0.48.0 (no `runtime` version bump)&quot;">​</a></h3><ul><li><strong>Default plugin bundle reduced to Sovereign Tasks only — epic task 3.31 done.</strong> Previously, <code>sovereign.plugins.json</code> was committed at the repo root with 16 entries (every first-party plugin plus all 7 example plugins), so a fresh checkout, <code>pnpm dev</code>, and every CI-published image shipped all of them baked in. <code>sovereign.plugins.json</code> is now a <strong>local, gitignored</strong> file — a fresh checkout has none, and <code>scripts/install-plugins.ts</code> falls back to the newly-committed <code>sovereign.plugins.default.json</code>, which declares only <strong>Sovereign Tasks</strong>. The platform plugins (console/launcher/account) are unaffected — they ship in this repository regardless. <ul><li><strong>Action required if you rely on a previously-bundled plugin</strong> (Plainwrite, Shopper, Wallet, Tritext, Healthlog, Ledger, Tally, Docs, or any example plugin): before your next rebuild, create your own <code>sovereign.plugins.json</code> at the repo root (copy <code>sovereign.plugins.default.json</code> as a starting template) declaring every plugin you want, then rebuild. See <code>docs/self-hosting.md</code>&#39;s &quot;Bundled default plugins&quot; section for the two supported ways to carry a custom <code>sovereign.plugins.json</code> into a production build.</li><li><strong>No data is affected either way.</strong> Dropping a plugin from the image never touches its database tables — disabling or losing a plugin&#39;s code just means its data sits untouched until the plugin is re-declared and the image rebuilt.</li><li><strong>The official, CI-published images</strong> (<code>SOVEREIGN_VERSION=...</code>, no local Dockerfile) always use the new Tasks-only default from this version forward — they build from a clean checkout with no local <code>sovereign.plugins.json</code> to pick up.</li><li>This is a build-tooling/CI change with no <code>runtime</code>, <code>@sovereignfs/db</code>, or <code>@sovereignfs/auth</code> code touched — only the root <code>package.json</code> (roadmap milestone) bumps.</li></ul></li></ul><h3 id="v0-57-→-v0-58" tabindex="-1">v0.57 → v0.58 <a class="header-anchor" href="#v0-57-→-v0-58" aria-label="Permalink to &quot;v0.57 → v0.58&quot;">​</a></h3><ul><li><strong>External OAuth 2.0 / OIDC provider for non-plugin apps (RFC 0072) — epic task 1.18 done.</strong> <code>apps/auth</code> can now act as an identity provider for standalone external apps that are not Sovereign plugins (e.g. a companion app on its own domain wanting &quot;log in with Sovereign&quot;), via <code>@better-auth/oauth-provider</code>. <strong>No action required</strong> for existing instances — the feature is inert until an admin registers a client. <ul><li>New Console section <strong>External clients</strong> (Console → External clients, gated to <code>platform:admin</code>/<code>platform:owner</code>) to register, rotate, and revoke clients. No dynamic/self-service client registration in v1.</li><li>New discovery/token endpoints reachable at the auth server&#39;s public URL: <code>/.well-known/openid-configuration</code>, <code>/oauth2/authorize</code>, <code>/oauth2/token</code>, <code>/oauth2/userinfo</code>, <code>/.well-known/jwks.json</code>. See <code>docs/self-hosting.md</code>&#39;s &quot;External OAuth/OIDC provider&quot; section.</li><li>New consent page at <code>/oauth2/consent</code> on the auth server.</li><li>The plugin&#39;s own schema (<code>oauthClient</code>, <code>oauthAccessToken</code>, <code>oauthRefreshToken</code>, <code>oauthConsent</code>) is auto-discovered and created by better-auth&#39;s existing migrator (<code>apps/auth/src/migrate.ts</code>) on next startup — no manual migration step.</li><li><code>better-auth</code> bumped <code>^1.6.16</code> → <code>^1.6.25</code> (non-breaking, same major) — required by <code>@better-auth/oauth-provider</code>&#39;s peer range. The bundled <code>oidc-provider</code> plugin this repo previously considered is deprecated as of this better-auth version in favor of the new dedicated package; RFC 0072 was updated to build on the latter instead.</li></ul></li><li><strong><code>runtime</code> → 0.58.0</strong>, <strong><code>@sovereignfs/auth</code> → 0.14.0</strong>, <strong><code>@sovereignfs/plugin-console</code> → 0.25.0</strong>.</li></ul><h3 id="v0-56-→-v0-57" tabindex="-1">v0.56 → v0.57 <a class="header-anchor" href="#v0-56-→-v0-57" aria-label="Permalink to &quot;v0.56 → v0.57&quot;">​</a></h3><ul><li><strong>Opt-in single-key SQLite at-rest encryption (RFC 0071) — epic task 8.14 done.</strong> <code>SOVEREIGN_DB_ENCRYPTION_KEY</code> (presence is the toggle, off by default) encrypts <code>sovereign.db</code>, <code>auth.db</code>, and every isolated plugin database via SQLCipher (<code>better-sqlite3-multiple-ciphers</code>). <strong>No action required</strong> for instances that don&#39;t set this variable — behavior is byte-for-byte unchanged. To adopt it on an existing plaintext instance, stop the server and run <code>sv db encrypt</code> (see <code>docs/self-hosting.md</code>&#39;s &quot;SQLite at-rest encryption&quot; section for the full procedure, including the named-volume production path via the new <code>tools</code> Docker Compose service/profile — <code>docker compose -f docker-compose.prod.yml --profile tools run --rm tools pnpm sv db encrypt --dataDir /app/data</code>). Protects a stolen disk, leaked volume snapshot, or copied <code>.db</code> file — it does not protect against a live compromised process or an operator who holds the key (see RFC 0060&#39;s client-side encryption for that tier).</li><li><strong>New manifest field <code>database.requireEncryption</code></strong> — raise-only: a plugin can force its own isolated SQLite database to be encrypted, but can never opt out of encryption the operator has enabled instance-wide. Enforced both at manifest-validation time and at runtime plugin startup (fails naming the plugin for SQLite, warns for Postgres, which has no SQLCipher equivalent).</li><li><strong><code>runtime</code> → 0.57.0</strong>, <strong><code>@sovereignfs/db</code> → 1.14.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.23.0</strong> (this release also carries RFC 0062&#39;s <code>mailer:sendExternal</code> permission from the same manifest version).</li></ul><h3 id="v0-55-→-v0-56" tabindex="-1">v0.55 → v0.56 <a class="header-anchor" href="#v0-55-→-v0-56" aria-label="Permalink to &quot;v0.55 → v0.56&quot;">​</a></h3><ul><li><strong>Notification transport default changed from <code>polling</code> to <code>sse</code> (RFC 0034).</strong> Newly-provisioned instances now get instant in-process push delivery for free (no extra infra) instead of a 10s polling loop. <strong>Action required for multi-container/multi-process deployments only</strong>: <code>sse</code> uses an in-process <code>EventEmitter</code>, which cannot see notifications published on a different runtime container — if you scale to more than one runtime replica, set <code>NOTIFICATION_TRANSPORT=redis</code> (with <code>REDIS_URL</code>) explicitly, or it will silently degrade to per-user missed real-time updates (nothing is lost — the next explicit fetch always catches up — it just stops being instant). Single-container deployments (this repo&#39;s default <code>docker-compose.prod.yml</code> topology) are unaffected and need no action.</li><li><strong>Fixed: the notification broker singleton was never actually visible to API routes.</strong> <code>runtime/src/notification-broker.ts</code> stored its broker instance in a module-level variable, but Next.js compiles <code>instrumentation.ts</code> (where <code>initBroker()</code> runs) into a separate module graph from route handlers (where <code>getBroker()</code> is read) — even in the standalone server this app ships in Docker. In practice this meant <code>sse</code> and <code>redis</code> transport modes silently no-op&#39;d back to polling for every request, regardless of configuration, since RFC 0034 shipped. The broker is now stored on <code>globalThis</code>, which is shared across all of Next.js&#39;s separately-bundled chunks within a process. <strong>If you already had <code>NOTIFICATION_TRANSPORT=sse</code> or <code>=redis</code> set</strong>, this fix is what actually makes it take effect for the first time — expect the bell to switch from polling to push delivery after this upgrade.</li><li><strong>Fixed: <code>NotificationBell</code> fetched, polled, and (once the bug above is fixed) opened an <code>EventSource</code> twice per page</strong> — the sidebar and mobile header both always mount the bell (visibility is CSS-only), and each ran its own independent fetch/poll/SSE loop. It now shares one connection across both mounted instances via a module-level store. Internal-only, no operator action.</li><li><strong><code>runtime</code> → 0.56.0</strong>.</li></ul><h3 id="v0-54-→-v0-55" tabindex="-1">v0.54 → v0.55 <a class="header-anchor" href="#v0-54-→-v0-55" aria-label="Permalink to &quot;v0.54 → v0.55&quot;">​</a></h3><ul><li><strong>Plugin mailer permission and SDK email surface (RFC 0062) — epic task 3.26 done.</strong> <code>sdk.mailer.send()</code> now resolves the calling plugin&#39;s ID from an explicitly passed request <code>Headers</code> object (pass <code>await headers()</code> as the second argument, same convention as <code>sdk.notifications.send()</code>) and rejects the call unless the plugin declares <code>mailer:send</code> — a plugin without it gets a clear permission error instead of a silent send. A raw <code>to:</code> address is treated as an external recipient regardless of whether it happens to match a platform user&#39;s email, and additionally requires the new <code>mailer:sendExternal</code> permission. Both plugin email surfaces are now rate-limited per plugin and per recipient. New additive <code>sdk.email.sendToUser({ recipientUserId, templateId, subject, html?, text?, data? })</code> is the recommended default: the platform resolves the recipient&#39;s email server-side and only requires <code>mailer:send</code>, so there is no external-recipient escape hatch to reason about. <strong>Action required for any plugin already calling <code>sdk.mailer.send()</code>:</strong> declare <code>mailer:send</code> (and <code>mailer:sendExternal</code> if the recipient isn&#39;t a platform user resolved by ID) in <code>manifest.json</code>, and pass <code>await headers()</code> as the second argument — no plugin in this monorepo currently calls <code>sdk.mailer.send()</code>, so this has no first-party impact.</li><li><strong><code>runtime</code> → 0.55.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.23.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.26.0</strong>.</li></ul><h3 id="v0-53-→-v0-54" tabindex="-1">v0.53 → v0.54 <a class="header-anchor" href="#v0-53-→-v0-54" aria-label="Permalink to &quot;v0.53 → v0.54&quot;">​</a></h3><ul><li><strong>Public plugin page routes (RFC 0042) — epic task 2.14 done.</strong> New optional manifest <code>publicRoutes</code> field lets a plugin exempt a path prefix (relative to its own <code>routePrefix</code>) from the platform&#39;s session-redirect gate — for shared documents, published read-only views, or token-protected previews. The plugin itself is responsible for authorizing every request under a public route and must fail closed (404) for anything invalid, expired, or unknown. Disabled-plugin and paywall gates still apply: a monetized plugin&#39;s public routes block anonymous access by default (there is no <code>paywallExempt</code> escape hatch). <strong>No action required</strong> — purely additive; plugins that don&#39;t declare <code>publicRoutes</code> are unaffected.</li><li><strong><code>runtime</code> → 0.54.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.22.0</strong>.</li></ul><h3 id="v0-42-→-v0-43" tabindex="-1">v0.42 → v0.43 <a class="header-anchor" href="#v0-42-→-v0-43" aria-label="Permalink to &quot;v0.42 → v0.43&quot;">​</a></h3><ul><li><strong>Cross-plugin references and dependency discovery (RFC 0051) — epic task 3.20 done.</strong> New <code>sdk.plugins.get(id)</code> / <code>sdk.plugins.list(filter?)</code> return <code>PluginAvailability</code> (install/enable status folded with the current user&#39;s disabled/adminOnly/paywall gates) so a plugin can check an optional sibling before offering an integration. <code>sdk.plugins.getConsentStatus(ref)</code> checks a data-contract consent grant without a full <code>sdk.data.query()</code> call. New <code>PluginReference</code> type defines the standard opaque-link shape (<code>providerId</code>, <code>resourceType</code>, <code>resourceId</code>, <code>contract?</code>, <code>version?</code>, <code>labelSnapshot?</code>, <code>metadata?</code>, <code>linkedAt</code>) for a plugin to store a pointer to another plugin&#39;s record without a cross-plugin foreign key. <strong>No action required</strong> — purely additive SDK surface, no schema change.</li><li><strong>New optional manifest <code>integrations.optional</code> field</strong> — informational sibling-plugin metadata for install/discovery UX (Console, Account, plugin UI hints). Declaring one grants nothing by itself; never an install blocker.</li><li><strong>Plugin portability export metadata (RFC 0052) complete — epic task 8.8 done.</strong> <code>PluginExportSection</code> gains <code>references?: PluginReference[]</code>, carried through export and import as <strong>inert metadata only</strong> — the platform never dereferences them; importing a reference never grants access to the provider plugin. This was the last outstanding RFC 0052 deliverable (the rest shipped in the v0.41 → v0.42 entry below).</li><li><strong><code>runtime</code> → 0.43.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.25.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.20.0</strong>.</li></ul><h3 id="v0-41-→-v0-42" tabindex="-1">v0.41 → v0.42 <a class="header-anchor" href="#v0-41-→-v0-42" aria-label="Permalink to &quot;v0.41 → v0.42&quot;">​</a></h3><ul><li><strong>Plugin portability export metadata (RFC 0052, partial).</strong> <code>PluginExportSection</code> gains <code>pluginVersion</code> (always overwritten by the runtime from the installed manifest — a resolver cannot misreport it), <code>secretMetadata</code> (per-plugin secret metadata, never plaintext values), and <code>warnings</code>. <code>ExportContext</code> gains <code>options: { includeFiles }</code> so a resolver can honor the user&#39;s choice to skip large attachments. All additive — existing resolvers that don&#39;t read these fields are unaffected.</li><li><strong>Account → Data export gains an &quot;include files and attachments&quot; toggle</strong> (<code>?includeFiles=false</code> on <code>GET /api/account/export</code>), and a throwing plugin exporter no longer aborts the whole export — it&#39;s excluded and recorded in the bundle manifest&#39;s new <code>failures</code> list.</li><li><strong>Not yet implemented</strong>: cross-plugin reference preservation in exports (depends on RFC 0051, still unimplemented) — the remaining piece of RFC 0052.</li><li><strong><code>runtime</code> → 0.42.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.24.0</strong>, <strong><code>plugins/account</code> → 0.17.0</strong>.</li></ul><h3 id="v0-40-→-v0-41" tabindex="-1">v0.40 → v0.41 <a class="header-anchor" href="#v0-40-→-v0-41" aria-label="Permalink to &quot;v0.40 → v0.41&quot;">​</a></h3><ul><li><strong>Client-side encryption core complete, steps 6 of 7 (RFC 0060) — epic task 8.9 done.</strong> Account export now includes a user&#39;s encryption profile, recovery wrapper, and active device enrollments (still wrapped ciphertext and non-sensitive KDF/algorithm metadata only — never plaintext). Account deletion already removed this data unconditionally; export was the missing half. <strong>No action required</strong> — no schema change, existing <code>e2ee_*</code> tables and DB helper functions were reused as-is.</li><li><strong>Import is additive-safe, never destructive.</strong> If the importing user already has an encryption profile on this instance, imported e2ee data is silently skipped rather than overwriting a live setup that might not correspond to the same Client Master Key.</li><li><strong>No <code>sdk.storage</code>/<code>sdk.portability</code> API change.</strong> Plugin-owned encrypted objects (e.g. a future Sovereign Wallet document) already compose through each plugin&#39;s own export/import handlers calling <code>sdk.storage</code> directly — documented in <code>docs/plugin-development.md</code>, no new platform mechanism needed.</li><li><strong><code>docs/security.md</code> corrected</strong> — it previously described client-side encryption as &quot;post-v1 (charted)&quot;; it&#39;s implemented and opt-in as of this release.</li><li><strong><code>runtime</code> → 0.41.0</strong>. No SDK/db/manifest version change — this task only touched the platform&#39;s internal export/import wiring (<code>runtime/src/portability/</code>), not any published package&#39;s contract.</li></ul><h3 id="v0-39-→-v0-40" tabindex="-1">v0.39 → v0.40 <a class="header-anchor" href="#v0-39-→-v0-40" aria-label="Permalink to &quot;v0.39 → v0.40&quot;">​</a></h3><ul><li><strong>Client-side encryption core, steps 1–5 of 7 (RFC 0060).</strong> New <code>sdk.e2ee</code> persistence surface (profile/recovery-wrapper/device-enrollment plumbing) and a new Account → Security section for setup, recovery-secret unlock, and device enrollment/revocation. Three new platform tables (<code>e2ee_profiles</code>, <code>e2ee_recovery_wrappers</code>, <code>e2ee_device_enrollments</code>) auto-create via the existing bootstrap DDL path — <strong>no action required</strong>. All CMK/DEK generation, wrap/unwrap, and object encryption happens in the browser (WebCrypto); the server only ever stores wrapped ciphertext and non-sensitive KDF/algorithm metadata, never plaintext key material.</li><li><strong>New optional <code>e2ee:use</code> manifest permission</strong>, reserved for a future plugin consumer (not yet declared by any first-party plugin).</li><li><strong><code>@sovereignfs/sdk</code> gains <code>sdk.e2ee.*</code></strong> plus browser-only helpers across four subpaths: <code>generateCmk</code>/<code>wrapCmkWithRecoverySecret</code>/etc. (<code>e2ee-crypto</code>, also <code>generateDek</code>/<code>wrapDekWithCmk</code>/<code>unwrapDekWithCmk</code> for per-object keys), <code>getOrCreateDeviceId</code>/<code>storeDeviceKey</code>/etc. (<code>e2ee-device</code>), <code>encryptBlob</code>/<code>decryptBlob</code>/<code>encryptJson</code>/<code>decryptJson</code> (<code>e2ee-object</code>), and <code>getE2eeLocalState</code> for normalized locked/unlocked state detection (<code>e2ee-state</code>). Additive, experimental tier — no existing SDK surface changed.</li><li><strong><code>sdk.storage</code> integrated with client-side encryption (step 5).</strong> No dedicated storage method was needed — <code>StorageObject.metadata</code> (see the <code>@sovereignfs/sdk</code> 1.22.0 → 1.23.0 migration note below) is where an encrypted object&#39;s wrapped DEK and algorithm version travel alongside its ciphertext.</li><li><strong>Not yet implemented</strong>: export/delete hook integration — the final step of the same RFC 0060 adoption path.</li><li><strong><code>runtime</code> → 0.40.1</strong>, <strong><code>@sovereignfs/sdk</code> → 1.23.0</strong>, <strong><code>@sovereignfs/db</code> → 1.10.2</strong>, <strong><code>@sovereignfs/manifest</code> → 0.19.0</strong>, <strong><code>plugins/account</code> → 0.16.1</strong>. <code>db</code> 1.10.2 adds a missing unique index on <code>(tenant_id, user_id)</code> for <code>e2ee_profiles</code>/<code>e2ee_recovery_wrappers</code> (migration <code>0013</code>) — the initial <code>0012</code> migration created the tables without it, so the recovery-wrapper upsert&#39;s <code>ON CONFLICT</code> clause had no matching constraint to target and threw on first use.</li></ul><h3 id="v0-37-→-v0-38" tabindex="-1">v0.37 → v0.38 <a class="header-anchor" href="#v0-37-→-v0-38" aria-label="Permalink to &quot;v0.37 → v0.38&quot;">​</a></h3><ul><li><strong>Plugin background schedules — Phase 1 (RFC 0046).</strong> Plugins can now declare recurring server-side jobs (manifest <code>schedules</code>) that the runtime invokes in-process on a 60-second tick, independent of any browser request. <strong>No action required</strong> — the scheduler starts automatically at boot and is a no-op when no installed plugin declares a schedule.</li><li><strong>New optional <code>SOVEREIGN_SCHEDULER_DISABLED</code> env var.</strong> Set to <code>1</code> to stop the runtime invoking plugin schedules — a kill-switch for debugging a misbehaving plugin job. Unset (default) = scheduler runs. See <a href="./self-hosting.html#environment-variables"><code>docs/self-hosting.md</code></a>.</li><li><strong><code>@sovereignfs/sdk</code> gains <code>ScheduleContext</code>/<code>ScheduleHandler</code> types.</strong> Additive, experimental tier — no existing SDK surface changed.</li><li><strong>No database schema change</strong> at the platform layer. Individual plugins that adopt <code>schedules</code> may ship their own migrations (e.g. <code>sovereign-tasks</code> 0.11.0&#39;s <code>tasks_notification_prefs</code> table).</li><li><strong><code>runtime</code> → 0.38.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.18.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.18.0</strong>.</li></ul><h3 id="v0-27-→-v0-28" tabindex="-1">v0.27 → v0.28 <a class="header-anchor" href="#v0-27-→-v0-28" aria-label="Permalink to &quot;v0.27 → v0.28&quot;">​</a></h3><ul><li><strong>White-labeling Phase 1 (RFC 0027).</strong> Operators can now set a brand name, primary colour, logo, and favicon via Console → Settings → Branding. A new <code>tenant_branding</code> table is created by the Drizzle migration automatically on startup — <strong>no manual step required</strong>.</li><li><strong><code>sdk.platform.getConfig()</code> gains <code>brandName</code> and <code>brandPrimaryColor?</code>.</strong> Existing calls are unaffected; the fields are additive.</li><li><strong>Three new <code>--sv-brand-*</code> CSS tokens</strong> (<code>--sv-brand-logo</code>, <code>--sv-brand-logo-dark</code>, <code>--sv-brand-favicon</code>) are set at <code>:root</code> by <code>BrandProvider</code> and are available in plugin CSS without any import.</li><li><strong>Seven new optional <code>BRAND_*</code> env vars</strong> control deployment-level brand defaults. All are optional; Sovereign defaults apply when unset. See <a href="./self-hosting.html#environment-variables"><code>docs/self-hosting.md</code></a>.</li><li><strong><code>runtime</code> → 0.28.0</strong>, <strong><code>@sovereignfs/db</code> → 1.6.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.10.0</strong>, <strong><code>@sovereignfs/ui</code> → 0.10.0</strong>, <strong><code>plugins/console</code> → 0.12.0</strong>.</li></ul><h3 id="v0-26-→-v0-27" tabindex="-1">v0.26 → v0.27 <a class="header-anchor" href="#v0-26-→-v0-27" aria-label="Permalink to &quot;v0.26 → v0.27&quot;">​</a></h3><ul><li><strong>Production dev-mode &amp; diagnostics (RFC 0020).</strong> A request-scoped dev-mode switch (<code>SOVEREIGN_DEV_MODE_ENABLED=true</code>) routes SDK DB calls to a mock database for a single request when the correct <code>X-Sovereign-Dev-Mode-Secret</code> header is present. Real requests are completely unaffected. Off by default.</li><li><strong>Structured logging.</strong> <code>LOG_LEVEL</code> (error/warn/info/debug; default <code>warn</code>) controls a newline-delimited JSON logger to stdout. No egress.</li><li><strong>Richer <code>/api/admin/health</code>.</strong> Now includes <code>database.migrationVersion</code>, <code>plugins.installed</code>/<code>adminOnly</code>, and <code>diagnostics.{logLevel, devModeEnabled}</code>.</li><li><strong>Four new optional env vars:</strong> <code>LOG_LEVEL</code>, <code>SOVEREIGN_DEV_MODE_ENABLED</code>, <code>SOVEREIGN_DEV_MODE_SECRET</code>, <code>SOVEREIGN_DEV_DATABASE_URL</code>. All documented in <code>.env.example</code> and <code>docs/self-hosting.md</code>.</li><li><strong><code>runtime</code> → 0.27.0</strong>.</li></ul><h3 id="v0-25-→-v0-26" tabindex="-1">v0.25 → v0.26 <a class="header-anchor" href="#v0-25-→-v0-26" aria-label="Permalink to &quot;v0.25 → v0.26&quot;">​</a></h3><ul><li><strong>Per-plugin isolated databases (RFC 0004).</strong> Plugins can now opt into a dedicated database by setting <code>&quot;database&quot;: &quot;isolated&quot;</code> in their manifest. This is <strong>entirely opt-in</strong> — existing <code>shared</code> plugins (the default) are completely unaffected and require no changes.</li><li><strong>No action required for operators.</strong> The <code>data/plugins/</code> directory is created automatically when the first isolated plugin provisions its store. It is included in <code>sv backup</code> archives automatically. Nothing in the existing platform schema changes.</li><li><strong>Plugin authors adopting <code>isolated</code>:</strong> set <code>&quot;database&quot;: &quot;isolated&quot;</code> in your <code>manifest.json</code>, create <code>plugins/&lt;id&gt;/migrations/sqlite/</code> and <code>plugins/&lt;id&gt;/migrations/postgres/</code> directories for your Drizzle migration files, and remove the slug prefix from your table names (optional but recommended — you have your own namespace). <code>sdk.db.getClient()</code> call sites need no changes.</li><li><strong><code>sv plugin remove</code> now drops the store.</strong> When removing an isolated plugin, <code>sv plugin remove &lt;id&gt;</code> deletes the store (SQLite file or Postgres schema). Pass <code>--keep-data</code> to retain the store for manual inspection or migration.</li><li><strong><code>@sovereignfs/db</code> → 1.5.0</strong> (minor — new <code>plugin-client.ts</code> with <code>getPluginDb</code>, <code>provisionPluginDb</code>, <code>dropPluginDb</code>, <code>pluginMigrationsFolder</code>; <code>runPluginMigrations</code> added to migrate module).</li><li><strong><code>@sovereignfs/sdk</code> → 1.9.0</strong> (minor — <code>sdk.db.getClient()</code> now routes isolated plugins to their dedicated store transparently; <code>SdkHost.db.getClient</code> signature change is internal only).</li><li><strong><code>runtime</code> → 0.26.0</strong> (minor — SDK host routes isolated plugins; startup runs per-plugin migrations).</li></ul><p>See <a href="./plugin-database.html"><code>docs/plugin-database.md</code></a> for the full reference.</p><h3 id="v0-24-→-v0-25" tabindex="-1">v0.24 → v0.25 <a class="header-anchor" href="#v0-24-→-v0-25" aria-label="Permalink to &quot;v0.24 → v0.25&quot;">​</a></h3><ul><li><strong>Plugin monetization (RFC 0003).</strong> Plugin authors can now declare a <code>monetization</code> field in <code>manifest.json</code> to gate access with signed Ed25519 license tokens. A new <code>entitlements</code> table is created by the Drizzle migration automatically on startup — <strong>no manual schema change required</strong>.</li><li><strong>No action required for most operators.</strong> Plugins without a <code>monetization</code> field (or with <code>model: &quot;free&quot;</code>) are completely unaffected. Monetization is opt-in by plugin authors.</li><li><strong>Users with a paid plugin:</strong> if a user lacks an entitlement for a plugin, they are redirected to the plugin&#39;s paywall page (<code>/paywall/&lt;pluginId&gt;</code>) where they can paste a signed license token obtained from the plugin author. Once imported, access is granted immediately without restart. Users can manage their licenses in Account → Billing.</li><li><strong>Admins:</strong> Console → Entitlements shows all entitlements across all users. Admin key-authenticated <code>GET /api/admin/entitlements</code> returns the full list or (with <code>?userId=</code>) the set of paywalled plugin IDs for a specific user (used by the middleware).</li><li><strong>No Stripe / payment gateway required in v1.</strong> The platform implements only the offline Ed25519 license-token model (manual flow). Webhook integration with Stripe or other gateways is a post-v1 concern; plugin authors who want automated billing today can build their own webhook handler.</li><li><strong><code>@sovereignfs/db</code> → 1.3.0</strong> (minor — <code>entitlements</code> table + 7 helper functions).</li><li><strong><code>@sovereignfs/sdk</code> → 1.8.0</strong> (minor — <code>sdk.billing</code> stub: <code>getEntitlement()</code> and <code>requireEntitlement()</code> exported; <code>EntitlementRequiredError</code> exported).</li><li><strong><code>@sovereignfs/manifest</code> → 0.14.0</strong> (minor — <code>monetization</code> manifest field).</li></ul><h3 id="v0-23-→-v0-24" tabindex="-1">v0.23 → v0.24 <a class="header-anchor" href="#v0-23-→-v0-24" aria-label="Permalink to &quot;v0.23 → v0.24&quot;">​</a></h3><ul><li><strong>Web Push notifications (RFC 0016).</strong> Background push delivery for the in-app inbox. A new <code>push_subscriptions</code> table is created by the Drizzle migration automatically on startup — <strong>no manual schema change required</strong>.</li><li><strong>No action required for most operators.</strong> Push is opt-in and silently disabled when VAPID keys are absent. The in-app bell continues to work without any configuration.</li><li><strong>To enable push:</strong> generate a VAPID key pair once per deployment and add to <code>.env</code>:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> web-push</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generate-vapid-keys</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># then add to .env:</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># VAPID_PUBLIC_KEY=&lt;base64url public key&gt;</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># VAPID_PRIVATE_KEY=&lt;base64url private key&gt;</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># VAPID_CONTACT=mailto:admin@example.com</span></span></code></pre></div>Users then opt in per-device via Account → Notifications → &quot;Enable push notifications&quot;. Push respects per-user muted-category preferences (set in Account → Notifications).</li><li><strong>Stale subscriptions are pruned automatically.</strong> When a push service returns <code>410 Gone</code> (device unregistered or browser cleared), the subscription is deleted from the DB.</li><li><strong><code>@sovereignfs/db</code> → 1.2.0</strong> (minor — <code>push_subscriptions</code> table + 6 helper functions).</li></ul><h3 id="v0-22-→-v0-23" tabindex="-1">v0.22 → v0.23 <a class="header-anchor" href="#v0-22-→-v0-23" aria-label="Permalink to &quot;v0.22 → v0.23&quot;">​</a></h3><ul><li><strong>Notification Center (RFC 0015).</strong> In-app per-user notifications with a bell icon in the chrome, polling-based delivery (default 30s), and SSE streaming. Two new platform tables (<code>notifications</code>, <code>notification_prefs</code>) are added by the Drizzle migration automatically on startup.</li><li><strong>No action required for operators.</strong> The migration runs automatically. Existing plugins continue to work without changes.</li><li><strong>Plugin authors:</strong> to send notifications, add <code>&quot;notifications:send&quot;</code> to your manifest <code>permissions</code> and call <code>sdk.notifications.send(input, await headers())</code>. See <code>docs/plugin-development.md</code> for the full API.</li><li><strong>Admin broadcast:</strong> <code>POST /api/admin/broadcast</code> (admin key required) sends announcements to one or more users; rate-limited to once per 60 seconds.</li><li><strong><code>@sovereignfs/db</code> → 1.1.0</strong> (minor — new tables and helper functions).</li><li><strong><code>@sovereignfs/sdk</code> → 1.7.0</strong> (minor — <code>sdk.notifications</code> promoted from <code>NotImplementedError</code> stub to a working implementation; <code>SendNotificationInput</code> type exported).</li><li><strong><code>@sovereignfs/ui</code> → 0.9.0</strong> (minor — <code>Toast</code>/<code>ToastProvider</code>/<code>useToast</code> exported).</li></ul><h3 id="v0-21-→-v0-22" tabindex="-1">v0.21 → v0.22 <a class="header-anchor" href="#v0-21-→-v0-22" aria-label="Permalink to &quot;v0.21 → v0.22&quot;">​</a></h3><ul><li><strong>Plugin-declared capabilities (RFC 0022).</strong> Plugins may now declare a <code>capabilities</code> field in <code>manifest.json</code> to express fine-grained, namespaced permissions (e.g. <code>my-plugin:create-item</code>). This is an additive manifest change — existing manifests without a <code>capabilities</code> field are unaffected.</li><li><strong>No action required for operators.</strong> The platform automatically injects capabilities declared with <code>defaultGrant: &quot;all&quot;</code> into every authenticated session. Plugin authors who want to adopt the feature should see the new <code>### capabilities (RFC 0022)</code> section in <code>docs/plugin-development.md</code>.</li><li><strong><code>@sovereignfs/manifest</code> → 0.13.0</strong> (minor, no breaking changes). The internal manifest schema adds the optional <code>capabilities</code> field and exports a new <code>pluginCapabilityName(pluginId, capName)</code> helper.</li></ul><h3 id="v0-20-→-v0-21" tabindex="-1">v0.20 → v0.21 <a class="header-anchor" href="#v0-20-→-v0-21" aria-label="Permalink to &quot;v0.20 → v0.21&quot;">​</a></h3><ul><li><strong>Platform roles expanded to four (RFC 0021).</strong> The <code>platform:admin</code> role is preserved; two new roles are added: <code>platform:owner</code> (full privileges, including <code>role:assign</code>) and <code>platform:auditor</code> (read-only Console access).</li><li><strong>Automatic migration:</strong> on first startup after upgrading, the auth server promotes the oldest <code>platform:admin</code> user to <code>platform:owner</code> if no owner exists yet. No manual action required.</li><li><strong>Existing <code>platform:admin</code> users retain all their current capabilities.</strong> The admin preset loses only <code>role:assign</code> (the ability to assign roles), which is now owner-exclusive. If you need role assignment to remain with an existing admin, promote them to owner via Console → Users.</li><li><strong><code>platform:owner</code> is protected:</strong> the owner&#39;s role and account active state cannot be changed via the admin API. Use Console → Users (as the owner) or the <code>sv user reset-mfa</code> CLI for break-glass operations.</li><li><strong>SDK <code>@sovereignfs/sdk</code> 1.6.0:</strong> <code>SessionUser</code> gains <code>capabilities</code>, and <code>sdk.auth.hasCapability(session, cap)</code> is added. Plugin authors can use this instead of checking <code>user.role</code> directly. Both are backward-compatible additive additions.</li></ul><h3 id="v0-18-→-v0-19" tabindex="-1">v0.18 → v0.19 <a class="header-anchor" href="#v0-18-→-v0-19" aria-label="Permalink to &quot;v0.18 → v0.19&quot;">​</a></h3><ul><li><strong>Mobile responsiveness &amp; PWA hardening (RFC 0013).</strong> The shell&#39;s mobile footer is replaced by a single &quot;Apps&quot; Drawer button; the header gains an <code>ActivePluginTitle</code>; Console moves into the avatar menu on mobile. Dialog top-inset (<code>--sv-dialog-inset-top</code>) keeps the header visible above overlay sheets. Unified breakpoint at 768 px; <code>100dvh</code> throughout; safe-area insets; 44 px touch targets; <code>viewport-fit=cover</code> + immersive iOS status bar.</li><li><strong>New <code>Drawer</code> UI primitive</strong> added to <code>@sovereignfs/ui</code> (bottom-sheet, focus trap, safe-area-aware).</li><li><strong>PWA manifest polish:</strong> <code>display_override</code>, <code>orientation: &quot;any&quot;</code>, <code>categories: [&quot;productivity&quot;]</code>, <code>shortcuts</code> (Launcher + Account).</li><li><strong>No operator action required.</strong> All changes are in the shell UI.</li><li><strong><code>runtime</code> → 0.19.0</strong>, <strong><code>@sovereignfs/ui</code> → 0.6.0</strong>.</li></ul><h3 id="v0-17-→-v0-18" tabindex="-1">v0.17 → v0.18 <a class="header-anchor" href="#v0-17-→-v0-18" aria-label="Permalink to &quot;v0.17 → v0.18&quot;">​</a></h3><ul><li><strong>Minimal shell mode (RFC 0014).</strong> A <code>shell: &quot;minimal&quot;</code> manifest value now composes plugins into a chrome-free route group (<code>(minimal)/</code>). Previously <code>shell: &quot;minimal&quot;</code> failed the build. Minimal plugins can be used as the root plugin for kiosk use cases.</li><li><strong>No operator action required.</strong> Existing plugins are unaffected. The new route group is entirely generated and gitignored.</li><li><strong><code>runtime</code> → 0.18.0</strong>.</li></ul><h3 id="v0-16-→-v0-17" tabindex="-1">v0.16 → v0.17 <a class="header-anchor" href="#v0-16-→-v0-17" aria-label="Permalink to &quot;v0.16 → v0.17&quot;">​</a></h3><ul><li><strong>Plugin-scoped environment variables (RFC 0018).</strong> Plugins may now declare an <code>env</code> map in <code>manifest.json</code>; keys are auto-namespaced to <code>SV_PLUGIN_&lt;SLUG&gt;_&lt;KEY&gt;</code> (runtime) or <code>NEXT_PUBLIC_SV_PLUGIN_&lt;SLUG&gt;_&lt;KEY&gt;</code> (build). <code>sdk.env.get(&#39;KEY&#39;)</code> reads only the calling plugin&#39;s own vars.</li><li><strong>No operator action required.</strong> The feature is opt-in by plugin authors. Plugin-declared secrets must be supplied at runtime via the namespaced env var.</li><li><strong><code>runtime</code> → 0.17.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.5.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.12.0</strong>.</li></ul><h3 id="v0-15-→-v0-16" tabindex="-1">v0.15 → v0.16 <a class="header-anchor" href="#v0-15-→-v0-16" aria-label="Permalink to &quot;v0.15 → v0.16&quot;">​</a></h3><ul><li><strong>User data portability (RFC 0007).</strong> Users can export all their data as a versioned ZIP (Account → Data → Export) and import it on another instance. Plugin participation is opt-in via <code>sdk.portability.provideExport</code> / <code>provideImport</code> and the <code>data:export</code> / <code>data:import</code> manifest permissions.</li><li><strong>No operator action required.</strong> Export/import routes are session-gated. The 50 MB import cap is enforced by the server.</li><li><strong><code>runtime</code> → 0.16.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.4.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.11.0</strong>, <strong><code>plugins/account</code> → 0.5.0</strong>.</li></ul><h3 id="v0-19-→-v0-20" tabindex="-1">v0.19 → v0.20 <a class="header-anchor" href="#v0-19-→-v0-20" aria-label="Permalink to &quot;v0.19 → v0.20&quot;">​</a></h3><ul><li><p><strong>TOTP and passkey MFA available.</strong> No configuration is required for existing deployments — MFA is opt-in per user. The <code>twoFactor</code> and <code>passkey</code> tables are created automatically by better-auth on first startup.</p></li><li><p><strong>Three new optional env vars</strong> (all have safe defaults for <code>localhost</code>):</p><ul><li><code>AUTH_WEBAUTHN_RP_ID</code> — defaults to the hostname of <code>AUTH_BASE_URL</code>.</li><li><code>AUTH_WEBAUTHN_RP_NAME</code> — defaults to <code>Sovereign</code>.</li><li><code>AUTH_WEBAUTHN_ORIGIN</code> — defaults to <code>SOVEREIGN_AUTH_PUBLIC_URL</code> or <code>AUTH_BASE_URL</code>.</li></ul><p><strong>Production deployments must set these</strong> — the defaults will not work when your instance runs on a real domain. See <a href="./self-hosting.html#two-factor-authentication-mfa">self-hosting.md — Two-factor authentication</a> for the correct values and the <code>rpID</code> constraint.</p></li><li><p><strong><code>sv user reset-mfa &lt;email&gt;</code></strong> — new CLI break-glass command. Clears TOTP secrets and passkeys for a user directly in the SQLite auth database (no running server required). For Postgres instances, use Console → Users → Reset MFA instead.</p></li><li><p>No database migration required — better-auth creates the <code>twoFactor</code> and <code>passkey</code> tables at startup via its own DDL.</p></li></ul><h3 id="v0-13-→-v0-14" tabindex="-1">v0.13 → v0.14 <a class="header-anchor" href="#v0-13-→-v0-14" aria-label="Permalink to &quot;v0.13 → v0.14&quot;">​</a></h3><ul><li><strong>Activity log (RFC 0005).</strong> The <code>activity_log</code> table records platform and plugin actions. <code>sdk.activity.log()</code> is now implemented (no longer a stub). Personal activity appears in Account → Activity; platform-wide history is in Console → Activity. The migration runs automatically on startup.</li><li><strong>Icon system (RFC 0011).</strong> <code>@sovereignfs/ui</code> exports an <code>&lt;Icon&gt;</code> component backed by curated Lucide SVG icons. Plugin manifest <code>icon.svg</code> files are served from <code>/plugin-icons/&lt;id&gt;.svg</code>. Chrome monograms and emoji replaced with icons.</li><li><strong>No operator action required.</strong> All changes are additive.</li><li><strong><code>runtime</code> → 0.14.0</strong> (<code>0.14.1</code> for icon system), <strong><code>@sovereignfs/db</code> → 0.9.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.3.0</strong>, <strong><code>@sovereignfs/ui</code> → 0.5.0</strong>, <strong><code>plugins/account</code> → 0.4.0</strong>, <strong><code>plugins/console</code> → 0.5.0</strong>.</li></ul><h3 id="v0-12-→-v0-13" tabindex="-1">v0.12 → v0.13 <a class="header-anchor" href="#v0-12-→-v0-13" aria-label="Permalink to &quot;v0.12 → v0.13&quot;">​</a></h3><ul><li><strong>Cross-plugin data sharing (RFC 0002).</strong> Provider plugins can call <code>sdk.data.provide(contract, resolver)</code>; consumer plugins call <code>sdk.data.query(ref, params)</code>. Consent is managed by users in Account → Data. Two new tables (<code>consent_grants</code>, <code>data_access_log</code>) are created by migration on startup.</li><li><strong>Manifest:</strong> optional <code>data.provides[]</code> / <code>data.consumes[]</code> fields; <code>data:provide</code> and <code>data:consume</code> permissions promoted from reserved to active.</li><li><strong>No operator action required.</strong> The feature is entirely opt-in by plugin authors.</li><li><strong><code>runtime</code> → 0.13.0</strong>, <strong><code>@sovereignfs/db</code> → 0.8.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.2.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.10.0</strong>, <strong><code>plugins/account</code> → 0.3.0</strong>.</li></ul><h3 id="v0-11-→-v0-12" tabindex="-1">v0.11 → v0.12 <a class="header-anchor" href="#v0-11-→-v0-12" aria-label="Permalink to &quot;v0.11 → v0.12&quot;">​</a></h3><ul><li><strong>Plugin compatibility &amp; versioning (RFC 0024).</strong> The dormant <code>schemaVersion</code> and <code>compatibility.minPlatformVersion</code> fields in <code>manifest.json</code> are now enforced. An incompatible plugin is rejected at <code>sv plugin add</code>, at build, and is disabled at boot (not bricked). An advisory <code>maxPlatformVersion</code> generates a warning. Console shows an &quot;Incompatible&quot; badge; <code>/api/admin/health</code> lists incompatible plugins.</li><li><strong>No operator action required.</strong> Existing plugins without a <code>compatibility</code> field are unaffected. Platform plugins ship with appropriate <code>minPlatformVersion</code>.</li><li><strong><code>runtime</code> → 0.12.0</strong>, <strong><code>@sovereignfs/manifest</code> → 0.9.0</strong>.</li></ul><h3 id="v0-10-→-v0-11" tabindex="-1">v0.10 → v0.11 <a class="header-anchor" href="#v0-10-→-v0-11" aria-label="Permalink to &quot;v0.10 → v0.11&quot;">​</a></h3><ul><li><strong>SDK distribution &amp; plugin isolation boundary (RFC 0023).</strong> <code>@sovereignfs/sdk</code> now has zero runtime dependencies. Implementations are host-provided by the runtime at startup via <code>provideHost()</code> (<code>runtime/instrumentation.ts</code>). Outside the runtime, SDK calls throw &quot;no runtime host is registered&quot;. Plugin authors can type-check against the published SDK without installing <code>@sovereignfs/db</code> or <code>@sovereignfs/mailer</code>.</li><li><strong>Plugin authors:</strong> no call-site changes needed. If you were relying on the SDK importing from <code>@sovereignfs/db</code> directly in a non-runtime context (e.g. tests), that no longer works — use mocked host registrations instead.</li><li><strong><code>runtime</code> → 0.11.0</strong>, <strong><code>@sovereignfs/sdk</code> → 1.1.0</strong>.</li></ul><h3 id="v0-14-→-v0-15" tabindex="-1">v0.14 → v0.15 <a class="header-anchor" href="#v0-14-→-v0-15" aria-label="Permalink to &quot;v0.14 → v0.15&quot;">​</a></h3><ul><li><strong>Drizzle-kit migrations replace the interim DDL bootstrap.</strong> Platform schema migrations now live in <code>packages/db/migrations/</code> and are applied automatically at startup via <code>runMigrations()</code>. The migrations use <code>CREATE TABLE IF NOT EXISTS</code> throughout, so existing instances bootstrapped by earlier versions upgrade safely without any manual SQL.</li><li><strong><code>sv backup</code> / <code>sv restore</code></strong> are now available in the <code>bin/sv</code> CLI. Run <code>pnpm sv backup</code> before upgrading; the archive captures all SQLite databases and uploaded avatars.</li><li><strong>Downgrade detection.</strong> The runtime now records the running platform version in <code>platform_settings</code> on every startup. Starting an older binary against a database written by a newer binary is flagged in <code>GET /api/admin/health</code> as a <code>downgradeWarning</code>. Always restore a backup before downgrading.</li><li><strong>Published Docker images.</strong> Semver-tagged images are published to GHCR on every <code>v*.*.*</code> tag. Set <code>SOVEREIGN_VERSION=vX.Y.Z</code> in your environment to pull them without a local build step.</li><li><strong><code>AUTH_TRUSTED_ORIGINS</code></strong> (new, optional). Comma-separated list of additional origins that better-auth accepts for server-to-server CSRF checks. Set to <code>http://auth:3001</code> when <code>AUTH_BASE_URL</code> is a public domain (the Docker default in <code>docker-compose.prod.yml</code>).</li><li><strong><code>SOVEREIGN_AUTH_PUBLIC_URL</code></strong> (new, optional). Browser-facing auth URL for login redirects. Defaults to <code>SOVEREIGN_AUTH_URL</code>. Required if your auth server is not reachable from the browser on the same address used for internal server-to-server calls.</li><li><strong>Production auth port changed from unexposed to 4001.</strong> <code>docker-compose.prod.yml</code> now maps auth to <code>${AUTH_PORT:-4001}:3001</code>. Update reverse-proxy configs if you previously routed directly to the internal service.</li></ul><h3 id="v0-4-→-v0-5" tabindex="-1">v0.4 → v0.5 <a class="header-anchor" href="#v0-4-→-v0-5" aria-label="Permalink to &quot;v0.4 → v0.5&quot;">​</a></h3><ul><li><strong><code>SOVEREIGN_ADMIN_KEY</code> is required</strong> (auth + runtime). Set it in <code>.env</code> — both services refuse to start without it. Generate with <code>openssl rand -base64 32</code>.</li><li><strong>The runtime now reads <code>AUTH_SECRET</code></strong> to verify sessions locally (AUTH-05). It must equal the auth server&#39;s <code>AUTH_SECRET</code>; since both apps load the one root <code>.env</code>, no action is needed unless you set a distinct <code>SOVEREIGN_AUTH_SECRET</code>. The provided Compose files pass <code>AUTH_SECRET</code> to the runtime service.</li><li><strong>PostgreSQL is supported</strong> as an alternative to the default SQLite — opt in with <code>DB_DIALECT=postgres</code> + <code>DATABASE_URL</code>/<code>AUTH_DATABASE_URL</code>, or the <code>docker-compose.postgres.yml</code> overlay. See <a href="./self-hosting.html#postgresql">self-hosting.md</a>.</li><li>No data migration is required for existing SQLite instances.</li></ul><h3 id="v0-3-→-v0-4" tabindex="-1">v0.3 → v0.4 <a class="header-anchor" href="#v0-3-→-v0-4" aria-label="Permalink to &quot;v0.3 → v0.4&quot;">​</a></h3><ul><li>First-class <strong>Console</strong> (user + plugin management), <strong>Launcher</strong>, and <strong>Account</strong> plugins land. No configuration changes; existing data is carried forward. The first registered user is the platform admin.</li></ul><hr><h2 id="published-package-migrations" tabindex="-1">Published package migrations <a class="header-anchor" href="#published-package-migrations" aria-label="Permalink to &quot;Published package migrations&quot;">​</a></h2><h3 id="sovereignfs-ui-—-dialog-s-size-prop-drops-xl-full-adds-auto-breaking" tabindex="-1"><code>@sovereignfs/ui</code> — <code>Dialog</code>&#39;s <code>size</code> prop drops <code>xl</code>/<code>full</code>, adds <code>auto</code> (breaking) <a class="header-anchor" href="#sovereignfs-ui-—-dialog-s-size-prop-drops-xl-full-adds-auto-breaking" aria-label="Permalink to &quot;`@sovereignfs/ui` — `Dialog`&#39;s `size` prop drops `xl`/`full`, adds `auto` (breaking)&quot;">​</a></h3><p><strong><code>DialogSize</code> is now <code>&#39;sm&#39; | &#39;md&#39; | &#39;lg&#39; | &#39;auto&#39;</code></strong> — <code>xl</code> and <code>full</code> are removed outright, no deprecation period. <code>full</code> was a dead alias of <code>lg</code> (identical CSS on every breakpoint); <code>xl</code> was a fixed-48rem-width variant with content-driven height only. Consolidating the size scale down to three consistent behaviors (<code>sm</code>/<code>md</code>: fixed width, content-driven height, capped; <code>lg</code>: true fixed 100%/100% box; <code>auto</code>: content-driven on <strong>both</strong> width and height, capped rather than fixed on either axis) closes a real inconsistency — five names doing the work of two behaviors.</p><p><strong>Migration:</strong> replace <code>size=&quot;full&quot;</code> with <code>size=&quot;lg&quot;</code> (they always rendered identically). Replace <code>size=&quot;xl&quot;</code> with <code>size=&quot;auto&quot;</code> if the dialog&#39;s content genuinely varies in size (the case <code>xl</code> was originally added for — a card detail modal with an optional checklist/comments section), or with <code>size=&quot;lg&quot;</code> if it should behave like a fixed full-panel overlay instead.</p><p><strong>A <code>sovereign-plugin-kanban.local</code>-quality of plugin will need this migration</strong> — <code>CardDetailOverlay.tsx</code>&#39;s <code>size={isMobile ? &#39;full&#39; : &#39;xl&#39;}</code> call site is the only one found in this codebase&#39;s own review (mobile ignores <code>size</code> entirely regardless, so only the desktop <code>&#39;xl&#39;</code> branch is live). That specific plugin is outside this repo&#39;s ownership (a gitignored <code>.local</code> clone) and was not migrated as part of this change — any plugin using <code>xl</code>/<code>full</code> needs its own follow-up before adopting this <code>@sovereignfs/ui</code> version. <code>packages/ui</code> bumped <strong>minor</strong> per NFR-04&#39;s floor for a breaking change to a published package.</p><h3 id="sovereignfs-manifest-0-26-0-→-1-0-0-breaking-—-rfc-0078" tabindex="-1"><code>@sovereignfs/manifest</code> 0.26.0 → 1.0.0 (breaking — RFC 0078) <a class="header-anchor" href="#sovereignfs-manifest-0-26-0-→-1-0-0-breaking-—-rfc-0078" aria-label="Permalink to &quot;`@sovereignfs/manifest` 0.26.0 → 1.0.0 (breaking — RFC 0078)&quot;">​</a></h3><p><strong>The <code>offline</code> manifest field is now a plain boolean, replacing the <code>offline.routes[]</code>/<code>offline.root</code> object shape (RFC 0074).</strong> RFC 0078 generalizes Launcher&#39;s original <code>offline.root: true</code> pattern — one bare-<code>routePrefix</code> entry page, entirely client-rendered past that — into the only offline model, removing the per-route <code>routes[]</code> array. <code>@sovereignfs/manifest</code> isn&#39;t published to npm, but this is still a breaking change to the <code>manifest.json</code> contract every plugin author writes against.</p><p><strong>Before:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;routes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;prefix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/cards&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] } }</span></span></code></pre></div><p>or</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;root&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><p><strong>After:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>There is no field-for-field migration for a <code>routes[]</code> entry — the old model let a plugin cache several distinct sub-paths independently; the new model gives every offline-enabled plugin exactly one entry point (its bare <code>routePrefix</code>), and the plugin&#39;s own client-side code decides which screens or data to render past that. Restructure your offline experience as one client-rendered shell at your bare <code>routePrefix</code>, following <code>plugins/launcher/app/_components/LauncherOfflineView.tsx</code> as the reference pattern.</p><p>A manifest still using the old object shape now fails validation — this is intentional; there is no deprecation period. A new <code>offline:write</code> permission is also added (<code>permissions: [&quot;offline:write&quot;]</code>, requires <code>offline: true</code>) — reserved for a forthcoming offline write/sync capability (<code>@sovereignfs/sdk/offline-queue</code>), not yet implemented as of this version.</p><h3 id="sovereignfs-manifest-2-1-0-→-3-0-0-breaking-—-retire-database-isolation-shared" tabindex="-1"><code>@sovereignfs/manifest</code> 2.1.0 → 3.0.0 (breaking — retire <code>database.isolation</code>/<code>&quot;shared&quot;</code>) <a class="header-anchor" href="#sovereignfs-manifest-2-1-0-→-3-0-0-breaking-—-retire-database-isolation-shared" aria-label="Permalink to &quot;`@sovereignfs/manifest` 2.1.0 → 3.0.0 (breaking — retire `database.isolation`/`&quot;shared&quot;`)&quot;">​</a></h3><p><strong>Every <code>sovereign</code>/<code>community</code> plugin&#39;s database is now unconditionally isolated — its own dedicated SQLite file/namespace or Postgres schema. The <code>shared</code> manifest option (and the <code>isolation</code> field generally) is gone.</strong> Checking every real, installed plugin — in this monorepo or its known external repos — found <code>shared</code> used only by the three first-party <code>type: &quot;platform&quot;</code> plugins (<code>account</code>, <code>console</code>, <code>launcher</code>), none of which were genuine <code>shared</code>-mode data owners in the first place (see epic task <a href="./epics/data-sovereignty.html#-828--retire-the-databaseisolationshared-manifest-option">8.28</a> for the full reasoning); every real third-party plugin was already <code>isolated</code>.</p><p><strong>Before:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;database&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;shared&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>or</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;database&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;isolation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;isolated&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;requireEncryption&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><p><strong>After:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{}</span></span></code></pre></div><p>or</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;database&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;requireEncryption&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><p>A manifest still declaring <code>&quot;database&quot;: &quot;shared&quot;</code>, <code>&quot;database&quot;: &quot;isolated&quot;</code>, or <code>database.isolation</code> now fails validation — this is intentional, no deprecation period, the same shape as the <code>database.dialect</code> field removal (task 8.22). Drop the field or the <code>isolation</code> key entirely; every non-platform plugin gets a dedicated store regardless. If your plugin was genuinely relying on <code>shared</code> mode&#39;s cross-plugin SQL joins against platform tables, that access pattern is not supported for third-party plugins going forward — use the consent-gated <code>sdk.data</code> mechanism instead (RFC 0002).</p><p><code>type: &quot;platform&quot;</code> is reserved for first-party plugins in this monorepo — if you maintain a real third-party plugin, this change requires no manifest edit beyond removing an <code>isolation</code>/<code>&quot;shared&quot;</code> declaration if you had one; the resulting behavior (a dedicated store) is very likely already what your plugin was using.</p><h3 id="sovereignfs-manifest-3-0-0-→-4-0-0-breaking-—-research-0012" tabindex="-1"><code>@sovereignfs/manifest</code> 3.0.0 → 4.0.0 (breaking — research 0012) <a class="header-anchor" href="#sovereignfs-manifest-3-0-0-→-4-0-0-breaking-—-research-0012" aria-label="Permalink to &quot;`@sovereignfs/manifest` 3.0.0 → 4.0.0 (breaking — research 0012)&quot;">​</a></h3><p><strong>The <code>offline</code> manifest field is now a two-value enum, replacing the plain boolean from RFC 0078 above.</strong> This is the field&#39;s third shape: <code>offline.routes[]</code>/<code>offline.root</code> object (RFC 0074) → <code>offline: boolean</code> (RFC 0078) → <code>offline: &#39;offline-first&#39; | &#39;device-only&#39;</code> (research 0012, epic task 3.36). Sovereign&#39;s offline model has moved from online-first-with-an-allowlist to genuinely offline-first, and a single boolean can no longer say which of two materially different capability tiers a plugin needs — see <code>docs/research/0012-offline-first-architecture.md</code> for the full design.</p><p><strong>Before:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p><strong>After — pick the tier that matches what your plugin needs:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;offline-first&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>or</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;device-only&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><ul><li><strong><code>offline-first</code></strong> — the device holds a full replica of your plugin&#39;s data, kept fresh in the background; the server stays the source of truth. This is what <code>offline: true</code> meant before — if your plugin previously declared <code>offline: true</code>, <code>offline-first</code> is very likely the correct replacement, with no other change needed. Launcher migrated this way.</li><li><strong><code>device-only</code></strong> — your plugin&#39;s data never leaves the device; there is no server copy. Requires a durable, encrypted, device-auth-gated store, which today only a native shell provides — check <code>@sovereignfs/sdk/device-client</code>&#39;s <code>isDeviceOnlyTierAvailable()</code> before relying on it; it reports <code>false</code> everywhere until the underlying bridge capability ships.</li></ul><p>There is no explicit &quot;off&quot; value in the enum — omitting the field entirely still means no offline support, exactly as an absent field did before.</p><p><strong>The <code>offline:write</code> permission is removed outright</strong>, not replaced. Both tiers now imply local mutation, so the tier value itself is sufficient install-review signal; a manifest still declaring <code>offline:write</code> fails validation as an unknown permission, with no deprecation period. If your plugin used <code>sdk.offline-queue</code>, that module is unaffected by this manifest change — its own replacement is tracked separately (epic task 3.37).</p><p>A manifest still declaring the plain boolean shape (<code>offline: true</code>/<code>false</code>) now fails validation, same as the old object shape already did.</p><h3 id="sovereignfs-sdk-1-41-0-→-1-42-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.41.0 → 1.42.0 <a class="header-anchor" href="#sovereignfs-sdk-1-41-0-→-1-42-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.41.0 → 1.42.0&quot;">​</a></h3><p><strong><code>sdk.offline</code>&#39;s cache is now encrypted at rest</strong> (RFC 0093 task 8.20&#39;s <code>offline-first</code>-tier half) — every value is AES-GCM-encrypted under an automatically-generated, non-extractable device key before it reaches IndexedDB, no enrollment step or opt-in required. <code>offline.get</code>/<code>offline.set</code> keep the exact same signatures; nothing to change in a plugin that only stores JSON-like data through them (the documented, designed-for use case).</p><p><strong>Narrows accepted values to JSON-serializable data.</strong> Encryption requires byte-serializing the value first, so <code>IndexedDB</code>&#39;s native structured-clone support for extras like <code>Blob</code>/<code>Map</code>/<code>Set</code> no longer round-trips through <code>offline.set</code> — passing one now throws instead of silently storing it. No shipped plugin in this repo relies on that; <code>offline.set</code>&#39;s own soft per-entry size cap already estimated size via <code>JSON.stringify</code> and treated non-JSON-serializable values as an edge case, not the primary path.</p><p><strong>A value cached before this version won&#39;t be readable afterward.</strong><code>offline.get</code> returns <code>null</code> for it (a cache miss, not a thrown error) rather than failing, since this cache is disposable by design — the server is always the source of truth, and the normal &quot;never cached&quot; fetch-fresh path already handles it.</p><h3 id="sovereignfs-sdk-1-42-0-→-1-43-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.42.0 → 1.43.0 <a class="header-anchor" href="#sovereignfs-sdk-1-42-0-→-1-43-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.42.0 → 1.43.0&quot;">​</a></h3><p><strong><code>isDeviceOnlyTierAvailable()</code> now also reports <code>true</code> on plain web/PWA</strong> when WebAuthn PRF + OPFS are supported (RFC 0093 §1&#39;s web backend), not only when a native shell advertises the <code>secureStorage</code> bridge capability. This was a real gap, not an intentional native-only scope: the web backend (<code>@sovereignfs/sdk/device-only-kv</code>) shipped without this function being updated to know about it, so <code>DeviceOnlyGate</code> (the documented gating pattern) showed &quot;Phone only&quot; on every plain browser tab even after the web storage stack was fully working. No action needed if your plugin already follows the documented <code>DeviceOnlyGate</code>/<code>DeviceStorageKeyGate</code> pattern — it now correctly lets users onto a <code>device-only</code> plugin from a capable browser tab instead of only from a native shell.</p><h3 id="sovereignfs-sdk-1-47-0-→-1-48-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.47.0 → 1.48.0 <a class="header-anchor" href="#sovereignfs-sdk-1-47-0-→-1-48-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.47.0 → 1.48.0&quot;">​</a></h3><p><strong><code>sdk.data.provide()</code> is now namespaced per-provider and returns a <code>Promise</code>.</strong> Previously, <code>_resolverRegistry</code> (the host-side registry backing <code>sdk.data</code>) was keyed only by bare contract name — two unrelated plugins independently choosing the same local contract name (e.g. both naming a contract <code>&quot;expenses&quot;</code>) silently clobbered each other&#39;s registration, letting a consumer with a valid consent grant for provider A be served provider B&#39;s data. Found in a codebase audit; closed by namespacing the registry <code>&lt;providerId&gt;:&lt;contract&gt;</code>, mirroring how <code>sdk.tools</code>/<code>_toolRegistry</code> already avoided this exact class of collision.</p><p><strong>Plugin-author-visible change:</strong> <code>sdk.data.provide(contract, resolver)</code>&#39;s call shape is unchanged, but it is now <code>async</code> — code calling it without <code>await</code> still runs (the call is fire-and-forget), but should add <code>await</code> to guarantee registration completes before any consumer might query it. It also now throws <code>&#39;sdk.data.provide() requires a plugin route context...&#39;</code> if called with no <code>x-sovereign-plugin-id</code> header available (i.e. outside a real plugin route, matching <code>sdk.tools.provide()</code>&#39;s existing behavior) — this was previously silently accepted with an unnamespaced, collision-prone registration.</p><p><strong>Breaking for host implementers only</strong> — a custom <code>SdkHost</code> implementation&#39;s <code>data.provide</code> now takes <code>(providerId, contract, resolver)</code> instead of <code>(contract, resolver)</code>; the platform&#39;s own <code>runtime/src/sdk-host.ts</code> already implements the new signature. <code>manifest.json</code>&#39;s <code>data.provides[].contract</code> no longer needs to be globally unique — only unique within your own plugin&#39;s <code>provides</code> array — since the platform namespaces it internally.</p><p><strong><code>sdk.connections.*</code>, <code>sdk.secrets.*</code>, <code>sdk.handoffs.*</code>, <code>sdk.tools.preview()</code>/<code>execute()</code>, and <code>sdk.activity.log()</code> now work from a job/schedule handler.</strong> Same gap <code>1.46.0</code>/<code>1.47.0</code> closed for <code>sdk.storage</code>/ <code>sdk.env</code> — each of these called <code>next/headers()</code> directly with no fallback, so any call from an <code>sdk.jobs</code>/<code>sdk.schedules</code> handler (no real Next.js request) threw instead of resolving the calling plugin&#39;s identity from the handler&#39;s own background-invocation context. Found in a codebase audit, generalizing a bug class this repo had already independently rediscovered and fixed four times. <code>sdk.notifications.*</code> (except <code>send</code>), <code>sdk.plugins.get()</code>/<code>list()</code>/<code>getConsentStatus()</code>, <code>sdk.auth.getSession()</code>, <code>sdk.authz.hasGrant()</code>, <code>sdk.directory.*</code>, <code>sdk.e2ee.*</code>, and <code>sdk.data.query()</code> were reviewed too but left unchanged — each is inherently tied to a live user&#39;s own session/consent with no background-context equivalent to fall back to, so they now just fail gracefully (return their documented empty/false/null value) instead of throwing a raw <code>next/headers()</code> error, without gaining a plugin-identity fallback.</p><p><strong>Breaking for host implementers only</strong> — plugin authors see no signature or return-type change for any of these calls. <code>ConnectionContext.pluginId</code>, <code>SecretContext.pluginId</code>, and <code>HandoffRequestContext.pluginId</code> widen from <code>string</code> to <code>string | null</code>; <code>ToolContext.callerPluginId</code> widens the same way — a custom <code>SdkHost</code> implementation reading these fields as guaranteed non-null will fail to type-check. The platform&#39;s own <code>runtime/src/sdk-host.ts</code> already implements the fallback, mirroring <code>resolveStorageContext</code>&#39;s exact shape for each: falls back through the portability and background-invocation <code>AsyncLocalStorage</code> contexts, throwing <code>&quot;requires a plugin route context&quot;</code> only if every source comes up empty.</p><h3 id="sovereignfs-sdk-1-46-0-→-1-47-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.46.0 → 1.47.0 <a class="header-anchor" href="#sovereignfs-sdk-1-46-0-→-1-47-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.46.0 → 1.47.0&quot;">​</a></h3><p><strong><code>sdk.env.get()</code> now works from a job/schedule handler.</strong> Previously, any <code>sdk.env.get()</code> call made from an <code>sdk.jobs</code>/<code>sdk.schedules</code> handler (rather than a real Next.js request) threw, since <code>env.get()</code> called <code>next/headers()</code> directly with no fallback for a background invocation — the identical gap <code>1.46.0</code> closed for <code>sdk.storage</code>, just never generalized to <code>env</code>. Found implementing <code>sovereign-plugin-travellog</code>&#39;s T.20 (RFC 0018/0046).</p><p><strong>Breaking for host implementers only</strong> — plugin authors calling <code>sdk.env.get(key)</code> see no signature or return-type change. <code>SdkHost</code> gains a new required <code>env.get(key, pluginId)</code> member: a custom host implementation that constructs a <code>provideHost()</code> argument without an <code>env</code> property will fail to type-check. The platform&#39;s own <code>runtime/src/sdk-host.ts</code> already implements it, falling back through the same portability/background-plugin <code>AsyncLocalStorage</code> contexts <code>sdk.db.getClient()</code>/<code>sdk.storage</code> use. Unlike <code>sdk.storage</code>, <code>sdk.env.get()</code> still returns <code>null</code> (never throws) when no plugin id is resolvable from any of those sources — its existing &quot;outside a plugin route context → <code>null</code>&quot; contract is preserved, just with two more sources checked before giving up.</p><h3 id="sovereignfs-sdk-1-45-0-→-1-46-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.45.0 → 1.46.0 <a class="header-anchor" href="#sovereignfs-sdk-1-45-0-→-1-46-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.45.0 → 1.46.0&quot;">​</a></h3><p><strong><code>sdk.storage</code> now works from a job/schedule handler.</strong> Previously, any <code>sdk.storage.*</code> call made from an <code>sdk.jobs</code>/<code>sdk.schedules</code> handler (rather than a real Next.js request) threw, since <code>storageContext()</code> called <code>next/headers()</code> directly with no fallback for a background invocation — <code>sdk.db.getClient()</code> already had this fallback, <code>sdk.storage</code> didn&#39;t. Found and fixed building <code>sovereign-plugin-travellog</code>&#39;s Swarm importer (RFC 0044/0046).</p><p><strong>Breaking for host implementers only</strong> — plugin authors calling <code>sdk.storage.*</code> see no signature change. <code>StorageContext.pluginId</code> widens from <code>string</code> to <code>string | null</code>: a custom <code>HostImplementations.storage</code> implementation that assumed <code>context.pluginId</code> is always a string must now handle <code>null</code>. The platform&#39;s own <code>runtime/src/sdk-host.ts</code> already does — falling back to the same background-plugin <code>AsyncLocalStorage</code> context <code>sdk.db.getClient()</code> uses, and throwing a clear error if no plugin id is resolvable at all (outside both a request and a background-plugin context).</p><p><strong>Still a gap, by design, not by oversight:</strong> <code>sdk.storage.put()</code>&#39;s <code>ownerUserId</code> ownership check has no equivalent fallback for <em>user</em> identity — <code>JobContext</code> carries a plugin id, never a user id, since a job is plugin-scoped, not inherently user-scoped. An object uploaded with <code>ownerUserId</code> set remains unreadable from a job/schedule handler; omit <code>ownerUserId</code> on any object a background handler will read back. See <code>docs/architecture-rules.md</code>&#39;s matching entry for the full guidance and the account-deletion-sweep tradeoff that omitting it carries.</p><h3 id="sovereignfs-sdk-1-22-0-→-1-23-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.22.0 → 1.23.0 <a class="header-anchor" href="#sovereignfs-sdk-1-22-0-→-1-23-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.22.0 → 1.23.0&quot;">​</a></h3><p><strong><code>StorageObject</code> gains a <code>metadata</code> field</strong> (RFC 0044/0060). <code>sdk.storage.put()</code> already accepted a <code>metadata</code> input, but it was silently dropped and never returned by <code>get()</code>/<code>list()</code>/<code>put()</code>&#39;s response — a bug, not an intentional omission. It now round-trips unchanged, which is what makes <code>sdk.storage</code> usable for encrypted objects (RFC 0060 step 5): the wrapped per-object DEK and algorithm version travel alongside the ciphertext in this field instead of needing a separate table.</p><p>If your own test code or a mock host implementation constructs a <code>StorageObject</code> value directly (rather than only reading one returned by <code>sdk.storage</code>), add a <code>metadata: null</code> (or the actual value) — TypeScript now requires it. Real plugin code that only calls <code>sdk.storage.get()</code>/<code>list()</code>/<code>put()</code> and reads the result needs no changes; the new field is simply present.</p><h3 id="sovereignfs-sdk-1-1-0-→-1-2-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.1.0 → 1.2.0 <a class="header-anchor" href="#sovereignfs-sdk-1-1-0-→-1-2-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.1.0 → 1.2.0&quot;">​</a></h3><p><strong><code>sdk.data.provide</code> / <code>sdk.data.query</code> are implemented</strong> (RFC 0002). Plugins may now declare <code>data.provides[]</code> and <code>data.consumes[]</code> in their manifest. No action required for existing plugins that don&#39;t use cross-plugin data sharing.</p><h3 id="sovereignfs-sdk-1-0-0-→-1-1-0" tabindex="-1"><code>@sovereignfs/sdk</code> 1.0.0 → 1.1.0 <a class="header-anchor" href="#sovereignfs-sdk-1-0-0-→-1-1-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.0.0 → 1.1.0&quot;">​</a></h3><p><strong>Host-provided implementations (RFC 0023).</strong> <code>packages/sdk</code> no longer imports <code>@sovereignfs/db</code> or <code>@sovereignfs/mailer</code>. Implementations are registered by the runtime at startup via <code>provideHost()</code>. Plugins never call <code>provideHost()</code>.</p><p>If you were importing <code>@sovereignfs/sdk</code> in a non-runtime context and relying on the direct DB/mailer imports for testing — those no longer exist. Use mocked host registrations or test via the runtime instead.</p><h3 id="sovereignfs-sdk-0-6-0-→-0-7-0" tabindex="-1"><code>@sovereignfs/sdk</code> 0.6.0 → 0.7.0 <a class="header-anchor" href="#sovereignfs-sdk-0-6-0-→-0-7-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 0.6.0 → 0.7.0&quot;">​</a></h3><h3 id="sdk-db-getclient-is-now-implemented-and-async" tabindex="-1"><code>sdk.db.getClient()</code> is now implemented and async <a class="header-anchor" href="#sdk-db-getclient-is-now-implemented-and-async" aria-label="Permalink to &quot;`sdk.db.getClient()` is now implemented and async&quot;">​</a></h3><p><code>sdk.db.getClient()</code> previously threw <code>NotImplementedError</code>. It now returns the platform&#39;s Drizzle client wrapped in a <code>Promise</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Before:</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> db</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// threw NotImplementedError</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// After:</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> db</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><h3 id="sovereignfs-sdk-0-5-0-→-0-6-0" tabindex="-1"><code>@sovereignfs/sdk</code> 0.5.0 → 0.6.0 <a class="header-anchor" href="#sovereignfs-sdk-0-5-0-→-0-6-0" aria-label="Permalink to &quot;`@sovereignfs/sdk` 0.5.0 → 0.6.0&quot;">​</a></h3><h3 id="sdk-platform-getconfig-is-now-async" tabindex="-1"><code>sdk.platform.getConfig()</code> is now async <a class="header-anchor" href="#sdk-platform-getconfig-is-now-async" aria-label="Permalink to &quot;`sdk.platform.getConfig()` is now async&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Before:</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> config</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.platform.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// After:</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> config</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.platform.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>The returned <code>PlatformConfig</code> shape is unchanged.</p><h3 id="sovereignfs-sdk-1-9-0-→-1-10-0-white-labeling-phase-1-rfc-0027" tabindex="-1"><code>@sovereignfs/sdk</code> 1.9.0 → 1.10.0 (White-labeling Phase 1, RFC 0027) <a class="header-anchor" href="#sovereignfs-sdk-1-9-0-→-1-10-0-white-labeling-phase-1-rfc-0027" aria-label="Permalink to &quot;`@sovereignfs/sdk` 1.9.0 → 1.10.0 (White-labeling Phase 1, RFC 0027)&quot;">​</a></h3><p><strong><code>sdk.platform.getConfig()</code> gains branding fields.</strong> <code>PlatformConfig</code> now includes two new optional fields:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PlatformConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  inviteOnly</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  brandName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ← new; falls back to tenantName</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  brandPrimaryColor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ← new; validated hex or undefined</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Existing calls are unaffected — the new fields are additive.</p><p><strong>New <code>--sv-brand-*</code> CSS tokens</strong> (<code>--sv-brand-logo</code>, <code>--sv-brand-logo-dark</code>, <code>--sv-brand-favicon</code>) are set at <code>:root</code> by <code>BrandProvider</code> and are available in plugin CSS without any import.</p><hr><h3 id="platform-0-28-→-0-29-sovereignfs-ui-0-10-0-→-0-11-0-sovereignfs-sdk-1-10-0-→-1-11-0-instance-identity-rename-rfc-0032" tabindex="-1">Platform 0.28 → 0.29 (<code>@sovereignfs/ui</code> 0.10.0 → 0.11.0, <code>@sovereignfs/sdk</code> 1.10.0 → 1.11.0, Instance identity rename, RFC 0032) <a class="header-anchor" href="#platform-0-28-→-0-29-sovereignfs-ui-0-10-0-→-0-11-0-sovereignfs-sdk-1-10-0-→-1-11-0-instance-identity-rename-rfc-0032" aria-label="Permalink to &quot;Platform 0.28 → 0.29 (`@sovereignfs/ui` 0.10.0 → 0.11.0, `@sovereignfs/sdk` 1.10.0 → 1.11.0, Instance identity rename, RFC 0032)&quot;">​</a></h3><p><strong>Breaking: <code>--sv-brand-*</code> CSS tokens renamed to <code>--sv-instance-*</code>.</strong> Update any plugin CSS that references these tokens:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* Before */</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">background-image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: var(--sv-brand-logo);</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* After */</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">background-image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: var(--sv-instance-logo);</span></span></code></pre></div><p><strong>Breaking: <code>PlatformConfig.brandName</code> and <code>brandPrimaryColor</code> renamed.</strong> Update calls to <code>sdk.platform.getConfig()</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Before</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">brandName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">brandPrimaryColor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.platform.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// After</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">instanceName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">instancePrimaryColor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.platform.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p><strong>Breaking: <code>BRAND_*</code> environment variables renamed to <code>INSTANCE_*</code>.</strong> Update your <code>.env</code> or Compose env block:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BRAND_NAME            → INSTANCE_NAME</span></span>\n<span class="line"><span>BRAND_LOGO            → INSTANCE_LOGO</span></span>\n<span class="line"><span>BRAND_LOGO_DARK       → INSTANCE_LOGO_DARK</span></span>\n<span class="line"><span>BRAND_FAVICON         → INSTANCE_FAVICON</span></span>\n<span class="line"><span>BRAND_PRIMARY_COLOR   → INSTANCE_PRIMARY_COLOR</span></span>\n<span class="line"><span>BRAND_EMAIL_FROM_NAME → INSTANCE_EMAIL_FROM_NAME</span></span>\n<span class="line"><span>BRAND_EMAIL_LOGO      → INSTANCE_EMAIL_LOGO</span></span></code></pre></div><p><strong><code>/api/brand/*</code> routes renamed to <code>/api/instance/*</code>.</strong> If any external system fetches these routes directly, update those references.</p><p>The database migration (<code>0005_rename_tenant_branding</code>) runs automatically at startup — no manual SQL required.</p><hr><h3 id="platform-0-9-0-→-0-9-1-sovereignfs-sdk-1-11-0-→-1-12-0-user-data-deletion-rfc-0033" tabindex="-1">Platform 0.9.0 → 0.9.1 (<code>@sovereignfs/sdk</code> 1.11.0 → 1.12.0, User data deletion, RFC 0033) <a class="header-anchor" href="#platform-0-9-0-→-0-9-1-sovereignfs-sdk-1-11-0-→-1-12-0-user-data-deletion-rfc-0033" aria-label="Permalink to &quot;Platform 0.9.0 → 0.9.1 (`@sovereignfs/sdk` 1.11.0 → 1.12.0, User data deletion, RFC 0033)&quot;">​</a></h3><p><strong>New: <code>sdk.portability.provideDelete(handler)</code> — account deletion hook.</strong> Plugin authors should register a deletion handler to clean up per-user rows when a user account is deleted. Plugins without a handler will have their rows left in place (operator responsibility).</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// In a plugin route or Server Component:</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sdk } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sovereignfs/sdk&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.portability.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideDelete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // delete all rows belonging to this user from your plugin&#39;s tables</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> deleted</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> myCleanupFn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(db, userId);</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { deleted };</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><strong>New: <code>DELETE /api/account</code></strong> — users can now delete their own account from Account → Data. Requires password re-verification. Returns 409 if the user is the sole <code>platform:owner</code>. On success, clears session cookies.</p><p><strong>New: <code>DELETE /api/admin/users/[id]?deleteData=true</code></strong> — admins can delete a user and all their data from Console → Users. Requires <code>user:manage</code> capability. Cannot target a <code>platform:owner</code>.</p><p>No database migrations required — deletion removes existing rows.</p><h3 id="sovereignfs-ui-0-22-0-→-0-23-0" tabindex="-1"><code>@sovereignfs/ui</code> 0.22.0 → 0.23.0 <a class="header-anchor" href="#sovereignfs-ui-0-22-0-→-0-23-0" aria-label="Permalink to &quot;`@sovereignfs/ui` 0.22.0 → 0.23.0&quot;">​</a></h3><p><strong>Breaking: <code>FormField</code> children is now a render prop.</strong> The previous API computed <code>aria-describedby</code> but applied it to a wrapper <code>&lt;div&gt;</code> around the child, not the actual control, so screen readers didn&#39;t reliably announce hints/errors. <code>FormField</code> now passes the id/aria wiring to the control itself via a render-prop <code>children</code>, and generates its own <code>id</code> when one isn&#39;t provided:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Before</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FormField</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Email&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> htmlFor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Input</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;email&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FormField</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// After</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FormField</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Email&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">field</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">field} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;}</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FormField</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// id is optional — FormField generates one via useId() if omitted:</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FormField</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">field</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">field} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;}&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FormField</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p><code>field</code> is <code>{ id, &#39;aria-describedby&#39;?, &#39;aria-invalid&#39;?, required? }</code> — spread it directly onto any <code>@sovereignfs/ui</code> form control or a native element.</p><p><strong>New: <code>Textarea</code> component</strong> — additive, no migration required.</p><hr><h2 id="runtime-version-map" tabindex="-1">Runtime version map <a class="header-anchor" href="#runtime-version-map" aria-label="Permalink to &quot;Runtime version map&quot;">​</a></h2><p>Maps the <code>runtime</code> package version at which each major capability was added. The section headings above correspond to these runtime version transitions. <code>SOVEREIGN_VERSION</code> in Compose files should match the runtime version for the release you are running.</p><table tabindex="0"><thead><tr><th>Runtime version</th><th>Key capability delivered</th></tr></thead><tbody><tr><td>0.2.0</td><td>Platform DB (tenant_settings, root plugin config), Console settings</td></tr><tr><td>0.3.0</td><td>Launcher plugin, root-plugin-in-place rewrite</td></tr><tr><td>0.4.0</td><td>Account plugin (profile + preferences)</td></tr><tr><td>0.5.0</td><td>Plugin install script, PWA configuration</td></tr><tr><td>0.6.0</td><td>Local session verification (cookie-cache, AUTH-05)</td></tr><tr><td>0.7.0</td><td>Public <code>/api</code> namespace delegation (PLT-16)</td></tr><tr><td>0.8.0–0.9.1</td><td>Overlay shell mode (RFC 0001), Dialog UI primitive</td></tr><tr><td>0.9.0</td><td>Logout / self sign-out (AUTH-02)</td></tr><tr><td>0.10.0</td><td>Security hardening Tier 0 + Tier 1 (RFC 0008)</td></tr><tr><td>0.11.0</td><td>SDK distribution (RFC 0023), zero-dep published SDK</td></tr><tr><td>0.12.0</td><td>Plugin compatibility &amp; versioning (RFC 0024)</td></tr><tr><td>0.13.0</td><td>Cross-plugin data sharing (RFC 0002)</td></tr><tr><td>0.14.0–0.14.1</td><td>Activity log (RFC 0005), icon system (RFC 0011)</td></tr><tr><td>0.15.0</td><td>Drizzle-kit migrations, <code>sv backup</code>/<code>restore</code>, downgrade guard (RFC 0006)</td></tr><tr><td>0.16.0</td><td>User data portability (RFC 0007)</td></tr><tr><td>0.17.0</td><td>Plugin-scoped env vars (RFC 0018)</td></tr><tr><td>0.18.0</td><td>Minimal shell mode (RFC 0014)</td></tr><tr><td>0.19.0</td><td>Mobile responsiveness &amp; PWA hardening (RFC 0013)</td></tr><tr><td>0.20.0</td><td>Passkeys &amp; TOTP MFA (RFC 0012), offline connectivity banner</td></tr><tr><td>0.21.0</td><td>Platform roles &amp; capabilities (RFC 0021)</td></tr><tr><td>0.22.0</td><td>Notification Center (RFC 0015)</td></tr><tr><td>0.23.0</td><td>Web Push notifications (RFC 0016)</td></tr><tr><td>0.25.0–0.25.1</td><td>Plugin monetization (RFC 0003), license generator, entitlements</td></tr><tr><td>0.26.0</td><td>Per-plugin isolated database (RFC 0004)</td></tr><tr><td>0.27.0</td><td>Production dev-mode &amp; diagnostics (RFC 0020)</td></tr><tr><td>0.28.0</td><td>White-labeling Phase 1 (RFC 0027)</td></tr><tr><td>0.29.0</td><td>Instance identity rename (RFC 0032)</td></tr><tr><td>0.30.0</td><td>User data deletion (RFC 0033)</td></tr><tr><td>0.31.0</td><td>Notification transport (RFC 0034)</td></tr><tr><td>0.32.0</td><td>Sidebar customization (epic task 2.13)</td></tr><tr><td>0.33.0</td><td>Instance identity — <code>instanceId</code> field + terminology cleanup (RFC 0039)</td></tr><tr><td>0.34.0</td><td>Platform/runtime version reconciliation chore (RFC 0057 plan status fix)</td></tr><tr><td>0.35.0</td><td>iOS PWA launch screens (<code>apple-touch-startup-image</code>)</td></tr><tr><td>0.36.0</td><td>Admin disable surface for example plugins (epic task 12.3)</td></tr><tr><td>0.37.0</td><td>Account and security email delivery coverage (RFC 0062, epic task 1.14)</td></tr><tr><td>0.38.0</td><td>Plugin background schedules — Phase 1 (RFC 0046)</td></tr><tr><td>0.39.0</td><td>Plugin file storage — <code>sdk.storage</code> (RFC 0044)</td></tr><tr><td>0.40.0–0.40.1</td><td>Client-side encryption core, steps 1–5 — <code>sdk.e2ee</code>, Account UX, object crypto, <code>sdk.storage</code> integration (RFC 0060)</td></tr><tr><td>0.41.0</td><td>Client-side encryption core complete, step 6 — export/delete via <code>sdk.portability</code> (RFC 0060, epic task 8.9 done)</td></tr><tr><td>0.42.0</td><td>Plugin portability export metadata, partial (RFC 0052)</td></tr><tr><td>0.43.0</td><td>Cross-plugin references and dependency discovery (RFC 0051); RFC 0052 complete</td></tr><tr><td>0.44.0</td><td>Launcher grid respects saved sidebar order (epic task 2.22)</td></tr><tr><td>0.45.0</td><td>User groups foundation + per-user capability grants (RFC 0065/0070, epic tasks 1.15–1.16)</td></tr><tr><td>0.46.0</td><td>Plugin access policy enforcement (RFC 0065, epic task 2.21)</td></tr><tr><td>0.47.0</td><td>Plugin catalog and install-time activation model (RFC 0065, epic task 3.28)</td></tr><tr><td>0.48.0</td><td>Console plugin access management (RFC 0065, epic task 13.7)</td></tr><tr><td>0.49.0</td><td>Console plugin catalog browser and install-time activation (RFC 0065, epic task 13.8)</td></tr><tr><td>0.50.0</td><td>Plugin invite-scope grant resolution (RFC 0065, epic task 2.23)</td></tr><tr><td>0.51.0</td><td>Plugin directory browsing and self-service enable/disable (RFC 0065, epic task 15.3)</td></tr><tr><td>0.54.0</td><td>Public plugin page routes — <code>publicRoutes</code> manifest field (RFC 0042, epic task 2.14)</td></tr><tr><td>0.55.0</td><td>Plugin mailer permission and SDK email surface — <code>sdk.email.sendToUser()</code> (RFC 0062, epic task 3.26)</td></tr><tr><td>0.57.0</td><td>Opt-in single-key SQLite at-rest encryption (RFC 0071, epic task 8.14)</td></tr><tr><td>0.58.0</td><td>External OAuth 2.0 / OIDC provider for non-plugin apps (RFC 0072, epic task 1.18)</td></tr><tr><td>0.59.0</td><td>Offline-route wiring for manifest <code>offline.root</code> flag + neutral-shell handling of <code>/</code></td></tr><tr><td>0.60.0</td><td>Web push delivery status logging to Console/Account Activities (RFC 0016)</td></tr><tr><td>0.61.0</td><td>Per-plugin mobile header/footer toggle (RFC 0075)</td></tr><tr><td>0.62.0</td><td>CI dependency-vulnerability scanning; mobile Console access moved from the account menu to the Apps drawer</td></tr><tr><td>0.63.0</td><td>Device bridge protocol package — workstream 0003 leg 1 (RFC 0083, epic task 3.34); general per-IP rate limiting</td></tr><tr><td>0.64.0</td><td>Plugin device surface, permissions, and consent — workstream 0003 leg 2 (RFC 0083, epic task 3.35)</td></tr><tr><td>0.65.0</td><td>Forbidden page, authenticated 404 gate, hardened 500 boundary</td></tr><tr><td>0.65.1</td><td>Offline banner no longer overlaps the mobile header (renders below it on mobile)</td></tr><tr><td>0.65.2</td><td>Mobile shell consumes <code>MobileHeader</code>/<code>MobileFooter</code> — workstream 0007 leg 2 (RFC 0088, epic task 9.24)</td></tr><tr><td>0.65.3</td><td>Fix session gate blocking the custom Web Push worker chunk, which broke the service worker for logged-out visitors</td></tr><tr><td>0.65.4</td><td>Fix swapped logger args in <code>instrumentation.ts</code>; close a <code>tsconfig.json</code> include gap that left the boot path unchecked</td></tr><tr><td>0.66.0</td><td>Per-user service worker page-cache partitioning — workstream 0008 leg 2a (Research 0012, epic task 2.31)</td></tr><tr><td>0.67.0</td><td>Cold-start offline launch flow and the Offline page — workstream 0008 leg 2b (Research 0012, epic task 2.32)</td></tr><tr><td>0.67.1</td><td>Stop bundling plain <code>better-sqlite3</code> into the server graph (Webpack externalization fix)</td></tr><tr><td>0.67.2</td><td>Fix SW <code>runtimeCaching</code> matchers closing over <code>next.config.ts</code> module scope, which had silently disabled custom SW routing</td></tr><tr><td>0.68.0</td><td>Native mobile push relay — device-token schema + registration API, workstream 0005 leg 1 (RFC 0087, epic task 4.7)</td></tr><tr><td>0.69.0</td><td>Well-known first-party OAuth clients for native shells, seeding half (RFC 0072 addendum, epic task 1.24)</td></tr><tr><td>0.70.0</td><td>Native mobile push relay — <code>apps/relay</code> APNs/FCM sending, environment-gated, workstream 0005 leg 2 (RFC 0087, epic task 4.7)</td></tr><tr><td>0.71.0</td><td>Native mobile push relay — <code>fanOutPushToUser</code> native delivery branch + push encryption, workstream 0005 leg 3 (RFC 0087, epic task 4.7)</td></tr><tr><td>0.71.1</td><td>Isolated-Postgres plugin migration-table collision fix (epic task 8.26)</td></tr><tr><td>0.72.0</td><td>Desktop native push scaffold — widen push device tokens to macOS/Windows, workstream 0010 leg 1 (RFC 0087 addendum, epic task 4.8)</td></tr><tr><td>0.73.0</td><td>Retire the <code>database.isolation</code>/<code>&quot;shared&quot;</code> manifest option — every plugin unconditionally isolated (epic task 8.28)</td></tr><tr><td>0.74.0</td><td>Tiered plugin offline model — manifest <code>offline</code> enum + tier states, workstream 0008 leg 3 (Research 0012, epic tasks 3.36, 2.33)</td></tr><tr><td>0.87.1</td><td>Fix <code>/api/admin/{connections,data-grants,email-templates/preview}</code> authorizing off the spoofable <code>x-sovereign-user-role</code> header</td></tr><tr><td>0.89.0</td><td>Plugin-scoped roles and grants — <code>sdk.authz</code> provider/consumer wiring (RFC 0054, workstream 0017 leg 3, epic task 1.13)</td></tr><tr><td>0.90.0</td><td>Notification Center read/manage SDK surface — <code>sdk.notifications.list/markRead/markAllRead/dismiss/dismissAll</code> (RFC 0015 extension), gated by the existing <code>notifications:send</code> permission</td></tr><tr><td>0.91.0</td><td>Instance-level theme preset selection — <code>instanceThemePreset</code>, <code>instance-style.ts</code> delivery (RFC 0095), extending RFC 0094&#39;s <code>packages/ui</code> theme-preset mechanism</td></tr><tr><td>0.91.1</td><td><code>sdk.storage</code> background-invocation fix — <code>resolveStorageContext()</code> falls back to the background-plugin context for job/schedule handlers (<code>@sovereignfs/sdk</code> 1.46.0)</td></tr><tr><td>0.91.2</td><td><code>sdk.env.get()</code> background-invocation fix — <code>resolveEnvPluginId()</code> falls back to the same background-plugin context for job/schedule handlers (<code>@sovereignfs/sdk</code> 1.47.0)</td></tr><tr><td>0.99.1</td><td>better-auth 1.6.30; 1.7.0–1.7.2 skipped (required <code>account.issuer</code> column breaks boot on populated stores, reverted upstream in 1.7.3); genuine 1.6.25/1.7.2 cookie-cache fixtures added to <code>session-verify.test.ts</code> (epic task 1.26, parked)</td></tr></tbody></table><p><strong><code>runtime@0.33.0</code> — activity event name changed:</strong> The <code>settings.tenant_name_changed</code> activity log action has been renamed to <code>settings.instance_name_changed</code>. Historical rows already in your <code>activity_log</code> table are unaffected; new renames produce the new event name. If you have any custom tooling that queries <code>activity_log</code> for this specific action string, update it.</p><p><strong><code>runtime@0.33.0</code> — capability strings renamed:</strong><code>tenant:view</code> → <code>instance:view</code> and <code>tenant:configure</code> → <code>instance:configure</code>. No plugin manifests are known to declare these capabilities directly (they are platform-internal), but if you have custom plugins that gate logic on these strings, update them.</p><p>Some runtime minor versions (e.g. 0.24.0) were used by intermediate sub-tasks or patch releases and are not listed individually.</p><hr><h2 id="v1-0-0-release-checklist" tabindex="-1">v1.0.0 release checklist <a class="header-anchor" href="#v1-0-0-release-checklist" aria-label="Permalink to &quot;v1.0.0 release checklist&quot;">​</a></h2><p>Steps to execute when all pre-v1 tasks in <code>ROADMAP.md</code> are ✅:</p><ol><li><strong>Bump root <code>package.json</code> to <code>1.0.0</code></strong> and <strong>bump <code>runtime/package.json</code> to <code>1.0.0</code></strong> in the same PR — aligning both to the product release milestone. The runtime is the product from an operator&#39;s perspective; keeping both in sync avoids operators seeing <code>sovereign@1.0.0</code> running on <code>runtime@0.3x.0</code>.</li><li><strong>Tag the release</strong>: <code>git tag v1.0.0 &amp;&amp; git push --tags</code>. The Docker image publish workflow produces the <code>v1.0.0</code> GHCR image automatically.</li><li><strong>Update this file</strong> with final transition notes for the last <code>0.9.x → 1.0.0</code> jump, following the same format as the sections above.</li><li><strong>Reorganise upgrade guide section headings</strong> to use root <code>package.json</code> versions (<code>v1.0 → v1.1</code>, etc.) instead of runtime-internal ones going forward. The runtime version map above remains the historical reference for pre-v1 sections.</li><li><strong>Update <code>SOVEREIGN_VERSION</code></strong> in <code>docker-compose.prod.yml</code> to <code>1.0.0</code>.</li><li><strong>Branch convention changes</strong>: <code>main</code> becomes the production branch and <code>dev</code> the integration branch (as noted in <code>CLAUDE.md</code>).</li></ol>',230)])])}const u=o(a,[["render",n]]);export{g as __pageData,u as default};
