import{_ as t,o as n,c as a,a2 as o}from"./chunks/framework.CHeM0PsO.js";const m=JSON.parse('{"title":"Calendar connector: scope and platform fit","description":"","frontmatter":{"id":5,"title":"Calendar connector: scope and platform fit","status":"decided","date":"August 2026","author":"Claude Code (session with the developer)","scope":"mobile","summary":"Candidate Phase 2+ connector — let the model create, update, delete, and query calendar events, and set reminders","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/research/0005-calendar-connector.md"},"headers":[],"relativePath":"sovereign-edge/research/0005-calendar-connector.md","filePath":"sovereign-edge/research/0005-calendar-connector.md"}'),r={name:"sovereign-edge/research/0005-calendar-connector.md"};function s(i,e,c,d,l,h){return n(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="research-0005-—-calendar-connector-scope-and-platform-fit" tabindex="-1">Research 0005 — Calendar connector: scope and platform fit <a class="header-anchor" href="#research-0005-—-calendar-connector-scope-and-platform-fit" aria-label="Permalink to &quot;Research 0005 — Calendar connector: scope and platform fit&quot;">​</a></h1><p><strong>Related:</strong> <a href="./../epics/mobile/connector-framework.html">Connector Framework</a> (epics 2.1–2.5, the permission/manifest machinery this would plug into), <a href="./0006-files-document-summarization.html">Sovereign Tasks connector</a>-adjacent in spirit (a second &quot;direct action&quot; connector), see also <a href="./0009-device-connector.html">0009</a> for the same permission-model question applied to a different local capability</p><hr><h2 id="question" tabindex="-1">Question <a class="header-anchor" href="#question" aria-label="Permalink to &quot;Question&quot;">​</a></h2><p>Of the phone capabilities surveyed in conversation, Calendar was picked as the strongest first candidate for a new connector. What does <code>expo-calendar</code> actually give us on each platform, and what does &quot;set reminders&quot; mean once iOS and Android diverge?</p><h2 id="findings" tabindex="-1">Findings <a class="header-anchor" href="#findings" aria-label="Permalink to &quot;Findings&quot;">​</a></h2><ul><li><code>expo-calendar</code> is the natural module — same ecosystem this project already standardises on (research 0002). It is not currently a dependency; adding it is new work, not a config change.</li><li><strong>iOS (EventKit)</strong> exposes two distinct stores: Calendar events, and a separate Reminders list (<code>EKReminder</code> — due date, priority, completion state). These are genuinely different APIs, not two views of the same data.</li><li><strong>Android (<code>CalendarContract</code>)</strong> only has Calendar events. There is no OS-level Reminders list equivalent to iOS&#39;s — reminders-as-a-concept live in individual apps (Google Keep, Assistant), not a public platform API.</li><li>Permissions: iOS requires <code>NSCalendarsUsageDescription</code>, and since iOS 17 splits calendar access into full vs. write-only tiers (<code>NSCalendarsFullAccessUsageDescription</code> / <code>NSCalendarsWriteOnlyAccessUsageDescription</code>) — which tier we request changes whether the connector can answer &quot;what&#39;s on my calendar&quot; queries at all, or only blind-write new events. Android requires runtime-requested <code>READ_CALENDAR</code>/<code>WRITE_CALENDAR</code> (a dangerous-permission pair).</li><li>A device can have several calendars (personal, work, subscribed/read-only, holidays). Something has to decide which calendar a new event lands in — either a fixed default/primary calendar, or a one-time setup step asking the user to pick, mirroring the pattern the Sovereign Tasks connector epic (4.2) already uses for its own one-time instance/token setup.</li><li><strong>Resolved by reading the actual implementation</strong> (<code>src/connectors/manifest/schema.ts</code>, <code>src/connectors/runtime/execute.ts</code>, <code>src/connectors/permissions/grants.ts</code>): a Calendar connector is not a Tier 1 connector at all. Research 0001 and <a href="./../epics/mobile/connector-framework.html#overview">connector-framework.md</a> define three trust tiers, and Tier 3 — &quot;first-party native OS integration... not opened to third parties&quot; — names <strong>direct Contacts/Calendar writes</strong> explicitly as its motivating example. Tier 1&#39;s manifest schema <em>requires</em> an HTTP <code>origin</code> (<code>request.origin: z.string().url()</code>) and at least one network origin (<code>permissions.network.origins</code>, <code>.min(1)</code>); there is no way to express &quot;call <code>expo-calendar</code> instead of <code>fetch</code>&quot; in that shape. The runtime&#39;s <code>executeConnectorCall()</code> confirms this is expected, not an oversight: its switch has exactly one case (<code>case 1</code>), and task 2.4&#39;s own doc comment reserves the extension point for &quot;Tier 3 (native module dispatch), even if unimplemented until epic 5/9 needs [it]&quot; — Calendar would be the first connector to actually need it.</li><li>The <strong>permission state machine</strong> (<code>not-asked</code>/<code>granted</code>/<code>denied</code>, one record per connector ID, no blanket toggle, in <code>grants.ts</code>) is already fully generic and needs no changes. What&#39;s Tier-1-specific is the <em>scope</em> it tracks: <code>grant()</code>, <code>needsRedecision()</code>, and the stored <code>ConnectorGrant.grantedOrigins</code> field are all hardwired to <code>permissions.network.origins</code>. A Tier 3 connector has no origins — its scope is an OS capability (e.g. &quot;calendar read/write&quot;). Generalizing <code>grantedOrigins</code> into a generic granted-scope concept, filled by origins for Tier 1 and by capability names for Tier 3, is the actual small piece of new work here — not a redesign of the grant/consent model itself.</li></ul><h2 id="options-considered" tabindex="-1">Options considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options considered&quot;">​</a></h2><p><strong>A. Calendar events only, both platforms; model reminders as calendar-event alerts (an alarm offset on an event), not a separate Reminders surface.</strong> Uniform behavior across platforms since EventKit calendar events also support alerts. Recommended.</p><p><strong>B. Calendar events plus iOS EventKit Reminders as a second tool surface.</strong> More complete on iOS, platform-asymmetric, adds manifest and UI surface for a capability Android can&#39;t match.</p><p><strong>C. Defer &quot;reminders&quot; entirely to a future, unrelated <code>expo-notifications</code>-based local-reminders feature; ship Calendar events only.</strong></p><h2 id="recommendation" tabindex="-1">Recommendation <a class="header-anchor" href="#recommendation" aria-label="Permalink to &quot;Recommendation&quot;">​</a></h2><p>Option A. Reminders-as-event-alerts is the one interpretation that works identically on both platforms and ships as a single coherent connector rather than an iOS-only bolt-on.</p><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><ul><li><strong>This is a Tier 3 connector</strong>, not Tier 1 — first-party native OS integration, per research 0001 and connector-framework.md&#39;s own definitions. It is the first connector to actually need Tier 3, so it cannot ship until that reserved extension point (manifest schema variant, generalized grant scope, <code>case 3</code> runtime dispatch) is built — a small, shared prerequisite, not Calendar-specific work. <a href="./0009-device-connector.html">0009</a> and <a href="./0008-health-step-count.html">0008</a> need the exact same scaffolding.</li><li>Tool scope: <code>calendar.create_event</code>, <code>calendar.update_event</code>, <code>calendar.delete_event</code>, <code>calendar.query_events</code>; reminders expressed as an alert on a created event, not a separate API surface.</li><li>Recurrence, timezones, and all-day events are real edge-case surface; MVP should explicitly scope these down (e.g. single, non-recurring, timezone-naive events first) rather than solve them all at once.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Which calendar (of possibly several) new events are written to — fixed default, or a one-time setup step per epic 4.2&#39;s pattern?</li><li><code>expo-calendar</code>&#39;s compatibility with the installed Expo SDK 57 / RN 0.86 toolchain — confirm before implementation, not assumed here.</li><li>Exact shape of the generalized grant-scope concept (see Findings) — a design decision shared across all Tier 3 connectors, not this doc&#39;s to settle alone.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><p><strong>Done:</strong> the Tier 3 scaffolding prerequisite is now tracked as task <a href="./../epics/mobile/connector-framework.html#-26--tier-3-connector-scaffolding">2.6</a>, and this connector has its own epic: <a href="./../epics/mobile/calendar-connector.html">calendar-connector.md</a> (task 10.1), slotted into <a href="./../../ROADMAP.html">ROADMAP.md</a> at 0.2.2 — after 2.6, ahead of the Sovereign Tasks connector.</p><p><strong>Actual next step for whoever picks this up:</strong> task 2.6 has to land first (nothing here is implementable before it — there is no Tier 3 manifest shape or runtime dispatch yet). Once 2.6 is done, task 10.1 can start; the open questions above (which calendar, <code>expo-calendar</code> compatibility) are what&#39;s left to settle during that implementation, not before it.</p>',20)])])}const u=t(r,[["render",s]]);export{m as __pageData,u as default};
