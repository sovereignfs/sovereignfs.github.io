import{_ as s,o as a,c as i,a2 as n}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"RFC 0029 — Internationalization / Localization (i18n)","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0029-internationalization.md","filePath":"rfcs/0029-internationalization.md"}'),t={name:"rfcs/0029-internationalization.md"};function l(o,e,d,r,p,c){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="rfc-0029-—-internationalization-localization-i18n" tabindex="-1">RFC 0029 — Internationalization / Localization (i18n) <a class="header-anchor" href="#rfc-0029-—-internationalization-localization-i18n" aria-label="Permalink to &quot;RFC 0029 — Internationalization / Localization (i18n)&quot;">​</a></h1><p><strong>Status:</strong> Accepted<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/manifest</code>, <code>packages/sdk</code>, <code>packages/db</code>, <code>runtime</code>, <code>apps/auth</code>, <code>plugins/console</code>, <code>plugins/account</code>, <code>scripts/generate-registry.ts</code>, <code>docs/plugin-development.md</code>, <code>docs/self-hosting.md</code>; builds on RFC 0018 (plugin-scoped env), RFC 0022 (plugin capabilities)<br><strong>Incorporated into plan:</strong> Yes — epic tasks 11.1 (infrastructure) and 11.2 (platform shell adoption). <code>@sovereignfs/manifest</code> minor bump, <code>@sovereignfs/sdk</code> minor bump, <code>@sovereignfs/db</code> minor bump.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>RFC 0029 adds first-class internationalization infrastructure to Sovereign. Operators define which languages their instance supports via a Console setting; plugins opt in by declaring an <code>i18n</code> manifest field and providing <code>messages/&lt;locale&gt;.json</code> translation files. The active locale is resolved transparently per request — from the user&#39;s Account preference cookie (<code>sv-locale</code>), falling back to the instance default, falling back to English — and injected as an <code>x-sovereign-user-locale</code> request header. All server and client components consume translations through <strong>next-intl</strong>, the platform-blessed App Router-native library. The generate script merges plugin message files into a unified namespaced bundle at build time.</p><p>Localization is <strong>opt-in for plugins</strong>: a plugin without an <code>i18n</code> manifest field and no <code>messages/</code> directory is unaffected by this feature — it renders English regardless of the active locale, with zero build overhead. Platform chrome (sidebar, header, login page, auth app) ships with translated strings to prove the full stack end-to-end.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign is a self-hosted workspace platform. Instances serve teams that may be primarily non-English speaking — a German company&#39;s internal tools, a Tamil-speaking cooperative&#39;s shared workspace, a multilingual nonprofit. Today the platform is entirely English, with <code>lang=&quot;en&quot;</code> hardcoded in the root layout and no locale concept anywhere in the data model.</p><p>Operators need control over which languages their instance supports — enabling Sinhala without plugins that have Sinhala translations would produce a mixed-language UI, which is worse than a consistent English UI. The system must let the operator gate languages and let plugins declare which locales they cover.</p><p>Plugin developers need a consistent, low-friction path to add translations: a single convention (<code>messages/en.json</code>, <code>messages/de.json</code>) rather than per-plugin library choices that produce inconsistent patterns across the ecosystem.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><code>runtime/app/layout.tsx</code>: <code>&lt;html lang=&quot;en&quot; suppressHydrationWarning&gt;</code> — hardcoded English. <code>suppressHydrationWarning</code> already present for the theme script; locale detection follows the same pattern.</li><li><code>runtime/middleware.ts</code>: already injects <code>x-sovereign-user-role</code>, <code>x-sovereign-user-id</code>, <code>x-sovereign-user-capabilities</code> headers after session verification. Locale injection follows the same pattern.</li><li><code>packages/db/src/bootstrap.ts</code>: <code>platform_settings</code> key-value table already stores <code>root_plugin_id</code>, <code>invite_only</code>, <code>license_*</code> keys. New locale keys land here.</li><li><code>packages/sdk/src/types.ts</code>: <code>PlatformConfig</code> type already used by <code>sdk.platform.getConfig()</code>. New fields <code>enabledLanguages</code> and <code>defaultLanguage</code> extend it.</li><li><code>scripts/generate-registry.ts</code>: already generates <code>runtime/generated/registry.ts</code> and <code>runtime/generated/plugin-env.ts</code> from plugin manifests. Plugin message merging follows the same pattern.</li><li><code>plugins/account/app/preferences/page.tsx</code>: already has timezone and theme preferences. Language preference adds a third row.</li><li><code>plugins/console/app/settings/page.tsx</code>: already has Tenant, Registration, Branding sections. A Languages section is added alongside them.</li><li>Cookie <code>sv-theme</code>: the pattern for a transparent per-user preference stored in a cookie, set by the Account plugin, read by middleware and the root layout. <code>sv-locale</code> mirrors this exactly.</li><li>No existing i18n dependencies — this is greenfield.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-—-platform-config-enabled-languages" tabindex="-1">1 — Platform config: enabled languages <a class="header-anchor" href="#_1-—-platform-config-enabled-languages" aria-label="Permalink to &quot;1 — Platform config: enabled languages&quot;">​</a></h3><p>Two new keys in <code>platform_settings</code> (seeded on first run, no schema change):</p><table tabindex="0"><thead><tr><th>Key</th><th>Value (JSON string)</th><th>Default</th></tr></thead><tbody><tr><td><code>enabled_languages</code></td><td><code>&#39;[&quot;en&quot;,&quot;de&quot;,&quot;si&quot;,&quot;ta&quot;]&#39;</code></td><td><code>&#39;[&quot;en&quot;]&#39;</code></td></tr><tr><td><code>default_language</code></td><td><code>&#39;&quot;en&quot;&#39;</code></td><td><code>&#39;&quot;en&quot;&#39;</code></td></tr></tbody></table><p>English is always in <code>enabled_languages</code> and cannot be removed — it is the base language that all plugins must support as a fallback.</p><p><code>PlatformConfig</code> type gains:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">enabledLanguages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: string[];  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// BCP 47 tags; always includes &#39;en&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defaultLanguage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: string;     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// must be in enabledLanguages</span></span></code></pre></div><p><code>sdk.platform.getConfig()</code> returns these new fields (already async; no signature change).</p><p>A new Edge-readable admin API route mirrors the pattern of <code>/api/admin/plugins/disabled</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /api/admin/i18n</span></span>
<span class="line"><span>Authorization: x-sovereign-admin-key</span></span>
<span class="line"><span>Response: { enabledLanguages: string[], defaultLanguage: string }</span></span></code></pre></div><p>This route is added to <code>RESERVED_API_SEGMENTS</code> in <code>runtime/src/api-namespace.ts</code> (the dir-parity test guards it).</p><h3 id="_2-—-manifest-i18n-field" tabindex="-1">2 — Manifest <code>i18n</code> field <a class="header-anchor" href="#_2-—-manifest-i18n-field" aria-label="Permalink to &quot;2 — Manifest \`i18n\` field&quot;">​</a></h3><p>New optional field in the manifest schema (<code>packages/manifest/src/schema.ts</code>):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;i18n&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;supportedLocales&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;en&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;de&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;si&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Rules:</p><ul><li>The field is entirely <strong>optional</strong>. Its absence means the plugin is English-only and does not participate in message merging.</li><li><code>supportedLocales</code> must be a non-empty array of BCP 47 language tags.</li><li><code>&quot;en&quot;</code> must be present (generate script enforces this — build fails if missing).</li><li>The generate script <strong>fails</strong> if a listed locale has no <code>messages/&lt;locale&gt;.json</code> file in the plugin source directory.</li><li>The generate script <strong>warns</strong> (not fails) when a platform-enabled language is absent from a plugin&#39;s <code>supportedLocales</code>. The plugin falls back to English strings for that locale — which is acceptable but worth surfacing to the Console coverage table.</li></ul><p><code>manifestFieldNames</code> in <code>packages/manifest/src/schema.ts</code> gains <code>&#39;i18n&#39;</code> (required by the docs-parity test in <code>runtime/src/docs-parity.test.ts</code>).</p><h3 id="_3-—-locale-resolution-chain" tabindex="-1">3 — Locale resolution chain <a class="header-anchor" href="#_3-—-locale-resolution-chain" aria-label="Permalink to &quot;3 — Locale resolution chain&quot;">​</a></h3><p>Per-request locale resolution in the middleware:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sv-locale cookie</span></span>
<span class="line"><span>  ↓ present and in enabled_languages? → use it</span></span>
<span class="line"><span>  ↓ else</span></span>
<span class="line"><span>defaultLanguage (from /api/admin/i18n, cached in edge var)</span></span>
<span class="line"><span>  ↓ else</span></span>
<span class="line"><span>&#39;en&#39;</span></span></code></pre></div><p>The middleware injects the resolved locale as:</p><ul><li><code>x-sovereign-user-locale: &lt;locale&gt;</code> on the forwarded request headers (read by <code>next-intl</code>&#39;s <code>getRequestConfig</code>)</li></ul><p>The <code>/api/admin/i18n</code> response is cached in an edge-scoped variable on first fetch, same as the disabled-plugins list. The cache is invalidated when the middleware processes a request from the Console settings save action (passes a <code>x-sovereign-invalidate-i18n</code> header to trigger re-fetch on the next request).</p><h3 id="_4-—-next-intl-integration-no-url-routing" tabindex="-1">4 — next-intl integration (no URL routing) <a class="header-anchor" href="#_4-—-next-intl-integration-no-url-routing" aria-label="Permalink to &quot;4 — next-intl integration (no URL routing)&quot;">​</a></h3><p>Sovereign uses next-intl&#39;s <strong>&quot;without i18n routing&quot;</strong> mode. URLs do not change — there is no <code>/de/console</code> prefix. The locale lives only in the cookie and the request header. This is consistent with how theme and timezone preferences work.</p><p><strong><code>runtime/i18n/request.ts</code></strong> (new file):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { getRequestConfig } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;next-intl/server&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { headers } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;next/headers&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getRequestConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> locale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> headers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;x-sovereign-user-locale&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;en&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> messages</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`../generated/messages/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">locale</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).default;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { locale, messages };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><strong><code>runtime/next.config.ts</code></strong> wraps the existing config with <code>createNextIntlPlugin</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> createNextIntlPlugin </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;next-intl/plugin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> withNextIntl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createNextIntlPlugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./i18n/request.ts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> withNextIntl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withPWA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }));</span></span></code></pre></div><p><strong><code>runtime/app/layout.tsx</code></strong>: <code>NextIntlClientProvider</code> wraps <code>{children}</code> (server component — reads locale from headers, serializes messages). <code>&lt;html lang={locale}&gt;</code> is now dynamic, replacing the hardcoded <code>lang=&quot;en&quot;</code>. The <code>suppressHydrationWarning</code> attribute stays (still needed for the theme script).</p><p><strong>Plugin components</strong> use next-intl hooks with their plugin ID as namespace:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { useTranslations } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;next-intl&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PluginPage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> __t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useTranslations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fs.sovereign.console&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;users.title&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)}&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Platform shell components</strong> use the <code>&#39;platform&#39;</code> namespace:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> __t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useTranslations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;platform&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">span</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;nav.launcher&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)}&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">span</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><h3 id="_5-—-generate-script-message-merging" tabindex="-1">5 — Generate script: message merging <a class="header-anchor" href="#_5-—-generate-script-message-merging" aria-label="Permalink to &quot;5 — Generate script: message merging&quot;">​</a></h3><p>The generate script (<code>scripts/generate-registry.ts</code>) gains a <code>composeMessages()</code> step that runs after plugin composition:</p><ol><li>Reads <code>runtime/messages/&lt;locale&gt;.json</code> (platform shell strings).</li><li>For each installed plugin with an <code>i18n.supportedLocales</code> declaration, reads <code>plugins/&lt;id&gt;/messages/&lt;locale&gt;.json</code>.</li><li>Emits <code>runtime/generated/messages/&lt;locale&gt;.ts</code> for every locale that appears in any plugin&#39;s <code>supportedLocales</code> (union; not limited to platform-enabled locales — the generate script doesn&#39;t read runtime config).</li></ol><p>Generated file shape:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// runtime/generated/messages/en.ts  (auto-generated — do not edit)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  platform: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;nav.launcher&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Launcher&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;nav.console&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Console&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;fs.sovereign.console&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;users.title&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;plugins.title&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Plugins&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;fs.sovereign.account&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;preferences.language&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Language&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Generated files are added to <code>runtime/generated/.gitignore</code> (alongside <code>registry.ts</code> and <code>plugin-env.ts</code>).</p><p><strong>Validation in generate script:</strong></p><ul><li>If <code>i18n.supportedLocales</code> contains a locale but <code>messages/&lt;locale&gt;.json</code> is absent → <code>process.exit(1)</code> with a clear error message.</li><li>If <code>i18n.supportedLocales</code> does not include <code>&quot;en&quot;</code> → <code>process.exit(1)</code>.</li><li>If two plugins declare conflicting message keys within the same namespace → warning (last-write wins, by load order; unique namespaces per plugin ID make conflicts structurally impossible within a single plugin, but platform vs. plugin keys could theoretically collide — they don&#39;t since they use different namespace keys).</li></ul><h3 id="_6-—-sdk-surface-sdk-i18n-experimental" tabindex="-1">6 — SDK surface: <code>sdk.i18n</code> (experimental) <a class="header-anchor" href="#_6-—-sdk-surface-sdk-i18n-experimental" aria-label="Permalink to &quot;6 — SDK surface: \`sdk.i18n\` (experimental)&quot;">​</a></h3><p>New module <code>packages/sdk/src/i18n.ts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// All three are async — consistent with the platform data-layer contract.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getLocale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getEnabledLanguages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]&gt;;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getDefaultLanguage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><ul><li><code>getLocale()</code>: reads <code>x-sovereign-user-locale</code> from <code>next/headers()</code>. Convenience wrapper so plugins don&#39;t need to import <code>next/headers</code> directly.</li><li><code>getEnabledLanguages()</code> / <code>getDefaultLanguage()</code>: delegates to host, which reads from <code>platform_settings</code> (same path as <code>sdk.platform.getConfig()</code> for other settings).</li></ul><p><code>SdkHost</code> interface gains an <code>i18n</code> key:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">i18n</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  getLocale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  getEnabledLanguages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">string[]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  getDefaultLanguage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Implementation in <code>runtime/src/sdk-host.ts</code> reads from the platform DB.</p><p><code>sdk.i18n</code> is marked <strong>experimental</strong> in <code>docs/sdk-stability.md</code> — not covered by the semver guarantee; shape may change as ICU formatting and RTL support are added.</p><p><code>sdk</code> export in <code>packages/sdk/src/index.ts</code> gains <code>i18n</code> in the experimental group.</p><h3 id="_7-—-db-account-prefs-locale-column" tabindex="-1">7 — DB: <code>account_prefs.locale</code> column <a class="header-anchor" href="#_7-—-db-account-prefs-locale-column" aria-label="Permalink to &quot;7 — DB: \`account_prefs.locale\` column&quot;">​</a></h3><p>New nullable <code>locale TEXT</code> column on the <code>account_prefs</code> table. <code>NULL</code> means &quot;follow the platform default.&quot; Drizzle-kit migration for both dialects:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>packages/db/migrations/sqlite/0005_account_locale.sql</span></span>
<span class="line"><span>packages/db/migrations/postgres/0005_account_locale.sql</span></span></code></pre></div><p>The Account plugin&#39;s <code>PATCH /api/account/preferences</code> server action already handles arbitrary preference fields; it gains a <code>locale</code> field that writes <code>account_prefs.locale</code> and clears the <code>sv-locale</code> cookie (<code>maxAge: 0</code>) so the next request re-resolves fresh. This mirrors the pattern used for the <code>sv-theme</code> cookie on theme change.</p><h3 id="_8-—-translation-file-convention" tabindex="-1">8 — Translation file convention <a class="header-anchor" href="#_8-—-translation-file-convention" aria-label="Permalink to &quot;8 — Translation file convention&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/&lt;id&gt;/</span></span>
<span class="line"><span>  messages/</span></span>
<span class="line"><span>    en.json          ← required (base language)</span></span>
<span class="line"><span>    de.json          ← optional; must be declared in i18n.supportedLocales</span></span>
<span class="line"><span>    si.json</span></span>
<span class="line"><span>    ta.json</span></span>
<span class="line"><span>  app/               ← unchanged; route segments as today</span></span>
<span class="line"><span>  manifest.json      ← gains i18n field</span></span>
<span class="line"><span></span></span>
<span class="line"><span>runtime/</span></span>
<span class="line"><span>  messages/</span></span>
<span class="line"><span>    en.json          ← platform shell strings (sidebar, nav, errors)</span></span>
<span class="line"><span>    de.json</span></span>
<span class="line"><span>    si.json</span></span>
<span class="line"><span>    ta.json</span></span>
<span class="line"><span>  generated/</span></span>
<span class="line"><span>    messages/</span></span>
<span class="line"><span>      en.ts          ← AUTO-GENERATED by generate script</span></span>
<span class="line"><span>      de.ts</span></span>
<span class="line"><span>      si.ts</span></span>
<span class="line"><span>      ta.ts</span></span></code></pre></div><p><strong>Message key naming convention</strong>: flat namespaced keys using dot notation within a locale file. No nesting beyond two levels (<code>section.key</code>):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;users.title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Users&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;users.invite&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Invite user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;plugins.title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Plugins&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;plugins.toggle.enable&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Enable&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;plugins.toggle.disable&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Disable&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Translation hook naming convention</strong>: the return value of <code>useTranslations()</code> is always bound to <code>__t</code> (double underscore). This makes translation call-sites visually distinct and easy to grep, and avoids shadowing any ambient <code>t</code> variable. Plugin developers must follow this convention; it is documented in <code>docs/plugin-development.md</code> and enforced by code review.</p><p><strong>ICU message format</strong>: next-intl supports ICU syntax (<code>{count, plural, one {# item} other {# items}}</code>). This RFC does not mandate ICU — simple <code>{name}</code> interpolation is sufficient for v1 translations. Richer ICU usage (plurals, select, date/number formatting) is deferred to the open questions.</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><h3 id="flow-1-admin-enables-a-new-language" tabindex="-1">Flow 1: Admin enables a new language <a class="header-anchor" href="#flow-1-admin-enables-a-new-language" aria-label="Permalink to &quot;Flow 1: Admin enables a new language&quot;">​</a></h3><ol><li>Admin opens Console → Settings → Languages section.</li><li>Sees a list of ~30 languages. English is checked and greyed out (cannot disable).</li><li>Checks &quot;Tamil (ta)&quot;. The plugin coverage table below shows which installed plugins have <code>messages/ta.json</code> (green) and which do not (amber, will fall back to English).</li><li>Saves. <code>updateLanguageSettingsAction</code> writes <code>enabled_languages: [&quot;en&quot;,&quot;de&quot;,&quot;si&quot;,&quot;ta&quot;]</code> to <code>platform_settings</code>.</li><li>On the next request, the middleware picks up the updated value from <code>/api/admin/i18n</code>.</li></ol><h3 id="flow-2-user-selects-their-language" tabindex="-1">Flow 2: User selects their language <a class="header-anchor" href="#flow-2-user-selects-their-language" aria-label="Permalink to &quot;Flow 2: User selects their language&quot;">​</a></h3><ol><li>User opens Account → Preferences.</li><li>Sees a &quot;Language&quot; dropdown (new, below Theme). Options: &quot;Follow instance default (English)&quot; <ul><li>the enabled languages (&quot;German&quot;, &quot;Sinhala&quot;, &quot;Tamil&quot;).</li></ul></li><li>Selects &quot;Sinhala&quot;. On save, the server action writes <code>account_prefs.locale = &#39;si&#39;</code> and sets <code>sv-locale=si</code> cookie (<code>HttpOnly</code>, <code>SameSite=Lax</code>, no <code>Secure</code> in dev).</li><li>On next page load, middleware reads <code>sv-locale=si</code> → validates against enabled_languages → resolves to <code>&#39;si&#39;</code> → sets <code>x-sovereign-user-locale: si</code>.</li><li><code>getRequestConfig</code> loads <code>runtime/generated/messages/si.ts</code>. All translated components render in Sinhala. <code>&lt;html lang=&quot;si&quot;&gt;</code> is set.</li></ol><h3 id="flow-3-plugin-with-no-translations" tabindex="-1">Flow 3: Plugin with no translations <a class="header-anchor" href="#flow-3-plugin-with-no-translations" aria-label="Permalink to &quot;Flow 3: Plugin with no translations&quot;">​</a></h3><ol><li>Plugin <code>com.example.tasks</code> has no <code>i18n</code> field and no <code>messages/</code> directory.</li><li>Generate script ignores it entirely. Its routes are not affected.</li><li>When a Tamil-locale user visits the plugin, all strings render in English (the plugin&#39;s hardcoded strings). No errors, no fallback logic needed — the plugin just doesn&#39;t participate in the locale system.</li></ol><h2 id="auth-app-locale-handling" tabindex="-1">Auth app locale handling <a class="header-anchor" href="#auth-app-locale-handling" aria-label="Permalink to &quot;Auth app locale handling&quot;">​</a></h2><p>The auth app (<code>apps/auth/</code>) runs on a separate origin (<code>:3001</code> in dev; same domain in production via reverse proxy). The <code>sv-locale</code> cookie is set on the runtime origin (<code>:3000</code> in dev) and is not automatically shared with the auth origin in dev.</p><p><strong>Production (single domain, reverse proxy):</strong> Cookies are same-domain — auth middleware reads <code>sv-locale</code> exactly as the runtime middleware does.</p><p><strong>Dev (split origins):</strong> Auth middleware uses the <code>Accept-Language</code> request header for locale detection, falling back to <code>en</code>. Additionally, the runtime middleware passes <code>?locale=&lt;locale&gt;</code> as a query parameter in the login redirect URL, allowing auth to pre-select the correct locale without the cookie.</p><p>The auth app gets its own translation infrastructure:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>apps/auth/</span></span>
<span class="line"><span>  messages/</span></span>
<span class="line"><span>    en.json     ← login, register, 2FA, password reset strings</span></span>
<span class="line"><span>    de.json</span></span>
<span class="line"><span>    si.json</span></span>
<span class="line"><span>    ta.json</span></span>
<span class="line"><span>  i18n/</span></span>
<span class="line"><span>    request.ts  ← reads sv-locale cookie (prod) or Accept-Language (dev fallback)</span></span></code></pre></div><p>The auth <code>next.config.ts</code> is independently wrapped with <code>createNextIntlPlugin</code>.</p><p>This split-origin dev limitation is documented in <code>docs/self-hosting.md</code> (under &quot;Language config&quot;) and noted in the Console Languages section as &quot;Auth login page uses browser language in development; both are in sync in production.&quot;</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="url-prefix-routing-de-console-ta-launcher" tabindex="-1">URL-prefix routing (<code>/de/console</code>, <code>/ta/launcher</code>) <a class="header-anchor" href="#url-prefix-routing-de-console-ta-launcher" aria-label="Permalink to &quot;URL-prefix routing (\`/de/console\`, \`/ta/launcher\`)&quot;">​</a></h3><p>Next.js App Router has built-in support for locale-prefixed routing via <code>next-intl</code>&#39;s middleware. Rejected because:</p><ul><li>Every plugin&#39;s <code>routePrefix</code> becomes a locale-prefixed segment, complicating the generate script&#39;s route composition, the middleware&#39;s admin API namespace split, the root-plugin rewrite logic, and all internal links.</li><li>The URL structure becomes a public contract — renaming a locale breaks bookmarks and integrations.</li><li>For a self-hosted workspace (not a public-facing marketing site), SEO-driven locale routing has no value.</li><li>The <code>sv-locale</code> cookie pattern is already established for <code>sv-theme</code>; locale fits the same model exactly.</li></ul><h3 id="library-agnostic-no-blessed-library" tabindex="-1">Library-agnostic (no blessed library) <a class="header-anchor" href="#library-agnostic-no-blessed-library" aria-label="Permalink to &quot;Library-agnostic (no blessed library)&quot;">​</a></h3><p>Define the manifest field and <code>sdk.i18n.getLocale()</code> contract without prescribing how plugins load and apply messages. Rejected because:</p><ul><li>Each plugin would invent its own message-loading approach, producing inconsistent patterns across the ecosystem.</li><li>The generate script cannot merge messages into a unified bundle without knowing the format and loading convention.</li><li>The plugin starter template (RFC 0017) would have nothing useful to scaffold for translations.</li><li>next-intl is the dominant App Router–native solution, actively maintained, and has zero runtime dependencies when used in RSC-only mode (<code>next-intl/server</code>).</li></ul><h3 id="opt-out-all-plugins-are-localized-by-default" tabindex="-1">Opt-out (all plugins are localized by default) <a class="header-anchor" href="#opt-out-all-plugins-are-localized-by-default" aria-label="Permalink to &quot;Opt-out (all plugins are localized by default)&quot;">​</a></h3><p>Make English the default namespace and require plugins to opt out if they don&#39;t want to participate. Rejected because:</p><ul><li>The overwhelming majority of existing plugins have no translations. An opt-out model would generate empty <code>messages/en.json</code> stubs or silently include plugins with no strings, adding noise to the generated bundle.</li><li>Breaking existing plugins implicitly — a plugin that ships without translations would produce missing-key warnings for every string in a locale-switched context.</li><li>Opt-in is the right contract: you get localization when you declare it and provide the files.</li></ul><h3 id="separate-locale-service-edge-config-store" tabindex="-1">Separate locale service / edge config store <a class="header-anchor" href="#separate-locale-service-edge-config-store" aria-label="Permalink to &quot;Separate locale service / edge config store&quot;">​</a></h3><p>Cache enabled_languages in an edge-compatible key-value store (e.g. Next.js data cache with <code>revalidateTag</code>) rather than a <code>GET /api/admin/i18n</code> fetch in middleware. Rejected because:</p><ul><li>The existing pattern for edge-gated platform data (disabled plugins, root plugin ID) is already a fetch-and-cache in middleware. Consistency matters more than a minor performance delta.</li><li>A separate cache layer adds a new abstraction without a compelling reason given the low frequency of language config changes.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><p><strong>RTL language support</strong> — Arabic, Hebrew, Persian, and others require <code>dir=&quot;rtl&quot;</code> on <code>&lt;html&gt;</code> and CSS logical properties (<code>margin-inline-start</code> instead of <code>margin-left</code>). The design system (<code>packages/ui</code>) is not RTL-aware today. RTL is out of scope for Tasks 0.9.4–0.9.5 but should be tracked as a follow-up. The RFC should reserve the BCP 47 locale tags for RTL languages in the Console language list even if they are disabled in v1.</p></li><li><p><strong>ICU message format vs. simple interpolation</strong> — next-intl supports full ICU syntax for plurals (<code>{count, plural, one {# item} other {# items}}</code>), gender, and select. Do we mandate ICU for plugin translation files in v1, or leave it to the plugin author? A convention recommendation should land in <code>docs/plugin-development.md</code> but may not need to be enforced by the schema.</p></li><li><p><strong>Number and date formatting</strong> — <code>Intl.NumberFormat</code> and <code>Intl.DateTimeFormat</code> are browser-native and locale-aware. <code>next-intl</code> exposes <code>useFormatter()</code> for these. Should <code>sdk.i18n</code> provide a formatting helper, or should plugin authors call <code>useFormatter()</code> from next-intl directly? The latter is simpler but couples plugins to next-intl more deeply.</p></li><li><p><strong>Machine-translation CI tooling</strong> — some ecosystems provide a CI step that detects untranslated keys when new strings are added to <code>en.json</code>. This could be a useful registry/CI addition but is out of scope for v1.</p></li><li><p><strong>Per-plugin locale fallback beyond English</strong> — if a plugin declares <code>supportedLocales: [&quot;en&quot;, &quot;de&quot;]</code> and the active locale is Tamil, it falls back to English. Is this always the right fallback, or should plugins be able to declare an alternative fallback chain (<code>&quot;ta&quot; → &quot;si&quot; → &quot;en&quot;</code>)? Deferred to a follow-up RFC if the need arises.</p></li><li><p><strong>Auth app locale cookie sharing in dev</strong> — the <code>?locale=&lt;locale&gt;</code> redirect param is a workaround for the split-origin cookie limitation. A cleaner long-term solution might be an auth-app-specific locale API endpoint that the runtime middleware calls on login redirect, but this adds latency. Deferred until dev ergonomics are validated in practice.</p></li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><h3 id="task-1-0-3-—-i18n-infrastructure" tabindex="-1">Task 1.0.3 — i18n infrastructure <a class="header-anchor" href="#task-1-0-3-—-i18n-infrastructure" aria-label="Permalink to &quot;Task 1.0.3 — i18n infrastructure&quot;">​</a></h3><ul><li><code>packages/manifest</code>: new optional <code>i18n</code> field in schema, validation, <code>manifestFieldNames</code> update. Minor bump.</li><li><code>packages/db</code>: new <code>locale TEXT</code> column + drizzle-kit migration (both dialects). <code>PlatformConfig</code> gains <code>enabledLanguages</code> / <code>defaultLanguage</code>. Minor bump.</li><li><code>packages/sdk</code>: new <code>sdk.i18n</code> module (<code>getLocale</code>, <code>getEnabledLanguages</code>, <code>getDefaultLanguage</code>); <code>SdkHost</code> gains <code>i18n</code> key; host implementation in <code>runtime/src/sdk-host.ts</code>. Minor bump. Marked experimental in <code>docs/sdk-stability.md</code>.</li><li><code>scripts/generate-registry.ts</code>: <code>composeMessages()</code> step; validation rules. Generated files in <code>runtime/generated/messages/</code> (gitignored).</li><li><code>runtime/middleware.ts</code>: locale resolution + <code>x-sovereign-user-locale</code> injection. New <code>GET /api/admin/i18n</code> route. <code>RESERVED_API_SEGMENTS</code> update. Minor bump.</li><li><code>runtime/i18n/request.ts</code>: new file. <code>runtime/next.config.ts</code>: <code>createNextIntlPlugin</code> wrapper. Root layout: <code>NextIntlClientProvider</code>, dynamic <code>lang</code> attribute.</li><li><code>plugins/console</code>: Languages section in Settings (enable/disable, default, coverage table). Minor bump.</li><li><code>plugins/account</code>: Language preference in Preferences tab. Minor bump.</li><li>Docs: <code>docs/plugin-development.md</code> gains &quot;i18n&quot; section (manifest field, file convention, <code>sdk.i18n</code>, next-intl usage). <code>docs/self-hosting.md</code> gains &quot;Language config&quot; section (enabled languages, default language env vars). Both docs are required for the docs-parity test to pass.</li><li>New dep: <code>next-intl</code> in <code>runtime/package.json</code>.</li></ul><p>Root <code>package.json</code> does not bump (post-v1 task; ships after v1.0.0).</p><h3 id="task-1-0-4-—-platform-shell-i18n-adoption" tabindex="-1">Task 1.0.4 — Platform shell i18n adoption <a class="header-anchor" href="#task-1-0-4-—-platform-shell-i18n-adoption" aria-label="Permalink to &quot;Task 1.0.4 — Platform shell i18n adoption&quot;">​</a></h3><p>The platform ships with four built-in languages as its reference translation set:</p><table tabindex="0"><thead><tr><th>Locale</th><th>Language</th><th>Script</th></tr></thead><tbody><tr><td><code>en</code></td><td>English</td><td>Latin</td></tr><tr><td><code>de</code></td><td>German</td><td>Latin</td></tr><tr><td><code>si</code></td><td>Sinhala</td><td>Sinhala (non-Latin)</td></tr><tr><td><code>ta</code></td><td>Tamil</td><td>Tamil (non-Latin)</td></tr></tbody></table><p>Sinhala and Tamil use non-Latin scripts and exercise the full stack beyond ASCII — they validate that <code>getRequestConfig</code> loads the correct message file, that <code>lang={locale}</code> propagates to <code>&lt;html&gt;</code>, and that the runtime&#39;s default font stack falls back gracefully to system fonts for these scripts (no custom font bundling required in v1; system fonts cover Sinhala and Tamil on modern OSes and Android).</p><p>Deliverables:</p><ul><li><code>runtime/messages/{en,de,si,ta}.json</code>: all platform chrome strings (sidebar nav labels, offline banner, health page labels, error messages, etc.)</li><li><code>plugins/console/messages/{en,de,si,ta}.json</code>: all Console UI strings; <code>manifest.json</code> gains <code>i18n: { supportedLocales: [&quot;en&quot;,&quot;de&quot;,&quot;si&quot;,&quot;ta&quot;] }</code></li><li><code>plugins/account/messages/{en,de,si,ta}.json</code>: all Account UI strings; same manifest update</li><li><code>apps/auth/messages/{en,de,si,ta}.json</code>: login, register, 2FA, password-reset strings; <code>apps/auth/i18n/request.ts</code>; <code>apps/auth/next.config.ts</code> wrapped with <code>createNextIntlPlugin</code>. Minor bump.</li><li>RFC 0017 plugin starter template (<code>packages/create-plugin</code>): scaffold includes a <code>messages/en.json</code> stub and next-intl usage example.</li><li>E2E test: language switch to German (Latin) and Tamil (non-Latin) → Console/Account UI strings render in the selected language.</li></ul><p>Root <code>package.json</code> does not bump (post-v1 task; ships after v1.0.0).</p><h3 id="semver-summary" tabindex="-1">Semver summary <a class="header-anchor" href="#semver-summary" aria-label="Permalink to &quot;Semver summary&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Package</th><th>Change</th></tr></thead><tbody><tr><td><code>@sovereignfs/manifest</code></td><td>Minor — new optional <code>i18n</code> field (additive)</td></tr><tr><td><code>@sovereignfs/sdk</code></td><td>Minor — new experimental <code>sdk.i18n</code> surface (additive)</td></tr><tr><td><code>@sovereignfs/db</code></td><td>Minor — new <code>locale</code> column + migration</td></tr><tr><td><code>runtime</code></td><td>Minor — next-intl wiring, middleware, generate, API route</td></tr><tr><td><code>plugins/console</code></td><td>Minor — Languages section</td></tr><tr><td><code>plugins/account</code></td><td>Minor — Language preference</td></tr><tr><td><code>apps/auth</code></td><td>Minor — i18n/request.ts, messages, next.config.ts change</td></tr></tbody></table><p>No breaking changes to any published package surface.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft</td></tr></tbody></table>`,119)])])}const k=s(t,[["render",l]]);export{g as __pageData,k as default};
