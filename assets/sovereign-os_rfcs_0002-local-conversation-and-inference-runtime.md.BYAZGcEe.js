import{_ as a,o as t,c as n,a2 as i}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC-0002: Local Conversation and Inference Runtime Architecture","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/rfcs/0002-local-conversation-and-inference-runtime.md"},"headers":[],"relativePath":"sovereign-os/rfcs/0002-local-conversation-and-inference-runtime.md","filePath":"sovereign-os/rfcs/0002-local-conversation-and-inference-runtime.md"}'),r={name:"sovereign-os/rfcs/0002-local-conversation-and-inference-runtime.md"};function o(s,e,l,c,d,h){return t(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0002-local-conversation-and-inference-runtime-architecture" tabindex="-1">RFC-0002: Local Conversation and Inference Runtime Architecture <a class="header-anchor" href="#rfc-0002-local-conversation-and-inference-runtime-architecture" aria-label="Permalink to &quot;RFC-0002: Local Conversation and Inference Runtime Architecture&quot;">​</a></h1><p><strong>Status:</strong> Accepted (2026-08-09, project creator) <strong>Author:</strong> Project creator and Claude <strong>Created:</strong> 2026-08-09 <strong>Reviewers:</strong> Project creator <strong>Target phase:</strong> <a href="./../roadmap/01-2-local-conversation-capabilities.html">Milestone 01.2, Local Conversation and Capabilities</a><strong>Supersedes:</strong> None</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Define the architecture for Sovereign&#39;s own local conversation experience and the provider-neutral inference boundary underneath it: what the Conversation Service owns, the contract every model runner must speak through, and how Sovereign manages model artifacts independently of whichever runner is active. This RFC does not define the capability registry, the AI capability invocation schema, or any specific capability (those are RFC-0003, RFC-0004, and RFC-0006, reserved in <a href="./README.html">docs/rfcs/README.md</a> for this same milestone) — it defines the runtime those capabilities will be proposed and executed through.</p><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p>Milestone 2 (Production Update Operations) is now complete: the appliance update boundary — signed manifests, health-gated activation, rollback, persistent-data backup/restore, retention, and trust rotation — is implemented and hardware-qualified (see <a href="./0014-appliance-update-system.html">RFC-0014</a>, Accepted). Milestone 4 (Local Conversation and Capabilities) depends on exactly that stable boundary and is otherwise unblocked, but nothing in this milestone has an approved architecture yet. Direction research exists (<a href="./../research/local-ai-options.html">docs/research/local-ai-options.md</a>) and the milestone plan (<a href="./../roadmap/01-2-local-conversation-capabilities.html">docs/roadmap/01-2-local-conversation-capabilities.md</a>) names eight required documents before implementation may cross each boundary; this RFC is the first of those.</p><p>Without a defined boundary now, an implementation would naturally couple itself to whichever runner (llama.cpp, Ollama) is easiest to integrate first, the way <code>docs/research/local-ai-options.md</code> already warns against. That coupling would be expensive to undo later: model management, capability-call parsing, and conversation persistence would all need to assume one runner&#39;s idioms instead of a contract Sovereign controls.</p><h2 id="goals" tabindex="-1">Goals <a class="header-anchor" href="#goals" aria-label="Permalink to &quot;Goals&quot;">​</a></h2><ul><li>Define what the Conversation Service is responsible for, independent of any runner or model.</li><li>Define the inference provider contract every runner (local or, later, remote) must implement, so the runner is a replaceable implementation detail rather than an architectural dependency.</li><li>Define what Sovereign&#39;s own model management owns versus what it delegates to the runner.</li><li>Define the resource, privacy, and safety boundaries inference must respect on the Raspberry Pi 5 target, including coexistence with Pi-hole&#39;s DNS-latency requirements.</li><li>Establish the runner-selection method (benchmark criteria and pass/fail budgets) without prejudging the result before real hardware measurement.</li></ul><h2 id="non-goals" tabindex="-1">Non-Goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-Goals&quot;">​</a></h2><ul><li>The capability registry, executor, or any specific capability&#39;s schema (RFC-0003, RFC-0004).</li><li>Pi-hole capability mapping specifics (RFC-0006).</li><li>The <code>web.search</code>/<code>web.fetch</code> privacy design in full detail (named as a required document in the milestone plan, tracked separately, though the architectural boundary — inference has no generic outbound network access — is established here).</li><li>The actual runner/model selection. That is an ADR made after a real Raspberry Pi 5 benchmark, not a decision this RFC can make on paper.</li><li>Voice input/output, autonomous background agents, multi-user collaboration, or a general plugin marketplace — all explicitly out of scope for the whole milestone per the milestone plan&#39;s Non-Scope section.</li><li>Remote/hosted inference as anything but a later, explicitly configured, replaceable option. Local inference is the only default path this RFC designs for.</li></ul><h2 id="context-and-evidence" tabindex="-1">Context and Evidence <a class="header-anchor" href="#context-and-evidence" aria-label="Permalink to &quot;Context and Evidence&quot;">​</a></h2><ul><li><a href="./../research/local-ai-options.html">docs/research/local-ai-options.md</a> (Direction selected; hardware benchmark pending) already establishes that Sovereign must own a provider-neutral inference boundary and model lifecycle, names llama.cpp as the preferred benchmark candidate and Ollama as the comparison candidate, and specifies the benchmark method (time-to-first-token, memory, thermal, structured-output accuracy, DNS-latency impact) this RFC&#39;s runner-evaluation section reuses rather than re-deriving.</li><li><a href="./../roadmap/01-2-local-conversation-capabilities.html">docs/roadmap/01-2-local-conversation-capabilities.md</a> is the accepted milestone plan; its Architectural Commitments, Functional Components, and Exit Criteria sections are the direct source for this RFC&#39;s Proposal section. Where this RFC repeats language from that plan, it is making the plan&#39;s commitments RFC-concrete rather than introducing new scope.</li><li>Every appliance-update RFC and ADR to date (RFC-0014, RFC-0015, RFC-0016, ADR-0006, ADR-0011) establishes the pattern this RFC follows: a signed, versioned, health-gated boundary that treats the underlying mechanism (runner, in this case) as replaceable. The inference provider contract below is deliberately shaped the same way appliance releases and base-OS releases are — a stable interface the platform depends on, with a swappable implementation behind it.</li><li>This project has exactly one physical qualification device (a Raspberry Pi 5) and one maintainer. Nothing here proposes infrastructure (a model registry service, GPU offload, multi-tenant serving) that this project&#39;s actual scale would not use.</li></ul><h2 id="proposal" tabindex="-1">Proposal <a class="header-anchor" href="#proposal" aria-label="Permalink to &quot;Proposal&quot;">​</a></h2><h3 id="conversation-service" tabindex="-1">Conversation Service <a class="header-anchor" href="#conversation-service" aria-label="Permalink to &quot;Conversation Service&quot;">​</a></h3><p>Sovereign owns the conversation API, user experience, capability policy, and model catalog — never delegated to a runner or a third-party UI (Open WebUI is explicitly a development/evaluation tool only, per the milestone plan; it is not the product interface and is not authoritative for identity, permissions, capabilities, or conversation policy).</p><p>The Conversation Service is responsible for:</p><ul><li>accepting and streaming local conversations over Sovereign&#39;s own API, loopback-scoped the same way <code>sovereign-console-auth</code> is (<a href="./0007-console-authentication.html">ADR-0007</a> — the equivalent auth boundary should be reused, not redesigned, once this RFC is implemented);</li><li>constructing bounded model context (message history, capability results, and any retrieved evidence), with an explicit, documented bound rather than an unbounded context window;</li><li>exposing model, runner, and degraded-state information to the client (which model, which runner, healthy or degraded, local or would-be remote) so the interface can honestly represent what answered the question;</li><li>returning citations and capability results as structured data alongside generated prose, not embedded only in free text a client would have to re-parse; and</li><li>storing no conversation beyond a documented retention policy (the policy itself is deferred to the data-inventory update named in the milestone plan&#39;s required-documents list, not fixed by this RFC).</li></ul><h3 id="inference-provider-boundary" tabindex="-1">Inference Provider Boundary <a class="header-anchor" href="#inference-provider-boundary" aria-label="Permalink to &quot;Inference Provider Boundary&quot;">​</a></h3><p>Every runner — local now, remote later if ever added — implements the same contract. No component outside the provider adapter itself may depend on a specific runner&#39;s API shape. The contract must support:</p><ul><li>chat/text generation, given bounded context;</li><li>streamed token output;</li><li>structured JSON output;</li><li>capability/tool-call proposals, returned as structured data the executor (RFC-0003/0004) validates independently — the provider boundary&#39;s job is to surface a proposal, never to execute one;</li><li>cancellation and timeout, with a bounded worst-case resource hold; and</li><li>health status and model/runtime identity (model digest, quantization, runtime version), so a response can always be attributed to exactly what produced it.</li></ul><p>Embeddings are intentionally excluded from the initial contract, per the milestone plan, until a retrieval use case actually requires them — adding an unused capability now would be speculative.</p><p>The provider boundary must not expose an inference HTTP port to the LAN. Local inference is the default and only path this RFC designs an implementation for; remote inference is named only as a future, explicitly configured option so today&#39;s contract doesn&#39;t foreclose it without committing any engineering effort to it now.</p><h3 id="sovereign-model-management" tabindex="-1">Sovereign Model Management <a class="header-anchor" href="#sovereign-model-management" aria-label="Permalink to &quot;Sovereign Model Management&quot;">​</a></h3><p>Sovereign — not the runner — owns model identity and lifecycle, independent of which runner is active. This mirrors the same ownership principle the appliance updater already applies to appliance releases: the mechanism executing an artifact does not get to define the artifact&#39;s trust or lifecycle rules. Concretely, Sovereign owns:</p><ul><li>model identity and compatibility metadata (which runner(s) a given model artifact is valid for);</li><li>source, license, digest, size, quantization, and chat-template metadata;</li><li>download or offline import with checksum verification before a model is ever loaded;</li><li>activation, rollback, health, and storage lifecycle; and</li><li>persistent storage beneath <code>/data/sovereign/models/</code> — inside the same persistent-data boundary the backup/restore contract in <a href="./../../update/BACKUP_AND_JOURNAL.html">BACKUP_AND_JOURNAL.md</a> already protects, though model artifacts are large, replaceable-by-redownload data and a later decision may reasonably exclude them from the backup archive roles that document defines — that inclusion/exclusion decision is left to the data-inventory update, not fixed here.</li></ul><h3 id="runner-evaluation" tabindex="-1">Runner Evaluation <a class="header-anchor" href="#runner-evaluation" aria-label="Permalink to &quot;Runner Evaluation&quot;">​</a></h3><p>This RFC does not select a runner. It commits to the evaluation method <code>local-ai-options.md</code> already specifies: llama.cpp and Ollama run behind the same provider contract on the actual Raspberry Pi 5 qualification device, measuring time-to-first-token, generation rate, peak/steady memory, load time, thermal behavior under sustained load, structured- output and capability-selection accuracy, cancellation/crash recovery, storage overhead, and — critically for this device, which also serves DNS — Pi-hole DNS latency and failure rate with and without active inference. A runner passes only inside explicit resource and DNS budgets recorded at benchmark time; convenience is not a selection criterion by itself. The result is recorded in an ADR after real measurement, per <code>local-ai-options.md</code>&#39;s own Follow-up Decisions — this RFC only commits to the method existing and being followed.</p><h3 id="safety-boundary" tabindex="-1">Safety Boundary <a class="header-anchor" href="#safety-boundary" aria-label="Permalink to &quot;Safety Boundary&quot;">​</a></h3><p>Model output — generated text and any proposed capability call — is always untrusted. This RFC establishes the boundary the provider adapter and Conversation Service must respect regardless of which capability system (RFC-0003/0004) eventually validates a proposal in detail:</p><ul><li>the runner process has no shell access, no Docker socket access, no service credentials, and no unrestricted network access;</li><li>a capability proposal from a model is data, never a command — nothing in the Conversation Service or provider adapter executes a proposal itself; only the (separately specified) deterministic executor does, after independent schema and permission validation; and</li><li>inference must not degrade Pi-hole DNS responsiveness beyond the budget the runner-evaluation benchmark records — if a runner cannot stay inside that budget under realistic load, it fails qualification regardless of model quality.</li></ul><h2 id="interfaces-and-data-flow" tabindex="-1">Interfaces and Data Flow <a class="header-anchor" href="#interfaces-and-data-flow" aria-label="Permalink to &quot;Interfaces and Data Flow&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client (Sovereign conversation UI)</span></span>
<span class="line"><span>    &lt;-&gt; Conversation Service (auth, context construction, streaming,</span></span>
<span class="line"><span>         retention, structured citations/capability results)</span></span>
<span class="line"><span>            &lt;-&gt; Inference Provider Adapter (contract defined above;</span></span>
<span class="line"><span>                 implementation is the selected runner, behind this</span></span>
<span class="line"><span>                 boundary only)</span></span>
<span class="line"><span>            &lt;-&gt; Capability Executor (RFC-0003/0004; validates and</span></span>
<span class="line"><span>                 executes proposals the model surfaced as data, never</span></span>
<span class="line"><span>                 trusted or auto-executed)</span></span>
<span class="line"><span>Sovereign Model Management (identity, lifecycle, storage) is consulted</span></span>
<span class="line"><span>by the Conversation Service when selecting/activating a model, and by</span></span>
<span class="line"><span>the Inference Provider Adapter when loading one — it is not on the</span></span>
<span class="line"><span>per-request conversation path.</span></span></code></pre></div><p>No component upstream of the Inference Provider Adapter ever depends on a runner-specific API shape. No component downstream of a capability proposal treats that proposal as anything but unvalidated input.</p><h2 id="security-and-privacy" tabindex="-1">Security and Privacy <a class="header-anchor" href="#security-and-privacy" aria-label="Permalink to &quot;Security and Privacy&quot;">​</a></h2><ul><li>The Conversation Service&#39;s HTTP surface is loopback-only, matching the existing Console-auth precedent (ADR-0007) rather than introducing a second, differently-shaped local auth boundary.</li><li>The inference provider&#39;s HTTP port (if the selected runner exposes one, as <code>llama-server</code> does) is never exposed to the LAN — only reachable from the Conversation Service itself.</li><li>Model artifacts are verified by digest before load, the same supply-chain posture the appliance updater already applies to release bundles and Pi-hole images.</li><li>Conversation content, capability-call arguments, and any retrieved evidence are household data. Their retention/deletion policy is deferred to the data-inventory update the milestone plan names, but this RFC establishes that no such data crosses the provider boundary to a remote service unless a future, explicit remote-inference option is configured — which is out of scope for this RFC&#39;s implementation.</li></ul><h2 id="failure-and-recovery" tabindex="-1">Failure and Recovery <a class="header-anchor" href="#failure-and-recovery" aria-label="Permalink to &quot;Failure and Recovery&quot;">​</a></h2><ul><li>Provider timeout or crash must surface as a clear degraded state to the Conversation Service and the client, never as a silent hang or a fabricated answer. The contract&#39;s cancellation/timeout requirement exists specifically so a stuck runner cannot hold resources indefinitely on a single-device appliance.</li><li>A capability proposal the executor rejects (malformed, unsupported, or failing permission/schema checks) must fail safely and visibly — the Conversation Service surfaces the rejection to the user rather than retrying it as a different, unvalidated action.</li><li>Model activation failures (checksum mismatch, incompatible runner) must not take down an already-working model; the previous activated model stays active, mirroring the appliance updater&#39;s own stage-before-activate discipline.</li></ul><h2 id="compatibility-and-migration" tabindex="-1">Compatibility and Migration <a class="header-anchor" href="#compatibility-and-migration" aria-label="Permalink to &quot;Compatibility and Migration&quot;">​</a></h2><p>There is no existing conversation or inference surface to migrate from — this is new functionality. The only compatibility constraint is the one Milestone 4 already inherits: implementation must ship through the existing appliance update path (RFC-0014) without requiring a reflash, per the milestone&#39;s Exit Criteria.</p><h2 id="operations-and-observability" tabindex="-1">Operations and Observability <a class="header-anchor" href="#operations-and-observability" aria-label="Permalink to &quot;Operations and Observability&quot;">​</a></h2><ul><li>Model/runner health and identity must be inspectable the same way <code>sovereign-update status</code> already exposes appliance/base-OS state — an operator (or the Console) should be able to see which model and runner are active and whether they are healthy without reading logs.</li><li>Capability proposals and executions produce privacy-safe audit events (detailed schema deferred to RFC-0003/0004), consistent with how trust rotations and update transactions are already both journaled and kept free of secrets/PII in their audit trails.</li></ul><h2 id="testing-strategy" tabindex="-1">Testing Strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to &quot;Testing Strategy&quot;">​</a></h2><ul><li>Contract-level tests for the Inference Provider Adapter interface (streaming, structured output, cancellation, timeout, health reporting) that do not depend on a specific runner being installed — the same way the appliance updater&#39;s manifest/signature validation is tested independent of any specific release payload.</li><li>A reproducible benchmark harness (per <code>local-ai-options.md</code>&#39;s Benchmark Method) that records exact hardware, cooling, power mode, runtime version, and model digest — required before any runner selection ADR, and re-runnable if hardware or runtime versions change.</li><li>Real Raspberry Pi 5 hardware qualification of the selected runner under the DNS-latency and thermal budgets this RFC requires, following this project&#39;s standing practice of a dated report under <code>docs/research/</code> for every real hardware pass.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Adopt Open WebUI (or a similar existing chat UI) as the product interface.</strong> Rejected: its user, conversation, tool, and administration concerns overlap with responsibilities Sovereign must control itself (privacy indicators, capability confirmation, household identity) — per <code>local-ai-options.md</code>&#39;s own comparison table. It remains available as an optional development/evaluation tool, not the architecture.</li><li><strong>Couple directly to one runner (e.g., ship only an Ollama integration) to move faster.</strong> Rejected for the same reason RFC-0014 didn&#39;t couple appliance updates to <code>apt</code>/<code>docker pull latest</code>: it would be fast now and expensive to unwind once model management, capability parsing, and conversation persistence all assumed one runner&#39;s idioms.</li><li><strong>Skip the benchmark and pick llama.cpp outright, since it&#39;s already the stated preference.</strong> Rejected: <code>local-ai-options.md</code> is explicit that convenience or stated preference is not a selection criterion by itself — the runner must actually pass Pi-hole DNS-latency and thermal budgets on the real device, measured, not assumed.</li></ul><h2 id="drawbacks-and-maintenance-cost" tabindex="-1">Drawbacks and Maintenance Cost <a class="header-anchor" href="#drawbacks-and-maintenance-cost" aria-label="Permalink to &quot;Drawbacks and Maintenance Cost&quot;">​</a></h2><ul><li>A provider-neutral boundary is more work than integrating one runner directly, and Sovereign now owns model lifecycle logic a runner like Ollama would otherwise provide for free. This is an explicit, accepted tradeoff — see Alternatives Considered — not an oversight.</li><li>This RFC adds a new persistent-data category (<code>/data/sovereign/models/</code>) whose backup/retention treatment is not yet decided, and adds a new local network-facing service (the Conversation API) whose auth boundary must be kept in sync with Console auth&#39;s as both evolve.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved Questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to &quot;Unresolved Questions&quot;">​</a></h2><p>None of the following block acceptance of the architecture below — each is either implementation-level detail this RFC deliberately doesn&#39;t fix against an unbuilt system, or explicitly deferred to a later, named document. They are tracked here so implementation doesn&#39;t silently forget them, the same pattern RFC-0016 used for its own non-blocking open items.</p><ul><li>Conversation and capability-event retention policy (deferred to the data-inventory update named in the milestone plan).</li><li>Whether model artifacts under <code>/data/sovereign/models/</code> are included in the existing backup roles or treated as separately excluded, re-downloadable data.</li><li>Exact resource/thermal/DNS-latency budget numbers — this RFC commits to the evaluation method, not the numbers, which only real hardware measurement can produce.</li><li>Whether the Conversation Service&#39;s auth reuses <code>sovereign-console-auth</code> directly or is a structurally identical but separate service — implementation-level, but affects the Interfaces section above.</li><li>Exact provider-adapter interface shape (request/response schema) is intentionally left to implementation once a runner is selected, rather than specified here against an unselected runner&#39;s real capabilities.</li></ul><h2 id="acceptance-criteria" tabindex="-1">Acceptance Criteria <a class="header-anchor" href="#acceptance-criteria" aria-label="Permalink to &quot;Acceptance Criteria&quot;">​</a></h2><ul><li>The Inference Provider Adapter contract (chat/generation, streaming, structured output, capability proposals, cancellation/timeout, health and identity reporting) is implemented and covered by contract-level tests independent of any specific runner.</li><li>Sovereign model management (manifest, digest verification, activation, rollback, storage under <code>/data/sovereign/models/</code>) is implemented.</li><li>A reproducible benchmark harness has run llama.cpp and Ollama on the real Raspberry Pi 5 qualification device and recorded results against explicit resource and DNS-latency budgets.</li><li>A runner/model selection ADR exists, informed by that benchmark, not by convenience.</li><li>The Conversation Service streams a real conversation end-to-end on real hardware without degrading Pi-hole DNS latency beyond the recorded budget.</li><li>No component outside the Inference Provider Adapter depends on a runner-specific API shape, verified by the adapter being swappable without changing the Conversation Service or capability executor.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Accepted (2026-08-09, project creator).</strong> The architecture above — Conversation Service ownership, the provider-neutral Inference Provider Adapter contract, Sovereign-owned model management, and the untrusted- model-output safety boundary — is accepted as the direction for Milestone 4. Acceptance is of the architecture, not of a completed system: the Acceptance Criteria above (contract-level tests, the real Raspberry Pi 5 benchmark, a runner/model selection ADR, real-hardware conversation qualification) remain the gate for calling any of it <em>implemented</em>, matching how RFC-0014 was itself accepted well before its own Acceptance Criteria were met. RFC-0003 (capability contract), RFC-0004 (AI capability invocation), and RFC-0006 (Pi-hole capability mapping) remain to be drafted before implementation can begin in earnest — this RFC defines the runtime they will sit inside, not permission to start building without them.</p>`,55)])])}const m=a(r,[["render",o]]);export{u as __pageData,m as default};
