import{_ as s,o as t,c as i,a2 as n}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse(`{"title":"Warden — core assistant platform plugin and harness engine (formerly \\"Jarvis\\")","description":"","frontmatter":{"rfc":51,"title":"Warden — core assistant platform plugin and harness engine (formerly \\"Jarvis\\")","status":"Implemented (third revision — the second rewrite's phase (bring-your-own model providers, single persisted conversation, incognito) is Implemented: epic tasks 22.4-22.5 (workstream 0019) both done, plugin re-enabled. This third revision specs the multi-session UI phase that RFC 0063's own second rewrite explicitly deferred as \\"a future, not-yet-scheduled phase\\" — a collapsible sidebar with multiple named, pinnable sessions; a consolidated Settings surface (General/Providers/Models) replacing the standalone /warden/providers and /warden/models routes; and a redesigned composer. Sequenced as epic tasks 22.8-22.11, workstream 0021 — all four legs (22.8 sessions data model/API, 22.9 settings consolidation, 22.10 sidebar UI, 22.11 composer redesign) are done, see that workstream for detail. Tool execution/task handoff/floating button/voice remain a future, not-yet-scheduled phase, unchanged from the second rewrite)\\n","date":"August 2026 (second rewrite; originally drafted July 2026, first rewrite August 2026; third revision August 2026)\\n","author":"kasunben","scope":"plugins/warden (rewritten); this third revision also touches packages/ui (one or two new curated icons — sidebar toggle, pin — see Semver impact) but no code changes required to apps/harness, packages/sdk, or packages/manifest; builds on RFC 0043 (plugin secret vault, reused unchanged for provider API keys), RFC 0040 (Sovereign Harness — this RFC resolves its deferred \\"user-supplied API keys\\" open question), RFC 0092 (app-level field encryption, considered and deliberately not used — see Alternatives); supersedes this RFC's own first rewrite's local-engine-only design and its second rewrite's single-conversation UI\\n","incorporated_into_plan":"Yes — epic tasks 22.4-22.5 (workstream 0019, done) and 22.8-22.11 (workstream 0021, all four legs done)\\n","sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0063-core-assistant-warden.md"},"headers":[],"relativePath":"rfcs/0063-core-assistant-warden.md","filePath":"rfcs/0063-core-assistant-warden.md"}`),o={name:"rfcs/0063-core-assistant-warden.md"};function a(r,e,d,l,c,h){return t(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="rfc-0063-warden-core-assistant-platform-plugin-and-harness-engine" tabindex="-1">RFC 0063 - Warden: core assistant platform plugin and harness engine <a class="header-anchor" href="#rfc-0063-warden-core-assistant-platform-plugin-and-harness-engine" aria-label="Permalink to &quot;RFC 0063 - Warden: core assistant platform plugin and harness engine&quot;">​</a></h1><blockquote><p><strong>This RFC was rewritten a second time, August 2026.</strong> The first rewrite (also August 2026) replaced the original runtime-owned &quot;Jarvis&quot; design with a first-party plugin (<code>plugins/warden</code>) backed by a dedicated local-engine service (<code>apps/harness</code>, wrapping llama.cpp). That design shipped in full — epic tasks 22.1-22.3, workstream 0014, verified end to end against a real engine — and was then deliberately disabled (<code>plugins/warden/manifest.json</code>&#39;s <code>disabled: true</code>) because it was hardware-constrained: <a href="./../research/0015-harness-engine-benchmark.html">Research 0015</a>&#39;s own benchmark shows even the smaller <code>qwen3:0.6b</code> model straining on representative self-hosting hardware (2 vCPU / 3.7GB RAM, no GPU). This second rewrite reflects a direct architectural decision from the developer (kasunben): keep the local engine as an optional bonus, and make bring-your-own OpenAI-API-compatible model providers the primary path. See the Changelog for what changed and &quot;Alternatives considered&quot; for what was weighed.</p></blockquote><blockquote><p><strong>This RFC was revised a third time, August 2026.</strong> The second rewrite shipped in full (epic tasks 22.4-22.5, workstream 0019) and explicitly deferred &quot;multi-threaded conversations (the sidebar/thread-switcher a ChatGPT/Claude-style UI usually has)&quot; as a named, undesigned future phase — see that rewrite&#39;s own &quot;Product scope&quot; and Open questions. This revision is that phase: a collapsible three-column-capable layout (two columns used today, a third reserved for later), multiple named/pinnable sessions replacing the single persisted conversation, a consolidated Settings surface, and a redesigned composer. It does not touch request routing, provider/model discovery, or incognito&#39;s core semantics — those carry forward unchanged from the second rewrite. See §10-12 for the new design and the Changelog for what changed.</p></blockquote><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Revamp Warden&#39;s phase 1 foundation: instead of requiring a bundled local inference engine, each user configures their own model provider(s) — any endpoint compatible with the OpenAI chat completions API, with an API key (OpenRouter, a direct provider, or the user&#39;s own self-hosted server). Warden fetches and caches each provider&#39;s model list. If the existing <code>apps/harness</code> local-engine service (unchanged, from the first rewrite) is reachable and has a model ready, its model is folded into the same list automatically — no configuration needed, and its absence is not an error.</p><p>Chat became a single, persisted conversation per user in the second rewrite; this third revision replaces that with <strong>multiple named, pinnable sessions</strong> per user, listed in a collapsible left sidebar ordered by last-active timestamp, with a &quot;+ New&quot; action that always starts a fresh one. An <strong>incognito</strong> toggle (relocated into the composer) preserves the second rewrite&#39;s ephemeral behavior as an opt-in escape hatch: while on, nothing is written to durable storage. Provider/model management moves out of the chat header and into a consolidated Settings surface (General/Providers/Models tabs), reachable from the bottom of the sidebar; the composer itself is redesigned around a Claude-style card with the model picker as a popover.</p><p>Tool execution, task handoff, a floating quick-access button, and voice remain explicit future phases — unchanged from the first rewrite&#39;s discipline of shipping foundation before capability. A third sidebar column is reserved in the layout for later, undesigned functionality, but is not built or shown in this revision.</p><p>No new secret-storage mechanism is introduced. Provider API keys are stored via the existing plugin secret vault (<code>sdk.secrets</code>, RFC 0043) — the same mechanism every other plugin already uses for per-user OAuth tokens and API keys.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The first rewrite&#39;s phase 1 shipped completely and worked — workstream 0014&#39;s leg 3 outcome was verified end-to-end against a real <code>apps/harness</code>/<code>harness-engine</code> pair. It was disabled anyway, because <a href="./../research/0015-harness-engine-benchmark.html">Research 0015</a>&#39;s decisive finding — llama.cpp&#39;s ability to disable Qwen3&#39;s &quot;thinking&quot; mode — was a comparison between two constrained options on real self-hosting hardware, not a comparison that produced a genuinely good one. Running any useful local model needs resources most self-hosted instances (a modest VPS) don&#39;t have, and building Sovereign&#39;s own model-download/verification/GPU story further before a single real user had asked to use it was the wrong place to keep spending effort.</p><p>Bring-your-own-provider removes Sovereign from the inference-hosting business entirely, for phase 1&#39;s purposes. It also happens to be work this codebase already anticipated and then punted on: <a href="./../rfcs/0040-sovereign-harness.html">RFC 0040</a> §3-5 describes almost exactly this design — an <code>openrouter</code> / <code>openai-compatible</code> / <code>local</code> provider abstraction, explicit external-model disclosure, and a consent gate for sending data externally — but deferred &quot;user-supplied API keys per user&quot; specifically because it needed &quot;a secure secret store... likely field-level encryption&quot; that didn&#39;t exist at the time. That gap has since closed: <a href="./../rfcs/0043-plugin-secret-vault.html">RFC 0043</a> (<code>sdk.secrets</code>) shipped afterward and is the documented, canonical mechanism for exactly this case — &quot;per-user API keys&quot; is one of its own worked examples in <code>docs/plugin-development.md</code>. This RFC is the first real consumer of that combination.</p><p>This is not a retreat from Sovereign&#39;s privacy-first posture — it&#39;s an explicit, disclosed, user-controlled tradeoff, not a silent one. The user chooses which provider(s) see their conversation, that choice is visible and revocable at any time (deleting a provider deletes its key with it, via <code>sdk.secrets.delete</code>), and a user who wants zero third-party data flow can still point Warden at their own self-hosted OpenAI-compatible server — that path costs the same one row of configuration as OpenRouter does. What Sovereign no longer does is bundle, host, or manage that inference itself. Everything else — conversation history, provider configuration, and the memory layer this is ultimately meant to grow into — stays in the operator&#39;s own database, under the same ownership model as every other plugin&#39;s data.</p><h2 id="current-state" tabindex="-1">Current state <a class="header-anchor" href="#current-state" aria-label="Permalink to &quot;Current state&quot;">​</a></h2><ul><li>The first rewrite&#39;s design fully shipped and still works: <code>plugins/warden</code> (chat UI, streaming Route Handler) and <code>apps/harness</code> (llama.cpp wrapper, enrollment trust boundary, lazy model download, Compose profile) — none of it is abandoned code. <code>manifest.json</code> has <code>disabled: true</code> (<code>docs/architecture-rules.md</code>&#39;s hard-disable primitive), set specifically to take the shipped result out of reach until re-prioritized. This RFC is that re-prioritization.</li><li><strong><code>apps/harness</code> needs no code changes for this revision.</strong> It keeps doing exactly what it does today: wrap one local engine, expose an internal-only chat API, stay unreachable from the public internet, authenticate Warden&#39;s server code via the enrollment-token pattern. What changes is only how Warden&#39;s server code <em>treats</em> it — as one optional entry in a merged model list instead of the only backend, degrading to &quot;not offered&quot; rather than &quot;unavailable&quot; when it&#39;s unreachable.</li><li><strong><code>sdk.secrets</code> (RFC 0043) already exists and fits exactly.</strong> <code>scope: &#39;user&#39;</code> secrets, AES-256-GCM envelope (<code>runtime/src/secrets.ts</code>), AAD bound to <code>{tenantId, pluginId, scope, userId}</code>, metadata-only listing, hard deletion on account deletion, never exported or shown in Account UI. No manifest permission is required for <code>user</code>-scoped secrets (only <code>instance</code>-scoped secrets require <code>instance:configure</code>) — Warden&#39;s manifest needs no new permission for this.</li><li><strong>RFC 0092 (<code>sdk.crypto</code> field encryption) also exists, but is the wrong tool here</strong> — see &quot;Alternatives considered.&quot;</li><li><strong>RFC 0047 (plugin tool contracts) is Implemented but has zero real consumers today.</strong> It remains the sanctioned mechanism for the deferred tool-execution phase; this RFC does not touch it.</li><li><strong>RFC 0040 (Sovereign Harness) is still <code>Draft</code>, &quot;pending revisit.&quot;</strong> This RFC materially advances that revisit — it resolves RFC 0040&#39;s own deferred &quot;user-supplied API keys&quot; open question — but does not attempt the full revisit RFC 0040 itself still needs (whether Harness ends up <em>being</em> this foundation extended with orchestration, or a separate later product). That remains open.</li><li><strong>This third revision starts from a genuinely single-conversation schema.</strong><code>plugins/warden/app/_db/schema.ts</code>&#39;s <code>wardenConversation</code> table is, today, exactly one row per user, enforced by <code>getOrCreateConversation()</code> in <code>_lib/conversations.ts</code> always returning the same existing row rather than ever creating a second one. <code>@sovereignfs/ui</code>&#39;s <code>ThreeColumnLayout</code> component already exists and fits this design&#39;s shape (sidebar + main, optionally + a third column) but is deliberately unopinionated — no built-in collapse/toggle behavior, no responsive/mobile handling of its own; several <code>.local</code> plugins (docs, sheets, tally, tasks, ledger) already hand-roll a collapsible-sidebar-plus-mobile-shell pattern on top of it, which this revision follows rather than inventing a new one.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-naming-and-boundaries" tabindex="-1">1. Naming and boundaries <a class="header-anchor" href="#_1-naming-and-boundaries" aria-label="Permalink to &quot;1. Naming and boundaries&quot;">​</a></h3><p>Unchanged from the first rewrite: Warden is the plugin, <code>apps/harness</code> is the local-engine service. What changes is <code>apps/harness</code>&#39;s standing: it was &quot;the&quot; backend; it is now one optional provider among several, with no special status in the UI beyond &quot;free, no API key needed, only available if the operator runs it.&quot;</p><h3 id="_2-product-scope" tabindex="-1">2. Product scope <a class="header-anchor" href="#_2-product-scope" aria-label="Permalink to &quot;2. Product scope&quot;">​</a></h3><p>Warden supports, as of the second rewrite (workstream 0019, done):</p><ul><li>per-user model provider configuration — label, base URL, API key — added through a first-run setup flow and editable later;</li><li>fetching and caching each configured provider&#39;s model list (<code>GET {baseUrl}/models</code>, OpenAI-compatible shape);</li><li>folding <code>apps/harness</code>&#39;s local model into the same list automatically when it&#39;s reachable and has a model ready — zero configuration, silently absent otherwise;</li><li>health/unavailable/auth-failure states per provider, and a clear first-run empty state when no provider is configured yet;</li><li>install/enable through the existing plugin system, unchanged;</li><li><strong>no privileged runtime access beyond an ordinary plugin</strong> — unchanged from the first rewrite. Warden still has no tools to call, so there&#39;s nothing to be privileged about yet;</li><li>automatic/silent fallback between providers or models is still never allowed — switching is always an explicit user action, the same principle RFC 0040 §5 already established for local-vs-external (&quot;must never silently fall back&quot;), extended here to apply between any two configured providers.</li></ul><p>This third revision (workstream 0021, planned) additionally adds:</p><ul><li><strong>multiple named, pinnable sessions per user</strong>, replacing the single persisted conversation — a collapsible sidebar lists them ordered by last-active timestamp, with up to 5 pinned to a group above the rest; model selection is per-session (a default, changeable), not per-account (see §10);</li><li>a consolidated <strong>Settings</strong> surface (General/Providers/Models tabs) replacing the standalone <code>/warden/providers</code> and <code>/warden/models</code> routes (see §11);</li><li>a redesigned composer — model picker as a popover (linking into Settings), incognito relocated from the chat header into the composer toolbar as an icon toggle, the disabled &quot;Web search — Soon&quot; placeholder removed outright (see §12).</li></ul><p>Warden still explicitly does <strong>not</strong> support, unchanged from the first rewrite:</p><ul><li>tool selection or execution of any kind;</li><li>task handoff to other plugins;</li><li>a floating quick-access action button;</li><li>voice input or output;</li><li>per-user preferences beyond ordinary plugin visibility rules.</li></ul><p>A third sidebar column is reserved in this revision&#39;s layout (§10) for undesigned future functionality — space is kept, nothing is built or shown there yet.</p><p>All of the above are real, intended future phases, not rejected ideas — listed under &quot;Adoption path.&quot;</p><h3 id="_3-data-model" tabindex="-1">3. Data model <a class="header-anchor" href="#_3-data-model" aria-label="Permalink to &quot;3. Data model&quot;">​</a></h3><p>As implemented (second rewrite): provider configuration lives on <code>sdk.connections</code>/<code>sdk.secrets</code> (RFC 0049/0043), not a bespoke table — see workstream 0019 leg 1&#39;s outcome. Conversation history is plugin-owned, <code>sdk.db</code>-scoped (tenant + user scoped, following <code>docs/architecture-rules.md</code>&#39;s &quot;plugin tables are slug-prefixed&quot; rule):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warden_conversation {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text primary key</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  tenantId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createdAt</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Exactly one row per user as shipped — enforced by getOrCreateConversation()</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// always returning the same existing row.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warden_messages {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text primary key</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  conversationId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // fk -&gt; warden_conversation</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  role</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // &#39;user&#39; | &#39;assistant&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  content</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  providerId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fk -&gt; warden_providers, null for local</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createdAt</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>This third revision replaces <code>warden_conversation</code> with <code>warden_sessions</code>, dropping the &quot;exactly one row per user&quot; invariant:</strong></p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warden_sessions {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text primary key</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  tenantId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// nullable until an LLM-generated title</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   // lands (see §10) or the user renames it</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  pinnedAt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: integer                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// nullable; non-null = pinned, sorts the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   // pinned group (max 5 enforced at pin</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   // time, see §10); null = unpinned</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  lastActiveAt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: integer not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // bumped on every message sent in this</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   // session — sidebar sort key</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  createdAt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: integer not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warden_messages {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text primary key</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  sessionId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // fk -&gt; warden_sessions (renamed from</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   // conversationId)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  role</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // &#39;user&#39; | &#39;assistant&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  content</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  providerId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fk -&gt; warden_providers/sdk.connections,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   // null for local</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: text not </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createdAt</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Migration is a clean slate, not a backfill.</strong> Per direct developer instruction: existing users&#39; single <code>warden_conversation</code> row and its messages are not carried forward into a first session — negligible real usage exists this early, and computing a synthetic title/<code>lastActiveAt</code> for legacy rows isn&#39;t worth the complexity. The migration drops and recreates these tables under their new names/shapes rather than an in-place <code>ALTER</code> plus backfill.</p><p>A session is created lazily, only on first send — not eagerly when &quot;+ New&quot; is clicked — so idle clicking through empty sessions doesn&#39;t clutter the sidebar (same lazy-creation principle <code>getOrCreateConversation()</code> already uses today, just no longer collapsing to a single row).</p><p>Incognito messages never reach <code>warden_messages</code> — the request/response round-trips the in-progress transcript directly, exactly like the original phase 1&#39;s ephemeral design (<code>plugins/warden/app/_lib/harness-client.ts</code>&#39;s existing shape already does this; it&#39;s reused, not rebuilt) — see §10 for how incognito interacts with session selection.</p><p>The API key itself is still never a column anywhere Warden owns — it lives behind the <code>sdk.secrets</code> ref <code>sdk.connections</code> already carries (RFC 0049/0043). Deleting a provider connection also deletes its secret (<code>sdk.connections.disconnect()</code>, verified to cascade in <code>runtime/src/platform-db.ts</code>), so nothing outlives its owning row.</p><h3 id="_4-model-discovery-and-merge" tabindex="-1">4. Model discovery and merge <a class="header-anchor" href="#_4-model-discovery-and-merge" aria-label="Permalink to &quot;4. Model discovery and merge&quot;">​</a></h3><p>Warden&#39;s server-side code resolves the model list on demand (cached, short TTL — an implementation detail, not architecture):</p><ol><li>For each <code>warden_providers</code> row the user has configured: fetch <code>GET {baseUrl}/models</code> using the <code>sdk.secrets</code>-retrieved key. A fetch failure marks that provider &quot;unreachable&quot; in the UI; it does not fail the whole list.</li><li>Independently, check <code>apps/harness</code>&#39;s existing health/model endpoint. If reachable and a model is ready, add it as a distinct, clearly-labeled &quot;local&quot; entry — no key, no user configuration.</li><li>Present the merged list. The user&#39;s last-used (or explicitly set default) model is preselected; switching is manual.</li></ol><h3 id="_5-request-routing" tabindex="-1">5. Request routing <a class="header-anchor" href="#_5-request-routing" aria-label="Permalink to &quot;5. Request routing&quot;">​</a></h3><ul><li><strong>Local model selected:</strong> Warden&#39;s server code calls <code>apps/harness</code> over the internal network, authenticated via the existing enrollment-token pattern — completely unchanged from the first rewrite.</li><li><strong>External provider selected:</strong> Warden&#39;s server code calls that provider&#39;s <code>baseUrl</code> directly (chat completions, streaming), using the key retrieved from <code>sdk.secrets</code> for that request only — never cached in application memory beyond the request&#39;s lifetime, never sent to the browser.</li></ul><p>The browser never talks to any provider directly, local or external — same non-negotiable as the first rewrite: every request is proxied through Warden&#39;s own server-side code, matching how no plugin&#39;s client code talks to <code>apps/relay</code>/<code>apps/auth</code> directly either.</p><p><code>apps/harness</code> is not a proxy for external providers — it has no reason to sit in that path, and adding one would introduce a dependency (and reimplement a trust boundary already solved elsewhere) that buys nothing. It keeps doing exactly one job: wrap the local engine.</p><h3 id="_6-incognito-mode" tabindex="-1">6. Incognito mode <a class="header-anchor" href="#_6-incognito-mode" aria-label="Permalink to &quot;6. Incognito mode&quot;">​</a></h3><p>A visible toggle — in the chat header as shipped, relocated into the composer toolbar as an icon in this third revision (§12). Semantics are unchanged either way. While on:</p><ul><li>the conversation is a fresh, separate scratch context — not a &quot;pause&quot; of the persisted thread — so there&#39;s no ambiguity about which messages count as saved;</li><li>nothing is written to <code>warden_messages</code>;</li><li>turning it off (or navigating away, or reloading) discards it entirely; there is no &quot;recover my incognito session&quot; path, by design — the same posture as a browser&#39;s own incognito window.</li></ul><p>In the multi-session UI (§10), incognito stays a single global scratch context orthogonal to session selection — not a per-session mode and not a session of its own in the sidebar. Toggling it on shows the same ephemeral transcript regardless of which session is open underneath; toggling it off returns to whichever session was selected. This mirrors the second rewrite&#39;s existing state shape (<code>persistedTurns</code> vs. <code>incognitoTurns</code> in <code>ChatView.tsx</code>) exactly — persisted turns simply become &quot;whichever session is selected&quot; instead of one fixed thread.</p><h3 id="_7-docker-and-deployment" tabindex="-1">7. Docker and deployment <a class="header-anchor" href="#_7-docker-and-deployment" aria-label="Permalink to &quot;7. Docker and deployment&quot;">​</a></h3><p>Unchanged from the first rewrite. <code>apps/harness</code>&#39;s Compose profile (<code>profiles: [&#39;harness&#39;]</code>, never started by a plain <code>up</code>, no port exposed to the public internet by default) is untouched — it was already fully optional infrastructure before this revision, and stays exactly that afterward. This revision only changes how &quot;the profile isn&#39;t running&quot; is perceived: from &quot;Warden is unavailable&quot; to &quot;the local option isn&#39;t offered today, everything else still works.&quot;</p><h3 id="_8-failure-modes-and-limits" tabindex="-1">8. Failure modes and limits <a class="header-anchor" href="#_8-failure-modes-and-limits" aria-label="Permalink to &quot;8. Failure modes and limits&quot;">​</a></h3><table tabindex="0"><thead><tr><th>State</th><th>Expected behavior</th></tr></thead><tbody><tr><td>No provider configured, local unreachable</td><td>First-run setup screen — not a broken chat UI.</td></tr><tr><td>A configured provider unreachable or auth-fails</td><td>That provider shows an error and retry in the picker; others unaffected.</td></tr><tr><td>Local <code>apps/harness</code> unreachable</td><td>Silently absent from the model list — not an error state.</td></tr><tr><td>Selected provider fails mid-conversation</td><td>Request fails with a retry affordance; no silent fallback to another provider.</td></tr><tr><td>Provider deleted while it&#39;s the active model</td><td>Falls back to prompting for a new selection, not a crash.</td></tr></tbody></table><p>Limits carried forward unchanged from the first rewrite&#39;s reasoning: request timeout, max input characters, max output tokens, a concurrency cap — plus, new for persisted history, <strong>max recent turns replayed as context</strong> on each request, since a thread with no length cap will eventually exceed any model&#39;s context window. Phase 1 uses simple recency-based truncation; real memory/summarization is future work (see Open questions).</p><h3 id="_9-data-deletion-and-export" tabindex="-1">9. Data deletion and export <a class="header-anchor" href="#_9-data-deletion-and-export" aria-label="Permalink to &quot;9. Data deletion and export&quot;">​</a></h3><p><code>warden_sessions</code> and <code>warden_messages</code> (renamed from <code>warden_conversation</code> in this revision, see Data model) participate in the existing plugin portability hooks (<code>sdk.portability.provideExport</code>/<code>provideDelete</code>) like any other plugin&#39;s data — a user&#39;s account deletion or data export must include (or deliberately, visibly exclude) chat history, same as any other plugin is expected to wire up. <code>provideExport</code>&#39;s current implementation returns one flat message list for the single conversation; this revision updates it to export every session, grouped by session id/title, since there are now genuinely many. Deleting a provider connection deletes its <code>sdk.secrets</code> row in the same action (see Data model), unchanged.</p><h3 id="_10-sessions-and-sidebar" tabindex="-1">10. Sessions and sidebar <a class="header-anchor" href="#_10-sessions-and-sidebar" aria-label="Permalink to &quot;10. Sessions and sidebar&quot;">​</a></h3><p>Replaces the single persisted conversation (§3) with multiple named, pinnable sessions, surfaced in a collapsible left sidebar.</p><p><strong>Layout.</strong> Two columns today — sidebar and main — composed with <code>@sovereignfs/ui</code>&#39;s existing <code>ThreeColumnLayout</code> (a third child is added only when the reserved third column gets real content in a later phase; no inert placeholder is shown for it now, following this codebase&#39;s own no-premature-abstraction convention). The sidebar collapses via a toggle icon rendered in the main column&#39;s own top-left corner — deliberately not inside the sidebar itself, so the only way to bring it back isn&#39;t hidden by collapsing it. Collapsed/expanded state persists in <code>localStorage</code> (a client-only cosmetic preference; no SSR flash-of-wrong-state concern justifies a cookie here).</p><p><strong>Creating a session.</strong> &quot;+ New&quot; at the top of the sidebar always starts a fresh, unsaved session — the input box is centered in the main column (see §12) until the first message is sent. The session row itself is created lazily, on that first send, not when &quot;+ New&quot; is clicked (§3) — clicking through several empty &quot;+ New&quot; states must not clutter the sidebar with empty rows.</p><p><strong>Ordering.</strong> Non-pinned sessions sort by <code>lastActiveAt</code> descending — bumped only when a message is actually sent in that session, not merely by opening it to look. Pinned sessions (up to 5) form a separate group above the rest, sorted by <code>pinnedAt</code> descending (most-recently-pinned first). Attempting to pin a 6th session is rejected with a message asking the user to unpin one first — not a silent auto-evict of the oldest pin (see Alternatives considered).</p><p><strong>Visible history depth.</strong> The sidebar shows the 10 most recently active sessions (pinned sessions count separately, up to their own cap of 5, and don&#39;t consume a slot in the 10). Sessions beyond that cutoff are not reachable from the sidebar in this revision — no &quot;load more&quot; or search affordance yet (see Open questions).</p><p><strong>Titles.</strong> A session&#39;s title is LLM-generated after its first exchange (a short summarization prompt against the session&#39;s own selected model — see Open questions for the cost/latency tradeoff of that choice vs. a fixed lightweight default) and manually renameable at any time via a per-row overflow menu, which also carries pin/unpin and delete actions. Deleting a session removes its <code>warden_messages</code> rows and its own <code>warden_sessions</code> row; there is no &quot;recently deleted&quot; recovery path, matching incognito&#39;s own no-recovery posture.</p><p><strong>Settings entry point.</strong> A &quot;Settings&quot; item is pinned to the bottom of the sidebar (always visible, not part of the scrollable session list), opening <code>/warden/settings</code> (§11) in the main column.</p><h3 id="_11-settings" tabindex="-1">11. Settings <a class="header-anchor" href="#_11-settings" aria-label="Permalink to &quot;11. Settings&quot;">​</a></h3><blockquote><p><strong>Update (superseded in part).</strong> The three-tab consolidation below shipped as task 22.9, and has since been reversed for Providers and Models.</p><p>The consolidation was designed before §10&#39;s sidebar existed. Once it did, a full-page settings surface meant the navigation the user had just used disappeared out from under them — and after the shell moved into a route group layout, a settings <em>page</em> was the only thing in Warden that still tore the whole screen down.</p><p>Current shape:</p><ul><li><strong>Providers</strong> → <code>/warden/providers</code>, and <strong>Models</strong> → <code>/warden/models</code>, restored as standalone routes but rendered inside the chat shell&#39;s main column (<code>app/(chat)/</code>), so the sidebar persists and both rows can show a real active state. <code>ProvidersView</code>/<code>ModelsView</code> are unchanged in behavior — relocated only, again.</li><li><strong>General</strong> — what remains (default model, retention, export link) is small and self-contained, so it is a dialog opened from the sidebar rather than a route of its own.</li><li><code>/warden/settings</code> is removed outright, along with the <code>Tabs</code>-based <code>SettingsView</code>. No redirect, for the same reason the original routes got none: negligible real usage this early.</li></ul><p>The prose below is kept as the record of what 22.9 decided and why.</p></blockquote><p>Consolidates provider and model management — currently the standalone <code>/warden/providers</code> and <code>/warden/models</code> routes — into one settings surface reachable only from the sidebar (§10), with three tabs:</p><ul><li><strong>General</strong> — default model for new sessions, a retention control, and an export action. v1 scope for retention is a manual action (e.g. &quot;delete sessions older than N days,&quot; run on demand), not a recurring background job — automatic scheduled retention would need a new <code>sdk.schedules</code> capability Warden doesn&#39;t declare today, and is left to a future phase rather than designed here (see Open questions). Whether &quot;export&quot; reuses the account-wide portability export flow (a deep link into Account&#39;s existing &quot;export my data&quot;) or adds a new Warden-only JSON download by invoking <code>provideExport</code>&#39;s callback directly from plugin UI is also left open (see Open questions) — the latter isn&#39;t how that hook is normally invoked today.</li><li><strong>Providers</strong> — the existing <code>ProvidersView</code> content (add/edit/remove, live health status), relocated unchanged in behavior.</li><li><strong>Models</strong> — the existing <code>ModelsView</code> content (per-model visibility curation, &quot;Recheck models&quot;), relocated unchanged in behavior.</li></ul><p><code>/warden/providers</code> and <code>/warden/models</code> are removed outright, not redirected — given how little real usage exists this early (same reasoning as the clean-slate session migration in §3), a redirect isn&#39;t worth the added surface.</p><h3 id="_12-composer-redesign" tabindex="-1">12. Composer redesign <a class="header-anchor" href="#_12-composer-redesign" aria-label="Permalink to &quot;12. Composer redesign&quot;">​</a></h3><p>The composer keeps its existing capabilities (text input, attach, send, streaming) but is restyled around a Claude-style card and reorganized:</p><ul><li><strong>Positioning.</strong> Centered in the main column for a session with no messages yet; docks to the bottom the instant the first message is sent. This isn&#39;t new logic — <code>ChatView.tsx</code> already appends the user&#39;s message to its turn list optimistically, synchronously, before the network request even starts, so the existing <code>turns.length === 0</code> condition already flips at exactly the right moment; the layout only needs to key off that same condition instead of introducing a new one.</li><li><strong>Model picker.</strong> Moves from an inline <code>&lt;select&gt;</code> to a popover, grouped by provider like <code>ModelsView</code> already groups its list, with a footer linking into Settings → Providers and Settings → Models (§11) — so &quot;I don&#39;t see the model I want&quot; resolves in one click instead of a separate navigation hunt.</li><li><strong>Incognito.</strong> Moves from the chat header into the composer toolbar as an icon toggle (§6), replacing the header&#39;s <code>Toggle</code> + label.</li><li><strong>Removed outright:</strong> the chat header&#39;s &quot;Manage providers&quot;/&quot;Manage models&quot; links (superseded by the model picker&#39;s popover footer and the sidebar&#39;s Settings entry) and the disabled &quot;Web search — Soon&quot; placeholder toggle — not hidden, deleted.</li></ul><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><h3 id="first-run" tabindex="-1">First run <a class="header-anchor" href="#first-run" aria-label="Permalink to &quot;First run&quot;">​</a></h3><ol><li>User installs/opens Warden. No provider is configured and <code>apps/harness</code> isn&#39;t reachable (or is, but that&#39;s just one more option).</li><li>Setup screen: add a provider (label, base URL, API key) or, if a local model is available, start chatting with that immediately.</li><li>Once at least one option exists, the ordinary chat view opens.</li></ol><h3 id="ongoing-chat" tabindex="-1">Ongoing chat <a class="header-anchor" href="#ongoing-chat" aria-label="Permalink to &quot;Ongoing chat&quot;">​</a></h3><ol><li>User opens Warden, sidebar shows their sessions (pinned group, then up to 10 most recently active).</li><li>User opens a session and sees it continue where it left off, or clicks &quot;+ New&quot; for a fresh one (input centered until the first send, §12).</li><li>User picks a model via the popover (defaults to the session&#39;s last-used model, or the account default for a new session — §11), asks a question.</li><li>Warden&#39;s server code routes to the right provider (§5), persists both sides of the exchange against that session, bumps its <code>lastActiveAt</code>, and streams the response. On the very first exchange, a title-generation request also fires (§10).</li></ol><h3 id="managing-sessions" tabindex="-1">Managing sessions <a class="header-anchor" href="#managing-sessions" aria-label="Permalink to &quot;Managing sessions&quot;">​</a></h3><ol><li>User pins a session from its overflow menu — it moves into the pinned group; a 6th pin attempt is rejected with a message to unpin one first.</li><li>User renames a session from the same menu, or deletes it (no recovery path, same posture as incognito).</li></ol><h3 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h3><ol><li>User clicks &quot;Settings&quot; at the bottom of the sidebar.</li><li>The main column shows the General/Providers/Models tabs (§11); Providers and Models behave exactly as the standalone pages did.</li><li>Leaving Settings returns to whichever session was open before.</li></ol><h3 id="incognito" tabindex="-1">Incognito <a class="header-anchor" href="#incognito" aria-label="Permalink to &quot;Incognito&quot;">​</a></h3><ol><li>User toggles incognito from the composer toolbar icon (§6, §12).</li><li>A fresh, unsaved scratch context starts; whichever session is selected underneath is untouched.</li><li>Toggling off (or leaving) discards the incognito context permanently and returns to the underlying session&#39;s own transcript.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="keep-the-bundled-local-engine-as-the-only-option-status-quo" tabindex="-1">Keep the bundled local engine as the only option (status quo) <a class="header-anchor" href="#keep-the-bundled-local-engine-as-the-only-option-status-quo" aria-label="Permalink to &quot;Keep the bundled local engine as the only option (status quo)&quot;">​</a></h3><p><strong>Rejected.</strong> This is the design being replaced — see Motivation. Hardware-constrained per Research 0015; the plugin has been <code>disabled: true</code> since it shipped for exactly this reason.</p><h3 id="route-external-provider-traffic-through-apps-harness-too" tabindex="-1">Route external-provider traffic through <code>apps/harness</code> too <a class="header-anchor" href="#route-external-provider-traffic-through-apps-harness-too" aria-label="Permalink to &quot;Route external-provider traffic through \`apps/harness\` too&quot;">​</a></h3><p><strong>Rejected.</strong> <code>apps/harness</code> exists to wrap the local engine and nothing else. Making it a generic outbound proxy for arbitrary external endpoints would add a hop and a service dependency to the common case (a user running no local engine at all) for no benefit — Warden&#39;s own server-side code can call an external HTTPS endpoint directly with the same trust properties (browser never involved) that <code>apps/harness</code>&#39;s dedicated internal API gives for the local case.</p><h3 id="instance-wide-admin-configured-provider-config-instead-of-per-user" tabindex="-1">Instance-wide (admin-configured) provider config instead of per-user <a class="header-anchor" href="#instance-wide-admin-configured-provider-config-instead-of-per-user" aria-label="Permalink to &quot;Instance-wide (admin-configured) provider config instead of per-user&quot;">​</a></h3><p><strong>Considered, rejected per direct developer instruction.</strong> RFC 0040&#39;s original sketch used instance-level env vars specifically because per-user secret storage didn&#39;t exist yet. It does now (<code>sdk.secrets</code>). Given Warden&#39;s framing as a <em>personal</em> assistant, per-user configuration (each person&#39;s own key, own usage/billing) fits better than one shared instance-wide credential — an operator wanting a shared default for everyone remains a possible future addition, not designed here.</p><h3 id="new-field-level-encryption-classification-sdk-crypto-rfc-0092-for-the-keys" tabindex="-1">New field-level encryption classification (<code>sdk.crypto</code>, RFC 0092) for the keys <a class="header-anchor" href="#new-field-level-encryption-classification-sdk-crypto-rfc-0092-for-the-keys" aria-label="Permalink to &quot;New field-level encryption classification (\`sdk.crypto\`, RFC 0092) for the keys&quot;">​</a></h3><p><strong>Considered, rejected.</strong> RFC 0092&#39;s classification/policy model is built for structured or free-text <em>data</em> columns (health notes, financial records) with fuzzy-search tradeoffs and an operator-controlled policy that can be off by default. Provider API keys are exactly the runtime-created, per-user, individually revocable credential that <code>sdk.secrets</code> (RFC 0043) was purpose-built for — using it means zero new mechanism, zero new manifest permission, and the same code path every other connection-style plugin already exercises (<a href="./../rfcs/0049-plugin-external-connections.html">RFC 0049</a> builds on <code>sdk.secrets</code> the same way).</p><h3 id="multi-threaded-conversations-in-the-second-rewrite" tabindex="-1">Multi-threaded conversations in the second rewrite <a class="header-anchor" href="#multi-threaded-conversations-in-the-second-rewrite" aria-label="Permalink to &quot;Multi-threaded conversations in the second rewrite&quot;">​</a></h3><p><strong>Rejected at the time, done in this third revision.</strong> A thread list/switcher was real, expected work for a Claude/ChatGPT-style UI, but was UI-layer scope, not foundation — the data model deliberately avoided blocking it (see Data model), so building it later wasn&#39;t wasted effort, just sequenced after the provider/persistence foundation proved out. Consistent with the first rewrite&#39;s own discipline of shipping foundation before extending capability; this revision is that later sequencing playing out.</p><h3 id="preserve-the-existing-single-conversation-as-a-user-s-first-session" tabindex="-1">Preserve the existing single conversation as a user&#39;s first session <a class="header-anchor" href="#preserve-the-existing-single-conversation-as-a-user-s-first-session" aria-label="Permalink to &quot;Preserve the existing single conversation as a user&#39;s first session&quot;">​</a></h3><p><strong>Considered, rejected per direct developer instruction.</strong> Migrating the one existing row into a titled/timestamped first session would need synthetic values for columns that didn&#39;t exist before (title, <code>pinnedAt</code>, <code>lastActiveAt</code>) and extra migration logic for a case with negligible real usage this early — a clean slate (drop and recreate under the new name/shape, §3) costs nothing meaningful and avoids inventing placeholder data no one asked for.</p><h3 id="auto-evict-the-oldest-pin-when-a-6th-pin-is-attempted" tabindex="-1">Auto-evict the oldest pin when a 6th pin is attempted <a class="header-anchor" href="#auto-evict-the-oldest-pin-when-a-6th-pin-is-attempted" aria-label="Permalink to &quot;Auto-evict the oldest pin when a 6th pin is attempted&quot;">​</a></h3><p><strong>Rejected.</strong> Silently unpinning something the user deliberately pinned earlier — without them asking for it — is a surprising, easy-to-miss data loss of intent. Rejecting the 6th pin with a clear message asking the user to choose what to unpin keeps the action explicit, at the cost of one extra click in the (presumably rare) case of wanting more than 5 pins at once.</p><h3 id="redirect-the-retired-warden-providers-warden-models-routes" tabindex="-1">Redirect the retired <code>/warden/providers</code>/<code>/warden/models</code> routes <a class="header-anchor" href="#redirect-the-retired-warden-providers-warden-models-routes" aria-label="Permalink to &quot;Redirect the retired \`/warden/providers\`/\`/warden/models\` routes&quot;">​</a></h3><p><strong>Considered, rejected.</strong> A 301/308 redirect protects external bookmarks/ deep links at the cost of two permanent routes that do nothing but forward. Given how little real usage exists this early (same reasoning as the session migration above), that cost isn&#39;t justified — the routes are removed outright.</p><h3 id="persist-chat-history-by-default" tabindex="-1">Persist chat history by default <a class="header-anchor" href="#persist-chat-history-by-default" aria-label="Permalink to &quot;Persist chat history by default&quot;">​</a></h3><p><strong>Reversed from the first rewrite.</strong> The original phase 1 rejected persistence to avoid opening storage/deletion/export/moderation questions before they were needed. Revisited here because a Claude/ChatGPT-style assistant with no memory of the conversation so far isn&#39;t really the product being asked for, and the actual new surface area is smaller than it looked in the abstract: personal chat logs, deletable per the existing portability hook pattern every plugin already implements, with incognito as an explicit, low-effort escape hatch for anyone who wants the old ephemeral-only behavior for a given session.</p><h3 id="allow-tool-execution-or-task-handoff-in-this-revision" tabindex="-1">Allow tool execution or task handoff in this revision <a class="header-anchor" href="#allow-tool-execution-or-task-handoff-in-this-revision" aria-label="Permalink to &quot;Allow tool execution or task handoff in this revision&quot;">​</a></h3><p><strong>Not revisited — still rejected</strong>, for the same reason the first rewrite gave: shipping foundation and capability in the same change is exactly the scope creep this plugin&#39;s own workstream previously flagged as its biggest risk. OpenAI-compatible chat APIs already carry <code>tools</code>/<code>tool_calls</code> at the wire level, so nothing about this design blocks wiring RFC 0047&#39;s tool contracts in later — it&#39;s deferred by choice, not by architecture.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><strong>Real memory beyond recency truncation.</strong> Context-window handling is &quot;replay the last N turns.&quot; Whether Warden eventually needs summarization or a proper memory layer — the &quot;multi-agentic harness&quot; direction this plugin is ultimately meant to grow into — is real, and explicitly out of scope here.</li><li><strong>Provider &quot;test connection&quot; UX.</strong> Should saving a new provider validate it (a live <code>/models</code> call) before accepting it, or save first and surface errors on first use? An implementation detail, not resolved here.</li><li><strong>Model list cache TTL / manual refresh.</strong> Left to implementation.</li><li><strong>Should Warden ever offer a shared, admin-configured default provider</strong> for users who don&#39;t want to bring their own key? Not designed here — see &quot;Alternatives considered.&quot;</li><li><strong>RFC 0040&#39;s full revisit remains open.</strong> This RFC resolves RFC 0040&#39;s specific deferred &quot;user-supplied API keys&quot; question but not its larger one: whether Sovereign Harness ends up being this foundation extended with orchestration/memory, or a separate later product built on top of it.</li><li><strong>Sidebar cutoff beyond 10 sessions.</strong> Hard cutoff (older sessions simply unreachable from the sidebar) vs. a &quot;load more&quot;/search affordance — left to implementation; §10 currently specs the hard-cutoff version as the v1 behavior.</li><li><s><strong>Which model generates a session&#39;s title.</strong></s> <strong>Resolved (task 22.8).</strong> No model call at all — <code>deriveTitle()</code> derives a title synchronously from the session&#39;s first user message (trimmed, whitespace-collapsed, truncated to 60 chars). Avoids the cost/latency of a vendor call, and a title the user can always rename didn&#39;t justify one.</li><li><s><strong>Retention mechanism.</strong></s> <strong>Resolved for v1 (task 22.9).</strong> A manual, on-demand &quot;delete sessions inactive for over N days&quot; action, excluding pinned sessions unconditionally. Whether Warden ever needs <em>automatic</em> scheduled retention (which would require declaring a new <code>sdk.schedules</code> capability) remains a real future question, not designed here.</li><li><s><strong>Export mechanism.</strong></s> <strong>Resolved (task 22.9).</strong> Settings → General&#39;s export action deep-links to the existing account-wide portability export (<code>/account/data</code>) — no new Warden-only download mechanism.</li><li><strong>Mobile shell timing.</strong> This revision is scoped to desktop/web only (§10); componentization should keep the door open for a dedicated mobile shell later (matching how <code>.local</code> plugins like docs/sheets/tally split a desktop shell from a mobile one sharing state/hooks), but no mobile UI is designed or scheduled here.</li><li><strong>New curated icon(s) in <code>packages/ui</code>.</strong> A sidebar-toggle icon (and likely a pin icon) don&#39;t exist in the current curated <code>Icon</code> set (<code>packages/ui/src/components/Icon/icons.ts</code>) and need adding — a small, real cross-package touch worth flagging, not yet designed (exact icon choice, whether it&#39;s one icon or two).</li></ul><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Phase 1 (second rewrite, workstream 0019) — foundation revamp, <strong>done</strong>:</p><ul><li>Provider registry on <code>sdk.connections</code>/<code>sdk.secrets</code>, first-run setup UI, empty state (epic task 22.4).</li><li>Model discovery: per-provider <code>/models</code> fetch, <code>apps/harness</code> health check folded into the same merged list, explicit model selection (22.4).</li><li>Persisted chat: <code>warden_conversation</code>/<code>warden_messages</code>, single-thread chat UI, request-limit carryover including the max-recent-turns context guard (22.5).</li><li>Incognito toggle, reusing the existing ephemeral request/response shape (22.5).</li><li>Data deletion/export wiring via the existing portability hooks (22.5).</li><li>Plugin re-enabled (<code>manifest.json</code>&#39;s <code>disabled: true</code> removed) (22.5).</li></ul><p>Phase 1.5 (this third revision, workstream 0021) — multi-session UI, <strong>planned, not started:</strong></p><ul><li><code>warden_sessions</code> schema (replacing <code>warden_conversation</code>, clean-slate migration) and multi-session CRUD — create (lazy, on first send), list/order, pin (max 5)/unpin, rename, delete; chat API updated to route by session id (epic task 22.8, see §3, §10).</li><li>Settings consolidation: new <code>/warden/settings</code> (General/Providers/Models tabs), <code>/warden/providers</code> and <code>/warden/models</code> removed (22.9, see §11).</li><li>Sidebar UI: collapsible two-column layout, &quot;+ New,&quot; pinned/recent session groups, per-row rename/pin/delete, LLM-generated titles, Settings entry point (22.10, see §10).</li><li>Composer redesign: Claude-style card, model picker popover linking into Settings, incognito relocated into the toolbar, &quot;Manage providers&quot;/ &quot;Manage models&quot; links and the disabled web-search toggle removed (22.11, see §12).</li></ul><p>See <a href="./../workstreams/0021-warden-multi-session-ui.html">workstream 0021</a> for leg sequencing, decisions locked, and dependencies between these four tasks. <code>apps/harness</code> and <code>packages/sdk</code> need no changes for this phase either; <code>packages/ui</code> gains one or two new curated icons (see Open questions).</p><p>Phase 2+ — unchanged, still future and unscheduled:</p><ul><li>Tool selection and execution via RFC 0047.</li><li>A floating quick-access action button.</li><li>Voice input/output.</li><li>Per-user preferences beyond plugin-level visibility.</li><li>The reserved third sidebar column&#39;s actual functionality (§10) — space is kept, nothing is designed for it yet.</li><li>The RFC 0040 (Sovereign Harness) full revisit.</li></ul><p>Semver impact:</p><ul><li><code>apps/harness</code>: <strong>no change</strong> — it keeps its existing internal API and version; this RFC changes only how Warden treats it.</li><li><code>packages/sdk</code>: <strong>no change</strong> for this phase either — <code>sdk.secrets</code>/ <code>sdk.connections</code>/<code>sdk.portability</code> already cover everything this design needs; no new permission, no new SDK surface.</li><li><code>packages/ui</code>: <strong>minor bump</strong> — one or two new curated icons added to the <code>Icon</code> set (additive, non-breaking); <code>ThreeColumnLayout</code> is consumed as-is, no change needed to that package itself.</li><li><code>plugins/warden</code>: manifest <code>version</code> bump at implementation time for each leg (plugins version only their own manifest, never <code>package.json</code>); a real schema migration (§3) ships with the first leg that touches it.</li></ul><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft — &quot;Jarvis,&quot; runtime-owned, optional <code>apps/inference</code> sidecar, any OpenAI-compatible endpoint</td></tr><tr><td>0.2</td><td>August 2026</td><td>First rewrite — renamed to Warden; architecture reversed from runtime-owned to a first-party platform plugin (<code>plugins/warden</code>) backed by a dedicated <code>apps/harness</code> engine service (auth/relay pattern); engine choice (llama.cpp vs. Ollama) deferred to a real benchmark (Research 0015); tool execution, task handoff, floating quick-access button, and voice moved to explicit, undesigned future phases; phase 1 scoped to exactly 3 tasks per direct developer instruction to ship foundation only</td></tr><tr><td>0.3</td><td>August 2026</td><td>Corrected &quot;Current state&quot;: <code>sovereign-mobile</code>/<code>sovereign-desktop</code> don&#39;t run local inference — <code>sovereign-edge</code> and <code>sovereign-os</code> are the real local-inference precedents, the latter having already run its own llama.cpp-vs-Ollama benchmark for a differently constrained target</td></tr><tr><td>0.4</td><td>August 2026</td><td>Phase 1 shipped in full (epic tasks 22.1-22.3, workstream 0014), verified end to end, then deliberately disabled — hardware-constrained per Research 0015&#39;s own benchmark data</td></tr><tr><td>0.5</td><td>August 2026</td><td><strong>Second rewrite.</strong> Revamped to bring-your-own OpenAI-API-compatible model providers per user, with the existing local <code>apps/harness</code> engine folded in as one optional entry rather than the required backend; persisted single-threaded conversation history (reversing the first rewrite&#39;s ephemeral-only decision) with an incognito toggle preserving that behavior as an opt-in; provider API keys stored via the existing <code>sdk.secrets</code> vault (RFC 0043), no new secret-storage mechanism; no code changes required to <code>apps/harness</code>, <code>packages/sdk</code>, or <code>packages/manifest</code>. Not yet implemented or scheduled.</td></tr><tr><td>0.6</td><td>August 2026</td><td>Second rewrite shipped in full (epic tasks 22.4-22.5, workstream 0019), verified end to end against a self-hosted mock provider; plugin re-enabled</td></tr><tr><td>0.7</td><td>August 2026</td><td><strong>Third revision.</strong> Specs the multi-session UI phase explicitly deferred in 0.5/0.6 as future/undesigned: <code>warden_sessions</code> replaces the single <code>warden_conversation</code> (clean-slate migration, no backfill of existing rows), collapsible two-column sidebar with pinnable (max 5) named sessions ordered by last-active timestamp and LLM-generated titles, a consolidated <code>/warden/settings</code> (General/Providers/Models) replacing the standalone provider/model routes, and a Claude-style composer redesign (model picker as a popover, incognito relocated into the toolbar, web-search placeholder removed). Incognito&#39;s own semantics, request routing, and provider/model discovery are unchanged. Sequenced as epic tasks 22.8-22.11, workstream 0021. Planning only — not yet implemented</td></tr><tr><td>0.8</td><td>August 2026</td><td>Third revision shipped in full (epic tasks 22.8-22.11, workstream 0021, all four legs), verified end to end live against a real logged-in dev session at each leg. Three of this revision&#39;s own Open Questions were resolved at implementation time rather than left as designed: session titles use no model call at all (a synchronous truncation of the first message, not an LLM request); retention is a manual, on-demand action, never a scheduled job; export deep-links to the existing account-wide portability flow rather than a new Warden-only mechanism</td></tr></tbody></table>`,115)])])}const g=s(o,[["render",a]]);export{u as __pageData,g as default};
