import{_ as o,o as t,c as s,a2 as d}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Hard Architectural Rules","description":"","frontmatter":{},"headers":[],"relativePath":"architecture-rules.md","filePath":"architecture-rules.md"}'),i={name:"architecture-rules.md"};function r(c,e,n,a,l,u){return t(),s("div",null,[...e[0]||(e[0]=[d('<h1 id="hard-architectural-rules" tabindex="-1">Hard Architectural Rules <a class="header-anchor" href="#hard-architectural-rules" aria-label="Permalink to &quot;Hard Architectural Rules&quot;">​</a></h1><p>Full reference for load-bearing constraints enforced by ESLint, CI, or runtime behaviour. CLAUDE.md carries a critical-violations summary; this document has the full detail.</p><hr><ul><li><strong>SDK is the only plugin↔platform contract.</strong> Plugins MUST NOT import from <code>runtime/src</code>. ESLint enforces this (established in Task 0.3.3, verified in Task 0.3.8). Plugins use <code>packages/sdk</code> only.</li><li><strong><code>@sovereignfs/sdk</code> is a types-first contract with zero runtime dependencies</strong> (RFC 0023, Task 0.5.21). <code>packages/sdk</code> does not import <code>@sovereignfs/db</code> or <code>@sovereignfs/mailer</code>. Implementations are registered by the runtime at startup via <code>provideHost()</code> in <code>runtime/instrumentation.ts</code> → <code>runtime/src/sdk-host.ts</code>. Never add <code>@sovereignfs/db</code>/<code>@sovereignfs/mailer</code> back as dependencies of the SDK — the <code>noExternal</code>-bundle plan is explicitly dropped. Platform internals belong in <code>runtime/src/sdk-host.ts</code>, not in <code>packages/sdk</code>.</li><li><strong>Every package/app extends <code>packages/tsconfig</code></strong> (<code>base</code>/<code>nextjs</code>/<code>library</code>), established in Task 0.3.2. Easy to forget on new packages.</li><li><strong>Manifests are validated at build time.</strong> Invalid manifest = failed build.</li><li><strong>Plugin tables are slug-prefixed</strong> (<code>tasks_lists</code>, <code>splitify_groups</code>). Single shared schema, no per-plugin DBs in v1.</li><li><strong><code>tenant_id</code> everywhere</strong> on user-scoped tables from day one (future multi-tenancy), even though no multi-tenant logic exists in v1.</li><li><strong>DB is dialect-agnostic</strong> (Drizzle): SQLite default, Postgres via env only. No SQLite-specific SQL in app code.</li><li><strong>The platform data layer is async</strong> (Task 0.5.3). Postgres (node-postgres) has no synchronous query, so <code>getPlatformDb()</code> and every <code>packages/db</code> platform helper (<code>getPlatformSetting</code>, <code>setAccountPrefs</code>, …) and <code>sdk.platform.getConfig()</code> return promises — always <code>await</code> them. (On SQLite the underlying better-sqlite3 calls still run synchronously; the async signature is the dialect-agnostic contract.) Never reintroduce a synchronous platform-DB read.</li><li><strong>Relative SQLite paths resolve against the workspace root</strong> (nearest ancestor with <code>pnpm-workspace.yaml</code>), not the process cwd — all SQLite files land in the single root-level <code>data/</code> directory regardless of which app opens them. Implemented in both <code>packages/db</code> and <code>apps/auth/src/db.ts</code> (duplicated deliberately; auth does not depend on <code>packages/db</code>).</li><li><strong>No secrets with defaults.</strong> <code>AUTH_SECRET</code> / <code>SOVEREIGN_AUTH_SECRET</code> etc. must throw on startup if unset.</li><li><strong>Plugins compose at their <code>routePrefix</code> under a <code>shell</code>-selected route group.</strong> The generate script injects <code>plugins/[id]/app/</code> into <code>runtime/app/(platform)/(plugins)/&lt;routePrefix&gt;/</code> for <code>shell: default</code> plugins (they inherit the platform sidebar via App Router layout nesting — no rewrites). The route segment is the manifest <code>routePrefix</code>, not the source directory name, so <code>routePrefix</code> is the single source of truth for a plugin&#39;s URL; the <code>(plugins)</code> route group is URL-transparent, so <code>routePrefix: /console</code> serves at <code>/console</code>. The composed segments are <strong>copies in dev, symlinks in production</strong> (<code>NODE_ENV</code>) — dev must copy because Next&#39;s dev route watcher does not follow symlinked route dirs (a symlinked plugin 404s under <code>next dev</code>); production uses a real symlink instead so a plugin&#39;s imports resolve through <em>its own</em> <code>node_modules</code> rather than requiring every dependency it uses to also be declared in <code>runtime/package.json</code>. Composed segments are gitignored by a <code>.gitignore</code> inside each route group — never edit or commit them. Source of truth is always <code>plugins/[id]/app/</code>. Because production&#39;s symlink isn&#39;t followed by TypeScript&#39;s own module resolution either, <code>runtime/tsconfig.json</code> excludes composed plugin directories (<code>(plugins)</code> and <code>(minimal)</code>) from its type scope — each plugin already typechecks itself in its own repo/CI. <strong><code>shell: overlay</code> (RFC 0001, Task 0.5.10) composes TWICE:</strong> the full-page fallback under <code>(plugins)/&lt;routePrefix&gt;/</code> (same as default) <strong>and</strong> an interception copy under <code>(plugins)/@modal/(.)&lt;routePrefix&gt;/</code>. The <code>@modal</code> parallel-route slot lives <strong>inside</strong> <code>(plugins)</code> (hosted by a committed <code>(plugins)/layout.tsx</code> that renders <code>{children}{modal}</code>) so the interceptor and the fallback are folder-siblings in the same route group — interception across the group boundary from <code>(platform)</code> fails at runtime with <code>initialTree is not iterable</code>. The slot&#39;s hand-written <code>@modal/default.tsx</code> (empty fallback) and <code>@modal/layout.tsx</code> (the <code>Dialog</code> chrome; renders the Dialog only when <code>useSelectedLayoutSegment()</code> is an intercepted segment, never null, so no empty scrim on ordinary pages) are <strong>committed</strong>; the <code>@modal/(.)*</code> copies are generated and gitignored (the <code>(plugins)/.gitignore</code> keeps <code>layout.tsx</code> + <code>@modal/{default,layout}.tsx</code>, ignores the rest). Overlay <code>routePrefix</code> must be a single segment, and overlay plugins are ineligible as the root plugin (CON-11) — <code>validateRootPlugin</code> rejects <code>shell: &#39;overlay&#39;</code>. <strong>Intra-overlay navigation MUST use <code>replace</code>, not push.</strong> The dialog is dismissed with <code>router.back()</code>, which unwinds exactly one history entry; if a plugin&#39;s in-dialog tab/section links push (the <code>&lt;Link&gt;</code> default), each one stacks on history and a single dismiss only steps back one tab instead of closing — stale dialog states pile up behind it. Console and Account tab links use <code>&lt;Link replace&gt;</code>; this is documented as a convention for third-party overlay plugins in <code>docs/plugin-development.md</code>. Never reintroduce push-based intra-overlay navigation. <strong>Dialog size is plugin-declared</strong> via the optional manifest <code>shellConfig.overlaySize</code> (<code>sm</code> | <code>md</code> | <code>lg</code>, default <code>lg</code>); the <code>@modal/layout.tsx</code> resolves it from the selected interception segment (<code>overlaySizeForSegment</code> in <code>runtime/src/overlay.ts</code>). The <code>@sovereignfs/ui</code><code>Dialog</code> renders <strong>fixed-size boxes</strong> (each size sets width AND height, content scrolls inside) so the dialog never resizes as its content changes between tabs — <code>lg</code> fills the viewport minus a fixed margin, <code>md</code>/<code>sm</code> are fixed and centred; on mobile every size is a full-screen sheet. The <code>Dialog</code> scrim is full-viewport by default but offsets its left edge by the <code>--sv-dialog-inset-left</code> CSS var (default <code>0</code>); the shell sets it to the sidebar width (<code>--sv-shell-sidebar-width</code>, reset to <code>0</code> on mobile) on <code>.shell</code> so overlay dialogs start at the sidebar&#39;s right edge and leave the rail visible/usable — never hardcode the sidebar width into the <code>Dialog</code>.</li><li><strong><code>shell: minimal</code> (RFC 0014, Task 0.5.25) composes into <code>runtime/app/(minimal)/</code></strong> — a chrome-free, full-bleed route group (no sidebar, header, or footer). The committed <code>(minimal)/layout.tsx</code> applies <code>100dvh</code> and safe-area insets; generated composed routes land alongside it (gitignored by <code>(minimal)/.gitignore</code> which keeps <code>layout.tsx</code> and <code>minimal.module.css</code>). The session gate still applies — the middleware enforces auth before the plugin renders. Multi-segment <code>routePrefix</code> is allowed (unlike overlay, which must be single-segment). <strong><code>minimal</code> plugins ARE eligible as the root plugin</strong> (kiosk use case — <code>validateRootPlugin</code> accepts <code>shell: &#39;minimal&#39;</code>); when used as root there is no platform nav, so the plugin must provide its own navigation back to <code>/launcher</code> or other routes if needed. Never reintroduce <code>process.exit(1)</code> for the minimal case in <code>generate-registry.ts</code> — it is wired.</li><li><strong><code>adminOnly</code> routes are gated in the runtime middleware.</strong> A request under an admin-only plugin&#39;s <code>routePrefix</code> from a non-<code>platform:admin</code> user returns 403 (SRS §3.4, PLT-03).</li><li><strong>Invite-only is dual-written, and the auth-server copy is authoritative.</strong> The Console toggle (CON-10) writes <code>invite_only</code> to both the platform DB (<code>platform_settings</code>, read by <code>sdk.platform.getConfig()</code>) and the auth server&#39;s own <code>auth_settings</code> table (via the runtime PATCH proxying to <code>apps/auth</code>). Registration enforcement reads only the auth copy — the auth server owns identity and does not read the platform DB. A stored value overrides the <code>AUTH_INVITE_ONLY</code> env default; absent a stored value, the env default applies. Never make registration read the platform DB instead.</li><li><strong><code>root_plugin_id</code> lives in <code>platform_settings</code>, seeded on first run</strong> to <code>fs.sovereign.launcher</code> (PLT-14/PLT-15). The eligible set is installed + enabled + non-<code>adminOnly</code> (validated in <code>runtime/src/root-plugin.ts</code>). <code>/</code><strong>serves the root plugin in place</strong> — the middleware rewrites <code>/</code> to the configured plugin&#39;s <code>routePrefix</code> (URL stays <code>/</code>; the plugin remains reachable at its own prefix too), resolving the prefix at request time via <code>GET /api/admin/root-plugin</code> (Edge middleware can&#39;t read the DB, same fetch pattern as <code>/api/admin/plugins/disabled</code>). <code>(platform)/page.tsx</code> keeps a <code>redirect()</code> as a fallback for when that resolution fetch fails. Platform tables (<code>tenants</code>, <code>plugin_status</code>, <code>platform_settings</code>) are bootstrapped with <strong>dialect-aware</strong> CREATE-TABLE-IF-NOT-EXISTS + seed rows in <code>packages/db</code>&#39;s <code>getPlatformDb()</code> (<code>packages/db/src/bootstrap.ts</code>, <code>INTEGER</code>/<code>BIGINT</code> + <code>INTEGER</code>/<code>BOOLEAN</code> per dialect); the DDL must stay in sync with the Drizzle schemas (<code>schema/sqlite</code> + <code>schema/postgres</code>, guarded by a parity test). drizzle-kit migrations replace this later (0.5.05+).</li><li><strong>Chrome plugins</strong> (<code>fs.sovereign.launcher</code>, <code>fs.sovereign.account</code>, <code>fs.sovereign.console</code>) are reached through the sidebar chrome (home <code>/</code>, Console ⚙, Account avatar), never via the Launcher grid or the sidebar&#39;s middle plugin-icon section (LCH-04, PLT-12). The canonical ID set is <code>CHROME_PLUGIN_IDS</code> in <code>runtime/src/launcher-plugins.ts</code> — reuse it, never re-hardcode the list.</li><li><strong>Plugins that need the installed-plugin list fetch the gated <code>/api/plugins</code></strong> (forwarding the session cookie), not import the registry — the SDK boundary rule forbids plugins importing <code>runtime/src</code> or internal packages. The route is session-gated (middleware injects <code>x-sovereign-user-role</code>) and role-filters via <code>selectLauncherPlugins</code>; <code>sdk.db</code> replaces this fetch in Task 0.5.5.</li><li><strong>The <code>/api/*</code> namespace is split: reserved runtime segments vs. the public provider namespace (PLT-16).</strong> The runtime serves its own first-level segments — <code>account</code>, <code>admin</code>, <code>auth</code>, <code>health</code>, <code>instance</code>, <code>manifest</code>, <code>plugins</code> (<code>runtime/app/api/*</code>) — listed in <code>RESERVED_API_SEGMENTS</code> (<code>runtime/src/api-namespace.ts</code>); a parity test asserts the set matches the on-disk dirs, so a new runtime API route can&#39;t silently become delegatable. Every other <code>/api/&lt;slug&gt;/*</code> is the <strong>public</strong> namespace: the middleware handles it <strong>before</strong> the session gate (it is unauthenticated — the provider owns auth, e.g. API keys), rewriting to the single registered provider&#39;s serve route <code>&lt;routePrefix&gt;/serve/&lt;slug&gt;/&lt;path&gt;</code>, or 404 when no enabled provider is installed. The provider is the one plugin with <code>apiProvider: true</code> in its manifest; <strong>exactly one per instance</strong> — the generate script fails the build on a second one (<code>findApiProvider</code> in <code>@sovereignfs/manifest</code> is the shared resolver). Never add a new <code>runtime/app/api/*</code> segment without adding it to <code>RESERVED_API_SEGMENTS</code>.</li><li><strong>Server-to-server calls to better-auth (<code>/api/auth/*</code>) must send an <code>Origin</code> header</strong> equal to the auth base URL (<code>SOVEREIGN_AUTH_URL</code>) — better-auth enforces a CSRF origin check and rejects originless POSTs with <code>MISSING_OR_NULL_ORIGIN</code> (403). Applies to <code>update-user</code> (Account profile) and <code>change-password</code>. The session cookie is host-scoped, so forwarding it across the runtime↔auth origins works.</li><li><strong>The middleware&#39;s unauthenticated <code>/login</code> redirect MUST be <code>303</code>, not the <code>NextResponse.redirect</code> default of <code>307</code>.</strong> 307 preserves the request method, so an unauthenticated <strong>POST</strong> to a gated route (the logout form once the session has lapsed, any plugin form submit, a server action) redirects as <code>POST /login</code> — and <code>runtime/app/login/route.ts</code> only handles <code>GET</code>, returning 405. 303 (See Other) forces the browser to GET. Any browser-facing redirect to <code>/login</code> (middleware, logout route) must target the <strong>public</strong> auth URL (<code>SOVEREIGN_AUTH_PUBLIC_URL</code>), never the internal <code>SOVEREIGN_AUTH_URL</code> (<code>auth:3001</code>), which the browser cannot resolve in Docker.</li><li><strong><code>&#39;use client&#39;</code> components must never read browser APIs (<code>navigator</code>, <code>window</code>, <code>localStorage</code>, etc.) inside a <code>useState</code> initializer or during render.</strong> The server renders without those globals, producing different HTML than the client and triggering a React hydration error. The pattern: always initialise state to a server-safe value (e.g. <code>useState(&#39;online&#39;)</code>), then read the browser API inside <code>useEffect</code> and call the setter if needed. The one-frame delay before the UI reflects the real browser state is imperceptible. <code>OfflineBanner</code> is the canonical example — it initialises to <code>&#39;online&#39;</code> and checks <code>navigator.onLine</code> in <code>useEffect</code> (<code>runtime/app/(platform)/_components/OfflineBanner.tsx</code>).</li><li><strong>Never read <code>NEXT_PUBLIC_*</code> env vars for a value that must vary per deployment at run time.</strong> Next.js inlines <code>process.env.NEXT_PUBLIC_*</code> literals at <strong>build time</strong> into every bundle (client and server); the Docker images build without an <code>.env</code>, so such a read freezes to its fallback and ignores the env injected at container start. The post-login redirect target (<code>NEXT_PUBLIC_RUNTIME_URL</code>) is therefore resolved <strong>server-side at request time</strong> via <code>apps/auth/src/runtime-url.ts</code> (which reads through a computed key so the inliner can&#39;t match it) and passed as a prop to the client <code>LoginForm</code>/ <code>RegisterForm</code>. The auth login/register <code>page.tsx</code> are server components for this reason. <code>.env.example</code> leaves <code>NEXT_PUBLIC_RUNTIME_URL</code> / <code>SOVEREIGN_AUTH_URL</code> commented so the per-environment Compose/code defaults apply (dev → :3000/:3001, prod → :4000/:4001); a hardcoded dev value in <code>.env</code> leaks into the prod stack via interpolation.</li><li><strong>better-auth&#39;s fresh-session gate is disabled (<code>session.freshAge: 0</code></strong> in <code>apps/auth/src/auth.ts</code>). By default <code>freshSessionMiddleware</code> (guarding <code>GET /list-sessions</code>, used by <code>sdk.auth.listSessions</code>) returns <code>403 SESSION_NOT_FRESH</code> once a session is older than <code>freshAge</code> (1 day) — so the Account Security tab broke for day-old sessions. Self-hosted users stay signed in for weeks, so freshness re-auth is off. Don&#39;t re-enable it without a re-auth flow. (A regression test asserts <code>freshAge === 0</code>.)</li><li><strong>Theme is applied before first paint by an inline script in <code>runtime/app/layout.tsx</code></strong> reading the <code>sv-theme</code> cookie (<code>light</code>/<code>dark</code> applied directly; <code>system</code>/unset follows <code>prefers-color-scheme</code>). The Account plugin writes the choice to <code>account_prefs</code> (authoritative) and mirrors it to the <code>sv-theme</code> cookie on PATCH. Avatars live on disk at <code>data/avatars/&lt;user_id&gt;.&lt;ext&gt;</code> (served by <code>/api/account/avatar/[userId]</code>); the user record&#39;s <code>image</code> field holds the servable URL.</li><li><strong>Security headers are split: static via <code>next.config.ts</code>, CSP via middleware</strong> (RFC 0008 Tier 0, Task 0.5.16). Both apps&#39; <code>next.config.ts</code> emit the static headers (<code>X-Frame-Options: DENY</code>, <code>X-Content-Type-Options: nosniff</code>, <code>Referrer-Policy</code>, <code>Permissions-Policy</code>, and <strong>HSTS production-only</strong>). The <strong>Content-Security-Policy is strict and nonce-based</strong>, set per-request in middleware (<code>runtime/middleware.ts</code> extends the session middleware; <code>apps/auth/middleware.ts</code> is dedicated) — every middleware return path must run through <code>applyCsp</code>, and the rendered-path request headers carry the nonce so Next nonces its own inline scripts. CSP builders live in <code>runtime/src/security.ts</code> and the duplicated <code>apps/auth/src/security.ts</code> (the apps share no code). The runtime&#39;s pre-paint theme script is a <strong>fixed string</strong> (<code>runtime/src/theme-script.ts</code>) allowed by a <strong>CSP hash</strong> (<code>THEME_SCRIPT_CSP_HASH</code>, guarded by a drift test) — not a nonce — so the root layout stays statically renderable (the PWA <code>/offline</code> fallback needs it). The nonce only applies on <strong>dynamically-rendered</strong> pages; the runtime&#39;s gated pages are dynamic via the <code>(platform)</code> layout&#39;s <code>headers()</code>, and <code>apps/auth</code> forces dynamic (<code>export const dynamic</code> in its root layout). Never add <code>&#39;unsafe-inline&#39;</code> to <code>script-src</code>. <strong>The runtime CSP&#39;s <code>form-action</code> must include the browser-facing auth origin, not just <code>&#39;self&#39;</code>.</strong> The logout form lives on the runtime origin but its POST 303-redirects to the auth login page on a different origin (<code>SOVEREIGN_AUTH_PUBLIC_URL</code>, e.g. <code>:3001</code>/<code>:4001</code>); browsers check <code>form-action</code> against the whole redirect chain, so <code>&#39;self&#39;</code> alone silently blocks it (the page just flickers, no navigation). The middleware feeds the parsed auth origin (<code>authPublicOrigin()</code>) to <code>buildContentSecurityPolicy</code> (<code>authFormActionOrigin</code>). Postgres connects over TLS when the connection string sets <code>sslmode</code> (<code>pgSslMode</code> in <code>packages/db/src/client.ts</code>; CA via <code>PGSSLROOTCERT</code>). At-rest encryption (Tiers 2–4) is deferred to Task 1.0.01.</li><li><strong>The runtime is an installable PWA</strong> (<code>@ducanh2912/next-pwa</code>, PLT-09). The web manifest (<code>runtime/public/manifest.json</code>) and PNG icons (<code>runtime/public/icons/</code>) are committed <strong>source</strong>; the service worker (<code>sw.js</code>, <code>workbox-*.js</code>, <code>fallback-*.js</code>) is <strong>generated into <code>runtime/public/</code> at build</strong> and is gitignored + ignored by ESLint and Prettier — never commit or lint it. The SW is <strong>disabled in dev</strong> (so it never interferes with HMR), so installability/Lighthouse only apply to a production build (<code>next build</code>). The PWA assets and the <code>/offline</code> fallback are excluded from the middleware session gate (they must load without a session).</li><li><strong>Production images build from Next.js standalone output</strong> (Task 0.5.2). Both <code>next.config.ts</code> set <code>output: &#39;standalone&#39;</code> <strong>and</strong><code>outputFileTracingRoot</code> to the monorepo root — required in a pnpm monorepo or the trace misses workspace package files. The standalone tree mirrors the repo layout, so the runner runs <code>node runtime/server.js</code> / <code>node apps/auth/server.js</code> (not <code>next start</code>). The standalone server reads <code>PORT</code>/<code>HOSTNAME</code> env (the old <code>next start --port</code> flag is gone): the runner sets <code>PORT</code> + <code>HOSTNAME=0.0.0.0</code> (runtime 3000, auth 3001). <code>runtime/.next/static</code> and <code>runtime/public</code> (which holds the generated PWA assets) must be <strong>copied explicitly</strong> into the runner — standalone does not include them. Healthchecks use <strong><code>127.0.0.1</code>, not <code>localhost</code></strong> (busybox <code>localhost</code>→<code>::1</code>, but the server binds IPv4 <code>0.0.0.0</code> → connection refused on <code>localhost</code>). The runtime&#39;s <code>HEALTHCHECK</code> hits the public <code>/api/health</code> liveness route (excluded from the middleware gate); the admin-key-gated <code>/api/admin/health</code> stays the richer report. <strong><code>docker-compose.prod.yml</code> uses a named volume (<code>sovereign_data</code>), not a host bind mount</strong>, for <code>/app/data</code>: the images run <strong>non-root</strong>, and a named volume inherits the image&#39;s <code>/app/data</code> ownership so SQLite/avatar writes work with zero host <code>chown</code> (a bind mount keeps host ownership and breaks non-root writes on Linux — macOS VirtioFS hides this). Dev (<code>docker-compose.yml</code>) keeps the <code>./data</code> bind mount (runs as root). The named volume is <strong>pinned with an explicit <code>name: sovereign_data</code></strong> so Compose doesn&#39;t prefix it with the project (checkout-dir) name — the documented backup/restore commands reference it by that exact name.</li><li><strong>Both standalone images COPY <code>pnpm-workspace.yaml</code> into <code>/app</code></strong> (runtime and <code>apps/auth</code> Dockerfiles). Next.js standalone <code>server.js</code> calls <code>process.chdir(__dirname)</code> at boot, moving cwd to <code>/app/runtime</code> (or <code>/app/apps/auth</code>). <code>findWorkspaceRoot()</code> walks up from cwd and stops at the <code>pnpm-workspace.yaml</code> marker, returning <code>/app</code> — so <strong>relative SQLite paths</strong> (<code>sovereign.db</code>, <code>auth.db</code>) and the <strong>drizzle migrations folder</strong> (<code>packages/db/migrations/</code>, runtime only) resolve against <code>/app</code>, i.e. the mounted <code>/app/data</code> volume. <strong>Without the marker</strong> <code>findWorkspaceRoot()</code> falls back to the post-<code>chdir</code> cwd and the DBs land at <code>/app/runtime/data</code> / <code>/app/apps/auth/data</code> — OUTSIDE the volume: data does not persist across container recreates and is missing from backups, and the runtime fails to boot (<code>Can&#39;t find meta/_journal.json</code>). Never drop the <code>pnpm-workspace.yaml</code> COPY from either Dockerfile.</li><li><strong>The runtime Dockerfile must ship every plugin&#39;s <code>manifest.json</code> and <code>migrations/</code> folder into the runner image</strong>, staged into a curated <code>/app/.deploy/plugins</code> directory in the builder stage (not the full <code>plugins/*/</code> tree — that would drag each plugin&#39;s <code>app/</code> source and <code>node_modules</code> into the production image for no benefit, since routes are already compiled into the standalone build). <code>runAllPluginMigrations()</code> (<code>runtime/src/plugin-migrations.ts</code>) and <code>buildIdToDirMap()</code> resolve these paths at server startup relative to the workspace root; if absent, a missing-migrations plugin is <strong>silently skipped</strong> (<code>existsSync</code> guard, no error logged) rather than failing loudly — this was the case for every shared/isolated-mode plugin until Sovereign Tasks (bundled with the platform by default) was the first to actually need it, surfacing as a production 500 (<code>relation &quot;...&quot; does not exist</code>) with nothing in the logs pointing at the cause.</li><li><strong>A shared or isolated-mode plugin whose application code queries through one dialect&#39;s schema (typically <code>sqlite-core</code>) needs a genuinely separate <code>pgTable</code>-based schema file to generate Postgres migrations from</strong> — <code>drizzle-kit generate --dialect postgresql</code> cannot read a <code>sqliteTable()</code> schema; it silently reports zero tables. That Postgres schema file must use plain <code>integer</code> for booleans/timestamps, never native Postgres <code>boolean</code>/<code>bigint</code> — Drizzle&#39;s query-builder dialect is bound to the client connection, not to the table object&#39;s origin, so the existing SQLite-typed query code keeps working against a Postgres-backed client only if the physical columns serialize identically to what the SQLite column mappers already produce. See <code>docs/plugin-database.md</code> for the full pattern (<code>packages/db/src/schema/{sqlite,postgres}/platform.ts</code> is a different case — the platform&#39;s own query code is dialect-aware via <code>packages/db/src/exec.ts</code>, so its Postgres schema uses native types).</li></ul>',4)])])}const p=o(i,[["render",r]]);export{g as __pageData,p as default};
