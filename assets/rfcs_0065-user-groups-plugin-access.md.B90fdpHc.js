import{_ as a,o as n,c as s,a2 as i}from"./chunks/framework.BWuWLRhz.js";const h=JSON.parse('{"title":"RFC 0065 — User groups and plugin access policy","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0065-user-groups-plugin-access.md","filePath":"rfcs/0065-user-groups-plugin-access.md"}'),l={name:"rfcs/0065-user-groups-plugin-access.md"};function o(t,e,r,p,d,c){return n(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0065-—-user-groups-and-plugin-access-policy" tabindex="-1">RFC 0065 — User groups and plugin access policy <a class="header-anchor" href="#rfc-0065-—-user-groups-and-plugin-access-policy" aria-label="Permalink to &quot;RFC 0065 — User groups and plugin access policy&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/db</code>, <code>runtime</code>, <code>plugins/console</code>, <code>plugins/launcher</code>, docs; builds on RFC 0021, RFC 0022, RFC 0041, RFC 0054, and existing <code>plugin_status</code> behavior<br><strong>Incorporated into plan:</strong> Yes — epic tasks 1.15, 2.21, and 13.7. Roadmap slot versions are deferred.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Installed plugins are currently platform-wide: once a plugin is installed and not globally disabled, it is visible and openable for all active users unless the plugin has bespoke checks such as admin-only handling. Sovereign needs a platform-owned access layer so an operator can decide which users may open each installed plugin.</p><p>This RFC adds two foundations:</p><ul><li>User groups, managed by admins/owners, as reusable audience sets.</li><li>Plugin access policies: <code>everyone</code>, <code>admins</code>, <code>selected_users</code>, <code>selected_groups</code>, and <code>disabled</code>.</li></ul><p><code>everyone</code> is the default for existing behavior. <code>disabled</code> is the strongest state: the plugin remains installed and manageable from Console, but no user can open the app until an admin changes the policy.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign is single-tenant and multi-user in v1. Operators need to install one plugin once and expose it only to the users who should use it. Examples:</p><ul><li>A finance plugin should be visible only to the finance group.</li><li>A support plugin should be visible to selected staff users during rollout.</li><li>An experimental plugin should be installed for validation without appearing for everyone.</li><li>A sensitive plugin should be disabled immediately without uninstalling it or deleting data.</li></ul><p>Launcher-only filtering is not enough. A user who cannot see a plugin must also be denied direct routes, public API entry points, and generated runtime routes for that plugin.</p><h2 id="current-state" tabindex="-1">Current state <a class="header-anchor" href="#current-state" aria-label="Permalink to &quot;Current state&quot;">​</a></h2><ul><li><code>plugin_status</code> provides install/enable state and some admin-only behavior.</li><li>Launcher, sidebar, and mobile nav are built from installed plugin metadata and user preferences, but preferences are not authorization.</li><li>RFC 0054 covers plugin-scoped roles and resource grants inside a plugin after a user has opened it. It does not answer whether the platform should let that user open the plugin at all.</li><li>RFC 0041 provides user directory and member selection primitives, but there is no reusable group/audience model yet.</li></ul><h2 id="proposal" tabindex="-1">Proposal <a class="header-anchor" href="#proposal" aria-label="Permalink to &quot;Proposal&quot;">​</a></h2><h3 id="two-enforcement-layers" tabindex="-1">Two enforcement layers <a class="header-anchor" href="#two-enforcement-layers" aria-label="Permalink to &quot;Two enforcement layers&quot;">​</a></h3><p>Plugin availability remains platform-owned and is evaluated before any plugin route or SDK surface is exposed:</p><ol><li>The plugin must be installed and enabled.</li><li>The plugin access policy must allow the current user.</li></ol><p>Plugin-local authorization remains plugin-owned. Once the platform allows a user to open a plugin, RFC 0054-style plugin roles and grants decide what that user may do inside the plugin.</p><h3 id="user-groups" tabindex="-1">User groups <a class="header-anchor" href="#user-groups" aria-label="Permalink to &quot;User groups&quot;">​</a></h3><p>Add platform-managed user groups as a prerequisite for group-targeted plugin access.</p><p>Proposed tables:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>user_groups(</span></span>
<span class="line"><span>  id,</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  name,</span></span>
<span class="line"><span>  slug,</span></span>
<span class="line"><span>  description,</span></span>
<span class="line"><span>  created_by_user_id,</span></span>
<span class="line"><span>  created_at,</span></span>
<span class="line"><span>  updated_at</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>user_group_members(</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  group_id,</span></span>
<span class="line"><span>  user_id,</span></span>
<span class="line"><span>  added_by_user_id,</span></span>
<span class="line"><span>  added_at</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>Groups are administrative audiences, not plugin-domain roles. They should be reusable across plugin access policies, notification audiences, and future operator workflows.</p><h3 id="plugin-access-policies" tabindex="-1">Plugin access policies <a class="header-anchor" href="#plugin-access-policies" aria-label="Permalink to &quot;Plugin access policies&quot;">​</a></h3><p>Add a policy field to plugin status/configuration:</p><table tabindex="0"><thead><tr><th>Policy</th><th>Meaning</th></tr></thead><tbody><tr><td><code>everyone</code></td><td>Any active authenticated user can open the plugin. Default for existing behavior.</td></tr><tr><td><code>admins</code></td><td>Only platform admins and owners can open the plugin.</td></tr><tr><td><code>selected_users</code></td><td>Only explicitly granted users can open the plugin.</td></tr><tr><td><code>selected_groups</code></td><td>Only members of explicitly granted groups can open the plugin.</td></tr><tr><td><code>disabled</code></td><td>No user can open the plugin. Console can still manage the installed plugin and its data.</td></tr></tbody></table><p><code>disabled</code> is stronger than every other grant. A disabled plugin does not appear in the Launcher or shell navigation, and direct routes return 404.</p><p>For <code>selected_users</code> and <code>selected_groups</code>, admins and owners are not automatically granted app access. They can grant themselves access in Console, and that action should be audited. Console management access is separate from plugin app access.</p><h3 id="storage-model" tabindex="-1">Storage model <a class="header-anchor" href="#storage-model" aria-label="Permalink to &quot;Storage model&quot;">​</a></h3><p>Extend the existing plugin status/configuration persistence instead of introducing a separate installation concept.</p><p>Proposed additions:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin_status.access_policy</span></span>
<span class="line"><span></span></span>
<span class="line"><span>plugin_access_users(</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  plugin_id,</span></span>
<span class="line"><span>  user_id,</span></span>
<span class="line"><span>  granted_by_user_id,</span></span>
<span class="line"><span>  granted_at</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>plugin_access_groups(</span></span>
<span class="line"><span>  tenant_id,</span></span>
<span class="line"><span>  plugin_id,</span></span>
<span class="line"><span>  group_id,</span></span>
<span class="line"><span>  granted_by_user_id,</span></span>
<span class="line"><span>  granted_at</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>The current enabled/disabled flag should migrate cleanly:</p><ul><li>Existing enabled plugins use <code>access_policy = everyone</code>.</li><li>Existing disabled plugins either remain <code>enabled = false</code> with an effective disabled state or migrate to <code>access_policy = disabled</code>.</li><li>The admin UI should present one coherent disabled concept to operators.</li></ul><h3 id="authorization-resolver" tabindex="-1">Authorization resolver <a class="header-anchor" href="#authorization-resolver" aria-label="Permalink to &quot;Authorization resolver&quot;">​</a></h3><p>Centralize the check so Launcher, middleware, API routes, and Console summaries all agree.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>canOpenPlugin(user, plugin):</span></span>
<span class="line"><span>  if plugin is not installed or not enabled:</span></span>
<span class="line"><span>    return false</span></span>
<span class="line"><span>  if plugin.access_policy == disabled:</span></span>
<span class="line"><span>    return false</span></span>
<span class="line"><span>  if plugin.access_policy == everyone:</span></span>
<span class="line"><span>    return user is active</span></span>
<span class="line"><span>  if plugin.access_policy == admins:</span></span>
<span class="line"><span>    return user is platform admin or owner</span></span>
<span class="line"><span>  if plugin.access_policy == selected_users:</span></span>
<span class="line"><span>    return user has direct plugin access grant</span></span>
<span class="line"><span>  if plugin.access_policy == selected_groups:</span></span>
<span class="line"><span>    return user belongs to a granted group</span></span>
<span class="line"><span>  return false</span></span></code></pre></div><p>The resolver should return enough metadata for Console to explain why a plugin is or is not available, but route guards should not expose private policy details to unauthorized users.</p><h3 id="runtime-enforcement" tabindex="-1">Runtime enforcement <a class="header-anchor" href="#runtime-enforcement" aria-label="Permalink to &quot;Runtime enforcement&quot;">​</a></h3><p>Apply the resolver to every platform-owned entry point:</p><ul><li>Launcher app grid.</li><li>Sidebar and mobile navigation.</li><li><code>/api/plugins</code> and any plugin registry endpoint exposed to authenticated users.</li><li>Root plugin resolution and fallback.</li><li>Generated plugin route eligibility in runtime middleware.</li><li>Public API namespace delegation where authenticated plugin APIs are routed.</li></ul><p>Denied direct plugin app routes should return 404. This keeps restricted plugin existence from being disclosed to users who are not allowed to open it.</p><h3 id="console-ux" tabindex="-1">Console UX <a class="header-anchor" href="#console-ux" aria-label="Permalink to &quot;Console UX&quot;">​</a></h3><p>Console plugin management gets an Access section for each installed plugin:</p><ul><li>Policy selector: Everyone, Admins and owners, Selected users, Selected groups, Disabled.</li><li>User picker for <code>selected_users</code>, backed by the user directory primitives.</li><li>Group picker for <code>selected_groups</code>.</li><li>Effective access summary and empty-state warnings.</li><li>Clear copy that managing a plugin from Console does not automatically grant app access.</li><li>Audit events for policy changes and user/group grant changes.</li></ul><p>Console also gets user group management:</p><ul><li>Create, rename, and describe groups.</li><li>Add/remove users.</li><li>Show where a group is used by plugin access policies.</li><li>Prevent accidental deletion of an in-use group unless the admin explicitly confirms the impact.</li></ul><h3 id="root-plugin-behavior" tabindex="-1">Root plugin behavior <a class="header-anchor" href="#root-plugin-behavior" aria-label="Permalink to &quot;Root plugin behavior&quot;">​</a></h3><p>If the configured root plugin is not openable by the current user, runtime should fall back to Launcher. If Launcher is also inaccessible or no plugins are available, show a platform owned &quot;No apps available&quot; state rather than leaking inaccessible plugin names.</p><h3 id="manifest-defaults" tabindex="-1">Manifest defaults <a class="header-anchor" href="#manifest-defaults" aria-label="Permalink to &quot;Manifest defaults&quot;">​</a></h3><p>No manifest-level default access policy is required for v1. Access is operator policy, not a developer guarantee. A future manifest hint may be considered later for install-time Console recommendations, but the platform must treat operator configuration as authoritative.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="per-user-only" tabindex="-1">Per-user only <a class="header-anchor" href="#per-user-only" aria-label="Permalink to &quot;Per-user only&quot;">​</a></h3><p>Direct per-user grants are simple but do not scale for real teams. They are useful for exceptions and rollout, but groups should be the primary reusable audience primitive.</p><h3 id="groups-only" tabindex="-1">Groups only <a class="header-anchor" href="#groups-only" aria-label="Permalink to &quot;Groups only&quot;">​</a></h3><p>Groups-only access is clean but too rigid. Operators often need to test a plugin with one user or grant temporary access without creating a permanent group.</p><h3 id="deny-overrides" tabindex="-1">Deny overrides <a class="header-anchor" href="#deny-overrides" aria-label="Permalink to &quot;Deny overrides&quot;">​</a></h3><p>Allow/deny matrices add complexity and make effective access harder to reason about. This RFC intentionally starts with one positive policy plus the hard <code>disabled</code> state.</p><h3 id="launcher-only-filtering" tabindex="-1">Launcher-only filtering <a class="header-anchor" href="#launcher-only-filtering" aria-label="Permalink to &quot;Launcher-only filtering&quot;">​</a></h3><p>Hiding apps from Launcher would improve ergonomics but would not be authorization. Direct routes and plugin APIs must enforce the same policy.</p><h3 id="per-user-installation" tabindex="-1">Per-user installation <a class="header-anchor" href="#per-user-installation" aria-label="Permalink to &quot;Per-user installation&quot;">​</a></h3><p>Installing separate plugin copies per user conflicts with Sovereign&#39;s platform-plugin model and creates avoidable upgrade/data duplication problems.</p><h3 id="use-plugin-scoped-roles-only" tabindex="-1">Use plugin-scoped roles only <a class="header-anchor" href="#use-plugin-scoped-roles-only" aria-label="Permalink to &quot;Use plugin-scoped roles only&quot;">​</a></h3><p>RFC 0054 is still needed for inside-plugin permissions, but platform plugin access must be enforced before plugin code runs.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>Should <code>selected_users</code> and <code>selected_groups</code> stay separate policy values, or should the final schema use one <code>selected</code> policy that supports both grant types? The UI can still present the current five choices either way.</li><li>Should plugin manifests support an optional access-policy hint for install-time defaults, while keeping operator policy authoritative?</li><li>When an admin/owner can manage but not open a plugin, should Console show a disabled &quot;Open&quot; button with the reason, or hide it entirely?</li><li>What cache invalidation mechanism should be used so policy changes take effect immediately across middleware, shell navigation, and API routing?</li></ul><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li>Add user groups and Console group management.</li><li>Add the plugin access schema, migration, and centralized resolver.</li><li>Enforce access in runtime routing, Launcher, shell navigation, and plugin API delegation.</li><li>Add Console plugin Access management.</li><li>Add regression tests for every policy, including direct-route denial and root-plugin fallback.</li><li>Update operator and plugin developer docs to distinguish platform app access from plugin-scoped authorization.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><ul><li>2026-07 — Drafted after deciding that plugin visibility must become platform-enforced access control, backed by user groups and explicit plugin policies.</li></ul>`,70)])])}const g=a(l,[["render",o]]);export{h as __pageData,g as default};
