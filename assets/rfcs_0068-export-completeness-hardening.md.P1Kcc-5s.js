import{_ as t,o as i,c as o,a2 as s}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0068 — Export completeness hardening","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0068-export-completeness-hardening.md","filePath":"rfcs/0068-export-completeness-hardening.md"}'),a={name:"rfcs/0068-export-completeness-hardening.md"};function n(r,e,d,l,p,c){return i(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="rfc-0068-—-export-completeness-hardening" tabindex="-1">RFC 0068 — Export completeness hardening <a class="header-anchor" href="#rfc-0068-—-export-completeness-hardening" aria-label="Permalink to &quot;RFC 0068 — Export completeness hardening&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> runtime/src/portability, packages/sdk (portability types), plugins/* (hook audit), docs; amends RFC 0052, builds on RFC 0007<br><strong>Incorporated into plan:</strong> Yes — epic task 8.13</p><h2 id="implementation-notes" tabindex="-1">Implementation notes <a class="header-anchor" href="#implementation-notes" aria-label="Permalink to &quot;Implementation notes&quot;">​</a></h2><p>The two open questions below were resolved during implementation: <code>notExported</code> ships with a single <code>no-export-hook</code> reason (plus <code>disabled</code> for a plugin the export-eligibility filter excludes) — the only case this repo&#39;s plugins actually hit — and the size question was resolved as a documented, enforced ceiling (<code>MAX_EXPORT_BYTES</code>, 50 MB, matching the existing import cap) rather than a background job. <code>installedPlugins</code>/<code>notExported</code> land in <code>BundleManifest</code> (<code>runtime/src/portability/bundle.ts</code>), populated by <code>installedPluginsRoster()</code> (<code>runtime/src/portability/platform.ts</code>) and <code>assembleExport()</code> (<code>runtime/src/portability/assemble.ts</code>); <code>PortabilityPanel.tsx</code> unzips the downloaded bundle client-side to surface non-participating apps by name. The plugin hook audit found <code>sovereign-tasks</code>, <code>sovereign-wallet</code>, <code>sovereign-plainwrite</code>, and <code>sovereign-healthlog</code> already wired; <code>sovereign-shopper</code>, <code>sovereign-ledger</code>, <code>sovereign-docs</code>, <code>sovereign-tally</code>, and <code>sovereign-tritext</code> were closed by adding <code>app/_lib/portability.ts</code> + manifest permissions to each (<code>sovereign-ledger</code> and <code>sovereign-docs</code> had declared the permission without a hook — the exact mismatch this RFC set out to catch).</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>RFC 0007 and RFC 0052 shipped a working, plugin-extensible export/import system: a versioned ZIP bundle assembled from a platform slice plus whatever plugins choose to register via <code>sdk.portability.provideExport</code>/<code>provideImport</code>. That system has a silent gap — a plugin that is installed but has not wired up a portability hook contributes nothing to the bundle, and nothing in the bundle records that the plugin was ever installed. A user cannot tell, from their own export, whether &quot;no data for Wallet&quot; means &quot;you have no Wallet data&quot; or &quot;Wallet doesn&#39;t support export yet.&quot; This RFC hardens the existing implementation to close that gap: it adds an installed-plugin roster to the bundle manifest, makes non-participation an explicit, visible fact instead of an omission, and requires an audit of every shipped plugin&#39;s manifest permissions against its actual hook registrations.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign&#39;s core promise is data ownership — a user should be able to get <strong>all</strong> of their data out, or know precisely what was left behind and why. Today&#39;s bundle only records what succeeded (<code>BundleManifest.sections</code>) and what threw (<code>BundleManifest.failures</code>); a plugin that simply never registered an exporter produces neither entry (<code>runtime/src/portability/assemble.ts:108-110</code>, <code>if (!exporter) continue;</code>). Of the plugins shipped in this repo, only <code>sovereign-plainwrite</code> and <code>sovereign-wallet</code> currently register portability hooks — every other plugin (<code>sovereign-tasks</code>, <code>sovereign-shopper.local</code>, <code>console</code>, <code>launcher</code>, <code>account</code>, and the example plugins) is either silently absent from every export or declares <code>data:export</code>/<code>data:import</code> in its manifest without honoring it. A user reviewing the Account Data tab&#39;s &quot;Data is merged — nothing is overwritten. Plugins not installed are skipped&quot; copy has no way to distinguish &quot;not installed&quot; from &quot;installed but non-participating.&quot;</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><code>runtime/src/portability/bundle.ts:51-70</code> — <code>BundleManifest</code> has <code>sections</code> (successful plugin exports) and <code>failures</code> (exporter threw). No field enumerates plugins installed for the user&#39;s tenant regardless of export participation.</li><li><code>runtime/src/portability/assemble.ts:88-149</code> — <code>assembleExport()</code> iterates <code>exportPlugins: Record&lt;pluginId, version&gt;</code> (the allow-list of eligible, permission-declaring plugins) and calls each plugin&#39;s registered exporter at lines 109-118; if <code>getExporter(pluginId)</code> returns nothing, the loop <code>continue</code>s with no record (line 108-110).</li><li><code>runtime/src/portability/platform.ts:34-57</code> — <code>eligiblePluginIds()</code> / <code>eligibleExportPlugins()</code> filter <code>getInstalledPlugins()</code> down to not-disabled plugins whose manifest declares <code>data:export</code>/<code>data:import</code>. This eligibility list already exists at export time and is discarded after filtering instead of being recorded.</li><li><code>runtime/src/portability/registry.ts:9-14</code> — hook registration is process-local (<code>globalThis[Symbol.for(...)]</code>) and plugins register from their own <code>app/layout.tsx</code> at boot; a plugin can declare the manifest permission without ever calling <code>sdk.portability.provideExport</code>, and nothing today checks for that mismatch.</li><li><code>packages/sdk/src/portability.ts:1-99</code> — the SDK hook contract (<code>ExportContext</code>, <code>PluginExportSection</code>, etc.) is otherwise sufficient; this RFC does not change the hook signatures.</li><li>Reference implementations: <code>plugins/sovereign-plainwrite/app/_lib/portability.ts</code>, <code>plugins/sovereign-wallet/app/_lib/portability.ts</code> — the only two plugins that currently register hooks.</li><li>RFC 0064 (operator backups, Draft, epic tasks 8.10-8.12) independently plans a <code>backup-manifest.json</code> with &quot;installed plugin status&quot; in its per-plugin artifact inventory (<code>docs/epics/data-sovereignty.md:240-242</code>) — the same idea this RFC proposes for the user-level bundle, at the operator layer. The two manifests are separate documents for separate audiences (operator disaster-recovery vs. self-service user export) and this RFC does not merge them, but the shapes should stay conceptually aligned.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="installed-plugin-roster-in-the-bundle-manifest" tabindex="-1">Installed-plugin roster in the bundle manifest <a class="header-anchor" href="#installed-plugin-roster-in-the-bundle-manifest" aria-label="Permalink to &quot;Installed-plugin roster in the bundle manifest&quot;">​</a></h3><p>Add to <code>BundleManifest</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">installedPlugins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  pluginVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  enabled</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  participatesExport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  participatesImport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}&gt;;</span></span></code></pre></div><p>Populated in <code>assembleExport()</code> from the full <code>getInstalledPlugins()</code> list for the tenant — not just the permission-filtered eligible subset — so the bundle always reflects every plugin the user actually uses, regardless of portability support.</p><h3 id="visible-non-participation" tabindex="-1">Visible non-participation <a class="header-anchor" href="#visible-non-participation" aria-label="Permalink to &quot;Visible non-participation&quot;">​</a></h3><p>Where <code>assembleExport()</code> currently <code>continue</code>s past a missing exporter (<code>assemble.ts:108-110</code>), record an entry instead:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notExported</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: Array</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pluginId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: string; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">reason</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;no-export-hook&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;disabled&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Surface this list in <code>PortabilityPanel.tsx</code> (e.g. &quot;3 of 9 installed apps don&#39;t support data export yet: Tasks, Shopper, Console&quot;) so the gap is visible to the user at export time, not discovered by absence.</p><h3 id="plugin-hook-audit" tabindex="-1">Plugin hook audit <a class="header-anchor" href="#plugin-hook-audit" aria-label="Permalink to &quot;Plugin hook audit&quot;">​</a></h3><p>Audit every manifest in <code>plugins/*</code> for <code>data:export</code>/<code>data:import</code> permission declarations against actual <code>sdk.portability.provideExport</code>/ <code>provideImport</code> registrations. For each mismatch, either wire up a real hook or remove the unearned permission declaration — a plugin&#39;s manifest should never claim a capability it doesn&#39;t implement.</p><h3 id="size-and-assembly-mode-decision" tabindex="-1">Size and assembly mode decision <a class="header-anchor" href="#size-and-assembly-mode-decision" aria-label="Permalink to &quot;Size and assembly mode decision&quot;">​</a></h3><p><code>POST /api/account/import</code> caps bundles at 50 MB (<code>runtime/app/api/account/import/route.ts:8</code>) and <code>assembleExport()</code> runs synchronously in the request/response cycle. RFC 0007 already flagged large/async export as an open question (comment at <code>route.ts:21</code>) rather than deciding it. This RFC requires an explicit decision — either a documented, enforced ceiling with a clear user-facing error when a full multi-plugin export would exceed it, or a move to background assembly with a download-when-ready flow — rather than leaving the limit as an implicit, undocumented constraint that a &quot;complete&quot; export could silently exceed.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="leave-non-participation-implicit-document-it-in-prose-only" tabindex="-1">Leave non-participation implicit, document it in prose only <a class="header-anchor" href="#leave-non-participation-implicit-document-it-in-prose-only" aria-label="Permalink to &quot;Leave non-participation implicit, document it in prose only&quot;">​</a></h3><p>Rejected — prose in the Account UI (&quot;plugins not installed are skipped&quot;) does not distinguish &quot;not installed&quot; from &quot;installed but unsupported,&quot; and doesn&#39;t survive the bundle leaving the instance (e.g. for GDPR requests, migration review, or support debugging).</p><h3 id="fold-this-into-rfc-0064-s-operator-backup-manifest-instead" tabindex="-1">Fold this into RFC 0064&#39;s operator backup manifest instead <a class="header-anchor" href="#fold-this-into-rfc-0064-s-operator-backup-manifest-instead" aria-label="Permalink to &quot;Fold this into RFC 0064&#39;s operator backup manifest instead&quot;">​</a></h3><p>Rejected as the primary mechanism — RFC 0064 is instance-operator-facing disaster recovery with a different audience, format, and encryption posture. A user-initiated self-service export must remain self-contained and must not require operator-side tooling to be trustworthy.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li>Should <code>notExported</code> distinguish &quot;plugin never registered a hook&quot; from &quot;plugin declared the permission but the hook itself threw during registration at boot,&quot; or is one <code>no-export-hook</code> reason sufficient for v1?</li><li>Does the size/assembly decision (background job vs. documented ceiling) block this RFC&#39;s other changes, or can the roster/visibility work ship first with size handling as a fast-follow?</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Documentation-first. No code lands under this RFC number until accepted; epic task 8.13 tracks implementation once scheduled. Implementation order:</p><ol><li>Add <code>installedPlugins</code> and <code>notExported</code> to <code>BundleManifest</code> and populate both in <code>assembleExport()</code>.</li><li>Update <code>PortabilityPanel.tsx</code> to render non-participating installed plugins.</li><li>Audit <code>plugins/*</code> manifests vs. hook registrations; close gaps or drop unearned permissions.</li><li>Decide and implement the size/assembly-mode stance.</li><li>Update RFC 0052 (<code>Incorporated into plan</code> note pointing here for the completeness addendum), <code>docs/epics/data-sovereignty.md</code> §8.8/§8.13, and <code>ROADMAP.md</code> in the same PR as the code, per this repo&#39;s doc-parity convention.</li></ol><p>No manifest schema field changes, no SDK hook signature changes — additive to the bundle format only. <code>EXPORT_FORMAT_VERSION</code> should bump from <code>1</code> to <code>2</code> per <code>bundle.ts:114-124</code>&#39;s <code>assertSupportedFormat()</code> guard, since older importers reading a v2 bundle would not understand <code>installedPlugins</code>/ <code>notExported</code> (though they are additive and non-breaking for the <em>fields importers already read</em>, the version bump exists to let readers assert intent explicitly rather than infer forward-compatibility).</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr></tbody></table>`,37)])])}const g=t(a,[["render",n]]);export{u as __pageData,g as default};
