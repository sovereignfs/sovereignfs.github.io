import{_ as o,o as t,c as a,a2 as i}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"ADR-0014: llama-server Deployment and Model Provisioning","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/adrs/0014-llama-server-deployment-and-model-provisioning.md"},"headers":[],"relativePath":"sovereign-os/adrs/0014-llama-server-deployment-and-model-provisioning.md","filePath":"sovereign-os/adrs/0014-llama-server-deployment-and-model-provisioning.md"}'),n={name:"sovereign-os/adrs/0014-llama-server-deployment-and-model-provisioning.md"};function s(r,e,d,l,c,h){return t(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="adr-0014-llama-server-deployment-and-model-provisioning" tabindex="-1">ADR-0014: llama-server Deployment and Model Provisioning <a class="header-anchor" href="#adr-0014-llama-server-deployment-and-model-provisioning" aria-label="Permalink to &quot;ADR-0014: llama-server Deployment and Model Provisioning&quot;">​</a></h1><p><strong>Status:</strong> Accepted</p><p><strong>Date:</strong> 2026-08-09</p><p><strong>Decision owner:</strong> Project creator</p><p><strong>Related RFCs/research:</strong><a href="./../rfcs/0002-local-conversation-and-inference-runtime.html">RFC-0002</a> (Accepted; names &quot;Sovereign Model Management&quot; as a real but separately-scoped component, and requires digest verification &quot;the same supply-chain posture the appliance updater already applies to release bundles and Pi-hole images&quot;), <a href="./0013-initial-inference-runner-and-model-selection.html">ADR-0013</a> (Accepted; selects llama.cpp + Qwen2.5-3B-Instruct-Q4_K_M), <a href="./../research/conversation-service-smoke-test-report.html">conversation-service-smoke-test-report.md</a> and <a href="./../research/conversation-service-authentication-smoke-test-report.html">conversation-service-authentication-smoke-test-report.md</a> (both real-hardware passes that manually stood up llama-server via Docker each time, with no lasting footprint on the device).</p><p><strong>Supersedes:</strong> None</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Conversation Service is implemented, tested, auth-wired, and smoke-tested twice on real hardware — but only ever against a hand-started, throwaway <code>docker run</code> the project owner ran themselves each time. Nothing on the device runs llama-server persistently, and <code>sovereign-conversation.service</code> is deliberately not in <code>customize90-sovereign</code>&#39;s enable-units list because of exactly that gap: enabling it today would ship a service that always returns <code>PROVIDER_UNAVAILABLE</code>.</p><p>Closing this gap means building the piece RFC-0002 named but explicitly did not commit to implementing there: &quot;Sovereign Model Management — download or offline import with checksum verification before a model is ever loaded... persistent storage beneath <code>/data/sovereign/models/</code>.&quot; This ADR scopes and decides a <strong>minimal</strong> version of that — enough to run the one pinned runner/model ADR-0013 already selected, not the full multi-model identity/lifecycle/rollback system RFC-0002 describes as a later concern.</p><h3 id="a-real-constraint-this-decision-must-work-within" tabindex="-1">A real constraint this decision must work within <a class="header-anchor" href="#a-real-constraint-this-decision-must-work-within" aria-label="Permalink to &quot;A real constraint this decision must work within&quot;">​</a></h3><p>Pi-hole&#39;s own container image is embedded directly in the base OS image and every subsequent release bundle: <code>image-builder/sovereign/post-build.sh</code> uses <code>skopeo copy</code> at <strong>image build time</strong> to fetch the exact digest-pinned image (<code>pihole-image.env</code>) into <code>/usr/lib/sovereign/artifacts/pihole-arm64.oci.tar</code>, which ships inside the release the same way <code>appliance/bin/*</code> does. That works because Pi-hole&#39;s image is small. The qualification device&#39;s actual A/B system partition has almost no room to spare — <code>df</code> on <code>sovereign.local</code> during this session&#39;s smoke tests showed <code>/dev/disk/by-slot/active/system</code> at <strong>2.9G total, only ~2.0G free</strong>. Qwen2.5-3B-Instruct-Q4_K_M alone is <strong>~2GB</strong> (<code>2104932768</code> bytes, confirmed twice this session via real download). Embedding the model weights the same way Pi-hole&#39;s image is embedded would not fit — not &quot;would be wasteful,&quot; would not physically fit next to everything already on that partition.</p><p>llama.cpp&#39;s own container image (<code>ghcr.io/ggml-org/llama.cpp:server</code>), by contrast, is a normal-sized container image with no model weights baked in — the same rough size class as Pi-hole&#39;s — and has no reason not to follow Pi-hole&#39;s existing, already-qualified embedding pattern.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Split the runner and the model weights into two different provisioning paths, matching where each one actually fits.</p><h3 id="_1-the-llama-cpp-runner-image-embedded-exactly-like-pi-hole-s" tabindex="-1">1. The llama.cpp runner image: embedded, exactly like Pi-hole&#39;s <a class="header-anchor" href="#_1-the-llama-cpp-runner-image-embedded-exactly-like-pi-hole-s" aria-label="Permalink to &quot;1. The llama.cpp runner image: embedded, exactly like Pi-hole&#39;s&quot;">​</a></h3><p>Add <code>llama-image.env</code> (repository, tag, digest, platform — same shape as <code>pihole-image.env</code>), pinned to a specific digest of <code>ghcr.io/ggml-org/llama.cpp:server</code>. <code>post-build.sh</code> gains the same <code>skopeo copy</code> step Pi-hole&#39;s already has, producing <code>/usr/lib/sovereign/artifacts/llama-arm64.oci.tar</code>, shipped in the base image and every release the same way. Three new systemd units mirror Pi-hole&#39;s exact three-stage shape 1:1:</p><ul><li><code>sovereign-llama-artifact.service</code> → <code>verify-llama-artifact</code> (sha256 the embedded archive, mirrors <code>verify-pihole-artifact</code>)</li><li><code>sovereign-llama-import.service</code> → <code>import-llama-image</code> (<code>docker load</code><ul><li>digest + platform check + readiness marker, mirrors <code>import-pihole-image</code>)</li></ul></li><li><code>sovereign-llama-server.service</code> → <code>start-llama-server</code> / <code>stop-llama-server</code> (mirrors <code>sovereign-pihole.service</code> / <code>start-pihole</code> / <code>stop-pihole</code>)</li></ul><h3 id="_2-the-model-weights-downloaded-into-data-verified-on-every-start" tabindex="-1">2. The model weights: downloaded into <code>/data</code>, verified on every start <a class="header-anchor" href="#_2-the-model-weights-downloaded-into-data-verified-on-every-start" aria-label="Permalink to &quot;2. The model weights: downloaded into `/data`, verified on every start&quot;">​</a></h3><p>Not embedded — RFC-0002&#39;s own words apply here, not the Pi-hole pattern: &quot;download or offline import with checksum verification before a model is ever loaded... persistent storage beneath <code>/data/sovereign/models/</code>.&quot;</p><p><code>start-llama-server</code> gains the download/verify step inline (the same shape <code>start-pihole</code> already uses for its own idempotent generate-if-missing password step — no separate systemd stage needed for something this simple):</p><ul><li>If <code>/data/sovereign/models/qwen2.5-3b-instruct-q4_k_m.gguf</code> is missing, download it from the exact URL every benchmark report&#39;s own &quot;Reproduction&quot; section already uses (<code>https://huggingface.co/Qwen/Qwen2.5-3B-Instruct-GGUF/resolve/main/qwen2.5-3b-instruct-q4_k_m.gguf</code>), to a <code>.tmp</code> path, atomically renamed on success (matching <code>pihole-admin-password</code>&#39;s own tmp-then-<code>mv</code> pattern).</li><li><strong>Verify the SHA-256 digest against a pinned value on <em>every</em> start, not just at download time</strong> — an existing file that fails the check is treated exactly like a missing one (re-downloaded), not silently trusted. This is the actual trust boundary, not the URL: pointing at <code>resolve/main</code> (HuggingFace&#39;s mutable &quot;current file on this branch&quot; path, not an immutable commit-pinned URL) is fine specifically <em>because</em> nothing downstream ever trusts the file without this check — if the upstream file ever changed, the digest check fails closed and triggers a fresh download attempt, not silent use of an unverified artifact.</li><li>The pinned digest is recorded the same way <code>PIHOLE_IMAGE_DIGEST</code> is: computed once from a real download (this project already has <code>qwen2.5-3b-instruct-q4_k_m.gguf</code>&#39;s digest from two independent real downloads to <code>sovereign.local</code> this session, both landing on the identical byte count) and checked into a new <code>llama-model.env</code>. Confirmed a third time from a fresh download during this ADR&#39;s own implementation, all three landing on the identical <code>2104932768</code>-byte file: <code>sha256:626b4a6678b86442240e33df819e00132d3ba7dddfe1cdc4fbb18e0a9615c62d</code> (cross-checked against the <code>md5:45fc4fbbb4e49a4da2d68fd372d1e6b5</code> this project&#39;s own real-hardware smoke test reports already recorded). The llama.cpp runner image is pinned the same way, from a real registry query against <code>ghcr.io/ggml-org/llama.cpp:server</code>&#39;s current manifest index: <code>sha256:78e8d0748ad92c3266bc72a33fe39574a9b3a6bc88d27e371a1377d39d89c68a</code> for <code>linux/arm64</code> (revision <code>7ba604f1cb61cd14898138e9abc0b4ff2601f180</code>, upstream version tag <code>b10331</code>, as of 2026-08-09).</li></ul><h3 id="_3-wiring-sovereign-conversation-service-to-it" tabindex="-1">3. Wiring <code>sovereign-conversation.service</code> to it <a class="header-anchor" href="#_3-wiring-sovereign-conversation-service-to-it" aria-label="Permalink to &quot;3. Wiring `sovereign-conversation.service` to it&quot;">​</a></h3><p><code>sovereign-conversation.service</code> gains <code>After=sovereign-llama-server.service</code> — deliberately <code>After=</code>, not <code>Requires=</code>. The Conversation Service already has a real, tested degraded state for this (<code>ProviderError</code> → <code>TurnError(&quot;PROVIDER_UNAVAILABLE&quot;, ...)</code> → HTTP 502), so a llama-server that&#39;s still downloading the model or has failed to start should produce that existing, well-defined degraded response, not prevent the Conversation Service itself from starting.</p><h3 id="_4-auto-enabling" tabindex="-1">4. Auto-enabling <a class="header-anchor" href="#_4-auto-enabling" aria-label="Permalink to &quot;4. Auto-enabling&quot;">​</a></h3><p>Both <code>sovereign-llama-server.service</code> (which pulls in the artifact/import stages via its own <code>Requires=</code>/<code>Wants=</code>, matching Pi-hole&#39;s shape) and <code>sovereign-conversation.service</code> are added to <code>customize90-sovereign</code>&#39;s enable-units list. This is the concrete change the milestone has been blocked on.</p><h3 id="explicitly-out-of-scope" tabindex="-1">Explicitly out of scope <a class="header-anchor" href="#explicitly-out-of-scope" aria-label="Permalink to &quot;Explicitly out of scope&quot;">​</a></h3><ul><li>Multiple models, model switching, activation/rollback, or any per-model lifecycle state — this ADR pins exactly the one runner/model combination ADR-0013 already selected. A real &quot;Sovereign Model Management&quot; component (RFC-0002&#39;s fuller vision) is future work this ADR does not attempt.</li><li>Changing the pinned model or runner without a new ADR.</li><li>Any mechanism to update the model independently of an appliance software update.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Unblocks the one thing standing between &quot;the Conversation Service is a tested, working API&quot; and &quot;a household can actually reach it without the project owner hand-starting Docker containers.&quot;</li><li>Reuses Pi-hole&#39;s exact, already-hardware-qualified embedding pattern for the runner image — no new mechanism to design or trust there.</li><li>The always-re-verify-on-start rule means a corrupted or tampered model file self-heals (re-downloads) rather than silently degrading answer quality or crashing llama-server in a confusing way.</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li><strong>A fresh install&#39;s first boot needs real internet access and a multi-minute, ~2GB download before conversation works at all</strong> — a real regression from &quot;fully offline after the base image is written,&quot; which is how Pi-hole itself already works. This is a genuine tension with this project&#39;s own name and stated posture, not a minor detail.</li><li>The model source is a third-party host (HuggingFace), not self-hosted — this project has already implicitly accepted that precedent (every benchmark report&#39;s Reproduction section already points there), but this ADR is the first time it becomes something the appliance itself does unattended, not something the project owner types by hand for a benchmark run.</li><li>Re-verifying a ~2GB file&#39;s SHA-256 on every service start adds real time (low-single-digit seconds on this hardware class) to every <code>sovereign-llama-server.service</code> start, including ordinary boots once the file already exists and is valid.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li>If HuggingFace is unreachable on first boot (household network not yet configured, outage, etc.), the Conversation Service stays degraded indefinitely until connectivity exists and the unit is restarted — there is no offline-import fallback path in this ADR&#39;s scope, unlike Pi-hole&#39;s fully-embedded image.</li><li>A future decision to change the pinned model requires a new ADR <em>and</em> a fresh multi-GB download on every existing device — there is currently no delta-update mechanism for model weights the way appliance releases have one for code.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Embed the model weights the same way as Pi-hole&#39;s image.</strong> Rejected outright — does not fit in the ~2.0GB free on the real device&#39;s A/B system partition, confirmed this session, not a theoretical concern.</li><li><strong>Use Ollama&#39;s own <code>pull</code>-and-cache mechanism instead of a manual curl+digest-verify step.</strong> Rejected — ADR-0013 already selected llama.cpp specifically (better v1-corpus accuracy, no cold-start penalty); re-introducing Ollama here just to inherit its model-pull UX would undo that decision for a convenience this project can build directly in ~20 lines of shell, with an explicit verify-before-use guarantee Ollama&#39;s own pull flow doesn&#39;t specifically promise.</li><li><strong>A separate systemd stage for model download/verify, mirroring the artifact/import split used for the container image.</strong> Rejected as unnecessary ceremony for this scope — <code>start-pihole</code> already establishes the precedent that a single idempotent step inside the main start script is the right shape for &quot;generate/fetch this one thing if it&#39;s missing,&quot; and the model-download step is no more complex than that.</li></ul><h2 id="validation-and-revisit-conditions" tabindex="-1">Validation and Revisit Conditions <a class="header-anchor" href="#validation-and-revisit-conditions" aria-label="Permalink to &quot;Validation and Revisit Conditions&quot;">​</a></h2><ul><li>Revisit if the A/B system partition&#39;s size budget ever changes meaningfully (e.g., a base-OS layout change) — embedding might become viable and would remove the first-boot download dependency entirely.</li><li>Revisit once a real &quot;Sovereign Model Management&quot; component (multi-model, activation, rollback) is actually built — this ADR&#39;s mechanism should likely be absorbed into or replaced by it, not maintained in parallel.</li><li>Revisit if ADR-0013&#39;s runner/model selection is ever revisited — this ADR&#39;s pinned digests and URLs are specific to that exact selection.</li></ul>',38)])])}const u=o(n,[["render",s]]);export{p as __pageData,u as default};
