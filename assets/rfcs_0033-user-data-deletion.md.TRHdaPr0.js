import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"User data deletion (Right to erasure)","description":"","frontmatter":{"rfc":27,"title":"User data deletion (Right to erasure)","status":"Implemented","date":"June 2026","author":"kasunben","scope":"packages/db, packages/sdk, runtime, apps/auth, plugins/account, plugins/console, docs\\n","incorporated_into_plan":"Yes — epic task 1.7 (completed)"},"headers":[],"relativePath":"rfcs/0033-user-data-deletion.md","filePath":"rfcs/0033-user-data-deletion.md"}'),n={name:"rfcs/0033-user-data-deletion.md"};function o(r,e,l,d,c,h){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0033-—-user-data-deletion-right-to-erasure" tabindex="-1">RFC 0033 — User data deletion (Right to erasure) <a class="header-anchor" href="#rfc-0033-—-user-data-deletion-right-to-erasure" aria-label="Permalink to &quot;RFC 0033 — User data deletion (Right to erasure)&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Users can permanently delete all their data from a Sovereign instance with a single confirmation in Account → Data. Admins can trigger the same cascade from Console → Users. The platform wipes every owned record and delegates to installed plugins so their data tables are cleaned up too. The entire operation is irreversible — a strong confirmation step (re-enter password) guards against accidental deletion.</p><p>This is the natural counterpart to RFC 0007 (user data portability): export your data first, then delete.</p><hr><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Users who want to leave a Sovereign instance have no way to remove their footprint today — they can only ask the operator to do it manually. Three groups need this:</p><ul><li><strong>Privacy-conscious users</strong> exercising a GDPR Art. 17 / CCPA-equivalent &quot;right to erasure.&quot; Even in jurisdictions that do not mandate it, users expect the ability to delete their account from any modern platform.</li><li><strong>Operators</strong> who need a safe, auditable way for admins to purge departed users without writing raw SQL against the DB.</li><li><strong>Plugin developers</strong> whose data tables need a clean hook rather than being left with orphaned rows when a user is manually removed.</li></ul><hr><h2 id="what-gets-deleted" tabindex="-1">What gets deleted <a class="header-anchor" href="#what-gets-deleted" aria-label="Permalink to &quot;What gets deleted&quot;">​</a></h2><h3 id="platform-tables-always" tabindex="-1">Platform tables (always) <a class="header-anchor" href="#platform-tables-always" aria-label="Permalink to &quot;Platform tables (always)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Table</th><th>What is deleted</th></tr></thead><tbody><tr><td>User record (better-auth)</td><td>The <code>user</code> + <code>session</code> + <code>account</code> rows in the auth DB</td></tr><tr><td><code>account_prefs</code></td><td>All preference rows for the user</td></tr><tr><td><code>activity_log</code></td><td>All activity rows attributed to the user</td></tr><tr><td><code>consent_grants</code></td><td>All consent grants for the user</td></tr><tr><td><code>data_access_log</code></td><td>All audit entries for the user</td></tr><tr><td><code>notifications</code></td><td>All notifications for the user</td></tr><tr><td><code>notification_prefs</code></td><td>The user&#39;s notification preference row</td></tr><tr><td><code>push_subscriptions</code></td><td>All push subscription endpoints for the user</td></tr><tr><td><code>entitlements</code></td><td>All entitlement rows for the user</td></tr><tr><td>Avatar file</td><td><code>data/avatars/&lt;user_id&gt;.*</code> on disk</td></tr></tbody></table><h3 id="plugin-data-opt-in-via-sdk" tabindex="-1">Plugin data (opt-in via SDK) <a class="header-anchor" href="#plugin-data-opt-in-via-sdk" aria-label="Permalink to &quot;Plugin data (opt-in via SDK)&quot;">​</a></h3><p>Plugins register a deletion handler via <code>sdk.portability.provideDelete(handler)</code>. The handler receives <code>{ userId, tenantId, db }</code> and is responsible for deleting all rows in the plugin&#39;s tables that belong to the user. Plugins that do not register a handler have their rows left in place (the operator is responsible for cleaning those up manually — documented in <code>docs/plugin-development.md</code>).</p><p><strong>Isolated-database plugins</strong> (<code>database: &quot;isolated&quot;</code>): the runtime drops the entire plugin DB only if the deleted user is the <strong>only user</strong> on the instance; otherwise the plugin&#39;s deletion handler runs and cleans up per-user rows.</p><h3 id="what-is-not-deleted" tabindex="-1">What is NOT deleted <a class="header-anchor" href="#what-is-not-deleted" aria-label="Permalink to &quot;What is NOT deleted&quot;">​</a></h3><ul><li><strong>Shared / aggregate data</strong> that references the user only by attribution (e.g. plugin content visible to other users): plugin handlers decide; the platform does not impose.</li><li><strong>The audit record of the deletion itself</strong>: a single <code>account.deleted</code> activity entry is written as the last action before the user row is removed — written to a separate admin-visible append-only log, not the user&#39;s own feed.</li></ul><hr><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="sdk-extension" tabindex="-1">SDK extension <a class="header-anchor" href="#sdk-extension" aria-label="Permalink to &quot;SDK extension&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/sdk/src/portability.ts  (stable surface, sdk → 1.12.0)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeletionContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  tenantId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  db</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// DrizzleClient — same as sdk.db.getClient()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeletionResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">deleted</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">errors</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// New:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.portability.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provideDelete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(handler: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeletionContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DeletionResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>provideDelete</code> follows the same registration model as <code>provideExport</code>/<code>provideImport</code> (one handler per plugin, called from the plugin&#39;s server initialisation code).</p><h3 id="api-routes" tabindex="-1">API routes <a class="header-anchor" href="#api-routes" aria-label="Permalink to &quot;API routes&quot;">​</a></h3><h4 id="delete-api-account-user-initiated" tabindex="-1"><code>DELETE /api/account</code> (user-initiated) <a class="header-anchor" href="#delete-api-account-user-initiated" aria-label="Permalink to &quot;\`DELETE /api/account\` (user-initiated)&quot;">​</a></h4><ul><li>Gated by the <strong>session</strong> (<code>x-sovereign-user-id</code> header — only the current user can delete their own account).</li><li>Request body: <code>{ password: string }</code> — must match the current user&#39;s password (verified via a server-to-server call to better-auth <code>change-password</code> endpoint&#39;s verification path, or a dedicated <code>verify-credentials</code> endpoint).</li><li>Returns <code>200</code> with <code>{ deletedAt: string }</code> on success; <code>403</code> on wrong password; <code>409</code> if the user is the sole <code>platform:owner</code> (guard — the last owner cannot self-delete without first assigning another owner).</li><li>After success: the route clears the session cookies and returns. The client redirects to <code>/login?accountDeleted=1</code>.</li></ul><h4 id="delete-api-admin-users-id-admin-initiated" tabindex="-1"><code>DELETE /api/admin/users/[id]</code> (admin-initiated) <a class="header-anchor" href="#delete-api-admin-users-id-admin-initiated" aria-label="Permalink to &quot;\`DELETE /api/admin/users/[id]\` (admin-initiated)&quot;">​</a></h4><p>Extends the existing user-management route. Adds a <code>?deleteData=true</code> query parameter:</p><ul><li>Without <code>?deleteData=true</code> (existing behaviour): deactivates the user.</li><li>With <code>?deleteData=true</code>: runs the full deletion cascade. Requires <code>user:manage</code> capability. Cannot target a <code>platform:owner</code>.</li></ul><h3 id="deletion-cascade-runtime" tabindex="-1">Deletion cascade (runtime) <a class="header-anchor" href="#deletion-cascade-runtime" aria-label="Permalink to &quot;Deletion cascade (runtime)&quot;">​</a></h3><p><code>runtime/src/user-deletion.ts</code> — a single async function <code>deleteUser(userId, tenantId)</code>:</p><ol><li>Collect all registered plugin deletion handlers from the SDK host.</li><li>Run all plugin handlers in parallel (each in its own try/catch; partial failure is recorded but does not abort the platform deletion).</li><li>Delete all platform table rows in dependency order (consent_grants → data_access_log → activity_log → notifications → notification_prefs → push_subscriptions → entitlements → account_prefs).</li><li>Delete the avatar file from disk.</li><li>Call better-auth&#39;s admin API to delete the user record (<code>DELETE /api/auth/admin/remove-user</code>).</li><li>Return a <code>DeletionSummary</code> (<code>{ pluginResults, platformRowsDeleted, errors }</code>) for the activity log entry and/or the admin API response.</li></ol><p>The cascade runs inside a single async transaction on the <strong>platform DB</strong> for the platform tables. Plugin handlers run against their own DB connections outside the transaction (since isolated-plugin DBs are separate stores).</p><h3 id="activity-log" tabindex="-1">Activity log <a class="header-anchor" href="#activity-log" aria-label="Permalink to &quot;Activity log&quot;">​</a></h3><p>Before the user row is deleted, a <code>account.deleted</code> event is written to the admin activity log using <code>logActivity()</code> with the admin actor (or <code>account.self_deleted</code> for self-deletion). This entry survives deletion because it uses the <code>userId</code> only as a string label, not a FK — the log is append-only with no ON DELETE CASCADE.</p><h3 id="ui" tabindex="-1">UI <a class="header-anchor" href="#ui" aria-label="Permalink to &quot;UI&quot;">​</a></h3><h4 id="account-→-data-tab" tabindex="-1">Account → Data tab <a class="header-anchor" href="#account-→-data-tab" aria-label="Permalink to &quot;Account → Data tab&quot;">​</a></h4><p>Below the Export/Import section:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>────────────────────────────────────────</span></span>
<span class="line"><span>Delete your account</span></span>
<span class="line"><span>Remove all your data from this instance permanently. This cannot be undone.</span></span>
<span class="line"><span>Export your data first if you want a copy.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Delete my account]  ← danger-style button</span></span></code></pre></div><p>Clicking opens a native <code>&lt;dialog&gt;</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Delete your account?</span></span>
<span class="line"><span></span></span>
<span class="line"><span>All your data will be permanently removed, including your profile, preferences,</span></span>
<span class="line"><span>activity history, notifications, and any data held by installed plugins.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>To confirm, enter your password:</span></span>
<span class="line"><span>[                        ]   ← password field</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Cancel]  [Delete permanently]</span></span></code></pre></div><p><code>[Delete permanently]</code> calls <code>DELETE /api/account</code>; on success redirects to <code>/login?accountDeleted=1</code>. The login page shows a <code>&quot;Your account has been deleted.&quot;</code> notice (same pattern as <code>?signedout=1</code>).</p><h4 id="console-→-users" tabindex="-1">Console → Users <a class="header-anchor" href="#console-→-users" aria-label="Permalink to &quot;Console → Users&quot;">​</a></h4><p>The existing user row gets a <strong>Delete</strong> action alongside Deactivate/Reactivate:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Role ▼] [Deactivate] [Delete…]</span></span></code></pre></div><p><code>[Delete…]</code> opens a confirmation dialog (<code>&lt;dialog&gt;</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Delete user: alice@example.com?</span></span>
<span class="line"><span></span></span>
<span class="line"><span>This will permanently remove all their data from this instance, including</span></span>
<span class="line"><span>their profile, activity history, plugin data, and files. This cannot be undone.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Cancel]  [Delete permanently]</span></span></code></pre></div><p>Calls <code>DELETE /api/admin/users/[id]?deleteData=true</code>.</p><hr><h2 id="semver-impact" tabindex="-1">Semver impact <a class="header-anchor" href="#semver-impact" aria-label="Permalink to &quot;Semver impact&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Package</th><th>Bump</th><th>Version</th><th>Reason</th></tr></thead><tbody><tr><td><code>@sovereignfs/sdk</code></td><td>minor</td><td>→ <code>1.12.0</code></td><td><code>sdk.portability.provideDelete</code> added to stable surface</td></tr><tr><td><code>@sovereignfs/db</code></td><td>patch</td><td>current → next</td><td><code>deleteUser</code> helpers (no public type changes)</td></tr><tr><td><code>runtime</code></td><td>minor</td><td>current → next</td><td>New <code>DELETE /api/account</code> route; <code>user-deletion.ts</code> cascade; <code>DELETE /api/admin/users/[id]?deleteData=true</code> extension</td></tr><tr><td><code>apps/auth</code></td><td>patch</td><td>current → next</td><td><code>verify-credentials</code> server-to-server helper (or reuse existing auth flow)</td></tr><tr><td><code>plugins/account</code></td><td>minor</td><td>current → next</td><td>New UI section in Data tab</td></tr><tr><td><code>plugins/console</code></td><td>patch</td><td>current → next</td><td>Delete action added to Users table</td></tr><tr><td>Root <code>package.json</code></td><td>patch</td><td>—</td><td>One pre-v1 hardening task; slot in ROADMAP.md</td></tr></tbody></table><hr><h2 id="security-considerations" tabindex="-1">Security considerations <a class="header-anchor" href="#security-considerations" aria-label="Permalink to &quot;Security considerations&quot;">​</a></h2><ul><li><strong>Password re-verification:</strong> <code>DELETE /api/account</code> must verify the current user&#39;s credentials server-side before proceeding. It must not rely solely on the session cookie — a stolen cookie must not be enough to delete an account.</li><li><strong>Last-owner guard:</strong> the route returns <code>409</code> if the target user is the sole <code>platform:owner</code>. The admin must assign another owner before deleting. Console UI disables &quot;Delete&quot; for <code>platform:owner</code> rows.</li><li><strong>No undo:</strong> deletion is irreversible. The confirmation UX (password re-entry for self-deletion; named confirmation dialog for admin deletion) is the sole guard. There is no soft-delete / grace period in v1 — document this clearly.</li><li><strong>Partial failure:</strong> if a plugin handler throws, the cascade records the error in <code>DeletionSummary</code> but continues with platform cleanup. The admin API response and the activity log entry surface the partial failure. Orphaned plugin rows must be manually cleaned by the operator.</li><li><strong>Admin targeting:</strong> <code>DELETE /api/admin/users/[id]?deleteData=true</code> requires <code>user:manage</code> capability and must reject attempts to target <code>platform:owner</code> users.</li></ul><hr><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><p><strong>Grace period / soft-delete?</strong> Some platforms queue deletions and execute them after 48 hours, allowing a cancellation window. For v1 the deletion is immediate — simpler, and consistent with the &quot;operators own their instance&quot; model. Operators who want a grace period can deactivate the user first and delete later.</p></li><li><p><strong>Plugin handler timeout?</strong> Plugin handlers are awaited in parallel with individual try/catch. Should there be a per-handler timeout (e.g. 10 s)? Without one, a slow plugin blocks the cascade. <strong>Proposed default:</strong> 30 s per handler, configurable by the runtime.</p></li><li><p><strong>Invitation-only instances — deleting an invited user&#39;s slot?</strong> Deleting a user frees their email slot. If invite-only is on, the operator would need to re-invite if the same email wants to return. No special handling required.</p></li></ol><hr><h2 id="review-checklist" tabindex="-1">Review checklist <a class="header-anchor" href="#review-checklist" aria-label="Permalink to &quot;Review checklist&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># User-initiated: DELETE /api/account with wrong password → 403</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># User-initiated: DELETE /api/account with correct password → 200; cookies cleared;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   redirect to /login?accountDeleted=1; login page shows deletion notice</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Self-delete as sole platform:owner → 409</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Admin: DELETE /api/admin/users/[id]?deleteData=true → cascade runs; 200</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Admin: attempt to delete platform:owner → 403</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Plugin with provideDelete handler: handler called; rows removed</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Plugin without handler: no crash; deletion proceeds; summary notes missing handler</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># activity_log: account.self_deleted / account.deleted entry survives user row deletion</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Avatar file removed from disk</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># All platform table rows for the user absent after deletion</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># pnpm format:check &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm test</span></span></code></pre></div><hr><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><ul><li><strong>v0.1</strong> (June 2026) — Initial draft.</li></ul>`,62)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
