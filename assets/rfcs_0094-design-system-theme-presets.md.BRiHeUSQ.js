import{_ as s,o as t,c as i,a2 as a}from"./chunks/framework.CHeM0PsO.js";const k=JSON.parse('{"title":"RFC 0094 — Design-system theme presets","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0094-design-system-theme-presets.md"},"headers":[],"relativePath":"rfcs/0094-design-system-theme-presets.md","filePath":"rfcs/0094-design-system-theme-presets.md"}'),o={name:"rfcs/0094-design-system-theme-presets.md"};function n(r,e,d,l,h,c){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0094-—-design-system-theme-presets" tabindex="-1">RFC 0094 — Design-system theme presets <a class="header-anchor" href="#rfc-0094-—-design-system-theme-presets" aria-label="Permalink to &quot;RFC 0094 — Design-system theme presets&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> August 2026<br><strong>Author:</strong> Claude Code<br><strong>Scope:</strong> <code>packages/ui</code> only (tokens, component CSS Modules, Storybook). Builds on the token architecture in <code>docs/design-system.md</code>, the delivery precedent in <a href="./0077-instance-radius-control.html">RFC 0077</a> (instance radius control), and <a href="./../research/0018-design-system-level-theming-neobrutalism.html">research 0018</a>. Companion RFC <a href="./0095-instance-theme-preset-selection.html">0095</a> covers making a preset operator-selectable — deliberately out of scope here.<br><strong>Incorporated into plan:</strong> Yes — shipped in the same change as this RFC&#39;s own draft (design and implementation done together in one session, per explicit developer direction, rather than staged). Every item in Proposed design and Adoption path landed: <code>theme-presets.ts</code>, the generator script, both new token tiers, the <code>Button</code> internal change, the border-width migration, and the Storybook toolbar — verified via <code>pnpm design:tokens:check</code>/<code>typecheck</code>/<code>lint</code>/full test suite/<code>build-storybook</code>, plus a live visual check in Storybook confirming both light and dark neobrutalism render correctly.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Add a small, closed set of named <strong>theme presets</strong> to <code>@sovereignfs/ui</code> — <code>&#39;default&#39;</code> (today&#39;s look, byte-identical) and <code>&#39;neobrutalism&#39;</code> (the first new one) — each a bundle of semantic-token overrides (border width, shadow shape, corner radius, a restrained set of colour/typography tweaks) selected by a new, independent CSS selector that composes with, rather than replaces, the existing <code>[data-theme=&#39;dark&#39;]</code> light/dark switch. This generalizes the exact mechanism <a href="./0077-instance-radius-control.html">RFC 0077</a> already proved for one dimension (corner radius) into a mechanism that can carry a whole visual identity, while keeping every one of that RFC&#39;s guarantees: no component API changes, no <code>runtime/</code> changes, additive and non-breaking.</p><p>Presets are <strong>closed and built-in</strong> — shipped as <code>packages/ui</code> releases, not operator-authored — a deliberate scope decision (see Alternatives considered) to avoid the security and versioning surface an open custom-theme-authoring feature would introduce.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p><a href="./../research/0018-design-system-level-theming-neobrutalism.html">Research 0018</a> explored whether Sovereign could support a strongly distinct alternate visual identity (neobrutalism was the worked example) purely at the design-system level, and found the answer is yes for most of it, with two real gaps: border width is a hardcoded literal, not a token, almost everywhere, and <code>Button</code> has no tactile press interaction. That research also warned against chasing an &quot;authentic,&quot; multi-colour neobrutalism, since it fights the design system&#39;s stated monochrome-minimal identity at a philosophical level, not just a token level — this RFC deliberately stays inside the existing one-accent-colour model.</p><p>The follow-up decision (this RFC&#39;s actual trigger) was to stop treating neobrutalism as a one-off and instead build the <strong>general mechanism</strong> — so that a second, third, or fourth theme later is &quot;add one more entry to a lookup table,&quot; the same way RFC 0077 made every future radius preset trivial once the scale-factor mechanism existed.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>Two-tier token architecture.</strong> <code>packages/ui/src/tokens/primitives.css</code> (fixed) → <code>packages/ui/src/tokens/semantic.css</code> (the theming surface). Every component references semantic tokens by name (<code>docs/design-system.md:57-101</code>).</li><li><strong>Three existing precedents for &quot;override semantic tokens at a selector, zero component changes&quot;:</strong> dark mode (<code>semantic.css:102-141</code>, <code>[data-theme=&#39;dark&#39;]</code>), tenant accent colour (RFC 0027/0032, delivered via <code>runtime/src/instance-style.ts</code>), and instance corner-radius (RFC 0077, <code>--sv-radius-scale</code> in <code>primitives.css:106-117</code>).</li><li><strong><code>data-theme</code> is reserved for light/dark and cannot be reused.</strong><code>runtime/src/theme-script.ts</code>&#39;s pre-paint script unconditionally sets <code>document.documentElement.dataset.theme = dark ? &#39;dark&#39; : &#39;light&#39;</code> on every load, and the script&#39;s exact string is pinned by a CSP hash (<code>THEME_SCRIPT_CSP_HASH</code>). A third value written anywhere else would be overwritten before paint. A theme preset needs its <strong>own</strong> selector.</li><li><strong>Real gaps identified in research 0018:</strong> border width is a hardcoded <code>1px</code> literal across ~29 component CSS Modules (e.g. <code>Card.module.css:9</code>, <code>Button.module.css:6</code>), documented as an intentional exception to &quot;every value is a token&quot; (<code>docs/design-system.md:274-278</code>, exempt from <code>pnpm design:tokens:check</code>). <code>Button</code>&#39;s <code>:active</code> states only <code>color-mix()</code>-darken the background (<code>Button.module.css:104-105, 120-121, 135-136, 151-152</code>) — no shadow, no transform, and <code>Button</code> consumes no <code>--sv-shadow-*</code> token at all today.</li><li><strong><code>pnpm design:tokens:check</code></strong> (<code>scripts/design-tokens-check.ts</code>) fails on undefined <code>var()</code> references and hardcoded colour literals; it does not restrict adding new token definitions.</li><li><strong>Storybook&#39;s Themes toolbar</strong> already toggles <code>data-theme</code> on the canvas root (<code>docs/design-system.md:1675-1679</code>) — the existing pattern to extend for a second, independent toggle.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-single-source-of-truth-a-plain-ts-object-not-raw-css" tabindex="-1">1. Single source of truth: a plain TS object, not raw CSS <a class="header-anchor" href="#_1-single-source-of-truth-a-plain-ts-object-not-raw-css" aria-label="Permalink to &quot;1. Single source of truth: a plain TS object, not raw CSS&quot;">​</a></h3><p><code>packages/ui/src/tokens/theme-presets.ts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThemePresetName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;default&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neobrutalism&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ThemePresetTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  light</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sparse — only tokens this preset overrides</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  dark</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> THEME_PRESETS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ThemePresetName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ThemePresetTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  default: { light: {}, dark: {} }, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// empty — guarantees byte-identical output</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  neobrutalism: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    light: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-radius-scale&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-border-width-hairline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;2px&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-shadow-card&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4px 4px 0 0 var(--sv-color-border-strong)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-shadow-hover&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;6px 6px 0 0 var(--sv-color-border-strong)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-shadow-popover&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4px 4px 0 0 var(--sv-color-border-strong)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-shadow-overlay&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;8px 8px 0 0 var(--sv-color-border-strong)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-button-shadow&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4px 4px 0 0 var(--sv-color-border-strong)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-button-press-x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4px&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;--sv-button-press-y&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4px&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dark: {</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* same shape, dark-appropriate shadow colour */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>This is the <strong>only</strong> place preset token values are authored. It is deliberately plain data (no CSS-in-JS, no build tooling dependency) so it can be imported two ways with zero duplication:</p><ul><li>A small generation step (mirrors <code>scripts/icon-list.ts</code> → <code>pnpm generate:icons</code>&#39;s existing pattern of &quot;TS list generates a committed artifact&quot;) emits <code>packages/ui/src/tokens/theme-presets.css</code> — static CSS for Storybook and standalone (non-runtime) consumers.</li><li>RFC 0095 imports <code>THEME_PRESETS</code> <strong>directly as a TS object</strong> into <code>runtime/src/instance-style.ts</code> — no CSS parsing, no second copy of the values to drift out of sync.</li></ul><p>Rejected alternative: hand-writing the CSS directly and leaving RFC 0095 to duplicate the values in a separate TS lookup (the way <code>RADIUS_SCALE</code> is today). See Alternatives considered.</p><h3 id="_2-selector-independent-of-data-theme-composes-with-it" tabindex="-1">2. Selector: independent of <code>data-theme</code>, composes with it <a class="header-anchor" href="#_2-selector-independent-of-data-theme-composes-with-it" aria-label="Permalink to &quot;2. Selector: independent of \`data-theme\`, composes with it&quot;">​</a></h3><p>Generated CSS shape:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">data-theme-preset</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;neobrutalism&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* ...light tokens... */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">data-theme-preset</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;neobrutalism&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">][</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">data-theme</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dark&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* ...dark tokens... */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The combined selector for the dark variant has specificity <code>(0,0,2,0)</code> — strictly higher than either single-attribute selector alone — so it correctly wins regardless of source order in the compiled stylesheet. No attribute is ever set on <code>&lt;html&gt;</code> for the <code>&#39;default&#39;</code> preset; absence of the attribute is the default state, matching today&#39;s behaviour exactly.</p><p><strong>Correctness constraint, found during implementation: this selector must always match the document root element (<code>&lt;html&gt;</code>), never a nested/scoped wrapper.</strong> <code>--sv-radius-scale</code> is the root of a <code>calc()</code> chain — <code>--sv-radius-sm</code> through <code>-3xl</code> are each <code>calc(var(--sv-radius-scale) * ...)</code>, declared exactly once, at <code>:root</code>, in <code>primitives.css</code>. A custom property referenced inside another custom property&#39;s <code>calc()</code> is resolved using the value cascaded <strong>at the element where the outer property is declared</strong> — not re-resolved per descendant. Since <code>--sv-radius-sm</code> etc. are only ever declared at <code>:root</code>, overriding <code>--sv-radius-scale</code> on some element that isn&#39;t <code>:root</code> (e.g. a plugin&#39;s own root <code>&lt;div&gt;</code>, or a <code>Dialog</code>&#39;s content region) has no effect on them — they keep inheriting whatever <code>--sv-radius-scale</code> resolved to at <code>:root</code>. This is the same constraint <code>.storybook/preview.ts</code>&#39;s pre-existing <code>withRadius</code> decorator comment already documents, discovered independently while building RFC 0077&#39;s own Token Gallery preset demo — this RFC hits the identical issue because the <code>neobrutalism</code> preset also overrides <code>--sv-radius-scale</code>. In practice this means: <code>data-theme-preset</code> must be set on <code>&lt;html&gt;</code> specifically (exactly where <code>data-theme</code> is already required to be set), never on a descendant — Storybook&#39;s activation decorator, RFC 0095&#39;s production delivery (which sidesteps the whole selector question by injecting flat <code>:root { }</code> lines directly, see that RFC), and any future standalone consumer must all honour this. The other new tokens this RFC adds (border width, button-shadow, button-press-x/-y) are leaf tokens with no dependent <code>calc()</code> chain, so they don&#39;t share this constraint — they&#39;d resolve correctly at any nesting level. Only <code>--sv-radius-scale</code> (and any future preset token that composes into another token via <code>calc()</code>) is affected.</p><h3 id="_3-new-tokens-both-default-to-a-no-op" tabindex="-1">3. New tokens, both default to a no-op <a class="header-anchor" href="#_3-new-tokens-both-default-to-a-no-op" aria-label="Permalink to &quot;3. New tokens, both default to a no-op&quot;">​</a></h3><ul><li><p><code>--sv-border-width-hairline: 1px</code> (primitive, <code>primitives.css</code>) — every component switches its literal <code>border: 1px solid ...</code> to <code>border: var(--sv-border-width-hairline) solid ...</code>. Default value is unchanged from today; this is a mechanical, behaviour-preserving migration across the ~29 files research 0018 identified.</p></li><li><p><code>--sv-button-shadow: none</code>, <code>--sv-button-press-x: 0</code>, <code>--sv-button-press-y: 0</code> (semantic, <code>semantic.css</code>) — <code>Button.module.css</code> gains, unconditionally:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.button</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  box-shadow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-button-shadow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  transition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* existing transitions */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    transform </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ease</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    box-shadow </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ease</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.button:active:not</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">:disabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  transform</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">translate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-button-press-x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">--sv-button-press-y</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  box-shadow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">none</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>With the default tokens (<code>none</code>/<code>0</code>/<code>0</code>), this is inert — <code>.button:active</code> already had no visible shadow and <code>translate(0, 0)</code> is a no-op. The rule exists unconditionally in the compiled CSS (no per-theme conditional component logic), but is only visible once a preset sets non-zero values. This is the one genuinely new interaction Sovereign ships as a result of this RFC — see Open questions on whether it should be preset-gated forever or eventually offered as a general opt-in.</p></li></ul><h3 id="_4-storybook-activation-this-rfc-s-own-validation-path" tabindex="-1">4. Storybook activation (this RFC&#39;s own validation path) <a class="header-anchor" href="#_4-storybook-activation-this-rfc-s-own-validation-path" aria-label="Permalink to &quot;4. Storybook activation (this RFC&#39;s own validation path)&quot;">​</a></h3><p>A second toolbar control, alongside the existing Themes (light/dark) addon, setting <code>data-theme-preset</code> on the canvas root (<code>document.documentElement</code>, per the correctness constraint in §2). This is the <strong>only</strong> activation path this RFC delivers — nothing in <code>runtime/</code> sets this attribute; a live Sovereign instance cannot show this preset until RFC 0095 (or some other activation) ships. That&#39;s intentional: this RFC is fully buildable, testable, and revertable without touching platform code.</p><p><strong>A second correctness issue found during implementation:</strong> the existing <code>withRadius</code> decorator (<code>.storybook/preview.ts</code>) sets <code>--sv-radius-scale</code> via an <strong>inline style</strong> (<code>document.documentElement.style.setProperty(...)</code>). An inline style always wins over any stylesheet rule for the same property, regardless of selector specificity — so if <code>withRadius</code> ran unconditionally, it would permanently shadow the <code>neobrutalism</code> preset&#39;s own <code>--sv-radius-scale</code> override, and the Radius toolbar&#39;s last value would silently win instead, even while previewing a preset that&#39;s supposed to control it. Fixed by making <code>withRadius</code> check whether the active theme preset owns <code>--sv-radius-scale</code> (reading <code>THEME_PRESETS</code> directly — no duplicated lookup) and, if so, clear its own inline override instead of setting one, so the preset&#39;s stylesheet rule actually applies. The Radius toolbar becomes advisory while a preset that defines its own radius is active — a deliberate choice: Storybook&#39;s job here is to show what the preset actually looks like, not a half-preset/half-manual hybrid.</p><h3 id="_5-per-repo-convention-storybook-hygiene" tabindex="-1">5. Per repo convention (Storybook hygiene) <a class="header-anchor" href="#_5-per-repo-convention-storybook-hygiene" aria-label="Permalink to &quot;5. Per repo convention (Storybook hygiene)&quot;">​</a></h3><p>New/renamed tokens go in <code>TokenGallery.stories.tsx</code>; the neobrutalism preset gets a row in <code>DesignSystemOverview.stories.tsx</code>&#39;s design-rules section.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Reuse <code>[data-theme]</code> for the preset too</strong> (e.g. <code>data-theme=&quot;neobrutalism-dark&quot;</code>). Rejected — collides with the CSP-hash-pinned pre-paint script (see Current state); light/dark and &quot;brand skin&quot; are orthogonal axes, not one enum.</li><li><strong>Open, operator-authored themes</strong> (raw token values via Console, no code release needed). Rejected for now, per explicit decision during this RFC&#39;s scoping — real security surface (arbitrary CSS-like values need validation/sanitization), a storage format, and versioning risk as <code>packages/ui</code>&#39;s own token set evolves across upgrades. Revisit only if closed built-in presets prove insufficient.</li><li><strong>A full multi-colour palette for neobrutalism</strong> (distinct colours per surface, the way neobrutalism.com itself does it). Rejected — fights the design system&#39;s monochrome-plus-one-accent identity at a philosophical level; the preset stays inside the existing <code>--sv-color-accent</code> model.</li><li><strong>Hand-write the CSS and let RFC 0095 re-derive a separate TS lookup</strong> (mirroring how <code>RADIUS_SCALE</code> exists only in <code>runtime/src/instance-style.ts</code> today). Rejected — two representations of the same values is a drift risk that gets worse with every future preset; a single TS source generating both consumption paths costs little extra and removes the risk entirely.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><s><strong>Exact neobrutalism values are illustrative, not final</strong></s> — <strong>resolved.</strong> Visual QA pass completed in Storybook (light and dark, toolbar-toggled) across the full component catalog: the &quot;Design System Overview&quot; gallery (~90 components, both color schemes), every overlay component individually triggered into its actual open state (<code>Dialog</code>, <code>Drawer</code>, <code>Popover</code>, <code>ContextMenu</code>, <code>Toast</code> — <code>ConfirmDialog</code> has no standalone story; it&#39;s a thin <code>Dialog</code> wrapper, already covered), and every form control (<code>Input</code>, <code>Select</code>, <code>Textarea</code>, <code>Combobox</code>, <code>DatePicker</code>, <code>TagInput</code>), including focus-visible state. Verified via <code>getComputedStyle()</code> on the actual rendered elements, not just visual inspection. Findings: <ul><li>The mechanism is correct everywhere checked: square corners (<code>border-radius: 0px</code>), 2px hairline borders, and hard offset <code>box-shadow</code>s (e.g. <code>8px 8px 0px 0px</code> on <code>Dialog</code>/<code>Toast</code>, <code>4px 4px 0px 0px</code> on the shared popover-style panel used by <code>Popover</code>/<code>ContextMenu</code>/ <code>Combobox</code>) apply consistently. No clipping or z-index issues in any overlay&#39;s open state.</li><li>Focus rings (<code>box-shadow: 0 0 0 2px var(--sv-color-focus-ring)</code>, no offset, no hardcoded radius) automatically inherit the square corners from <code>--sv-radius-scale: 0</code> — nothing theme-specific needed there.</li><li><code>--sv-radius-full</code> elements (<code>TagInput</code> tag pills, avatars, toggles) correctly stay rounded under the preset — the RFC 0077-documented exemption working as designed, not a bug.</li><li>One real aesthetic finding, not a defect: every neobrutalism shadow resolved to <code>--sv-color-border-strong</code>, a soft mid-grey (<code>rgb(212, 212, 216)</code> in light mode) rather than the bolder, higher- contrast (often pure black) shadow &quot;authentic&quot; neobrutalism references use. <strong>Fixed</strong>: added a new semantic token, <code>--sv-color-shadow-strong</code> (<code>semantic.css</code>; <code>--sv-grey-950</code> in light, <code>--sv-grey-50</code> in dark — the same near-black/near-white pairing <code>--sv-color-accent</code> and <code>--sv-color-focus-ring</code> already use, kept as its own token rather than reusing <code>--sv-color-border-strong</code> since a hard offset shadow needs more contrast than a border does), and repointed all six shadow-related properties in <code>theme-presets.ts</code>&#39;s neobrutalism preset (<code>--sv-shadow-card/-hover/-popover/-overlay/-control</code>, <code>--sv-button-shadow</code>) at it — borders are untouched, still on <code>--sv-color-border</code>/<code>-border-strong</code>. Verified via <code>getComputedStyle()</code> in both themes (<code>rgb(9, 9, 11) 8px 8px 0px 0px</code> light, <code>rgb(250, 250, 250) 8px 8px 0px 0px</code> dark) and live in the Token Gallery&#39;s &quot;Theme presets&quot; demo.</li></ul></li><li><strong>Should the <code>Button</code> press-interaction tokens be considered neobrutalism-specific, or a generally available enhancement</strong> any future theme (or even the default theme, eventually) could opt into? Leaning toward &quot;generally available, just currently only set by one preset,&quot; since the tokens themselves are inert by default either way.</li><li><strong>No automated visual-regression tooling exists yet</strong> (<code>Card.module.css</code> notes this directly, epic task 9.14 not landed as of this writing) — verifying the <code>&#39;default&#39;</code> preset stays pixel-identical after the border-width migration relies on manual review, not a CI guarantee.</li><li>Whether the border-width migration across ~29 files ships as one PR or is staged — an adoption-sequencing question, not a design one.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Single PR is feasible — every change is additive and mechanical (new tokens, a new generated stylesheet, a systematic literal-to-token swap, one Storybook addon, one <code>Button</code> internal change). Suggested order: <code>theme-presets.ts</code> data module → generation script → <code>primitives.css</code>/<code>semantic.css</code> new tokens → border-width migration across components → <code>Button</code> internal change → Storybook toolbar + Token Gallery/Overview updates.</p><p><strong>Semver:</strong> additive only — no existing token is renamed or removed, no component prop changes. Ships as a <code>packages/ui</code> minor bump per NFR-04, same conclusion RFC 0077 reached for the identical shape of change.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>August 2026</td><td>Initial draft</td></tr><tr><td>0.2</td><td>August 2026</td><td>Implemented. Found and documented two correctness constraints during implementation, both now fixed in code: (1) <code>--sv-radius-scale</code> overrides must land on the actual <code>:root</code> element (§2), not a scoped wrapper — added to Proposed design. (2) Storybook&#39;s pre-existing <code>withRadius</code> decorator&#39;s inline-style override was silently shadowing the preset&#39;s own stylesheet-based radius override — fixed by making it defer to the active preset when one owns <code>--sv-radius-scale</code> (§4). No change to the design&#39;s public shape (token names, selector, <code>THEME_PRESETS</code> structure all as originally proposed).</td></tr><tr><td>0.3</td><td>August 2026</td><td>Open question 1 resolved: full visual QA pass completed in Storybook (Overview gallery in light/dark, every overlay component triggered open, every form control incl. focus state). Mechanism confirmed correct everywhere. One aesthetic finding — neobrutalism shadows used <code>--sv-color-border-strong</code> (soft grey) instead of a bolder/higher-contrast color — fixed in the same pass: added <code>--sv-color-shadow-strong</code> (<code>semantic.css</code>, near-black light / near-white dark) and repointed the six shadow-related neobrutalism properties at it; borders unchanged. <code>packages/ui</code> bumped <code>0.68.0</code> → <code>0.69.0</code>. See Open questions §1 for the full account.</td></tr></tbody></table>`,39)])])}const g=s(o,[["render",n]]);export{k as __pageData,g as default};
