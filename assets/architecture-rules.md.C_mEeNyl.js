import{_ as o,o as t,c as s,a2 as n}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"Hard Architectural Rules","description":"","frontmatter":{"docSection":"contributors","docType":"policy","audiences":["contributor"],"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/architecture-rules.md"},"headers":[],"relativePath":"architecture-rules.md","filePath":"architecture-rules.md"}'),i={name:"architecture-rules.md"};function d(a,e,r,c,l,h){return t(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="hard-architectural-rules" tabindex="-1">Hard Architectural Rules <a class="header-anchor" href="#hard-architectural-rules" aria-label="Permalink to &quot;Hard Architectural Rules&quot;">​</a></h1><p>Full reference for load-bearing constraints enforced by ESLint, CI, or runtime behaviour. CLAUDE.md carries a critical-violations summary; this document has the full detail.</p><hr><ul><li><p><strong>SDK is the only plugin↔platform contract.</strong> Plugins MUST NOT import from <code>runtime/src</code>, including via the <code>@/</code> alias (<code>runtime/tsconfig.json</code>&#39;s <code>&quot;@/*&quot;: [&quot;./*&quot;]</code> resolves to the same location — closed as an ESLint gap in Task 3.24, workstream 0012 leg 2, after <code>plugins/console</code> was found using it unflagged). ESLint enforces this (established in Task 0.3.3, verified in Task 0.3.8; the <code>@/</code>-alias gap closed in Task 3.24). Plugins use <code>packages/sdk</code> only. <strong><code>plugins/console</code> is the one documented exception</strong> to the <code>runtime/src</code>/<code>@/</code> restriction specifically — it is a platform-type plugin built and shipped as part of this same monorepo, not a third-party one, and its <code>@sovereignfs/db</code>/<code>manifest</code>/<code>mailer</code> restriction still fully applies (<code>eslint.config.ts</code> gives it its own, narrower rule block, not a blanket exclusion). Anything Console needs from those packages goes behind an admin-key-authenticated <code>runtime/app/api/admin/*</code> route it calls over HTTP (the <code>entitlements/actions.ts</code> pattern) — <code>enqueueBackupJob</code>/<code>listBackupJobs</code> had to move behind such a route for exactly this reason. Do not extend this exception to Launcher, Account, or any other plugin without the same deliberate, documented reasoning.</p></li><li><p><strong>Reusable UI/UX capability ships from the design system, not from plugins.</strong> Interaction hooks, overlay surfaces, secondary headers, motion, and controls belong in <code>packages/ui</code> (or the runtime shell when they are shell chrome); plugins — including first-party ones like Sovereign Tasks — only consume them. A fix discovered inside a plugin is designed as a <code>packages/ui</code> addition plus a thin adoption change in the plugin, never as a plugin-local implementation &quot;to be promoted later&quot;. React-coupled UI utilities live in <code>@sovereignfs/ui</code>, not <code>@sovereignfs/sdk</code> (the SDK stays a framework-lean capability contract). See &quot;Design principles&quot; in <code>docs/design-system.md</code>.</p></li><li><p><strong><code>@sovereignfs/sdk</code> is a types-first contract with zero runtime dependencies</strong> (RFC 0023, Task 0.5.21). <code>packages/sdk</code> does not import <code>@sovereignfs/db</code> or <code>@sovereignfs/mailer</code>. Implementations are registered by the runtime at startup via <code>provideHost()</code> in <code>runtime/instrumentation.ts</code> → <code>runtime/src/sdk-host.ts</code>. Never add <code>@sovereignfs/db</code>/<code>@sovereignfs/mailer</code> back as dependencies of the SDK — the <code>noExternal</code>-bundle plan is explicitly dropped. Platform internals belong in <code>runtime/src/sdk-host.ts</code>, not in <code>packages/sdk</code>.</p></li><li><p><strong>Adding a new top-level member to <code>SdkHost</code> (<code>packages/sdk/src/host.ts</code>) is a host-implementer-facing breaking change per NFR-04 — bump <code>@sovereignfs/sdk</code> minor, not patch.</strong> Same bar as widening an existing field&#39;s type (e.g. <code>StorageContext.pluginId</code>, RFC 0044). Not just a semver formality: it breaks <code>pnpm typecheck</code> immediately, not just in theory. Five hand-rolled mock <code>SdkHost</code> objects exist specifically to test the SDK-side wrapper functions in isolation, without a real runtime — <code>packages/sdk/src/__tests__/{events,jobs,mailer-email-plugin-id,sdk,webhooks}.test.ts</code> — each needs a matching stub added for the new member before typecheck passes again, since none of them import the real <code>runtime/src/sdk-host.ts</code>. Found generalizing the <code>sdk.storage</code>/<code>sdk.db.getClient()</code> background-invocation fallback to <code>sdk.env.get()</code>, which needed a new <code>env</code> host member with no prior host indirection at all.</p></li><li><p><strong>Every package/app extends <code>packages/tsconfig</code></strong> (<code>base</code>/<code>nextjs</code>/<code>library</code>), established in Task 0.3.2. Easy to forget on new packages.</p></li><li><p><strong>Manifests are validated at build time.</strong> Invalid manifest = failed build.</p></li><li><p><strong>Plugin tables are slug-prefixed</strong> (<code>tasks_lists</code>, <code>splitify_groups</code>), and every non-platform plugin gets its own isolated store — a <code>plugin_&lt;slug&gt;</code> Postgres schema or sqld namespace — since epic task 8.28 retired the <code>database.isolation: &quot;shared&quot;</code> manifest option. Only the platform-type plugins (Console, Launcher, Account) write to the platform DB.</p></li><li><p><strong><code>tenant_id</code> everywhere</strong> on user-scoped tables from day one (future multi-tenancy), even though no multi-tenant logic exists in v1.</p></li><li><p><strong>DB is dialect-agnostic</strong> (Drizzle): SQLite default, Postgres via env only. No SQLite-specific SQL in app code.</p></li><li><p><strong>The platform data layer is async</strong> (Task 0.5.3). Postgres (node-postgres) has no synchronous query, so <code>getPlatformDb()</code> and every <code>packages/db</code> platform helper (<code>getPlatformSetting</code>, <code>setAccountPrefs</code>, …) and <code>sdk.platform.getConfig()</code> return promises — always <code>await</code> them. (SQLite is served by sqld over <code>@libsql/client</code>, which is itself async — no synchronous SQLite path is left in the live server; <code>better-sqlite3</code> survives only in the one-time legacy-file readers behind <code>sv db migrate-to-postgres</code> and the sqld cutover.) Never reintroduce a synchronous platform-DB read.</p></li><li><p><strong>Relative on-disk paths resolve against the workspace root</strong> (nearest ancestor with <code>pnpm-workspace.yaml</code>, via <code>findWorkspaceRoot()</code>), not the process cwd — the drizzle migrations folder (<code>packages/db/migrations/&lt;dialect&gt;</code>), the avatars directory (<code>runtime/src/avatars.ts</code>), auth&#39;s legal content, and the legacy <code>.db</code> files that <code>sv db migrate-to-postgres</code> and the sqld cutover read. The live server never opens a SQLite file itself: SQLite is sqld-backed with no plain-file fallback (<code>packages/db/src/client.ts</code>). Implemented in both <code>packages/db</code> and <code>apps/auth</code> (duplicated deliberately; auth does not depend on <code>packages/db</code>).</p></li><li><p><strong>No secrets with defaults.</strong> <code>AUTH_SECRET</code> / <code>SOVEREIGN_AUTH_SECRET</code> etc. must throw on startup if unset.</p></li><li><p><strong>Plugins compose at their <code>routePrefix</code> under a <code>shell</code>-selected route group.</strong> The generate script injects <code>plugins/[id]/app/</code> into <code>runtime/app/(platform)/(plugins)/&lt;routePrefix&gt;/</code> for <code>shell: default</code> plugins (they inherit the platform sidebar via App Router layout nesting — no rewrites). The route segment is the manifest <code>routePrefix</code>, not the source directory name, so <code>routePrefix</code> is the single source of truth for a plugin&#39;s URL; the <code>(plugins)</code> route group is URL-transparent, so <code>routePrefix: /console</code> serves at <code>/console</code>. The composed segments are <strong>copies in dev, symlinks in production</strong> (<code>NODE_ENV</code>) — dev must copy because Next&#39;s dev route watcher does not follow symlinked route dirs (a symlinked plugin 404s under <code>next dev</code>); production uses a real symlink instead so a plugin&#39;s imports resolve through <em>its own</em> <code>node_modules</code> rather than requiring every dependency it uses to also be declared in <code>runtime/package.json</code>. Composed segments are gitignored by a <code>.gitignore</code> inside each route group — never edit or commit them. Source of truth is always <code>plugins/[id]/app/</code>. Because production&#39;s symlink isn&#39;t followed by TypeScript&#39;s own module resolution either, <code>runtime/tsconfig.json</code> excludes composed plugin directories (<code>(plugins)</code> and <code>(minimal)</code>) from its type scope — each plugin already typechecks itself in its own repo/CI. The platform plugins in this monorepo have no <code>typecheck</code> script of their own either (<code>cd plugins/console &amp;&amp; npx tsc --noEmit</code> silently resolves an unrelated root tsconfig), so <strong><code>pnpm typecheck</code> never compiles a composed plugin&#39;s route files — only <code>pnpm --filter runtime build</code> does</strong>; a wrong prop name on a <code>@sovereignfs/ui</code> component in Console passed every standalone check and was caught only by a component test. <strong><code>shell: overlay</code> (RFC 0001, Task 0.5.10) composes TWICE:</strong> the full-page fallback under <code>(plugins)/&lt;routePrefix&gt;/</code> (same as default) <strong>and</strong> an interception copy under <code>(plugins)/@modal/(.)&lt;routePrefix&gt;/</code>. The <code>@modal</code> parallel-route slot lives <strong>inside</strong> <code>(plugins)</code> (hosted by a committed <code>(plugins)/layout.tsx</code> that renders <code>{children}{modal}</code>) so the interceptor and the fallback are folder-siblings in the same route group — interception across the group boundary from <code>(platform)</code> fails at runtime with <code>initialTree is not iterable</code>. The slot&#39;s hand-written <code>@modal/default.tsx</code> (empty fallback) and <code>@modal/layout.tsx</code> (the <code>Dialog</code> chrome; renders the Dialog only when <code>useSelectedLayoutSegment()</code> is an intercepted segment, never null, so no empty scrim on ordinary pages) are <strong>committed</strong>; the <code>@modal/(.)*</code> copies are generated and gitignored (the <code>(plugins)/.gitignore</code> keeps <code>layout.tsx</code> + <code>@modal/{default,layout}.tsx</code>, ignores the rest). Overlay <code>routePrefix</code> must be a single segment, and overlay plugins are ineligible as the root plugin (CON-11) — <code>validateRootPlugin</code> rejects <code>shell: &#39;overlay&#39;</code>. <strong>Intra-overlay navigation MUST use <code>replace</code>, not push.</strong> The dialog is dismissed with <code>router.back()</code>, which unwinds exactly one history entry; if a plugin&#39;s in-dialog tab/section links push (the <code>&lt;Link&gt;</code> default), each one stacks on history and a single dismiss only steps back one tab instead of closing — stale dialog states pile up behind it. Console and Account tab links use <code>&lt;Link replace&gt;</code>; this is documented as a convention for third-party overlay plugins in <code>docs/plugin-development.md</code>. Never reintroduce push-based intra-overlay navigation. <strong>Dialog size is plugin-declared</strong> via the optional manifest <code>shellConfig.overlaySize</code> (<code>sm</code> | <code>md</code> | <code>lg</code> | <code>auto</code> | <code>fixed</code>, default <code>lg</code>); the <code>@modal/layout.tsx</code> resolves it from the selected interception segment (<code>overlaySizeForSegment</code> in <code>runtime/src/overlay.ts</code>). <code>lg</code> is a <strong>fixed-size box</strong> (width AND height set, content scrolls inside) — no overlay-shell plugin currently renders into it (Console did, until workstream 0022 moved it to <code>shell: &quot;default&quot;</code>; the <code>PluginAccessDialog</code>/<code>CapabilitiesButton</code> examples below are ordinary in-page <code>Dialog</code> usages inside Console, unaffected by that shell change). <code>fixed</code> is also a <strong>true fixed-size box</strong> (width AND height both set, content never resizes it) but capped at <code>64rem × 44rem</code> rather than filling the viewport — the size Account (epic task 14.5) actually renders into: a <code>lg</code> box read as too large for a settings surface (full-viewport, mostly empty for shorter sections like Notifications), but a content-driven size (<code>auto</code>, tried first) had its own real problem — Account&#39;s <code>/account</code> route client-side redirects to <code>/account/profile</code> through a near-empty intermediate render, and <code>auto</code>&#39;s <code>fit-content</code> visibly shrank the panel to fit that empty page, then grew once the real content landed. <code>fixed</code>&#39;s constant footprint has neither failure mode, at the cost of some benign empty space below a short section&#39;s content (bounded to 44rem, not the full viewport <code>lg</code> left empty). <code>auto</code> remains available for a plugin whose own footprint genuinely varies a lot with no redirect-shaped gap in its route tree (matching its original design intent — a card detail modal with an optional checklist/comments section) — it sizes to content on <strong>both</strong> width and height, each capped at <code>min(48rem, 100%)</code>, and a plugin adopting it should still give its own root element an explicit <code>width</code> at (or under) that cap and a light <code>min-height</code> floor, since otherwise <code>fit-content</code> measures whatever content currently renders and the panel&#39;s width/height visibly jump around between sub-routes; even done well this only bounds, not eliminates, resize-between-routes the way <code>fixed</code>&#39;s true fixed box does. <code>sm</code>/<code>md</code> are fixed-width but content-driven height, capped at a per-size max-height (28rem/42rem) beyond which content scrolls internally rather than the panel growing further — changed from also-fixed-height because a short-content consumer (<code>plugins/console/app/plugins/PluginAccessDialog.tsx</code>&#39;s own nested &quot;Access&quot; dialog, <code>size=&quot;sm&quot;</code>) rendered as a mostly-empty box: measured live, its content filled 191px of a 448px fixed panel, and a sibling nested dialog (<code>CapabilitiesButton.tsx</code>, one grantable capability today) filled only 52px of 430px. <code>lg</code> fills the viewport minus a fixed margin, and is centred and capped the same way <code>md</code>/<code>sm</code> are; on mobile every size is a full-screen sheet. The <code>Dialog</code> scrim is full-viewport by default but offsets its left edge by the <code>--sv-dialog-inset-left</code> CSS var (default <code>0</code>); the shell sets it to the sidebar width (<code>--sv-shell-sidebar-width</code>, reset to <code>0</code> on mobile) on <code>.shell</code> so overlay dialogs start at the sidebar&#39;s right edge and leave the rail visible/usable — never hardcode the sidebar width into the <code>Dialog</code>.</p></li><li><p><strong><code>shell: minimal</code> (RFC 0014, Task 0.5.25) composes into <code>runtime/app/(minimal)/</code></strong> — a chrome-free, full-bleed route group (no sidebar, header, or footer). The committed <code>(minimal)/layout.tsx</code> applies <code>100dvh</code> and safe-area insets; generated composed routes land alongside it (gitignored by <code>(minimal)/.gitignore</code> which keeps <code>layout.tsx</code> and <code>minimal.module.css</code>). The session gate still applies — the middleware enforces auth before the plugin renders. Multi-segment <code>routePrefix</code> is allowed (unlike overlay, which must be single-segment). <strong><code>minimal</code> plugins ARE eligible as the root plugin</strong> (kiosk use case — <code>validateRootPlugin</code> accepts <code>shell: &#39;minimal&#39;</code>); when used as root there is no platform nav, so the plugin must provide its own navigation back to <code>/launcher</code> or other routes if needed. Never reintroduce <code>process.exit(1)</code> for the minimal case in <code>generate-registry.ts</code> — it is wired.</p></li><li><p><strong>Composed route pruning is recursive to the leaf directory, not top-level-only</strong> — a multi-segment <code>routePrefix</code> (<code>shell: minimal</code> explicitly allows this, e.g. <code>/kiosk/display</code>) that gets renamed or uninstalled must have its stale nested directory actually removed from disk, not just orphaned in place with the shared parent segment still matching an active entry. <code>pruneGeneratedEntriesAt</code>/<code>collectComposedLeaves</code> (<code>scripts/generate/compose-routes.ts</code>) walk the full relative path against the active-entry set at every depth, recursing into (and removing-if-empty) any directory that is an ancestor of a currently active target, and removing outright anything that is neither active nor an ancestor of something active. <code>composePlugins()</code> also runs <code>assertNoOrphanedRouteDirectories()</code> after composing/pruning — a <code>generate</code>-time consistency check that walks the composed tree a second, independent way (by presence of a <code>page.tsx</code>/<code>layout.tsx</code>) and fails loudly if anything served by Next.js doesn&#39;t correspond to a currently active plugin target. <strong>The leaf-detection walk must check the active-entry set before falling back to the <code>page.tsx</code>/<code>layout.tsx</code> heuristic</strong> — checking the heuristic first produces false-positive orphan reports on a plugin&#39;s own internal Next.js route groups (e.g. <code>(home)</code>), which have no <code>routePrefix</code> mapping of their own but sit above a real <code>page.tsx</code>.</p></li><li><p><strong><code>shell: default</code> plugins can independently hide the mobile header and/or footer via <code>shellConfig.mobileHeader</code>/<code>shellConfig.mobileFooter</code> (RFC 0075, both boolean, default <code>true</code>) — this is a per-request runtime branch inside <code>(platform)/layout.tsx</code>, not build-time route-group composition</strong> (unlike <code>overlay</code>/<code>minimal</code> above). It has to be: the desktop sidebar must render identically regardless of the toggle, and there&#39;s no route-group split that varies just the two mobile-only elements without duplicating the sidebar markup across every combination. <code>getMobileChromeConfig()</code> (<code>runtime/src/registry.ts</code>) resolves the per-plugin override; the middleware sets <code>x-sovereign-mobile-header</code>/<code>x-sovereign-mobile-footer</code> (mirroring the offline-route flag below) only when a value deviates from the default; <code>(platform)/layout.tsx</code> omits the <code>&lt;header data-mobile-header&gt;</code> block and/or <code>&lt;MobileNav&gt;</code> <strong>server-side</strong> (not CSS-hidden) based on those headers. <strong>Load-bearing details, easy to regress:</strong> (1) <code>shell.module.css</code>&#39;s mobile grid (<code>grid-template-rows: auto 1fr auto</code>) relied on implicit DOM-order placement before this — <code>.mobileHeader</code>, <code>.content</code>, and <code>MobileNav.module.css</code>&#39;s <code>.footer</code> now each declare an explicit <code>grid-row</code> so omitting a sibling can&#39;t renumber the others (a stretched footer over the content row is the failure mode if this is ever removed). (2) <code>--sv-shell-header-height</code>/<code>--sv-shell-footer-height</code> collapse to <code>0px</code> via <code>.shell[data-mobile-header-hidden]</code>/<code>[data-mobile-footer-hidden]</code> attribute selectors (higher specificity than the base <code>.shell</code> rule, same element) — every downstream consumer (<code>.content</code>&#39;s footer-clearance padding, <code>--sv-dialog-inset-top</code>, <code>MobileSearch</code>&#39;s top/bottom clearance, <code>NotificationBell</code>&#39;s popover offset) inherits the fix through the CSS custom-property cascade; don&#39;t re-derive per-consumer overrides. (3) <code>ClientShell</code>&#39;s offline-route refresh-diffing (see below) is generalized — not duplicated — to also force <code>router.refresh()</code> when a client-side navigation crosses a mobile-chrome visibility boundary, using the shared <code>mobileHeaderVisible</code>/<code>mobileFooterVisible</code> helpers in <code>runtime/src/mobile-chrome.ts</code>; without it a soft nav into/out of a reduced-chrome plugin would keep rendering the previous route&#39;s header/ footer state. That same effect also directly sets/clears the <code>--sv-dialog-inset-top</code> inline style on transition, since <code>syncViewport()</code>&#39;s <code>[data-mobile-header]</code> measurement only runs on mount and specific resize/visibility events — never on a plain pathname change — so it can&#39;t be relied on to correct a stale inline value across navigations.</p></li><li><p><strong><code>adminOnly</code> routes are gated in the runtime middleware.</strong> A request under an admin-only plugin&#39;s <code>routePrefix</code> from a user without the <code>console:access</code> capability (RFC 0021 — owner, admin, and auditor hold it) returns 403 (SRS §3.4, PLT-03).</p></li><li><p><strong>Every server action authorizes inside the action — the middleware&#39;s path-based gate is not a substitute.</strong> A <code>&#39;use server&#39;</code> function is a public POST endpoint dispatched by <strong>action id</strong>, not by the page it was written on, so the <code>adminOnly</code> check the middleware applies to <code>/console</code> cannot be assumed to cover it. Every action begins with <code>const session = await sdk.auth.requireSession();</code> <strong>and</strong> an explicit <code>sdk.auth.hasCapability(session, &#39;&lt;capability&gt;&#39;)</code> check — <code>requireSession()</code> alone only proves the caller is <em>some</em> logged-in user, which is not an admin check. This binds hardest in <code>plugins/console/app/**/actions.ts</code>, whose <code>adminFetch</code> helper attaches <code>SOVEREIGN_ADMIN_KEY</code>: an action reachable without a capability check hands the platform&#39;s own admin credentials to whoever calls it. Precedent: <code>plugins/console/app/plugins/remove-actions.ts</code> shipped with no check at all while every sibling action file had one (found by CodeQL alert #1, which flagged the <code>execSync</code> in it and not the missing authorization).</p></li><li><p><strong>Never interpolate a request-derived value into a shell command string.</strong> Use <code>execFileSync</code>/<code>execFile</code> with an argv array so no shell is spawned. <code>JSON.stringify()</code> is <strong>not</strong> shell quoting — it escapes <code>&quot;</code> and <code>\\</code> but leaves <code>$(…)</code>, backticks and <code>${…}</code> live inside the double quotes it adds, and the shell expands all three. Where the value identifies a known entity, resolve it against a server-side allowlist (e.g. the installed-plugin registry) and pass the <em>resolved</em> string, so the subprocess argument never originates from the request at all.</p></li><li><p><strong>An anchored quantifier immediately before <code>$</code>/<code>^</code> is quadratic even alone, with no <code>g</code> flag and no alternation.</strong> <code>/-+$/</code> against a long run of <code>-</code> that does <strong>not</strong> end in one (e.g. <code>&#39;-&#39;.repeat(n) + &#39;X&#39;</code>) backtracks the whole run one character at a time trying to satisfy <code>$</code>, fails, then the search restarts that same backtrack from the next starting offset — O(n²) from a single anchored <code>+</code>. Dropping <code>g</code> or splitting <code>/^-+|-+$/g</code> into two separate replaces (a real first attempt, corrected in the same PR that introduced it — CodeQL alert #5/#8) removes the multiplicative effect of global re-matching but <strong>not</strong> this. The actual fix is to not use a backtracking regex for the trim at all: a manual index scan (<code>while (s[start] === &#39;-&#39;) start++</code>, mirrored from the end) is O(n) with nothing to exploit. See <code>slugify()</code> in <code>runtime/app/api/admin/groups/route.ts</code> and <code>slugifyVitePressHeading()</code> in <code>scripts/check-doc-links.ts</code>.</p></li><li><p><strong>Invite-only is dual-written, and the auth-server copy is authoritative.</strong> The Console toggle (CON-10) writes <code>invite_only</code> to both the platform DB (<code>platform_settings</code>, read by <code>sdk.platform.getConfig()</code>) and the auth server&#39;s own <code>auth_settings</code> table (via the runtime PATCH proxying to <code>apps/auth</code>). Registration enforcement reads only the auth copy — the auth server owns identity and does not read the platform DB. A stored value overrides the <code>AUTH_INVITE_ONLY</code> env default; absent a stored value, the env default applies. Never make registration read the platform DB instead.</p></li><li><p><strong><code>root_plugin_id</code> lives in <code>platform_settings</code>, seeded on first run</strong> to <code>fs.sovereign.launcher</code> (PLT-14/PLT-15). The eligible set is installed + enabled + non-<code>adminOnly</code> (validated in <code>runtime/src/root-plugin.ts</code>). <code>/</code><strong>serves the root plugin in place</strong> — the middleware rewrites <code>/</code> to the configured plugin&#39;s <code>routePrefix</code> (URL stays <code>/</code>; the plugin remains reachable at its own prefix too), resolving the prefix at request time via <code>GET /api/admin/root-plugin</code> (Edge middleware can&#39;t read the DB, same fetch pattern as <code>/api/admin/plugins/disabled</code>). <code>(platform)/page.tsx</code> keeps a <code>redirect()</code> as a fallback for when that resolution fetch fails. Platform tables (<code>tenants</code>, <code>plugin_status</code>, <code>platform_settings</code>) are bootstrapped with <strong>dialect-aware</strong> CREATE-TABLE-IF-NOT-EXISTS + seed rows in <code>packages/db</code>&#39;s <code>getPlatformDb()</code> (<code>packages/db/src/bootstrap.ts</code>, <code>INTEGER</code>/<code>BIGINT</code> + <code>INTEGER</code>/<code>BOOLEAN</code> per dialect); the DDL must stay in sync with the Drizzle schemas (<code>schema/sqlite</code> + <code>schema/postgres</code>, guarded by a parity test). drizzle-kit migrations (<code>packages/db/migrations/&lt;dialect&gt;</code>, <code>runMigrations()</code>) are the primary schema mechanism, but this bootstrap DDL is still what creates these tables wherever migrations haven&#39;t run first — see the <code>bootstrap.ts</code> bullet below; a new column needs both.</p></li><li><p><strong>A row-less plugin (no <code>plugin_status</code> row) defaults to disabled and access-restricted — except automatically in local dev.</strong> <code>getDisabledPluginIds</code> and <code>canUserOpenPlugin</code>/<code>getRestrictedPluginIds</code> (<code>runtime/src/plugin-status.ts</code>, <code>runtime/src/plugin-access-server.ts</code>) both short-circuit to &quot;fully visible, open to everyone&quot; whenever <code>bypassPluginVisibilityInDev()</code> is true, which is an equality check on <code>NODE_ENV === &#39;development&#39;</code> (never <code>!== &#39;production&#39;</code>, since Vitest sets <code>NODE_ENV=test</code> and must keep exercising the real gating logic). This exists because a freshly scaffolded plugin used to stay hidden from its own author until an admin visited Console &gt; Plugins — a DX regression, not intended production behavior. Never applies outside <code>next dev</code>; a production or staging deployment still defaults new plugins closed.</p></li><li><p><strong>A manifest <code>disabled: true</code> plugin is hard-disabled unconditionally, including in local dev.</strong> Unlike the row-less-defaults-to-open dev bypass above, <code>getDisabledPluginIds()</code> (<code>runtime/src/plugin-status.ts</code>) computes manifest hard-disabled ids (<code>getHardDisabledPluginIds()</code>, <code>runtime/src/registry.ts</code>) <em>before</em> checking <code>bypassPluginVisibilityInDev()</code> and returns them regardless. This is deliberate: a hard disable is the plugin author&#39;s own declaration shipped with the code, not a missing-row default a local session should see through. It also has no DB override — <code>activatePlugin()</code> (<code>runtime/src/plugin-catalog.ts</code>) refuses to create a <code>plugin_status</code> row for one at all, and <code>runAllPluginMigrations()</code> (<code>runtime/src/plugin-migrations.ts</code>) skips it entirely.</p></li><li><p><strong>Chrome plugins</strong> (<code>fs.sovereign.launcher</code>, <code>fs.sovereign.account</code>, <code>fs.sovereign.console</code>, <code>fs.sovereign.inbox</code>) are reached through the sidebar chrome (home <code>/</code>, Console ⚙, Account avatar, Inbox 🔔 bell), never via the Launcher grid or the sidebar&#39;s middle plugin-icon section (LCH-04, PLT-12). The canonical ID set is <code>CHROME_PLUGIN_IDS</code> in <code>runtime/src/launcher-plugins.ts</code> — reuse it, never re-hardcode the list. <strong>Always enabled, never toggleable, by construction</strong> — <code>PATCH /api/admin/plugins/[id]</code> refuses to flip a chrome plugin&#39;s <code>plugin_status</code> row in either direction, and <code>getPluginCatalog()</code> excludes them from Console&#39;s Apps list entirely, so there is no admin UI path to a chrome plugin at all. <code>checkBootCompatibility()</code> (<code>runtime/src/boot-compat.ts</code>) must honor this too — it used to disable any incompatible plugin unconditionally, which could silently 404 a chrome plugin platform-wide with no way back (fixed: chrome plugins are now exempted from that write, though still marked incompatible for health/admin visibility). If a chrome plugin&#39;s row ever ends up <code>enabled: false</code> regardless (a manual DB edit, a future bug), <code>sv plugin status &lt;id&gt;</code> / <code>sv plugin enable &lt;id&gt;</code> (<code>bin/sv.ts</code>) are the only recovery path — a direct, unguarded DB write, deliberately outside the admin API&#39;s refusal.</p></li><li><p><strong>Plugins that need the installed-plugin list fetch the gated <code>/api/plugins</code></strong> (forwarding the session cookie), not import the registry — the SDK boundary rule forbids plugins importing <code>runtime/src</code> or internal packages. The route is session-gated (middleware injects <code>x-sovereign-user-role</code>) and role-filters via <code>selectLauncherPlugins</code>. Launcher, Account&#39;s portability panel, and Console&#39;s plugin actions all use this route; <code>sdk.db</code> is for a plugin&#39;s own data, never the registry.</p></li><li><p><strong>The <code>/api/*</code> namespace is split: reserved runtime segments vs. the public provider namespace (PLT-16).</strong> The runtime serves its own first-level segments — <code>account</code>, <code>admin</code>, <code>auth</code>, <code>health</code>, <code>instance</code>, <code>manifest</code>, <code>plugins</code> (<code>runtime/app/api/*</code>) — listed in <code>RESERVED_API_SEGMENTS</code> (<code>runtime/src/api-namespace.ts</code>); a parity test asserts the set matches the on-disk dirs, so a new runtime API route can&#39;t silently become delegatable. Every other <code>/api/&lt;slug&gt;/*</code> is the <strong>public</strong> namespace: the middleware handles it <strong>before</strong> the session gate (it is unauthenticated — the provider owns auth, e.g. API keys), rewriting to the single registered provider&#39;s serve route <code>&lt;routePrefix&gt;/serve/&lt;slug&gt;/&lt;path&gt;</code>, or 404 when no enabled provider is installed. The provider is the one plugin with <code>apiProvider: true</code> in its manifest; <strong>exactly one per instance</strong> — the generate script fails the build on a second one (<code>findApiProvider</code> in <code>@sovereignfs/manifest</code> is the shared resolver). Never add a new <code>runtime/app/api/*</code> segment without adding it to <code>RESERVED_API_SEGMENTS</code>.</p></li><li><p><strong>Server-to-server calls to better-auth (<code>/api/auth/*</code>) must send an <code>Origin</code> header</strong> equal to the auth base URL (<code>SOVEREIGN_AUTH_URL</code>) — better-auth enforces a CSRF origin check and rejects originless POSTs with <code>MISSING_OR_NULL_ORIGIN</code> (403). Applies to <code>update-user</code> (Account profile) and <code>change-password</code>. The session cookie is host-scoped, so forwarding it across the runtime↔auth origins works.</p></li><li><p><strong>The middleware&#39;s unauthenticated <code>/login</code> redirect MUST be <code>303</code>, not the <code>NextResponse.redirect</code> default of <code>307</code>.</strong> 307 preserves the request method, so an unauthenticated <strong>POST</strong> to a gated route (the logout form once the session has lapsed, any plugin form submit, a server action) redirects as <code>POST /login</code> — and <code>runtime/app/login/page.tsx</code> is a page route that only serves <code>GET</code>, so the browser gets 405. 303 (See Other) forces the browser to GET. Any browser-facing redirect to <code>/login</code> (middleware, logout route) must target the <strong>public</strong> auth URL (<code>SOVEREIGN_AUTH_PUBLIC_URL</code>), never the internal <code>SOVEREIGN_AUTH_URL</code> (<code>auth:3001</code>), which the browser cannot resolve in Docker.</p></li><li><p><strong><code>&#39;use client&#39;</code> components must never read browser APIs (<code>navigator</code>, <code>window</code>, <code>localStorage</code>, etc.) inside a <code>useState</code> initializer or during render.</strong> The server renders without those globals, producing different HTML than the client and triggering a React hydration error. The pattern: always initialise state to a server-safe value (e.g. <code>useState(&#39;online&#39;)</code>), then read the browser API inside <code>useEffect</code> and call the setter if needed. The one-frame delay before the UI reflects the real browser state is imperceptible. <code>OfflineBanner</code> is the canonical example — it initialises to <code>&#39;online&#39;</code> and checks <code>navigator.onLine</code> in <code>useEffect</code> (<code>runtime/app/(platform)/_components/OfflineBanner.tsx</code>).</p></li><li><p><strong>Never read <code>NEXT_PUBLIC_*</code> env vars for a value that must vary per deployment at run time.</strong> Next.js inlines <code>process.env.NEXT_PUBLIC_*</code> literals at <strong>build time</strong> into every bundle (client and server); the Docker images build without an <code>.env</code>, so such a read freezes to its fallback and ignores the env injected at container start. The post-login redirect target (<code>NEXT_PUBLIC_RUNTIME_URL</code>) is therefore resolved <strong>server-side at request time</strong> via <code>apps/auth/src/runtime-url.ts</code> (which reads through a computed key so the inliner can&#39;t match it) and passed as a prop to the client <code>LoginForm</code>/ <code>RegisterForm</code>. The auth login/register <code>page.tsx</code> are server components for this reason. <code>.env.example</code> leaves <code>NEXT_PUBLIC_RUNTIME_URL</code> / <code>SOVEREIGN_AUTH_URL</code> commented so the per-environment Compose/code defaults apply (dev → :3000/:3001, prod → :4000/:4001); a hardcoded dev value in <code>.env</code> leaks into the prod stack via interpolation.</p></li><li><p><strong>better-auth&#39;s fresh-session gate is disabled (<code>session.freshAge: 0</code></strong> in <code>apps/auth/src/auth.ts</code>). By default <code>freshSessionMiddleware</code> (guarding <code>GET /list-sessions</code>, used by <code>sdk.auth.listSessions</code>) returns <code>403 SESSION_NOT_FRESH</code> once a session is older than <code>freshAge</code> (1 day) — so the Account Security tab broke for day-old sessions. Self-hosted users stay signed in for weeks, so freshness re-auth is off. Don&#39;t re-enable it without a re-auth flow. (A regression test asserts <code>freshAge === 0</code>.)</p></li><li><p><strong>Profile self-mutations must clear both <code>session_data</code> cookie variants</strong> (<code>better-auth.session_data</code> and <code>__Secure-better-auth.session_data</code>, each with <code>maxAge: 0</code>) — better-auth&#39;s signed session cache lives for 300s, so a self-edit that only clears one variant (or neither) leaves the chrome showing the stale name/avatar for up to 5 minutes even though the underlying record already changed. Done in <code>runtime/app/api/account/avatar/route.ts</code> and <code>plugins/account/app/actions.ts</code>.</p></li><li><p><strong>Theme is applied before first paint by an inline script in <code>runtime/app/layout.tsx</code></strong> reading the <code>sv-theme</code> cookie (<code>light</code>/<code>dark</code> applied directly; <code>system</code>/unset follows <code>prefers-color-scheme</code>). The Account plugin writes the choice to <code>account_prefs</code> (authoritative) and mirrors it to the <code>sv-theme</code> cookie on PATCH. Avatars live on disk at <code>data/avatars/&lt;user_id&gt;.&lt;ext&gt;</code> (served by <code>/api/account/avatar/[userId]</code>); the user record&#39;s <code>image</code> field holds the servable URL.</p></li><li><p><strong>Security headers are split: static via <code>next.config.ts</code>, CSP via middleware</strong> (RFC 0008 Tier 0, Task 0.5.16). Both apps&#39; <code>next.config.ts</code> emit the static headers (<code>X-Frame-Options: DENY</code>, <code>X-Content-Type-Options: nosniff</code>, <code>Referrer-Policy</code>, <code>Permissions-Policy</code>, and <strong>HSTS production-only</strong>). The <strong>Content-Security-Policy is strict and nonce-based</strong>, set per-request in middleware (<code>runtime/proxy.ts</code> extends the session middleware; <code>apps/auth/proxy.ts</code> is dedicated) — every middleware return path must run through <code>applyCsp</code>, and the rendered-path request headers carry the nonce so Next nonces its own inline scripts. CSP builders live in <code>runtime/src/security.ts</code> and the duplicated <code>apps/auth/src/security.ts</code> (the apps share no code). The runtime&#39;s pre-paint theme script is a <strong>fixed string</strong> (<code>runtime/src/theme-script.ts</code>) allowed by a <strong>CSP hash</strong> (<code>THEME_SCRIPT_CSP_HASH</code>, guarded by a drift test) — not a nonce — so the root layout stays statically renderable (the PWA <code>/offline</code> fallback needs it). The nonce only applies on <strong>dynamically-rendered</strong> pages; the runtime&#39;s gated pages are dynamic via the <code>(platform)</code> layout&#39;s <code>headers()</code>, and <code>apps/auth</code> forces dynamic (<code>export const dynamic</code> in its root layout). Never add <code>&#39;unsafe-inline&#39;</code> to <code>script-src</code>. <strong>The runtime CSP&#39;s <code>form-action</code> must include the browser-facing auth origin, not just <code>&#39;self&#39;</code>.</strong> The logout form lives on the runtime origin but its POST 303-redirects to the auth login page on a different origin (<code>SOVEREIGN_AUTH_PUBLIC_URL</code>, e.g. <code>:3001</code>/<code>:4001</code>); browsers check <code>form-action</code> against the whole redirect chain, so <code>&#39;self&#39;</code> alone silently blocks it (the page just flickers, no navigation). The middleware feeds the parsed auth origin (<code>authPublicOrigin()</code>) to <code>buildContentSecurityPolicy</code> (<code>authFormActionOrigin</code>). Postgres connects over TLS when the connection string sets <code>sslmode</code> (<code>pgSslMode</code> in <code>packages/db/src/client.ts</code>; CA via <code>PGSSLROOTCERT</code>). Application-level at-rest encryption (RFC 0071, SQLite-only) shipped and was later retired after repeated hardening rounds (<code>docs/incidents/2026-07-24-rfc-0071-encryption-rollout.md</code>); no dialect has one today.</p></li><li><p><strong>A quick-entry input that commits on Enter must also commit on blur</strong>, via <code>useCommitOnEnterOrBlur</code> (<code>@sovereignfs/ui</code>). iOS Safari&#39;s native &quot;Previous / Next / Done&quot; keyboard-accessory toolbar — added automatically whenever a Sheet/form has more than one focusable field, with no supported way to suppress it — only ever fires a <code>blur</code> when its Done/checkmark is tapped, never a keydown and never a form submit. An <code>onKeyDown</code>-only Enter handler silently discards whatever was typed the moment a user dismisses the keyboard that way instead of pressing the on-screen Return key. Found and fixed across <code>sovereign-tasks</code> and <code>sovereign-shopper</code>&#39;s quick-add rows (add task/subtask/list, mobile rename Sheet); see <code>docs/plugin-development.md</code>&#39;s &quot;Committing quick-entry input&quot; for the pattern and its one exception (a field inside a form with its own always-visible submit button — e.g. login, payment — should NOT commit on blur; that&#39;s the correct, safer default there).</p></li><li><p><strong>The <code>touch-action</code> CSS property&#39;s effective value at any point is the <em>intersection</em> of that element&#39;s own value and every ancestor&#39;s value, not independently scoped per element.</strong> Declaring <code>pan-y</code> on a vertically scrolling child nested inside an ancestor declaring <code>pan-x</code> (e.g. a native horizontal swipe-between-lists carousel) does not &quot;hand off&quot; the horizontal axis to the ancestor — the intersection of <code>{pan-y}</code> and <code>{pan-x}</code> is empty, so neither axis is handled natively anywhere in that subtree, breaking both gestures at once. Fix nested-scroller touch/scroll conflicts with behavioural CSS that doesn&#39;t touch <code>touch-action</code> (e.g. <code>overflow-anchor</code>) or JS-level gesture arbitration, not by declaring narrower <code>touch-action</code> values on nested perpendicular scroll containers. <code>@sovereignfs/ui</code>&#39;s <code>SwipableMobileCarousel</code>/<code>SwipableMobileCarouselSlideBody</code> already account for this (see that component&#39;s CSS) — a new vertically-scrolling slide body should not need to touch <code>touch-action</code> at all.</p></li><li><p><strong>The runtime is an installable PWA</strong> (PLT-09). The web manifest (<code>runtime/public/manifest.json</code>) and PNG icons (<code>runtime/public/icons/</code>) are committed <strong>source</strong>; the service worker (<code>runtime/worker/index.ts</code> + <code>routes.ts</code>, on Serwist) is <strong>bundled into <code>runtime/public/sw.js</code> by <code>runtime/scripts/build-sw.ts</code> after <code>next build</code></strong> — one self-contained script, gitignored + ignored by ESLint and Prettier; never commit or lint it. The build step is bundler-agnostic (it globs <code>.next/static</code> for the precache manifest), which is what let the runtime drop the webpack-only <code>@ducanh2912/next-pwa</code> and build with Turbopack under Next 16. The SW is <strong>disabled in dev</strong> (nothing builds it, and <code>app/_components/ServiceWorkerRegistration.tsx</code> unregisters a stale one so it never interferes with HMR), so installability/Lighthouse only apply to a production build (<code>pnpm --filter @sovereignfs/runtime build</code>). The PWA assets and the <code>/offline</code> fallback are excluded from the middleware session gate (they must load without a session). A plugin declaring <code>installable: true</code> (RFC 0081) gets its own manifest at <code>/api/manifest/[pluginId]</code>, nested under the already-reserved, already session-exempt <code>manifest</code> API segment — safe for the same reason: it re-exposes only fields the plugin&#39;s own manifest already publishes (name, description, icon, routePrefix), all of which any authenticated user already sees on that plugin&#39;s sidebar icon or Launcher tile. No per-user data, no DB write. <strong>The <code>manifest[].icons</code> array&#39;s own URLs need the identical exemption, on a different matcher entry</strong> — <code>runtime/proxy.ts</code>&#39;s matcher must exclude <code>plugin-icons/</code> alongside <code>icons/</code>, or a manifest icon fetch 303-redirects instead of returning an image. This is not merely inconsistent; most browsers don&#39;t follow a redirect when fetching a manifest icon for an installability check, so a gated icon can silently fail the <em>entire install prompt</em> with no other symptom — confirmed live (<code>curl</code> against a running instance) before the matcher entry was added, task 2.26. <code>runtime/src/plugin-manifest.ts</code>&#39;s <code>buildPluginManifestIcons()</code> and <code>scripts/generate-registry.ts</code>&#39;s <code>copyPluginIcons()</code> are the two halves of this: the former lists an icon URL only if the latter is guaranteed to have written that exact file (an <code>icon</code> fallback, or a matching author-supplied <code>icons.*</code> path) — never a guess.</p></li><li><p><strong>A generated maskable plugin icon (RFC 0081, <code>scripts/generate-registry.ts</code>) is always composited onto an opaque background plate, never left transparent.</strong> A transparent maskable icon renders as a floating glyph on a platform-chosen background and looks broken on Android. The plate color is the same <code>#09090b</code> both manifest routes (instance-level and per-plugin) already hardcode as <code>theme_color</code>/<code>background_color</code> — there is no per-instance <code>background_color</code> config field anywhere in the platform today (only <code>instancePrimary</code>/<code>instanceRadius</code> do), and these icons are static, build-time PNGs baked into one Docker image shared across every deployment, so they couldn&#39;t vary per-instance even if that field existed.</p></li><li><p><strong>A cached authenticated document must never be served to a different user.</strong> Sovereign&#39;s pages are per-user SSR (nav, plugin list, account state), so replaying a cached rendered shell for the wrong account — after logout/login on a shared device, or on a device several people use — discloses one user&#39;s content to another. This is the guarantee; the mechanism below is how it is currently met, and may change as long as the guarantee does not.</p><p><strong>How it is met today:</strong> the gated-page route in <code>runtime/worker/routes.ts</code> (<code>isGatedPageNavigation</code>: every same-origin navigation that isn&#39;t an API route or a declared offline shell — Console, Account, any plugin without an <code>offline</code> tier) is handled by Serwist&#39;s <code>NetworkOnly</code>, a strategy with no cache to read or write, bounded by <code>networkTimeoutSeconds</code>; on any failure the fallback plugin serves the precached <code>/offline</code> page. There is no cached document to ever replay to the wrong user, because none exists. (Under next-pwa this was a <code>NetworkFirst</code> whose <code>cacheWillUpdate</code> returned <code>null</code>, because workbox-build&#39;s config schema rejected <code>networkTimeoutSeconds</code> on <code>NetworkOnly</code>; the route table is real bundled code now, so the strategy can say what it means.) Manifest-declared offline routes (<code>offline: &#39;offline-first&#39; | &#39;device-only&#39;</code>, research 0012) are the one exception, cached via the separate <code>offline-shells</code> entry — safe because that document is required to be a <strong>user-neutral shell</strong> (enforced by <code>runtime/src/__tests__/offline-route-neutrality.test.ts</code>: no per-user SSR content at all, personalization hydrates client-side), not because of any per-request identity check. <code>/</code> shares this treatment whenever Launcher (the default root) is itself offline-first — see <code>runtime/src/registry.ts</code>&#39;s <code>getOfflineRoutePrefixes()</code>.</p><p><strong>A signed, per-user offline session assertion previously partitioned the <code>pages</code> cache instead</strong> (research 0012, epic tasks 1.21/2.31/2.32) — every entry keyed by a signature-verified user id, so a request whose user couldn&#39;t be established got an anonymous key rather than colliding with a real one. Removed after live testing (sign a user out, take the device offline, reload) found two compounding problems: the client-side half that populated the assertion was never actually wired into the app, so the partition key was always the anonymous fallback in practice; and separately, next-pwa&#39;s own default caching of bare <code>/</code> (the &quot;start-url&quot; cache) had no partitioning or session check of any kind and was never routed through this mechanism at all, so a signed-out user&#39;s last-cached <code>/</code> replayed indefinitely regardless. Both findings point the same direction: a never-exercised, per-request signature-verification path is a much larger attack surface to keep correct than simply not caching what cannot be proven safe to replay. If per-user offline access to non-neutral pages is ever revisited, budget for the same live, adversarial verification this fix relied on — the failure here shipped past <code>pnpm test</code> and code review and was only caught by actually signing out and going offline against a real build.</p><p><strong>This rule previously read &quot;never switch these entries to a stale-serving strategy, keep them <code>NetworkFirst</code>.&quot;</strong> That was rewritten once (research 0012) to state the requirement rather than the mechanism, on the premise that offline-first needs to serve <em>some</em> cached document with no network. That premise still holds for manifest-declared offline routes (met via neutrality, above) but not for arbitrary per-user pages — <code>pages</code> is back to never stale-serving, which happens to again read like the original mechanism, this time for a verified rather than assumed reason.</p><p>Separately, the safe lever for the iOS &quot;white flash on standalone launch&quot; (a real, largely irreducible WebKit launch-image-to-first-paint gap for non-native-wrapped PWAs) remains bounding the worst case with <code>networkTimeoutSeconds</code> + <code>fallbacks.document</code>, not loosening the above. See <code>docs/research/0011-ios-pwa-inspection-findings.md</code> #5.</p></li><li><p><strong>Production images build from Next.js standalone output</strong> (Task 0.5.2). Both <code>next.config.ts</code> set <code>output: &#39;standalone&#39;</code> <strong>and</strong><code>outputFileTracingRoot</code> to the monorepo root — required in a pnpm monorepo or the trace misses workspace package files. The standalone tree mirrors the repo layout, so the runner runs <code>node runtime/server.js</code> / <code>node apps/auth/server.js</code> (not <code>next start</code>). The standalone server reads <code>PORT</code>/<code>HOSTNAME</code> env (the old <code>next start --port</code> flag is gone): the runner sets <code>PORT</code> + <code>HOSTNAME=0.0.0.0</code> (runtime 3000, auth 3001). <code>runtime/.next/static</code> and <code>runtime/public</code> (which holds the generated PWA assets) must be <strong>copied explicitly</strong> into the runner — standalone does not include them. Healthchecks use <strong><code>127.0.0.1</code>, not <code>localhost</code></strong> (busybox <code>localhost</code>→<code>::1</code>, but the server binds IPv4 <code>0.0.0.0</code> → connection refused on <code>localhost</code>). The runtime&#39;s <code>HEALTHCHECK</code> hits the public <code>/api/health</code> liveness route (excluded from the middleware gate); the admin-key-gated <code>/api/admin/health</code> stays the richer report. <strong><code>docker-compose.prod.yml</code> uses a named volume (<code>sovereign_data</code>), not a host bind mount</strong>, for <code>/app/data</code>: the images run <strong>non-root</strong>, and a named volume inherits the image&#39;s <code>/app/data</code> ownership so avatar and other on-disk writes work with zero host <code>chown</code>; database data itself lives in the <code>sqld</code> service&#39;s own <code>sovereign_sqld_data</code> volume (a bind mount keeps host ownership and breaks non-root writes on Linux — macOS VirtioFS hides this). Dev (<code>docker-compose.yml</code>) keeps the <code>./data</code> bind mount (runs as root). The named volume is <strong>pinned with an explicit <code>name: sovereign_data</code></strong> so Compose doesn&#39;t prefix it with the project (checkout-dir) name — the documented backup/restore commands reference it by that exact name.</p></li><li><p><strong>Both standalone images COPY <code>pnpm-workspace.yaml</code> into <code>/app</code></strong> (runtime and <code>apps/auth</code> Dockerfiles). Next.js standalone <code>server.js</code> calls <code>process.chdir(__dirname)</code> at boot, moving cwd to <code>/app/runtime</code> (or <code>/app/apps/auth</code>). <code>findWorkspaceRoot()</code> walks up from cwd and stops at the <code>pnpm-workspace.yaml</code> marker, returning <code>/app</code> — so the <strong>drizzle migrations folder</strong> (<code>packages/db/migrations/</code>, runtime only), the avatars directory, and auth&#39;s legal content resolve against <code>/app</code>, i.e. the mounted <code>/app/data</code> volume. <strong>Without the marker</strong> <code>findWorkspaceRoot()</code> falls back to the post-<code>chdir</code> cwd: on-disk writes land at <code>/app/runtime/data</code> / <code>/app/apps/auth/data</code> — OUTSIDE the volume, lost on container recreate and missing from backups — and the runtime fails to boot (<code>Can&#39;t find meta/_journal.json</code>). (Before the sqld cutover this also put the SQLite database files themselves outside the volume; database data now lives in the <code>sqld</code> service&#39;s <code>sovereign_sqld_data</code> volume.) Never drop the <code>pnpm-workspace.yaml</code> COPY from either Dockerfile.</p></li><li><p><strong>The runtime Dockerfile must ship every plugin&#39;s <code>manifest.json</code> and <code>migrations/</code> folder into the runner image</strong>, staged into a curated <code>/app/.deploy/plugins</code> directory in the builder stage (not the full <code>plugins/*/</code> tree — that would drag each plugin&#39;s <code>app/</code> source and <code>node_modules</code> into the production image for no benefit, since routes are already compiled into the standalone build). <code>runAllPluginMigrations()</code> (<code>runtime/src/plugin-migrations.ts</code>) and <code>buildIdToDirMap()</code> resolve these paths at server startup relative to the workspace root; if absent, a missing-migrations plugin is <strong>silently skipped</strong> (<code>existsSync</code> guard, no error logged) rather than failing loudly — this was the case for every shared/isolated-mode plugin until Sovereign Tasks (bundled with the platform by default) was the first to actually need it, surfacing as a production 500 (<code>relation &quot;...&quot; does not exist</code>) with nothing in the logs pointing at the cause.</p></li><li><p><strong>A shared or isolated-mode plugin whose application code queries through one dialect&#39;s schema (typically <code>sqlite-core</code>) needs a genuinely separate <code>pgTable</code>-based schema file to generate Postgres migrations from</strong> — <code>drizzle-kit generate --dialect postgresql</code> cannot read a <code>sqliteTable()</code> schema; it silently reports zero tables. That Postgres schema file must use plain <code>integer</code> for booleans/timestamps, never native Postgres <code>boolean</code>/<code>bigint</code> — Drizzle&#39;s query-builder dialect is bound to the client connection, not to the table object&#39;s origin, so the existing SQLite-typed query code keeps working against a Postgres-backed client only if the physical columns serialize identically to what the SQLite column mappers already produce. See <code>docs/plugin-database.md</code> for the full pattern (<code>packages/db/src/schema/{sqlite,postgres}/platform.ts</code> is a different case — the platform&#39;s own query code is dialect-aware via <code>packages/db/src/exec.ts</code>, so its Postgres schema uses native types).</p></li><li><p><strong><code>sdk.device.getSurface()</code> / <code>x-sovereign-surface</code> (RFC 0080), and <code>x-sovereign-focus-plugin</code> (RFC 0082), are presentation hints only, never a security boundary.</strong> Both derive from the shell&#39;s own User-Agent — a client-controlled value any caller can set to anything — so neither must ever be an input to authorization, entitlement, paywall, or data-access decisions. Anything that must not be reachable is gated by session, capability, or plugin permission, never by surface or focus. RFC 0082&#39;s route lock (<code>runtime/src/route-lock.ts</code>&#39;s <code>decideFocusRoute()</code>, wired into <code>runtime/proxy.ts</code>) is a UX and product-scoping mechanism on top of the focus signal, not a security boundary: an out-of-focus path redirects to the focused plugin&#39;s root rather than being denied, and a forged focus target (or an edited User-Agent) reaches exactly the routes the caller&#39;s session/capability/plugin-permission gates already allow — those gates run entirely independently of the lock, before and after it. <code>runtime/proxy.ts</code> strips any inbound <code>x-sovereign-surface</code> / <code>x-sovereign-shell-version</code> / <code>x-sovereign-focus-plugin</code> header before injecting its own (all parsed from the shell&#39;s single <code>Sovereign-Shell/&lt;mobile|desktop&gt;-&lt;platform&gt; &lt;version&gt; (focus=&lt;pluginId&gt;)</code> User-Agent token via <code>runtime/src/surface.ts</code> — RFC 0082 deliberately extends RFC 0080&#39;s existing token rather than inventing a second one), the same discipline already applied to the <code>x-sovereign-user-*</code> family — but the stripping only protects against a spoofed <em>header</em>; it does nothing about a spoofed <em>User-Agent</em>, which is the actual trust boundary this rule exists to name.</p></li><li><p><strong><code>apps/auth</code> reads the exact same <code>DB_DIALECT</code>/<code>POSTGRES_DB_URL</code> the platform does — no separate auth-specific database variable.</strong> This replaced an earlier design where auth resolved its own dialect independently from a separate <code>AUTH_DATABASE_URL</code>, which was a real production gap: an instance whose platform core had already been migrated to Postgres (<code>DB_DIALECT=postgres</code>) could leave <code>AUTH_DATABASE_URL</code> unset, silently stranding auth on its SQLite default while everything else moved, invisible because nothing compared the two. Removing the second variable removes the disagreement entirely, rather than detecting it after the fact. Auth still gets its own dedicated store — a Postgres schema (<code>sovereign_auth</code>, pinned via the connection&#39;s <code>search_path</code> startup option, same technique isolated plugins use) or an sqld namespace of the same name — so better-auth&#39;s tables can never collide with the platform&#39;s own. <code>apps/auth</code> still deliberately doesn&#39;t import <code>@sovereignfs/db</code> (service-boundary independence: own Dockerfile, own deploy) — it duplicates the small amount of dialect/sqld resolution logic it needs (<code>apps/auth/src/db.ts</code>) rather than sharing code, but reads the same env var <em>names</em> the platform does.</p></li><li><p><strong>Every isolated-mode Postgres plugin needs its own <code>migrationsTable</code> (<code>pluginMigrationsTableName(id)</code>), passed explicitly to <code>runPluginMigrations()</code> — the untouched default collides across plugins.</strong> drizzle-orm&#39;s node-postgres migrator tracks applied migrations in a table that lives in a <strong>fixed <code>drizzle</code> schema</strong>, regardless of the connecting pool&#39;s <code>search_path</code> — so two isolated plugins left on the default table name (<code>__drizzle_migrations</code>) silently share ONE tracking table between them, even though their actual data tables correctly land in separate <code>plugin_&lt;slug&gt;</code> schemas via <code>search_path</code>. The second plugin&#39;s migrator compares its own migration timestamps against whatever the first plugin&#39;s migrations left as the newest row in that shared table and concludes &quot;already applied&quot; — skipping every <code>CREATE TABLE</code> statement with <strong>no error, no warning</strong>: the schema exists, empty, indistinguishable from a successful no-op migration. <code>pluginMigrationsTableName()</code> already existed to prevent this exact hazard for <strong>shared</strong>-mode plugins (writing into the platform DB) — it was never extended to isolated-mode Postgres because, until a real production migration (workstream: legacy SQLite → Postgres, task 8.25&#39;s follow-up) added a second one, only one isolated Postgres plugin (<code>com.mooniak.tritext</code>) had ever existed, so the collision was latent, not yet possible. <strong>Never apply this to isolated SQLite plugins</strong> — a genuinely separate file per plugin has no collision risk, and every existing SQLite-isolated plugin already has real migration history under the untouched default name; passing a different table name there would orphan it, not fix anything. Scope the fix by <code>pluginDb.dialect === &#39;postgres&#39;</code>, exactly as <code>runtime/src/plugin-migrations.ts</code> and both <code>bin/sv.ts</code> isolated-Postgres call sites do.</p></li><li><p><strong>Trust between two first-party services on the internal Docker network reuses <code>apps/relay/src/enrollment.ts</code>&#39;s signed-token pattern — do not design a new mechanism per service pair.</strong> A self-verifying, HMAC-SHA256 <code>header.payload.signature</code> token (hand-rolled with <code>node:crypto</code>, no library), verified with <code>timingSafeEqual</code>, no persistent state, revocation via secret rotation. First applied beyond <code>apps/relay</code> itself in <code>apps/harness/src/enrollment.ts</code> (RFC 0063, workstream 0014 leg 2) — <code>apps/harness</code> and Warden&#39;s server-side code authenticate each other this way, <code>SOVEREIGN_HARNESS_ENROLLMENT_SECRET</code> in place of <code>RELAY_ENROLLMENT_SECRET</code>. The scenario doesn&#39;t have to match relay&#39;s exactly (many remote instances enrolling with one shared service) for the pattern to still apply — <code>apps/harness</code> has exactly one caller on a private network, and reuses the same shape anyway, since it costs nothing extra and any later genuinely-remote consumer needs no redesign.</p></li><li><p><strong>A Compose service needing a file to exist before its own process starts (no wait/retry of its own) needs an explicit wait-loop <code>entrypoint</code> override — <code>depends_on</code> only orders container <em>start</em>, not readiness for an arbitrary precondition like a downloaded file landing on a shared volume.</strong> <code>docker-compose.yml</code>&#39;s <code>harness-engine</code> service (llama.cpp server) is the example: <code>-m &lt;path&gt;</code> is a required startup arg with no built-in wait behavior, so its <code>entrypoint</code> polls <code>until [ -s /models/model.gguf ]</code> before <code>exec</code>-ing the real binary. Verify the image&#39;s actual entrypoint binary path first (<code>docker inspect &lt;image&gt; --format &#39;{{.Config.Entrypoint}}&#39;</code>) before writing the override — guessing it wrong silently breaks the service with no useful error.</p></li><li><p><strong>A self-hosted service&#39;s first-boot asset download (a model file, a large bundle) should be lazy and non-blocking, not a gate on container startup or the Docker healthcheck.</strong> <code>apps/harness/src/model.ts</code> starts the Next.js server immediately and downloads the GGUF model in the background; <code>/api/health</code> exposes <code>modelStatus</code> separately so a caller can distinguish &quot;engine unreachable&quot; from &quot;engine up, model still downloading&quot; from &quot;ready&quot; (RFC 0063 §6&#39;s failure-mode table treats this as a normal, observable state, not an edge case to hide behind orchestration timing). Download completion is signaled by an atomic rename to a fixed final filename — any reader (including a separate sidecar container polling for the file) only ever observes either nothing or a fully-verified file, never a partial download.</p></li><li><p><strong>A plugin&#39;s page padding and max-width come from <code>@sovereignfs/ui</code>&#39;s <code>PageContainer</code>, never from local <code>padding</code>/<code>max-width</code> in <code>app/layout.tsx</code> or a page-level CSS module (task 9.25).</strong> The runtime shell (<code>runtime/app/(platform)/shell.module.css</code>) applies no gutter of its own to plugin content — only clearance for chrome it alone can measure (the offline banner&#39;s reserved height while visible, the mobile footer&#39;s height). A plugin that skips <code>PageContainer</code> renders edge-to-edge; a plugin that pads itself <strong>and</strong> wraps in <code>PageContainer</code> double-pads (the exact bug this rule exists to prevent — every plugin under <code>example-plugins/</code> was double-padded until task 9.25 fixed both the plugins and the two scaffolders, <code>bin/helpers.ts</code> and <code>packages/create-plugin/src/index.ts</code>, that had been generating the anti-pattern into every new plugin). The one case that needs no manual handling: an overlay-shell (<code>shell: &quot;overlay&quot;</code>) plugin renders the same page tree into both <code>Dialog</code> (soft navigation) and the shell&#39;s full-page fallback (hard navigation) — <code>Dialog</code>&#39;s content region sets <code>--sv-page-gutter: 0</code>, which <code>PageContainer</code> reads automatically to stand its own padding down, so the tree is padded correctly in both hosts with no per-context branching in plugin code. Plugins that opt out of the shell entirely via <code>data-plugin-fullbleed</code> (<code>sovereign-tasks</code>, <code>sovereign-shopper</code>) manage their own layout and don&#39;t use <code>PageContainer</code> at all. Full detail: <code>docs/design-system.md</code>&#39;s &quot;Page layout&quot; section.</p></li><li><p><strong>A <code>position: sticky</code> element with no compositing layer of its own can go stale/blank for a stretch of frames during a fast scroll on iOS Safari</strong>, instead of repainting at its stuck position — a WebKit momentum-scroll re-tiling quirk, not a stacking-order or z-index bug (<code>overflow: hidden</code> alone does not fix it: that clips the element&#39;s own painted content but doesn&#39;t stop a stale raster tile from the scrolling ancestor showing through underneath it for a frame). Fix: <code>transform: translateZ(0)</code> on the sticky element, promoting it to its own compositing layer rasterized independently of the scroller&#39;s tile grid. This is a generic CSS gotcha, not specific to any one plugin — apply it to <strong>any</strong> <code>position: sticky</code> element that lives inside a container real users will fast-scroll on a touch device (a sticky list/section header, a sticky bottom action bar, a sticky detail-pane header). Skip it on <code>position: sticky</code> elements that are desktop/pointer-only (e.g. the runtime shell&#39;s own desktop sidebar, <code>shell.module.css</code>&#39;s <code>.sidebar</code>) — the bug is a touch-momentum artifact, and an unnecessary compositing layer has its own cost (a new stacking context, extra GPU memory). Not reliably reproducible in this repo&#39;s own Chromium-based browser-preview tooling — confirming it requires a real iOS Safari session (device or Simulator) with a genuinely fast flick, not a scripted/synthetic gesture; a fix&#39;s correctness can only be verified by confirming the CSS rule itself is live (<code>getComputedStyle</code> resolves the <code>transform</code>), not by reproducing the underlying flash in this tooling. Found live (<code>sovereign-tasks</code>, via <code>ffmpeg</code> frame-by-frame analysis of a user-supplied screen recording, after several rounds of synthetic-gesture reproduction attempts failed) across four separate elements — a task row&#39;s swipe-actions container, a sticky list header, a sticky task-detail header, and a sticky bulk-action bar — then applied proactively at the platform level to every other <code>position: sticky</code> element found to be at real risk (mobile, inside scrolling content): <code>@sovereignfs/ui</code>&#39;s <code>MobileHeader</code> and <code>SystemBanner</code>, and the <code>account</code>/<code>console</code> platform plugins&#39; own mobile tab-strip headers. Since plugins outside this monorepo are not something this codebase controls or can audit, the two shared-chrome cases (<code>MobileHeader</code>, <code>SystemBanner</code>) matter most — fixing them once here protects every plugin that renders them, present and future, without requiring any plugin author to know this rule exists.</p></li><li><p><strong>A self-rendered <code>MobileHeader</code>/<code>MobileFooter</code> (<code>@sovereignfs/ui</code>, the <code>shellConfig.mobileHeader</code>/<code>mobileFooter: false</code> pattern, RFC 0075/0088) publishes its own measured height as <code>--sv-shell-header-height</code>/ <code>--sv-shell-footer-height</code> on <code>#sv-app-shell</code> automatically</strong> — both components call <code>usePublishShellChromeHeight</code> internally (<code>packages/ui/src/hooks/usePublishShellChromeHeight.ts</code>) and need no wiring from the consumer. Before this existed, only the <em>platform&#39;s own</em> header/footer had its height accounted for — a plugin opting out to render this chrome itself (a sanctioned pattern, not a workaround) had no way to tell the shell what its real height was, and silently inherited the shell&#39;s own <code>0px</code> override (<code>shell.module.css</code>&#39;s <code>data-mobile-header-hidden</code>/<code>data-mobile-footer-hidden</code>, which assumes &quot;no chrome at all,&quot; not &quot;chrome the shell doesn&#39;t know the size of&quot;). <code>Sheet</code>, <code>Drawer</code>, and <code>Dialog</code> all size themselves against these two variables to stop above/below the chrome instead of sliding underneath it; with the variable stuck at <code>0px</code>, they extended full-height, and since the header/footer&#39;s own <code>z-index</code> (101) beats an overlay&#39;s (100), the chrome visibly covered the overlay&#39;s last ~60px. Reported live as &quot;Drawer has broken&quot; / &quot;task edit screen content not scrollable&quot; (<code>sovereign-tasks</code> — the Delete button and Apps-drawer bottom row were unreachable, not actually a scroll bug) before being root-caused and fixed once here so every current and future consumer of these two components gets it for free. If you touch either component or these three overlays, preserve the contract: the measured value must always win over the shell&#39;s own <code>0px</code> collapse, and must be removed (not just left stale) on unmount so navigating away restores the shell&#39;s own cascade. Do not hand-roll this measurement in a plugin again — if <code>MobileHeader</code>/<code>MobileFooter</code> ever stop covering a real layout, extend the shared hook, not the plugin.</p></li><li><p><strong>Prefer a synchronous <code>getBoundingClientRect()</code> read (in <code>useLayoutEffect</code>, re-run on a <code>window</code> <code>resize</code> listener) over <code>ResizeObserver</code> for measuring an already-rendered element&#39;s size.</strong> In live testing, a freshly created <code>ResizeObserver</code> never fired its callback even once for an already-mounted, stably-sized, non-zero element — reproduced in both this repo&#39;s own Chromium-based browser-preview tooling and a real WebKit iOS Simulator session, cause not fully root-caused. This is not a universal claim that <code>ResizeObserver</code> is broken — only that it has been observed to silently fail in exactly this scenario in this codebase&#39;s own tooling, and a <code>getBoundingClientRect()</code>-based measurement worked immediately and reliably as a drop-in replacement both times it was tried. If a <code>ResizeObserver</code>-based measurement isn&#39;t firing and the element is provably rendered with a stable, non-zero size, don&#39;t spend time adding more <code>ResizeObserver</code> debugging — switch to this pattern first. <code>usePublishShellChromeHeight</code> (above) uses it.</p></li><li><p><strong>A signed-download route&#39;s token lives in its own <code>[token]</code> path segment, never in <code>params</code> without a matching folder and never read off <code>params</code> at all if the route was only ever given a <code>[jobId]</code>-shaped directory.</strong> Found in <code>runtime/app/api/backup-jobs/[jobId]/download/route.ts</code> (RFC 0084, epic task 8.16): the handler destructured <code>token</code> from <code>params</code>, but the route&#39;s own directory only had a <code>[jobId]</code> segment — no <code>[token]</code> folder existed, so <code>token</code> was always <code>undefined</code> and every request 404&#39;d unconditionally, even with a correctly-signed token, from the moment the route shipped. Fixed by moving the file to <code>[jobId]/download/[token]/route.ts</code>, matching the existing, working precedent this route was already supposed to mirror: <code>runtime/app/api/storage/[token]/route.ts</code> (RFC 0044). When adding a new signed-URL download route, copy that file&#39;s shape — dynamic segment per path element the handler reads from <code>params</code>, verified by checking the actual <code>app/api/**</code> directory tree, not just the doc comment describing it. <strong>A second, distinct bug was later found in this same route (epic task 8.17 live verification):</strong> the route&#39;s own doc comment claimed a <code>runtime/proxy.ts</code> matcher exemption &quot;by design&quot; — modeling itself on <code>api/storage</code>&#39;s already-correct exemption — but <code>api/backup-jobs</code> was never actually added to the matcher&#39;s negative-lookahead list. A request carrying a genuinely valid, unexpired signed token still 303-redirected to <code>/login</code> before the route&#39;s own token check ever ran, because middleware intercepted it first; reproduced live via <code>curl</code> with no session cookie against a real completed backup job, and confirmed fixed the same way against a real Postgres-backed job (200, correct bytes, decrypts with the real passphrase). The lesson generalizes: a route&#39;s own doc comment claiming a middleware exemption is not proof it was actually applied — check the real <code>matcher</code> array in <code>runtime/proxy.ts</code> directly before trusting it. Fixed by adding <code>api/backup-jobs</code> to that array — safe here specifically because, like <code>api/storage</code>, no other route shares this prefix (unlike <code>api/instance</code> above, which is deliberately kept <em>inside</em> the matcher because other, privileged routes share its prefix and must stay session-gated). This affected both epic 8.18&#39;s existing Account download flow and epic 8.17&#39;s new Console one, since both surface the exact same download URL. A new regression test exercises <code>config.matcher</code> directly (<code>runtime/src/__tests__/middleware-regression.test.ts</code>&#39;s <code>middleware matcher</code> describe block) rather than relying on a doc comment — confirmed to fail against the pre-fix matcher before being trusted.</p></li><li><p><strong>The production <code>runner</code> image runs instance backup/restore through a bundled, dependency-free CLI, and the backup worker runs inside the runtime process — never a separate worker process.</strong> <code>runner</code> is a minimal <code>node:24-alpine</code> image with only the traced Next.js standalone output — no <code>bin/</code>, <code>scripts/</code>, <code>tsx</code>, or full <code>node_modules</code> — so <code>runtime/src/backup-run.ts</code>&#39;s <code>runInstanceBackup</code> cannot shell out to <code>pnpm sv backup</code> there (it <code>ENOENT</code>&#39;d in production until epic task 8.16). Instead <code>bin/backup-restore.ts</code> holds the <code>backup</code>/<code>restore</code> command logic (shared unchanged with <code>bin/sv.ts</code>), <code>bin/sv-backup-cli.ts</code> registers only those two commands, and <code>tsup</code> (<code>bin/tsup.config.ts</code>, <code>pnpm run build:cli</code>) bundles it into <code>runtime/dist-cli/sv-backup-cli.js</code>, which the Dockerfile copies into <code>runner</code> and <code>runInstanceBackup</code> spawns with plain <code>node</code> when present, falling back to <code>pnpm sv backup</code> on native <code>pnpm dev</code>/<code>tools</code> checkouts. Consequences: those two <code>bin/</code> files never import <code>runtime/src</code> or <code>@sovereignfs/db</code> (two trivial helpers from <code>packages/db/src/{dialect,client}.ts</code> are duplicated locally rather than pulling that barrel — <code>drizzle-orm</code>/<code>cron-parser</code>/<code>@libsql/client</code>/the native <code>better-sqlite3-multiple-ciphers</code> addon — into a bundle that needs two env-var reads), and any dependency added there must be pure JS and listed in <code>noExternal</code>. Moving the worker to a separate <code>tools</code>-based process was considered and rejected: <code>runtime/src/portability/registry.ts</code>&#39;s export-hook registry is populated by request-scoped plugin code as pages load, never at boot, so a headless worker process sees an always-empty registry and silently produces user-scope backups missing every plugin&#39;s data. <code>runner</code> also installs <code>git</code> and <code>postgresql16-client</code> (version-pinned to <code>docker-compose.postgres.yml</code>&#39;s <code>postgres:16-alpine</code>) — previously in neither <code>runner</code> nor <code>tools</code>, which would have <code>ENOENT</code>&#39;d the user-scope git-push/restore-fetch path (<code>runtime/src/git-backup.ts</code>) too, confirmed empirically (<code>docker run --rm node:24-alpine sh -c &quot;git --version&quot;</code> → not found). <strong>Postgres dialect only</strong> — SQLite (sqld) instance backup/restore has no CLI implementation yet; building it needs its own RFC first, per <code>docs/research/0017-sqld-backup-and-restore.md</code>&#39;s gate.</p></li><li><p><strong><code>packages/db/src/bootstrap.ts</code> is a second, hand-written schema definition for at least <code>instance_config</code> and <code>backup_jobs</code>, and it does not automatically stay in sync with <code>schema/{sqlite,postgres}/platform.ts</code> or the generated Drizzle migrations.</strong> Its own doc comment calls it an &quot;interim DDL bootstrap... replaced by drizzle-kit migrations later (0.5.05+)&quot; — that replacement never fully happened for every table, so <code>platformBootstrapStatements()</code>&#39;s <code>CREATE TABLE IF NOT EXISTS</code> DDL is still what actually creates these tables&#39; columns wherever migrations aren&#39;t run first (found live: CI&#39;s Postgres-dialect test job spins up a bare <code>postgres:16</code> container and calls <code>bootstrapPlatformDb()</code> directly, never <code>drizzle-kit migrate</code> — <code>packages/db/src/__tests__/platform-db.pg.test.ts</code> is gated behind <code>describe.skipIf(!PG_URL)</code>, so this path only executes where <code>TEST_DATABASE_URL</code> is set, which is true in CI but not in a typical local run, letting the gap through code review and a clean local <code>pnpm exec vitest run</code>). Adding a real column to one of these tables (RFC 0095&#39;s <code>instance_config.theme_preset</code>, mirroring RFC 0077&#39;s <code>brand_radius</code> precedent) means updating three places, not two: both dialect schema files, the generated migration, <strong>and</strong> this file&#39;s matching <code>CREATE TABLE IF NOT EXISTS</code> statement — verified by reproducing the exact CI failure locally (<code>docker run postgres:16</code> with matching credentials, <code>TEST_DATABASE_URL=... pnpm exec vitest run packages/db/src/__tests__/platform-db.pg.test.ts</code>) before and after the bootstrap.ts fix, not just inferred from the error message.</p></li><li><p><strong>A storage object uploaded with <code>ownerUserId</code> set is unreadable from any job/schedule handler, and there is no background-context fallback that fixes this</strong> (found live building <code>sovereign-plugin-travellog</code>&#39;s Swarm importer, RFC 0044/0046). <code>packages/db</code>&#39;s <code>canAccessStorageObject</code> denies read access whenever <code>ownerUserId</code> is set and the reading context&#39;s <code>userId</code> doesn&#39;t match — including <code>null</code> — and a job/schedule handler&#39;s <code>userId</code> always resolves to <code>null</code>: <code>JobContext</code> (<code>packages/sdk/src/types.ts</code>) carries a plugin id, never a user id, so unlike <code>sdk.db.getClient()</code>&#39;s <code>pluginId</code> (which falls back to <code>getBackgroundPluginContext()</code>, an <code>AsyncLocalStorage</code> populated by <code>runtime/src/jobs.ts</code>/<code>scheduler.ts</code>), there is no equivalent identity to fall back to for <code>userId</code> — a job is plugin-scoped, not inherently user-scoped, so the platform has nothing trustworthy to populate it with even if it wanted to. <code>sdk.storage.put()</code> a plugin intends to read back from a job/schedule handler (as opposed to from a real request, e.g. <code>getSignedUrl</code> calls issued to render a page) must omit <code>ownerUserId</code> entirely; the plugin&#39;s own DB rows (tenant/user-scoped) remain the actual access-control boundary for who can ever learn that storage key, same as every other per-user resource. The tradeoff: an unowned object is invisible to <code>hardDeleteUserStorageObjects</code> (RFC 0033&#39;s cross-plugin <code>owner_user_id</code> sweep on account deletion) and will not be automatically cleaned up by it — a plugin relying on this pattern needs its own deletion path (e.g. a <code>sdk.portability.provideDelete</code> handler) for those keys. Confirmed live: the upload succeeded and the row/bytes were both genuinely present (verified via a raw table dump on the same DB connection, bypassing Drizzle), yet every import job failed with &quot;no longer available in storage&quot; until <code>ownerUserId</code> was dropped from that one <code>put()</code> call.</p></li><li><p><strong>A plugin&#39;s <code>schedules</code>/<code>jobs</code>/<code>events</code> handler must be imported from its real source file, never its composed route-tree copy</strong> — found via a real production deployment failure reported directly (image build succeeded, deploy failed), not live-testing. <code>scripts/generate/plugin-schedules.ts</code>, <code>plugin-jobs.ts</code>, and <code>plugin-events.ts</code> each generate one static <code>import handlerN from &quot;&lt;path&gt;&quot;</code> per manifest-declared handler into <code>runtime/generated/plugin-{schedules,jobs,events}.ts</code>. All three previously computed <code>&lt;path&gt;</code> from <code>resolveComposeTargets(manifest)</code>&#39;s <code>targets[0]</code> — the plugin&#39;s <em>composed</em> app directory (<code>runtime/app/(platform)/(plugins)/&lt;id&gt;/...</code> or <code>(minimal)/&lt;id&gt;/...</code>), which <code>compose-routes.ts</code>&#39;s <code>linkOrCopyTarget</code> makes a real <code>symlinkSync</code> in production (<code>isProd = NODE_ENV === &#39;production&#39;</code>), not a physical copy. <code>runtime/tsconfig.json</code>&#39;s own <code>exclude</code> list — <code>app/(platform)/(plugins)/**</code>, <code>app/(minimal)/**</code> — deliberately keeps composed plugin directories out of the project&#39;s normal type-check scope, but <code>runtime/generated/*.ts</code> is <em>not</em> excluded (it&#39;s covered by the <code>include</code> glob <code>generated/**/*.ts</code>), so a generated handler import reaches into the symlinked tree from outside it. TypeScript resolves that file&#39;s own further relative imports (e.g. a sibling <code>_lib/ids.ts</code>&#39;s <code>import &#39;nanoid&#39;</code>) against the <em>symlink&#39;s apparent location</em> when the file is reached this way — walking up through <code>runtime/</code>&#39;s own <code>node_modules</code> — instead of following the symlink to its real target the way Next&#39;s own App Router route-file discovery does. <code>runtime/package.json</code> doesn&#39;t declare every package an arbitrary plugin might import (it does directly redeclare <code>drizzle-orm</code>, since every plugin&#39;s schema/queries need it, but had never needed to redeclare <code>nanoid</code>), so resolution silently fails with <code>TS2307: Cannot find module</code> — but only for a plugin whose schedule/job/event handler (transitively) imports something not on that list, which is why this went unnoticed: <code>sovereign-plugin-ledger</code> was the first plugin ever to combine a manifest <code>schedules</code> declaration with such an import; <code>sovereign-plugin-tasks</code> (the only other schedule-declaring plugin) never hit it because its own job handler doesn&#39;t happen to import anything <code>runtime</code> doesn&#39;t already have. Root-caused via an isolated <code>git worktree</code> reproduction of the exact failing CI build — deliberately not run against the live dev server sharing the actual working directory, and not run with <code>NODE_ENV=production</code> there either, both to avoid disrupting it — that reproduced the identical error byte-for-byte, then proved it wasn&#39;t a general <code>nanoid</code>-resolution problem by deleting just the one plugin&#39;s import and watching every other <code>nanoid</code>-importing plugin (Kanban, Docs, Sheets — identical import pattern) compile successfully in the same build; <code>tsc -p tsconfig.json --traceResolution</code> on the generated file&#39;s own transitive import chain then confirmed the exact symlink-vs-real-path mechanism, module by module. Fixed at the generator, not with a <code>runtime/package.json</code> redeclaration band-aid (which does also work and was verified independently, but only patches this one instance — any future plugin combining a schedule/job/event with a not-yet-redeclared dependency would hit the identical failure): all three <code>collectPlugin{Schedules,Jobs,Events}</code> functions now import from <code>srcFile</code> (the plugin&#39;s real source path, already computed for the existence check just above) instead of a <code>composedFile</code> built from <code>targets[0]</code>. The <code>resolveComposeTargets</code> call itself stays — it still performs real validation (rejecting a multi-segment <code>routePrefix</code> on an <code>overlay</code>-shell plugin) — only its <code>targets[0]</code> result is no longer used for the import path. Verified via the same isolated-worktree reproduction with the fix applied (the full production build succeeds, <code>/ledger</code>&#39;s route compiles into real server output), the three generators&#39; updated unit tests (<code>scripts/__tests__/generate-registry.test.ts</code>), and the full existing test suite.</p></li><li><p><strong>A Postgres session-level advisory lock (<code>pg_advisory_lock</code>/ <code>pg_advisory_unlock</code>) must be acquired and released on the exact same physical connection, or the unlock silently no-ops and the lock leaks forever.</strong> <code>packages/db/src/migrate.ts</code>&#39;s <code>runMigrations()</code> used to route the lock and unlock through two independent <code>dbGet(pdb, ...)</code> calls; since <code>pdb.db</code> (Postgres) is backed by a <code>pg.Pool</code>, each call is free to be served by a different physical connection, and <code>pg_advisory_unlock</code> only releases a lock held by the calling session — issued from the wrong connection, it does nothing, with no error. This caused a real production outage: a stale lock from one boot (an unrelated redeploy&#39;s restart lost the pool&#39;s race) hung every subsequent <code>runMigrations()</code> call — i.e. every later container restart — indefinitely, taking the entire app down on every request, since Next.js never finishes preparing the server. Diagnosed live via <code>pg_stat_activity</code>/<code>pg_locks</code> against the affected production database; resolved immediately by terminating the stuck holder session (<code>pg_terminate_backend</code>). Fixed by checking out one dedicated client for the whole lock→migrate→unlock window (<code>pdb.db.$client.connect()</code> — <code>$client</code> is drizzle-orm&#39;s own exposed raw <code>Pool</code>, confirmed via its <code>.d.ts</code>) instead of letting the pool route each statement independently; <code>migratePg</code>/<code>seedPlatformData</code> keep using the shared pool as before, since only the lock/unlock pair needs session affinity. The general lesson extends beyond this one call site: any session-scoped Postgres primitive (advisory locks, <code>SET search_path</code>/session GUCs meant to persist across statements, temp tables, <code>pg_backend_pid()</code>-keyed state) is unsafe to use against a <code>pg.Pool</code>-backed Drizzle client without explicitly pinning a single checked-out client for the whole sequence — a bare <code>db.execute()</code>/ <code>db.query()</code> call gives no such guarantee, silently, even though it looks identical to a normal one-shot query. Verifying the fix under CI-representative conditions (the full <code>.pg.test.ts</code> suite run together, matching CI&#39;s one shared Postgres <code>services:</code> container) surfaced two more, independent, pre-existing test-fixture bugs in the same area, fixed alongside it: <code>migrate.pg.test.ts</code> derived <code>migrationsTable</code> names by concatenating a fixed prefix onto a full 36-char UUID-based schema name, silently overflowing Postgres&#39;s 63-byte <code>NAMEDATALEN</code> identifier limit into truncated, colliding names once several tests ran concurrently; and <code>plugin-isolation-migration.pg.test.ts</code>&#39;s <code>afterEach</code> ran a blanket <code>DROP SCHEMA &quot;drizzle&quot; CASCADE</code> — the one fixed schema Postgres&#39;s migrator always tracks applied migrations in, regardless of any connection&#39;s <code>search_path</code> — which raced and deleted <em>other</em> concurrently-running test files&#39; own in-use migrations tables mid-run. Both are instances of the same underlying hazard <code>migrate.ts</code>&#39;s own doc comment already flagged for one describe block in that file (a blanket schema-wide <code>DROP</code>/<code>CASCADE</code> against the shared <code>drizzle</code> schema is never safe when other <code>.pg.test.ts</code> files run concurrently against it) but that had not been generalized to every file using that schema. Regression coverage: a new test independently probes the lock via <code>pg_try_advisory_lock</code> from a completely separate connection after <code>runMigrations()</code> returns, confirmed to fail against the pre-fix code by temporarily reverting the fix.</p></li><li><p><strong><code>checkAdminKey()</code> (<code>runtime/src/admin-guard.ts</code>, <code>apps/auth/src/admin-guard.ts</code>) compares the <code>SOVEREIGN_ADMIN_KEY</code> bearer token with <code>crypto.timingSafeEqual</code> on length-checked buffers, never plain <code>!==</code>.</strong> This is the sole authorization boundary for the entire <code>/api/admin/*</code> surface on both services — <code>runtime/proxy.ts</code>&#39;s matcher deliberately excludes this path (self-authenticated by design), so a plain string comparison is also a timing side-channel with nothing else standing in front of it. A dedicated per-IP rate limiter (<code>admin-rate-limit.ts</code>, one instance per service, duplicated rather than shared across the service boundary) backs the comparison: it counts only failed comparisons, never successful ones — <code>checkAdminKey</code> is the single call point behind every <code>/api/admin/*</code> route (e.g. Console&#39;s <code>adminFetch</code>, called with the correct key on every admin action), so counting successes too would throttle legitimate traffic instead of just repeated bad-key guesses. Once an IP trips the limiter, every request from it returns <code>429</code> until the window resets, including one presenting the correct key. <code>checkAdminKey()</code> is also the <em>only</em> thing these routes may trust: because the matcher skips this path, the <code>x-sovereign-user-id</code>/<code>-role</code> headers middleware injects elsewhere are never set <em>or stripped</em> here — a caller can forge <code>x-sovereign-user-role: platform:owner</code> with <code>curl -H</code>, and three routes were found doing exactly that (fixed in <code>0.94.15</code>). The one route that cannot carry an <code>Authorization</code> header (the email-template preview <code>&lt;iframe&gt;</code>) verifies the real session cookie in-route via <code>verifySession</code> instead.</p></li></ul><hr><h2 id="rules-promoted-from-the-claude-md-release-log-2026-09-06" tabindex="-1">Rules promoted from the CLAUDE.md release log (2026-09-06) <a class="header-anchor" href="#rules-promoted-from-the-claude-md-release-log-2026-09-06" aria-label="Permalink to &quot;Rules promoted from the CLAUDE.md release log (2026-09-06)&quot;">​</a></h2><p>These were each learned in a specific task and previously recorded only in CLAUDE.md&#39;s per-release narrative (now archived in <code>docs/task-history.md</code>). Each is a genuine, reusable constraint; the originating release is noted so the full story is one grep away.</p><ul><li><strong>A deletion handler severs cross-user attribution; it never deletes the row and never writes a sentinel id</strong> (RFC 0097). <code>sdk.portability.provideDelete</code> handlers are usually written as &quot;delete everything scoped by <code>owner_user_id</code>&quot;, which silently ignores a third class of row: one another user owns but the departing user is <em>attributed</em> on (<code>shopper_list_items.added_by</code>, <code>shopper_purchases.purchased_by</code>, <code>sovereign-tasks</code>&#39; <code>assignee_id</code>). Deleting it corrupts the owner&#39;s data; leaving it keeps a deleted account&#39;s id in the table. Set the attribution column to <code>null</code> and count those rows in <code>DeletionResult.anonymized</code>, never in <code>deleted</code> — a severed row still exists, and <code>deleted</code> is the number an operator cites to evidence an erasure. Attribution columns are declared nullable; ownership columns (<code>owner_user_id</code>, <code>user_id</code>) stay <code>NOT NULL</code> and their rows are deleted outright. A &quot;deleted user&quot; sentinel id was considered and rejected: it is invisible to the type system (every existing read site keeps compiling), must be excluded by hand from every filter and aggregate forever, and buys nothing at render time because <code>sdk.directory.resolveUsers()</code> returns nothing for it either way. The platform already answered this question the same way for <code>activity_log.actor_id</code> (nullable, paired with an <code>actor_type</code> discriminator). Relaxing an already-shipped <code>NOT NULL</code> column is a plain <code>drizzle-kit generate</code> in both dialects — the &quot;hand-author the migration&quot; rule covers <em>renames</em>, which drizzle-kit cannot do non-interactively, not nullability changes.</li><li><strong>A plugin declaring <code>data:import</code> must register <code>sdk.portability.provideImport()</code></strong>, and <code>data:export</code> must be paired with <code>provideExport()</code>. The restore path looks up handlers by plugin id at import time and <em>skips</em> a section with no handler (&quot;no import handler registered for this plugin&quot;) rather than failing, so a manifest can claim a permission the plugin never honours and a user&#39;s data silently fails to come back. Every first-party plugin that declares both permissions implements both handlers symmetrically. Found in <code>0.130.2</code> (Warden had declared <code>data:import</code> since epic 22.5 with no handler).</li><li><strong><code>runtime/proxy.ts</code> is Next&#39;s <code>proxy</code> convention (the renamed <code>middleware</code>, Next 16) and runs on the Node.js runtime — the Edge runtime is not supported there. Decision (epic task 0.29): it keeps the Edge discipline as a convention</strong> — no Node built-ins, no <code>ioredis</code>, no DB access, sessions verified from the signed cookie cache with <code>/api/verify</code> as the only network fallback. Reasons: the gate stays portable to an edge/CDN deployment (Next&#39;s own guidance for proxies), the offline verify path stays the hot path with no per-request DB round trip, and the regression suite (<code>runtime/src/__tests__/proxy-regression.test.ts</code>) assumes exactly these constraints. Relaxing any of them is a deliberate change with its own task, not a convenience. The historical rule follows. It cannot load <code>ioredis</code> (needs Node <code>net</code>/<code>tls</code>) or any Node built-in, and it avoids DB writes for the same reason. Moving it to Node.js Middleware is a platform-wide change with its own blast radius (paused task 2.29, RFC 0086 open question), not something to fold into another task.</li><li><strong>node-postgres returns <code>bigint</code> columns and <code>COUNT(*)</code> as JS strings</strong> from raw queries unless a global type parser is registered. <code>row.started === now</code> comparisons silently fail; <code>Number()</code> before comparing. Bit twice: HMAC rotation (<code>0.102.0</code>) and unread-notification counts (<code>0.120.0</code>).</li><li><strong>Never fork a <em>navigation</em> decision by viewport.</strong> <code>ResponsiveSurface</code>&#39;s <code>useIsMobile()</code> returns <code>false</code> on first paint and corrects in a <code>useEffect</code>; React runs child effects before parent effects in the same commit, so a redirect inside the &quot;desktop&quot; branch fires on real phones too, before the corrective effect runs. Fork only what <em>renders</em> — Console&#39;s bare <code>/console</code> and Account&#39;s bare <code>/account</code> both work this way. <code>0.119.0</code>.</li><li><strong>A React element produced by a Server Component and stored into a Client Component&#39;s state loses its <code>key</code>.</strong> It crosses the RSC boundary as an opaque Flight reference object with no <code>.key</code>, so a downstream <code>Children.toArray</code>/reconciliation sees a stable positional key and updates the existing fiber in place — <code>defaultValue</code>-seeded inputs stay frozen on the first selection. Thread a plain string key prop through props/context and apply it as <code>key</code> on a <code>Fragment</code> the client creates itself. <code>0.114.2</code>.</li><li><strong>A spawned <code>git</code> never inherits a repository from the environment.</strong> Git exports <code>GIT_DIR</code> (and can export <code>GIT_WORK_TREE</code>, <code>GIT_INDEX_FILE</code>, …) to the processes it spawns — hooks above all: a <code>pre-push</code> hook run from a linked worktree gets <code>GIT_DIR=&lt;main&gt;/.git/worktrees/&lt;name&gt;</code>. Code that addresses a repository by <code>cwd</code> on a fresh temp directory — the backup push in <code>runtime/src/git-backup.ts</code>, the pinned-ref clone in <code>scripts/install-plugins.ts</code> — then silently operates on the <em>inherited</em> repository: <code>git init</code> re-initializes it, <code>add</code>/<code>commit</code>/<code>tag</code> land there with the temp dir as the work tree. Found the hard way: the test suite, run by the pre-push hook from a worktree, committed its backup fixtures onto the branch being pushed and flipped the shared config to <code>core.bare = true</code>. Build the child environment with <code>gitChildEnv()</code> (strips <code>GIT_REPOSITORY_ENV_VARS</code>), in tests too — the regression test in <code>git-backup.test.ts</code> sets <code>GIT_DIR</code> to a throwaway repo and asserts it stays empty.</li><li><strong>Credentials for a spawned <code>git</code> go through the environment.</strong> <code>GIT_ASKPASS</code> script for HTTPS tokens, <code>GIT_SSH_COMMAND</code> with an <code>IdentityFile</code> written to a <code>0600</code> temp file and removed in <code>finally</code> for SSH keys — never argv, never an embedded URL, both of which are visible to every user on the host via <code>ps</code>. <code>runtime/src/git-backup.ts</code> is the reference implementation. <code>0.124.0</code>.</li><li><strong><code>packages/ui</code> components are framework-agnostic.</strong> No <code>next/navigation</code> or <code>next/link</code> imports; active state comes from the consumer (<code>item.active</code>) and links via an optional <code>renderLink</code>. <code>NavTabs</code> and <code>NavList</code> both follow this; an epic task draft assuming <code>NavList</code> would call <code>usePathname()</code> internally was corrected before it shipped. <code>0.105.0</code>.</li><li><strong><code>drizzle-kit generate</code> cannot run non-interactively for a rename.</strong> A table or column rename triggers its own TTY prompt (&quot;renamed or created new?&quot;). Hand-author the migration for both dialects in drizzle-kit&#39;s own output format, then verify two ways: <code>drizzle-kit check</code> (snapshot/journal consistency) and a follow-up <code>drizzle-kit generate</code> that reports &quot;No schema changes&quot;. <code>0.103.0</code>.</li><li><strong>Third-party plugin dependencies are never committed to <code>runtime/package.json</code> or <code>runtime/generated/plugin-deps.json</code>.</strong><code>sovereign.plugins.json</code> is gitignored per checkout, and hoisting (RFC 0057, <code>bin/plugin-deps.ts</code>) runs on the operator&#39;s own checkout via <code>sv plugin add</code>, <code>scripts/install-plugins.ts</code>, and <code>pnpm dev</code>&#39;s <code>.local</code> sync. A first fix attempt that committed travellog&#39;s deps directly was reverted as exactly the anti-pattern the mechanism exists to avoid. <code>0.101.7</code>.</li><li><strong><code>sdk.*</code> permission checks reject a missing plugin id.</strong> A <code>getPluginId(headers) ?? &#39;unknown&#39;</code> fallback laundered a missing identity into a no-op check in <code>sdk.notifications.send()</code>; the check must fail closed. <code>0.120.0</code>.</li><li><strong>Every outbound <code>fetch</code> on a server render path has a timeout, and a route that blocks on network has a <code>loading.tsx</code>.</strong> An unbounded health probe left <code>/warden</code> rendering nothing for the duration of a stalled upstream; with a 3s timeout plus <code>loading.tsx</code>, the first HTML chunk arrived in 7ms while the slow discovery still took 8s in the background. <code>0.112.1</code>.</li><li><strong><code>router.push()</code> to the route&#39;s own current URL does not refetch server data</strong> — use <code>router.refresh()</code>. And a callback passed as a <code>useEffect</code> dependency must be memoized (<code>useCallback</code>), or a parent re-render re-fires the effect; guard success effects with a ref keyed on the state object so a forgotten memoization upstream can&#39;t replay a toast. <code>0.112.4</code>–<code>0.112.5</code>.</li><li><strong>Overriding a DS component&#39;s own module class needs higher specificity.</strong><code>.cardChevron { display: none }</code> vs <code>Icon.module.css</code>&#39;s <code>.root { display: inline-block }</code> is a tie decided by Next.js&#39;s per-route CSS bundle order, which differed between <code>/console/groups</code> and <code>/console/oauth-clients</code>. Qualify the selector (<code>svg.cardChevron</code>). <code>0.111.0</code>.</li><li><strong>A composer-style surface keeps a stable element tree across state transitions.</strong> Rendering the composer inside a wrapper that only exists in the empty state remounted the whole subtree — including the incognito toggle — every time the empty/non-empty boundary was crossed. Keep the siblings permanent and swap a modifier class. <code>0.110.0</code>.</li><li><strong>citty <code>defineCommand</code> args have no repeatable type</strong> — it delegates to <code>node:util</code>&#39;s <code>parseArgs</code> with <code>multiple</code> never set, so a repeated flag silently keeps the last value. Read <code>rawArgs</code> for a flag that may repeat (<code>--exclude-plugin</code>). <code>0.128.0</code>.</li><li><strong>A better-auth version bump is verified against a populated auth store, never only a fresh one, and never merged from a grouped Dependabot PR.</strong> better-auth&#39;s migrator (<code>dist/db/get-migration.mjs</code>) refuses to add a required column with no default to a table that has rows — it throws <code>UnsafeMigrationError</code>, which <code>apps/auth/src/migrate.ts</code>&#39;s <code>runAuthMigrations()</code> propagates, so the auth server fails to boot on every real instance while a fresh CI database passes. better-auth 1.7.0–1.7.2 did exactly this with a required <code>account.issuer</code> column (and keyed credential sign-in on <code>issuer === &#39;local:credential&#39;</code>), then 1.7.3 removed it again per better-auth&#39;s own 1.7 upgrade guide; <code>dependabot.yml</code> ignores that window and the three packages have their own Dependabot group. Two genuine <code>session_data</code> cookies captured from real 1.6.25 and 1.7.2 instances (<code>runtime/src/__tests__/fixtures/</code>) are permanent regression fixtures for the compact cookie-cache format <code>runtime/proxy.ts</code> verifies offline — a hand-forged partial record is not a valid fixture (1.7 validates the payload against better-auth&#39;s own record schemas and rejects it). Epic task 1.26 holds the re-entry checklist.</li><li><strong><code>vitest.config.ts</code> aliases every <code>next/*</code> import to <code>runtime/node_modules/next</code>, and that alias is load-bearing.</strong> In a real build Next&#39;s own webpack config aliases <code>next</code> to the running package, so <code>packages/sdk</code>&#39;s <code>import { headers } from &#39;next/headers&#39;</code> and the app share one module and one request-scope <code>AsyncLocalStorage</code>. Vitest has no such alias by default: each importer resolves <code>next</code> through its own <code>node_modules</code> symlink, and a pnpm lockfile that has split <code>next</code> into several peer-suffix variants (the routine Dependabot batch #642 did, on an <code>@types/node</code> 22-vs-26 suffix the tree already carried) hands runtime test files one physical <code>next/headers</code> and <code>packages/sdk</code> another. <code>vi.mock(&#39;next/headers&#39;)</code> patches the test file&#39;s copy; the SDK&#39;s real <code>headers()</code> runs outside any request scope, throws Next&#39;s E251, the <code>catch</code> treats it as &quot;no request&quot;, and every <code>sdk.db</code>/<code>sdk.storage</code>/<code>sdk.env</code> background-fallback test fails with &quot;expected &#39;platform-db-marker&#39; to be &#39;plugin-db-marker&#39;&quot; — while a single-variant lockfile passes by accident. Reproduced by rebuilding #642&#39;s exact tree (base commit + diff, <code>pnpm install --frozen-lockfile</code>): 8 of 18 routing tests failed, all 18 passed with only the alias added. The alias makes the mock target the same module by construction, regardless of how the lockfile resolves.</li><li><strong>Test hygiene learned the hard way:</strong> await every async operation a test starts (an un-awaited jsdom <code>FileReader</code> completion lands as an unhandled exception after the test returns and fails the whole run, <code>0.94.14</code>); scope a long <code>testTimeout</code> to the one slow test (<code>0.94.13</code>); a <code>.pg.test.ts</code> using an isolated schema must mock modules that resolve the real <code>getPlatformDb()</code> singleton internally — <code>sendPlatformEmail</code>, <code>fanOutPushToUser</code> (<code>0.120.0</code>, <code>0.123.0</code>); keep Postgres identifiers under 63 bytes or they truncate silently and collide (<code>0.101.9</code>).</li></ul>',8)])])}const g=o(i,[["render",d]]);export{p as __pageData,g as default};
