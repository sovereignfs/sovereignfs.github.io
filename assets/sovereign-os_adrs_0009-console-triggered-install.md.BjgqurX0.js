import{_ as t,o as a,c as o,a2 as i}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"ADR-0009: Console-Triggered Update Install","description":"","frontmatter":{},"headers":[],"relativePath":"sovereign-os/adrs/0009-console-triggered-install.md","filePath":"sovereign-os/adrs/0009-console-triggered-install.md"}'),s={name:"sovereign-os/adrs/0009-console-triggered-install.md"};function n(r,e,c,l,d,h){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="adr-0009-console-triggered-update-install" tabindex="-1">ADR-0009: Console-Triggered Update Install <a class="header-anchor" href="#adr-0009-console-triggered-update-install" aria-label="Permalink to &quot;ADR-0009: Console-Triggered Update Install&quot;">​</a></h1><p><strong>Status:</strong> Accepted <strong>Date:</strong> 2026-08-01 <strong>Decision owner:</strong> Project creator (delegated to the assistant for this decision, same as ADR-0008 — see Decision below) <strong>Related ADRs:</strong> <a href="./0007-console-authentication.html">ADR-0007</a>, <a href="./0008-console-privileged-action-invocation.html">ADR-0008</a><strong>Related RFCs:</strong> <a href="./../rfcs/0014-appliance-update-system.html">RFC-0014</a>, <a href="./../rfcs/0015-update-discovery.html">RFC-0015</a><strong>Related milestone:</strong> <a href="./../../ROADMAP.html">Update Discovery and Console Controls</a><strong>Supersedes:</strong> None</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR-0008 wired the first Console-triggered privileged action — <code>sovereign-update check</code> — through a deliberately minimal mechanism: an empty trigger file whose mere existence, not content, is the only signal a root-owned <code>systemd</code> path unit trusts. It explicitly scoped out anything bigger, and named exactly why in its own Revisit Conditions: an action that takes a parameter, has a blast radius beyond a read-only check, or needs granular progress reporting should not just reuse that same file-existence trigger.</p><p>Installing an update — <code>prepare</code> → <code>backup</code> → <code>stage</code> → <code>activate</code> — hits all three:</p><ul><li><strong>Parameter-shaped</strong>, naively: which version to install.</li><li><strong>Real blast radius</strong>: Pi-hole is quiesced during backup, the whole device briefly loses DNS during activation, and a failed step can leave a transaction in <code>recovery_required</code> needing operator attention.</li><li><strong>Genuinely long-running and multi-stage</strong>: unlike <code>check</code> (seconds, one outcome), an install runs through several durable transaction states over what could be minutes, downloading a multi-ten-megabyte bundle along the way.</li></ul><p>RFC-0015 already named the target policy: <strong>notify and require approval</strong>, never automatic download-and-install. This ADR is about what &quot;require approval&quot; concretely means for Console specifically.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Reuse ADR-0008&#39;s exact mechanism (file-existence trigger + <code>systemd</code> path-activated root oneshot) for the <em>trigger</em>, but resolve the &quot;takes a parameter&quot; problem by giving it none</strong> — the privileged side independently re-decides which release to install using the same trust-anchored discovery <code>check</code> already performs, never trusting anything Console sends beyond &quot;the user asked to install.&quot; This decision was delegated to the assistant by the project owner, the same as ADR-0008 — recorded here for provenance, not reviewed line-by-line before acceptance.</p><ul><li>A new <code>sovereign-update install</code> subcommand does the actual work, all inside the same root-owned, already-trusted binary: re-runs the <em>exact</em> discovery and signature-verification logic <code>check</code> already uses (<code>fetch_release_candidates</code>/<code>evaluate_release_candidate</code>, unchanged), then — only once a candidate re-verifies — downloads its update bundle, and finally calls the <em>existing</em> <code>prepare_update</code>/<code>create_backup</code>/ <code>stage_release</code>/<code>activate_release</code> functions in sequence, the same code every manual, already-hardware-qualified install has always run. No new trust logic, no new state machine: this command is an orchestrator over already-qualified primitives, not a new way of installing anything.</li><li><code>console-auth</code> gets <code>POST /api/v1/console/actions/install</code>, writing <code>/data/sovereign/console/actions/install.request</code> — same file-existence signal, same shape as <code>check</code>&#39;s trigger, different file and a different <code>.path</code>/<code>.service</code> pair (<code>sovereign-console-install-trigger.*</code>).</li><li><strong>Unlike <code>check</code>, this endpoint requires the password again</strong>, not just a valid session — re-verified against the stored credential exactly like login does, discarded immediately after (never stored, never becomes part of the session). This is ADR-0007&#39;s own already-recorded guidance acted on for the first time: <em>&quot;any mutating action (<code>activate</code>, <code>restore</code>, <code>rotate-trust</code>) is a reasonable point to require fresh confirmation regardless of session freshness.&quot;</em> A stolen or left-open browser session alone is not enough to trigger a real install.</li><li>Only proceeds if <code>/api/v1/update/check</code>&#39;s last result says <code>update_available</code> — Console cannot force-install against a device that hasn&#39;t itself already found and verified a candidate; it can only ask the device to do what it already told the user was available.</li><li><strong>Progress reporting reuses existing state, not new plumbing</strong>: <code>prepare</code>/<code>backup</code>/<code>stage</code>/<code>activate</code> already write <code>/data/sovereign/update-status.json</code> at every transition (RFC-0014). <code>console-health</code> now also serves that file&#39;s content, unauthenticated (<code>GET /api/v1/update/status</code>), the same non-sensitive-status reasoning as <code>/api/v1/update/check</code>. Console polls this to show real progress instead of a single before/after result.</li><li><strong>No per-step Console control.</strong> The install runs <code>prepare</code> → <code>backup</code> → <code>stage</code> → <code>activate</code> as one continuous, non-interactive sequence once triggered — not four separate Console-driven steps. <code>activate</code>&#39;s existing health gate and automatic rollback remain the only safety net mid-sequence, exactly as they are for a manually-run install today. This matches ordinary consumer-OS &quot;install update&quot; UX (one click, one outcome) rather than exposing the transaction state machine as something Console operates directly.</li></ul><h2 id="what-this-actually-protects" tabindex="-1">What this actually protects <a class="header-anchor" href="#what-this-actually-protects" aria-label="Permalink to &quot;What this actually protects&quot;">​</a></h2><p>The same shape of question ADR-0007 and ADR-0008 both asked: not a secret, but who&#39;s allowed to cause state change, and how much state change one click can cause. <code>check</code>&#39;s worst case was an extra GitHub API call. This action&#39;s worst case is real: Pi-hole downtime during activation, and — if a step fails outside <code>activate</code>&#39;s own health gate — a transaction stuck needing an operator&#39;s SSH session to resolve. The fresh-password requirement and the &quot;only if already discovered&quot; guard both exist specifically to raise the bar above &quot;a valid cookie,&quot; given that higher real cost.</p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><h3 id="option-a-—-extend-adr-0008-s-trigger-mechanism-unchanged-with-a-version-parameter-in-the-trigger-file-rejected" tabindex="-1">Option A — Extend ADR-0008&#39;s trigger mechanism unchanged, with a version parameter in the trigger file (rejected) <a class="header-anchor" href="#option-a-—-extend-adr-0008-s-trigger-mechanism-unchanged-with-a-version-parameter-in-the-trigger-file-rejected" aria-label="Permalink to &quot;Option A — Extend ADR-0008&#39;s trigger mechanism unchanged, with a version parameter in the trigger file (rejected)&quot;">​</a></h3><p>Write the target version into the trigger file instead of leaving it empty; the root-owned runner reads and acts on it.</p><ul><li><strong>Rejected:</strong> the entire point of the file-existence-only design in ADR-0008 was that the trigger&#39;s <em>content</em> is never trusted — the moment content is trusted, <code>console-auth</code> (unprivileged, parsing untrusted HTTP input) becomes able to influence <em>which</em> root action happens, not just <em>whether</em> one happens. Any bug in that parsing path becomes a privilege-relevant bug. Not worth it when the alternative (the privileged side re-decides the target itself, from the same trust-anchored source <code>check</code> already uses) has no such risk and costs nothing extra.</li></ul><h3 id="option-b-—-a-real-request-response-protocol-socket-or-queue-per-adr-0008-s-own-revisit-conditions" tabindex="-1">Option B — A real request/response protocol (socket or queue), per ADR-0008&#39;s own Revisit Conditions <a class="header-anchor" href="#option-b-—-a-real-request-response-protocol-socket-or-queue-per-adr-0008-s-own-revisit-conditions" aria-label="Permalink to &quot;Option B — A real request/response protocol (socket or queue), per ADR-0008&#39;s own Revisit Conditions&quot;">​</a></h3><p>Build the general request-protocol mechanism ADR-0008 flagged as the right answer once an action needs real parameters or granular status.</p><ul><li>The conceptually &quot;correct&quot; next step ADR-0008 explicitly anticipated.</li><li><strong>Not chosen for this specific action</strong>, because it turns out this action doesn&#39;t actually need a parameter once framed correctly (see Decision) — the file-existence trigger still works, it&#39;s just paired with existing-file-based status reporting instead of a live status channel. Worth building for a future action that genuinely can&#39;t avoid taking a parameter (e.g., restoring one specific backup among several).</li></ul><h3 id="option-c-—-console-orchestrates-each-step-itself-separate-triggers-for-prepare-backup-stage-activate" tabindex="-1">Option C — Console orchestrates each step itself (separate triggers for prepare/backup/stage/activate) <a class="header-anchor" href="#option-c-—-console-orchestrates-each-step-itself-separate-triggers-for-prepare-backup-stage-activate" aria-label="Permalink to &quot;Option C — Console orchestrates each step itself (separate triggers for prepare/backup/stage/activate)&quot;">​</a></h3><p>Expose four separate trigger actions; Console (or its user) confirms each one in sequence.</p><ul><li>Gives the most visibility and the most points to abort.</li><li><strong>Rejected</strong> as the wrong default UX for this project&#39;s stated policy (RFC-0015: notify and require approval — one approval, not four) and disproportionate complexity for a first pass; the four-step machinery already exists for CLI/operator use and remains available there for anyone who wants that granularity.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>No new trust logic anywhere: <code>install</code> is a thin orchestrator over already-hardware-qualified functions, verifying with the exact same code path <code>check</code> and every manual install this whole project has ever used.</li><li>Closes the last named gap from ADR-0007&#39;s original scope note (&quot;how the ask reaches root&quot;) for the one action that actually matters to a household user: getting an update installed without SSH.</li><li>The fresh-password requirement and discovery-gate (&quot;must already say <code>update_available</code>&quot;) are both free — no new mechanism, just reusing what already exists (login verification, <code>check</code>&#39;s own result file).</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li><code>sovereign-update install</code> is real new code — a network download of a large artifact from inside a command that previously never fetched more than a small manifest/signature, and a new orchestration path through four existing functions that have only ever been called individually, by an operator, with time to react between each. That sequencing itself needs its own hardware qualification, not just each step in isolation.</li><li>A failed install still leaves an operator-recovery case (<code>recovery_required</code>) that Console cannot resolve — &quot;retain a CLI recovery path independent of Console&quot; (already a stated ROADMAP requirement) remains load-bearing, not optional, once this ships.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li>Running <code>prepare</code>→<code>backup</code>→<code>stage</code>→<code>activate</code> back-to-back with no human in the loop between steps is qualitatively different from every prior qualification campaign this session, which always ran them one command at a time with a person watching. The full sequence needs its own explicit hardware qualification pass, not an assumption that &quot;each step already works&quot; implies the sequence does.</li><li>A large, previously-untested download path (the update bundle, tens of megabytes, over a live network connection from inside <code>sovereign-update</code> itself for the first time) is new attack surface for resource exhaustion or a stalled/partial download — needs explicit size and timeout bounds, not just reuse of the small-asset <code>http_get_bytes</code> limits <code>check</code> uses today.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><h3 id="wait-for-adr-0008-s-option-b-a-real-protocol-before-doing-anything" tabindex="-1">Wait for ADR-0008&#39;s Option B (a real protocol) before doing anything <a class="header-anchor" href="#wait-for-adr-0008-s-option-b-a-real-protocol-before-doing-anything" aria-label="Permalink to &quot;Wait for ADR-0008&#39;s Option B (a real protocol) before doing anything&quot;">​</a></h3><p>Rejected — as Decision explains, the version-parameter problem that would have required it dissolves once the privileged side is designed to re-decide the target itself. Building request/response machinery now would be solving a problem this specific action doesn&#39;t actually have.</p><h2 id="validation-and-revisit-conditions" tabindex="-1">Validation and Revisit Conditions <a class="header-anchor" href="#validation-and-revisit-conditions" aria-label="Permalink to &quot;Validation and Revisit Conditions&quot;">​</a></h2><p>Revisit this ADR if: a future action genuinely cannot avoid taking a Console-supplied parameter (e.g., &quot;restore backup <code>X</code>&quot; where <code>X</code> must be one of several real choices, not &quot;whatever&#39;s already verified&quot;) — that needs Option B. Also revisit if hardware qualification of the full <code>prepare</code>→<code>activate</code> sequence surfaces a failure mode specific to running it unattended (no operator watching between steps) that the individually-qualified steps never exercised.</p>',34)])])}const g=t(s,[["render",n]]);export{p as __pageData,g as default};
