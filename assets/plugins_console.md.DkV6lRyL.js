import{_ as t,o as s,c as a,a2 as n}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Console Plugin","description":"","frontmatter":{},"headers":[],"relativePath":"plugins/console.md","filePath":"plugins/console.md"}'),i={name:"plugins/console.md"};function o(l,e,d,r,c,h){return s(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="console-plugin" tabindex="-1">Console Plugin <a class="header-anchor" href="#console-plugin" aria-label="Permalink to &quot;Console Plugin&quot;">​</a></h1><p><strong>Version:</strong> 0.1<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Purpose:</strong> Canonical specification for the Console core plugin — the single source of truth for its manifest, access model, data model, and build plan.<br><strong>Status:</strong> Draft</p><hr><p><strong>Console</strong> is Sovereign&#39;s core administration plugin — the admin surface for user management, plugin management, tenant configuration, and system health. It ships in the monorepo (<code>plugins/console/</code>) and is the canonical reference for a <code>type: platform</code> plugin.</p><p>This document is the <strong>single source of truth for the Console plugin&#39;s spec</strong>. It consolidates and refines what the proposal/SRS (<code>docs/sovereign-proposal-plan-srs.md</code>) and the implementation tasks (<code>ROADMAP.md</code>) define, and resolves the gaps those documents leave open. Where this doc and the SRS disagree on Console-specific detail, this doc wins; the SRS remains authoritative for platform-wide concerns (roles, capabilities, manifest schema).</p><h2 id="contents" tabindex="-1">Contents <a class="header-anchor" href="#contents" aria-label="Permalink to &quot;Contents&quot;">​</a></h2><ul><li><a href="#what-console-is">What Console is</a></li><li><a href="#identity-and-manifest">Identity and manifest</a></li><li><a href="#access-control">Access control</a></li><li><a href="#functional-requirements">Functional requirements</a></li><li><a href="#directory-structure">Directory structure</a></li><li><a href="#data-model">Data model</a></li><li><a href="#sdk-dependencies">SDK dependencies</a></li><li><a href="#ui">UI</a></li><li><a href="#build-plan">Build plan</a></li><li><a href="#open-questions">Open questions</a></li><li><a href="#changelog">Changelog</a></li></ul><hr><h2 id="what-console-is" tabindex="-1">What Console is <a class="header-anchor" href="#what-console-is" aria-label="Permalink to &quot;What Console is&quot;">​</a></h2><p>A first-class plugin, not a special-cased admin app. The platform decision (decision log, Jun 2026) is deliberate:</p><blockquote><p><strong>Console as core plugin, not a separate app</strong> — consistent with the plugin architecture, avoids running a third Next.js process, admin scope enforced by middleware.</p></blockquote><p>This means Console proves the plugin contract end to end: it is installed, registered, routed, and access-gated through exactly the same machinery a third-party plugin uses. Anything Console needs that the SDK cannot provide is a gap in the platform, not a reason to special-case Console.</p><p>Console owns <strong>no domain data of its own</strong> in v1. It is a read/write <em>view</em> over platform-level tables (users, plugin status, tenant config) exposed through the SDK. Its own <code>db/schema.ts</code> is empty.</p><h2 id="identity-and-manifest" tabindex="-1">Identity and manifest <a class="header-anchor" href="#identity-and-manifest" aria-label="Permalink to &quot;Identity and manifest&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>fs.sovereign.console</code></td></tr><tr><td><code>name</code></td><td><code>Console</code></td></tr><tr><td><code>type</code></td><td><code>platform</code></td></tr><tr><td><code>runtime</code></td><td><code>native</code></td></tr><tr><td><code>routePrefix</code></td><td><code>/console</code></td></tr><tr><td><code>shell</code></td><td><code>overlay</code></td></tr><tr><td><code>shellConfig</code></td><td><code>{ overlaySize: &quot;lg&quot; }</code></td></tr><tr><td><code>adminOnly</code></td><td><code>true</code></td></tr><tr><td><code>icon</code></td><td><code>icon.svg</code></td></tr><tr><td><code>database</code></td><td><code>shared</code> (omitted — <code>shared</code> is the default)</td></tr><tr><td><code>permissions</code></td><td><code>auth:session</code>, <code>db:readWrite</code>, <code>mailer:send</code></td></tr></tbody></table><p>Proposed <code>manifest.json</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;schemaVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;fs.sovereign.console&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Console&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Platform administration: users, plugins, settings, and system health.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;platform&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;runtime&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;native&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;routePrefix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/console&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;shell&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;overlay&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;shellConfig&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;overlaySize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;adminOnly&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;icon&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;icon.svg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auth:session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;db:readWrite&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mailer:send&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;compatibility&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;minPlatformVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.4.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Notes:</p><ul><li><code>adminOnly: true</code> is the canonical way to require admin. The middleware maps it to the <code>console:access</code> capability check. Do <strong>not</strong> also list <code>admin:*</code> in <code>permissions</code> — that union member exists for future fine-grained plugin-level admin scopes, and <code>adminOnly</code> already covers Console&#39;s need (SRS §5).</li><li><code>repository</code> is omitted: it is required only for <code>sovereign</code>/<code>community</code> types.</li><li><code>database</code> is omitted because <code>shared</code> is the default and Console declares no tables.</li><li>No <code>minPlatformVersion</code> below <code>0.4.0</code> — Console <em>is</em> the v0.4 milestone, so it cannot target an earlier platform.</li><li><code>icon.svg</code> is what the shell renders in the <strong>sidebar bottom section</strong> (and the mobile footer launcher), visible to <code>platform:admin</code> only (PLT-11). Console is shell chrome — it never appears in the sidebar middle section or the Launcher grid (LCH-04).</li><li><code>shell: overlay</code> (RFC 0001): clicking the ⚙ opens Console as a dialog over the current page; a hard load of <code>/console</code> renders the full-page fallback. The <code>adminOnly</code> 403 gate applies identically in both modes (it is enforced by URL prefix in the middleware, before presentation). <code>shellConfig.overlaySize: &quot;lg&quot;</code> sizes the dialog (fixed box, fills the viewport minus a margin); Console&#39;s section tabs navigate with <code>replace</code> so a single dismiss closes the dialog.</li></ul><h2 id="access-control" tabindex="-1">Access control <a class="header-anchor" href="#access-control" aria-label="Permalink to &quot;Access control&quot;">​</a></h2><p>Console is admin-only. The full chain, per SRS §4.1 and PLT-03:</p><ul><li>Role <code>platform:admin</code> carries the <code>console:access</code> capability; <code>platform:user</code> does not.</li><li><code>adminOnly: true</code> in the manifest → middleware requires <code>console:access</code>.</li><li><strong>Unauthenticated</strong> request to a <code>/console</code> route → redirect to login (PLT-02).</li><li><strong>Authenticated non-admin</strong> → <strong>403</strong> (PLT-03).</li><li>Console appears in the launcher <strong>only</strong> for users who hold <code>console:access</code>.</li></ul><p>The first user to register on a fresh instance is auto-assigned <code>platform:admin</code> (AUTH-08). Every subsequent promotion to admin happens through Console itself (CON-05) — so the bootstrap admin is the platform&#39;s root of trust for the admin role.</p><h2 id="functional-requirements" tabindex="-1">Functional requirements <a class="header-anchor" href="#functional-requirements" aria-label="Permalink to &quot;Functional requirements&quot;">​</a></h2><p>Mirrors SRS §4.4. These are the acceptance surface for the plugin.</p><table tabindex="0"><thead><tr><th>ID</th><th>Requirement</th></tr></thead><tbody><tr><td>CON-01</td><td>Accessible only to <code>platform:admin</code> (<code>console:access</code>).</td></tr><tr><td>CON-02</td><td>View all registered users with role, status, and join date.</td></tr><tr><td>CON-03</td><td>Invite a new user by email — generate an invite token, send the invite email.</td></tr><tr><td>CON-04</td><td>Deactivate and reactivate user accounts.</td></tr><tr><td>CON-05</td><td>Change a user&#39;s role between <code>platform:admin</code> and <code>platform:user</code>.</td></tr><tr><td>CON-06</td><td>View all installed plugins with version and enabled/disabled status.</td></tr><tr><td>CON-07</td><td>Enable or disable a plugin — disabling hides it from the launcher and blocks its routes.</td></tr><tr><td>CON-08</td><td>Configure tenant settings: tenant name.</td></tr><tr><td>CON-09</td><td>Display a system health summary: runtime version, DB type + connection status, auth status, disk usage.</td></tr><tr><td>CON-10</td><td>Toggle invite-only registration from Console without editing environment config.</td></tr><tr><td>CON-11</td><td>View the current root plugin and change it to any installed, enabled, non-adminOnly plugin. Change takes effect immediately without restart. The root plugin serves the platform root <code>/</code>; the sidebar&#39;s first icon resolves to it.</td></tr><tr><td>CON-12</td><td>Show or hide the bundled example plugins (manifest <code>example: true</code>) instance-wide from a Settings → Example plugins toggle, persisted in <code>platform_settings</code> and overriding the <code>SOVEREIGN_EXAMPLES_ENABLED</code> env default (no restart). A per-plugin toggle (CON-07) still overrides the instance default for that one example. Examples ship hidden by default (fresh instances create no <code>plugin_status</code> row for them — see &quot;Resolved decisions&quot; below) from the Launcher, sidebar, <strong>and</strong> the Plugins page table (CON-14): the table&#39;s &quot;Show examples&quot; filter defaults to the resolved instance setting, not hard-coded on, so a bulk-disabled example without an individual override is hidden there too until the admin either flips the bulk toggle or ticks the filter to reveal-and-manage it individually.</td></tr><tr><td>CON-13</td><td>Set a per-plugin access policy governing who may <strong>open</strong> the plugin&#39;s app — Everyone, Admins and owners, Selected users, Selected groups, or Disabled — from an Access dialog on the Plugins page, independent of CON-07&#39;s enable/disable state and of <code>console:access</code> (which governs who can manage plugins in Console, not who can open them). Selected-user/group policies support a self-service toggle (requires <code>plugins:self-manage</code>, RFC 0070) and warn when no user/group is yet granted. Policy and grant changes are audited.</td></tr><tr><td>CON-14</td><td>List every cataloged plugin — active or not — in a single table (no separate catalog/installed/examples sections), with keyword search (name/id/description) and a status filter (Inactive/Enabled/Disabled/Incompatible). Row actions are contextual to status: <code>Activate</code> for inactive, the existing toggle/Access/Open/Remove for active. Chrome plugins (Account/Console/Launcher) never show Access — access policy is a permanent no-op for them.</td></tr><tr><td>CON-15</td><td>Flag a plugin as still under active development (manifest <code>development: true</code>) with an amber &quot;in development&quot; badge next to its type badge on the Plugins page. Purely informational, like CON-12&#39;s <code>example</code> classification — no effect on routing, access policy, or the enable/disable default. The same flag also badges the plugin&#39;s Launcher tile (LCH-09).</td></tr><tr><td>CON-16</td><td><code>SOVEREIGN_HIDE_DEVELOPMENT_PLUGINS</code> (env, no Console setting) hard-hides every <code>development: true</code> plugin instance-wide — routes 404, no sidebar/Launcher entry — with <strong>no per-plugin override</strong>: unlike CON-12&#39;s example toggle, an explicit enable on this page does not undo it. The plugin still lists on this page (like any other plugin, per CON-06) so an admin can see it&#39;s flagged, but its <code>enabled</code> toggle is inert while the env var is set. See <code>docs/self-hosting.md</code>.</td></tr></tbody></table><h2 id="directory-structure" tabindex="-1">Directory structure <a class="header-anchor" href="#directory-structure" aria-label="Permalink to &quot;Directory structure&quot;">​</a></h2><p>Follows the standard plugin internal structure (SRS §2.3). Console adds no <code>migrations/</code>, <code>components/business-logic</code>, or <code>db</code> tables of its own in v1.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/console/</span></span>
<span class="line"><span>├── manifest.json            # identity, permissions, routing (above)</span></span>
<span class="line"><span>├── icon.svg                 # Console icon — rendered in the sidebar bottom section</span></span>
<span class="line"><span>├── app/                     # Next.js route segment, mounted at /console</span></span>
<span class="line"><span>│   ├── layout.tsx           # console shell layout (nav between sub-sections)</span></span>
<span class="line"><span>│   ├── page.tsx             # home — links to users / plugins / settings / health</span></span>
<span class="line"><span>│   ├── users/</span></span>
<span class="line"><span>│   │   ├── page.tsx         # paginated user list (CON-02)</span></span>
<span class="line"><span>│   │   └── invite/</span></span>
<span class="line"><span>│   │       └── page.tsx     # invite form (CON-03)</span></span>
<span class="line"><span>│   ├── plugins/</span></span>
<span class="line"><span>│   │   └── page.tsx         # unified filterable plugin table (CON-06, CON-07, CON-14)</span></span>
<span class="line"><span>│   ├── settings/</span></span>
<span class="line"><span>│   │   └── page.tsx         # tenant name, invite-only, example plugins, root plugin (CON-08, CON-10, CON-11, CON-12)</span></span>
<span class="line"><span>│   └── health/</span></span>
<span class="line"><span>│       └── page.tsx         # system health dashboard (CON-09)</span></span>
<span class="line"><span>├── db/</span></span>
<span class="line"><span>│   └── schema.ts            # intentionally empty — Console owns no tables in v1</span></span>
<span class="line"><span>└── package.json</span></span></code></pre></div><p>Mutations (role change, deactivate/reactivate, enable/disable, settings writes) are implemented as <strong>Next.js server actions</strong>, not REST handlers, consistent with the implementation tasks.</p><h2 id="data-model" tabindex="-1">Data model <a class="header-anchor" href="#data-model" aria-label="Permalink to &quot;Data model&quot;">​</a></h2><p>Console reads and writes <strong>platform tables</strong>, never its own. The tables it touches:</p><table tabindex="0"><thead><tr><th>Table</th><th>Owner</th><th>Console use</th></tr></thead><tbody><tr><td>users (auth)</td><td>better-auth / <code>packages/db</code></td><td>List, role, status, join date (CON-02/04/05).</td></tr><tr><td><code>plugin_status</code></td><td>platform (<code>packages/db</code>)</td><td>Per-plugin enabled/disabled flag (CON-06/07).</td></tr><tr><td><code>tenants</code></td><td>platform (<code>packages/db</code>)</td><td>Tenant name + invite-only flag (CON-08/10).</td></tr><tr><td><code>platform_settings</code></td><td>platform (<code>packages/db</code>)</td><td>Key-value config; <code>root_plugin_id</code> (CON-11, PLT-15), <code>examples_enabled</code> (CON-12).</td></tr></tbody></table><p>Resolved decisions (gaps the SRS left open):</p><ul><li><strong><code>plugin_status</code> and <code>tenants</code> are platform tables defined in <code>packages/db</code></strong>, not in <code>plugins/console/db/</code>. Console only declares no tables because these belong to the platform schema — the runtime, auth server, and middleware also read them. Console is a consumer, not the owner.</li><li>All user-scoped tables carry <code>tenant_id</code> from day one per the hard architectural rules, even though v1 is single-tenant.</li><li>The invite-only flag lives on <code>tenants</code> and is read by the <strong>auth server</strong> at registration time (AUTH-03). Console writes it; auth enforces it. No restart required (review checklist, 0.4.04).</li><li><strong>(2026-07-19 correction) On a fresh instance&#39;s first boot, only the platform chrome plugins (Account/Console/Launcher — never gated by <code>plugin_status</code> at all) are active by default.</strong> Every other plugin, examples included, starts with <strong>no</strong> <code>plugin_status</code> row and is therefore inactive/hidden across the sidebar, Launcher, and this Plugins table, until an admin either activates it individually (CON-07) or — for examples — flips the Settings → Example plugins bulk toggle (CON-12). An earlier boot-time backfill (<code>backfillPluginCatalogOnce</code>, RFC 0065 Task 3.28) used to eagerly create an <code>enabled: true</code> row for every non-chrome plugin including examples on first boot, which defeated both of those controls from the very first boot — see RFC 0065&#39;s changelog for the full writeup and why it was safe to remove.</li></ul><h2 id="sdk-dependencies" tabindex="-1">SDK dependencies <a class="header-anchor" href="#sdk-dependencies" aria-label="Permalink to &quot;SDK dependencies&quot;">​</a></h2><p>Console exercises four SDK surfaces. Their readiness is sequenced across v0.4 and v0.5:</p><table tabindex="0"><thead><tr><th>SDK surface</th><th>Used for</th><th>Status when Console needs it</th></tr></thead><tbody><tr><td><code>sdk.auth</code></td><td>session, user list, role, deactivate</td><td><strong>Real impl wired in Task 0.4.2</strong> (prerequisite).</td></tr><tr><td><code>sdk.mailer</code></td><td>sending invite emails (CON-03)</td><td><strong>Real impl wired in Task 0.4.2</strong> (prerequisite).</td></tr><tr><td><code>sdk.platform</code></td><td><code>getConfig()</code> for tenant name (CON-08)</td><td>Stubbed through v0.4; <strong>completed in Task 0.5.5</strong>.</td></tr><tr><td><code>sdk.db</code></td><td>reading/writing platform tables</td><td>Stubbed through v0.4; <strong>completed in Task 0.5.5</strong>.</td></tr></tbody></table><p><strong>Sequencing caveat (carry into the build):</strong> Task 0.4.4 needs <code>sdk.platform.getConfig()</code> and <code>sdk.db</code>, but their real implementations don&#39;t land until Task 0.5.5. For v0.4, Console&#39;s settings/health pages work against the stubbed <code>platform</code>/<code>db</code> surfaces (or read the tables directly via <code>packages/db</code> where the SDK stub is insufficient), and the SDK-routed path is finalized in 0.5.05. This is a known ordering wrinkle, not a contradiction — record any temporary direct-table access so 0.5.05 can route it through the SDK.</p><h2 id="ui" tabindex="-1">UI <a class="header-anchor" href="#ui" aria-label="Permalink to &quot;UI&quot;">​</a></h2><p>Console consumes <code>@sovereignfs/ui</code> (the Sovereign Design System) — it is named explicitly as a first-party consumer alongside the runtime shell (<code>docs/design-system.md</code>). Console uses both <strong>tokens</strong> and <strong>components</strong>:</p><ul><li>Build all Console UI from <code>@sovereignfs/ui</code> components (<code>Button</code>, <code>Card</code>, <code>Input</code>, <code>Badge</code>, …). No hardcoded colours, spacing, or radii — reference <code>--sv-*</code> semantic tokens only.</li><li>Tables (user list, plugin list) and forms (invite, settings) should drive any net-new primitives they need <em>into</em> <code>packages/ui</code> rather than building one-off styled markup in the plugin, so third-party plugins inherit them.</li><li>A detailed component-level spec (which <code>@sovereignfs/ui</code> primitives each Console page composes) is <strong>not yet written</strong> — see open questions.</li></ul><h2 id="build-plan" tabindex="-1">Build plan <a class="header-anchor" href="#build-plan" aria-label="Permalink to &quot;Build plan&quot;">​</a></h2><p>Phase v0.4 — four sequenced tasks, one branch + one PR each, cut from an up-to-date <code>main</code>. Doc task numbers are for local tracking only; never put them in branch names, commits, or PR titles.</p><h3 id="_1-—-console-scaffold" tabindex="-1">1 — Console scaffold <a class="header-anchor" href="#_1-—-console-scaffold" aria-label="Permalink to &quot;1 — Console scaffold&quot;">​</a></h3><p>Directory structure, manifest, and routing wired into the runtime via the generate script.</p><ul><li><code>manifest.json</code> (as above), <code>app/layout.tsx</code>, <code>app/page.tsx</code>, empty <code>db/schema.ts</code>, <code>package.json</code>.</li><li><code>pnpm generate</code> picks up the Console manifest and wires it into the runtime.</li></ul><p><strong>Done when:</strong> <code>/console</code> 403s for <code>platform:user</code> and is accessible for <code>platform:admin</code>; the generate script registers Console; Console shows in the launcher for admins only.</p><h3 id="_2-—-user-management" tabindex="-1">2 — User management <a class="header-anchor" href="#_2-—-user-management" aria-label="Permalink to &quot;2 — User management&quot;">​</a></h3><p>User list, invite, role change, deactivate/reactivate (CON-02–CON-05).</p><ul><li><code>app/users/page.tsx</code> (paginated: name, email, role, status, join date).</li><li><code>app/users/invite/page.tsx</code> (invite form → token + email via <code>sdk.mailer</code>).</li><li>Role change and deactivate/reactivate as server actions.</li><li><strong>Prerequisite landed here:</strong> real <code>sdk.auth</code> and <code>sdk.mailer</code> implementations.</li></ul><p><strong>Done when:</strong> user list shows correct data; invite email sends (or no-ops when SMTP unconfigured); role change persists; a deactivated user cannot log in.</p><h3 id="_3-—-plugin-management" tabindex="-1">3 — Plugin management <a class="header-anchor" href="#_3-—-plugin-management" aria-label="Permalink to &quot;3 — Plugin management&quot;">​</a></h3><p>Installed plugin list with enable/disable (CON-06, CON-07, PLT-04).</p><ul><li><code>app/plugins/page.tsx</code> lists registry plugins: name, version, type, status.</li><li>Enable/disable server action writes to <code>plugin_status</code>.</li><li>Runtime middleware respects disabled status — <strong>404</strong> for disabled plugin routes; disabled plugins hidden from the launcher.</li></ul><p><strong>Done when:</strong> disabling blocks routes immediately (no rebuild); disabled plugin disappears from the launcher; re-enabling restores access.</p><h3 id="plugin-access-policy-rfc-0065-con-13" tabindex="-1">Plugin access policy (RFC 0065, CON-13) <a class="header-anchor" href="#plugin-access-policy-rfc-0065-con-13" aria-label="Permalink to &quot;Plugin access policy (RFC 0065, CON-13)&quot;">​</a></h3><p>Access dialog on the Plugins page, additive to plugin management (task 3): who may <strong>open</strong> each installed plugin&#39;s app.</p><ul><li>Policy selector: Everyone, Admins and owners, Selected users, Selected groups, Disabled — auto-saves on change, writes <code>plugin_status.accessPolicy</code>.</li><li>Selected users/groups policies show a user or group picker (search-and-grant, backed by the directory SDK and the user groups foundation) plus a self-service checkbox and an empty-grant warning (&quot;nobody can open this plugin until you grant at least one&quot;).</li><li>Managing a plugin&#39;s access here never grants the acting admin/owner app access themselves — Console management (<code>console:access</code>) and plugin app access (this policy) are deliberately separate axes. Chrome plugins (Account, Console, Launcher) are exempt — the policy never applies to them, since they are core platform UI.</li><li>Disabled is the strongest state: nobody can open the plugin, even admins/owners or an already-granted user/group. The plugin stays installed and manageable.</li><li>Every policy change and grant/revoke is logged to the activity feed (<code>plugin.access_policy_changed</code>, <code>plugin.access_user_granted</code>/<code>revoked</code>, <code>plugin.access_group_granted</code>/<code>revoked</code>).</li><li>Middleware and the route guard enforce the policy on every request (<code>runtime/src/plugin-access-server.ts</code>) — the Console UI is advisory framing around a decision that&#39;s authoritative at the request layer, not the source of truth itself.</li></ul><p><strong>Done when:</strong> an Everyone-policy plugin opens for any user; a Selected-users-policy plugin with no grants shows the warning and blocks everyone including the current admin from opening it (verified by the &quot;Open&quot; affordance being disabled with a reason, and a direct route hit 404ing); a grant made through the picker immediately allows that user/group to open the plugin.</p><h3 id="unified-plugin-table-rfc-0065-task-13-9-con-14" tabindex="-1">Unified plugin table (RFC 0065 Task 13.9, CON-14) <a class="header-anchor" href="#unified-plugin-table-rfc-0065-task-13-9-con-14" aria-label="Permalink to &quot;Unified plugin table (RFC 0065 Task 13.9, CON-14)&quot;">​</a></h3><p>One row per cataloged plugin, whether active or not — replaces what were originally three separate sections (catalog, installed, examples) with a single filterable table/card list.</p><ul><li>Row <code>status</code> is derived, not stored: <code>incompatible</code> (a <code>compatibilityError</code> wins regardless of active/enabled) → <code>inactive</code> (no <code>plugin_status</code> row, and — for a non-example plugin, or an example with the bulk toggle off — that&#39;s the fresh-install default, not a transient state) → <code>enabled</code>/<code>disabled</code> (active, keyed off the enabled flag). One exception: a row-less example plugin counts as <code>enabled</code>, not <code>inactive</code>, when the examples bulk toggle is on — it&#39;s genuinely live in the sidebar/Launcher in that state, so the table must not claim otherwise.</li><li>Filter bar: keyword search (client-side — plugin counts are small enough that a server round trip isn&#39;t warranted), a status pill row (All/Inactive/Enabled/Disabled/Incompatible), and a &quot;Show examples&quot; toggle whose <strong>initial</strong> value mirrors the resolved <code>examples_enabled</code> setting (2026-07-19 correction — it previously hard-coded to <code>on</code> regardless of the instance setting, so a bulk-disabled example without an individual override never actually disappeared from this table, contradicting CON-12). The admin can still flip it locally to reveal-and-manage a specific example while the bulk toggle stays off; the checkbox is a client-side view filter, not a second copy of the persisted setting.</li><li>Row actions are contextual: <code>inactive</code> gets <code>Activate</code> (+ <code>Remove</code> if removable), transitioning in place to the existing &quot;pick an initial policy&quot; prompt (Task 13.8) on success; <code>enabled</code>/<code>disabled</code> get the existing toggle/Access/Open/Remove, with Access hidden for chrome plugins; <code>incompatible</code> shows the reason only.</li><li>Desktop keeps all actions inline; mobile moves Access and Remove behind a <code>⋯</code> kebab menu (reusing <code>@sovereignfs/ui</code>&#39;s <code>Menu</code>, the same component and pattern the Users page&#39;s <code>UserCard</code> already uses), keeping only the primary toggle/Activate and the Open link inline.</li></ul><p><strong>Done when:</strong> no plugin appears more than once on the page in any filter combination; searching and status-filtering combine correctly; the just-activated policy prompt still appears in place (no regression of the Task 13.8 race-condition fix); chrome plugins show no Access control; the mobile kebab menu doesn&#39;t clip against its card list container.</p><h3 id="_4-—-tenant-settings-and-system-health" tabindex="-1">4 — Tenant settings and system health <a class="header-anchor" href="#_4-—-tenant-settings-and-system-health" aria-label="Permalink to &quot;4 — Tenant settings and system health&quot;">​</a></h3><p>Tenant name configuration, invite-only toggle, and the health dashboard (CON-08, CON-09, CON-10, PLT-06).</p><ul><li><code>app/settings/page.tsx</code> — tenant name field + invite-only toggle.</li><li><code>app/health/page.tsx</code> — runtime version, DB type + connection status, auth server status, disk usage (SQLite file size or Postgres connection).</li><li>Tenant name stored in <code>tenants</code>, exposed via <code>sdk.platform.getConfig()</code>; invite-only toggle written to <code>tenants</code>, read by the auth server at registration.</li></ul><p><strong>Done when:</strong> tenant name change reflects in <code>sdk.platform.getConfig()</code> immediately; health page reports accurate DB type (SQLite vs Postgres); invite-only toggle takes effect on the next registration without restart.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><p>Tracked here so they&#39;re resolved deliberately, not silently during implementation:</p><ol><li><strong>Console component-level UI spec.</strong> Which <code>@sovereignfs/ui</code> primitives each page composes, and which net-new primitives Console forces into <code>packages/ui</code> (e.g. a <code>Table</code>, a <code>Toggle</code>, a paginated-list pattern). Needs a pass once the v0.3.07 design-system scaffold lands.</li><li><strong>Health metrics precision.</strong> &quot;Disk usage&quot; for SQLite is the DB file size; for Postgres the SRS says &quot;connection&quot; — the exact Postgres health signal (connection liveness only, or also DB size via a query) isn&#39;t pinned.</li><li><strong>Invite token lifecycle.</strong> Token format, expiry, single-use vs reusable, and where it&#39;s stored (auth server vs platform table) — referenced by CON-03 but not specified. Likely belongs to the auth spec, cross-referenced from here.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft — consolidated from SRS §4.4 and the v0.4 tasks.</td></tr></tbody></table>`,73)])])}const g=t(i,[["render",o]]);export{u as __pageData,g as default};
