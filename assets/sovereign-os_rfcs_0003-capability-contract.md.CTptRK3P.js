import{_ as t,o as a,c as i,a2 as o}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC-0003: Capability Contract","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/rfcs/0003-capability-contract.md"},"headers":[],"relativePath":"sovereign-os/rfcs/0003-capability-contract.md","filePath":"sovereign-os/rfcs/0003-capability-contract.md"}'),n={name:"sovereign-os/rfcs/0003-capability-contract.md"};function r(s,e,c,l,d,h){return a(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="rfc-0003-capability-contract" tabindex="-1">RFC-0003: Capability Contract <a class="header-anchor" href="#rfc-0003-capability-contract" aria-label="Permalink to &quot;RFC-0003: Capability Contract&quot;">​</a></h1><p><strong>Status:</strong> Accepted (2026-08-09, project creator) <strong>Author:</strong> Project creator and Claude <strong>Created:</strong> 2026-08-09 <strong>Reviewers:</strong> Project creator <strong>Target phase:</strong> <a href="./../roadmap/01-2-local-conversation-capabilities.html">Milestone 01.2, Local Conversation and Capabilities</a><strong>Supersedes:</strong> None</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Define what a &quot;capability&quot; is in Sovereign, how it is registered, and how the deterministic executor validates and runs one — independent of who or what is asking. <a href="./0002-local-conversation-and-inference-runtime.html">RFC-0002</a> (Accepted) already established that a model&#39;s capability proposal is untrusted data the executor validates independently, and named the executor as a component without specifying it. This RFC specifies it. RFC-0004 (AI capability invocation, reserved in <a href="./README.html">docs/rfcs/README.md</a>) then defines the narrower question of how a model&#39;s proposal specifically maps onto the contract this RFC defines — prompt-side tool-call shaping, disambiguation, and the model-invocation confirmation flow. RFC-0006 (Pi-hole capability mapping) defines the specific Pi-hole capabilities&#39; data shape using this contract. This RFC is the platform primitive all three build on; it does not itself specify any capability&#39;s business logic beyond classifying the milestone&#39;s four named ones.</p><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p>Milestone 01.2&#39;s plan (<a href="./../roadmap/01-2-local-conversation-capabilities.html">docs/roadmap/01-2-local-conversation-capabilities.md</a>) names the Capability Registry and Executor as a required functional component, with explicit requirements (typed/versioned registration, read-only/mutating distinction, independent argument validation, bounded output/network/execution-time policy, privacy-safe audit events, and confirmation for sensitive actions). Nothing yet specifies the schema a capability is defined with, the validation pipeline an invocation must pass, or how &quot;mutating&quot; and &quot;external network&quot; are distinguished as separate risk axes — a capability can be read-only with respect to Sovereign&#39;s own state (<code>web.search</code>) while still making real contact with the outside world, which is not the same risk <code>system.health</code> (read-only, fully local) carries.</p><p>Without this contract fixed before RFC-0004 and RFC-0006 are written, each would have to invent its own validation and audit shape, and a later capability (Home Assistant, Milestone 5) would either duplicate that work or retrofit onto whatever RFC-0004/0006 happened to assume.</p><h2 id="goals" tabindex="-1">Goals <a class="header-anchor" href="#goals" aria-label="Permalink to &quot;Goals&quot;">​</a></h2><ul><li>Define a capability&#39;s schema: name, version, typed arguments, typed result, side-effect classification, network classification, and execution bounds.</li><li>Define the deterministic executor&#39;s validation pipeline: the fixed sequence every invocation passes through regardless of caller, before anything executes.</li><li>Define the confirmation requirement model: which classifications require explicit user confirmation before execution, and which may execute without it.</li><li>Define the audit event shape produced for every invocation attempt, successful or not.</li><li>Classify this milestone&#39;s four named capabilities (<code>system.health</code>, read-only Pi-hole capabilities, <code>web.search</code>, <code>web.fetch</code>) against this contract, without specifying their individual argument/result schemas.</li><li>Establish that capabilities are a fixed, Sovereign-authored registry — not a dynamically loadable or user-installable plugin system — matching the milestone&#39;s explicit non-scope.</li></ul><h2 id="non-goals" tabindex="-1">Non-Goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-Goals&quot;">​</a></h2><ul><li>How a model proposes a capability call, disambiguates between candidates, or is asked to confirm a mutating action — RFC-0004.</li><li>Pi-hole&#39;s specific capability arguments, result shape, and which Pi-hole data is exposed at all — RFC-0006.</li><li><code>web.search</code>/<code>web.fetch</code>&#39;s specific privacy design (query disclosure, SearXNG deployment, result-fetch content-safety policy) — named as its own required document in the milestone plan, tracked separately; this RFC only fixes that both are capabilities with an external-network classification.</li><li>Mutating capabilities&#39; actual behavior. None ship in this milestone (Pi-hole mutation and Home Assistant control are explicit non-scope); this RFC defines the classification and confirmation model mutating capabilities will use in Milestone 5, so that milestone doesn&#39;t need to redesign this contract, but does not introduce any mutating capability now.</li><li>A general plugin system, third-party capability installation, or runtime capability registration. The registry this RFC defines is fixed at build/deploy time, the same way the appliance&#39;s file allowlist is (see <a href="./../research/appliance-file-set-update-ceiling-finding.html">the appliance file-set finding</a> for why an unbounded, runtime-extensible allowlist is a real hazard this project has already hit once, in a different subsystem).</li></ul><h2 id="context-and-evidence" tabindex="-1">Context and Evidence <a class="header-anchor" href="#context-and-evidence" aria-label="Permalink to &quot;Context and Evidence&quot;">​</a></h2><ul><li><a href="./../roadmap/01-2-local-conversation-capabilities.html">docs/roadmap/01-2-local-conversation-capabilities.md</a>, &quot;Capability Registry and Executor&quot; and &quot;Initial Capabilities&quot; sections, is the direct source for this RFC&#39;s requirements and the four capabilities classified below.</li><li><a href="./0002-local-conversation-and-inference-runtime.html">RFC-0002</a>&#39;s Safety Boundary section already committed to: a capability proposal is data, never a command; only the executor this RFC specifies may execute one; and the runner process itself has no shell, Docker, credential, or unrestricted network access. This RFC is the concrete mechanism that commitment depends on.</li><li>This project&#39;s existing deterministic-validation precedent — <code>sovereign-update</code>&#39;s manifest/signature/trust validation, which simulates every operation against current state before applying anything and rejects atomically on any failure (see <code>rotate_trust</code> in the updater, and its <a href="./../research/rotate-trust-real-signed-cycle-qualification-report.html">real signed-cycle qualification report</a>) — is the model this RFC&#39;s executor pipeline follows: validate everything, execute nothing, until validation fully passes.</li><li>The append-only, non-secret audit log pattern already established for trust rotations (<code>/data/sovereign/update-state/trust-rotations.jsonl</code>) and update transactions (<code>events.jsonl</code> per transaction) is reused here rather than inventing a new audit shape.</li></ul><h2 id="proposal" tabindex="-1">Proposal <a class="header-anchor" href="#proposal" aria-label="Permalink to &quot;Proposal&quot;">​</a></h2><h3 id="capability-definition" tabindex="-1">Capability Definition <a class="header-anchor" href="#capability-definition" aria-label="Permalink to &quot;Capability Definition&quot;">​</a></h3><p>A capability is a fixed, Sovereign-authored registry entry with:</p><ul><li><strong><code>name</code></strong> — namespaced and dotted, matching the milestone plan&#39;s own naming (<code>system.health</code>, <code>pihole.*</code>, <code>web.search</code>, <code>web.fetch</code>).</li><li><strong><code>version</code></strong> — an integer, independent per capability. A breaking argument/result schema change is a new version, not a mutation of an existing one, so a previously-valid proposal never silently changes meaning.</li><li><strong><code>argument_schema</code></strong> / <strong><code>result_schema</code></strong> — typed schemas (JSON Schema, matching this project&#39;s existing manifest-validation style rather than introducing a new schema language) that the executor validates independently of whatever produced the arguments.</li><li><strong><code>side_effect</code></strong> — <code>read_only</code> or <code>mutating</code>. Read-only capabilities observe Sovereign or external state without changing it. No mutating capability is registered in this milestone.</li><li><strong><code>network</code></strong> — <code>local</code> or <code>external</code>. <code>local</code> capabilities never leave the device. <code>external</code> capabilities contact something outside it (<code>web.search</code>, <code>web.fetch</code>) and are classified as such regardless of their <code>side_effect</code> value — <code>web.search</code> is <code>read_only</code> <em>and</em><code>external</code>, and both classifications apply independently, not as a single combined risk level.</li><li><strong><code>confirmation</code></strong> — <code>automatic</code> or <code>required</code>, derived from <code>side_effect</code> and <code>network</code> (see Confirmation Model below), not set independently per capability, so a capability&#39;s author cannot accidentally under-classify its own risk.</li><li><strong><code>bounds</code></strong> — timeout, maximum result size, and maximum invocations per conversation turn. Every capability declares these; there is no unbounded default.</li></ul><h3 id="registry" tabindex="-1">Registry <a class="header-anchor" href="#registry" aria-label="Permalink to &quot;Registry&quot;">​</a></h3><p>The registry is a static list compiled into Sovereign, not a directory scanned at runtime and not installable by a user or a model. This mirrors the appliance file allowlist&#39;s lesson: an open-ended, runtime- extensible set of executable things is a real hazard this project has already paid down elsewhere, not a design to repeat here. Adding a capability is a Sovereign release, reviewed and shipped through the existing signed appliance-update path — the same trust boundary that already governs every other executable thing on the device.</p><h3 id="deterministic-executor" tabindex="-1">Deterministic Executor <a class="header-anchor" href="#deterministic-executor" aria-label="Permalink to &quot;Deterministic Executor&quot;">​</a></h3><p>Every invocation — regardless of caller — passes through the same fixed pipeline before anything runs, mirroring <code>sovereign-update rotate_trust</code>&#39;s validate-fully-before-applying-anything discipline:</p><ol><li><strong>Resolve.</strong> The proposed <code>name</code>/<code>version</code> must match a registered capability exactly. An unknown name or version is rejected before any other check runs — there is no fuzzy or partial match at this layer.</li><li><strong>Validate arguments.</strong> The proposed arguments are validated against <code>argument_schema</code> independently of the caller&#39;s own claims about their shape. A capability&#39;s implementation never receives arguments the schema didn&#39;t already accept.</li><li><strong>Check policy.</strong> The capability&#39;s classification is checked against current device policy — e.g., whether <code>web.search</code> is enabled at all (opt-in, per the milestone plan&#39;s web-search policy) — before proceeding. A capability disabled by policy is rejected here, not silently no-op&#39;d.</li><li><strong>Gate on confirmation.</strong> If <code>confirmation</code> is <code>required</code>, the executor does not proceed until an explicit, freshly-obtained user confirmation for <em>this specific proposed invocation</em> (not a standing blanket approval) is present. How that confirmation is solicited from a model-proposed call is RFC-0004&#39;s concern; this RFC only fixes that the executor itself enforces the gate and cannot be bypassed by the caller claiming confirmation already happened.</li><li><strong>Execute, bounded.</strong> The capability implementation runs under its declared <code>bounds</code> — timeout, result-size cap, per-turn invocation cap — with no ambient access beyond what that specific implementation needs (no shared shell, no shared credential store, no network access for a <code>local</code> capability).</li><li><strong>Audit, always.</strong> An audit event is produced whether the invocation succeeded, was rejected at any pipeline stage, or timed out. Rejection is not a silent non-event.</li></ol><h3 id="confirmation-model" tabindex="-1">Confirmation Model <a class="header-anchor" href="#confirmation-model" aria-label="Permalink to &quot;Confirmation Model&quot;">​</a></h3><table tabindex="0"><thead><tr><th><code>side_effect</code></th><th><code>network</code></th><th><code>confirmation</code></th></tr></thead><tbody><tr><td><code>read_only</code></td><td><code>local</code></td><td><code>automatic</code></td></tr><tr><td><code>read_only</code></td><td><code>external</code></td><td><code>required</code></td></tr><tr><td><code>mutating</code></td><td><code>local</code></td><td><code>required</code></td></tr><tr><td><code>mutating</code></td><td><code>external</code></td><td><code>required</code></td></tr></tbody></table><p>Only the fully local, fully read-only combination executes without per-invocation confirmation. Every combination involving either mutation or external network contact requires it. This is deliberately conservative for a single-household appliance with no operator watching most invocations in real time — matching the milestone plan&#39;s web-search policy (&quot;search only when the user requests it or the user approves a proposed search&quot;) generalized to the whole contract rather than special- cased to just <code>web.search</code>.</p><h3 id="audit-events" tabindex="-1">Audit Events <a class="header-anchor" href="#audit-events" aria-label="Permalink to &quot;Audit Events&quot;">​</a></h3><p>Every invocation produces a structured, append-only, non-secret event recording: timestamp, capability name/version, <code>side_effect</code>/<code>network</code> classification, which pipeline stage it reached (resolved, validated, policy-checked, confirmed, executed, or rejected-at-<code>&lt;stage&gt;</code>), result size (not result content), and duration. Arguments and results are never written to the audit log verbatim — household data (a search query, a Pi-hole domain) is not the same class of information as the fact an invocation happened, and the existing trust-rotation/update-transaction logs already establish the precedent of auditing the <em>event</em> without persisting the <em>sensitive content</em>.</p><h3 id="initial-capability-classification" tabindex="-1">Initial Capability Classification <a class="header-anchor" href="#initial-capability-classification" aria-label="Permalink to &quot;Initial Capability Classification&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Capability</th><th><code>side_effect</code></th><th><code>network</code></th><th><code>confirmation</code></th></tr></thead><tbody><tr><td><code>system.health</code></td><td><code>read_only</code></td><td><code>local</code></td><td><code>automatic</code></td></tr><tr><td>Pi-hole read-only capabilities</td><td><code>read_only</code></td><td><code>local</code></td><td><code>automatic</code></td></tr><tr><td><code>web.search</code></td><td><code>read_only</code></td><td><code>external</code></td><td><code>required</code></td></tr><tr><td><code>web.fetch</code></td><td><code>read_only</code></td><td><code>external</code></td><td><code>required</code></td></tr></tbody></table><p>All four are read-only with respect to Sovereign&#39;s own state, matching the milestone&#39;s non-scope of any mutating capability this pass. <code>web.search</code> and <code>web.fetch</code> are classified <code>external</code> even though they don&#39;t mutate anything Sovereign controls, because they genuinely leave the device — the classification tracks where data goes, not just what it changes.</p><h2 id="interfaces-and-data-flow" tabindex="-1">Interfaces and Data Flow <a class="header-anchor" href="#interfaces-and-data-flow" aria-label="Permalink to &quot;Interfaces and Data Flow&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Caller (Conversation Service, on behalf of a model proposal per RFC-0004,</span></span>
<span class="line"><span>        or a future direct UI-triggered invocation)</span></span>
<span class="line"><span>    -&gt; Executor.invoke(name, version, arguments, confirmation_token?)</span></span>
<span class="line"><span>        1. resolve            (unknown name/version -&gt; rejected)</span></span>
<span class="line"><span>        2. validate arguments (schema mismatch -&gt; rejected)</span></span>
<span class="line"><span>        3. check policy       (disabled by policy -&gt; rejected)</span></span>
<span class="line"><span>        4. gate on confirmation (required and absent -&gt; rejected,</span></span>
<span class="line"><span>                                  pending-confirmation state returned)</span></span>
<span class="line"><span>        5. execute, bounded   (timeout/size cap enforced)</span></span>
<span class="line"><span>        6. audit               (always, regardless of outcome)</span></span>
<span class="line"><span>    &lt;- result | rejection, plus the audit event&#39;s identifier</span></span></code></pre></div><p>The executor is a standalone component the Conversation Service calls into — it is not coupled to AI invocation specifically. A future directly-triggered UI action (a button, not a model proposal) uses the identical pipeline, which is exactly why this contract is a separate RFC from RFC-0004 rather than folded into it.</p><h2 id="security-and-privacy" tabindex="-1">Security and Privacy <a class="header-anchor" href="#security-and-privacy" aria-label="Permalink to &quot;Security and Privacy&quot;">​</a></h2><ul><li>The registry being fixed at build/deploy time means no invocation can ever target a capability that wasn&#39;t reviewed and shipped through the signed appliance-update path — there is no runtime path to register or invoke something arbitrary.</li><li>Argument validation happens against the schema, never against whatever the caller (model or otherwise) claims about its own input — the same &quot;never trust the caller&#39;s own framing&quot; posture <code>validate_release_payload</code> and <code>validate_trust_rotation_manifest</code> already apply to update artifacts.</li><li><code>external</code>-classified capabilities are the only ones with real network egress, and that classification is structural (a table lookup), not a per-invocation judgment call that could be prompt-injected into skipping confirmation.</li><li>Audit events are privacy-safe by construction: they record that an invocation happened and what pipeline stage it reached, never the argument or result content, which may contain household data (search terms, Pi-hole domain queries).</li></ul><h2 id="failure-and-recovery" tabindex="-1">Failure and Recovery <a class="header-anchor" href="#failure-and-recovery" aria-label="Permalink to &quot;Failure and Recovery&quot;">​</a></h2><ul><li>Rejection at any pipeline stage is a normal, expected, always-audited outcome — not an error state requiring recovery. The executor has no persistent state to roll back on rejection; nothing executes until stage 5.</li><li>A capability implementation that exceeds its declared timeout or result-size bound is terminated and reported as a bounded failure, not allowed to run to completion outside its declared bounds.</li><li>A confirmation that is requested but never given simply expires the pending invocation; it does not retry automatically or escalate.</li></ul><h2 id="compatibility-and-migration" tabindex="-1">Compatibility and Migration <a class="header-anchor" href="#compatibility-and-migration" aria-label="Permalink to &quot;Compatibility and Migration&quot;">​</a></h2><p>There is no existing capability system to migrate from. The registry&#39;s build-time-fixed nature means adding a capability is an ordinary appliance release through the existing update path (RFC-0014) — no new update mechanism is required. A capability version bump is additive: an older proposal referencing an old version either still resolves (if the implementation kept it) or is rejected at the resolve stage exactly like an unknown capability, never silently reinterpreted under the new version&#39;s schema.</p><h2 id="operations-and-observability" tabindex="-1">Operations and Observability <a class="header-anchor" href="#operations-and-observability" aria-label="Permalink to &quot;Operations and Observability&quot;">​</a></h2><ul><li>The audit log is the operational record of what capabilities have been invoked, how often, and how many were rejected at which stage — useful both for the device operator and for evaluating whether a model is proposing well-formed capability calls (relevant to RFC-0004&#39;s structured-output accuracy requirements).</li><li>Policy state (which <code>external</code> capabilities are enabled) should be inspectable the same way update/trust-rotation state already is, rather than requiring a log search to determine current configuration.</li></ul><h2 id="testing-strategy" tabindex="-1">Testing Strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to &quot;Testing Strategy&quot;">​</a></h2><ul><li>Contract-level tests for the executor pipeline itself — resolution, schema validation, policy gating, confirmation gating, bounded execution, and audit emission — using fixture capabilities, so the pipeline is proven correct independent of any real capability&#39;s business logic.</li><li>Adversarial-input tests: malformed arguments, arguments that validate against the schema but are semantically nonsensical, requests for unknown names/versions, and confirmation-bypass attempts, all of which must be rejected at the correct pipeline stage and audited.</li><li>Once <code>system.health</code> and the Pi-hole read-only capabilities exist (RFC-0006), real-hardware qualification that the full pipeline runs correctly against real device state, following this project&#39;s standing practice of a dated report under <code>docs/research/</code> for real hardware passes.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Let each capability define its own confirmation requirement.</strong> Rejected: this makes under-classification a per-capability author mistake instead of a structural property of the classification table, and this project&#39;s own history (the appliance file-allowlist gap) is a direct example of what happens when a boundary is enforced by scattered judgment calls instead of one central table.</li><li><strong>A dynamically loadable/pluggable capability system</strong>, closer to a general extension model. Rejected per the milestone&#39;s explicit non-scope (no general plugin installation) and this project&#39;s standing preference against infrastructure this single-device, single-maintainer project&#39;s actual scale doesn&#39;t need.</li><li><strong>Audit full argument/result content for debuggability.</strong> Rejected: household data (search queries, Pi-hole domains) doesn&#39;t belong in a durable log by default, mirroring why update/trust-rotation audit logs already exclude manifest contents and DNS queries.</li></ul><h2 id="drawbacks-and-maintenance-cost" tabindex="-1">Drawbacks and Maintenance Cost <a class="header-anchor" href="#drawbacks-and-maintenance-cost" aria-label="Permalink to &quot;Drawbacks and Maintenance Cost&quot;">​</a></h2><ul><li>A build-time-fixed registry means adding a capability always requires a full appliance release cycle, not a lightweight runtime registration — an intentional tradeoff (see Alternatives Considered), but real friction if capabilities need to iterate quickly during development.</li><li>The confirmation model&#39;s default conservatism (everything but fully local read-only requires confirmation) may prove too aggressive in practice for read-only external capabilities used often (<code>web.search</code>); loosening it later is a contract change, not a policy toggle, since confirmation is derived structurally rather than configured per capability.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved Questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to &quot;Unresolved Questions&quot;">​</a></h2><p>None of the following block acceptance of the contract below; each is either implementation-level or explicitly deferred to RFC-0004/0006.</p><ul><li>Exact JSON Schema dialect and validation library — implementation detail, not an architectural question.</li><li>Where policy state (which <code>external</code> capabilities are enabled) is stored and how it&#39;s changed — likely alongside existing device configuration, but not fixed here.</li><li>Whether confirmation tokens are single-use, time-bounded, or both — left to RFC-0004, since it depends on how a model-proposed confirmation flow actually surfaces to the user.</li><li>The exact mutating-capability confirmation UX (beyond &quot;required&quot;) is deferred to Milestone 5, when a mutating capability first actually ships.</li></ul><h2 id="acceptance-criteria" tabindex="-1">Acceptance Criteria <a class="header-anchor" href="#acceptance-criteria" aria-label="Permalink to &quot;Acceptance Criteria&quot;">​</a></h2><ul><li>The capability schema (name, version, argument/result schema, <code>side_effect</code>, <code>network</code>, <code>confirmation</code>, <code>bounds</code>) is implemented as the registration format every capability uses.</li><li>The six-stage executor pipeline (resolve, validate, policy-check, confirm, execute-bounded, audit) is implemented and covered by contract-level tests independent of any specific capability.</li><li>The Confirmation Model table is enforced structurally — no code path allows a capability to declare its own <code>confirmation</code> value independent of its <code>side_effect</code>/<code>network</code> classification.</li><li>Audit events are produced for every invocation outcome, verified to never contain argument or result content, only classification and outcome metadata.</li><li><code>system.health</code>, the Pi-hole read-only capabilities, <code>web.search</code>, and <code>web.fetch</code> are each registered against this contract with the classification this RFC assigns them.</li><li>Adversarial-input tests (malformed arguments, unknown name/version, confirmation bypass attempts) are rejected at the correct pipeline stage, not merely rejected somewhere.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Accepted (2026-08-09, project creator).</strong> The capability schema, the six-stage executor pipeline, the structural confirmation model, and the audit event shape are accepted as the platform contract RFC-0004 and RFC-0006 build on. The Unresolved Questions above are non-blocking follow-ups, not gating conditions.</p>`,54)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
