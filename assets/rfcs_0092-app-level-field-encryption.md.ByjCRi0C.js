import{_ as t,o as s,c as i,a2 as a}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"RFC 0092 — App-Level Field Encryption (Platform-Wide by Classification)","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign/blob/main/docs/rfcs/0092-app-level-field-encryption.md"},"headers":[],"relativePath":"rfcs/0092-app-level-field-encryption.md","filePath":"rfcs/0092-app-level-field-encryption.md"}'),o={name:"rfcs/0092-app-level-field-encryption.md"};function n(r,e,d,l,c,p){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0092-—-app-level-field-encryption-platform-wide-by-classification" tabindex="-1">RFC 0092 — App-Level Field Encryption (Platform-Wide by Classification) <a class="header-anchor" href="#rfc-0092-—-app-level-field-encryption-platform-wide-by-classification" aria-label="Permalink to &quot;RFC 0092 — App-Level Field Encryption (Platform-Wide by Classification)&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> August 2026<br><strong>Author:</strong> kasunben &amp; Claude Code<br><strong>Scope:</strong> <code>packages/sdk</code>, <code>packages/db</code>, <code>packages/manifest</code>, <code>runtime/src</code>, <code>bin/sv</code>, <code>.env.example</code>, <code>docs/self-hosting.md</code>, <code>docs/plugin-development.md</code>; narrows and partially supersedes RFC 0008&#39;s Tier 2/3 scope (epic task 8.5); builds on <a href="./../research/0013-layered-database-encryption-strategy.html">Research 0013</a><br><strong>Incorporated into plan:</strong> Yes — epic tasks 8.31–8.34, sequenced by workstream 0011 (leg 1 shipped as v0.79.0).</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Add server-side field encryption as a platform capability: plugin authors <strong>classify</strong> a column&#39;s sensitivity in schema code; the operator sets an <strong>instance-wide policy</strong> deciding which sensitivity classes are actually encrypted. Classified fields are encrypted in the Node.js app tier (AES-256-GCM, per-class Data Encryption Keys wrapped under a master Key Encryption Key) before any write reaches the database, so Postgres and sqld only ever store ciphertext. Exact-match querying over encrypted fields is preserved via HMAC-SHA256 blind-index companion columns.</p><p>This is Layer 2 of Research 0013&#39;s three-layer model — the answer to the threat neither disk encryption nor anything inside the database can address: a database operator with a live, authenticated connection. Layer 1 (TLS) is already shipped; Layer 3 (RFC 0060&#39;s client-side zero-knowledge encryption) already exists as an opt-in SDK capability and is unchanged by this RFC.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Since the sqld cutover retired RFC 0071&#39;s SQLCipher path, <strong>neither dialect has any application-level at-rest encryption</strong> — <code>docs/upgrade.md</code> directs operators to disk/volume encryption, which only protects a stolen disk. A Sovereign instance&#39;s database operator (or anyone who compromises the database container) can read every user&#39;s data in plaintext with a standard client. For a privacy-first, self-hosted product whose plugin registry is open to third-party authors, &quot;every plugin author must independently remember to protect sensitive data, with no platform mechanism to do it&quot; is not a defensible posture.</p><p>The classification/policy split is deliberate: the plugin author is the only party who knows what a column <em>holds</em>, but the operator is the only party who knows the instance&#39;s threat model. Separating the two means a third-party plugin from the registry — whose author never thought about encryption — still gets its <code>pii</code>-classified fields protected on any instance whose operator turned that class on.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>No live at-rest encryption in either dialect.</strong> <code>packages/db/src/sqlite-encryption.ts:1-15</code> — RFC 0071 retired; only legacy-read primitives for <code>sv db migrate-to-postgres</code> remain.</li><li><strong>A working AES-256-GCM envelope pattern already ships.</strong> <code>runtime/src/secrets.ts:57-70</code> (<code>sdk.secrets</code>): random 12-byte IV, AEAD auth tag, AAD bound to <code>{tenantId, pluginId, scope, userId}</code>, versioned envelope string (<code>sv1:iv:tag:ciphertext</code>), keyed by <code>SOVEREIGN_VAULT_KEY</code> with a fail-fast malformed-key loader. This RFC extends that shape, not a new scheme.</li><li><strong>The API surface is already named.</strong> <code>packages/sdk/src/types.ts:203</code> references <code>sdk.crypto.encryptField()</code> as the planned RFC 0008 Tier 3 surface; the <code>crypto:use</code> permission is reserved in SRS §5. Neither is implemented.</li><li><strong>Task 8.5 scoped the dependency but is partly obsolete.</strong> Its Tier 2 SQLCipher deliverables died with RFC 0071&#39;s retirement; its KEK→DEK envelope key management and Tier 3 field-level scope are what this RFC now carries (see the scope note added to task 8.5).</li><li><strong>RFC 0060 client-side encryption is live</strong> (<code>packages/sdk/src/e2ee-*.ts</code>, <code>e2ee:use</code> permission) — the stronger, opt-in tier for data where zero server-side visibility justifies losing server-side compute. This RFC does not change it.</li><li><strong>In-database options were evaluated and rejected</strong> in Research 0013: <code>pgcrypto</code> leaks its key through the query/log surface; <code>pg_tde</code>/<code>pgsodium</code> need non-stock images unavailable on managed Postgres; all of them, plus RLS, are defeated by a live superuser connection.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="sensitivity-taxonomy" tabindex="-1">Sensitivity taxonomy <a class="header-anchor" href="#sensitivity-taxonomy" aria-label="Permalink to &quot;Sensitivity taxonomy&quot;">​</a></h3><p>A closed initial enum, deliberately small:</p><table tabindex="0"><thead><tr><th>Class</th><th>Intended contents</th></tr></thead><tbody><tr><td><code>pii</code></td><td>Directly identifying personal data: names in free text, addresses, birthdays</td></tr><tr><td><code>health</code></td><td>Health, medical, and wellbeing data</td></tr><tr><td><code>financial</code></td><td>Financial records, account details, transaction descriptions</td></tr><tr><td><code>sensitive</code></td><td>Catch-all for data the author judges sensitive but unclassifiable above</td></tr></tbody></table><p>Extending the enum is a platform change (minor SDK bump), not something plugins can do ad hoc. Registry submission review covers whether a plugin&#39;s classifications are plausible — same trust model as its permission list.</p><h3 id="classification-schema-helpers" tabindex="-1">Classification: schema helpers <a class="header-anchor" href="#classification-schema-helpers" aria-label="Permalink to &quot;Classification: schema helpers&quot;">​</a></h3><p>Two paired helpers, exported from <code>@sovereignfs/sdk</code> (plugins may only import the SDK — the ESLint boundary rule):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { encryptedText, blindIndex } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sovereignfs/sdk&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> journalEntries</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sqliteTable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;healthjournal_entries&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  id: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primaryKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tenantId: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tenant_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  loggedAt: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;logged_at&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notNull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// plaintext metadata — filter/sort freely</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  notes: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">encryptedText</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;notes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { sensitivity: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;health&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  notesIdx: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">blindIndex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;notes_bidx&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { source: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;notes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }), </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// exact-match lookups only</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>encryptedText()</code> wraps drizzle&#39;s <code>text()</code> via <code>customType</code>, attaching sensitivity metadata the write path reads; <code>blindIndex()</code> declares an HMAC-SHA256 companion column maintained automatically alongside its source. Both are dialect-portable by construction — the stored value is an ordinary string in both <code>schema/sqlite/*.ts</code> and <code>schema/postgres/*.ts</code>. The SDK&#39;s zero-deps rule (<code>no @sovereignfs/db</code>, no <code>@sovereignfs/mailer</code>) holds: the helpers depend only on <code>drizzle-orm</code>, which every plugin with a schema already has; the crypto itself is host-provided via <code>provideHost()</code> like every other SDK implementation.</p><h3 id="enforcement-operator-policy" tabindex="-1">Enforcement: operator policy <a class="header-anchor" href="#enforcement-operator-policy" aria-label="Permalink to &quot;Enforcement: operator policy&quot;">​</a></h3><p><code>SOVEREIGN_ENCRYPT_CLASSES</code> — a comma-separated list of enabled classes (e.g. <code>pii,health</code>). Classes not listed are stored plaintext even if classified; an empty/unset value means no field encryption (safe default for existing instances; no behavior change on upgrade). Read at startup; <code>.env.example</code> + <code>docs/self-hosting.md</code> + docs-parity in the same PR per convention. Console → Settings later gains a read-only display of the active policy (not an editable toggle in this pass — changing policy has migration implications, below).</p><p>Turning a class <strong>on</strong> encrypts <em>new writes</em> from that point. It never silently rewrites existing rows — backfill is a separate, explicit, operator-triggered step (<code>sv db encrypt-fields</code>). This is a direct lesson from the RFC 0071 rollout incident (<code>docs/incidents/2026-07-24-rfc-0071-encryption-rollout.md</code>): an instance-wide toggle must never fan out into cross-plugin bulk mutation as a boot side effect.</p><h3 id="key-hierarchy" tabindex="-1">Key hierarchy <a class="header-anchor" href="#key-hierarchy" aria-label="Permalink to &quot;Key hierarchy&quot;">​</a></h3><ul><li><strong>KEK:</strong> <code>SOVEREIGN_FIELD_KEK</code> env var — 32 bytes, same encoding and fail-fast loader discipline as <code>SOVEREIGN_VAULT_KEY</code> (<code>runtime/src/secrets.ts</code>), no default. Required only when <code>SOVEREIGN_ENCRYPT_CLASSES</code> is non-empty; boot fails loudly if the policy is set without the key. Deliberately <strong>not</strong> a reuse of <code>SOVEREIGN_VAULT_KEY</code>: the vault protects a handful of high-value secrets; field encryption covers broad routine data — different rotation cadence, different blast radius.</li><li><strong>DEKs:</strong> one per (sensitivity class × plugin), generated on first use, wrapped under the KEK, stored in a platform table. Per-plugin scoping bounds a leaked DEK&#39;s blast radius to one plugin&#39;s slice of one class, and lets a plugin&#39;s data be re-keyed independently.</li><li><strong>Blind-index keys:</strong> a separate HMAC-SHA256 key per (class × plugin), same wrap/store lifecycle — never the DEK itself, so revealing an index key never reveals ciphertext contents.</li><li><strong>Rotation:</strong> <code>sv keys rotate-field-kek</code> re-wraps every DEK under a new KEK — no bulk data re-encryption, minutes not hours. DEK rotation (which <em>does</em> require re-encrypting that DEK&#39;s rows) rides the same machinery as backfill.</li></ul><h3 id="envelope-format" tabindex="-1">Envelope format <a class="header-anchor" href="#envelope-format" aria-label="Permalink to &quot;Envelope format&quot;">​</a></h3><p><code>svf1:&lt;dekId&gt;:&lt;iv&gt;:&lt;tag&gt;:&lt;ciphertext&gt;</code> (base64url segments) — the <code>secrets.ts</code> shape plus an explicit DEK identifier, so rotation and per-plugin keys resolve without guesswork. AAD binds <code>{tenantId, pluginId, class, column}</code> — a ciphertext lifted from one column/plugin/tenant fails authentication anywhere else, even with the right DEK.</p><h3 id="sdk-surface-and-permission" tabindex="-1">SDK surface and permission <a class="header-anchor" href="#sdk-surface-and-permission" aria-label="Permalink to &quot;SDK surface and permission&quot;">​</a></h3><p><code>sdk.crypto.encryptField(value, { sensitivity })</code> / <code>sdk.crypto.decryptField(envelope)</code> — implemented host-side (<code>runtime/src/sdk-host.ts</code>), gated by the <code>crypto:use</code> manifest permission (already reserved in SRS §5). The schema helpers route through the same host implementation; direct SDK calls exist for values that never live in a drizzle column (blob metadata, export payloads).</p><h3 id="search-and-sort-caveats-documented-not-hidden" tabindex="-1">Search and sort caveats (documented, not hidden) <a class="header-anchor" href="#search-and-sort-caveats-documented-not-hidden" aria-label="Permalink to &quot;Search and sort caveats (documented, not hidden)&quot;">​</a></h3><p>Encrypted columns lose <code>LIKE</code>/range/ORDER BY. The supported patterns, in <code>docs/plugin-development.md</code>:</p><ol><li><strong>Blind index</strong> — exact match only (<code>WHERE notes_bidx = hmac(term)</code>).</li><li><strong>Plaintext metadata</strong> — keep non-sensitive dates/categories/IDs unencrypted and filter on those.</li><li><strong>Decrypt-and-filter in the app</strong> — the fallback for fuzzy search, with its cost stated plainly.</li></ol><h3 id="blind-index-rotation-and-the-generic-re-seal-walker-gate-b-design-—-leg-4" tabindex="-1">Blind-index rotation and the generic re-seal walker (gate B design — leg 4) <a class="header-anchor" href="#blind-index-rotation-and-the-generic-re-seal-walker-gate-b-design-—-leg-4" aria-label="Permalink to &quot;Blind-index rotation and the generic re-seal walker (gate B design — leg 4)&quot;">​</a></h3><p>Rotating a blind-index HMAC key invalidates every stored index value: queries hash the search term under the new key while rows still carry old-key HMACs. The leg-4 design makes rotation a bounded, resumable, operator-triggered operation during which <strong>search results stay identical</strong> — never an indefinite both-keys mode.</p><p><strong>Key state (additive migration to <code>field_encryption_keys</code>):</strong></p><ul><li><code>wrapped_hmac_key_previous</code> (nullable) — the outgoing key, retained only during the rotation window.</li><li><code>hmac_rotation_started_at</code> (nullable) — window start; both cleared at completion. A rotation window <strong>exists</strong> iff <code>wrapped_hmac_key_previous</code> is non-null. Scope stays per-(class × plugin), like every key in the hierarchy — one rotation never touches another plugin&#39;s (or class&#39;s) rows.</li></ul><p><strong>Dual-read lives at the query layer, not in stored data.</strong> Plugin schemas stay untouched (one index column). Two additions:</p><ul><li>Host: <code>sdk.crypto</code> gains <code>hashFieldCandidates(value, { sensitivity })</code> → <code>string[]</code> — <code>[new]</code> normally, <code>[new, old]</code> during a window.</li><li><code>@sovereignfs/sdk/drizzle</code> gains <strong><code>blindIndexMatch(column, value, { sensitivity })</code></strong> → a drizzle <code>SQL</code> condition (<code>eq</code> normally, <code>inArray</code> during a window). This becomes the <strong>documented query pattern</strong> for blind indexes; the leg-3 <code>eq(col, await hashField(term))</code> form keeps working but is documented as not rotation-safe. <code>seal()</code> always writes the new key&#39;s HMAC — new rows are findable immediately, old rows via the old candidate until re-sealed.</li></ul><p><strong>The generic re-seal walker — and the registration mechanism enabling it.</strong> The platform cannot see plugin schemas (they are plugin code), so both the rotation re-seal and the leg-4 backfill need plugins to hand the platform their classified tables once:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// once, at plugin server-entry scope (same lifecycle as</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sdk.portability.provideExport — the established registration precedent)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.crypto.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerTables</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entries, otherTable);</span></span></code></pre></div><p>The table objects carry everything the walker needs (table name, column names, sensitivity classes, blind-index sources) via the leg-3 metadata brand. The walker then, per registered table: batch-reads rows, decrypts each <code>svf1</code> source under the (unchanged) DEK with the stored column-name context, recomputes the blind index under the new HMAC key, and updates the row — checkpointed per (table, primary key) so interruption resumes rather than restarts. <code>sv db encrypt-fields</code> (backfill) is the same walker with a different row transform (plaintext → sealed), sharing the checkpoint machinery. A classified table that is never registered is skipped and named in the tool&#39;s output — visible, not silent.</p><p><strong><code>sv keys rotate-blind-index --plugin &lt;id&gt; [--class &lt;cls&gt;]</code>:</strong></p><ol><li><strong>Swap</strong> (one transactional row update): generate + wrap the new HMAC key; <code>previous ← current</code>, <code>current ← new</code>, <code>started_at ← now</code>. Refuses to start over an unfinished window (resume it instead).</li><li><strong>Re-seal</strong> via the walker. Idempotent per row; crash-resume from the checkpoint; <code>--status</code> reports window age and per-table progress.</li><li><strong>Complete</strong>: after a clean full pass, clear <code>previous</code> + <code>started_at</code> — the old key is gone and un-re-sealed rows can no longer exist.</li></ol><p><strong>Never-indefinite enforcement:</strong> boot logs a warning (surfaced in Console via the existing <code>recordWarnings</code> path) whenever a rotation window is older than 7 days. Completion is the tool&#39;s success path, not a separate manual step.</p><p><strong>Interactions:</strong> <code>sv keys rotate-field-kek</code> re-wraps <code>previous</code> HMAC keys too when present (small leg-4 amendment to the leg-1 tool) — KEK rotation and index rotation compose. The unkeyed no-KEK <code>hashField</code> fallback needs no rotation (nothing secret to rotate); enabling a KEK later is a backfill (<code>sv db encrypt-fields</code>), which re-seals indexes as part of its transform.</p><h3 id="what-this-does-not-protect-against" tabindex="-1">What this does not protect against <a class="header-anchor" href="#what-this-does-not-protect-against" aria-label="Permalink to &quot;What this does not protect against&quot;">​</a></h3><p>A compromised <strong>app server</strong> sees plaintext and keys at encrypt/decrypt time — that boundary is Layer 3&#39;s (RFC 0060) job, at Layer 3&#39;s UX cost. The docs state this plainly, mirroring task 8.5&#39;s original review-checklist language (&quot;server-held keys do not defend against a curious operator or RCE&quot; — of the <em>app</em> host).</p><h3 id="docker-config-impact" tabindex="-1">Docker/config impact <a class="header-anchor" href="#docker-config-impact" aria-label="Permalink to &quot;Docker/config impact&quot;">​</a></h3><p>Two new env vars (<code>SOVEREIGN_FIELD_KEK</code>, <code>SOVEREIGN_ENCRYPT_CLASSES</code>) → <code>.env.example</code>, <code>docker-compose.prod.yml</code> pass-through, <code>docs/self-hosting.md</code>, docs-parity test. Set identically on <code>runtime</code> and <code>auth</code>? <strong>No</strong> — auth&#39;s better-auth tables are not plugin schema and are out of scope for this pass (open question 3). No native deps; <code>node:crypto</code> only.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Disk/volume encryption only (status quo).</strong> Protects stolen disks; useless against a live connection. Remains the documented baseline for the disk-theft threat — it is not displaced, just insufficient alone.</li><li><strong>In-database: <code>pgcrypto</code> / <code>pg_tde</code> / <code>pgsodium</code> / RLS.</strong> Rejected in Research 0013 — key exposure via query logging (<code>pgcrypto</code>), non-stock images unavailable on managed Postgres (<code>pg_tde</code>/<code>pgsodium</code>), and all decrypt transparently for any authenticated connection. RLS is access control, superuser-bypassed, not encryption.</li><li><strong>Opt-in per-field with no policy layer</strong> (author alone decides, à la <code>e2ee:use</code>). Simpler, matches precedent — but coverage then depends on every registry author&#39;s diligence, which is exactly the gap. The classification/policy split keeps the author&#39;s knowledge and the operator&#39;s authority separate.</li><li><strong>Per-user DEKs</strong> (task 8.5&#39;s original Tier 3 sketch) instead of per-class×plugin. Stronger isolation, but explodes key count, complicates blind indexes (an index over rows of many users needs one HMAC key anyway), and blocks any legitimate cross-user server-side operation. Per-class×plugin is the tractable middle; per-user protection is Layer 3&#39;s territory.</li><li><strong>Reusing <code>SOVEREIGN_VAULT_KEY</code>.</strong> One less env var, but couples the rotation/compromise story of routine field data to high-value secrets. Rejected.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Taxonomy sign-off.</strong> The four-class enum above is a proposal; it needs kasunben&#39;s explicit approval before the manifest/SDK enum lands (workstream 0011 gate A).</li><li><strong>Blind-index key rotation.</strong> <s>Rotating an HMAC key invalidates every stored index value.</s> Designed — see &quot;Blind-index rotation and the generic re-seal walker&quot; above (gate B; awaiting kasunben&#39;s sign-off before leg 4 starts). One sub-decision is called out there: table registration via <code>sdk.crypto.registerTables()</code> (recommended, mirrors the portability-resolver precedent) rather than a manifest-declared table list or schema-file scanning.</li><li><strong>Auth-store fields.</strong> better-auth owns <code>apps/auth</code>&#39;s schema; classifying its columns means patching generated tables. Deferred — out of scope for this RFC, tracked as a follow-up once the plugin-side machinery is proven.</li><li><strong>Export/backup interaction.</strong> Resolved in leg 3: <code>sdk.crypto</code>&#39;s context falls back to the portability plugin context host-side, so export resolvers <code>open()</code> rows and emit plaintext (documented as a requirement in <code>docs/plugin-development.md</code>); <code>sv backup</code> archives ciphertext.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Documentation-first now. On acceptance: workstream 0011 (four legs = epic tasks 8.31–8.34 — key service, SDK surface + permission, schema helpers + policy, backfill tooling). New SDK exports (<code>encryptedText</code>, <code>blindIndex</code>, <code>sdk.crypto.*</code>) and the new <code>crypto:use</code> permission value are <strong>minor</strong> <code>@sovereignfs/sdk</code> and <code>@sovereignfs/manifest</code> bumps (additive, NFR-04-compliant). No breaking changes for existing plugins or operators: unset policy = current behavior exactly.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>August 2026</td><td>Initial draft</td></tr><tr><td>0.2</td><td>August 2026</td><td>Accepted (developer go-ahead including gate A, the four-class taxonomy); leg 1 (epic task 8.31 — key service) implemented</td></tr><tr><td>0.3</td><td>August 2026</td><td>Leg 2 (epic task 8.32) implemented: <code>sdk.crypto.encryptField()</code>/<code>decryptField()</code>, <code>crypto:use</code> permission, <code>svf1</code> data envelope + <code>svf0</code> passthrough discriminator; decryption deliberately ignores policy so ciphertext outlives a disabled class</td></tr><tr><td>0.4</td><td>August 2026</td><td>Leg 3 (epic task 8.33) implemented, with an approved design amendment: transparent drizzle interception rejected at the leg&#39;s stop condition (sync, identity-blind <code>toDriver</code>; raw client handed to plugins) in favor of declarative classification + one mechanical <code>seal()</code>/<code>open()</code> call per statement + a synchronous <code>toDriver</code> tripwire that makes unsealed writes to classified columns throw. Added <code>sdk.crypto.hashField()</code> (blind-index primitive, unkeyed fallback without a KEK) and the <code>@sovereignfs/sdk/drizzle</code> subpath. Verified end-to-end on live sqld and live Postgres with the same sqlite-core table definition.</td></tr><tr><td>0.5</td><td>August 2026</td><td>Gate B design drafted (pending sign-off): per-key dual-read rotation window (<code>wrapped_hmac_key_previous</code> + <code>hmac_rotation_started_at</code>), query-layer dual-read via <code>hashFieldCandidates()</code>/<code>blindIndexMatch()</code>, the <code>sdk.crypto.registerTables()</code> registration mechanism enabling a generic checkpointed re-seal walker shared by <code>sv keys rotate-blind-index</code> and <code>sv db encrypt-fields</code>, 7-day stale-window boot warning, KEK-rotation composition. Open questions 2 and 4 updated.</td></tr><tr><td>0.6</td><td>August 2026</td><td>Leg 4 (epic task 8.34) implemented per the gate B design, with one strengthening refinement: <code>registerTables()</code> <strong>persists</strong> table metadata (<code>field_table_registrations</code>) so the CLI walker works outside the runtime process. Shipped: rotation window columns (migration 0023), fresh-read HMAC resolution (live rotation visibility), <code>hashFieldCandidates()</code>/<code>blindIndexMatch()</code>, the checkpointed re-seal walker, <code>sv db encrypt-fields</code>, <code>sv keys rotate-blind-index</code> (<code>--status</code>, <code>--force</code> completion guard), KEK-rotation carry-over of previous keys, 7-day stale-window boot warning, and the Console read-only status section. Dual-read continuity and checkpoint resume verified live.</td></tr></tbody></table>`,57)])])}const u=t(o,[["render",n]]);export{g as __pageData,u as default};
