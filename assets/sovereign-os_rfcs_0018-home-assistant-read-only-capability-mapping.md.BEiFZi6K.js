import{_ as t,o,c as a,a2 as i}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"RFC-0018: Home Assistant Read-Only Capability Mapping","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/rfcs/0018-home-assistant-read-only-capability-mapping.md"},"headers":[],"relativePath":"sovereign-os/rfcs/0018-home-assistant-read-only-capability-mapping.md","filePath":"sovereign-os/rfcs/0018-home-assistant-read-only-capability-mapping.md"}'),s={name:"sovereign-os/rfcs/0018-home-assistant-read-only-capability-mapping.md"};function n(r,e,c,d,l,h){return o(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0018-home-assistant-read-only-capability-mapping" tabindex="-1">RFC-0018: Home Assistant Read-Only Capability Mapping <a class="header-anchor" href="#rfc-0018-home-assistant-read-only-capability-mapping" aria-label="Permalink to &quot;RFC-0018: Home Assistant Read-Only Capability Mapping&quot;">​</a></h1><p><strong>Status:</strong> Accepted (2026-08-21, project creator) <strong>Author:</strong> Project creator and Claude <strong>Created:</strong> 2026-08-21 <strong>Reviewers:</strong> Project creator <strong>Target phase:</strong> <a href="./../roadmap/01-2-local-conversation-capabilities.html">Milestone 01.2, Local Conversation and Capabilities</a> §11, Following Vertical Slice; <a href="./../../ROADMAP.html">ROADMAP.md</a> Milestone 5, Home Automation Integration <strong>Supersedes:</strong> None</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Register two read-only Home Assistant capabilities — <code>home_assistant.list_entities</code> and <code>home_assistant.get_history</code> — against <a href="./0003-capability-contract.html">RFC-0003</a>&#39;s contract, reusing <a href="./0017-web-search-and-fetch-capability-mapping.html">RFC-0017</a>&#39;s confirmation pause/resume wire format unchanged. This is deliberately the roadmap&#39;s &quot;first slice&quot; only (discover and read allowlisted entities, answer state and history questions) — allowlisted mutating actions are explicit Non-Goal, left to a future RFC once that slice is actually built, matching this project&#39;s own precedent of splitting a read-only capability mapping from mutation (RFC-0006 did the same for Pi-hole).</p><p>The one genuinely new design question this RFC has to resolve that RFC-0006 and RFC-0017 didn&#39;t: Home Assistant is neither device-local (Pi-hole&#39;s loopback container) nor public-internet (SearXNG&#39;s federated search, an arbitrary fetched URL) — it is a separate, household-owned device on the same LAN. RFC-0003&#39;s <code>network</code> axis only has <code>local</code>/ <code>external</code>, and this RFC does not attempt to add a third tier by fiat (see Proposal, Classification). It also fixes the entity allowlist mechanism the roadmap already named but never specified, and where the Home Assistant connection&#39;s long-lived access token is stored, following this project&#39;s established credential-storage precedent.</p><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p><a href="./../roadmap/01-2-local-conversation-capabilities.html">docs/roadmap/01-2-local-conversation-capabilities.md</a> §11 names the plan directly: &quot;introduce Home Assistant through the same capability executor: 1. discover and read allowlisted entities; 2. answer state and history questions; 3. propose allowlisted actions; 4. require confirmation according to risk; and 5. preserve deterministic authorization outside the model.&quot; <a href="./../../ROADMAP.html">ROADMAP.md</a>&#39;s Milestone 5 entry narrows the first slice further: &quot;read-only Home Assistant entity discovery, state, and history. Later slices may propose allowlisted actions... The model never receives unrestricted Home Assistant, shell, Docker, or network access.&quot; <a href="./0003-capability-contract.html">RFC-0003</a> already anticipated this by name in its own Non-Goals (&quot;Home Assistant control is explicit non-scope&quot; for that milestone) and Compatibility section (a later capability &quot;Home Assistant, Milestone 5&quot; reuses its classification model), and <a href="./0017-web-search-and-fetch-capability-mapping.html">RFC-0017</a>&#39;s own Compatibility section built its confirmation wire format &quot;intentionally general... so Milestone 5&#39;s future mutating Home Assistant capabilities reuse it rather than needing a second design.&quot;</p><p>None of that is a capability mapping yet. Four concrete gaps block items 1–2 above from being buildable, none hypothetical:</p><ol><li><strong>No capability is registered.</strong> <code>sovereign_conversation.py</code>&#39;s <code>build_registry()</code> has no Home Assistant entry at all today (confirmed by direct code inspection).</li><li><strong>No classification decision.</strong> RFC-0003&#39;s Confirmation Model table is keyed on <code>side_effect</code> × <code>network</code>, and <code>network</code> is <code>local</code> or <code>external</code>, defined as &quot;<code>local</code> capabilities never leave the device.&quot; A request to a separate Home Assistant host genuinely leaves this device&#39;s own process boundary — but it never leaves the household&#39;s own network the way <code>web.search</code>/<code>web.fetch</code> do. RFC-0003 doesn&#39;t contemplate this middle case, and nothing else in this project has needed to before (Pi-hole is a loopback-only container on the same device; SearXNG/llama.cpp are also device-local containers). This RFC has to decide how to classify a same-LAN, different-host, household-owned service under the existing two-value axis, not invent a third value unilaterally.</li><li><strong>No allowlist mechanism.</strong> The roadmap says &quot;allowlisted entities&quot; twice without specifying where that allowlist lives, who populates it, or how a capability&#39;s <code>entity_id</code> argument gets checked against it. Without this, a naive implementation would either expose every Home Assistant entity (locks, cameras, presence/person trackers — a real privacy and safety miss for a household product) or hardcode a household-specific list at build time, which is not renewable configuration.</li><li><strong>No credential storage decision.</strong> Reading Home Assistant&#39;s state requires a bearer token (its own long-lived access token mechanism, confirmed directly against Home Assistant&#39;s REST API documentation — see Context and Evidence). This project already has an established, qualified pattern for exactly this class of problem (<code>pihole-admin-password</code>), and a naive implementation would either reinvent it worse or skip the question entirely.</li></ol><h2 id="goals" tabindex="-1">Goals <a class="header-anchor" href="#goals" aria-label="Permalink to &quot;Goals&quot;">​</a></h2><ul><li>Register <code>home_assistant.list_entities</code> and <code>home_assistant.get_history</code> against RFC-0003&#39;s contract: schemas, classification, and bounds.</li><li>Decide how a same-LAN, different-host, household-owned service is classified under RFC-0003&#39;s existing <code>local</code>/<code>external</code> axis, without silently amending that Accepted RFC&#39;s structural model.</li><li>Fix the entity allowlist: where it&#39;s stored, its shape, and exactly which pipeline stage rejects a non-allowlisted <code>entity_id</code> and why that stage (before or after the confirmation prompt).</li><li>Fix where the Home Assistant base URL and access token are stored, matching this project&#39;s existing secret-storage precedent (<code>pihole-admin-password</code>) rather than inventing a new one.</li><li>Resolve <a href="./0017-web-search-and-fetch-capability-mapping.html">RFC-0017</a>&#39;s own deferred Unresolved Question — &quot;whether <code>/data/sovereign/capabilities/policy.json</code> should hold other future opt-in toggles... deferred until Milestone 5 or another feature needs its own toggle&quot; — since that moment is now.</li><li>Reuse RFC-0017&#39;s confirmation pause/resume wire format unchanged, confirming in practice the generality that RFC&#39;s own Compatibility section already claimed for it.</li></ul><h2 id="non-goals" tabindex="-1">Non-Goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-Goals&quot;">​</a></h2><ul><li><strong>Allowlisted mutating actions</strong> (&quot;propose allowlisted actions; require confirmation according to risk&quot; — roadmap items 3–4). This is explicitly the roadmap&#39;s <em>next</em> slice after the one this RFC covers, matching RFC-0006&#39;s own precedent of shipping Pi-hole read-only first and naming mutation as separate future work. A service-call capability (<code>POST /api/services/&lt;domain&gt;/&lt;service&gt;</code> in Home Assistant&#39;s own REST API) genuinely changes physical household state (locks, switches, climate) and deserves its own RFC once this slice is stable, not a bundled afterthought here.</li><li><strong>Amending RFC-0003&#39;s <code>network</code> classification axis</strong> to add a distinct &quot;local-network&quot; tier between <code>local</code> and <code>external</code>. Real tension with that binary is named directly below (Proposal, Classification, and Drawbacks) but resolving it is a structural change to an Accepted RFC&#39;s contract, out of this mapping RFC&#39;s scope — exactly the posture RFC-0017 already took toward a different tension in the same table (see RFC-0017&#39;s Alternatives Considered, &quot;auto-approve after first opt-in&quot;).</li><li><strong>Deploying, installing, or managing Home Assistant itself.</strong> Unlike Pi-hole, SearXNG, and llama.cpp — all embedded in Sovereign&#39;s own base image and lifecycle-managed by <code>sovereign-update</code> — Home Assistant is a third-party, household-provided system this project does not run, version, or update. This RFC assumes a reachable instance already exists on the household LAN; provisioning one is entirely outside Sovereign&#39;s control and this RFC&#39;s scope.</li><li><strong>The WebSocket API</strong>, Home Assistant&#39;s other real-time interface. RFC-0003&#39;s executor model is per-invocation request/response (<code>invoke()</code> returns once); a persistent subscribed connection doesn&#39;t map onto that shape without a structural change to the executor itself, which this RFC doesn&#39;t propose. The REST API&#39;s <code>/api/states</code> and <code>/api/history/period</code> already answer the roadmap&#39;s &quot;state and history questions&quot; scope without it.</li><li><strong>Console&#39;s actual settings UI</strong> (the page where a household enters the base URL/token and picks allowlisted entities) — this RFC fixes the API contract that UI depends on, matching how RFC-0006/RFC-0017 fixed their own API contracts without building Console UI themselves. Necessary follow-up implementation, named in Acceptance Criteria, not designed here.</li><li>Re-deriving the confirmation <em>architecture</em> or wire format — both already fixed by RFC-0004 and RFC-0017 respectively. This RFC only confirms both capabilities land on RFC-0003&#39;s existing <code>read_only</code>/<code>external</code>/<code>required</code> row, reusing the mechanism as-is.</li></ul><h2 id="context-and-evidence" tabindex="-1">Context and Evidence <a class="header-anchor" href="#context-and-evidence" aria-label="Permalink to &quot;Context and Evidence&quot;">​</a></h2><ul><li><a href="./../roadmap/01-2-local-conversation-capabilities.html">docs/roadmap/01-2-local-conversation-capabilities.md</a> §11 and <a href="./../../ROADMAP.html">ROADMAP.md</a> Milestone 5 — the direct source of this RFC&#39;s scope, quoted in Problem above.</li><li><a href="./0003-capability-contract.html">RFC-0003</a> (Accepted): the six-stage executor, the Confirmation Model table, and its own explicit anticipation of Home Assistant as a future capability reusing this contract, without specifying how.</li><li><a href="./0017-web-search-and-fetch-capability-mapping.html">RFC-0017</a> (Draft, not yet formally accepted at the time of writing, but already implemented and hardware-smoke-tested per its own Acceptance Criteria strikethrough notes): the confirmation pause/resume wire format (<code>pending_confirmation</code>/<code>confirmation</code> fields on <code>POST /api/v1/conversation/message</code>) this RFC reuses unchanged, and its own deferred question about <code>policy.json</code>&#39;s scope, resolved here.</li><li><code>image-builder/sovereign/appliance/lib/sovereign_pihole.py</code>, read directly: <code>PIHOLE_PASSWORD_PATH = /data/sovereign/secrets/pihole-admin-password</code>, read by <code>sovereign-conversation.service</code> itself (not a separate root-run script) at call time — the direct precedent for how a <code>DynamicUser</code> service in this project reads a credential.</li><li><code>image-builder/sovereign/layer/sovereign-proof.rootfs-overlay/etc/systemd/system/sovereign-conversation.service</code>, read directly: <code>SupplementaryGroups=sovereign-pihole-secrets</code> is how that credential read access is actually granted — a sysusers.d group created by a root-owned bootstrap step, not something the service grants itself. <code>ReadWritePaths=/data/sovereign/capabilities</code> is the existing, already-granted writable path <code>policy.json</code> and the audit log already use.</li><li>Home Assistant&#39;s REST API, confirmed directly against its own developer documentation (<code>developers.home-assistant.io/docs/api/rest/</code>): bearer-token authentication (<code>Authorization: Bearer &lt;token&gt;</code>), a long-lived access token created by the household through their own Home Assistant profile page (Sovereign never generates or manages this token, only stores what the household pastes in — the same posture this project already takes toward not touching signing/production keys it doesn&#39;t own, per <a href="./../adrs/0006-production-signing-key-custody.html">ADR-0006</a>&#39;s precedent for a different secret); <code>GET /api/states</code> (all entities, each with <code>entity_id</code>/<code>state</code>/<code>attributes</code>/<code>last_changed</code>); <code>GET /api/states/&lt;entity_id&gt;</code> (one entity); <code>GET /api/history/period/&lt;timestamp&gt;?filter_entity_id=&lt;id&gt;</code> (history, ISO timestamp, entity-scoped); <code>POST /api/services/&lt;domain&gt;/&lt;service&gt;</code> (service calls — the mutating-action surface this RFC explicitly does not map). Default port <code>8123</code>; base URL shape <code>http://&lt;host&gt;:8123/api/</code>.</li><li>Home Assistant&#39;s own local-network discovery: it advertises itself via Zeroconf/mDNS under the <code>_home-assistant._tcp.local.</code> service type (confirmed via Home Assistant&#39;s developer network-discovery documentation and community references), the same class of mechanism this project already relies on for its own device (<code>sovereign.local</code>, via <code>avahi-daemon</code>/<code>libnss-mdns</code>, per the Phase 01 image&#39;s own package set). This RFC treats discovery as a Console-UI convenience (pre-filling a base URL, not authenticating or trusting anything by itself) rather than a capability-executor concern — see Proposal.</li><li>Entity ID format (<code>domain.object_id</code>, e.g. <code>light.kitchen</code>, <code>sensor.living_room_temperature</code>) is a stable, long-documented Home Assistant convention this RFC relies on for allowlist and argument validation.</li></ul><h2 id="proposal" tabindex="-1">Proposal <a class="header-anchor" href="#proposal" aria-label="Permalink to &quot;Proposal&quot;">​</a></h2><h3 id="classification" tabindex="-1">Classification <a class="header-anchor" href="#classification" aria-label="Permalink to &quot;Classification&quot;">​</a></h3><p>Both capabilities are <code>read_only</code> — neither changes Home Assistant state. For <code>network</code>, this RFC classifies them <strong><code>external</code></strong>, taking RFC-0003&#39;s existing definition at its word (&quot;<code>local</code> capabilities never leave the device&quot;) rather than reading a same-household-LAN exception into it. A request to Home Assistant genuinely leaves this device&#39;s own process and network-namespace boundary to reach a different physical host — the same fact pattern RFC-0003&#39;s definition describes, even though the destination never leaves the household&#39;s own network the way <code>web.search</code>/<code>web.fetch</code> do. Per RFC-0003&#39;s Confirmation Model table, <code>read_only</code>/<code>external</code> is <code>required</code> confirmation — structurally derived, not a choice this RFC makes independently.</p><p>This is a deliberate, disclosed tradeoff, not an oversight: it means &quot;what&#39;s the temperature in the living room,&quot; a fully local, household-owned, ambient query, costs the same per-invocation confirmation click as a query that reaches the public internet. The Alternatives Considered and Drawbacks sections below name this tension directly rather than resolving it by quietly stretching RFC-0003&#39;s <code>local</code> definition to cover a case it wasn&#39;t written for.</p><h3 id="home-assistant-list-entities" tabindex="-1"><code>home_assistant.list_entities</code> <a class="header-anchor" href="#home-assistant-list-entities" aria-label="Permalink to &quot;\`home_assistant.list_entities\`&quot;">​</a></h3><ul><li><strong>Purpose:</strong> answer &quot;what is it&quot; and &quot;what&#39;s it doing right now&quot; questions (&quot;discover and read allowlisted entities&quot;) by returning the current state of every entity the household has allowlisted.</li><li><strong>Arguments:</strong> none. The full allowlisted set is small by construction (see Entity Allowlist below) and returning all of it in one call avoids needing a second &quot;discover the allowlist&quot; round trip before a &quot;read state&quot; round trip — the same reasoning RFC-0017 gave for not splitting <code>web.search</code>&#39;s query and result-count into separate capabilities.</li><li><strong>Result:</strong> <code>{ &quot;entities&quot;: [ { &quot;entity_id&quot;: string, &quot;friendly_name&quot;: string, &quot;domain&quot;: string, &quot;state&quot;: string, &quot;unit_of_measurement&quot;: string | null, &quot;last_changed&quot;: timestamp } ], &quot;retrieved_at&quot;: timestamp }</code>. Fields mirror what <code>GET /api/states</code> already returns per entity, narrowed to what a conversation needs — no raw <code>attributes</code> blob passed through unfiltered, since Home Assistant&#39;s own <code>attributes</code> object can contain fields well beyond what a household intends to expose conversationally (e.g. device identifiers, supported-feature bitmasks).</li><li><strong>Classification:</strong> <code>read_only</code>, <code>external</code>, <code>required</code> confirmation.</li><li><strong>Bounds:</strong> <code>timeout_seconds=8</code> (a same-LAN request to a real second host — meaningfully faster than the public internet round trip <code>web.fetch</code> budgets for, but not the near-zero latency of a loopback container <code>pihole.summary</code> assumes), <code>max_result_bytes</code> left at <code>DEFAULT_MAX_RESULT_BYTES</code> (64KiB — comfortably enough for a reasonably-sized allowlist; a household approaching this limit has an allowlist large enough to warrant its own review, not a bigger byte budget), <code>max_invocations_per_turn=1</code>, matching every other registered capability&#39;s default.</li></ul><h3 id="home-assistant-get-history" tabindex="-1"><code>home_assistant.get_history</code> <a class="header-anchor" href="#home-assistant-get-history" aria-label="Permalink to &quot;\`home_assistant.get_history\`&quot;">​</a></h3><ul><li><strong>Purpose:</strong> answer &quot;what happened&quot; and &quot;when did X last change&quot; questions for one specific allowlisted entity.</li><li><strong>Arguments:</strong> <code>{ &quot;entity_id&quot;: string (required, must be in the allowlist), &quot;period&quot;: &quot;hour&quot; | &quot;day&quot; | &quot;week&quot; (required) }</code>. A bounded enum, not a free-form date range — the same &quot;bounded, enumerated argument set&quot; precedent RFC-0006 established for Pi-hole&#39;s own <code>period</code> argument and RFC-0017 explicitly cited for <code>web.search</code>&#39;s <code>time_range</code>. A free-form range would need its own parsing/validation surface and could request an unbounded amount of history from Home Assistant&#39;s own database for no scope benefit this milestone&#39;s actual questions need. (<code>period</code> maps to <code>/api/history/period/&lt;timestamp&gt;</code>&#39;s required timestamp by computing &quot;now minus one hour/day/week&quot; at invocation time — an implementation detail, not an architectural one, the same class of deferral RFC-0017 already made for its own extraction-method specifics.)</li><li><strong>Result:</strong> <code>{ &quot;entity_id&quot;: string, &quot;period&quot;: string, &quot;changes&quot;: [ { &quot;state&quot;: string, &quot;changed_at&quot;: timestamp } ], &quot;retrieved_at&quot;: timestamp }</code>. <code>changes</code> is capped at 50 entries — plenty for a conversational answer (&quot;it&#39;s changed state 4 times today&quot;) without becoming a raw data export, the same &quot;summary endpoint by design, not an export&quot; bound RFC-0006 and RFC-0017 both already applied to their own list-shaped results.</li><li><strong>Classification:</strong> <code>read_only</code>, <code>external</code>, <code>required</code> confirmation.</li><li><strong>Bounds:</strong> <code>timeout_seconds=10</code> (a real database query on the Home Assistant side, not a simple in-memory state read — deliberately longer than <code>list_entities</code>&#39;s budget), <code>max_result_bytes</code> left at <code>DEFAULT_MAX_RESULT_BYTES</code>, <code>max_invocations_per_turn=1</code>.</li></ul><h3 id="entity-allowlist" tabindex="-1">Entity Allowlist <a class="header-anchor" href="#entity-allowlist" aria-label="Permalink to &quot;Entity Allowlist&quot;">​</a></h3><ul><li>A new persisted file, <code>/data/sovereign/capabilities/home-assistant.json</code>, holds <code>{ &quot;enabled&quot;: bool, &quot;base_url&quot;: string, &quot;allowlisted_entities&quot;: [ string ] }</code>. Kept <strong>separate</strong> from <code>policy.json</code> — this directly resolves RFC-0017&#39;s own deferred Unresolved Question. <code>policy.json</code> stays a flat single-flag file (<code>web_search_enabled</code>); Home Assistant&#39;s configuration is structurally bigger (a connection endpoint and a list, not one bool) and belongs in its own file rather than growing <code>policy.json</code> into a general, ever-expanding device-config blob. Both files live under the same already-granted, already-lazily-created <code>/data/sovereign/capabilities/</code> directory (<code>ReadWritePaths=</code> unchanged from today) — no new systemd grant needed for this file.</li><li><strong>Default: <code>enabled: false</code>, empty allowlist.</strong> Matching every other <code>external</code> capability&#39;s conservative default in this project (<code>web_search_enabled</code> defaults <code>false</code>) and the milestone plan&#39;s own &quot;allowlisted&quot; framing — a fresh device exposes nothing until the household explicitly configures it.</li><li><strong>Allowlist membership is checked at RFC-0003&#39;s stage 3 (policy check), before any confirmation is generated and before any request reaches Home Assistant.</strong> This is a deliberate, different choice from <code>web.fetch</code>&#39;s SSRF check, which happens at stage 5 (execute) because it needs a live DNS resolution to prevent a rebinding race between check and connect. Allowlist membership needs no network call at all — it&#39;s a pure comparison against already-loaded local configuration — so checking it as early as possible gives the best user experience: a proposal for a non-allowlisted <code>entity_id</code> is rejected immediately with a specific <code>ENTITY_NOT_ALLOWLISTED</code> error, the model can tell the user why, and no confirmation prompt is ever generated for a request that was always going to fail. This mirrors <code>web.search</code>&#39;s own &quot;policy off → rejected before confirmation&quot; precedent exactly, applied to a per-argument check instead of a whole-capability toggle.</li><li><strong>Populating the allowlist is a Console-settings concern, not a capability.</strong> The model never sees Home Assistant&#39;s full, unfiltered entity list — only whatever the household has already allowlisted. Building that allowlist requires a household member to browse the full set once, in Console&#39;s own settings UI, which needs its own authenticated proxy endpoint (<code>GET /api/v1/conversation/home-assistant/entities</code>, forwarding <code>GET /api/states</code> with the stored token) distinct from the two model-facing capabilities above — named in Acceptance Criteria as necessary follow-up, not built here, matching how RFC-0006 and RFC-0017 both fixed their own settings-adjacent API shape without building the settings page itself.</li></ul><h3 id="credential-storage" tabindex="-1">Credential Storage <a class="header-anchor" href="#credential-storage" aria-label="Permalink to &quot;Credential Storage&quot;">​</a></h3><ul><li>The Home Assistant long-lived access token is stored at <code>/data/sovereign/secrets/home-assistant/access-token</code>, <strong>not</strong> inside <code>home-assistant.json</code> alongside the non-sensitive <code>base_url</code>/allowlist fields — the same separation-of-secret-from-config this project already applies to <code>policy.json</code> vs. <code>pihole-admin-password</code>.</li><li>Unlike <code>pihole-admin-password</code> (generated by a root-run <code>start-pihole</code> script, then read by <code>sovereign-conversation.service</code> via the <code>sovereign-pihole-secrets</code> sysusers.d group), Home Assistant&#39;s token is not something Sovereign generates — the household creates it themselves in their own Home Assistant instance and submits it through Console. Since <code>sovereign-conversation.service</code> is therefore both the writer (accepting the household&#39;s submission through its own authenticated endpoint) and the only reader, this needs no new sysusers.d group or root-run bootstrap step — a new <code>ReadWritePaths=/data/sovereign/secrets/home-assistant</code> entry on the existing unit is sufficient, and the directory is lazily self-created the same way <code>/data/sovereign/capabilities/</code> already is (RFC-0017&#39;s own precedent, cited there against inventing a <code>tmpfiles.d</code> entry). This is deliberately a <strong>separate</strong> directory from <code>/data/sovereign/secrets/pihole-admin-password</code>&#39;s own — scoping this service&#39;s new write access to only its own subdirectory, so a compromised <code>sovereign-conversation.service</code> process gains no new ability to overwrite Pi-hole&#39;s credential, which it does not need write access to today and still won&#39;t.</li><li><code>GET /api/v1/conversation/home-assistant</code> (the config-read endpoint) never echoes the token value back — only <code>{ &quot;enabled&quot;: bool, &quot;base_url&quot;: string, &quot;has_access_token&quot;: bool, &quot;allowlisted_entities&quot;: [string] }</code>. The same &quot;never log or return a secret verbatim&quot; posture this project&#39;s audit-log design already applies to capability arguments.</li><li><code>POST /api/v1/conversation/home-assistant</code> accepts <code>base_url</code>, <code>access_token</code> (optional — omitted means &quot;leave the stored token unchanged,&quot; so re-saving the allowlist doesn&#39;t require re-pasting the token every time), and <code>allowlisted_entities</code>, authenticated identically to <code>/message</code> and <code>/policy</code> (delegating to console-auth&#39;s <code>verify-mutating</code>).</li></ul><h2 id="interfaces-and-data-flow" tabindex="-1">Interfaces and Data Flow <a class="header-anchor" href="#interfaces-and-data-flow" aria-label="Permalink to &quot;Interfaces and Data Flow&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Model proposes home_assistant.list_entities({})       [RFC-0004 flow]</span></span>
<span class="line"><span>    -&gt; RFC-0003 executor stages 1-3: resolve, validate arguments</span></span>
<span class="line"><span>       (no arguments to validate for list_entities),</span></span>
<span class="line"><span>       check policy (enabled must be true, read fresh from</span></span>
<span class="line"><span>       /data/sovereign/capabilities/home-assistant.json each request)</span></span>
<span class="line"><span>    -&gt; stage 3 fails (disabled) -&gt; rejection appended to context</span></span>
<span class="line"><span>    -&gt; stage 3 passes, confirmation required (structural, both</span></span>
<span class="line"><span>       capabilities) -&gt; confirmation_store.issue() -&gt; round halts,</span></span>
<span class="line"><span>       pending_confirmation returned [RFC-0017&#39;s existing wire format,</span></span>
<span class="line"><span>       unchanged]</span></span>
<span class="line"><span>    -&gt; Console prompts using the same confirmation card RFC-0017 already</span></span>
<span class="line"><span>       built: &quot;home_assistant.list_entities wants to read your allowlisted</span></span>
<span class="line"><span>       Home Assistant entities — this leaves your device. Approve?&quot;</span></span>
<span class="line"><span>    -&gt; user approves -&gt; client resubmits with</span></span>
<span class="line"><span>       {&quot;confirmation&quot;: {&quot;token&quot;: &quot;...&quot;, &quot;approve&quot;: true}}</span></span>
<span class="line"><span>    -&gt; confirmation_store.consume() -&gt; invoke() proceeds through stages</span></span>
<span class="line"><span>       5-6: GET &lt;base_url&gt;/api/states with the stored bearer token,</span></span>
<span class="line"><span>       filtered to allowlisted_entities, shaped to the declared</span></span>
<span class="line"><span>       result_schema -&gt; appended to context</span></span>
<span class="line"><span></span></span>
<span class="line"><span>home_assistant.get_history({&quot;entity_id&quot;: &quot;...&quot;, &quot;period&quot;: &quot;day&quot;}):</span></span>
<span class="line"><span>    -&gt; stage 2 validates entity_id/period against their argument schema</span></span>
<span class="line"><span>       (shape only, no allowlist knowledge)</span></span>
<span class="line"><span>    -&gt; stage 3 additionally checks entity_id against the allowlist</span></span>
<span class="line"><span>       (ENTITY_NOT_ALLOWLISTED if absent) -- before stage 4&#39;s confirmation</span></span>
<span class="line"><span>       gate is ever reached, so an out-of-allowlist request never</span></span>
<span class="line"><span>       generates a prompt</span></span>
<span class="line"><span>    -&gt; stage 5: GET &lt;base_url&gt;/api/history/period/&lt;timestamp&gt;</span></span>
<span class="line"><span>       ?filter_entity_id=&lt;entity_id&gt;, shaped to the declared result_schema</span></span></code></pre></div><h2 id="security-and-privacy" tabindex="-1">Security and Privacy <a class="header-anchor" href="#security-and-privacy" aria-label="Permalink to &quot;Security and Privacy&quot;">​</a></h2><ul><li><strong>Every</strong> invocation of either capability requires per-invocation user confirmation, structurally, per RFC-0003&#39;s table — there is no automatic path, matching <code>web.search</code>/<code>web.fetch</code>&#39;s own posture. A prompt-injected model can cause a proposal, never an execution.</li><li>The entity allowlist is the primary privacy control: a household that has a Home Assistant <code>person</code>/<code>device_tracker</code> entity (presence, location), a <code>lock</code>/<code>alarm_control_panel</code> entity, or a camera entity simply never adds it to the allowlist, and the model can never discover, name, or query it — not merely &quot;the model is asked not to,&quot; but structurally absent from every result this capability can ever return.</li><li>The allowlist check happening before confirmation (see Entity Allowlist above) means a proposal for a non-allowlisted entity never even discloses that entity&#39;s existence in a confirmation prompt — the rejection is generic (<code>ENTITY_NOT_ALLOWLISTED</code>), not &quot;entity X exists but isn&#39;t allowlisted,&quot; which would itself leak information about the household&#39;s device inventory to anything reading the conversation transcript.</li><li>The access token is a real credential (bearer access to whatever the household&#39;s own Home Assistant instance permits, potentially much broader than the specific allowlisted entities this RFC&#39;s capabilities ever query) — stored outside conversation context entirely, never passed to or visible from the model, matching <code>pihole-admin-password</code>&#39;s own handling.</li><li>Audit events follow RFC-0003 unchanged: the fact of invocation, classification, outcome, and which stage was reached — never entity state values or history content, which is real household activity data at least as sensitive as the search-query content RFC-0017 already refused to log.</li><li>Unlike <code>web.fetch</code>, the destination (<code>base_url</code>) is household-configured and never model-influenced, so no SSRF-style resolve-then-check policy is needed here — the only untrusted, model-supplied input is <code>entity_id</code>/<code>period</code>, both closed, bounded values checked against server-held state before any request is made.</li></ul><h2 id="failure-and-recovery" tabindex="-1">Failure and Recovery <a class="header-anchor" href="#failure-and-recovery" aria-label="Permalink to &quot;Failure and Recovery&quot;">​</a></h2><ul><li>Home Assistant unreachable or returning a non-2xx response: fails through RFC-0003&#39;s normal bounded-execution failure path, audited like any other capability failure, narrated by the model as a failure (&quot;I couldn&#39;t reach Home Assistant right now&quot;) — not a fabricated empty-state answer.</li><li><code>enabled: true</code> but <code>base_url</code>/<code>access_token</code> not yet configured (a household turned the feature on before finishing setup, or Console hasn&#39;t been used yet): rejected at stage 3 with a distinct <code>CAPABILITY_NOT_CONFIGURED</code> code, separate from <code>CAPABILITY_DISABLED</code>, so the model can narrate the actual situation (&quot;Home Assistant isn&#39;t set up yet&quot;) instead of a generic &quot;disabled&quot; message that would be misleading once the household believes they&#39;ve turned it on.</li><li>An <code>entity_id</code> that was allowlisted but has since been removed or renamed on the Home Assistant side (a real, expected drift case — Home Assistant entities can be deleted or renamed independent of Sovereign) surfaces as a normal bounded-execution failure (Home Assistant&#39;s own 404 from <code>/api/states/&lt;entity_id&gt;</code>) once past the allowlist check — Sovereign&#39;s allowlist does not attempt to stay synchronized with Home Assistant&#39;s own entity registry in real time; this is a disclosed, accepted limitation of a locally-cached allowlist rather than a live query against Home Assistant on every check.</li><li>A confirmation token that expires while genuinely pending, or a <code>sovereign-conversation</code> process restart invalidating an in-memory token, behaves identically to RFC-0017&#39;s own already-implemented and qualified handling — no new behavior, reusing the same mechanism.</li></ul><h2 id="compatibility-and-migration" tabindex="-1">Compatibility and Migration <a class="header-anchor" href="#compatibility-and-migration" aria-label="Permalink to &quot;Compatibility and Migration&quot;">​</a></h2><p>No existing Home Assistant capability to migrate from. Both new capabilities and the <code>home-assistant.json</code>/credential-directory additions are purely additive: no change to <code>POST /api/v1/conversation/message</code>&#39;s wire format beyond what RFC-0017 already shipped (the <code>pending_confirmation</code>/<code>confirmation</code> fields), verified by the existing <code>test_conversation_service.py</code> suite continuing to pass unmodified. A future allowlisted-actions RFC can reuse this RFC&#39;s allowlist storage shape and confirmation reuse pattern rather than needing a third design.</p><h2 id="operations-and-observability" tabindex="-1">Operations and Observability <a class="header-anchor" href="#operations-and-observability" aria-label="Permalink to &quot;Operations and Observability&quot;">​</a></h2><ul><li><code>home-assistant.json</code>&#39;s <code>enabled</code>/<code>base_url</code>/<code>allowlisted_entities</code> (never the token) should be inspectable the same way <code>policy.json</code> already is — a future Console settings panel reads and writes it, not a separate config path, per RFC-0003&#39;s own Operations precedent.</li><li>The capability audit log records how often each Home Assistant capability is proposed, approved, denied, or rejected by policy or allowlist — the same operational signal RFC-0017 already named for judging whether a confirmation-gated capability&#39;s UX is actually working in practice, applicable here without change.</li></ul><h2 id="testing-strategy" tabindex="-1">Testing Strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to &quot;Testing Strategy&quot;">​</a></h2><ul><li>Contract-level tests mirroring RFC-0006/RFC-0017&#39;s pattern: argument validation rejects a malformed <code>entity_id</code> (wrong <code>domain.object_id</code> shape) and an invalid <code>period</code> value, both before any allowlist or network concern is reached.</li><li>Allowlist tests: an <code>entity_id</code> absent from <code>allowlisted_entities</code> is rejected with <code>ENTITY_NOT_ALLOWLISTED</code> at stage 3, before a confirmation is generated — verified directly, the same structural proof RFC-0017 required for <code>external_enabled: false</code> rejecting before confirmation.</li><li>Policy-gate tests: <code>enabled: false</code> (the real default) rejects both capabilities before confirmation; <code>enabled: true</code> with an unset <code>base_url</code>/token produces <code>CAPABILITY_NOT_CONFIGURED</code>, distinctly from <code>CAPABILITY_DISABLED</code>.</li><li>Confirmation wire-format tests confirming RFC-0017&#39;s existing mechanism is reused correctly for a second pair of capabilities — no new wire-format code path is exercised, only a second registration consuming the same one.</li><li>Fixture-backed tests for both capabilities&#39; Home Assistant-calling happy path (a stub HTTP server returning realistic <code>/api/states</code>/ <code>/api/history/period</code> shapes) without requiring a live Home Assistant instance for every test run — mirroring RFC-0006/RFC-0017&#39;s own testing strategy.</li><li>Credential-handling tests: <code>GET /api/v1/conversation/home-assistant</code> never includes the token value in its response body, verified directly against the raw JSON, not merely against a client that happens not to render it.</li><li>Real-hardware qualification is explicitly <strong>dependent on a real Home Assistant instance being reachable on the project&#39;s own household network</strong> — unlike Pi-hole/SearXNG/llama.cpp, this is not something Sovereign provisions itself as part of qualification, so this RFC cannot commit to a qualification timeline the way RFC-0017 could for a capability backed by an embedded, Sovereign-managed service.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Classify Home Assistant as <code>local</code></strong>, reasoning that &quot;same household network&quot; is functionally equivalent to &quot;same device&quot; for privacy purposes. Rejected: RFC-0003&#39;s own text defines <code>local</code> as &quot;never leaves the device,&quot; not &quot;never leaves the household&quot; — stretching that definition here would be an undisclosed, ad hoc amendment to an Accepted RFC&#39;s structural contract, exactly the kind of scattered judgment call RFC-0003 itself rejected as an alternative during its own drafting (&quot;let each capability define its own confirmation requirement&quot;). If this classification proves too conservative in practice, RFC-0003 names its own Drawbacks section as the place to revisit it deliberately — not this mapping RFC.</li><li><strong>Auto-approve after a first per-conversation opt-in</strong>, rather than per-invocation confirmation, to reduce the UX cost of the <code>external</code> classification above. Rejected for the identical reason RFC-0017 rejected it for <code>web.search</code>: RFC-0003&#39;s confirmation model already fixed &quot;freshly-obtained... never a standing blanket approval&quot; as structural.</li><li><strong>Store the access token inside <code>home-assistant.json</code></strong> alongside the non-sensitive configuration, for a simpler single-file design. Rejected: this project already treats secrets and config as separately-stored, separately-permissioned classes of data (<code>policy.json</code> vs. <code>pihole-admin-password</code>); mixing them here would be a regression from established practice for no real simplification benefit.</li><li><strong>Use the WebSocket API for a live-updating entity view.</strong> Considered, since it&#39;s Home Assistant&#39;s more idiomatic real-time interface, but rejected for this RFC: RFC-0003&#39;s executor is a per-invocation request/response model, and a persistent subscription doesn&#39;t fit it without a structural executor change this RFC doesn&#39;t propose. The REST API fully answers &quot;discover and read&quot; and &quot;answer state and history questions&quot; without needing a live connection.</li><li><strong>A free-form date-range argument for <code>get_history</code></strong> instead of a bounded <code>period</code> enum. Rejected for the same reason RFC-0006 and RFC-0017 both chose bounded enums over free-form ranges: it&#39;s real additional parsing/validation surface and an unbounded-size result risk for a milestone whose actual questions (&quot;what happened today,&quot; &quot;when did it last change&quot;) are all satisfied by a small closed set.</li></ul><h2 id="drawbacks-and-maintenance-cost" tabindex="-1">Drawbacks and Maintenance Cost <a class="header-anchor" href="#drawbacks-and-maintenance-cost" aria-label="Permalink to &quot;Drawbacks and Maintenance Cost&quot;">​</a></h2><ul><li>The <code>external</code> classification (see Alternatives Considered) means even the most mundane, fully-local, household-owned query — &quot;what&#39;s the temperature&quot; — costs the same confirmation round trip as a query that genuinely reaches the public internet. This is the single biggest, disclosed UX cost of this RFC&#39;s design, inherited directly from RFC-0003&#39;s existing structural model rather than introduced by this RFC. If real usage after this ships shows this makes the read-only slice too friction-heavy to be useful — the exact tension RFC-0003&#39;s own Drawbacks section already flagged in the abstract for &quot;read-only external capabilities used often&quot; — the right fix is a deliberate RFC-0003 amendment informed by that real evidence, not a workaround invented here.</li><li>A second, separate persisted-config file (<code>home-assistant.json</code>) and a second, separate secrets directory add real, ongoing surface area beyond what <code>policy.json</code>/<code>pihole-admin-password</code> already established — justified here by genuinely different data shapes (a connection endpoint and a list, not one bool; a household-submitted token, not a Sovereign-generated one), not by convenience.</li><li>The locally-cached allowlist can drift from Home Assistant&#39;s own entity registry (see Failure and Recovery) — an accepted, disclosed limitation rather than building live allowlist-registry synchronization this milestone&#39;s actual scope doesn&#39;t need.</li><li>Real-hardware qualification depends on external infrastructure (a household&#39;s own Home Assistant instance) this project doesn&#39;t control, unlike every prior capability&#39;s qualification path — a real, disclosed schedule risk, not a defect of this RFC&#39;s design.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved Questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to &quot;Unresolved Questions&quot;">​</a></h2><p>None of the following block acceptance of the mapping and design fixed above:</p><ul><li>Whether the <code>external</code> classification (see Alternatives Considered) should eventually motivate a genuine RFC-0003 amendment introducing a distinct &quot;local-network&quot; tier — explicitly named as a candidate future revisit, not decided here, and not blocking this RFC.</li><li>The exact Console settings-page flow for building the allowlist (a live-browsed picker vs. a manually-entered list of entity IDs) is UI design, not architecture.</li><li>Whether <code>home-assistant.json</code> should eventually support more than one Home Assistant instance (some households run more than one, e.g. a separate test instance) — no evidence this milestone&#39;s actual scope needs it; single-instance is the smallest design that answers the roadmap&#39;s real questions.</li><li>Real behavior when Home Assistant&#39;s own long-lived token expires or is revoked by the household from their own Home Assistant UI — expected to surface as a normal bounded-execution authentication failure, but unverified against a real instance.</li><li><strong>TLS certificate verification for <code>base_url</code>, found during review and not addressed in the Proposal above.</strong> A household running Home Assistant with <code>https://</code> (its own self-signed certificate, or a LAN-internal CA — both common for local installs, unlike <code>web.fetch</code>&#39;s targets, which are public and hold ordinary publicly-trusted certs) may fail to connect under ordinary certificate verification, or — if the implementation instead disables verification to route around that — quietly lose any protection against a spoofed on-LAN response. Neither half of that tradeoff is decided here. This is implementation-level, not a change to this RFC&#39;s classification, allowlist, or credential design, so it does not block acceptance, but it must be resolved (most likely: support a plain <code>http://</code> default matching Home Assistant&#39;s own common local-network configuration, and require normal certificate verification if a household opts into <code>https://</code>, never a blanket bypass) before <code>POST /api/v1/conversation/home-assistant</code> ships.</li></ul><h2 id="acceptance-criteria" tabindex="-1">Acceptance Criteria <a class="header-anchor" href="#acceptance-criteria" aria-label="Permalink to &quot;Acceptance Criteria&quot;">​</a></h2><ul><li><code>home_assistant.list_entities</code> and <code>home_assistant.get_history</code> are registered against RFC-0003&#39;s contract with the schemas above, classified <code>read_only</code>/<code>external</code>/<code>required</code>.</li><li><code>/data/sovereign/capabilities/home-assistant.json</code> is read fresh per request (not cached at process start), matching <code>policy.json</code>&#39;s existing freshness behavior — verified by a test that toggling the file&#39;s content between requests changes the outcome without a service restart.</li><li>A proposal for an <code>entity_id</code> outside <code>allowlisted_entities</code> is rejected with <code>ENTITY_NOT_ALLOWLISTED</code> before any confirmation is generated and before any Home Assistant request is attempted — verified directly, mirroring RFC-0017&#39;s own proof pattern for <code>external_enabled: false</code>.</li><li><code>enabled: false</code> (the real default on a device that has never touched this configuration) rejects both capabilities at the policy stage — verified directly, confirming the milestone&#39;s allowlist/opt-in requirement is structurally true on a fresh device.</li><li><code>enabled: true</code> with no stored <code>base_url</code>/token produces <code>CAPABILITY_NOT_CONFIGURED</code>, distinct from <code>CAPABILITY_DISABLED</code> — verified directly.</li><li>The confirmation pause/resume flow for both capabilities reuses RFC-0017&#39;s existing <code>pending_confirmation</code>/<code>confirmation</code> fields with no new wire-format code path — verified by extending <code>test_conversation_service.py</code>&#39;s existing confirmation tests to a second capability pair rather than writing a parallel mechanism.</li><li><code>GET /api/v1/conversation/home-assistant</code> never returns the stored access token value — verified directly against the raw response body.</li><li>Existing tests (<code>test_conversation_service.py</code>, <code>test_capabilities.py</code>, RFC-0017&#39;s own web-search/fetch tests) continue to pass unmodified, confirming this RFC&#39;s additions are additive to the existing turn loop and executor.</li></ul><p>Explicitly <strong>not</strong> required for acceptance, named as necessary follow-up implementation matching this project&#39;s precedent of separating proposal from build-out:</p><ul><li>The capability implementation itself (<code>sovereign_homeassistant.py</code> or equivalent), the <code>home-assistant.json</code>/credential-directory reading and writing, and the two new HTTP endpoints (<code>GET</code>/<code>POST /api/v1/conversation/home-assistant</code>, plus the entity-browsing proxy endpoint for the settings page).</li><li>Console&#39;s settings UI for entering the base URL/token and building the allowlist, and the confirmation-card copy for these two specific capabilities (the card mechanism itself is already built, per RFC-0017&#39;s Acceptance Criteria).</li><li>Real-hardware qualification against an actual Home Assistant instance — dependent on one being available on the project&#39;s household network, per Testing Strategy&#39;s disclosed schedule risk.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Accepted (2026-08-21, project creator, reviewed by Claude at the project creator&#39;s direction).</strong> The <code>home_assistant.list_entities</code>/ <code>home_assistant.get_history</code> mapping, the <code>external</code> classification of Home Assistant traffic, the entity allowlist mechanism (checked at stage 3, before confirmation), and the credential-storage design are accepted as this milestone&#39;s platform contract. Review before acceptance found and fixed one real internal inconsistency (the Interfaces and Data Flow diagram misstated the allowlist check as stage 2 instead of stage 3, contradicting the Entity Allowlist section&#39;s own prose) and one genuine gap not previously addressed (TLS certificate verification behavior for <code>base_url</code>, now recorded as a named, non-blocking Unresolved Question with a concrete direction: support plain <code>http://</code> for the common local case, and real certificate verification — never a blanket bypass — if a household opts into <code>https://</code>). Both are fixed in this revision. The Unresolved Questions above, including whether RFC-0003&#39;s <code>network</code> axis should eventually grow a distinct local-network tier, are accepted as non-blocking follow-ups, not gating conditions, matching RFC-0003&#39;s own precedent for its Unresolved Questions.</p>`,52)])])}const m=t(s,[["render",n]]);export{p as __pageData,m as default};
