import{_ as o,o as a,c as t,a2 as n}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Calendar Connector","description":"","frontmatter":{"epic":10,"title":"Calendar Connector","status":"⏳ In Progress — 10.1 (mobile) done; 10.2 (desktop, macOS only) done; 10.3 (desktop, Windows/Linux) planned as a fast-follow","scope":"shared","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/epics/mobile/calendar-connector.md"},"headers":[],"relativePath":"sovereign-edge/epics/mobile/calendar-connector.md","filePath":"sovereign-edge/epics/mobile/calendar-connector.md"}'),s={name:"sovereign-edge/epics/mobile/calendar-connector.md"};function r(c,e,i,d,l,p){return a(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="epic-calendar-connector" tabindex="-1">Epic: Calendar Connector <a class="header-anchor" href="#epic-calendar-connector" aria-label="Permalink to &quot;Epic: Calendar Connector&quot;">​</a></h1><blockquote><p>A first-party Tier 3 connector letting the model create, update, delete, and query the user&#39;s own on-device calendar.</p></blockquote><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The first connector that reaches an on-device OS capability rather than the network — a <strong>Tier 3</strong> connector per the Connector Framework&#39;s own original three-tier design (research 0001), not Tier 1 like Search and Sovereign Tasks. Blocked on epic <a href="./connector-framework.html">2</a>&#39;s task 2.6 (Tier 3 scaffolding), which this epic is the first real consumer of on both mobile and desktop.</p><p>Full findings, options, and the reasoning behind this epic&#39;s scope live in <a href="./../../research/0005-calendar-connector.html">research 0005</a> (mobile) and <a href="./../../research/0011-desktop-calendar-connector.html">research 0011</a> (desktop — why macOS only) — this file states the decided scope as tasks, those docs keep the <em>why</em>. Originally scoped mobile-only; the user asked for desktop too, which is what task 10.2 and research 0011 add.</p><h2 id="tasks" tabindex="-1">Tasks <a class="header-anchor" href="#tasks" aria-label="Permalink to &quot;Tasks&quot;">​</a></h2><h4 id="✅-10-1-—-calendar-connector-mobile" tabindex="-1">✅ 10.1 — Calendar connector (mobile) <a class="header-anchor" href="#✅-10-1-—-calendar-connector-mobile" aria-label="Permalink to &quot;✅ 10.1 — Calendar connector (mobile)&quot;">​</a></h4><p><strong>Goal:</strong> Let the model create, update, delete, and query events on the user&#39;s own device calendar, via <code>expo-calendar</code>.</p><p><strong>Deliverables:</strong></p><ul><li>A Tier 3 connector manifest (per task 2.6&#39;s schema) declaring four tools: <code>calendar.create_event</code>, <code>calendar.update_event</code>, <code>calendar.delete_event</code>, <code>calendar.query_events</code>.</li><li>Reminders are modeled as an alert offset on a created event, not a separate API surface — the one interpretation of &quot;reminders&quot; that behaves identically on iOS and Android (research 0005&#39;s findings: iOS EventKit has a genuinely separate Reminders store; Android has no equivalent at all).</li><li>MVP scope is deliberately narrow: single, non-recurring, timezone-naive events. Recurrence, timezone handling, and all-day events are explicitly out of scope for this task.</li><li>New events are written to a fixed default/primary calendar for MVP — see Open Questions below for the eventual calendar-picker flow.</li></ul><p><strong>Dependencies:</strong> Task 2.6 (Tier 3 scaffolding).</p><p><strong>Review checklist:</strong></p><ul><li>Creating an event via chat results in a real event appearing in the device&#39;s own Calendar app, on both platforms.</li><li>Querying &quot;what&#39;s on my calendar&quot; returns events actually present on the device, not a stale or cached view.</li><li>Deleting or updating an event via chat is reflected in the Calendar app immediately.</li><li>Revoking this connector&#39;s permission does not affect any other connector&#39;s permission (Search, Sovereign Tasks) or vice versa — the same bar task 2.2 already set for Tier 1 connectors, now proven for Tier 3.</li></ul><p><strong>Open questions from research 0005, resolved during implementation:</strong></p><ul><li>Which calendar new events are written to — resolved as the primary/ default calendar; no calendar-picker setup step was added, kept as a plausible fast-follow rather than in-scope here.</li><li><code>expo-calendar@57.0.1</code> is compatible with the installed Expo SDK 57/RN 0.86 toolchain — confirmed by a real <code>pnpm install</code> and reading its actual <code>package.json</code>/type declarations, not assumed. One real surprise: SDK 57&#39;s <code>expo-calendar</code> ships a new, sync-first default API (under <code>&#39;expo-calendar&#39;</code> itself) that has <strong>no event CRUD at all</strong> — the functions this connector needs (<code>createEventAsync</code>, <code>getEventsAsync</code>, <code>requestCalendarPermissionsAsync</code>, etc.) only exist under the <code>&#39;expo-calendar/legacy&#39;</code> subpath, confirmed by reading the package&#39;s own <code>exports</code> map, not guessed from an older SDK&#39;s shape.</li><li>iOS&#39;s full-access vs. write-only permission tiers: resolved by <em>not</em> setting <code>writeOnlyAccess: true</code> in the <code>expo-calendar</code> config plugin (<code>app.json</code>) — its default path sets <code>NSCalendarsFullAccessUsageDescription</code>, giving <code>query_events</code> the access tier it needs, confirmed by reading the plugin&#39;s own source rather than assumed from the option name.</li></ul><p><strong>Decided (not in research 0005):</strong></p><ul><li><strong>Four separate manifests/connector ids, not one manifest with four tools.</strong> The Tier 3 schema gives every manifest exactly one <code>tool</code> and one <code>handler.capability</code> — there is no shape for &quot;one connector, several tools.&quot; This is a real fit with the app&#39;s &quot;no blanket toggle, per-connector consent&quot; philosophy (a user can allow <code>query_events</code> without allowing <code>delete_event</code>), not a workaround.</li><li><strong>One real OS permission prompt for all four, not four.</strong> All four manifests share one underlying OS permission domain — <code>permissions/ calendarAccess.ts</code>&#39;s <code>ensureCalendarAccess()</code> checks current status first and only prompts if undetermined, called from <code>ConnectorsScreen.tsx</code>&#39;s grant handler <em>before</em> the app&#39;s own <code>grant()</code>, never after (an OS denial must never let the app record &quot;granted&quot; for a connector EventKit/Calendar will actually refuse to run).</li></ul><p><strong>Verified:</strong></p><ul><li><code>pnpm --filter mobile typecheck</code>/<code>lint</code>/<code>test</code> (293 tests, up from 268 — <code>calendar/manifest.test.ts</code>, <code>calendar/handlers.test.ts</code> (mocking <code>expo-calendar/legacy</code>), <code>permissions/calendarAccess.test.ts</code>, and new <code>ConnectorsScreen.test.tsx</code> cases for the permission-then-grant flow and OS-denial message).</li><li>Real iOS Simulator build and launch: a real <code>pod install</code> (linked the new <code>ExpoCalendar (57.0.1)</code> native module for the first time) and a real <code>xcodebuild</code> against the actual Xcode project, then a real tap through Settings → Connectors on a booted simulator, confirming the four Calendar rows are genuinely rendered from the real manifests (not a mock) and that tapping one calls the real <code>ensureCalendarAccess()</code> → real <code>expo-calendar/legacy</code> <code>requestCalendarPermissionsAsync()</code> → a real OS response, handled correctly (no <code>grant()</code> call, the exact denial message shown, state remains <code>not-asked</code> rather than falsely <code>granted</code>).</li></ul><p><strong>Honest gap:</strong></p><ul><li><strong>A real interactive &quot;Allow&quot; tap could not be captured.</strong> This automated build+launch context (via this environment&#39;s iOS Simulator build tool) never surfaced a visible system permission dialog to tap — the OS resolved straight to denied. <code>xcrun simctl privacy grant calendar &lt;bundle-id&gt;</code> was also tried directly against the booted simulator and did not flip what <code>getCalendarPermissionsAsync()</code> reads back at runtime. The denial <em>path</em> itself is proven correct end to end (real API call, real response, correctly handled) — only the <em>granted</em> path&#39;s real device behavior remains unconfirmed, the same class of gap as task 10.2&#39;s desktop EventKit dialog. A real device or an interactive (non-headless) Simulator.app session, with a human&#39;s one-time tap, would close it.</li></ul><h2 id="related-docs" tabindex="-1">Related Docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related Docs&quot;">​</a></h2><ul><li><a href="./../../research/0001-concept-and-connector-architecture.html">research 0001</a></li><li><a href="./../../research/0005-calendar-connector.html">research 0005</a></li><li><a href="./../../research/0011-desktop-calendar-connector.html">research 0011</a> (desktop, task 10.2)</li><li><a href="./connector-framework.html">Connector Framework</a> (task 2.6, the Tier 3 scaffolding this epic depends on)</li></ul><h2 id="cross-references" tabindex="-1">Cross-references <a class="header-anchor" href="#cross-references" aria-label="Permalink to &quot;Cross-references&quot;">​</a></h2><ul><li>Shares its Tier 3 dependency (task 2.6) with the <a href="./device-connector.html">Device connector</a> (epic 11).</li><li>Mirrors the Sovereign Tasks connector&#39;s (epic 4) &quot;direct integration, not delegation&quot; pattern — this connector writes to the calendar itself, it does not hand off to another app&#39;s UI.</li></ul><hr><h4 id="✅-10-2-—-desktop-calendar-connector-macos-only" tabindex="-1">✅ 10.2 — Desktop calendar connector (macOS only) <a class="header-anchor" href="#✅-10-2-—-desktop-calendar-connector-macos-only" aria-label="Permalink to &quot;✅ 10.2 — Desktop calendar connector (macOS only)&quot;">​</a></h4><p><strong>Goal:</strong> The same four tools, on desktop, via real Apple EventKit bindings — macOS is the only desktop OS this pass supports; see <a href="./../../research/0011-desktop-calendar-connector.html">research 0011</a> for why Windows (package-identity requirement this app&#39;s unpackaged distribution doesn&#39;t have) and Linux (no single calendar API) are out of scope for now.</p><p><strong>Deliverables:</strong></p><ul><li><code>apps/desktop/src-tauri/src/connectors/calendar/{mod,manifest,handlers,access}.rs</code>, <code>#[cfg(target_os = &quot;macos&quot;)]</code>-gated: four manifests mirroring mobile&#39;s (same ids/tools, <code>platforms: [&quot;desktop&quot;]</code>), synchronous CRUD/query handlers via <code>objc2-event-kit</code>&#39;s <code>EKEventStore</code>, and the one completion-handler → async bridge this connector needs (<code>requestFullAccessToEventsWithCompletion</code>, exposed as its own <code>request_calendar_access</code> Tauri command).</li><li><code>known_connector_manifests()</code> in <code>lib.rs</code> includes the four manifests via <code>connectors::calendar::calendar_manifests()</code>, which itself returns an empty <code>Vec</code> off macOS — no <code>cfg!</code> needed at the call site, and Windows/ Linux builds never advertise a connector they can&#39;t run.</li><li>Same permission-ordering rule as mobile: <code>ConnectorsScreen.tsx</code> calls <code>request_calendar_access</code> and only calls <code>set_connector_granted(id, true)</code> on a real <code>true</code> result.</li></ul><p><strong>Dependencies:</strong> Task 2.6 (Tier 3 scaffolding, desktop side); research 0011.</p><p><strong>Review checklist:</strong></p><ul><li>Same four review points as task 10.1, on macOS: create/update/delete/ query all reflect in the real macOS Calendar app; revoking one calendar connector doesn&#39;t touch another connector&#39;s grant.</li><li>A Windows or Linux build&#39;s Connectors screen never shows a Calendar row at all (not a broken one).</li></ul><p><strong>Decided:</strong></p><ul><li><strong>Same capability-doubles-as-permission-scope shape mobile ended up needing</strong>, for the same reason: the validator&#39;s cross-field rule (<code>validate.rs</code>&#39;s <code>tier3CrossFieldIssues</code>) requires <code>handler.capability</code> to be a <em>member</em> of <code>permissions.device.capabilities</code> — a coarser shared scope across the three write manifests isn&#39;t expressible without duplicating both strings per manifest, which would just make the per-connector scope text shown to the user redundant.</li><li><strong>A fresh <code>EKEventStore</code> per handler call, not a held singleton.</strong> Apple&#39;s own docs call a long-lived instance a <em>performance</em> recommendation, not a correctness requirement, and a <code>Retained&lt;T&gt;</code> Objective-C object isn&#39;t safely shareable across this app&#39;s multi-threaded Tauri command dispatch without its own synchronization — a fresh instance per call sidesteps that entirely.</li><li><strong>No date/time crate dependency</strong> for parsing the <code>YYYY-MM-DDTHH:MM:SSZ</code> strings the manifests declare — <code>parse_iso8601_utc</code>/<code>days_from_civil</code> in <code>handlers.rs</code> reuse the same Howard Hinnant civil-date algorithm <code>permissions/grants.rs</code>&#39;s own <code>iso_now</code>/<code>civil_from_days</code> already established as this app&#39;s precedent for one-format date handling without a new dependency.</li></ul><p><strong>Honest gap:</strong></p><ul><li><strong>The real macOS system Calendar-access dialog cannot be clicked by any tool available in this environment</strong> — no macOS screen-automation tool exists (unlike the iOS Simulator, which does support tapping through system dialogs). <code>already_authorized()</code>/<code>request_access()</code> are proven correct against the real, side-effect-free <code>authorizationStatusForEntityType</code> query and a real <code>cargo build</code>/launch, but the actual &quot;user clicks Allow&quot; round trip needs a human&#39;s one-time manual click during a follow-up check — the same class of gap <code>vault_smoke.rs</code>&#39;s real-keychain integration test already carries for this codebase.</li></ul><p><strong>Verified:</strong></p><ul><li><code>cargo fmt</code>/<code>clippy</code>/<code>test</code> (150 tests, up from 137 — 13 new calendar tests: manifest validation against the real validator, ISO-8601 parse/ format round-trip against a real computed epoch timestamp, required- field refusal for all four handlers, and <code>native_handler_for</code> capability resolution).</li><li><code>pnpm --filter desktop typecheck</code>/<code>lint</code>/<code>test</code> (53 tests, up from 49).</li><li>Real debug binary build (<code>pnpm tauri build --debug --no-bundle</code>, including a real <code>objc2-event-kit</code>/<code>block2</code> link) and launch-smoke check: process starts, stays running, no panic.</li></ul><hr><h4 id="📋-10-3-—-desktop-calendar-connector-windows-linux-fast-follow" tabindex="-1">📋 10.3 — Desktop calendar connector, Windows/Linux (fast-follow) <a class="header-anchor" href="#📋-10-3-—-desktop-calendar-connector-windows-linux-fast-follow" aria-label="Permalink to &quot;📋 10.3 — Desktop calendar connector, Windows/Linux (fast-follow)&quot;">​</a></h4><p><strong>Goal:</strong> Extend task 10.2&#39;s desktop calendar connector to Windows and Linux, closing the gap research 0011 documented rather than solved. Explicitly <strong>not scoped for this pass</strong> — handed off as a real, scoped fast-follow rather than left as a vague &quot;someday.&quot; macOS&#39;s own connector (task 10.2) is unaffected either way: <code>calendar_manifests()</code> stays <code>#[cfg(target_os = &quot;macos&quot;)]</code>-gated today, and whoever picks this up adds sibling <code>#[cfg(target_os = &quot;windows&quot;)]</code>/<code>#[cfg(target_os = &quot;linux&quot;)]</code> modules alongside it, not a rewrite of it.</p><p><strong>Deliverables (per research 0011&#39;s findings — this is scope, not new research):</strong></p><ul><li><strong>Windows:</strong> <code>windows::ApplicationModel::Appointments::AppointmentStore</code> (the official <code>windows</code> crate) is the real API, but it requires package identity this app&#39;s current unpackaged NSIS/MSI distribution doesn&#39;t have. First decision to make: adopt a sparse/external-location package (identity without full MSIX) — check this doesn&#39;t conflict with <code>.github/workflows/desktop-release.yml</code>&#39;s existing release pipeline before committing to it — or fall back to Outlook COM automation (works unpackaged, but only when Outlook happens to be installed, an assumption this app makes nowhere else today).</li><li><strong>Linux:</strong> no single calendar API. Real support means picking one backend — GNOME&#39;s <code>evolution-data-server</code> D-Bus interface is the pragmatic default research 0011 flagged (more standardized than KDE&#39;s Akonadi), not a decision already made — and documenting other desktop environments as unsupported, the same honest-gap pattern task 10.2 used for Windows/Linux as a whole.</li><li>Same four tools, same manifest ids/capability shape as task 10.2 (one manifest = one tool = one <code>handler.capability</code>, matching <code>permissions.device.capabilities</code> per the validator&#39;s cross-field rule); <code>known_connector_manifests()</code> needs no change beyond each new platform&#39;s own <code>calendar_manifests()</code> returning real entries instead of <code>Vec::new()</code>.</li></ul><p><strong>Dependencies:</strong> Task 10.2 (the manifest/handler shape to mirror); research 0011 (the platform findings — read this before starting, it&#39;s not just a stub).</p><p><strong>Review checklist:</strong></p><ul><li>Windows: create/update/delete/query all reflect in the real Windows Calendar app (or Outlook, if that&#39;s the fallback taken), from a real installed (not just <code>cargo run</code>) build — package-identity issues often only surface post-install, not in a dev build.</li><li>Linux (GNOME): same four operations reflect in GNOME Calendar/Evolution. A non-GNOME Linux desktop&#39;s Connectors screen shows no Calendar row (same &quot;absent, not broken&quot; bar task 10.2 set for pre-this-task Windows/ Linux) rather than a row that fails at dispatch time.</li></ul><p><strong>Not decided — real forks for whoever picks this up:</strong></p><ul><li>Windows: sparse package vs. Outlook COM (see Deliverables above) — this is a real architectural fork with distribution-pipeline consequences, not a detail to default silently.</li><li>Linux: GNOME/EDS only, or also attempt KDE/Akonadi? EDS-only was research 0011&#39;s pragmatic-default flag, not its decision.</li></ul>',49)])])}const m=o(s,[["render",r]]);export{u as __pageData,m as default};
