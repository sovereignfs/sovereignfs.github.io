import{_ as i,o as s,c as t,a2 as n}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0078 — Generic offline read+write for plugins","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0078-offline-plugin-writes.md","filePath":"rfcs/0078-offline-plugin-writes.md"}'),a={name:"rfcs/0078-offline-plugin-writes.md"};function o(l,e,r,d,h,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="rfc-0078-—-generic-offline-read-write-for-plugins" tabindex="-1">RFC 0078 — Generic offline read+write for plugins <a class="header-anchor" href="#rfc-0078-—-generic-offline-read-write-for-plugins" aria-label="Permalink to &quot;RFC 0078 — Generic offline read+write for plugins&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/manifest</code> (breaking change to the <code>offline</code> field — removes <code>offline.routes[]</code>, flattens <code>offline.root</code> to a plain <code>offline</code> boolean, adds a new <code>offline:write</code> permission), <code>packages/sdk</code> (new <code>@sovereignfs/sdk/offline-queue</code> client surface), <code>plugins/launcher</code> (manifest migration to the flattened field), <code>plugins/sovereign-shopper</code> (first adopter of the generic model, prototyping read+write), <code>runtime</code> (simplification of route-prefix resolution, service worker precaching, and the offline-route neutral-shell mechanism — fewer cases, not new ones), <code>docs/plugin-development.md</code>, <code>docs/upgrade.md</code>; amends RFC 0074 (offline-capable plugin routes) and builds on RFC 0062&#39;s <code>mailer:send</code> permission-enforcement precedent.<br><strong>Incorporated into plan:</strong> No roadmap slot yet — documentation-first, same posture as RFC 0071 and RFC 0077. Implementation, once scheduled, lands as a sequence of small tasks (manifest change, runtime simplification, SDK module, Shopper adoption) rather than one PR.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>RFC 0074 shipped a <strong>read-only</strong> offline-plugin-routes system: a plugin enumerates specific route prefixes in its manifest (<code>offline.routes[]</code>), each one individually validated and neutrality-scanned, and reads cached data client-side from <code>sdk.offline</code> (a plugin-scoped IndexedDB store). This RFC does two things:</p><ol><li><strong>Adds writes.</strong> A new client-side mutation queue, <code>@sovereignfs/sdk/offline-queue</code>, lets a plugin enqueue writes made while offline, apply them optimistically to its own local view, and drain them against its own sync endpoint once connectivity returns — with last-write-wins conflict resolution and an idempotent apply contract that makes retries safe.</li><li><strong>Eliminates route-based declaration.</strong> <code>offline.routes[]</code> is removed. Launcher&#39;s already-shipped <code>offline.root: true</code> — one bare-<code>routePrefix</code> entry page, entirely client-rendered past that, no per-route enumeration at all — becomes the <em>only</em> offline model, flattened to a plain <code>offline: boolean</code>. A plugin declares itself offline-capable once; which screens, lists, or records it actually supports offline is the plugin&#39;s own client-side decision, invisible to the manifest schema and to the platform&#39;s route-neutrality tooling.</li></ol><p>Both changes are deliberately <strong>generic, plugin-agnostic capabilities</strong> — <code>sdk.offline</code> and <code>sdk.offline-queue</code> take a <code>pluginId</code> and plugin-chosen key/payload strings, never anything Shopper-specific. Shopper is this RFC&#39;s worked example and first adopter (view lists, add/edit/delete items, mark items bought — all offline, all synced back on reconnect), not a special-cased target.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>RFC 0074 delivered &quot;open the app offline, see your stuff&quot; and explicitly, repeatedly deferred writes to a future RFC — a shopping list you can only <em>look at</em> offline is close to useless; the value is crossing an item off in a shop with no signal, or adding one you thought of on the walk there. This RFC closes that gap.</p><p>Separately, building the read+write prototype surfaced a problem with RFC 0074&#39;s declaration model itself: enumerating Next.js route prefixes in the manifest (<code>offline.routes[]</code>) forces a 1:1 mapping between &quot;what&#39;s offline-capable&quot; and &quot;which server-rendered pages exist,&quot; which:</p><ul><li>couples an offline/data-layer concern to routing structure, so every new offline-capable screen is also a manifest change and a new neutrality-scan target;</li><li>doesn&#39;t match how the one plugin that has actually shipped offline support today (Launcher) works — Launcher never used <code>routes[]</code>, it used <code>offline.root: true</code>, a single client-rendered entry point, from the start; and</li><li>doesn&#39;t generalize cleanly to writes: a mutation queue is keyed by plugin + operation, not by which URL rendered the button that enqueued it.</li></ul><p>Generalizing Launcher&#39;s pattern into the only model removes an entire class of manifest surface (route-prefix validation, per-route neutrality scanning) and lets &quot;what&#39;s offline-capable&quot; be answered entirely in application code — exactly where a plugin author already decides what their app supports.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>RFC 0074&#39;s read-only mechanism</strong>, still the foundation: manifest field <code>offline</code> (<code>packages/manifest/src/schema.ts:227-263</code> today — object shape <code>{ routes?: [{prefix, description}], root?: boolean }</code>), the user-neutral shell rendering rule enforced by <code>runtime/src/__tests__/offline-route-neutrality.test.ts</code> (a static scanner for <code>headers()</code>/<code>cookies()</code>/session helpers in declared offline routes&#39; server-component source), a dedicated <code>offline-shells</code> Workbox matcher in <code>runtime/next.config.ts</code> (<code>StaleWhileRevalidate</code>, listed ahead of the general <code>pages</code> <code>NetworkFirst</code> matcher), <code>runtime/src/registry.ts</code>&#39;s <code>getOfflineRoutePrefixes()</code> resolving manifest declarations into concrete path prefixes, <code>runtime/middleware.ts</code>&#39;s <code>x-sovereign-offline-route</code> header (read by <code>runtime/app/(platform)/layout.tsx</code> to render a fixed, identical-for-everyone shell — no name/email/avatar, no personalized sidebar — for offline-declared requests), and an unconditional <code>offline.clearAll()</code> purge on both sign-out (<code>runtime/app/(platform)/_components/AccountMenu.tsx:155</code>) and sign-in (<code>runtime/src/complete-sign-in.ts:22</code>), which is what makes plugin-only (not per-user) IndexedDB key scoping safe on a shared device.</li><li><strong><code>sdk.offline</code></strong> (<code>packages/sdk/src/offline.ts</code>), exposed via the dedicated subpath <code>@sovereignfs/sdk/offline</code> rather than the main <code>@sovereignfs/sdk</code> barrel — the barrel transitively reaches server-only <code>next/headers</code> code, and Next&#39;s client/server boundary check flags the whole reachable module graph, so a <code>&#39;use client&#39;</code> component importing from the barrel would fail to build. <code>sdk.offline</code> is a plugin-scoped (not user-scoped) IndexedDB key/value cache — <code>get</code>/<code>set</code>/<code>remove</code>/<code>keys</code>/ <code>clear</code>/<code>clearAll</code> — backed by a single <code>sovereign-offline</code> database, with a write-epoch guard plus a <code>BroadcastChannel(&#39;sovereign-offline-clear&#39;)</code> to stop a cross-tab write racing a <code>clearAll()</code>, and a <code>5 MB</code> soft per-entry cap throwing a typed <code>OfflineQuotaExceededError</code>. It has <strong>no server round-trip at all</strong> — no <code>SdkHost</code> entry, no <code>provideHost()</code> wiring — architecturally unlike every other SDK namespace, which are thin RPC wrappers calling <code>requireHost()</code> (wired in <code>runtime/src/sdk-host.ts</code> via <code>provideHost()</code> from <code>runtime/instrumentation.ts</code>).</li><li><strong>Launcher — the one existing adopter, and the pattern this RFC generalizes.</strong> <code>plugins/launcher/manifest.json</code> declares <code>offline: { root: true }</code>; <code>plugins/launcher/app/page.tsx</code> renders nothing per-user; all data-fetching happens in <code>plugins/launcher/app/_components/LauncherOfflineView.tsx</code>, a <code>&#39;use client&#39;</code> component that reads <code>offline.get(pluginId, &#39;plugins&#39;)</code> on mount and renders immediately if cached, then always attempts a live <code>fetch(&#39;/api/plugins&#39;)</code> in parallel, re-rendering and <code>offline.set()</code>-ing on success, falling back to an &quot;unavailable offline&quot; empty state if there&#39;s truly nothing cached. Launcher never used <code>routes[]</code> — it has exactly one page, and <code>root: true</code> was already sufficient. This is the proof that the enumerated-route-array shape was never load-bearing for the simplest, and so far <em>only</em>, real adopter.</li><li><strong>The <code>mailer:send</code>/<code>mailer:sendExternal</code> permission-enforcement precedent</strong> (RFC 0062): <code>runtime/src/plugin-mailer.ts</code>&#39;s <code>requireMailerPluginContext()</code> is called from inside <code>runtime/src/sdk-host.ts</code>&#39;s <code>provideHost({...})</code> — a shared host boundary every plugin&#39;s <code>sdk.mailer.send()</code> call passes through, which is what makes that gate centrally enforceable. No equivalent shared boundary exists (or can exist) for a plugin&#39;s own offline sync endpoint — see &quot;Permission enforcement&quot; below.</li><li><strong>Plugins may add their own Route Handlers</strong>, not just Server Actions, under their own <code>routePrefix</code>, gated by the ordinary session middleware — e.g. <code>plugins/sovereign-tritext/app/export/[projectId]/route.ts</code> calls <code>sdk.auth.requireSession()</code> directly inside a <code>GET</code> handler, because Server Actions aren&#39;t fetchable from arbitrary client code the way a plain Route Handler is. The same reasoning applies to a plugin&#39;s sync endpoint.</li><li><strong>Zero prior art in this repo for a mutation queue, outbox, or background sync</strong> — confirmed by a repo-wide search. This RFC has no existing pattern to reuse beyond <code>offline.ts</code>&#39;s IndexedDB plumbing style (epoch guard, <code>BroadcastChannel</code>, pending-purge retry).</li><li><strong>The Background Sync API has no iOS Safari support.</strong> The only reliable trigger for &quot;sync when connectivity returns&quot; on the platform that matters most for a PWA is the app being foregrounded — a mount, or a live <code>online</code> event while the tab is open. <code>runtime/app/(platform)/_components/OfflineBanner.tsx</code> already listens to <code>window.addEventListener(&#39;online&#39;/&#39;offline&#39;, ...)</code> — the same signal this RFC&#39;s drain trigger uses.</li><li><strong>A known, adjacent wrinkle in the neutral-shell mechanism</strong>, surfaced while building toward this RFC but not itself in scope here: the neutral-shell rule (RFC 0074 §2) is only strictly required for the document a service worker might <em>precache and later replay to a different user</em> — a <strong>live, currently-online</strong> tab sitting on an offline-declared route doesn&#39;t need degraded chrome, only the cached copy does. Any future implementation of this RFC&#39;s shell simplification (below) should restore personalized chrome for a live tab via client-side hydration (fetching the real sidebar/plugin list once mounted, the same way <code>AccountMenu.tsx</code> hydrates the authoritative session for its own self-mutation flows) rather than leaving a live, online visit permanently degraded. This RFC does not specify the exact mechanism — it is called out here as a requirement the implementation must satisfy, not a design this document commits to.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-manifest-—-one-flag-no-route-enumeration" tabindex="-1">1. Manifest — one flag, no route enumeration <a class="header-anchor" href="#_1-manifest-—-one-flag-no-route-enumeration" aria-label="Permalink to &quot;1. Manifest — one flag, no route enumeration&quot;">​</a></h3><p>Replace the current object shape</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;routes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;prefix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/lists/[listId]&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;root&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>with a plain boolean:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;offline&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>true</code> means: this plugin&#39;s bare <code>routePrefix</code> page is its one offline-capable entry point. It renders a user-neutral shell server-side and hydrates everything else — which screens, which data, which records — from client-side code the plugin owns entirely. The manifest schema no longer knows or validates anything about &quot;paths&quot;; that is 100% application logic, exactly where a plugin author already decides what their app supports offline.</p><p><code>packages/manifest/src/schema.ts:227-263</code>&#39;s <code>offline.routes[]</code> array — the prefix-safety refinement (must start with <code>/</code>, must not be <code>/</code>, no <code>..</code>, no route-group markers) and the uniqueness check — is deleted outright, not deprecated. <code>offline.root</code> is renamed/flattened into the top-level <code>offline</code> boolean.</p><p>Add <code>&#39;offline:write&#39;</code> to <code>permissionSchema</code> (<code>packages/manifest/src/schema.ts:19-36</code>, alongside <code>mailer:send</code>/ <code>mailer:sendExternal</code>). A new cross-field <code>.refine()</code> requires <code>offline === true</code> whenever <code>&#39;offline:write&#39;</code> is declared — write capability only exists on top of the generic single-shell model; there&#39;s no narrower &quot;just this route can write&quot; option, because there&#39;s only one entry point per plugin now.</p><p><strong>This is a breaking manifest-schema change</strong>, accepted deliberately rather than deprecate-and-coexist: <code>docs/upgrade.md</code> gets a new entry describing the removal of <code>offline.routes[]</code>/<code>offline.root</code> and the migration path (&quot;restructure your offline experience as one client-rendered shell at your bare <code>routePrefix</code>, following <code>plugins/launcher/app/_components/LauncherOfflineView.tsx</code> as the reference pattern — there is no field-for-field migration for <code>routes[]</code> entries; each becomes a client-side view inside the one shell&quot;). <code>@sovereignfs/manifest</code> takes a <strong>major</strong> version bump. <code>plugins/launcher/manifest.json</code> updates in the same implementation task (<code>offline: { root: true }</code> → <code>offline: true</code>) so the one plugin that already relies on the field doesn&#39;t break.</p><h3 id="_2-runtime-—-simpler-not-more-complex" tabindex="-1">2. Runtime — simpler, not more complex <a class="header-anchor" href="#_2-runtime-—-simpler-not-more-complex" aria-label="Permalink to &quot;2. Runtime — simpler, not more complex&quot;">​</a></h3><p>Because there is now exactly one possible offline path per plugin (its bare <code>routePrefix</code>), several existing mechanisms shrink:</p><ul><li><code>runtime/src/registry.ts</code>&#39;s <code>getOfflineRoutePrefixes()</code> returns just <code>routePrefix</code> for every plugin declaring <code>offline === true</code> — no more per-route array resolution.</li><li><code>runtime/next.config.ts</code>&#39;s <code>offline-shells</code> Workbox matcher matches those single bare paths — functionally what it already did for <code>root: true</code> plugins; the array-handling branch for <code>routes[]</code> is deleted, not extended.</li><li><code>runtime/middleware.ts</code>&#39;s <code>x-sovereign-offline-route</code> header logic simplifies the same way.</li><li><code>offline-route-neutrality.test.ts</code> scans each offline-enabled plugin&#39;s root <code>page.tsx</code> and <code>layout.tsx</code> (both are part of the one precached document) instead of an enumerated, arbitrary route list — a smaller, more mechanical scanner.</li></ul><p><strong>Named, accepted tradeoff</strong>: while offline, only a plugin&#39;s bare <code>routePrefix</code> is reachable via the service worker. A stale bookmark or link to a nested path (what would have been e.g. <code>/shopper/lists/42</code> under the old model) is not SW-served offline — it falls through to the generic <code>/offline</code> fallback. This matches the actual described usage (open the app, tap the Shopper tile from Launcher, land at <code>/shopper</code>) and matches how Launcher itself already behaves (only reachable via <code>/</code> when offline) — a consistent consequence of the one-entry-point model, not a regression against anything that worked before.</p><h3 id="_3-sdk-—-sovereignfs-sdk-offline-queue-a-generic-mutation-queue" tabindex="-1">3. SDK — <code>@sovereignfs/sdk/offline-queue</code>, a generic mutation queue <a class="header-anchor" href="#_3-sdk-—-sovereignfs-sdk-offline-queue-a-generic-mutation-queue" aria-label="Permalink to &quot;3. SDK — \`@sovereignfs/sdk/offline-queue\`, a generic mutation queue&quot;">​</a></h3><p><code>sdk.offline</code> was already scoped by <strong>plugin id + a plugin-chosen key string</strong>, never by route — eliminating route-based declaration requires <em>zero</em> changes to the read-cache primitive. The new mutation-queue module inherits the same property by design: this is the validation that the data layer was already built the right way, and only the manifest/shell-rendering layer needed to change.</p><p>New module <code>packages/sdk/src/offline-queue.ts</code>, its own dedicated subpath <code>@sovereignfs/sdk/offline-queue</code> (matches the <code>offline</code>/<code>e2ee-*</code> pattern — browser-only, excluded from the main barrel). <strong>A separate IndexedDB database</strong> (<code>sovereign-offline-queue</code>), not a bolted-on object store inside <code>offline.ts</code>&#39;s <code>sovereign-offline</code> — <code>offline.ts</code>&#39;s own doc comment scopes it to &quot;no server sync,&quot; and reusing it risks the already-shipped read cache. The epoch + <code>BroadcastChannel</code> cross-tab write-vs-clear guard from <code>offline.ts</code> is duplicated (not extracted/shared) under a new channel name — a deliberate lower-risk-over-DRY tradeoff for a first pass; a follow-up cleanup once both modules are stable is a reasonable future task, not this one.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> QueuedMutation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// client-generated ULID — the idempotency key</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  op</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// plugin-defined operation name, e.g. &#39;addItem&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  clientTimestamp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// epoch SECONDS — match whatever convention the plugin&#39;s own DB uses</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  attempts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  lastError</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OfflineQueueFullError</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> offlineQueue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  enqueue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    op</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  )</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">QueuedMutation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  list</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">QueuedMutation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;[]&gt;, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// enqueue order (ULID-sortable)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  remove</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mutationId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  markFailed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mutationId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  clear</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  clearAll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// wired into the same purge sites as offline.clearAll() — see §6</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SyncOutcome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;applied&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;skipped&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;failed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> drainQueue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  pluginId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  applyBatch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">batch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> QueuedMutation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TPayload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;[]) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SyncOutcome</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]&gt;,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">applied</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skipped</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">failed</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }[] }&gt;;</span></span></code></pre></div><p><code>enqueue()</code> enforces a <strong>count-based</strong> cap (proposed 500 entries per plugin — queue entries are small, numerous operations, unlike the read cache&#39;s large blobs) and throws <code>OfflineQueueFullError</code> past it. There is <strong>no eviction</strong>: silently dropping a queued write is data loss, not inconvenience, so it must be a hard, visible error — stricter than the read cache&#39;s soft-cap-with-silent-effect precedent.</p><p><code>drainQueue</code> lists the plugin&#39;s full pending batch, calls the plugin-supplied <code>applyBatch</code> once with it, then removes every <code>applied</code>/<code>skipped</code> id locally and leaves <code>failed</code> and never-attempted ids untouched. It is a pure on-demand primitive — no listener, no timer, no auto-retry; the plugin decides when to call it.</p><p><strong>Sync trigger is plugin-driven, not platform-orchestrated — stated explicitly, not left implicit.</strong> A generic auto-scheduling helper in the SDK would be a false promise on iOS Safari (no Background Sync support), so the plugin calls <code>drainQueue()</code> itself: on mount of its offline shell, on a <code>window.addEventListener(&#39;online&#39;, ...)</code> handler, and via an explicit &quot;Retry&quot; affordance for the stuck case. If the app isn&#39;t foregrounded when connectivity returns, sync doesn&#39;t happen until it is — a real, user-visible limitation of this design, not a hidden implementation detail.</p><p><strong>Documented as a generic platform capability.</strong> <code>docs/plugin-development.md</code> describes <code>sdk.offline-queue</code> the same way <code>sdk.notifications</code>/<code>sdk.mailer</code> are documented — a generic API with one worked example (Shopper) — not a Shopper-specific feature. <code>@sovereignfs/sdk</code> takes a <strong>minor</strong> version bump (purely additive, no <code>docs/upgrade.md</code> entry needed per NFR-04).</p><h3 id="_4-idempotent-absolute-apply-contract" tabindex="-1">4. Idempotent, absolute apply contract <a class="header-anchor" href="#_4-idempotent-absolute-apply-contract" aria-label="Permalink to &quot;4. Idempotent, absolute apply contract&quot;">​</a></h3><p>Every operation a plugin enqueues must be idempotent and describe an absolute end state, not a delta — so a retried request (the client got no response and doesn&#39;t know whether the server actually applied it) is always safe to resend verbatim, and no separate server-side &quot;have I seen this mutation id&quot; dedup table is needed:</p><ul><li><strong><code>addItem</code></strong> — the client mints the <strong>permanent</strong> row id (a ULID) at enqueue time, not a temporary one. The server applies with <code>INSERT ... ON CONFLICT (id) DO NOTHING</code>. This removes the temp-id → real-id reconciliation problem structurally — there is no &quot;swap the ID later&quot; step, because the ID was real from the moment of the optimistic action. Fields the client can&#39;t compute offline (server-derived sort order, auto-suggested category/icon) are reconciled by a <strong>full live re-fetch + cache overwrite</strong> immediately after a successful drain, not by patching individual fields into the cached blob.</li><li><strong><code>updateItem</code></strong> — absolute field values, naturally idempotent as a pure overwrite, gated by the last-write-wins timestamp check on <strong>every</strong> apply attempt (not just the first), so a retried request can never clobber an edit that landed in between.</li><li><strong><code>deleteItem</code></strong> — <code>DELETE WHERE id = X</code>, naturally idempotent (a no-op if already gone).</li><li><strong>A toggle-style operation (e.g. &quot;mark bought&quot;) must not be queued as a toggle.</strong> A retried toggle whose first attempt actually succeeded, but whose response was lost, would flip the value back to the wrong state on retry. The queued mutation must carry the <strong>absolute intended state</strong> (e.g. <code>{ itemId, bought: boolean, at: number }</code>) — the client already knows this at the moment of the optimistic action, since it&#39;s flipping its own local copy from one known state to the other. Apply logic no-ops if the target row already reflects the intended state.</li></ul><h3 id="_5-conflict-resolution-—-last-write-wins-by-timestamp" tabindex="-1">5. Conflict resolution — last-write-wins by timestamp <a class="header-anchor" href="#_5-conflict-resolution-—-last-write-wins-by-timestamp" aria-label="Permalink to &quot;5. Conflict resolution — last-write-wins by timestamp&quot;">​</a></h3><p>The simplest option, chosen deliberately for this scale of workload (small, low-concurrency plugin data, not heavy simultaneous editing). Every mutable row a plugin wants offline-writable needs a timestamp column reflecting when it was last touched, compared against the queued mutation&#39;s <code>clientTimestamp</code> on every apply attempt. <strong>Every existing online write path that mutates that row must also update the same timestamp column on every write</strong> — not just the new sync path — or an online edit&#39;s stale timestamp would wrongly lose to a later-synced offline mutation that shouldn&#39;t have won. This is a required change to already-shipped online code paths, easy to miss, and should be called out explicitly in any implementation that adopts this design.</p><ul><li><strong>Delete-vs-edit conflicts.</strong> A queued <code>updateItem</code>/mark-bought whose target row was deleted elsewhere before sync: return <code>skipped</code> — nothing to reconcile, the client&#39;s next full re-fetch removes it from the view. A queued <code>addItem</code> whose parent collection (e.g. the list itself) was deleted/archived in the meantime: return <code>failed</code> with a clear reason — silently dropping a user&#39;s added item is worse than surfacing it, unlike an edit-to-a-gone-item where &quot;it&#39;s fine, nothing to show&quot; is a legitimate resolution.</li><li><strong>Named, accepted limitation: LWW trusts the device clock.</strong> A device with a skewed clock could make a genuinely newer edit lose to a stale one. A hybrid logical clock is explicitly out of scope — over-engineering for this use case — but the tradeoff is named here rather than silently absorbed.</li></ul><h3 id="_6-permission-enforcement-—-honest-about-its-limits" tabindex="-1">6. Permission enforcement — honest about its limits <a class="header-anchor" href="#_6-permission-enforcement-—-honest-about-its-limits" aria-label="Permalink to &quot;6. Permission enforcement — honest about its limits&quot;">​</a></h3><p><code>offline:write</code> <strong>cannot</strong> be centrally enforced the way <code>mailer:send</code> is. <code>requireMailerPluginContext()</code> works because every plugin&#39;s <code>sdk.mailer.send()</code> call funnels through one shared function inside <code>runtime/src/sdk-host.ts</code> — a boundary the platform controls. A plugin&#39;s offline sync endpoint is the plugin&#39;s <strong>own</strong> Route Handler, code the platform does not intercept. This RFC recommends a small, documented, copy-pasteable <code>requirePluginPermission(pluginId, manifest, permission)</code> helper (generalizing <code>plugin-mailer.ts</code>&#39;s pattern) that a plugin&#39;s sync route is expected to call itself — real, useful signal, but not a closed enforcement boundary the way <code>mailer:send</code> is. State this plainly in <code>docs/plugin-development.md</code>: <code>offline:write</code>, like most permissions today (<code>db:readWrite</code>, <code>notifications:send</code>), is review/install-time metadata with an optional convenience helper, not a guarantee.</p><h3 id="_7-logout-login-purge-—-required-and-riskier-than-rfc-0074-s" tabindex="-1">7. Logout/login purge — required, and riskier than RFC 0074&#39;s <a class="header-anchor" href="#_7-logout-login-purge-—-required-and-riskier-than-rfc-0074-s" aria-label="Permalink to &quot;7. Logout/login purge — required, and riskier than RFC 0074&#39;s&quot;">​</a></h3><p><code>offlineQueue.clearAll()</code> must be wired into the same two call sites <code>offline.clearAll()</code> already is: <code>runtime/app/(platform)/_components/AccountMenu.tsx</code>&#39;s sign-out handler (~line 155) and <code>runtime/src/complete-sign-in.ts</code>&#39;s <code>completeSignIn()</code> (~line 22) — without it, a second user on a shared device could see, or trigger a sync of, the first user&#39;s un-synced queued writes after login, which is strictly worse than RFC 0074&#39;s read-cache leak risk (leaking a <em>write</em> is data corruption on shared data, not just a stale read).</p><p>But purging the <em>write queue</em> is a materially worse failure mode than purging the <em>read</em> cache: it <strong>silently discards edits the user made and hasn&#39;t yet gotten back online to save</strong>, not just a slower next cold-start. This RFC resolves that explicitly rather than inheriting RFC 0074&#39;s purge behavior by default:</p><ul><li>If the device is online at the moment of sign-out, attempt a best-effort, time-bounded (proposed 3s, matching the existing 4s <code>NetworkFirst</code> timeout precedent in <code>next.config.ts</code>) <code>drainQueue()</code> <strong>before</strong> purging. Sign-out must still complete regardless of the outcome — mirroring <code>AccountMenu.tsx</code>&#39;s existing <code>finally { form.submit() }</code> discipline — so this is &quot;try to save, don&#39;t block on it.&quot;</li><li>If offline, or the best-effort sync fails or times out, purge anyway — sign-out must never hang or get stuck.</li><li><strong>Open for reviewers</strong>: should a confirmation prompt (&quot;N unsynced changes — sign out anyway?&quot;) appear before the destructive purge, rather than purging silently? This is a product decision, not an engineering one — this RFC does not resolve it, and flags it explicitly rather than picking silently in an implementation.</li></ul><h3 id="docker-config-impact" tabindex="-1">Docker / config impact <a class="header-anchor" href="#docker-config-impact" aria-label="Permalink to &quot;Docker / config impact&quot;">​</a></h3><p>None expected — no new env var, port, on-disk path, or native dependency. Same conclusion as RFC 0074.</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><p><strong>First online visit to an offline-enabled plugin</strong> (e.g. Shopper):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>online → shell renders → client fetches lists/items → renders + offline.set(...)</span></span></code></pre></div><p><strong>Offline, viewing:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>offline → app opens → shell renders from precache</span></span>
<span class="line"><span>        → client offline.get(...) → renders cached data</span></span>
<span class="line"><span>        → OfflineBanner shows &quot;No internet connection&quot;</span></span></code></pre></div><p><strong>Offline, editing</strong> (add / edit / delete / mark bought):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>offline → user action → offlineQueue.enqueue(...) + optimistic local render</span></span>
<span class="line"><span>        → no visible difference from &quot;online and it just hasn&#39;t synced yet,&quot;</span></span>
<span class="line"><span>          except a persistent pending-sync indicator</span></span></code></pre></div><p><strong>Reconnect</strong> (shell mount or <code>online</code> event):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>online → drainQueue() runs → per-mutation outcomes reconcile the queue</span></span>
<span class="line"><span>       → full re-fetch overwrites the cache with canonical server state</span></span></code></pre></div><p><strong>Reconnect with a partial failure:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>online → drainQueue() → some mutations applied, one failed</span></span>
<span class="line"><span>       → pending indicator persists with a Retry action</span></span>
<span class="line"><span>       → nothing else on the list is blocked</span></span></code></pre></div><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep <code>offline.routes[]</code>, add writes on top of it.</strong> Rejected. It would perpetuate a route-enumeration burden that the one real adopter (Launcher) never needed, and doesn&#39;t map cleanly onto a mutation queue keyed by plugin + operation rather than by which URL rendered a button.</li><li><strong>Deprecate <code>offline.routes[]</code> but keep it valid, coexisting with the new model.</strong> Considered and rejected in favor of removing it outright — a single model is simpler to document, review, and reason about than two offline mechanisms with different rules, and no plugin in this repo has adopted <code>routes[]</code> yet, so the migration cost is bounded.</li><li><strong>Background Sync API</strong> for the sync trigger. Rejected — no iOS Safari support, would leave the majority mobile-PWA case unimplemented; plugin-driven drain-on-foreground works everywhere.</li><li><strong>Batch sync endpoint with all-or-nothing rollback.</strong> Rejected in favor of sequential-apply-halt-on-first-failure: rollback implies undoing already-applied idempotent writes, unnecessary complexity when each operation is independently safe to leave applied.</li><li><strong>Server-assigned real IDs with a client temp-ID swap.</strong> Rejected in favor of client-minted permanent IDs from the start (§4) — removes an entire reconciliation mechanism rather than building one.</li><li><strong>Hybrid logical clock instead of wall-clock LWW.</strong> Rejected as over-engineering for this RFC&#39;s explicitly &quot;simplest option, low concurrency&quot; scope; the clock-skew risk is accepted and documented instead (§5).</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Permission enforcement and logout-purge behavior (§§6–7) should ship resolved in the implementation, not left open</strong> — they&#39;re stated here with a recommended position, but are genuine product/security tradeoffs worth explicit reviewer sign-off before code lands.</li><li>Should <code>drainQueue</code> live in the SDK at all, or should each plugin&#39;s drain loop be 100% plugin-local, with the SDK only providing <code>enqueue</code>/<code>list</code>/<code>remove</code>? Leaning toward keeping it in the SDK — it reduces reimplementation for the next adopter after Shopper, which is the explicit point of prototyping in Shopper first — but open to reviewer input.</li><li>List-level operations (reorder, create/rename/archive a list, sharing) are out of scope for the first implementation — item-level operations only (add, edit, delete, mark bought). Whether those need finer per-operation permission granularity, beyond the single <code>offline:write</code> flag, is deferred to a future RFC once the one-shell pattern is validated with item-level writes.</li><li>The live-tab-vs-precached-document distinction noted under &quot;Current state&quot; (a live, online tab on an offline-declared route shouldn&#39;t show degraded chrome, only the cached copy needs to) is a real requirement for any implementation of §2&#39;s shell simplification, but this RFC does not specify the mechanism — left to the implementation task, informed by whatever pattern is in place by then.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Documentation-first, no roadmap slot yet (matches RFC 0071 and RFC 0077&#39;s posture). Once scheduled, implementation proceeds as a sequence of small, independently reviewable tasks rather than one PR:</p><ol><li>Manifest schema change (§1) + Launcher&#39;s manifest migrated in the same task, since the change is breaking to the one field already in use. <code>@sovereignfs/manifest</code> major bump.</li><li>Runtime simplification (§2) — registry/middleware/<code>next.config.ts</code>/ neutrality-scanner changes, verified against Launcher (the only existing adopter) as a regression check.</li><li>SDK mutation queue (§3) — <code>@sovereignfs/sdk/offline-queue</code>. <code>@sovereignfs/sdk</code> minor bump.</li><li>Shopper — schema migration (the new timestamp column + updating existing online write paths to maintain it, §5), then the read-only offline shell (§1–2 applied), then the write path on top (§3–7). Shopper&#39;s own independent semver, minor bump.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr></tbody></table>`,70)])])}const k=i(a,[["render",o]]);export{u as __pageData,k as default};
