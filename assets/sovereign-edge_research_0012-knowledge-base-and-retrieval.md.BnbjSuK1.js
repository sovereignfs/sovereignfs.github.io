import{_ as t,o,c as a,a2 as n}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Knowledge base and retrieval: where the corpus lives, and what that decides","description":"","frontmatter":{"id":12,"title":"Knowledge base and retrieval: where the corpus lives, and what that decides","status":"Decided — option A (on-device) first, remote deferred to a second phase","date":"September 2026","author":"Claude Code (session with the developer)","scope":"shared","summary":"Candidate capability — a knowledge base the model can answer from, and whether it is a chat feature, a connector, or both","sourceUrl":"https://github.com/sovereignfs/sovereign-edge/blob/main/docs/research/0012-knowledge-base-and-retrieval.md"},"headers":[],"relativePath":"sovereign-edge/research/0012-knowledge-base-and-retrieval.md","filePath":"sovereign-edge/research/0012-knowledge-base-and-retrieval.md"}'),s={name:"sovereign-edge/research/0012-knowledge-base-and-retrieval.md"};function r(i,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="research-0012-—-knowledge-base-and-retrieval-where-the-corpus-lives-and-what-that-decides" tabindex="-1">Research 0012 — Knowledge base and retrieval: where the corpus lives, and what that decides <a class="header-anchor" href="#research-0012-—-knowledge-base-and-retrieval-where-the-corpus-lives-and-what-that-decides" aria-label="Permalink to &quot;Research 0012 — Knowledge base and retrieval: where the corpus lives, and what that decides&quot;">​</a></h1><p><strong>Related:</strong> <a href="./0006-files-document-summarization.html">research 0006</a> (decided document input is a chat feature, and explicitly parked the chunking/ retrieval question this doc picks up), <a href="./0001-concept-and-connector-architecture.html">research 0001</a> (the tier model and the &quot;all outbound network goes through a connector&quot; rule), <a href="./../epics/mobile/sovereign-tasks-connector.html">Sovereign Tasks Connector</a> (the Tier 1 external-instance shape option B would copy), <a href="./../epics/shared/connector-store-sdk.html">Connector Store &amp; SDK</a> (task 5.6, Tier 2, deliberately blocked on a real use case)</p><hr><h2 id="question" tabindex="-1">Question <a class="header-anchor" href="#question" aria-label="Permalink to &quot;Question&quot;">​</a></h2><p>We want a knowledge base the model can answer from. Is that a chat feature, a connector, or both — and what does the answer depend on?</p><h2 id="findings" tabindex="-1">Findings <a class="header-anchor" href="#findings" aria-label="Permalink to &quot;Findings&quot;">​</a></h2><h3 id="the-question-forks-on-one-fact-where-the-corpus-lives" tabindex="-1">The question forks on one fact: where the corpus lives <a class="header-anchor" href="#the-question-forks-on-one-fact-where-the-corpus-lives" aria-label="Permalink to &quot;The question forks on one fact: where the corpus lives&quot;">​</a></h3><p>Everything else follows from this.</p><ul><li><strong>Corpus on the device.</strong> No trust boundary is crossed. Research 0006 already settled the identical question for single-document input and rejected the connector framing in as many words: &quot;no trust boundary is crossed; would misuse the framework built for the opposite case.&quot; A local knowledge base is the same argument at corpus scale — it is a chat feature.</li><li><strong>Corpus somewhere else</strong> (self-hosted wiki, a <code>sovereign</code> instance, a third-party service). <a href="./../../AGENTS.html">AGENTS.md</a>&#39;s hard rule 2 applies without exception: it goes through a permissioned, separately revocable connector.</li></ul><p>This is not a stylistic choice. Getting it backwards either dilutes what &quot;connector&quot; means in the product&#39;s trust story, or puts network code somewhere the architecture guarantees there is none.</p><h3 id="research-0006-parked-exactly-this-question" tabindex="-1">Research 0006 parked exactly this question <a class="header-anchor" href="#research-0006-parked-exactly-this-question" aria-label="Permalink to &quot;Research 0006 parked exactly this question&quot;">​</a></h3><p>0006&#39;s open questions include, verbatim: &quot;Context-window scoping for long documents: hard length cap for MVP, or is chunking/retrieval actually wanted? Needs product input.&quot; It deferred retrieval as &quot;a much bigger, separate piece of work.&quot; A knowledge base <em>is</em> that work. This doc is the continuation, not a new thread.</p><h3 id="the-retrieval-primitives-already-exist-on-both-platforms" tabindex="-1">The retrieval primitives already exist on both platforms <a class="header-anchor" href="#the-retrieval-primitives-already-exist-on-both-platforms" aria-label="Permalink to &quot;The retrieval primitives already exist on both platforms&quot;">​</a></h3><p>Neither app needs a new inference dependency for local retrieval.</p><ul><li><strong>Mobile</strong> — <code>llama.rn@0.12.8</code> exposes, on <code>LlamaContext</code>: <ul><li><code>embedding(text, params?)</code> → <code>{ embedding: number[] }</code></li><li><code>rerank(query, documents, params?)</code> → <code>RerankResult[]</code> of <code>{ score, index }</code> — but see the caveat below; this one is not free</li><li>Context params <code>embedding?: boolean</code>, <code>pooling_type?: number</code>, <code>embd_normalize?: number</code>; <code>NativeLlamaContext.model.nEmbd</code> reports the embedding dimension.</li></ul></li><li><strong>Desktop</strong> — <code>llama-cpp-2@0.1.154</code> exposes <code>LlamaContext::embeddings_ith</code> and <code>embeddings_seq_ith</code>, both returning <code>Result&lt;&amp;[f32], EmbeddingsError&gt;</code>.</li></ul><p><strong>Both platforms require a dedicated embedding context.</strong> Mobile&#39;s <code>embedding: boolean</code> context param and Rust&#39;s <code>EmbeddingsError::NotEnabled</code> / <code>NonePoolType</code> variants say the same thing from two directions: an embedding-enabled context with a pooling type is a <em>different</em> context from the chat context. You cannot serve both from one. On mobile that second resident context is the real cost of local retrieval, and it is a memory cost, not a CPU one.</p><p><strong><code>rerank()</code> costs a third model, not a free quality win.</strong> It is a method on <code>LlamaContext</code>, so it reranks using whatever model that context holds — which means a cross-encoder reranker model (bge-reranker class) in its own context with rank pooling, not the chat model and not the embedding model. Treat reranking as a later quality upgrade to be justified on measurement, not part of a first cut: on mobile, three resident models is a very different proposition from two. Cosine top-k alone is the MVP.</p><h3 id="two-concrete-gaps" tabindex="-1">Two concrete gaps <a class="header-anchor" href="#two-concrete-gaps" aria-label="Permalink to &quot;Two concrete gaps&quot;">​</a></h3><ul><li><strong>No embedding model in the catalog.</strong> All seven entries in <code>apps/mobile/src/models/catalog.ts</code> (mirrored in <code>apps/desktop/src-tauri/src/models/catalog.rs</code>) are generative. Local retrieval needs a small embedding GGUF — nomic-embed-text or MiniLM class — added to both catalogs. Acquiring it is covered by existing hard rule 3 (model acquisition is the one deliberate network exception), so this opens no new boundary; it is a catalog entry and a second resident context, not an architectural change.</li><li><strong>LFM2 1.2B RAG is currently unfed.</strong> Added to both catalogs in <code>a0f766c</code>, it is tuned to answer only from text supplied in the prompt — its own catalog comment says &quot;not a general chat model.&quot; Nothing in either app supplies it with retrieved text today. Whichever option below is chosen, it is the thing that makes that model make sense; until then it is a capability with no path to it.</li></ul><h3 id="where-a-local-index-may-live-—-a-hard-structural-constraint" tabindex="-1">Where a local index may live — a hard structural constraint <a class="header-anchor" href="#where-a-local-index-may-live-—-a-hard-structural-constraint" aria-label="Permalink to &quot;Where a local index may live — a hard structural constraint&quot;">​</a></h3><p><code>src/chat/</code> may not import <code>expo-file-system</code> <strong>at all</strong>. This is not the network rule leaking; it is its own entry in <code>apps/mobile/eslint.config.js</code>, justified there because that module &quot;carries <code>DownloadTask</code>.&quot; <code>src/chat/</code> also may not import <code>@/models</code> or <code>@/connectors</code>.</p><p>So no part of a local index — extraction, chunk storage, vectors, retrieval — can live in <code>src/chat/</code>. The established answer is the inversion <code>chatHistoryStore.ts</code> already uses: the store lives in the app shell, <code>ModelSessionProvider</code> owns it, and <code>src/chat/</code> reaches it only through an injected <code>ChatSessionContext</code> method. That file&#39;s own header comment documents the pattern and the reason. Retrieval would be a third consumer of an inversion already carrying the engine and the history store, which makes this considerably less speculative than it sounds.</p><h3 id="storage-has-no-vector-story-yet" tabindex="-1">Storage has no vector story yet <a class="header-anchor" href="#storage-has-no-vector-story-yet" aria-label="Permalink to &quot;Storage has no vector story yet&quot;">​</a></h3><p>Persistence today is plain JSON via <code>expo-file-system</code> — chat history, connector grants, installed connectors all use the same <code>Paths.document</code> pattern. There is no vector store, and at personal-corpus scale there may not need to be one: brute-force cosine over a few thousand chunks in JS is adequate. Reaching for sqlite-vec, faiss, or similar before measuring would repeat the mistake research 0003 exists to record, where a measured benchmark overturned a design that looked obviously correct on paper.</p><h2 id="options-considered" tabindex="-1">Options considered <a class="header-anchor" href="#options-considered" aria-label="Permalink to &quot;Options considered&quot;">​</a></h2><p><strong>A. Local document knowledge base — a chat feature, no connector.</strong> Pick files → extract text → chunk → embed → persist vectors → at query time embed the query, cosine top-k, fold the passages into the existing offline pipeline (reranking deferred — it is a third resident model, see Findings). Fully offline; no new trust surface. Inherits 0006&#39;s unresolved PDF-extraction spike (iOS has PDFKit; Android has no first-party text API), but 0006&#39;s own staging answer applies — plain text and markdown ship without waiting on it. Costs a second resident context and a catalog addition.</p><p><strong>B. Remote knowledge base — Tier 1 connector.</strong> A manifest with a <code>kb.search</code> tool against a self-hosted endpoint, token in secure storage. Structurally identical to the Sovereign Tasks connector (4.1/4.2) already specced and next in the Phase 2 queue — the same shape with a different verb, on rails that are already laid. Retrieval happens server-side: no embedding model, no second context, no PDF problem, no vector storage question. The cost is that every query leaves the device. The connector framework makes that legible, permissioned, and revocable, which is exactly what it is for — but legible egress is not the same claim as offline, and this option should not be described as if it were.</p><p><strong>C. Hybrid — sync over a connector, index and retrieve locally.</strong> A connector pulls documents in a discrete, user-initiated sync; chunking, embedding, retrieval, and generation all stay on-device. The best fit for the product&#39;s central claim: the network touch becomes an occasional visible event rather than a per-query one. Also the most work — all of A, plus a connector, plus sync scheduling and staleness handling.</p><p><strong>D. Neither yet — cap context and defer.</strong> 0006&#39;s own MVP fallback: a hard length cap or &quot;first N pages,&quot; no retrieval at all. Worth stating as the honest baseline, since it is what the app effectively does today. Rejected as a destination, but it is the correct thing to keep shipping while A or C is built.</p><h2 id="recommendation" tabindex="-1">Recommendation <a class="header-anchor" href="#recommendation" aria-label="Permalink to &quot;Recommendation&quot;">​</a></h2><p>Split by corpus location, because the fork is genuine and no single answer covers both:</p><ul><li><strong>Corpus on-device → A, staged per 0006&#39;s option C.</strong> Ship plain-text and markdown attachment first; add PDF text-layer extraction as a fast-follow. Choose the embedding model before anything else — its dimension and memory footprint constrain every downstream decision, including whether a second resident context is viable on mid-range phones at all.</li><li><strong>Corpus remote → C as the destination, B as the first step.</strong> B is roughly one task on existing rails; C is an epic. Shipping B first proves the corpus is the right shape before investing in local indexing, and its manifest is reusable as C&#39;s sync mechanism rather than thrown away.</li></ul><p>In both cases the LFM2 1.2B RAG model already in the catalog is the intended generator, and the work is what finally makes it usable.</p><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><p><strong>Option A — the on-device knowledge base — is the direction, with a remote knowledge base deferred to a second phase (options B/C, unchanged above).</strong> Decided by the developer, September 2026. Scope added at the same time, and detailed in <a href="./../epics/mobile/knowledge-base.html">epic 16</a>:</p><ul><li>The knowledge base is <strong>opt-in</strong>, off by default.</li><li>When on, it <strong>archives the user&#39;s conversations</strong> so they can be retrieved later — see &quot;Conversations are a corpus the app currently destroys&quot; below, which is why this is not a small addition.</li><li>The user can <strong>add and update their own content</strong>, per 0006&#39;s staging: plain text and markdown first, PDF text-layer as a fast-follow.</li></ul><p>Also settled, and not to be relitigated:</p><ul><li>A device-local corpus is a chat feature, not a connector (research 0006&#39;s reasoning, applied at corpus scale).</li><li>Any remote corpus goes through the connector layer, separately revocable (hard rule 2). This still governs the second phase.</li><li>No part of a local index lives in <code>src/chat/</code>; it follows <code>chatHistoryStore</code>&#39;s inversion through <code>ChatSessionContext</code>.</li></ul><h2 id="conversations-are-a-corpus-the-app-currently-destroys" tabindex="-1">Conversations are a corpus the app currently destroys <a class="header-anchor" href="#conversations-are-a-corpus-the-app-currently-destroys" aria-label="Permalink to &quot;Conversations are a corpus the app currently destroys&quot;">​</a></h2><p>Recorded after the decision above, because it changes the shape of the work and was not anticipated when the options were drafted.</p><p>The app persists exactly <strong>one</strong> conversation thread — <code>history.json</code>, via <code>apps/mobile/src/settings/chatHistoryStore.ts</code>, whose own header calls it &quot;the one persisted conversation thread.&quot; There is no multi-conversation concept anywhere in either app.</p><p>More importantly, that thread is <strong>destructively capped on write</strong>, not just on read. <code>ChatScreen.tsx</code> calls <code>session.saveHistory(capMessages([...]))</code> on every turn, and <code>capMessages</code> slices messages off the front once the character budget is exceeded. The oldest messages are therefore deleted from disk as a conversation grows. This is correct for what the file is — a working buffer sized to the next request&#39;s context window — and its own comment says so: capped on write &quot;so the list this turn&#39;s own request is built from never grows unbounded.&quot;</p><p>Two consequences:</p><ol><li><strong>The archive must be a second, separate store.</strong> Append-only, uncapped, and distinct from <code>history.json</code>, which keeps its current behaviour untouched. The two have genuinely different jobs — one feeds the next request, the other feeds retrieval — and merging them would either blow out the request context or keep destroying the archive.</li><li><strong>Opting in cannot recover the past.</strong> Everything <code>capMessages</code> has already dropped is gone. The archive starts at the moment of opt-in, and the UI should say so rather than implying a retroactive history.</li></ol><p><strong>Desktop persists no chat at all</strong> — nothing in <code>apps/desktop/src/chat/</code> writes history, so conversations there vanish on quit. Desktop is therefore a larger lift than a port of the mobile work, and is sequenced after it.</p><h3 id="indexing-the-model-s-own-output" tabindex="-1">Indexing the model&#39;s own output <a class="header-anchor" href="#indexing-the-model-s-own-output" aria-label="Permalink to &quot;Indexing the model&#39;s own output&quot;">​</a></h3><p>If assistant replies are archived and later retrieved, the model&#39;s mistakes become retrievable &quot;knowledge&quot; and can be recycled into future answers. This is a real failure mode, not a hypothetical, and it argues for: tagging every chunk with its role at archive time, keeping user-authored content preferred at retrieval, and showing retrieval provenance in-chat the way task 2.5 already does for connectors. Whether assistant-authored content should be retrievable <em>at all</em> by default is left open below.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li><strong>Which embedding model</strong> — dimension, quantization, and whether a second resident context alongside a 1–4B chat model is viable on a mid-range Android device. Needs measurement, not a spec-sheet comparison; research 0003 is the precedent for why.</li><li><strong>Vector storage shape</strong> — brute-force cosine over JSON until proven inadequate, or a real index from the start? Prefer the former until a measured corpus size says otherwise.</li><li><strong>Chunking strategy</strong> — fixed-size windows with overlap, or structure-aware splitting on headings? Interacts with whether the corpus is markdown (structured) or PDF-extracted (flat).</li><li><strong>Does a remote KB connector need Tier 2?</strong> Task 5.6 is deliberately blocked on a real Tier 2 use case existing. Reshaping arbitrary third-party KB API responses could be the first genuine one — worth watching for, not worth forcing.</li><li><strong>Android PDF text extraction</strong> — still 0006&#39;s open spike, still unanswered, and still a prerequisite for option A&#39;s second stage only (not its first).</li><li><strong>Should assistant-authored content be retrievable by default?</strong> Archiving both roles is necessary — half a conversation is not a conversation — but retrieving the model&#39;s own past output risks recycling its mistakes. Role tagging is the mechanism; the default is a product call.</li><li><strong>When does embedding happen?</strong> Embedding each turn as it arrives means an embedding context resident for the whole session, on top of the chat model. Batching instead (on opening the knowledge-base screen, or an explicit &quot;index now&quot;) keeps archive writes cheap and confines the second context to a bounded window. Prefer batching until measurement says otherwise; this interacts directly with the model-choice spike above.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><p>Superseded by <a href="./../epics/mobile/knowledge-base.html">epic 16</a>, which carries the task breakdown. The gating item is unchanged and is that epic&#39;s first task: <strong>spike the embedding model on real hardware before building anything on top of it.</strong> Its dimension and memory footprint decide whether a second resident context is viable at all on a mid-range Android device, and every later task assumes an answer. On-device measurement is the gate, per this repo&#39;s own verification convention and research 0003&#39;s precedent.</p>',51)])])}const g=t(s,[["render",r]]);export{u as __pageData,g as default};
