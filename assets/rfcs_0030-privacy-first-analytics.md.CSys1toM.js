import{_ as a,o as s,c as t,a2 as i}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Privacy-First Analytics","description":"","frontmatter":{"rfc":24,"title":"Privacy-First Analytics","status":"Accepted","date":"June 2026","author":"kasunben","scope":"packages/manifest, packages/sdk, packages/db, runtime, plugins/analytics (new), plugins/console\\n","incorporated_into_plan":"Yes — epic tasks 6.1 (server-side infrastructure) and 6.2 (client-side tracking + heatmaps) [post-v1]"},"headers":[],"relativePath":"rfcs/0030-privacy-first-analytics.md","filePath":"rfcs/0030-privacy-first-analytics.md"}'),n={name:"rfcs/0030-privacy-first-analytics.md"};function l(o,e,d,c,r,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-0030-—-privacy-first-analytics" tabindex="-1">RFC 0030 — Privacy-First Analytics <a class="header-anchor" href="#rfc-0030-—-privacy-first-analytics" aria-label="Permalink to &quot;RFC 0030 — Privacy-First Analytics&quot;">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Adds a fully self-hosted, operator-controlled analytics system to Sovereign as a new <strong>platform plugin</strong> (<code>plugins/analytics/</code>, id <code>fs.sovereign.analytics</code>). All data stays on the instance. Third-party analytics services are explicitly out of scope — they contradict Sovereign&#39;s privacy-first, cloud-autonomy positioning.</p><p>The system respects the browser&#39;s Do Not Track (<code>DNT</code>) and Global Privacy Control (<code>Sec-GPC</code>) signals as a <strong>hard block</strong> (zero data collected when either signal is set). Data collection is <strong>off by default</strong> — the plugin is installed and enabled, but collects nothing until an admin explicitly enables it in Analytics → Settings.</p><p>Analytics being a plugin (not a built-in feature) means operators who want zero analytics footprint can disable it via Console → Plugin Management, which blocks all routes, no-ops the SDK write path, and makes the config endpoint return <code>{ enabled: false }</code> — exactly like disabling any other plugin.</p><p>Implementation is split across two roadmap tasks: <strong>Task 1.0.5</strong> (server-side infrastructure: plugin scaffold, page-view recording, <code>sdk.analytics</code>, Analytics dashboard + settings) and <strong>Task 1.0.6</strong> (client-side click/scroll tracking and the heatmap visualization).</p><hr><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign targets operators who self-host for privacy and autonomy. They need usage insights — which plugins are most used, when the platform is busiest, which pages users get stuck on — but they should not have to route that data through a third-party service. Existing self-hosted analytics tools (Plausible, Umami, Matomo) solve this but require running a separate service and accepting its data model. A native analytics surface gives operators zero-configuration insights inside their existing deployment.</p><p>The activity log (RFC 0005, Task 0.5.12) captures explicit user actions for audit purposes. Analytics is a separate concern: aggregate usage patterns, page-view counts, click density, and plugin-authored conversion events. Mixing the two would dirty the audit trail with high-volume page-view noise and break the personal-feed guarantees of the activity log.</p><p>Making analytics a plugin (rather than built-in) is philosophically consistent with Sovereign&#39;s core thesis: the plugin system IS the product. Analytics is an optional feature, and optional features should be plugins. This also creates a clear extension point — third-party operators could build and install alternative analytics plugins.</p><hr><h2 id="current-state" tabindex="-1">Current state <a class="header-anchor" href="#current-state" aria-label="Permalink to &quot;Current state&quot;">​</a></h2><ul><li>The <strong>activity log</strong> (<code>activity_log</code> table, <code>sdk.activity.log()</code>) records explicit mutations. It is an audit trail, not a usage-metrics system.</li><li>The <strong>Console health dashboard</strong> shows liveness indicators but no usage data.</li><li>The <strong>middleware</strong> already has access to every request&#39;s plugin ID, pathname, session token, locale, and role — all the raw material for page-view analytics — but does nothing with it today.</li><li>There is <strong>no client-side telemetry</strong> of any kind.</li><li>The disabled-plugin list (<code>fetchDisabledPluginIds</code>) is only fetched inside the middleware <strong>when the request is under a plugin prefix</strong> (line 237 in <code>runtime/middleware.ts</code>). It is not fetched for every request. Analytics page-view gating therefore requires its own lightweight config fetch (<code>GET /api/analytics/config</code>), not a reuse of the disabled-plugin list.</li></ul><hr><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-core-principles" tabindex="-1">1. Core principles <a class="header-anchor" href="#_1-core-principles" aria-label="Permalink to &quot;1. Core principles&quot;">​</a></h3><ol><li><p><strong>Platform plugin, not runtime built-in.</strong> Analytics ships as <code>plugins/analytics/</code> (<code>type: platform</code>, <code>adminOnly: true</code>). Disabling the plugin via Console → Plugin Management removes the UI, blocks its routes, and no-ops all SDK writes — exactly the same as disabling any other plugin.</p></li><li><p><strong>Collection off by default.</strong> The analytics plugin is installed and enabled by default (it is a platform plugin), but data collection is disabled until an admin explicitly enables it in Analytics → Settings. There are two separate controls:</p><table tabindex="0"><thead><tr><th>Control</th><th>Location</th><th>Effect</th></tr></thead><tbody><tr><td>Collection enabled</td><td>Analytics → Settings tab</td><td>Toggles <code>analytics_collection_enabled</code> in <code>platform_settings</code>. UI still accessible.</td></tr><tr><td>Plugin enabled</td><td>Console → Plugin Management</td><td>Disables plugin entirely: routes blocked, SDK no-ops, config endpoint returns <code>{ enabled: false }</code>.</td></tr></tbody></table></li><li><p><strong>Hard DNT / GPC block.</strong> When either <code>DNT: 1</code> or <code>Sec-GPC: 1</code> is present in the request headers, zero data is collected for that request. The client-side script additionally checks <code>navigator.doNotTrack</code> and <code>navigator.globalPrivacyControl</code> before initializing — if either is truthy, the script exits immediately with no event listeners attached.</p></li><li><p><strong>No user IDs in analytics tables.</strong> Events are keyed to a daily-rotating session hash (<code>SHA-256(session_token + daily_salt)</code>). The salt is stored in <code>platform_settings</code> alongside the date it was issued; when the date changes, a new salt is generated and the old one is discarded. Cross-day individual tracking is architecturally impossible.</p></li><li><p><strong>Admin-only access.</strong> Analytics data is never returned to <code>platform:user</code> sessions. All analytics API routes require the <code>health:view</code> capability or the admin key.</p></li><li><p><strong>Configurable retention with automated cleanup.</strong> Default 90 days. Rows older than <code>analytics_retention_days</code> are deleted at startup. No indefinite accumulation.</p></li><li><p><strong>No third-party services, CDNs, or external endpoints.</strong> The client script is served from the runtime. All data is written to the platform DB.</p></li></ol><h3 id="_2-analytics-as-a-platform-plugin" tabindex="-1">2. Analytics as a platform plugin <a class="header-anchor" href="#_2-analytics-as-a-platform-plugin" aria-label="Permalink to &quot;2. Analytics as a platform plugin&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/analytics/</span></span>
<span class="line"><span>├── manifest.json</span></span>
<span class="line"><span>│   { &quot;id&quot;: &quot;fs.sovereign.analytics&quot;,</span></span>
<span class="line"><span>│     &quot;name&quot;: &quot;Analytics&quot;,</span></span>
<span class="line"><span>│     &quot;type&quot;: &quot;platform&quot;,</span></span>
<span class="line"><span>│     &quot;shell&quot;: &quot;default&quot;,</span></span>
<span class="line"><span>│     &quot;routePrefix&quot;: &quot;/analytics&quot;,</span></span>
<span class="line"><span>│     &quot;adminOnly&quot;: true,</span></span>
<span class="line"><span>│     &quot;database&quot;: &quot;shared&quot;,</span></span>
<span class="line"><span>│     &quot;permissions&quot;: [&quot;auth:session&quot;, &quot;db:readWrite&quot;],</span></span>
<span class="line"><span>│     &quot;compatibility&quot;: { &quot;minPlatformVersion&quot;: &quot;1.0.0&quot; } }</span></span>
<span class="line"><span>├── icon.svg</span></span>
<span class="line"><span>├── app/</span></span>
<span class="line"><span>│   ├── layout.tsx         # nav tabs: Dashboard / Heatmap (Task 1.0.6) / Settings</span></span>
<span class="line"><span>│   ├── page.tsx           # Dashboard: DAU, page views by plugin, custom events</span></span>
<span class="line"><span>│   ├── heatmap/</span></span>
<span class="line"><span>│   │   └── page.tsx       # Click heatmap + scroll depth (Task 1.0.6)</span></span>
<span class="line"><span>│   └── settings/</span></span>
<span class="line"><span>│       └── page.tsx       # Collection toggle, retention, export, clear-all</span></span>
<span class="line"><span>├── db/</span></span>
<span class="line"><span>│   └── schema.ts          # EMPTY — analytics tables live in packages/db</span></span>
<span class="line"><span>└── package.json</span></span></code></pre></div><p><code>shell: &quot;default&quot;</code> — full-page navigation (suitable for a dashboard with charts). The analytics icon appears in the sidebar&#39;s middle section for admin users alongside other installed plugins. <code>adminOnly: true</code> means <code>platform:user</code> sessions see no icon and get 403 on any <code>/analytics</code> route.</p><h4 id="why-api-routes-live-in-the-runtime-not-the-plugin" tabindex="-1">Why API routes live in the runtime, not the plugin <a class="header-anchor" href="#why-api-routes-live-in-the-runtime-not-the-plugin" aria-label="Permalink to &quot;Why API routes live in the runtime, not the plugin&quot;">​</a></h4><p>The middleware must call <code>GET /api/analytics/config</code> on <strong>every request</strong> (before the session gate, before any plugin routing) to decide whether to record the page view. If this were a plugin route at <code>/analytics/api/*</code>, the middleware would need to complete its own gating logic before calling it — a circular dependency.</p><p>Similarly, <code>POST /api/analytics/internal/page-view</code> is called fire-and-forget by the middleware. It must be a runtime route. The client-side <code>POST /api/analytics/event</code> and <code>GET /api/analytics/script.js</code> are excluded from the session gate and are simpler as runtime routes.</p><p>All analytics API routes live in <code>runtime/app/api/analytics/</code>. <code>&#39;analytics&#39;</code> is added to <code>RESERVED_API_SEGMENTS</code> (dir-parity test passes).</p><h3 id="_3-analytics-config-endpoint" tabindex="-1">3. Analytics config endpoint <a class="header-anchor" href="#_3-analytics-config-endpoint" aria-label="Permalink to &quot;3. Analytics config endpoint&quot;">​</a></h3><p><code>GET /api/analytics/config</code> (edge-cached 60 s) returns:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;enabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;salt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;a1b2c3...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;saltDate&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-06-22&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;retentionDays&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">90</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>enabled = plugin_status.is_enabled(&#39;fs.sovereign.analytics&#39;) AND platform_settings.analytics_collection_enabled == &#39;true&#39;</code></p><p>The middleware reads this endpoint at the top of every request. If <code>enabled: false</code>, it skips all page-view recording for that request. If <code>enabled: true</code> but <code>DNT</code>/<code>GPC</code> header is present, it also skips.</p><h3 id="_4-database-tables" tabindex="-1">4. Database tables <a class="header-anchor" href="#_4-database-tables" aria-label="Permalink to &quot;4. Database tables&quot;">​</a></h3><p>Four new tables in <code>packages/db</code> (shared platform DB, same precedent as <code>activity_log</code>, <code>notifications</code>). All carry <code>tenant_id</code> for the v1 multi-tenancy forward-compatibility rule.</p><h4 id="analytics-page-views-task-1-0-5" tabindex="-1"><code>analytics_page_views</code> <em>(Task 1.0.5)</em> <a class="header-anchor" href="#analytics-page-views-task-1-0-5" aria-label="Permalink to &quot;\`analytics_page_views\` _(Task 1.0.5)_&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text (ULID)</td><td>PK</td></tr><tr><td><code>tenant_id</code></td><td>text</td><td>Multi-tenant scoping</td></tr><tr><td><code>session_hash</code></td><td>text</td><td>SHA-256(session_token + daily_salt); never a user ID</td></tr><tr><td><code>plugin_id</code></td><td>text nullable</td><td>Plugin whose route was visited; null = platform chrome</td></tr><tr><td><code>pathname</code></td><td>text</td><td>URL path only — no query string (query strings can contain tokens or PII)</td></tr><tr><td><code>locale</code></td><td>text nullable</td><td>From <code>x-sovereign-user-locale</code> header</td></tr><tr><td><code>screen_class</code></td><td>text nullable</td><td><code>&#39;mobile&#39;</code>|<code>&#39;tablet&#39;</code>|<code>&#39;desktop&#39;</code> bucketed from screen width; NOT the User-Agent string</td></tr><tr><td><code>created_at</code></td><td>integer/bigint</td><td>Unix seconds</td></tr></tbody></table><p>Indexes: <code>(tenant_id, plugin_id, created_at DESC)</code>, <code>(session_hash)</code>, <code>(created_at)</code>.</p><h4 id="analytics-events-task-1-0-5" tabindex="-1"><code>analytics_events</code> <em>(Task 1.0.5)</em> <a class="header-anchor" href="#analytics-events-task-1-0-5" aria-label="Permalink to &quot;\`analytics_events\` _(Task 1.0.5)_&quot;">​</a></h4><p>Plugin-authored custom events via <code>sdk.analytics.track()</code>.</p><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text (ULID)</td><td>PK</td></tr><tr><td><code>tenant_id</code></td><td>text</td><td></td></tr><tr><td><code>session_hash</code></td><td>text</td><td>Same daily hash</td></tr><tr><td><code>plugin_id</code></td><td>text</td><td>Source plugin; runtime auto-namespaces the event name</td></tr><tr><td><code>event_name</code></td><td>text</td><td><code>&lt;pluginId&gt;.&lt;event&gt;</code>, e.g. <code>fs.sovereign.console.plugin_enabled</code></td></tr><tr><td><code>properties</code></td><td>text nullable</td><td>JSON blob; plugin-provided (PII warning in docs)</td></tr><tr><td><code>created_at</code></td><td>integer/bigint</td><td></td></tr></tbody></table><p>Indexes: <code>(tenant_id, event_name, created_at DESC)</code>, <code>(plugin_id, created_at)</code>.</p><h4 id="analytics-click-events-task-1-0-6" tabindex="-1"><code>analytics_click_events</code> <em>(Task 1.0.6)</em> <a class="header-anchor" href="#analytics-click-events-task-1-0-6" aria-label="Permalink to &quot;\`analytics_click_events\` _(Task 1.0.6)_&quot;">​</a></h4><p>Click positions collected by the client script.</p><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text (ULID)</td><td>PK</td></tr><tr><td><code>tenant_id</code></td><td>text</td><td></td></tr><tr><td><code>session_hash</code></td><td>text</td><td></td></tr><tr><td><code>plugin_id</code></td><td>text nullable</td><td></td></tr><tr><td><code>pathname</code></td><td>text</td><td></td></tr><tr><td><code>x_norm</code></td><td>real</td><td>Click X as fraction of viewport width (0.0–1.0)</td></tr><tr><td><code>y_norm</code></td><td>real</td><td>Click Y as fraction of document height (0.0–1.0, scroll-offset included)</td></tr><tr><td><code>element_selector</code></td><td>text nullable</td><td><code>tag.firstClass</code> only. IDs excluded (can contain user data). Form fields (<code>input</code>, <code>textarea</code>, <code>select</code>) excluded entirely.</td></tr><tr><td><code>created_at</code></td><td>integer/bigint</td><td></td></tr></tbody></table><p>Indexes: <code>(tenant_id, plugin_id, pathname, created_at DESC)</code>.</p><h4 id="analytics-scroll-events-task-1-0-6" tabindex="-1"><code>analytics_scroll_events</code> <em>(Task 1.0.6)</em> <a class="header-anchor" href="#analytics-scroll-events-task-1-0-6" aria-label="Permalink to &quot;\`analytics_scroll_events\` _(Task 1.0.6)_&quot;">​</a></h4><p>One row per page visit, sent on page unload.</p><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text (ULID)</td><td>PK</td></tr><tr><td><code>tenant_id</code></td><td>text</td><td></td></tr><tr><td><code>session_hash</code></td><td>text</td><td></td></tr><tr><td><code>plugin_id</code></td><td>text nullable</td><td></td></tr><tr><td><code>pathname</code></td><td>text</td><td></td></tr><tr><td><code>max_scroll_pct</code></td><td>integer</td><td>0–100: deepest scroll position reached</td></tr><tr><td><code>time_on_page_s</code></td><td>integer</td><td>Seconds capped at 1 800 (30 min) to prevent idle inflation</td></tr><tr><td><code>created_at</code></td><td>integer/bigint</td><td></td></tr></tbody></table><h3 id="_5-platform-settings-keys" tabindex="-1">5. Platform settings keys <a class="header-anchor" href="#_5-platform-settings-keys" aria-label="Permalink to &quot;5. Platform settings keys&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Key</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>analytics_collection_enabled</code></td><td><code>&#39;false&#39;</code></td><td>Master collection switch; set in Analytics → Settings</td></tr><tr><td><code>analytics_retention_days</code></td><td><code>&#39;90&#39;</code></td><td>Row age limit; cleanup runs at startup</td></tr><tr><td><code>analytics_daily_salt</code></td><td>auto-generated UUID</td><td>Rotated lazily when <code>analytics_salt_date</code> ≠ today</td></tr><tr><td><code>analytics_salt_date</code></td><td>today&#39;s ISO date</td><td>Rotation trigger</td></tr></tbody></table><p>Salt rotation is <strong>lazy</strong>: on every page-view write, compare <code>analytics_salt_date</code> against today&#39;s UTC date. If they differ, generate a new <code>analytics_daily_salt</code>, update both keys atomically, use the new salt for the current request.</p><h3 id="_6-manifest-permission-analytics-write" tabindex="-1">6. Manifest permission: <code>analytics:write</code> <a class="header-anchor" href="#_6-manifest-permission-analytics-write" aria-label="Permalink to &quot;6. Manifest permission: \`analytics:write\`&quot;">​</a></h3><p>A new permission string for OTHER plugins to call <code>sdk.analytics.track()</code>. Follows the existing permission model in <code>packages/manifest/src/schema.ts</code>.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auth:session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;analytics:write&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }</span></span></code></pre></div><p>The analytics plugin itself uses <code>db:readWrite</code> (to query analytics tables for the dashboard). It does not need <code>analytics:write</code> — the SDK host writes directly.</p><h3 id="_7-sdk-analytics-surface" tabindex="-1">7. <code>sdk.analytics</code> surface <a class="header-anchor" href="#_7-sdk-analytics-surface" aria-label="Permalink to &quot;7. \`sdk.analytics\` surface&quot;">​</a></h3><p>New <code>packages/sdk/src/analytics.ts</code>, exported from <code>packages/sdk/src/index.ts</code> in the <strong>experimental</strong> group:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> analytics</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * Track a custom analytics event.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   *</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * No-ops when:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * - The analytics plugin is disabled.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * - Analytics collection is disabled in Analytics → Settings.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * - The current plugin lacks the \`analytics:write\` manifest permission.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * - The request carries \`DNT: 1\` or \`Sec-GPC: 1\`.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   *</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * The event name is auto-namespaced to \`&lt;pluginId&gt;.&lt;event&gt;\` by the runtime.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * Never put PII in \`properties\` — the platform does not redact it.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  track</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">properties</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * Returns true if the analytics plugin is enabled AND collection is enabled AND</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * the current request does not carry a DNT/GPC opt-out signal.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   * Useful for conditionally rendering analytics-related UI in a plugin.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  isEnabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>SdkHost</code> gains an <code>analytics</code> key. Implementation in <code>runtime/src/sdk-host.ts</code>:</p><ul><li>Checks <code>isAnalyticsEnabled()</code> (reads plugin status + collection setting, 60 s in-process cache).</li><li>Checks <code>DNT</code>/<code>Sec-GPC</code> headers from the request context.</li><li>Writes to <code>analytics_events</code> via <code>packages/db</code> helpers.</li></ul><h3 id="_8-middleware-changes" tabindex="-1">8. Middleware changes <a class="header-anchor" href="#_8-middleware-changes" aria-label="Permalink to &quot;8. Middleware changes&quot;">​</a></h3><p>After the session-verification step, before the plugin gate, the middleware performs page-view recording:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. GET /api/analytics/config (edge-cached 60 s)</span></span>
<span class="line"><span>   → { enabled: boolean, salt, saltDate, retentionDays }</span></span>
<span class="line"><span>2. If enabled = false → skip (analytics plugin disabled or collection off).</span></span>
<span class="line"><span>3. If header DNT === &#39;1&#39; OR Sec-GPC === &#39;1&#39; → skip (hard block).</span></span>
<span class="line"><span>4. Lazy salt rotation check: if saltDate ≠ today, call POST /api/analytics/internal/rotate-salt.</span></span>
<span class="line"><span>5. session_hash = SHA-256(session_token + salt) in hex.</span></span>
<span class="line"><span>6. Fire-and-forget: POST /api/analytics/internal/page-view</span></span>
<span class="line"><span>   { session_hash, plugin_id, pathname, locale, screen_class }</span></span>
<span class="line"><span>   (non-blocking — analytics must never add latency to the response path).</span></span></code></pre></div><p><code>screen_class</code> is derived from the <code>sv-screen-class</code> cookie (set by the client script on first load), NOT from User-Agent parsing. If the cookie is absent, <code>screen_class</code> is null.</p><h3 id="_9-runtime-api-routes" tabindex="-1">9. Runtime API routes <a class="header-anchor" href="#_9-runtime-api-routes" aria-label="Permalink to &quot;9. Runtime API routes&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Route</th><th>Auth</th><th>Notes</th></tr></thead><tbody><tr><td><code>GET /api/analytics/config</code></td><td>admin key</td><td>Edge-cached; reads plugin status + collection setting</td></tr><tr><td><code>POST /api/analytics/internal/page-view</code></td><td>admin key (internal)</td><td>Node-only; writes <code>analytics_page_views</code></td></tr><tr><td><code>GET /api/admin/analytics</code></td><td><code>health:view</code> capability</td><td>Aggregate query endpoint: DAU, page views by plugin, events</td></tr><tr><td><code>POST /api/analytics/event</code></td><td>session cookie</td><td>Client-side click/scroll receiver (Task 1.0.6)</td></tr><tr><td><code>GET /api/analytics/script.js</code></td><td>public</td><td>Self-hosted client script (Task 1.0.6)</td></tr></tbody></table><p>All analytics routes excluded from the middleware session gate (they handle their own auth). <code>&#39;analytics&#39;</code> added to <code>RESERVED_API_SEGMENTS</code>.</p><h3 id="_10-analytics-plugin-ui" tabindex="-1">10. Analytics plugin UI <a class="header-anchor" href="#_10-analytics-plugin-ui" aria-label="Permalink to &quot;10. Analytics plugin UI&quot;">​</a></h3><h4 id="dashboard-page-analytics" tabindex="-1">Dashboard page (<code>/analytics</code>) <a class="header-anchor" href="#dashboard-page-analytics" aria-label="Permalink to &quot;Dashboard page (\`/analytics\`)&quot;">​</a></h4><ul><li><strong>Active sessions / day</strong> — bar chart, count of unique <code>session_hash</code> values per day (proxy for DAU without naming individuals).</li><li><strong>Page views by plugin</strong> — stacked bar chart, last 7 / 30 / 90 days (date range picker).</li><li><strong>Top pages</strong> — table: pathname, view count, median time on page.</li><li><strong>Custom events</strong> — table: event name, plugin, count, last seen.</li><li>Charts rendered with native <code>&lt;canvas&gt;</code> (no charting library dependency).</li></ul><h4 id="heatmap-page-analytics-heatmap-task-1-0-6" tabindex="-1">Heatmap page (<code>/analytics/heatmap</code>) <em>(Task 1.0.6)</em> <a class="header-anchor" href="#heatmap-page-analytics-heatmap-task-1-0-6" aria-label="Permalink to &quot;Heatmap page (\`/analytics/heatmap\`) _(Task 1.0.6)_&quot;">​</a></h4><ul><li><strong>Route selector</strong>: plugin dropdown + free-text path input (e.g. <code>/console/users</code>).</li><li><strong>Click heatmap</strong>: a <code>&lt;canvas&gt;</code> sized to a 1×1 normalized coordinate space. Each click position is rendered as a radial gradient dot; overlapping dots accumulate color intensity (cold = blue, warm = yellow, hot = red). Optional <code>&lt;iframe sandbox=&quot;allow-same-origin allow-scripts&quot; style=&quot;pointer-events:none&quot;&gt;</code> behind the canvas shows the live plugin page as layout reference.</li><li><strong>Scroll depth bar</strong>: segmented bar showing % of sessions that reached 25/50/75/100%.</li><li>Date range filter.</li></ul><h4 id="settings-page-analytics-settings" tabindex="-1">Settings page (<code>/analytics/settings</code>) <a class="header-anchor" href="#settings-page-analytics-settings" aria-label="Permalink to &quot;Settings page (\`/analytics/settings\`)&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Setting</th><th>Control</th><th>Effect</th></tr></thead><tbody><tr><td>Enable collection</td><td>Toggle</td><td>Writes <code>analytics_collection_enabled</code></td></tr><tr><td>Retention period</td><td>Dropdown: 30 / 60 / 90 / 180 days</td><td>Writes <code>analytics_retention_days</code></td></tr><tr><td>Export data</td><td>Button</td><td><code>GET /api/admin/analytics?format=csv</code> — all tables</td></tr><tr><td>Clear all data</td><td>Destructive button (confirm dialog)</td><td>Truncates all <code>analytics_*</code> tables</td></tr></tbody></table><h3 id="_11-console-changes-minimal" tabindex="-1">11. Console changes (minimal) <a class="header-anchor" href="#_11-console-changes-minimal" aria-label="Permalink to &quot;11. Console changes (minimal)&quot;">​</a></h3><p>Analytics moves OUT of Console. Console gets smaller:</p><ul><li>No Analytics settings section in Console Settings.</li><li>No Analytics tab.</li><li>The plugin list in Console → Plugin Management shows the Analytics plugin (same as all platform plugins), including the enable/disable toggle.</li><li>Console → Health page gets a &quot;View analytics →&quot; link to <code>/analytics</code>.</li></ul><h3 id="_12-client-side-analytics-script-task-1-0-6" tabindex="-1">12. Client-side analytics script <em>(Task 1.0.6)</em> <a class="header-anchor" href="#_12-client-side-analytics-script-task-1-0-6" aria-label="Permalink to &quot;12. Client-side analytics script _(Task 1.0.6)_&quot;">​</a></h3><p>Self-hosted at <code>GET /api/analytics/script.js</code>. Returns empty 200 if analytics is disabled or collection is off. Loaded in <code>runtime/app/layout.tsx</code> via <code>&lt;Script src=&quot;/api/analytics/script.js&quot; strategy=&quot;afterInteractive&quot;&gt;</code>.</p><p>On load, the script:</p><ol><li>Checks <code>navigator.doNotTrack === &#39;1&#39;</code> and <code>navigator.globalPrivacyControl</code>. If either is truthy, exits immediately — no event listeners, no network calls.</li><li>Derives <code>screen_class</code> from <code>window.innerWidth</code>, sends it once to a lightweight route that sets the <code>sv-screen-class</code> cookie (skipped if cookie already exists).</li><li>Attaches a delegated <code>click</code> listener on <code>document</code>. On each click: <ul><li><code>x_norm = event.clientX / window.innerWidth</code></li><li><code>y_norm = (event.clientY + window.scrollY) / document.body.scrollHeight</code></li><li><code>element_selector = tag.firstClass</code> (excludes <code>input</code>, <code>textarea</code>, <code>select</code>, <code>option</code>, <code>label</code>; excludes IDs).</li><li>Adds to a local batch (capped at 50 events per flush).</li></ul></li><li>Tracks <code>max_scroll_pct</code> via a throttled <code>scroll</code> listener.</li><li>On page unload (<code>visibilitychange</code> hidden + <code>pagehide</code>), sends the batch to <code>POST /api/analytics/event</code> via <code>navigator.sendBeacon</code>.</li></ol><p>~3 KB minified. No cookies, no localStorage, no persistent identifier in the browser.</p><hr><h2 id="data-flow-diagrams" tabindex="-1">Data flow diagrams <a class="header-anchor" href="#data-flow-diagrams" aria-label="Permalink to &quot;Data flow diagrams&quot;">​</a></h2><h3 id="page-view-server-side-task-1-0-5" tabindex="-1">Page view (server-side, Task 1.0.5) <a class="header-anchor" href="#page-view-server-side-task-1-0-5" aria-label="Permalink to &quot;Page view (server-side, Task 1.0.5)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Browser           Edge Middleware                Node Runtime              DB</span></span>
<span class="line"><span>  │                     │                              │                    │</span></span>
<span class="line"><span>  │──GET /launcher──────▶                              │                    │</span></span>
<span class="line"><span>  │                     │── GET /api/analytics/config ─▶                   │</span></span>
<span class="line"><span>  │                     │◀─ { enabled: true, salt }────│                   │</span></span>
<span class="line"><span>  │                     │── check DNT/GPC header        │                   │</span></span>
<span class="line"><span>  │                     │   (DNT absent → proceed)      │                   │</span></span>
<span class="line"><span>  │                     │── fire-and-forget ────────────▶                  │</span></span>
<span class="line"><span>  │                     │   POST /analytics/internal/   │── INSERT ─────────▶</span></span>
<span class="line"><span>  │                     │   page-view                   │   analytics_page_views</span></span>
<span class="line"><span>  │◀── 200 page HTML ───│                              │                    │</span></span></code></pre></div><h3 id="plugin-enabled-disabled-effect-on-collection" tabindex="-1">Plugin enabled/disabled effect on collection <a class="header-anchor" href="#plugin-enabled-disabled-effect-on-collection" aria-label="Permalink to &quot;Plugin enabled/disabled effect on collection&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Admin: Console → Plugin Management → disable Analytics</span></span>
<span class="line"><span>  → plugin_status.is_enabled = false for &#39;fs.sovereign.analytics&#39;</span></span>
<span class="line"><span>  → GET /api/analytics/config cache invalidated (60 s max staleness)</span></span>
<span class="line"><span>  → Middleware sees { enabled: false } → stops recording page views</span></span>
<span class="line"><span>  → sdk.analytics.track() called by any plugin → isEnabled() returns false → no-op</span></span>
<span class="line"><span>  → GET /analytics → 404 (middleware blocks disabled plugin routes)</span></span></code></pre></div><h3 id="custom-event-plugin-server-component" tabindex="-1">Custom event (plugin server component) <a class="header-anchor" href="#custom-event-plugin-server-component" aria-label="Permalink to &quot;Custom event (plugin server component)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Plugin server component        sdk-host.ts                  DB</span></span>
<span class="line"><span>  │                                 │                         │</span></span>
<span class="line"><span>  │──await sdk.analytics.track(     │                         │</span></span>
<span class="line"><span>  │    &#39;feature.used&#39;, { plan:&#39;pro&#39;}│                         │</span></span>
<span class="line"><span>  │  )                              │                         │</span></span>
<span class="line"><span>  │                                 │── isEnabled() (cached)  │</span></span>
<span class="line"><span>  │                                 │── check DNT header      │</span></span>
<span class="line"><span>  │                                 │── namespace event name  │</span></span>
<span class="line"><span>  │                                 │── INSERT ───────────────▶</span></span>
<span class="line"><span>  │                                 │   analytics_events       │</span></span></code></pre></div><hr><h2 id="privacy-guarantees" tabindex="-1">Privacy guarantees <a class="header-anchor" href="#privacy-guarantees" aria-label="Permalink to &quot;Privacy guarantees&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Guarantee</th><th>Mechanism</th></tr></thead><tbody><tr><td>No PII in analytics tables</td><td>User IDs, email addresses, names never stored; only daily-rotating anonymous session hash</td></tr><tr><td>Hard DNT / GPC block</td><td>Middleware and client script check both signals; zero-write when either is set</td></tr><tr><td>Daily amnesia window</td><td>Salt rotates daily; cross-day session correlation is architecturally impossible</td></tr><tr><td>No third-party exfiltration</td><td>All writes go to <code>/api/analytics/*</code> on the same origin; no external HTTP calls</td></tr><tr><td>Admin-only read access</td><td>All analytics query routes require <code>health:view</code> capability or admin key</td></tr><tr><td>Configurable retention</td><td>Rows older than <code>analytics_retention_days</code> deleted at startup</td></tr><tr><td>No User-Agent strings stored</td><td>Screen class is bucketed (3 values); UA strings dropped</td></tr><tr><td>No query strings in page views</td><td><code>pathname</code> is stripped of query strings before storage</td></tr><tr><td>Form fields excluded from click tracking</td><td><code>input</code>/<code>textarea</code>/<code>select</code> elements excluded from <code>element_selector</code></td></tr><tr><td>True disable via plugin management</td><td>Disabling the plugin stops all collection — no partial state</td></tr></tbody></table><hr><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="built-in-runtime-feature-with-a-settings-toggle" tabindex="-1">Built-in runtime feature with a settings toggle <a class="header-anchor" href="#built-in-runtime-feature-with-a-settings-toggle" aria-label="Permalink to &quot;Built-in runtime feature with a settings toggle&quot;">​</a></h3><p>Keeping analytics as a runtime built-in with <code>analytics_enabled</code> in <code>platform_settings</code> was the initial plan (RFC 0030 rev 1). Rejected because:</p><ul><li>It is inconsistent with Sovereign&#39;s plugin philosophy — analytics is an optional feature, and optional features should be plugins.</li><li>&quot;Disable analytics entirely&quot; becomes a buried settings toggle rather than a first-class plugin management action.</li><li>Third-party operators cannot replace the built-in with their own analytics plugin.</li><li>Routes, DB write paths, and SDK registrations exist in the runtime even when analytics is &quot;off&quot; — unnecessary footprint.</li></ul><h3 id="third-party-self-hosted-analytics-plausible-umami-matomo" tabindex="-1">Third-party self-hosted analytics (Plausible, Umami, Matomo) <a class="header-anchor" href="#third-party-self-hosted-analytics-plausible-umami-matomo" aria-label="Permalink to &quot;Third-party self-hosted analytics (Plausible, Umami, Matomo)&quot;">​</a></h3><p>Purpose-built and feature-rich, but require a separate service, add a compose entry, and put data in an external DB with no Console integration. Rejected.</p><h3 id="extending-the-activity-log" tabindex="-1">Extending the activity log <a class="header-anchor" href="#extending-the-activity-log" aria-label="Permalink to &quot;Extending the activity log&quot;">​</a></h3><p>The activity log is an audit trail per-user. Adding page-view noise (hundreds of rows per session per day) breaks its primary purpose and personal-feed performance. Rejected.</p><h3 id="soft-dnt-aggregate-always-per-session-suppressed" tabindex="-1">Soft DNT (aggregate always, per-session suppressed) <a class="header-anchor" href="#soft-dnt-aggregate-always-per-session-suppressed" aria-label="Permalink to &quot;Soft DNT (aggregate always, per-session suppressed)&quot;">​</a></h3><p>Some tools count all requests and suppress only per-session attribution for DNT users. This interpretation is contested and inconsistently applied. Hard block is simpler, safer, and more honest. Rejected.</p><hr><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><p><strong><code>Sec-GPC</code> equality.</strong> <code>Sec-GPC: 1</code> (Global Privacy Control) is sent by Firefox with Enhanced Tracking Protection and by most privacy-focused browsers. The recommendation is to treat it identically to <code>DNT: 1</code>. Any objection?</p></li><li><p><strong>Per-user opt-out without browser DNT.</strong> Should Account Preferences expose an &quot;Analytics opt-out&quot; toggle so users can opt out without setting DNT globally? Would add an <code>account_prefs</code> flag and one more middleware read. Deferred — the hard block covers users who set DNT at the browser level.</p></li><li><p><strong>Salt rotation precision.</strong> Lazy rotation means the salt changes on the first analytics-eligible request after midnight UTC, not exactly at midnight. Sessions straddling midnight may appear as &quot;new sessions&quot; in daily counts. Acceptable tradeoff for avoiding a cron job?</p></li><li><p><strong>Element selector safety.</strong> The simplified selector <code>tag.firstClass</code> excludes IDs and form fields. Should it also strip classes that are purely numeric or utility-named (e.g. <code>p-4</code>, <code>text-sm</code>)? These carry no semantic information and inflate cardinality.</p></li><li><p><strong>Iframe heatmap background.</strong> An <code>&lt;iframe&gt;</code> with <code>pointer-events: none</code> showing the live plugin page behind the canvas is useful for spatial context. Alternative: show no background (just a coordinate grid). Open for implementation to decide.</p></li><li><p><strong>Custom event PII in <code>properties</code>.</strong> The platform does not validate or redact the JSON blob. A lint warning for known PII key names (<code>email</code>, <code>name</code>, <code>userId</code>) in <code>sdk.analytics.track()</code> calls would be a useful future hardening step.</p></li><li><p><strong>Analytics plugin icon.</strong> The icon should be visually distinct from Console (⚙) and semantically clear. A bar-chart or activity graph icon (available in the existing Lucide icon set via RFC 0011) would work — e.g. <code>BarChart2</code> or <code>Activity</code>.</p></li></ol><hr><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><h3 id="task-1-0-5-—-analytics-phase-1-server-side-infrastructure-plugin-scaffold" tabindex="-1">Task 1.0.5 — Analytics Phase 1: Server-side infrastructure + plugin scaffold <a class="header-anchor" href="#task-1-0-5-—-analytics-phase-1-server-side-infrastructure-plugin-scaffold" aria-label="Permalink to &quot;Task 1.0.5 — Analytics Phase 1: Server-side infrastructure + plugin scaffold&quot;">​</a></h3><p><strong>Deliverables:</strong></p><ul><li><code>plugins/analytics/</code> scaffold: <code>manifest.json</code>, <code>icon.svg</code>, <code>app/layout.tsx</code>, <code>app/page.tsx</code> (Dashboard), <code>app/settings/page.tsx</code>, empty <code>db/schema.ts</code>, <code>package.json</code>.</li><li><code>packages/db</code>: <code>analytics_page_views</code> + <code>analytics_events</code> table schemas for both dialects; drizzle-kit migrations <code>0005_analytics_phase1</code>; DB helpers <code>recordPageView()</code>, <code>recordAnalyticsEvent()</code>, <code>queryAnalyticsAggregates()</code>, <code>cleanupOldAnalyticsRows()</code>; bootstrap seeds <code>analytics_collection_enabled</code>, <code>analytics_retention_days</code>, <code>analytics_daily_salt</code>, <code>analytics_salt_date</code>.</li><li><code>packages/manifest</code>: new <code>analytics:write</code> permission string.</li><li><code>packages/sdk</code>: <code>sdk.analytics.track()</code> + <code>sdk.analytics.isEnabled()</code> (experimental group); <code>SdkHost</code> gains <code>analytics</code> key.</li><li><code>runtime</code>: <ul><li><code>GET /api/analytics/config</code> (edge-cached 60 s; reads plugin status + collection flag).</li><li><code>POST /api/analytics/internal/page-view</code> (Node-only write route, admin-key-authed).</li><li><code>GET /api/admin/analytics</code> (aggregate queries, <code>health:view</code>-gated).</li><li><code>&#39;analytics&#39;</code> added to <code>RESERVED_API_SEGMENTS</code>; dir-parity test passes.</li><li>Middleware page-view recording (DNT guard, session hash, fire-and-forget POST).</li><li>SDK host <code>analytics</code> implementation (60 s in-process cache for enabled check).</li><li>Startup cleanup: <code>cleanupOldAnalyticsRows()</code> in <code>runtime/instrumentation.ts</code> <code>register()</code>.</li></ul></li><li><code>plugins/console</code>: Health page gets &quot;View analytics →&quot; link.</li><li>Docs: <code>sdk.analytics</code> + <code>analytics:write</code> in <code>plugin-development.md</code>; settings keys in <code>self-hosting.md</code>; <code>sdk-stability.md</code>; <code>packages/sdk/CHANGELOG.md</code>.</li><li>Version bumps: <code>@sovereignfs/manifest</code> minor, <code>@sovereignfs/sdk</code> minor, <code>@sovereignfs/db</code> minor, <code>runtime</code> minor, <code>plugins/console</code> patch.</li><li>Root <code>package.json</code> does not bump (post-v1 task; ships after v1.0.0).</li></ul><h3 id="task-1-0-6-—-analytics-phase-2-client-side-click-tracking-heatmaps" tabindex="-1">Task 1.0.6 — Analytics Phase 2: Client-side click tracking + heatmaps <a class="header-anchor" href="#task-1-0-6-—-analytics-phase-2-client-side-click-tracking-heatmaps" aria-label="Permalink to &quot;Task 1.0.6 — Analytics Phase 2: Client-side click tracking + heatmaps&quot;">​</a></h3><p><strong>Deliverables:</strong></p><ul><li><code>packages/db</code>: <code>analytics_click_events</code> + <code>analytics_scroll_events</code> schemas + migrations <code>0006_analytics_phase2</code>; batch-insert and heatmap aggregate DB helpers.</li><li><code>runtime</code>: <ul><li><code>POST /api/analytics/event</code> (client-side event receiver; session-cookie-authed; re-checks DNT; batch-inserts click and scroll rows).</li><li><code>GET /api/analytics/script.js</code> (self-hosted client script, ~3 KB minified; returns empty 200 when analytics disabled or collection off).</li><li>Root layout: <code>&lt;Script src=&quot;/api/analytics/script.js&quot; strategy=&quot;afterInteractive&quot;&gt;</code>.</li><li><code>GET /api/admin/analytics?type=clicks</code> and <code>?type=scroll</code> — heatmap aggregate queries.</li></ul></li><li><code>plugins/analytics/app/heatmap/page.tsx</code>: click density canvas, scroll depth bar, optional iframe background.</li><li>Version bumps: <code>@sovereignfs/db</code> minor, <code>runtime</code> minor, <code>plugins/analytics</code> minor.</li><li>Root <code>package.json</code> does not bump (post-v1 task; ships after v1.0.0).</li></ul><hr><h2 id="review-checklist-task-1-0-5" tabindex="-1">Review checklist (Task 1.0.5) <a class="header-anchor" href="#review-checklist-task-1-0-5" aria-label="Permalink to &quot;Review checklist (Task 1.0.5)&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Analytics plugin appears in Console → Plugin Management</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># GET /analytics → Dashboard renders (admin session)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># GET /analytics → 403 for platform:user session</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Collection off by default: visit pages, confirm NO rows in analytics_page_views</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SELECT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">FROM</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> analytics_page_views</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">--</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> →</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Enable collection in Analytics → Settings, then visit a plugin page:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;x-sovereign-admin-key: </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$KEY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/api/admin/analytics?range=7d&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → { pageViews: [...], activeSessionsPerDay: [...], topPages: [...], events: [...] }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Hard DNT block:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;DNT: 1&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:3000/launcher</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → no new row in analytics_page_views</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># sdk.analytics.track() from plugin WITH analytics:write permission:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → row in analytics_events with event_name = &#39;&lt;pluginId&gt;.my_event&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># sdk.analytics.track() from plugin WITHOUT analytics:write permission:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → silent no-op, no row, no error thrown</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Disable analytics plugin via Console → Plugin Management:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># GET /analytics → 404</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># sdk.analytics.track() → no-op (isEnabled() returns false)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># GET /api/analytics/config → { enabled: false }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Retention cleanup: set retention to 1 day, restart → old rows deleted at startup</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> format:check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Docs-parity: analytics:write permission + sdk.analytics documented</span></span></code></pre></div><h2 id="review-checklist-task-1-0-6" tabindex="-1">Review checklist (Task 1.0.6) <a class="header-anchor" href="#review-checklist-task-1-0-6" aria-label="Permalink to &quot;Review checklist (Task 1.0.6)&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Load a plugin page, interact (click, scroll), navigate away:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SELECT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">FROM</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> analytics_click_events</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;    </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">--</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> →</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SELECT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> COUNT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">FROM</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> analytics_scroll_events</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">--</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> →</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Form field clicks produce no element_selector:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SELECT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> element_selector</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> FROM</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> analytics_click_events</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> WHERE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> element_selector</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> LIKE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;input%&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → 0 rows</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># GET /api/analytics/script.js with collection disabled → empty 200 body</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Browser with navigator.doNotTrack = &#39;1&#39;:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → DevTools Network: no request to /api/analytics/event on page unload</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Console Heatmap tab: select plugin + path → canvas renders click density gradient</span></span></code></pre></div><hr><h2 id="versioning-impact" tabindex="-1">Versioning impact <a class="header-anchor" href="#versioning-impact" aria-label="Permalink to &quot;Versioning impact&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Package</th><th>Bump type</th><th>Task</th></tr></thead><tbody><tr><td><code>@sovereignfs/manifest</code></td><td>minor</td><td>0.9.6 (new <code>analytics:write</code> permission)</td></tr><tr><td><code>@sovereignfs/sdk</code></td><td>minor</td><td>Task 1.0.5 (new experimental <code>sdk.analytics</code> surface)</td></tr><tr><td><code>@sovereignfs/db</code></td><td>minor</td><td>Task 1.0.5 (new tables + helpers), again minor in Task 1.0.6</td></tr><tr><td><code>runtime</code></td><td>minor</td><td>both tasks</td></tr><tr><td><code>plugins/analytics</code></td><td>initial <code>0.1.0</code></td><td>Task 1.0.5 (new plugin), minor in Task 1.0.6</td></tr><tr><td><code>plugins/console</code></td><td>patch</td><td>Task 1.0.5 (health-page link only)</td></tr><tr><td>Root <code>package.json</code></td><td>no bump</td><td>post-v1 tasks; ships after v1.0.0</td></tr></tbody></table><hr><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.2</td><td>June 2026</td><td>Revised: analytics becomes a platform plugin (<code>plugins/analytics/</code>); two-control model (collection toggle + plugin enable/disable); Console Analytics section removed; API routes remain in runtime to avoid circular middleware dependency</td></tr><tr><td>0.1</td><td>June 2026</td><td>Initial draft (analytics as runtime built-in + Console section)</td></tr></tbody></table>`,124)])])}const u=a(n,[["render",l]]);export{g as __pageData,u as default};
