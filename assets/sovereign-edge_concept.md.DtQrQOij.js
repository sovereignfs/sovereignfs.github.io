import{_ as t,o,c as n,a2 as a}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"Sovereign Edge","description":"","frontmatter":{},"headers":[],"relativePath":"sovereign-edge/concept.md","filePath":"sovereign-edge/concept.md"}'),r={name:"sovereign-edge/concept.md"};function i(s,e,l,c,h,d){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="sovereign-edge" tabindex="-1">Sovereign Edge <a class="header-anchor" href="#sovereign-edge" aria-label="Permalink to &quot;Sovereign Edge&quot;">​</a></h1><p><em>(Working codename, tied to this repo&#39;s directory name — not yet a decided consumer-facing product name. See <a href="./docs/research/0001-concept-and-connector-architecture.html#open-questions">research 0001</a>.)</em></p><h2 id="concept-paper-draft-v0-1" tabindex="-1">Concept Paper (Draft v0.1) <a class="header-anchor" href="#concept-paper-draft-v0-1" aria-label="Permalink to &quot;Concept Paper (Draft v0.1)&quot;">​</a></h2><h3 id="vision" tabindex="-1">Vision <a class="header-anchor" href="#vision" aria-label="Permalink to &quot;Vision&quot;">​</a></h3><p>Sovereign Edge is a privacy-first, fully offline AI companion for phones — and, later, desktops. Users download a small local language model (Gemma, Qwen, Phi, or similar) straight to their own device and talk to it with <strong>zero network requests of any kind</strong> from the chat itself. Nothing about that promise depends on trusting a vendor&#39;s word for it — the chat and inference path has no network code in it at all.</p><p>On top of that offline core sits an optional, explicitly-permissioned <strong>connector layer</strong>: a small set of agents that can reach outside the device — to search the web, or create a task in the user&#39;s own self-hosted software — but only once the user has granted that specific connector that specific permission. Nothing reaches outside the sandbox by default, and each connector&#39;s reach is scoped to exactly what it was granted, not a blanket &quot;this app can use the network&quot; toggle.</p><h3 id="the-problem" tabindex="-1">The Problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The Problem&quot;">​</a></h3><p>Two projects already do &quot;chat with a local LLM offline,&quot; and neither answers the actual problem:</p><ul><li><strong>Google&#39;s AI Edge Gallery</strong> asks a privacy-conscious user to trust Google&#39;s closed pipeline that it really doesn&#39;t phone home — an unverifiable claim from the one company most associated with data extraction, which no one who takes privacy seriously should have to accept on faith.</li><li><strong>OGAM (Off-Grid AI)</strong> gets the on-device chat part right, but its &quot;agentic&quot; tier is paywalled ($69 lifetime / $49/yr) and routes through <strong>cloud</strong> MCP servers (Notion, Linear, GitHub) — which quietly reintroduces the exact cloud dependency an &quot;off-grid&quot; tool is supposed to remove. It&#39;s also bulkier than it needs to be, with a lot of surface area most users don&#39;t need, and its model downloads don&#39;t always work.</li></ul><p>There is no option today that is both genuinely, verifiably offline for its core function, and free of a company payment gate or cloud dependency for the parts of it that reach outside the device.</p><h3 id="the-solution" tabindex="-1">The Solution <a class="header-anchor" href="#the-solution" aria-label="Permalink to &quot;The Solution&quot;">​</a></h3><p>Sovereign Edge separates the product into two trust tiers that the user can always see:</p><ol><li><strong>Chat</strong> — fully local, fully offline, always. Writing help, brainstorming, grammar fixes, drafting — the model runs entirely on-device.</li><li><strong>Connectors</strong> — a small, explicit, permissioned layer. When a task needs something outside the device — searching the web, creating a task in the user&#39;s own self-hosted Sovereign instance — a specific connector handles it, only once granted that specific permission, and the app is transparent in the UI about which connector was used for which reply.</li></ol><p>The core app and a baseline set of connectors are free forever. The connector layer is also where the product opens up: <strong>anyone can develop and publish a connector</strong>, and some can be sold.</p><h3 id="core-principles" tabindex="-1">Core Principles <a class="header-anchor" href="#core-principles" aria-label="Permalink to &quot;Core Principles&quot;">​</a></h3><p><strong>User ownership.</strong> The model, the chat history, and the memory layer live on the user&#39;s device. Nothing is uploaded anywhere by default.</p><p><strong>Privacy first, provably.</strong> No telemetry, no accounts, no cloud fallback for the core chat function — not &quot;trust us,&quot; but true by construction, since there&#39;s no network code in that path to begin with.</p><p><strong>Explicit, scoped permission for anything that leaves the device.</strong> Every connector requests its own permission, separately revocable, and the UI always shows which connector (if any) touched the network for a given reply.</p><p><strong>Open connector ecosystem, sized to what&#39;s actually safe.</strong> &quot;Anyone can build a plugin&quot; doesn&#39;t mean &quot;anyone can run arbitrary native code inside this app&quot; — see <a href="#platform-architecture">Platform Architecture</a> below for why that distinction matters and how it&#39;s structured.</p><p><strong>Free core, forever.</strong> The chat app and a baseline set of connectors never require payment. Monetization lives entirely in the optional connector layer.</p><p><strong>Build once, widen without rework.</strong> The connector interface is designed the same way from day one whether it&#39;s serving one hardcoded connector or a thousand downloaded ones — see <a href="#phasing-build-a-foundation-that-evolves-without-friction">Phasing</a>.</p><h3 id="platform-architecture" tabindex="-1">Platform Architecture <a class="header-anchor" href="#platform-architecture" aria-label="Permalink to &quot;Platform Architecture&quot;">​</a></h3><p>Two layers:</p><h4 id="core-local-inference" tabindex="-1">Core: local inference <a class="header-anchor" href="#core-local-inference" aria-label="Permalink to &quot;Core: local inference&quot;">​</a></h4><p><code>llama.cpp</code>/GGUF via <code>llama.rn</code> (the same combination used by the open-source PocketPal AI), wrapped in a React Native app targeting iOS and Android from day one. A model manager lets the user pick and download a small GGUF model (Gemma/Qwen/Phi-class). Nothing in this layer ever makes a network call.</p><h4 id="connectors-a-tiered-trust-model" tabindex="-1">Connectors: a tiered trust model <a class="header-anchor" href="#connectors-a-tiered-trust-model" aria-label="Permalink to &quot;Connectors: a tiered trust model&quot;">​</a></h4><p>Mobile app stores don&#39;t allow the kind of &quot;arbitrary third-party code running inside your app&quot; model that <code>sovereign</code>&#39;s own web-based plugin system uses (iOS forbids dynamically loading native code at runtime outside the reviewed binary). So the connector layer is split by how much trust each tier actually requires:</p><ul><li><strong>Tier 1 — declarative connectors.</strong> A manifest: the tool schema the model sees, an endpoint, an auth method (a token in the OS keychain, scoped per connector), and request/response templates. No code ships at all. This covers the large majority of real connectors — search, task creation, most REST-API integrations — and is safe to open to any third-party developer, because there&#39;s nothing to sandbox.</li><li><strong>Tier 2 — sandboxed transform scripts.</strong> For a connector that needs real logic beyond request/response templating: a small script running in a capability-restricted engine, with no ambient device/network/filesystem access beyond what the host explicitly hands it.</li><li><strong>Tier 3 — deep OS integration.</strong> Anything needing genuine native modules (Shortcuts/App Intents, direct Calendar/Contacts access) ships inside the app&#39;s own binary, first-party only, on the normal release cycle. Not opened to third parties unless a native-plugin-loading story that survives app store review emerges later.</li></ul><h3 id="business-philosophy" tabindex="-1">Business Philosophy <a class="header-anchor" href="#business-philosophy" aria-label="Permalink to &quot;Business Philosophy&quot;">​</a></h3><p>The core app is free forever — not a trial, not a limited tier. Revenue comes from optional paid connectors: on mobile through platform in-app purchase (required by Apple/Google for unlocking digital features inside an app binary), and on desktop through direct sale, the same way <code>sovereign</code> sells plugin entitlements today. User data is never the product, and never part of the business model — consistent with the wider <code>sovereignfs</code> ecosystem&#39;s &quot;digital self-determination&quot; stance.</p><h3 id="platform-philosophy" tabindex="-1">Platform Philosophy <a class="header-anchor" href="#platform-philosophy" aria-label="Permalink to &quot;Platform Philosophy&quot;">​</a></h3><p>Mobile-first: iOS and Android together from Phase 1, via React Native — the native modules Tier 3 connectors eventually need are per-platform regardless of client framework, so building both from the start costs nothing extra. Desktop is secondary and optional, picked up only once the mobile MVP has proven the concept.</p><h3 id="non-goals" tabindex="-1">Non-Goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-Goals&quot;">​</a></h3><p>Sovereign Edge is not:</p><ul><li>A cloud AI assistant, or dependent on one for its core function</li><li>A general-purpose autonomous &quot;computer use&quot; agent — the connector layer is task handoff and communication, not open-ended multi-step autonomy</li><li>A replacement for <code>sovereign</code> or any dependency of it — fully standalone</li><li>A data collection platform, or funded by one</li><li>A mandatory subscription — the core product is free forever</li></ul><h3 id="initial-mvp" tabindex="-1">Initial MVP <a class="header-anchor" href="#initial-mvp" aria-label="Permalink to &quot;Initial MVP&quot;">​</a></h3><p>Phase 1 is intentionally small: the offline chat layer (writing help, brainstorming, drafting), plus one built-in connector — Search — so the &quot;explicit-permission agent reaches outside, everything else stays local&quot; promise is proven end-to-end, on both iOS and Android. No connector store, no third-party anything, no monetization yet.</p><h3 id="phasing-build-a-foundation-that-evolves-without-friction" tabindex="-1">Phasing: build a foundation that evolves without friction <a class="header-anchor" href="#phasing-build-a-foundation-that-evolves-without-friction" aria-label="Permalink to &quot;Phasing: build a foundation that evolves without friction&quot;">​</a></h3><ol><li><strong>Phase 1 — MVP/POC.</strong> Chat layer + a hardcoded Search connector, already built internally to the Tier 1 connector shape even though nothing is yet downloadable — so opening it up later doesn&#39;t require rebuilding the tool-routing/permission core.</li><li><strong>Phase 2 — full agentic layer, default connectors.</strong> Add the Sovereign Tasks connector (talks directly to the user&#39;s own self-hosted <code>sovereign</code> instance) and real connector-management UI (per-connector permission toggles, in-chat provenance of which connector answered).</li><li><strong>Phase 3 — open connector ecosystem.</strong> Publish the connector SDK, a plugin template, first-party examples, a public registry, and an in-app connector store. Monetization (IAP on mobile, direct sale on desktop) turns on here.</li></ol><p>Desktop support is picked up after Phase 1 mobile ships, not in parallel with it.</p><h3 id="long-term-vision" tabindex="-1">Long-Term Vision <a class="header-anchor" href="#long-term-vision" aria-label="Permalink to &quot;Long-Term Vision&quot;">​</a></h3><p>Sovereign Edge aims to be the answer to &quot;I want a genuinely private AI on my phone&quot; that doesn&#39;t ask for trust it can&#39;t earn structurally, and doesn&#39;t quietly reintroduce a cloud dependency the moment it needs to act on the user&#39;s behalf. The connector ecosystem is where it grows — safely, at the trust level mobile platforms actually allow, open to any developer willing to build within it.</p>',42)])])}const m=t(r,[["render",i]]);export{u as __pageData,m as default};
