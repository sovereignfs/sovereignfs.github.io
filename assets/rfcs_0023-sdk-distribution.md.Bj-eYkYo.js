import{_ as t,o,c as s,a2 as n}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"RFC 0023 — SDK distribution & the plugin isolation boundary","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0023-sdk-distribution.md","filePath":"rfcs/0023-sdk-distribution.md"}'),i={name:"rfcs/0023-sdk-distribution.md"};function a(r,e,d,l,c,h){return o(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="rfc-0023-—-sdk-distribution-the-plugin-isolation-boundary" tabindex="-1">RFC 0023 — SDK distribution &amp; the plugin isolation boundary <a class="header-anchor" href="#rfc-0023-—-sdk-distribution-the-plugin-isolation-boundary" aria-label="Permalink to &quot;RFC 0023 — SDK distribution &amp; the plugin isolation boundary&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/sdk</code> (contract vs host-provided implementation), <code>packages/db</code> + <code>packages/mailer</code> (stay private), <code>.github/workflows/publish.yml</code> (<code>sdk-v*</code>), CLAUDE.md (the &quot;not npm-installable&quot; caveat + &quot;published&quot; designation), the SRS decision log, Task 0.5.20; <strong>amends RFC 0017</strong> (its publish prerequisite); contrasts <code>@sovereignfs/ui</code> (portable)<br><strong>Incorporated into plan:</strong> Yes — scheduled as roadmap Task 0.5.21; documentation-first. This RFC settles what a plugin can do in isolation and, from that, the SDK&#39;s distribution model; SRS requirement IDs, scheduling, and task allocation are deferred.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Decide whether and how to publish <code>@sovereignfs/sdk</code> by first answering the question underneath it: <strong>what can a plugin actually do in isolation?</strong> The answer — <strong>author and type-check, yes; build-as-an-app and run, no</strong> — determines that the SDK has no meaningful <em>runtime</em> existence outside the host. So:</p><ul><li><strong>Drop the &quot;bundle the private <code>db</code>/<code>mailer</code> into the SDK via <code>noExternal</code>&quot; plan</strong> (the standing follow-up). It would ship platform internals — including a native dependency — that never usefully run for a consumer.</li><li>If isolated <em>authoring</em> is a goal, <strong>publish the SDK as a types-first contract</strong> (host-provided/guarded implementations, <strong>zero <code>db</code>/<code>mailer</code> dependency</strong>), which also dissolves the private-deps blocker entirely. Otherwise <strong>don&#39;t publish</strong> and standardise on develop-in-a-checkout.</li><li>Either way, <strong>state plainly that plugins are host-hosted</strong>: there is no standalone build or run; the dev/test loop always uses a runtime (RFC 0017&#39;s <code>sv</code>/checkout flow).</li></ul><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>A standing follow-up — &quot;make <code>@sovereignfs/sdk</code> npm-installable&quot; (CLAUDE.md Task 0.5.07 caveat; an RFC 0017 prerequisite) — assumed the fix was to bundle the private <code>@sovereignfs/db</code>/<code>@sovereignfs/mailer</code> into the SDK&#39;s <code>dist</code>. Before doing that, the real question is whether publishing the SDK is <em>meaningful</em> at all. As the user put it: with no centralised service layer, the SDK has no existence outside the main codebase — so is publishing pointless? Validating that requires settling the plugin <strong>isolation boundary</strong> first; the distribution model falls out of it.</p><h2 id="current-state-evidence" tabindex="-1">Current state / evidence <a class="header-anchor" href="#current-state-evidence" aria-label="Permalink to &quot;Current state / evidence&quot;">​</a></h2><p>The SDK is <strong>in-process host glue with no service layer</strong> — every surface is bound to the running host:</p><ul><li><code>sdk.auth.getSession()</code> reads middleware-injected <code>next/headers</code> (<code>packages/sdk/src/auth.ts:10</code>) — <code>x-sovereign-user-*</code> exist only because the runtime injected them; elsewhere it returns <code>null</code>. The mutating methods <code>fetch</code> the host&#39;s auth server at <code>SOVEREIGN_AUTH_URL</code> with a forwarded session cookie.</li><li><code>sdk.db.getClient()</code> calls <code>getPlatformDb()</code> (<code>db.ts:12</code>) — opens the platform DB <strong>in the current process</strong>, pulling the native <code>better-sqlite3</code> dep through the private <code>@sovereignfs/db</code>.</li><li><code>sdk.platform.getConfig()</code> (<code>platform.ts:35</code>) reads the platform DB directly and the workspace-root <code>package.json</code> — its own comment returns <code>&#39;0.0.0&#39;</code> &quot;in standalone contexts&quot; (<code>platform.ts:18-26</code>).</li><li><code>sdk.mailer.send()</code> (<code>mailer.ts</code>) instantiates the private <code>@sovereignfs/mailer</code> from <code>process.env</code>.</li></ul><p>And <strong>composition shadows the plugin&#39;s SDK</strong>: a plugin&#39;s <code>app/</code> is copied into the runtime and compiled by the runtime&#39;s <code>next build</code> (<code>scripts/generate-registry.ts</code>), so <code>@sovereignfs/sdk</code> resolves to the <strong>host&#39;s</strong> workspace copy — the plugin&#39;s own installed SDK is <strong>never executed</strong> in the real path.</p><p><strong>The UI contrast:</strong> <code>@sovereignfs/ui</code> is presentational (React components + <code>--sv-</code> tokens). It renders anywhere, has no host binding, and is already publishable. The SDK is host-bound; UI is not. This asymmetry is why UI publishing was never the problem and the SDK question is real.</p><p>The current docs assume the SDK will be published with bundled implementations: CLAUDE.md flags the private-deps caveat as &quot;a separate follow-up before any <code>sdk-v*</code> tag&quot;; the SRS decision log lists <code>sdk</code>+<code>ui</code> as published and claims &quot;the published SDK has no runtime dependencies.&quot;</p><h2 id="the-plugin-isolation-boundary" tabindex="-1">The plugin isolation boundary <a class="header-anchor" href="#the-plugin-isolation-boundary" aria-label="Permalink to &quot;The plugin isolation boundary&quot;">​</a></h2><p>The crux question — <em>can a plugin be developed completely in isolation?</em> — resolves per lifecycle stage:</p><table tabindex="0"><thead><tr><th>Stage</th><th style="text-align:center;">Isolated (own repo, no checkout)?</th><th>Why</th></tr></thead><tbody><tr><td>Author / edit</td><td style="text-align:center;">✅</td><td>TS/TSX against <code>@sovereignfs/sdk</code> + <code>@sovereignfs/ui</code> types only.</td></tr><tr><td>Type-check / lint</td><td style="text-align:center;">✅</td><td><code>tsc --noEmit</code> / ESLint need only those types.</td></tr><tr><td>Build <strong>as an app</strong></td><td style="text-align:center;">❌</td><td>The plugin&#39;s <code>app/</code> is a route-group <strong>fragment</strong> — no root <code>&lt;html&gt;</code> layout, no <code>next.config</code>, no middleware, no <code>(platform)</code> shell. Not a buildable Next app alone.</td></tr><tr><td>Run / test</td><td style="text-align:center;">❌</td><td>Needs the host: middleware-injected headers (else <code>getSession()</code> is null), <code>getPlatformDb()</code>, the auth server, the composed route group, shell chrome, theme/tokens. The SDK is in-process glue with nothing to talk to standalone.</td></tr></tbody></table><p><strong>Conclusion: full isolation is architecturally impossible</strong> — it is a property of the composition model + the in-process SDK, not a packaging gap. Only <strong>isolated authoring</strong> (write + type-check + lint) is possible; running/testing is always <strong>runtime-hosted</strong> (a Sovereign checkout, or <code>sv plugin add</code>, per RFC 0017).</p><p>It follows that publishing the SDK&#39;s <strong>implementations</strong> is pointless (they never run for the consumer), and a <strong>types-first contract</strong> is the only meaningful artifact — and only if isolated authoring is a goal.</p><h2 id="proposed-distribution-model" tabindex="-1">Proposed distribution model <a class="header-anchor" href="#proposed-distribution-model" aria-label="Permalink to &quot;Proposed distribution model&quot;">​</a></h2><p><strong>Publish <code>@sovereignfs/sdk</code> as a types-first contract</strong> (recommended, conditional on wanting isolated-authoring repos):</p><ul><li>The published package is the <strong>typed API surface</strong>. Its implementations are <strong>host-provided/guarded</strong> — when (mistakenly) executed outside a Sovereign runtime they throw a clear error (e.g. <code>&quot;@sovereignfs/sdk runs inside the Sovereign runtime&quot;</code>), because in the real path the host&#39;s copy is what executes.</li><li><strong>Zero <code>db</code>/<code>mailer</code> dependency</strong> in the published package → <code>@sovereignfs/db</code> and <code>@sovereignfs/mailer</code> <strong>stay private</strong>, and the &quot;bundle private deps&quot; blocker <strong>disappears</strong>. The SRS claim &quot;the published SDK has no runtime dependencies&quot; becomes literally true.</li><li><strong><code>@sovereignfs/ui</code> stays as-is</strong> (already portable).</li><li>The <strong>dev/test loop remains runtime-hosted</strong> — RFC 0017&#39;s <code>sv plugin new</code> → <code>sv plugin add</code> / checkout flow. Publishing changes authoring ergonomics, not the fact that a runtime hosts the plugin.</li></ul><p>(If isolated authoring is <em>not</em> a goal, the alternative is <strong>don&#39;t publish</strong>: keep the SDK workspace-internal, develop plugins in a checkout via <code>workspace:*</code>, and drop the &quot;published&quot; designation — see Alternatives.)</p><h2 id="what-changes-when-this-is-accepted" tabindex="-1">What changes (when this is accepted) <a class="header-anchor" href="#what-changes-when-this-is-accepted" aria-label="Permalink to &quot;What changes (when this is accepted)&quot;">​</a></h2><ul><li><strong>CLAUDE.md</strong> — the Task 0.5.8 caveat is rewritten: the blocker is dissolved by the types-first split, not by bundling private deps; the SDK&#39;s &quot;published&quot; status is qualified as a types-first contract.</li><li><strong>SRS decision log</strong> — &quot;the published SDK has no runtime dependencies&quot; is made literally true by the restructure.</li><li><strong>RFC 0017</strong> — its publish <strong>prerequisite</strong> is amended from &quot;bundle/publish the private deps&quot; to &quot;restructure the SDK types-first&quot; (cross-referenced; a one-line note added now so the two RFCs don&#39;t contradict).</li><li><strong>A task owns the restructure</strong> — Task 0.5.20 (stable SDK &amp; semver) or a small new task; it splits the SDK into the published contract + the host-provided impls.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Don&#39;t publish at all.</strong> Keep the SDK workspace-internal; plugins develop inside a Sovereign checkout (<code>workspace:*</code>). Simplest and fully valid <strong>if</strong> isolated authoring isn&#39;t a goal — but it forces every third-party author to clone the platform to get types. Documented as the fallback.</li><li><strong>Publish the full bundle via <code>noExternal</code></strong> (the original assumption). Rejected — ships platform internals + a native dependency that never run for the consumer, and <code>@sovereignfs/ui</code>-style &quot;just works&quot; is impossible because the SDK is host-bound.</li><li><strong>Publish an <code>@sovereignfs/runtime</code> dev-harness</strong> so a plugin repo could pull in a runtime and run &quot;isolated.&quot; Heavier (the runtime isn&#39;t published and is large); the <code>sv</code>/checkout run loop already covers testing. Noted as a possible future, not required.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Guarded impls</strong> — throw vs no-op when the published SDK is executed outside a runtime; the exact error contract.</li><li><strong>Types entrypoint</strong> — does the types-first split warrant a separate types-only export, or is one entrypoint with guarded impls enough?</li><li><strong>Task ownership</strong> — fold the restructure into Task 0.5.20 or a new small task.</li><li><strong>Dev-harness</strong> — whether a published runtime harness is worth it post-v1.</li><li><strong>Requirement IDs</strong> — deferred until accepted.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Documentation-first (this RFC)</strong> — settles the isolation boundary + the distribution decision; corrects the assumptions in CLAUDE.md / SRS / RFC 0017.</li><li><strong>When accepted &amp; scheduled:</strong> restructure the SDK types-first (host provides the impls; <code>db</code>/<code>mailer</code> stay private), update <code>publish.yml</code>/docs, and reconcile RFC 0017 — <em>or</em>, if don&#39;t-publish is chosen, drop the &quot;published&quot; designation and update the same docs. Either way the <code>noExternal</code>-bundle plan is dropped.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft; establishes that plugins cannot run in isolation (composed fragments + in-process host-glue SDK), so publishing the full-impl SDK is meaningless; recommends a types-first contract (or not publishing), dropping the <code>noExternal</code>-bundle plan; documentation-first.</td></tr><tr><td>0.2</td><td>Jun 2026</td><td>Accepted; scheduled in the roadmap as Task 0.5.21.</td></tr></tbody></table>',33)])])}const g=t(i,[["render",a]]);export{p as __pageData,g as default};
