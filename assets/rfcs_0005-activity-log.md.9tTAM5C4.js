import{_ as t,o as i,c as s,a2 as a}from"./chunks/framework.BWuWLRhz.js";const u=JSON.parse('{"title":"RFC 0005 — Activity log","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0005-activity-log.md","filePath":"rfcs/0005-activity-log.md"}'),o={name:"rfcs/0005-activity-log.md"};function n(d,e,r,c,l,p){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0005-—-activity-log" tabindex="-1">RFC 0005 — Activity log <a class="header-anchor" href="#rfc-0005-—-activity-log" aria-label="Permalink to &quot;RFC 0005 — Activity log&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> SDK (<code>packages/sdk</code>), manifest schema (<code>packages/manifest</code>), platform DB (<code>packages/db</code>), runtime, Console, Account<br><strong>Incorporated into plan:</strong> Yes — SRS §3.14, §5 (<code>activity:write</code>), and <strong>Task 0.5.13</strong>. The reserved <code>sdk.activity</code> surface + <code>activity:write</code> permission landed in code as <code>NotImplementedError</code> stubs; the <code>activity_log</code> table, capture points, verify-boundary auth capture, and the Console/Account views are built in Task 0.5.13.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Define a <strong>scoped, durable activity log</strong>: a tenant-scoped record of meaningful actions across the platform, with <strong>view-based visibility</strong>. The platform and (later) plugins emit events; the runtime records them with actor and tenant context. Two complementary surfaces read one table:</p><ul><li><strong>Account <code>/activity</code></strong> — a <strong>personal</strong> feed for <strong>every</strong> user (admins included): the viewer&#39;s own actions plus actions about them.</li><li><strong>Console <code>/console/activity</code></strong> — the <strong>consolidated, platform-wide</strong> feed, admin-only.</li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// plugin emits a scoped event (reserved — throws NotImplementedError in v1)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.activity.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ action: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;list.created&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, subjectUserId: user.id });</span></span></code></pre></div><p>A reserved <code>sdk.activity.log()</code> surface and an <code>activity:write</code> permission land now as stubs; the mechanism behind them is specified here for later.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign keeps no audit trail. Admins cannot see who invited a user, changed a role, toggled a plugin, or altered tenant settings; users have no record of their own actions or of admin/system actions that affect them (&quot;your role was changed&quot;, &quot;your account was deactivated&quot;). As a single-tenant/multi-user platform grows, that is a transparency and accountability gap — and a security one (no record of privileged changes).</p><p>This is greenfield: there is no existing audit, activity, or event-history infrastructure. The reserved <code>sdk.events</code> surface is unrelated — it is a post-v1, fire-and-forget pub/sub channel, <strong>not</strong> a durable audit record (see Alternatives).</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>Reserved-surface pattern:</strong> the SDK already declares post-v1 surfaces (<code>storage</code>, <code>notifications</code>, <code>events</code>, <code>data</code>) as stubs that throw <code>NotImplementedError</code> (<code>packages/sdk/src/unimplemented.ts</code>, <code>packages/sdk/src/data.ts</code>), and the manifest permission enum reserves the matching capabilities (<code>packages/manifest/src/schema.ts</code>). This RFC reuses that pattern: <code>sdk.activity</code> + <code>activity:write</code> land now as stubs.</li><li><strong>Role scoping:</strong> the runtime injects <code>x-sovereign-user-role</code> / <code>x-sovereign-user-id</code> headers from the verified session; gated reads filter on them (e.g. <code>selectLauncherPlugins</code> in <code>runtime/src/launcher-plugins.ts</code>, and the user-scoped <code>runtime/app/api/account/prefs/route.ts</code>).</li><li><strong>Admin-key gating:</strong> <code>/api/admin/*</code> routes require <code>Authorization: Bearer &lt;SOVEREIGN_ADMIN_KEY&gt;</code> (<code>runtime/src/admin-guard.ts</code>).</li><li><strong>Platform DB conventions:</strong> ULID <code>text</code> ids, Unix-seconds timestamps (<code>integer</code> on SQLite, <code>bigint</code> on Postgres), <code>tenant_id</code> on every user-scoped table, dialect-mirrored schema guarded by a parity test (<code>packages/db/src/schema/{sqlite,postgres}/platform.ts</code>).</li><li><strong>Identity separation:</strong> <code>apps/auth</code> owns identity and never reads the platform DB; the runtime verifies sessions at its middleware boundary (<code>runtime/src/session-verify.ts</code> + the <code>/api/verify</code> fallback).</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><p>Everything in this section is <strong>deferred</strong> (specified for the post-acceptance task) except the reserved SDK stub and permission noted under &quot;Adoption path&quot;.</p><h3 id="_1-event-model-—-activity-log-table" tabindex="-1">1. Event model — <code>activity_log</code> table <a class="header-anchor" href="#_1-event-model-—-activity-log-table" aria-label="Permalink to &quot;1. Event model — \`activity_log\` table&quot;">​</a></h3><p>A single platform table (both dialects, mirrored, parity-tested):</p><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>text (ULID)</td><td>Primary key.</td></tr><tr><td><code>tenant_id</code></td><td>text, not null</td><td>Tenant scope (single-tenant in v1; present for future multi-tenancy).</td></tr><tr><td><code>actor_id</code></td><td>text, nullable</td><td>User who acted; <code>null</code> for system-originated events.</td></tr><tr><td><code>actor_type</code></td><td>text, not null</td><td><code>&#39;user&#39; | &#39;system&#39; | &#39;plugin&#39;</code>.</td></tr><tr><td><code>action</code></td><td>text, not null</td><td>Dotted verb, e.g. <code>user.invited</code>, <code>plugin.disabled</code>, <code>session.login</code>.</td></tr><tr><td><code>subject_user_id</code></td><td>text, nullable</td><td>The user an event is <strong>about</strong> (enables &quot;concerns me&quot; scoping).</td></tr><tr><td><code>target_type</code></td><td>text, nullable</td><td>Generic target kind, e.g. <code>plugin</code>, <code>setting</code>, <code>session</code>.</td></tr><tr><td><code>target_id</code></td><td>text, nullable</td><td>Generic target id (pluginId, setting key, …).</td></tr><tr><td><code>plugin_id</code></td><td>text, nullable</td><td>Emitting plugin for plugin-sourced events; <code>null</code> for core.</td></tr><tr><td><code>visibility</code></td><td>text, not null</td><td><code>&#39;admin&#39; | &#39;user&#39;</code> — drives the read scope (below).</td></tr><tr><td><code>summary</code></td><td>text, nullable</td><td>Human-readable one-liner.</td></tr><tr><td><code>metadata</code></td><td>text (JSON), nullable</td><td>Structured detail: before/after values, IP, user-agent.</td></tr><tr><td><code>created_at</code></td><td>integer/bigint</td><td>Unix seconds, caller-supplied.</td></tr></tbody></table><p>Indexes: <code>(tenant_id, created_at)</code>, <code>(actor_id)</code>, <code>(subject_user_id)</code>.</p><h3 id="_2-scoping-model-—-by-view-not-by-role" tabindex="-1">2. Scoping model — by <em>view</em>, not by role <a class="header-anchor" href="#_2-scoping-model-—-by-view-not-by-role" aria-label="Permalink to &quot;2. Scoping model — by _view_, not by role&quot;">​</a></h3><p>Visibility is a property of the <strong>view</strong>, derived from a stored <code>visibility</code> plus the actor/subject relationship — not a per-request ACL computation:</p><ul><li><strong>Personal scope</strong> (Account <code>/activity</code>, every user incl. admins):<div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tenant_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :tenant</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> visibility </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;user&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (actor_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">self</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> OR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subject_user_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div>The viewer&#39;s own actions plus actions about them. An admin viewing their Account tab sees only <em>their own</em> activity here, exactly like any user.</li><li><strong>Platform scope</strong> (Console <code>/console/activity</code>, admin-only): all rows for the tenant, no visibility filter — the consolidated cross-user feed.</li></ul><p><code>visibility</code> is set per action at <strong>write</strong> time:</p><ul><li>Cross-user, security, or configuration events that affect others default to <code>admin</code> (Console-only) — e.g. another user&#39;s role change, plugin toggles, tenant-settings edits.</li><li>An event <strong>about</strong> a user is written <code>visibility = &#39;user&#39;</code> with <code>subject_user_id</code> set, so the affected user sees it in their personal feed without leaking anyone else&#39;s activity — e.g. <em>your</em> role was changed, <em>you</em> were invited/deactivated, <em>your</em> password was changed.</li></ul><p>This keeps the user-facing query a single indexed predicate and avoids leaking events between users.</p><h3 id="_3-capture-points" tabindex="-1">3. Capture points <a class="header-anchor" href="#_3-capture-points" aria-label="Permalink to &quot;3. Capture points&quot;">​</a></h3><p>The runtime (and the admin API) call a <code>recordActivity()</code> write helper at the existing mutation points:</p><table tabindex="0"><thead><tr><th>Event</th><th>Where (existing code)</th><th>Default visibility</th></tr></thead><tbody><tr><td>User invited</td><td><code>plugins/console/app/users/actions.ts</code> → <code>apps/auth/app/api/admin/invites/route.ts</code></td><td><code>admin</code> (+ <code>user</code> to invitee on accept)</td></tr><tr><td>Role changed / (de)activated</td><td><code>plugins/console/app/users/actions.ts</code> → <code>apps/auth/app/api/admin/users/[id]/route.ts</code></td><td><code>user</code> (subject = affected user)</td></tr><tr><td>Plugin enabled / disabled</td><td><code>runtime/app/api/admin/plugins/[id]/route.ts</code></td><td><code>admin</code></td></tr><tr><td>Tenant name / root plugin / invite-only</td><td><code>runtime/app/api/admin/settings/route.ts</code></td><td><code>admin</code></td></tr><tr><td>Password / display-name / avatar change</td><td><code>plugins/account/app/actions.ts</code>, <code>runtime/app/api/account/avatar/route.ts</code></td><td><code>user</code> (self)</td></tr><tr><td>Timezone / theme change</td><td><code>plugins/account/app/actions.ts</code> (<code>/api/account/prefs</code>)</td><td><code>user</code> (self)</td></tr><tr><td>Session revoked</td><td><code>plugins/account/app/actions.ts</code></td><td><code>user</code> (self)</td></tr><tr><td>Login / session established</td><td>runtime <strong>verify boundary</strong> (<code>runtime/src/session-verify.ts</code> + <code>/api/verify</code>)</td><td><code>user</code> (self)</td></tr><tr><td>Plugin-sourced events</td><td><code>sdk.activity.log()</code> (mediated, deferred)</td><td>per-call (<code>user</code>)</td></tr></tbody></table><p><strong>Auth lifecycle is captured at the runtime verify boundary, not in <code>apps/auth</code></strong> — the auth server keeps its clean separation and never writes the platform DB. Registration is the one awkward case (it happens pre-session in <code>apps/auth</code>); the proposed approach is to record <code>auth.registered</code> on the first runtime verify of a not-yet-seen user. See Open questions.</p><h3 id="_4-write-path" tabindex="-1">4. Write path <a class="header-anchor" href="#_4-write-path" aria-label="Permalink to &quot;4. Write path&quot;">​</a></h3><p>A <code>recordActivity(db, entry)</code> helper in <code>packages/db</code> (alongside <code>platform-db.ts</code>), called by the core capture points and the admin API. It stamps <code>created_at</code> and <code>tenant_id</code> and inserts one row.</p><p><code>sdk.activity.log()</code> is <strong>runtime-mediated</strong>, like the planned <code>sdk.data</code>: a plugin supplies only <code>action</code>, <code>subjectUserId?</code>, <code>targetType?</code>, <code>targetId?</code>, <code>summary?</code>, <code>metadata?</code>; the runtime injects <code>actor_id</code> (the current user), <code>tenant_id</code>, and <code>plugin_id</code>, and forces <code>actor_type = &#39;plugin&#39;</code>. A plugin cannot forge actor identity, write outside its tenant, or set <code>visibility = &#39;admin&#39;</code> (plugin-sourced events are always <code>user</code>-scoped in this RFC).</p><h3 id="_5-views" tabindex="-1">5. Views <a class="header-anchor" href="#_5-views" aria-label="Permalink to &quot;5. Views&quot;">​</a></h3><p>Two surfaces over the one table — they differ only in their query bound, not in how the Account tab is gated:</p><ul><li><strong>Account <code>/activity</code> tab</strong> — a new tab beside Profile / Preferences / Security in the Account plugin, available to <strong>all</strong> users. Reads a session-gated route scoped by <code>x-sovereign-user-id</code> (personal-scope query), reusing Account&#39;s existing read pattern (cf. the Security tab&#39;s session list).</li><li><strong>Console <code>/console/activity</code> tab</strong> — admin-only (Console is <code>adminOnly</code>, whole-prefix gated by <code>runtime/src/route-guard.ts</code>). The consolidated tenant-wide feed with filters (actor, action, date range) and pagination, read via an admin-key-gated API.</li></ul><h3 id="_6-sdk-surface" tabindex="-1">6. SDK surface <a class="header-anchor" href="#_6-sdk-surface" aria-label="Permalink to &quot;6. SDK surface&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// reserved — throws NotImplementedError in v1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sdk.activity.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entry: ActivityLogEntry): </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Promise</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;void&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ActivityLogEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Dotted action verb, e.g. &quot;list.created&quot;. Plugin events are namespaced by the runtime. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  action</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** The user this event is about, if any (drives personal-feed visibility). */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  subjectUserId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Generic target kind/id (e.g. &quot;list&quot; / the list id). */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  targetType</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  targetId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Human-readable one-liner. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  summary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Structured detail (before/after, etc.). Avoid PII beyond what&#39;s necessary. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  metadata</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A future <code>sdk.activity.list(query)</code> (plugins reading their own emitted events) is <strong>reserved/out of scope</strong> for this RFC.</p><h3 id="_7-permissions" tabindex="-1">7. Permissions <a class="header-anchor" href="#_7-permissions" aria-label="Permalink to &quot;7. Permissions&quot;">​</a></h3><ul><li><code>activity:write</code> — the plugin may emit activity events via <code>sdk.activity.log()</code>.</li><li><code>activity:read</code> is <strong>reserved/future</strong> (not added now) — for plugins reading back their own events.</li></ul><h3 id="_8-security-and-scoping" tabindex="-1">8. Security and scoping <a class="header-anchor" href="#_8-security-and-scoping" aria-label="Permalink to &quot;8. Security and scoping&quot;">​</a></h3><ul><li><strong>Tenant- and actor-scoped.</strong> The runtime injects <code>tenant_id</code>/<code>actor_id</code>; plugins cannot write cross-tenant or impersonate another actor.</li><li><strong>No leakage between users.</strong> Non-admins only ever match <code>actor_id = self OR subject_user_id = self</code> on <code>visibility = &#39;user&#39;</code> rows.</li><li><strong>Auth stays clean.</strong> Login/session capture is at the runtime boundary; <code>apps/auth</code> does not write the platform DB.</li><li><strong>Append-only.</strong> The log is write-once; no plugin or user API mutates or deletes rows. Retention/pruning (if any) is a platform-operator concern (Open questions).</li></ul><h2 id="impact-when-accepted-deferred-—-beyond-the-reserved-stub-already-landed" tabindex="-1">Impact when accepted (deferred — beyond the reserved stub already landed) <a class="header-anchor" href="#impact-when-accepted-deferred-—-beyond-the-reserved-stub-already-landed" aria-label="Permalink to &quot;Impact when accepted (deferred — beyond the reserved stub already landed)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Where</th><th>Change</th></tr></thead><tbody><tr><td><code>packages/manifest</code></td><td>Enforce <code>activity:write</code>; tests.</td></tr><tr><td><code>packages/sdk</code></td><td>Implement <code>sdk.activity.log()</code> against the runtime (replace the stub).</td></tr><tr><td><code>packages/db</code></td><td><code>activity_log</code> table (both dialects + parity) + <code>recordActivity()</code> helper + bootstrap DDL.</td></tr><tr><td>Runtime</td><td>Capture points, verify-boundary login capture, <code>sdk.activity</code> write-mediation, read APIs.</td></tr><tr><td>Console</td><td><code>/console/activity</code> admin view (filters, pagination).</td></tr><tr><td>Account</td><td><code>/activity</code> personal-feed tab.</td></tr><tr><td>SRS §3 / §4</td><td>Promote to specified; new <code>ALG-xx</code> functional requirements + decision-log entry.</td></tr><tr><td><code>docs/roadmap.md</code></td><td>The implementation task entry (sequenced).</td></tr></tbody></table><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Build on <code>sdk.events</code>.</strong> <code>events</code> is a post-v1, fire-and-forget pub/sub channel — it carries no durable record, no actor/tenant provenance, and no visibility model. Complementary at best (a capture point could publish an event <em>and</em> log activity); not a substitute for an audit table.</li><li><strong><code>apps/auth</code> writes the platform DB directly</strong> (for login/logout/register). Breaks the identity/platform separation — auth would couple to the platform schema. Rejected in favour of runtime verify-boundary capture.</li><li><strong>Per-request derived ACL instead of a stored <code>visibility</code>.</strong> Computing who may see each row at read time is slower and error-prone; a stored <code>visibility</code> + <code>subject_user_id</code> makes the user feed a single indexed query.</li><li><strong>Self-access alternatives</strong> (rejected in favour of the universal Account tab): <ul><li>A single <em>role-adaptive</em> non-<code>adminOnly</code> activity route — would carve a non-admin route out of the <code>adminOnly</code> Console, which the route-guard gates by whole prefix.</li><li>A <em>dedicated user Activity plugin</em> — most surface to build, a second read API, and an extra sidebar/launcher entry for what is a list view.</li><li>A <em>notifications/inbox</em> framing of <code>subject_user_id = self</code> rows — higher user value but larger scope; a natural <strong>follow-on</strong> RFC that would build on the reserved <code>sdk.notifications</code> surface, not this one.</li></ul></li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Retention/pruning.</strong> Keep forever, or cap by age/row-count per tenant? Proposal: no automatic pruning in v1; revisit if volume warrants.</li><li><strong>Registration capture.</strong> Recording <code>auth.registered</code> requires observing a pre-session event owned by <code>apps/auth</code>. Proposal: record on first runtime verify of a not-yet-seen user; alternative is a minimal auth→runtime signal.</li><li><strong>Logout observability.</strong> Sessions usually expire silently; only an explicit sign-out is a clean event. Proposal: log explicit sign-out and session revoke; do not synthesise a &quot;logout&quot; for silent expiry.</li><li><strong>PII in <code>metadata</code>.</strong> What may capture points store (IP, user-agent, before/after values)? Proposal: minimise; document per-action.</li><li><strong>Export/pagination.</strong> Console feed needs pagination; CSV/JSON export is desirable but deferred.</li><li><strong><code>activity:read</code>.</strong> Ship the plugin read-back permission/surface with <code>activity:write</code>, or later? Proposal: later — writers first.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Now (this change):</strong> reserved <code>sdk.activity</code> stub (<code>log()</code> throws <code>NotImplementedError</code>) + <code>activity:write</code> permission, all additive (SDK and manifest <strong>minor</strong> bumps). No behaviour change for existing plugins.</li><li><strong>On acceptance:</strong> apply the &quot;Impact when accepted&quot; table — <code>activity_log</code> table + <code>recordActivity()</code>, capture points + verify-boundary login, <code>sdk.activity</code> mediation, then the Console and Account views.</li><li>The mechanism becomes part of the public manifest + SDK contract from the <code>@sovereignfs/sdk</code> / <code>@sovereignfs/manifest</code> minor release that ships it.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft; reserved <code>sdk.activity</code> stub + <code>activity:write</code> permission.</td></tr><tr><td>1.0</td><td>Jun 2026</td><td>Accepted; incorporated into SRS §3.14, §5 (<code>activity:write</code>), and Task 0.5.13.</td></tr></tbody></table>`,53)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
