import{_ as t,o as i,c as s,a2 as a}from"./chunks/framework.BWuWLRhz.js";const u=JSON.parse('{"title":"RFC 0003 — Plugin monetization","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0003-plugin-monetization.md","filePath":"rfcs/0003-plugin-monetization.md"}'),n={name:"rfcs/0003-plugin-monetization.md"};function o(r,e,l,d,h,c){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-0003-—-plugin-monetization" tabindex="-1">RFC 0003 — Plugin monetization <a class="header-anchor" href="#rfc-0003-—-plugin-monetization" aria-label="Permalink to &quot;RFC 0003 — Plugin monetization&quot;">​</a></h1><p><strong>Status:</strong> Accepted<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> Manifest schema (<code>packages/manifest</code>), SDK (<code>packages/sdk</code>), runtime middleware, plugin registry, <code>packages/ui</code>, Console/Account, SRS<br><strong>Incorporated into plan:</strong> Yes — Phase 1 completed as Task 0.8.0 (manifest field, <code>sdk.billing</code> stub, entitlement gating, offline Ed25519 verification, paywall, Account billing tab, Console entitlements view, license generator for operators). Phase 2 (automated payment collection) scheduled as post-v1 Task 1.0.2.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Let <strong>plugin authors monetize their plugins</strong>. The platform and a set of always-free plugins stay open; an author may mark a plugin as paid and require an end user to <strong>buy access</strong> before its routes load. A plugin declares its model in its manifest:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;monetization&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;model&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;free&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;one_time&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;recurring&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;pay_what_you_want&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;interval&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;day&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;week&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;month&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;year&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// recurring only</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;tiers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Pro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;price&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">900</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;USD&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;license&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;publicKey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// verifies author-issued entitlements</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The <strong>plugin author is the merchant</strong> and <strong>fixes the price</strong>. Access is granted by a <strong>signed entitlement (license) the author issues on payment</strong>, which the instance <strong>verifies offline</strong> against the author&#39;s public key — so monetization works on a self-hosted instance with <strong>no mandatory central service</strong>. Payment is collected through a <strong>provider adapter</strong> (manual/bank transfer, PayPal, Stripe to start; extensible). Enforcement is server-side: the runtime gates a paid plugin&#39;s routes by entitlement, and a reserved <code>sdk.billing</code> surface lets a plugin gate features by tier.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Sovereign&#39;s thesis is that the <strong>plugin system is the product</strong>. A durable plugin ecosystem needs a way for authors to be paid — otherwise non-trivial third-party plugins have no business model and the ecosystem stays hobby-scale. Today every installed plugin is free to every user on the instance; there is no notion of a paid plugin or of access tied to payment.</p><p>The constraint is that Sovereign is <strong>self-hosted</strong> and &quot;vendor lock-in is a defect&quot; (§1.7). Monetization must not require routing every instance&#39;s money through a Sovereign-operated service, and it must keep working when a plugin is installed on an air-gapped or self-managed box. The design below treats monetization as an <strong>optional, additive</strong> capability layered on the existing plugin model, and keeps the core platform fully functional without it.</p><p>This is <strong>not</strong> a hosted public marketplace (a v1 non-goal, §1.4/§4.6). It adds the <em>licensing/entitlement plumbing</em> that lets an author sell access to a plugin; discovery and a hosted store are explicitly out of scope (see Alternatives).</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>Manifest</strong> (<code>packages/manifest/src/schema.ts</code>, <code>.strict()</code>) declares a plugin&#39;s <code>type</code> (<code>platform</code>/<code>sovereign</code>/<code>community</code>), <code>permissions</code>, <code>repository</code>, etc. There is no pricing or access-cost concept.</li><li><strong>Route gating precedent:</strong> the runtime middleware already gates routes by <code>adminOnly</code> (403) and by disabled status (404), keyed on <code>routePrefix</code>. Because the Edge middleware can&#39;t read the DB, it fetches the gating facts from a Node-runtime route (the <code>/api/admin/plugins/disabled</code> pattern). Entitlement gating reuses this exact shape.</li><li><strong>Reserved-surface pattern:</strong> the SDK declares not-yet-implemented surfaces (<code>storage</code>, <code>notifications</code>, <code>events</code>, and <code>data</code> from RFC 0002) as stubs that throw <code>NotImplementedError</code>, with matching reserved manifest permissions. A <code>billing</code> surface would follow the same pattern.</li><li><strong>Registry</strong> (Task 1.0.1, <code>registry/plugins.json</code>) plus the manifest <code>repository</code>/<code>type</code> fields are the natural place to publish an author&#39;s license <strong>public key</strong> and establish author identity.</li><li><strong>Licensing</strong> (§2.7): third-party plugins may use any license; commercial / proprietary plugins are the motivating case for monetization.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-monetization-models-and-tiers" tabindex="-1">1. Monetization models and tiers <a class="header-anchor" href="#_1-monetization-models-and-tiers" aria-label="Permalink to &quot;1. Monetization models and tiers&quot;">​</a></h3><p>A plugin&#39;s manifest declares one <strong>model</strong>, author-fixed:</p><ul><li><code>free</code> — default; no entitlement required (equivalent to omitting the field).</li><li><code>one_time</code> — a single payment grants perpetual access.</li><li><code>recurring</code> — access requires an active subscription billed every <code>interval</code> (<code>day</code>/<code>week</code>/<code>month</code>/<code>year</code>).</li><li><code>pay_what_you_want</code> — the user chooses any amount ≥ an optional floor; grants access like <code>one_time</code> (or <code>recurring</code> if combined — out of scope v1).</li></ul><p><strong>Tiers</strong> (optional) are named access levels (e.g. <code>basic</code>/<code>pro</code>) with their own price; the active tier is carried in the entitlement so a plugin can unlock features accordingly. Prices are integer <strong>minor units</strong> + ISO 4217 currency (the money convention used across the platform).</p><h3 id="_2-what-is-monetizable" tabindex="-1">2. What is monetizable <a class="header-anchor" href="#_2-what-is-monetizable" aria-label="Permalink to &quot;2. What is monetizable&quot;">​</a></h3><ul><li><strong>Platform</strong> plugins (Console, Account, Launcher) and the platform itself are <strong>always free</strong> — they are core, ship in the monorepo, and cannot declare <code>monetization</code>.</li><li>Only <code>sovereign</code> / <code>community</code> plugins may be paid.</li><li>An instance operator may always <strong>disable</strong> a paid plugin (existing CON-07); a disabled plugin is never billable.</li></ul><h3 id="_3-author-as-merchant-via-decentralized-signed-licenses-recommended" tabindex="-1">3. Author-as-merchant via decentralized signed licenses (recommended) <a class="header-anchor" href="#_3-author-as-merchant-via-decentralized-signed-licenses-recommended" aria-label="Permalink to &quot;3. Author-as-merchant via decentralized signed licenses (recommended)&quot;">​</a></h3><p>The plugin author is the merchant of record and is paid directly. Access is proven by an <strong>author-issued, signed entitlement token</strong> — a &quot;license&quot;:</p><ol><li>The author holds a <strong>keypair</strong>. The <strong>public key</strong> is published in the plugin&#39;s manifest (<code>monetization.license.publicKey</code>) and/or the registry entry.</li><li>A user on an instance initiates purchase. The instance hands off to the author&#39;s <strong>checkout</strong> (the author&#39;s payment provider) or, for manual/bank, to the author&#39;s payment instructions.</li><li>On confirmed payment the author&#39;s billing issues a <strong>signed license</strong> scoped to <code>{ pluginId, subscriber identity, tier, issuedAt, expiresAt? }</code>, signed with the author&#39;s private key.</li><li>The instance stores the license and <strong>verifies its signature offline</strong> against the author&#39;s public key on every entitlement check. No call to a Sovereign service is required.</li><li><code>recurring</code> licenses carry <code>expiresAt</code>; the instance treats an expired license as no access and prompts renewal. Renewal re-issues a license (online check to the author&#39;s endpoint, or a fresh signed token). A <strong>grace period</strong> is allowed before hard lock.</li><li><strong>Manual / bank transfer</strong> is a first-class flow: the author confirms receipt out of band and issues the signed license, which the user (or the instance admin) imports. No automation or live gateway needed.</li></ol><p>This keeps money and merchant responsibility with the author, requires <strong>no central service</strong>, supports offline verification, and naturally accommodates manual payment — at the cost of authors running (or using a hosted) license-issuing checkout, and of revocation requiring expiry/online re-checks (see Open questions).</p><h3 id="_4-manifest-declarations" tabindex="-1">4. Manifest declarations <a class="header-anchor" href="#_4-manifest-declarations" aria-label="Permalink to &quot;4. Manifest declarations&quot;">​</a></h3><p><code>monetization</code> is an <strong>optional</strong> object (deferred — added when accepted):</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;monetization&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;model&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;recurring&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;interval&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;month&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tiers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;basic&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Basic&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;price&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;USD&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Pro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;price&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;USD&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;license&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;publicKey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;base64 ed25519 public key&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Validated at build time against the manifest schema (a <code>free</code>/absent value keeps every existing manifest valid). The manifest declares pricing; the operator and users do not change it.</p><h3 id="_5-payment-provider-adapters" tabindex="-1">5. Payment-provider adapters <a class="header-anchor" href="#_5-payment-provider-adapters" aria-label="Permalink to &quot;5. Payment-provider adapters&quot;">​</a></h3><p>A small <strong><code>PaymentProvider</code> interface</strong> abstracts collection so adapters are pluggable (same seam idea as the protocol adapters in other plugin specs):</p><ul><li><strong>Manual / bank transfer</strong> — present instructions; settlement and license issuance are confirmed out of band by the author.</li><li><strong>Stripe</strong> — hosted Checkout + signed <strong>webhooks</strong>; recurring handled by Stripe subscriptions; webhook → license issue/renew/revoke.</li><li><strong>PayPal</strong> — hosted checkout + IPN/webhooks, same shape.</li><li><strong>Extensible</strong> — new providers implement the interface without core changes.</li></ul><p><strong>No card data ever touches the instance</strong> — hosted checkout only (keeps PCI scope off the platform).</p><h3 id="_6-entitlement-enforcement" tabindex="-1">6. Entitlement enforcement <a class="header-anchor" href="#_6-entitlement-enforcement" aria-label="Permalink to &quot;6. Entitlement enforcement&quot;">​</a></h3><p>Server-side and authoritative; the client is never trusted.</p><ul><li>The runtime <strong>middleware gates a paid plugin&#39;s <code>routePrefix</code></strong> by entitlement, reusing the <code>adminOnly</code>/disabled pattern (Edge middleware fetches entitlement facts from a Node-runtime route). No valid entitlement → redirect to a <strong>paywall</strong> page (or <code>402 Payment Required</code> for API routes).</li><li>A reserved <strong><code>sdk.billing</code> / <code>sdk.entitlements</code></strong> surface lets a plugin check the current user&#39;s entitlement and <strong>tier</strong> to gate features in-app (<code>sdk.billing.requireEntitlement()</code> / <code>getEntitlement()</code>), throwing/normalizing when absent. Until implemented it throws <code>NotImplementedError</code>.</li><li>Entitlements are stored per <code>{ user, plugin, tier, status, source, expiresAt }</code> in the platform DB (with <code>tenant_id</code>).</li></ul><h3 id="_7-ux" tabindex="-1">7. UX <a class="header-anchor" href="#_7-ux" aria-label="Permalink to &quot;7. UX&quot;">​</a></h3><ul><li><strong>Account</strong> — purchase / import a license, view active subscriptions and renewal dates, cancel.</li><li><strong>Console</strong> — view entitlements across users, import/confirm manual payments, troubleshoot (admin oversight; the operator is not the merchant).</li><li><strong>Paywall</strong> — a runtime-owned page shown when a user without entitlement hits a paid plugin, showing the author&#39;s tiers/prices and a purchase/redeem action.</li></ul><h3 id="_8-security-and-legal" tabindex="-1">8. Security and legal <a class="header-anchor" href="#_8-security-and-legal" aria-label="Permalink to &quot;8. Security and legal&quot;">​</a></h3><ul><li>License <strong>signature verification</strong> with the author&#39;s public key; support <strong>key rotation</strong> (publish new keys via registry; keep old keys valid for issued licenses).</li><li>The <strong>author is merchant of record</strong> — taxes, invoicing, refunds, and chargebacks are the author&#39;s responsibility, not the instance operator&#39;s or the Sovereign project&#39;s.</li><li>Billing/PII data is minimized and subject to the platform&#39;s privacy posture (GDPR); the instance stores entitlements, not card data.</li></ul><h3 id="_9-self-host-reconciliation" tabindex="-1">9. Self-host reconciliation <a class="header-anchor" href="#_9-self-host-reconciliation" aria-label="Permalink to &quot;9. Self-host reconciliation&quot;">​</a></h3><p>Monetization is <strong>optional and additive</strong>: an instance with no paid plugins, or offline, is unaffected. Entitlements verify <strong>offline</strong> against published author keys, so a paid plugin keeps working without contacting any Sovereign service. This satisfies §1.7 (&quot;no cloud dependency for <strong>core</strong> functionality&quot;) — paid plugins are not core — and avoids making the project a payment intermediary.</p><h2 id="impact-when-accepted-deferred-—-no-edits-yet" tabindex="-1">Impact when accepted (deferred — no edits yet) <a class="header-anchor" href="#impact-when-accepted-deferred-—-no-edits-yet" aria-label="Permalink to &quot;Impact when accepted (deferred — no edits yet)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Where</th><th>Change</th></tr></thead><tbody><tr><td><code>packages/manifest</code></td><td>Optional <code>monetization</code> object (model/interval/tiers/license); validation + tests; <strong>minor</strong> bump.</td></tr><tr><td><code>packages/sdk</code></td><td>Reserved <code>sdk.billing</code>/<code>entitlements</code> surface (stub) + <code>EntitlementRequiredError</code>; <strong>minor</strong> bump.</td></tr><tr><td>Runtime middleware + a Node route</td><td>Entitlement gating by <code>routePrefix</code> (paywall / <code>402</code>), mirroring the disabled-plugin gating pattern.</td></tr><tr><td>Runtime</td><td><code>PaymentProvider</code> adapter interface; manual/bank, Stripe, PayPal adapters; webhook endpoints + verification.</td></tr><tr><td>Platform DB</td><td><code>entitlements</code> (+ a payments/transactions ledger), with <code>tenant_id</code>.</td></tr><tr><td>Plugin registry</td><td>Publish author identity + license public key(s); key rotation.</td></tr><tr><td><code>packages/ui</code></td><td>Paywall / pricing components; subscription-management UI.</td></tr><tr><td>Console / Account</td><td>Subscription management (Account) and entitlement oversight + manual-payment confirmation (Console).</td></tr><tr><td>SRS §1.4 / §4.6 / §1.7</td><td>Clarify that monetization (licensing plumbing, not a hosted marketplace) is in scope; reconcile wording.</td></tr><tr><td><code>docs/roadmap.md</code></td><td>A phased implementation task (manifest + entitlement gating first; providers; subscription UX).</td></tr></tbody></table><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>Central Sovereign billing / marketplace</strong> (a hosted store; Stripe Connect to route payouts to authors; central license issuance and discovery). Best UX and the only clean path to plugin <strong>discovery</strong>, but introduces a central dependency (against §1.7), makes the project a payment intermediary with the attendant PCI/tax/legal burden, and squarely revisits the marketplace non-goal. Recorded as a <strong>possible future convenience layer</strong> on top of the decentralized model, not the v1 mechanism.</li><li><strong>Operator-as-merchant</strong> (the instance admin connects their own gateway and sells plugin access to their users). Fits single-tenant self-host cleanly, but does not pay plugin authors — so it does not serve the stated goal. Rejected.</li><li><strong>Honor-system / unsigned flags</strong> (mark a plugin paid, no cryptographic entitlement). Trivial to bypass; provides no real gate. Rejected.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Decentralized license-key vs central store</strong> as the v1 mechanism — the primary fork. Recommendation: decentralized signed licenses now; central store as a later optional layer.</li><li><strong>License token format</strong> — signed JWT vs a compact license key; <strong>offline-only</strong> verification vs an optional <strong>online</strong> status check (for instant revocation).</li><li><strong>Revocation</strong> on refund/chargeback — short-lived tokens + renewal, an online revocation list, or accept eventual expiry. Trade-off vs offline guarantee.</li><li><strong>Payout routing</strong> if any central/hosted component is offered (Stripe Connect? author-direct only?).</li><li><strong>Free trials</strong>, <strong>regional pricing/currency</strong>, <strong>proration</strong>, <strong>taxes/invoicing</strong> — likely out of v1; confirm.</li><li><strong>Author identity &amp; key distribution/rotation</strong> and the <strong>trust model</strong> for <code>community</code> plugins (who vouches for a public key?).</li><li>Exact <strong>SRS wording</strong> to revise in §1.4/§4.6 (marketplace non-goal) and §1.7 (cloud-dependency), so &quot;no hosted marketplace&quot; and &quot;monetization exists&quot; coexist.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li>Accept RFC → land the manifest <code>monetization</code> field + reserved <code>sdk.billing</code> stub and <code>entitlements</code> table (additive, no behaviour change) — the same reserve-first step used for RFC 0002.</li><li>Implement <strong>entitlement gating</strong> (middleware + paywall) against manually imported signed licenses — proves the model end to end with the <strong>manual/bank</strong> adapter and zero external dependencies.</li><li>Add the <strong>Stripe</strong> then <strong>PayPal</strong> adapters (hosted checkout + webhooks) and the subscription-management UX in Account/Console.</li><li>Revisit a central discovery/checkout layer only if there is demand.</li></ol><h2 id="phase-2-—-automated-payment-collection-post-v1-task-1-0-2" tabindex="-1">Phase 2 — Automated payment collection (post-v1, Task 1.0.2) <a class="header-anchor" href="#phase-2-—-automated-payment-collection-post-v1-task-1-0-2" aria-label="Permalink to &quot;Phase 2 — Automated payment collection (post-v1, Task 1.0.2)&quot;">​</a></h2><p>Phase 1 (Task 0.8.0) ships the manual Ed25519 token model. Phase 2 adds automated payment collection — users subscribe and receive access without the operator generating and distributing a token by hand.</p><h3 id="bank-transfer-admin-confirmation-flow" tabindex="-1">Bank transfer + admin confirmation flow <a class="header-anchor" href="#bank-transfer-admin-confirmation-flow" aria-label="Permalink to &quot;Bank transfer + admin confirmation flow&quot;">​</a></h3><ol><li>User opens the paywall page and clicks &quot;Request access via bank transfer.&quot;</li><li><code>sdk.billing.requestSubscription({ pluginId, tierId })</code> creates a <code>payment_requests</code> row (<code>status: pending</code>) and presents the operator&#39;s configured payment instructions (IBAN / account details stored in Console Settings — a new <code>bank_transfer_details</code><code>platform_setting</code> key).</li><li>Console gains a <strong>Pending Payments</strong> sub-section under Entitlements — lists pending requests with subscriber, plugin, tier, amount, and requested-at timestamp.</li><li>The operator confirms receipt out of band (inspects their bank statement), then clicks <strong>Confirm payment</strong> → status transitions to <code>confirmed</code> → the runtime auto-creates an <code>entitlements</code> row (as if <code>source: &#39;bank_transfer&#39;</code>). The subscriber immediately gains access on their next request.</li><li><strong>Reject</strong> sets status to <code>rejected</code> and, if SMTP is configured, sends the subscriber a notification email.</li></ol><h3 id="stripe-paypal-webhook-flow" tabindex="-1">Stripe / PayPal webhook flow <a class="header-anchor" href="#stripe-paypal-webhook-flow" aria-label="Permalink to &quot;Stripe / PayPal webhook flow&quot;">​</a></h3><p>Plugin authors who prefer fully automated collection configure their payment gateway&#39;s webhook to POST to their plugin&#39;s serve route (<code>/api/&lt;slug&gt;/payment-webhook</code>), which is delegated via the existing public API namespace (RFC 0008/PLT-16). The plugin&#39;s server-side webhook handler:</p><ol><li>Verifies the gateway signature.</li><li>Calls <code>sdk.billing.grantEntitlement({ userId, pluginId, tierId, expiresAt })</code> to write the entitlement directly — no signed token required (server-side trust).</li></ol><p>The platform itself ships no gateway adapter code; payment integration is entirely in the plugin. <code>sdk.billing.grantEntitlement()</code> is the platform-side seam.</p><h3 id="new-db-table-phase-2" tabindex="-1">New DB table (Phase 2) <a class="header-anchor" href="#new-db-table-phase-2" aria-label="Permalink to &quot;New DB table (Phase 2)&quot;">​</a></h3><p><code>payment_requests</code> — <code>{ id, userId, pluginId, tierId, status, requestedAt, resolvedAt, resolvedBy, notes, tenantId }</code>.</p><p>Both SQLite and Postgres dialects; drizzle-kit migration added.</p><h3 id="new-updated-sdk-surfaces-phase-2" tabindex="-1">New / updated SDK surfaces (Phase 2) <a class="header-anchor" href="#new-updated-sdk-surfaces-phase-2" aria-label="Permalink to &quot;New / updated SDK surfaces (Phase 2)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Surface</th><th>Status</th><th>Notes</th></tr></thead><tbody><tr><td><code>sdk.billing.requestSubscription(input)</code></td><td>New (throws <code>NotImplementedError</code> in Phase 1)</td><td>Creates a <code>payment_requests</code> row; returns instructions.</td></tr><tr><td><code>sdk.billing.grantEntitlement(input)</code></td><td>New</td><td>Admin/webhook path to write an entitlement without a signed token. Callable only from plugin server code.</td></tr><tr><td><code>sdk.billing.getEntitlement(pluginId)</code></td><td>Phase 1 stub → Phase 2 implemented</td><td>Returns the current user&#39;s active entitlement for a plugin.</td></tr><tr><td><code>sdk.billing.requireEntitlement(pluginId)</code></td><td>Phase 1 stub → Phase 2 implemented</td><td>Throws <code>EntitlementRequiredError</code> if no active entitlement.</td></tr></tbody></table><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft.</td></tr><tr><td>0.2</td><td>Jun 2026</td><td>Added to the roadmap as exploratory Task 0.8.0 (gated on acceptance; still Draft).</td></tr><tr><td>0.3</td><td>Jun 2026</td><td>RFC accepted; status updated to Accepted; Task 0.8.0 now scheduled (no implementation yet).</td></tr><tr><td>0.4</td><td>Jun 2026</td><td>Phase 1 complete (Task 0.8.0). Phase 2 section added; roadmap Task 1.0.2 scheduled.</td></tr></tbody></table>`,64)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
