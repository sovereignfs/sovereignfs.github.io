import{_ as a,o as i,c as t,a2 as s}from"./chunks/framework.CHeM0PsO.js";const g=JSON.parse('{"title":"RFC 0022 — Plugin-declared capabilities","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0022-plugin-capabilities.md","filePath":"rfcs/0022-plugin-capabilities.md"}'),n={name:"rfcs/0022-plugin-capabilities.md"};function o(l,e,r,d,p,h){return i(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="rfc-0022-—-plugin-declared-capabilities" tabindex="-1">RFC 0022 — Plugin-declared capabilities <a class="header-anchor" href="#rfc-0022-—-plugin-declared-capabilities" aria-label="Permalink to &quot;RFC 0022 — Plugin-declared capabilities&quot;">​</a></h1><p><strong>Status:</strong> Implemented<br><strong>Date:</strong> June 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>packages/manifest</code> (a new <code>capabilities</code> declaration), <code>packages/sdk</code> (<code>hasCapability</code>/<code>requireCapability</code> resolve plugin capabilities), the capability resolver + session from RFC 0021, an optional platform/Console assignment surface, <code>docs/plugin-development.md</code>; <strong>builds on RFC 0021</strong> (capability model, resolver, SDK helper, <code>platform:</code>/<code>plugin:</code> namespacing), RFC 0018 (manifest declaration + namespacing pattern), RFC 0005 (audit)<br><strong>Incorporated into plan:</strong> Yes — scheduled as roadmap Task 0.6.1; documentation-first. This RFC specifies how plugins declare and use their own plugin-scoped capabilities; SRS requirement IDs, scheduling, and task allocation are deferred.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Let a <strong>plugin</strong> declare its own authorization vocabulary — namespaced capabilities like <code>splitify:create-group</code> or <code>splitify:settle</code> — so plugin code can gate features on something finer than &quot;is this user a platform admin.&quot; All users remain <code>platform:user</code> by default (RFC 0021); plugin capabilities are an <strong>additional, plugin-scoped</strong> layer:</p><ul><li>declared in the <strong>manifest</strong> (auto-namespaced by plugin slug, validated at build);</li><li><strong>enforced inside the plugin</strong> via the RFC 0021 SDK helper (<code>sdk.auth.hasCapability(&#39;splitify:create-group&#39;)</code>) — <strong>not</strong> by the platform route gate;</li><li>assigned through a model this RFC scopes (the central open question: platform-stored vs plugin-managed).</li></ul><p>This is the layer SRS §3.4 reserved when it chose <code>platform:</code> namespacing to &quot;scale to plugin-level roles&quot; and deferred per-plugin assignment.</p><h2 id="relationship-to-rfc-0021" tabindex="-1">Relationship to RFC 0021 <a class="header-anchor" href="#relationship-to-rfc-0021" aria-label="Permalink to &quot;Relationship to RFC 0021&quot;">​</a></h2><p>RFC 0021 makes <strong>capabilities</strong> the platform&#39;s enforcement unit, adds the resolver and the <code>hasCapability</code>/<code>requireCapability</code> SDK helper, and establishes the <code>platform:</code> / <code>plugin:</code> namespace split. RFC 0022 reuses all of that for the <strong>plugin</strong> namespace: plugins declare <code>&lt;slug&gt;:&lt;capability&gt;</code> and check them with the same helper. The distinction: <strong>platform</strong> capabilities gate platform routes/chrome; <strong>plugin</strong> capabilities gate behaviour <em>inside</em> a plugin.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>A plugin like Splitify has its own roles in spirit — who can create a group, settle balances, manage members — but today it has <strong>no declared vocabulary</strong> for them. A plugin that needs authorization must roll its own entirely in its slug-prefixed tables (it has <code>db:readWrite</code>) and check it ad hoc; <code>sdk.auth</code> exposes only the platform role. There&#39;s no shared way to <em>name</em> these capabilities, no consistency across plugins, and nothing the platform/owner can see, assign, or audit centrally. Declaring plugin capabilities gives them a first-class, namespaced, validated home.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong>No plugin authz vocabulary.</strong> Plugins implement any roles privately; there is no manifest field for capabilities and no SDK concept of a plugin capability.</li><li><strong>0021&#39;s foundation.</strong> The capability resolver, <code>hasCapability</code>/<code>requireCapability</code>, capabilities carried in the session, and the <code>platform:</code>/<code>plugin:</code> namespacing all come from RFC 0021.</li><li><strong>Reserved hooks.</strong> The manifest&#39;s <code>admin:*</code> permission was explicitly noted for &quot;future fine-grained plugin admin scopes&quot;; RFC 0018 established the manifest-declaration + auto-namespacing pattern (for env) that this mirrors.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="manifest-capabilities-declaration" tabindex="-1">Manifest <code>capabilities</code> declaration <a class="header-anchor" href="#manifest-capabilities-declaration" aria-label="Permalink to &quot;Manifest \`capabilities\` declaration&quot;">​</a></h3><p>A plugin declares its capabilities in the manifest, <strong>auto-namespaced</strong> by its slug:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;capabilities&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;create-group&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Create a new expense group.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;defaultGrant&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;settle&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Settle balances for a group.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;manage-members&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Add or remove group members.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>These surface as <code>splitify:create-group</code>, <code>splitify:settle</code>, etc. <code>defaultGrant: true</code> means every user of the plugin holds it implicitly; otherwise it requires explicit assignment. Validated at build like <code>permissions</code> / <code>env</code> (RFC 0018); namespacing prevents collisions with platform capabilities or other plugins. Adds a manifest field (minor bump) + a docs-parity entry.</p><h3 id="enforcement-is-intra-plugin-via-the-sdk" tabindex="-1">Enforcement is intra-plugin, via the SDK <a class="header-anchor" href="#enforcement-is-intra-plugin-via-the-sdk" aria-label="Permalink to &quot;Enforcement is intra-plugin, via the SDK&quot;">​</a></h3><p>Plugin code gates on its own capabilities with the RFC 0021 helper:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.auth.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">requireCapability</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;splitify:create-group&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><strong>The platform route gate does not enforce plugin capabilities</strong> — they are the plugin&#39;s concern, checked where the plugin decides. (Platform capabilities, by contrast, gate routes and chrome in the middleware.) Stating this plainly avoids the expectation that <code>adminOnly</code>-style gating applies to plugin capabilities.</p><h3 id="assignment-model-the-central-open-question" tabindex="-1">Assignment model (the central open question) <a class="header-anchor" href="#assignment-model-the-central-open-question" aria-label="Permalink to &quot;Assignment model (the central open question)&quot;">​</a></h3><p>Two shapes, with different trade-offs:</p><ul><li><strong>Platform-stored grants</strong> — the platform owns a <code>user × capability</code> store; plugin capabilities resolve into the session alongside platform ones; consistent assignment UI and central audit — but the platform then stores plugin-domain authz, and the session can bloat.</li><li><strong>Plugin-managed</strong> — the plugin stores its own grants in its slug-prefixed tables and checks them; the manifest declaration is the <strong>vocabulary</strong> that an optional shared/Console assignment surface can read. Keeps plugin authz in the plugin; less central consistency.</li></ul><p><strong>Recommendation:</strong> lean <strong>plugin-managed</strong> for genuinely plugin-scoped roles (e.g. a Splitify <em>group owner</em> grants <code>manage-members</code> within their group — domain data the plugin already owns), with the manifest vocabulary enabling a shared assignment UI where useful. Present both; mark the v1 storage choice as open.</p><h3 id="who-assigns" tabindex="-1">Who assigns <a class="header-anchor" href="#who-assigns" aria-label="Permalink to &quot;Who assigns&quot;">​</a></h3><p>Plugin-internal (a plugin&#39;s own owner/admin role grants to others within the plugin) vs the platform owner/Console. Likely <strong>plugin-internal</strong> for domain roles, with an <strong>optional platform-owner override</strong>. Open question.</p><h2 id="ui-flows" tabindex="-1">UI / flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI / flows&quot;">​</a></h2><p>A plugin declares <code>splitify:create-group</code> etc. in its manifest → its code calls <code>requireCapability(&#39;splitify:create-group&#39;)</code> where needed → a user without it is refused by the plugin (the plugin chooses the UX) → grants are assigned per the chosen model (e.g. a group owner grants within Splitify) and, where applicable, audited (RFC 0005).</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ol><li><strong>No declaration (status quo).</strong> Plugins fully roll their own authz. Works, but there&#39;s no shared vocabulary, no consistent assignment UI, and nothing the platform/owner can see or audit. Rejected — declaring capabilities is the point.</li><li><strong>Platform-stored only.</strong> Central and consistent, but puts plugin-domain authorization (and its data) in the platform and bloats the session. Kept as one option, not mandated.</li><li><strong>Plugin-managed only, no manifest declaration.</strong> Keeps authz in the plugin but loses the named vocabulary that makes a shared UI / audit possible. Rejected in favour of declaring the vocabulary even when storage is plugin-managed.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Storage</strong> — platform-stored vs plugin-managed vs hybrid (the central decision).</li><li><strong>Assignment actor/UI</strong> — plugin-internal vs platform owner vs both.</li><li><strong><code>defaultGrant</code> semantics</strong> — granted to all plugin users vs all platform users of the instance.</li><li><strong>Session carriage</strong> — do plugin capabilities ride in the session cookie (bloat, staleness) or are they checked on demand against the store?</li><li><strong>Owner override</strong> — can the platform owner override a plugin&#39;s grants?</li><li><strong>Plugin-level roles</strong> — bundles of plugin capabilities (<code>splitify:owner</code>) vs flat capabilities; do plugins get presets like the platform does?</li><li><strong>Requirement IDs</strong> — deferred until accepted.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Documentation-first (this RFC).</strong></li><li><strong>When accepted &amp; scheduled (after RFC 0021):</strong> the manifest <code>capabilities</code> field + validation, the SDK plumbing so <code>hasCapability</code>/<code>requireCapability</code> resolve plugin capabilities, and the chosen assignment/storage model + <code>docs/plugin-development.md</code>.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>Jun 2026</td><td>Initial draft; plugins declare namespaced <code>&lt;slug&gt;:&lt;capability&gt;</code> in the manifest, enforced intra-plugin via the RFC 0021 SDK helper; assignment-model (platform-stored vs plugin-managed) left as the central open question; documentation-first.</td></tr><tr><td>0.2</td><td>Jun 2026</td><td>Accepted; scheduled in the roadmap as Task 0.6.1.</td></tr></tbody></table>`,38)])])}const u=a(n,[["render",o]]);export{g as __pageData,u as default};
