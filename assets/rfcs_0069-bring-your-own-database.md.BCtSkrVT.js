import{_ as t,o,c as a,a2 as n}from"./chunks/framework.CHeM0PsO.js";const p=JSON.parse('{"title":"RFC 0069 — Bring-your-own database (per-user external Postgres)","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0069-bring-your-own-database.md","filePath":"rfcs/0069-bring-your-own-database.md"}'),r={name:"rfcs/0069-bring-your-own-database.md"};function s(i,e,d,l,c,u){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="rfc-0069-—-bring-your-own-database-per-user-external-postgres" tabindex="-1">RFC 0069 — Bring-your-own database (per-user external Postgres) <a class="header-anchor" href="#rfc-0069-—-bring-your-own-database-per-user-external-postgres" aria-label="Permalink to &quot;RFC 0069 — Bring-your-own database (per-user external Postgres)&quot;">​</a></h1><p><strong>Status:</strong> Withdrawn (evaluated, not recommended for v1)<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> packages/db, apps/auth, runtime/src (db resolution, admin aggregates), plugins/account, docs; touches RFC 0004, RFC 0036<br><strong>Incorporated into plan:</strong> No — evaluated and rejected for v1. Recorded here as a decision, not a design to build. Revisit only if federation becomes an active, funded initiative (see Alternatives/Open questions).</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Proposal under evaluation: let an individual end-user configure their own external Postgres connection from the Account section, after which that user&#39;s plugin data is read from and written to their remote database instead of the shared platform database — while every other user, and the platform&#39;s own default, keeps using the shared instance database unchanged. Isolated per-plugin databases (RFC 0004) stay as-is.</p><p>This RFC concludes the feature is <strong>not feasible as a stable v1 feature</strong>. The blocking issue is not the database connection itself — Postgres-only, one new connection string, looks small — but that nearly every layer of the data architecture (connection resolution, shared-table plugins, admin aggregate queries, cross-plugin references, migrations) currently assumes exactly one queryable database per instance, and this proposal is a form of per-user federation that the project has already, twice, evaluated and deferred at a smaller scope (per-plugin, not per-user).</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The underlying user need is legitimate and matches Sovereign&#39;s data-ownership positioning: some users may want their data to physically live somewhere they control, independent of the operator&#39;s infrastructure. The question this RFC answers is whether that need can be met today by adding an Account-level &quot;point your data at your own Postgres&quot; toggle, without a larger redesign.</p><h2 id="current-state-what-this-evaluation-rests-on" tabindex="-1">Current state (what this evaluation rests on) <a class="header-anchor" href="#current-state-what-this-evaluation-rests-on" aria-label="Permalink to &quot;Current state (what this evaluation rests on)&quot;">​</a></h2><ul><li><strong>Connection model is a process singleton, not per-request/per-user.</strong><code>packages/db/src/platform-db.ts</code> — <code>getPlatformDb()</code> memoises one client for the life of the process (<code>_dbPromise</code>, created exactly once). <code>packages/db/src/client.ts</code> — <code>createClient()</code> builds one <code>pg.Pool</code> (Postgres) or one <code>better-sqlite3</code> handle (SQLite) from a single boot-time connection string. The <strong>only</strong> per-request DB selection anywhere in the codebase is the RFC 0020 dev-mode switch (<code>runtime/src/db.ts</code>), and even that picks between two <strong>boot-time-fixed, env-configured</strong> connection strings via a shared-secret header — not a dynamically stored, user-supplied one.</li><li><strong><code>tenant_id</code> is a <code>WHERE</code>-clause filter, not a routing key.</strong> CLAUDE.md: <em>&quot;One deployment = one tenant... There is no concept of separate tenants within a single instance... the auth and data layers must not actively prevent [multi-tenancy]... but no multi-tenant logic is built in v1.&quot;</em><code>packages/db/src/platform-db.ts</code> — <code>DEFAULT_TENANT_ID = &#39;default&#39;</code>; every helper filters <code>WHERE tenant_id = ...</code> inside one physical connection. Nothing maps a tenant or user ID to a different connection string.</li><li><strong>RFC 0004 (per-plugin isolated database) already proposed and deferred exactly this idea, at the plugin level:</strong> its Alternatives-considered section lists <em>&quot;Bring-your-own / external database now (operator points a plugin at an external connection string, possibly a different engine). Most flexible but adds secret management, config surface, and dialect-divergence risk. <strong>Deferred</strong> to a future extension.&quot;</em> Its Implementation section resolves this: <em>&quot;BYO external database: <strong>deferred</strong>. The <code>DATABASE_URL</code>/<code>DB_DIALECT</code> env-config approach... is still the path if needed; not built in this task.&quot;</em> Isolation as actually shipped is schema-per-plugin <strong>on the same server/connection string</strong> (<code>packages/db/src/plugin-client.ts</code> — <code>CREATE SCHEMA IF NOT EXISTS</code>), never a different physical database.</li><li><strong>RFC 0036 (per-plugin dialect selection) evaluated it again and rejected it a second time:</strong> its Alternatives section names the shape almost exactly — a plugin-scoped external Postgres URL via an <code>SV_PLUGIN_&lt;SLUG&gt;_DB_URL</code>-style convention — and rejects it: <em>&quot;significantly expands scope (new env var convention, per-plugin connection pool management, operator documentation)... RFC 0004 already deferred BYO-DB explicitly. This can be a follow-on RFC when there is concrete demand.&quot;</em> No such follow-on RFC existed before this one, and this evaluation is that follow-on — at a harder scope (per-user, not per-plugin) than either prior RFC considered.</li><li><strong>Shared/core plugins commingle every user&#39;s rows in one table.</strong> RFC 0004 §1: <em>&quot;Platform/chrome plugins (Console/Account/Launcher) are always shared.&quot;</em> These tables are filtered by <code>user_id</code>, not physically separated per user. There is no existing per-user data boundary to redirect — one would have to be invented, table by table, plugin by plugin, before a connection-routing layer would even have something coherent to route.</li><li><strong>Cross-user queries are pervasive and assume one connection.</strong> Consent grants (<code>listAllConsentGrants</code>), plugin connection metadata for the Console operator surface (<code>listAllPluginConnectionRefs</code>), notifications (writes targeting a different <code>recipient_user_id</code> than the actor), and the activity log (<code>actor_id</code> vs. <code>subject_user_id</code>, admin-aggregated) all run one SQL query against one connection today. RFC 0051 (cross-plugin references) already avoids direct joins across plugin stores by using opaque, SDK-mediated references specifically because RFC 0004 made joins across isolated stores impossible — the same mitigation would be needed again here, but now for a user&#39;s data against every other query surface, not just against other plugins.</li><li><strong>Migrations are connection-agnostic in principle but single-target in practice.</strong> <code>packages/db/src/migrate.ts</code> — <code>runMigrations(pdb)</code> accepts any <code>PlatformDb</code> handle, so nothing prevents pointing the same migration files at an arbitrary connection string. But <code>_migrationResult</code> and the downgrade-guard state in <code>platform-db.ts</code> are single process-global slots used by <code>/api/admin/health</code> to report <strong>the</strong> platform&#39;s migration state — there is no per-connection migration-state tracking, and moving to N externally-hosted user databases turns &quot;keep schema in lockstep on deploy&quot; from a one-time operator action into an ongoing per-user reconciliation problem with no existing tooling.</li><li><strong>Auth is the one clean precedent, and it&#39;s already separate.</strong><code>apps/auth/src/db.ts</code> maintains its own connection, fully independent of <code>packages/db</code> (<em>&quot;Mirrors packages/db; not imported, as the auth server intentionally does not depend on packages/db&quot;</em>). This is the right seam if this were ever pursued — auth/identity centralized, only plugin data moves — but &quot;plugin data&quot; is not a clean, already-separated unit per user today the way &quot;auth&quot; is separated from &quot;platform+plugin data.&quot;</li><li><strong>Federation is an explicit non-goal, not an oversight.</strong> CLAUDE.md / SRS §1.4 non-goals lists multi-tenancy and calls out per-plugin isolated database as the only implemented item on that list. CLAUDE.md: <em>&quot;the <code>fs</code> denotes federated systems — reflecting the project&#39;s long-term federated direction (federation itself is a post-v1 concern; see SRS §1.4 non-goals).&quot;</em> Per-user BYO-DB is a form of federation (data residency decided per identity, not per instance), and the project has already decided, at the naming-and-scoping level, that federation is out of scope for now.</li></ul><h2 id="proposed-design-sketch-for-completeness-—-not-adopted" tabindex="-1">Proposed design (sketch, for completeness — not adopted) <a class="header-anchor" href="#proposed-design-sketch-for-completeness-—-not-adopted" aria-label="Permalink to &quot;Proposed design (sketch, for completeness — not adopted)&quot;">​</a></h2><p>Had this gone forward, the minimum viable shape would have required:</p><ol><li>A request-scoped (not process-singleton) DB resolver used consistently everywhere <code>getPlatformDb()</code>/<code>getPluginDb()</code> are called today, keyed by authenticated user ID, falling back to the shared instance DB by default.</li><li>A decision on shared/core plugins: either (a) BYO-DB users are excluded from Console/Account/Launcher&#39;s shared-table model and get a parallel per-user schema there too, or (b) BYO-DB only ever applies to plugin-scoped data for plugins that opt in, leaving core platform data (profile, sessions, consent, notifications) in the shared DB regardless — option (b) is far cheaper but means &quot;bring your own database&quot; would not actually mean what a user configuring it would expect.</li><li>A federation strategy for every cross-user aggregate query (admin dashboards, notifications, activity log, cross-plugin references) that currently assumes one connection — at minimum, explicitly excluding BYO-DB users&#39; rows from those aggregates, which is a product-visible behavior change, not just an engineering detail.</li><li>A per-user migration runner (extending the <code>runPluginMigrations</code> pattern) invoked at BYO-DB setup time and re-invoked on every platform upgrade for every configured remote DB, plus per-connection migration-state tracking surfaced somewhere the user or operator can see &quot;your remote DB is behind.&quot;</li><li>Secret handling for user-supplied connection strings/credentials (storage, rotation, connection validation, and explicit UX for unreachable/misconfigured remote databases — including what happens to login and shared-plugin usage when a user&#39;s own DB is down, since some subset of their data becomes unavailable mid-session).</li><li>Explicit scope: Postgres-only was stated in the request, but the platform default can be SQLite (RFC 0036); a BYO-DB Postgres target next to a SQLite platform default reintroduces the exact dialect-divergence risk RFC 0004&#39;s alternatives section flagged and deferred.</li></ol><p>None of this is a small addition to the Account section — it is a rework of the data-access layer&#39;s core assumption (one connection resolves the whole request) plus a federation strategy for every feature that currently reads across users.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="build-it-scoped-to-a-single-opt-in-plugin-not-platform-wide" tabindex="-1">Build it scoped to a single opt-in plugin, not platform-wide <a class="header-anchor" href="#build-it-scoped-to-a-single-opt-in-plugin-not-platform-wide" aria-label="Permalink to &quot;Build it scoped to a single opt-in plugin, not platform-wide&quot;">​</a></h3><p>Cheaper: a plugin manifest could declare <code>database: &#39;byo-external&#39;</code> and only that plugin&#39;s data moves, leaving Console/Account/Launcher and every other plugin in the shared DB. This still requires the request-scoped connection resolver (item 1 above) and a federation exclusion for that plugin&#39;s rows from any cross-user query, but avoids rearchitecting shared/core plugins. This is the shape RFC 0036&#39;s open question already gestures at as a possible follow-on. If demand becomes concrete, <strong>this</strong> is the narrower proposal worth re-evaluating — not the platform-wide version in this RFC.</p><h3 id="do-nothing-point-users-at-self-hosting-the-whole-instance-instead" tabindex="-1">Do nothing; point users at self-hosting the whole instance instead <a class="header-anchor" href="#do-nothing-point-users-at-self-hosting-the-whole-instance-instead" aria-label="Permalink to &quot;Do nothing; point users at self-hosting the whole instance instead&quot;">​</a></h3><p>Sovereign is already self-hostable per-instance. A user who wants full data control today can run their own instance rather than opting one user&#39;s data out of a shared instance. This doesn&#39;t serve the &quot;shared instance, some users want their own DB&quot; use case, but it&#39;s the zero-engineering-cost answer to the underlying data-ownership motivation, and is the honest answer to give users who ask for this before any narrower version (above) is built.</p><h3 id="encrypt-at-rest-client-side-encryption-instead-of-data-relocation" tabindex="-1">Encrypt-at-rest / client-side encryption instead of data relocation <a class="header-anchor" href="#encrypt-at-rest-client-side-encryption-instead-of-data-relocation" aria-label="Permalink to &quot;Encrypt-at-rest / client-side encryption instead of data relocation&quot;">​</a></h3><p>RFC 0008 (Tier 4) and RFC 0060 (client-side encryption core) already give users a way to keep specific data unreadable by the operator without moving it to different physical infrastructure. For users whose actual concern is &quot;the operator shouldn&#39;t be able to read my data&quot; rather than &quot;my data must physically live on hardware I control,&quot; this is a materially smaller lift that&#39;s already shipped or in progress, and should be the first thing pointed to if this request resurfaces.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li>If concrete user demand for this materializes, should the narrower single-plugin-scoped version (Alternatives, above) be re-evaluated as its own RFC, or does the federation-adjacent risk (item 3, cross-user aggregates) make even that scope unattractive?</li><li>Is &quot;data physically relocated to user-controlled infrastructure&quot; actually the need, or does client-side encryption (RFC 0060) already satisfy the underlying motivation for most users who&#39;d ask for this? Worth surfacing directly with whoever requested it before any further design work.</li><li>Should this non-recommendation be reflected in the SRS §1.4 non-goals list (as an example of federation) so future contributors don&#39;t re-propose the platform-wide version without finding this record first?</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><p>Not adopted. No implementation, no epic task, no roadmap entry. This document exists as a decision record: the platform-wide, per-user BYO-database feature described in the Summary was evaluated and rejected for v1 on architectural grounds (sections above), distinct from a simple lack of time/priority. Revisit only under the conditions in Open Questions #1.</p><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft.</td></tr><tr><td>—</td><td>July 2026</td><td>Withdrawn; not recommended for v1. Evaluated as platform-wide per-user BYO-DB; a narrower single-plugin-scoped variant remains a possible future follow-on if concrete demand emerges.</td></tr></tbody></table>',27)])])}const g=t(r,[["render",s]]);export{p as __pageData,g as default};
