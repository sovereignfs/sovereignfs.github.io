import{_ as t,o as a,c as n,a2 as r}from"./chunks/framework.CHeM0PsO.js";const m=JSON.parse('{"title":"ADR-0012: Local-Inference Resource and DNS-Latency Budgets","description":"","frontmatter":{"sourceUrl":"https://github.com/sovereignfs/sovereign-os/blob/main/docs/adrs/0012-local-inference-resource-and-dns-latency-budgets.md"},"headers":[],"relativePath":"sovereign-os/adrs/0012-local-inference-resource-and-dns-latency-budgets.md","filePath":"sovereign-os/adrs/0012-local-inference-resource-and-dns-latency-budgets.md"}'),o={name:"sovereign-os/adrs/0012-local-inference-resource-and-dns-latency-budgets.md"};function i(s,e,l,d,c,h){return a(),n("div",null,[...e[0]||(e[0]=[r('<h1 id="adr-0012-local-inference-resource-and-dns-latency-budgets" tabindex="-1">ADR-0012: Local-Inference Resource and DNS-Latency Budgets <a class="header-anchor" href="#adr-0012-local-inference-resource-and-dns-latency-budgets" aria-label="Permalink to &quot;ADR-0012: Local-Inference Resource and DNS-Latency Budgets&quot;">​</a></h1><p><strong>Status:</strong> Accepted</p><p><strong>Date:</strong> 2026-08-09</p><p><strong>Decision owner:</strong> Project creator</p><p><strong>Related RFCs/research:</strong><a href="./../rfcs/0002-local-conversation-and-inference-runtime.html">RFC-0002</a> (Accepted), <a href="./0004-provider-neutral-assistant-and-web-search.html">ADR-0004</a> (Accepted, names this exact budget as required follow-up), <a href="./../research/local-ai-options.html">docs/research/local-ai-options.md</a> (Follow-up Decision #2), <a href="./../research/llamacpp-qwen2.5-3b-benchmark-report.html">llama.cpp 3B</a>, <a href="./../research/llamacpp-qwen2.5-7b-benchmark-report.html">llama.cpp 7B</a>, <a href="./../research/ollama-qwen2.5-3b-benchmark-report.html">Ollama 3B</a>, and <a href="./../research/v1-corpus-benchmark-report.html">v1 corpus</a> qualification reports.</p><p><strong>Supersedes:</strong> None</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Four real benchmark passes now exist against the qualification Raspberry Pi 5, but nothing has turned their numbers into an accepted policy a runner/model configuration must pass. Both <code>local-ai-options.md</code> (&quot;The runner passes only if it remains inside explicit resource and DNS- service budgets... convenience alone is not a selection criterion&quot;) and RFC-0002&#39;s Runner Evaluation section committed to this being a real gate, not an afterthought — but the actual thresholds have never been written down.</p><p>This is not a hypothetical concern. The v1-corpus benchmark pass confirmed real thermal throttling on this device via <code>vcgencmd get_throttled</code> (<code>0xe0000</code>: frequency capping, throttling, and soft temperature limit bits all set) — the first genuine confirmation in this project&#39;s history, not an estimate. This device also serves the household&#39;s DNS through Pi-hole; a policy here is about protecting that responsibility, not just inference performance.</p><h3 id="what-the-evidence-actually-shows" tabindex="-1">What the Evidence Actually Shows <a class="header-anchor" href="#what-the-evidence-actually-shows" aria-label="Permalink to &quot;What the Evidence Actually Shows&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Metric</th><th>Idle baseline</th><th>Under sustained inference (3B)</th><th>Under sustained inference (7B)</th></tr></thead><tbody><tr><td>Temperature</td><td>56–59°C (consistent across every pass)</td><td>Plateaus 82–85°C over 3–5 min continuous generation; real throttling confirmed</td><td>Peaked 84.8°C on a shorter (~100s) run; not tested on the longer v1 corpus (deliberately skipped, see below)</td></tr><tr><td>Memory used</td><td>~14–28% (varies by what&#39;s already resident)</td><td>~28% steady</td><td>~58% steady, roughly double 3B</td></tr><tr><td>DNS latency (<code>dig</code>, before/after only)</td><td>13.6–14.5ms</td><td>15.0–19.8ms after a run</td><td>15.4–19.8ms after a run</td></tr></tbody></table><p>Two honest gaps in this evidence, both inherited from the benchmark harness&#39;s current limitations (named in every qualification report to date):</p><ol><li><strong>DNS latency has only ever been sampled before and after a run, never during active generation.</strong> The actual concern — does Pi-hole stay responsive <em>while the household is asking a question</em> — has not been directly measured. The numbers above bound the before/after delta, not the worst case.</li><li><strong>Every benchmark run so far used back-to-back requests with zero idle time between them</strong> — a genuine stress test, not a realistic household usage pattern. Real conversational use almost certainly has far more idle time between questions than any pass has modeled. The throttling observed is real, but it was produced under a harsher pattern than actual use is likely to be.</li></ol><p>This ADR sets a policy anyway, grounded in what&#39;s actually known, rather than waiting for a perfect measurement that doesn&#39;t exist yet — but it names both gaps as concrete, tracked follow-up work, not swept under the policy.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="thermal" tabindex="-1">Thermal <a class="header-anchor" href="#thermal" aria-label="Permalink to &quot;Thermal&quot;">​</a></h3><p>Raspberry Pi 5&#39;s own thermal throttling is a <em>safe</em>, designed protective mechanism — it reduces clock speed, it does not risk hardware damage. &quot;Throttling occurred&quot; is therefore treated here as a <strong>product-quality signal, not a safety failure</strong>: a household would notice slower responses, not a broken device.</p><ul><li>A configuration that throttles under a <strong>realistic, intermittent</strong> household conversation pattern fails this budget. A configuration that only throttles under <strong>continuous, back-to-back generation sustained for multiple minutes</strong> (the pattern every benchmark pass to date has actually used) does not automatically fail it, because that pattern is not representative of real use — but it is flagged as a real risk that needs the Conversation Service to actively manage (see Consequences).</li><li><strong>Numeric budget:</strong> sustained temperature must not exceed 80°C under a realistic usage pattern (a household asking occasional questions with normal gaps between them, not a benchmark corpus run end to end). 80°C is chosen deliberately below the ~85°C point where throttling was actually observed, to leave real margin rather than a budget the device is already known to violate under stress-test conditions.</li><li>This 80°C figure has <strong>not yet been validated against a realistic intermittent-use pattern</strong> — only against continuous stress-test runs, which is a different question. Validating it is required follow-up (see Validation and Revisit Conditions), not assumed to already hold.</li></ul><h3 id="memory" tabindex="-1">Memory <a class="header-anchor" href="#memory" aria-label="Permalink to &quot;Memory&quot;">​</a></h3><ul><li><strong>Numeric budget:</strong> a selected model&#39;s steady-state memory footprint must not exceed <strong>40% of total system RAM</strong>, leaving real headroom for Pi-hole, Console, the OS, and the capability executor to run concurrently without contention.</li><li>Qwen2.5-3B (~28% observed) is within this budget with real margin. Qwen2.5-7B (~58% observed) is not — this is a concrete, evidence-based reason 7B does not currently meet this policy, independent of the accuracy/speed findings the benchmark reports already named.</li></ul><h3 id="dns-latency" tabindex="-1">DNS Latency <a class="header-anchor" href="#dns-latency" aria-label="Permalink to &quot;DNS Latency&quot;">​</a></h3><ul><li><strong>Numeric budget (provisional):</strong> DNS resolution latency, measured locally against Pi-hole, must not exceed <strong>50ms</strong> and must not regress more than <strong>3x</strong> over the device&#39;s own idle baseline, during any window where inference is active.</li><li>This budget is explicitly <strong>provisional</strong> given the before/after-only measurement gap named above. It is set with real margin above every before/after delta observed to date (worst case ~20ms absolute, ~1.4x the idle baseline) specifically <em>because</em> the true concurrent-load number is unmeasured and could be worse. Closing that measurement gap (see Validation and Revisit Conditions) may tighten or loosen this number once real data exists.</li></ul><h3 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;Scope&quot;">​</a></h3><p>This budget applies to the <strong>runner + model combination as a whole</strong>, not to either in isolation — a fast runner with a model too large to fit the memory budget still fails, and vice versa. It is evaluated during real hardware qualification (per this project&#39;s standing practice), not estimated.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Turns &quot;convenience alone is not a selection criterion&quot; from a stated intention into an actual, checkable gate future runner/model qualification passes (including the still-open llama.cpp-7B v1-corpus data point) can be measured against.</li><li>Gives the eventual Conversation Service a concrete design constraint: since realistic-use thermal behavior isn&#39;t validated yet, it should actively avoid the continuous-generation pattern that&#39;s known to throttle — e.g., no unbounded back-to-back tool-calling loops without a cooldown, bounded per-turn capability-invocation counts (already a RFC-0003/RFC-0004 requirement for other reasons), and surfacing a &quot;the assistant is still catching up&quot; state rather than silently queuing requests during a hot stretch.</li><li>Makes Qwen2.5-7B&#39;s disqualification concrete and evidence-based (memory budget), not just &quot;seemed worse&quot; from the earlier benchmark reports&#39; softer framing.</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li>The thermal and DNS budgets are both explicitly provisional pending measurements this project doesn&#39;t have yet — this ADR is honest about that rather than presenting false precision, but it does mean the numbers here may need real revision, not just fine-tuning, once a realistic-pattern thermal test and a during-generation DNS-latency measurement actually exist.</li><li>A 40%-of-RAM memory ceiling is a real constraint on future model selection — it rules out larger, potentially more capable models without additional hardware (more RAM) or architecture changes (e.g., swap, model offloading) neither of which this ADR proposes.</li></ul><h3 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h3><ul><li>If the realistic-use thermal validation (follow-up, below) shows even intermittent household use throttles this device, the entire local- inference approach for this hardware generation may need reconsidering — a scope well beyond this ADR, but a real possibility this budget is designed to surface early rather than discover after Milestone 4 ships.</li><li>Setting the DNS-latency budget from before/after data alone risks it being too lenient if concurrent-load latency turns out much worse than the delta observed — the explicit provisional status is meant to keep this risk visible, not hide it.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives Considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives Considered&quot;">​</a></h2><ul><li><strong>Wait for a during-generation DNS-latency measurement before setting any budget at all.</strong> Rejected: this project&#39;s own research doc already named &quot;define measurable budgets&quot; as a required decision, and four real qualification passes already exist with real data. A provisional, clearly-labeled budget that can be tightened later is more useful than no budget while a perfect measurement is pending.</li><li><strong>Set the thermal budget at the observed throttle point (~85°C) rather than below it.</strong> Rejected: that would define &quot;safe&quot; as &quot;exactly the point we already confirmed causes throttling,&quot; which isn&#39;t a real margin — it&#39;s rubber-stamping stress-test behavior as normal.</li><li><strong>Set the memory budget higher (e.g., 60%) to allow 7B.</strong> Rejected: the benchmark reports already found no measured accuracy benefit from 7B on any corpus tried, at roughly double the memory cost and a real (if reproducible-shape, not definitively climbing) thermal cost — there&#39;s no evidence-based reason to spend the headroom.</li></ul><h2 id="validation-and-revisit-conditions" tabindex="-1">Validation and Revisit Conditions <a class="header-anchor" href="#validation-and-revisit-conditions" aria-label="Permalink to &quot;Validation and Revisit Conditions&quot;">​</a></h2><p>This ADR should be revisited when either of the following becomes available, and the numbers above should be treated as provisional until then:</p><ol><li><strong>A realistic, intermittent-use thermal pass</strong> — a benchmark run (or real household use, once the Conversation Service exists) with actual gaps between requests, not back-to-back generation, measuring whether 80°C genuinely holds under real conditions rather than only being inferred from stress-test data.</li><li><strong>A during-generation DNS-latency measurement</strong> — done for llama.cpp-3B on the starter corpus: the 50ms budget held across every sample (136 total, max 41.25ms), and the 3x-baseline sub-condition held too (worst case ≈2.8x). See the <a href="./../research/dns-latency-during-generation-qualification-report.html">DNS-latency-during-generation report</a>. <strong>Not yet closed universally</strong> — Ollama, the larger v1 corpus, and Qwen2.5-7B (already known to run longer and hotter, a real reason its DNS-latency behavior could differ) have not been measured with this mechanism yet.</li></ol><p>Until then, this ADR&#39;s numbers are the accepted policy, but explicitly a starting point grounded in real evidence rather than a final, precisely validated figure.</p>',37)])])}const g=t(o,[["render",i]]);export{m as __pageData,g as default};
