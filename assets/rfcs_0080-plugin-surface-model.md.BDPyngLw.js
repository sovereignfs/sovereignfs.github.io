import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"RFC 0080 — Plugin surface model (sdk.device.* and x-sovereign-surface)","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0080-plugin-surface-model.md","filePath":"rfcs/0080-plugin-surface-model.md"}'),n={name:"rfcs/0080-plugin-surface-model.md"};function o(r,e,d,l,c,h){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="rfc-0080-—-plugin-surface-model-sdk-device-and-x-sovereign-surface" tabindex="-1">RFC 0080 — Plugin surface model (<code>sdk.device.*</code> and <code>x-sovereign-surface</code>) <a class="header-anchor" href="#rfc-0080-—-plugin-surface-model-sdk-device-and-x-sovereign-surface" aria-label="Permalink to &quot;RFC 0080 — Plugin surface model (\`sdk.device.*\` and \`x-sovereign-surface\`)&quot;">​</a></h1><p><strong>Status:</strong> Draft<br><strong>Date:</strong> July 2026<br><strong>Author:</strong> kasunben<br><strong>Scope:</strong> <code>runtime/middleware.ts</code> (new <code>x-sovereign-surface</code> injected header), <code>packages/sdk</code> (new <code>device.ts</code> plus a browser-only <code>@sovereignfs/sdk/device-client</code> subpath — the first implementation of the long-promised <code>sdk.device.*</code>), <code>packages/manifest</code> (new optional <code>surfaces</code> field), <code>plugins/launcher</code> (availability filtering), <code>docs/plugin-development.md</code>, <code>docs/architecture-rules.md</code>. Implements the SDK abstraction tier promised by RFC 0058 (native mobile shell) and RFC 0038 (desktop shell); prerequisite for RFC 0082 (focused plugin app shell). Builds on research <a href="./../research/0006-standalone-plugin-apps.html">0006</a>.<br><strong>Incorporated into plan:</strong> Yes — epic tasks 3.32–3.33.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Give the platform one way to answer &quot;<strong>what surface am I running on?</strong>&quot; and expose it to plugins as <code>sdk.device.*</code> — the API RFC 0058 and RFC 0038 both promised and neither has shipped.</p><p>The signal is <strong>server-injected</strong>: a native shell identifies itself in its User-Agent, <code>runtime/middleware.ts</code> normalizes that into an <code>x-sovereign-surface</code> request header, and plugins read it server-side exactly as they already read <code>x-sovereign-user-*</code>. A second, client-only tier reports what the server cannot know — whether an ordinary browser is running as an installed PWA, and which device capabilities are actually present.</p><p>A plugin may additionally declare <code>surfaces: [...]</code> in its manifest to say where it is available at all, so Launcher can hide a mobile-only app on desktop.</p><p><strong>This RFC deliberately does not build a feature-flag system.</strong> Research 0005 separated four concerns that hide under that phrase; this RFC implements the two that are ambient facts (surface, capability), leaves operator flags to instance settings if a concrete need ever appears, and rejects rollout flags outright — Sovereign is single-tenant self-hosted, so there is no fleet to roll out across.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Features and UI elements increasingly belong to one surface and not another: a native photo picker only exists in the Capacitor shell, a system-tray affordance only in Tauri, an &quot;Add to Home Screen&quot; prompt only in a browser that has not already installed the app, a chrome-less layout only inside a native shell.</p><p>Today a plugin has no way to ask. <code>packages/sdk/src/device.ts</code> does not exist. The only related hook, <code>useIsMobile</code> in <code>packages/ui</code>, answers a <strong>viewport</strong> question, not a surface one — a desktop browser narrowed to 400px reports the same as a phone, and a tablet in the Capacitor shell reports the same as a tablet in Safari.</p><p>Worse, the obvious workarounds are both blocked by existing hard rules:</p><ul><li><code>NEXT_PUBLIC_SOVEREIGN_SURFACE</code> cannot work — Next inlines <code>NEXT_PUBLIC_*</code> at build time and Docker images build without <code>.env</code>, so the value freezes to its fallback for every deployment.</li><li>Reading <code>window.Capacitor</code> or <code>matchMedia(&#39;(display-mode: standalone)&#39;)</code> in a <code>&#39;use client&#39;</code> component&#39;s <code>useState</code> initializer or render is forbidden (hydration mismatch). Doing it correctly means <code>useEffect</code> plus a safe default — which means a <strong>visible flash of the wrong UI on every load</strong>, acceptable for progressive enhancement and not for gating whole elements.</li></ul><p>So the mechanism has to be server-side, and it has to be one mechanism rather than each plugin inventing its own User-Agent sniffing.</p><h2 id="current-state-what-this-builds-on" tabindex="-1">Current state (what this builds on) <a class="header-anchor" href="#current-state-what-this-builds-on" aria-label="Permalink to &quot;Current state (what this builds on)&quot;">​</a></h2><ul><li><strong><code>sdk.device.*</code> is promised but absent.</strong> RFC 0058&#39;s device-API strategy table and RFC 0038&#39;s mirror of it both specify a three-tier model (Web APIs → native plugins → <code>sdk.device.*</code> abstraction) and both state that plugin developers call <code>sdk.device.*</code> only. <code>packages/sdk/src/device.ts</code> does not exist. Epic tasks 17.7 (&quot;SDK <code>desktop</code> environment for <code>sdk.device.*</code>&quot;) and 20.3 (&quot;Mobile SDK native environment and bridge adapter&quot;) are both 📋 — they extend a base that was never built. <strong>This RFC builds that base</strong>; 17.7 and 20.3 become extensions of it rather than parallel inventions.</li><li><strong>The injected-header pattern is established and is the model to copy.</strong><code>runtime/middleware.ts:304</code> verifies the session and injects <code>x-sovereign-user-id</code>, <code>-email</code>, <code>-role</code>, <code>-capabilities</code>, <code>-session-expires-at</code>, <code>-name</code>, <code>-image</code>; <code>packages/sdk/src/auth.ts:13-34</code> reads them back via <code>next/headers</code>. <code>x-sovereign-plugin-id</code> is injected the same way and consumed by <code>packages/sdk/src/env.ts</code> and <code>packages/sdk/src/data.ts:76</code>. Adding <code>x-sovereign-surface</code> is a fifth consumer of a proven pattern, not a new mechanism.</li><li><strong>Shell chrome control already exists and is not being replaced.</strong><code>shell: &#39;default&#39; | &#39;minimal&#39; | &#39;overlay&#39;</code> plus <code>shellConfig.mobileHeader</code>/ <code>mobileFooter</code> (<code>packages/manifest/src/schema.ts:160-172</code>, RFC 0075) are build-time composition and per-plugin layout choices. This RFC is orthogonal: <code>shell</code> says <em>how this plugin is framed</em>, <code>surfaces</code> says <em>where it exists</em>, and <code>sdk.device.*</code> says <em>what the current environment supports</em>.</li><li><strong><code>useIsMobile</code></strong> (<code>packages/ui</code>, consumed by <code>DatePicker</code>, <code>ContextMenu</code>, <code>Combobox</code>, <code>Menu</code>, <code>HoverCard</code>) stays exactly as it is — a viewport hook. This RFC does not change it and does not route it through the surface model.</li><li><strong>The capability system is deliberately not reused.</strong><code>runtime/src/capabilities.ts</code> defines a pure, Edge-resolvable role→capability derivation, extended by per-user grants (RFC 0070) with an explicit <code>GRANTABLE_CAPABILITIES</code> allowlist. Research 0005 rejected folding surfaces into it: capabilities answer <em>authorization</em>, surfaces answer <em>availability</em>, and merging them yields checks whose failure is ambiguous between &quot;no permission&quot; and &quot;no camera&quot;.</li><li><strong><code>sovereign-desktop</code> already ships</strong> (epic 17.1 ✅) as a Tauri shell loading a remote instance, so one of the two native surfaces this RFC describes exists in the field today and can be the first real producer of the signal.</li><li><strong>The service worker&#39;s page caches are <code>NetworkFirst</code> by hard rule</strong>, because pages are per-user SSR and a stale-served document risks replaying one user&#39;s shell to another (<code>runtime/next.config.ts</code>, <code>CLAUDE.md</code>). Surface-varying SSR interacts with this directly — see &quot;Service-worker interaction&quot; below.</li></ul><h2 id="proposed-design" tabindex="-1">Proposed design <a class="header-anchor" href="#proposed-design" aria-label="Permalink to &quot;Proposed design&quot;">​</a></h2><h3 id="_1-surface-taxonomy-—-two-tiers-honestly-separated" tabindex="-1">1. Surface taxonomy — two tiers, honestly separated <a class="header-anchor" href="#_1-surface-taxonomy-—-two-tiers-honestly-separated" aria-label="Permalink to &quot;1. Surface taxonomy — two tiers, honestly separated&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Tier</th><th>Value</th><th>Knowable server-side?</th><th>Source</th></tr></thead><tbody><tr><td><strong>Surface</strong></td><td><code>browser</code> | <code>mobile</code> | <code>desktop</code></td><td><strong>Yes</strong></td><td>shell User-Agent → middleware</td></tr><tr><td><strong>Installed</strong></td><td><code>true</code> | <code>false</code></td><td>No</td><td><code>display-mode: standalone</code> (client only)</td></tr><tr><td><strong>Capability</strong></td><td>e.g. <code>camera</code>, <code>biometrics</code>, <code>haptics</code></td><td>No</td><td>native bridge or Web API probe (client)</td></tr></tbody></table><p><code>mobile</code> means the Capacitor shell (RFC 0058); <code>desktop</code> means the Tauri shell (RFC 0038); <code>browser</code> means anything else, including an installed PWA.</p><p><strong>An installed PWA is not its own surface.</strong> It is the same engine hitting the same origin and it cannot set a header, so <code>display-mode: standalone</code> is irreducibly client-side. Modelling it as a fourth server-side surface value would be a lie the middleware cannot back up. It is therefore a <em>refinement</em> of <code>browser</code>, reported only on the client tier.</p><h3 id="_2-shell-identification-—-a-hint-never-a-security-boundary" tabindex="-1">2. Shell identification — a hint, never a security boundary <a class="header-anchor" href="#_2-shell-identification-—-a-hint-never-a-security-boundary" aria-label="Permalink to &quot;2. Shell identification — a hint, never a security boundary&quot;">​</a></h3><p>Native shells append a token to their WebView User-Agent:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Sovereign-Shell/mobile-ios 1.0.0</span></span>
<span class="line"><span>Sovereign-Shell/mobile-android 1.0.0</span></span>
<span class="line"><span>Sovereign-Shell/desktop-macos 1.0.0</span></span></code></pre></div><p><code>runtime/middleware.ts</code> parses it and injects <code>x-sovereign-surface: mobile | desktop | browser</code>, plus an optional <code>x-sovereign-shell-version</code> for compatibility checks. Unrecognized or absent → <code>browser</code>. The middleware <strong>strips any inbound <code>x-sovereign-surface</code> header</strong> before injecting its own, exactly as it must for the <code>x-sovereign-user-*</code> family, so a client cannot forge the value directly.</p><blockquote><p><strong>Hard rule, to be added to <code>docs/architecture-rules.md</code>:</strong> the surface signal is a <strong>presentation hint only</strong>. It derives from a client-controlled User-Agent and is trivially spoofable. It must never be an input to authorization, entitlement, paywall, or data-access decisions. Anything that must not be reachable is gated by session, capability, or plugin permission — never by surface. RFC 0082&#39;s route lock is a UX and product-scoping mechanism, not a security boundary, and says so.</p></blockquote><h3 id="_3-sdk-surface-—-sdk-device" tabindex="-1">3. SDK surface — <code>sdk.device.*</code> <a class="header-anchor" href="#_3-sdk-surface-—-sdk-device" aria-label="Permalink to &quot;3. SDK surface — \`sdk.device.*\`&quot;">​</a></h3><p><strong>Server tier</strong> (<code>packages/sdk/src/device.ts</code>, in the main barrel alongside <code>auth</code>/<code>env</code>/<code>data</code>, since it reads <code>next/headers</code>):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Surface</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;browser&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;mobile&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;desktop&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> device</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** The current request&#39;s surface, from the runtime-injected header. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getSurface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Surface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** The native shell&#39;s version, or null outside a native shell. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getShellVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** True when running inside any native shell (mobile or desktop). */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isNativeShell</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>getSurface()</code> returns <code>&#39;browser&#39;</code> when the header is absent — outside a plugin route context, in a unit test, anywhere. It never throws and has no host dependency, matching <code>env.ts</code>&#39;s &quot;return the safe default&quot; discipline rather than <code>data.ts</code>&#39;s <code>requireHost()</code> RPC style.</p><p><strong>Client tier</strong> (<code>packages/sdk/src/device-client.ts</code>, exposed on the dedicated subpath <code>@sovereignfs/sdk/device-client</code>):</p><p>Following the precedent set by <code>@sovereignfs/sdk/offline</code> and the <code>e2ee-*</code> modules — the main barrel transitively reaches server-only <code>next/headers</code>, and Next&#39;s client/server boundary check flags the whole reachable module graph, so a <code>&#39;use client&#39;</code> component importing from the barrel fails to build.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeviceEnvironment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  surface</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Surface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Running as an installed PWA (display-mode: standalone). */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  installed</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** Read the environment on the client. Safe to call only after mount. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> readEnvironment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeviceEnvironment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** Environment as state, initialised to a safe default and filled on mount. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useDeviceEnvironment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DeviceEnvironment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>useDeviceEnvironment()</code> returns <code>null</code> on first render and the real value after mount — deliberately explicit, so a caller must handle &quot;not known yet&quot; rather than being handed a plausible-looking default that causes a flash. This is the hook&#39;s whole reason to exist: it makes the hard rule about browser globals in render impossible to violate by accident.</p><p>Capability probes and invocation (<code>camera</code>, <code>biometrics</code>, <code>haptics</code>, …) are <strong>not</strong> in this RFC. They are designed in <a href="./0083-device-bridge-capability-contract.html">RFC 0083</a>, which adds a third architectural piece on top of this one: a browser-only bridge with Capacitor/Tauri/Web transports, capability negotiation at handshake, and typed results. This RFC answers <em>where am I</em>; RFC 0083 answers <em>what can I do here, and how do I ask</em>. Epic tasks 17.7 and 20.3 are subsumed/rescoped by it — see RFC 0083 §8.</p><h3 id="_4-manifest-—-optional-surfaces-declaration" tabindex="-1">4. Manifest — optional <code>surfaces</code> declaration <a class="header-anchor" href="#_4-manifest-—-optional-surfaces-declaration" aria-label="Permalink to &quot;4. Manifest — optional \`surfaces\` declaration&quot;">​</a></h3><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;surfaces&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mobile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;desktop&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Added to <code>packages/manifest/src/schema.ts</code> as an optional array of the same three values, unique, non-empty when present. <strong>Absent means available everywhere</strong> — today&#39;s behavior for every existing plugin, so this is a purely additive, non-breaking change and <code>@sovereignfs/manifest</code> takes a <strong>minor</strong> bump.</p><p><code>surfaces</code> is an <strong>availability</strong> declaration, consumed by the platform:</p><ul><li>Launcher filters its grid to plugins available on the current surface.</li><li>The plugin&#39;s sidebar and mobile-drawer entries are filtered the same way.</li><li>Navigating directly to an unavailable plugin&#39;s route renders a clear &quot;not available on this surface&quot; state rather than a 404 — the plugin <em>is</em> installed, it just does not belong here, and a 404 would misdescribe that.</li></ul><p>Note the deliberate asymmetry with the route lock in RFC 0082: <code>surfaces</code> filters <em>presentation</em> and can be bypassed by anyone who edits their User-Agent, which is fine because nothing behind it is a secret.</p><h3 id="_5-service-worker-interaction-—-the-part-most-likely-to-bite" tabindex="-1">5. Service-worker interaction — the part most likely to bite <a class="header-anchor" href="#_5-service-worker-interaction-—-the-part-most-likely-to-bite" aria-label="Permalink to &quot;5. Service-worker interaction — the part most likely to bite&quot;">​</a></h3><p>If a document renders differently per surface and the service worker caches it, the wrong variant can be replayed to the wrong surface. This is the same class of bug as the existing hard rule keeping <code>pages</code>/<code>pages-rsc</code> on <code>NetworkFirst</code> because pages are per-user SSR.</p><p>Three acceptable resolutions, in order of preference:</p><ol><li><strong>Keep surface-varying differences client-side</strong> wherever the difference is cosmetic or additive. Nothing to cache-key; no risk.</li><li><strong>Send <code>Vary: User-Agent</code></strong> on documents whose SSR genuinely branches on surface. Correct but coarse — it fragments the cache badly, since UA strings are near-unique.</li><li><strong>Key the SW cache on surface</strong> via a Workbox <code>cacheKeyWillBeUsed</code> plugin that appends the normalized surface to the cache key. Precise, and the only option that keeps caching effective for genuinely surface-varying documents.</li></ol><p>The implementation task must pick one <strong>per cache</strong>, not globally, and record the choice in <code>docs/architecture-rules.md</code>. Defaulting to (1) and escalating only where needed is the recommended posture.</p><h3 id="_6-what-this-rfc-explicitly-does-not-add" tabindex="-1">6. What this RFC explicitly does not add <a class="header-anchor" href="#_6-what-this-rfc-explicitly-does-not-add" aria-label="Permalink to &quot;6. What this RFC explicitly does not add&quot;">​</a></h3><ul><li><strong>No operator feature flags.</strong> <code>PlatformConfig</code> (<code>packages/sdk/src/types.ts:88-107</code>) is the right home if a concrete need appears; none exists today, and building a Console settings surface nobody asked for is the wrong default.</li><li><strong>No rollout or percentage flags.</strong> Single-tenant self-hosted; no fleet.</li><li><strong>No capability-system extension.</strong> See &quot;Current state&quot;.</li><li><strong>No change to <code>useIsMobile</code></strong>, <code>shell</code>, or <code>shellConfig</code>.</li></ul><h3 id="docker-config-impact" tabindex="-1">Docker / config impact <a class="header-anchor" href="#docker-config-impact" aria-label="Permalink to &quot;Docker / config impact&quot;">​</a></h3><p>None. No new env var, port, on-disk path, or native dependency. The signal travels on the request.</p><h2 id="ui-flows" tabindex="-1">UI flows <a class="header-anchor" href="#ui-flows" aria-label="Permalink to &quot;UI flows&quot;">​</a></h2><p><strong>Plugin hidden on an unavailable surface:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>desktop shell → Launcher renders → surfaces filter applied</span></span>
<span class="line"><span>              → mobile-only plugin&#39;s tile absent, sidebar entry absent</span></span></code></pre></div><p><strong>Direct navigation to an unavailable plugin:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>desktop shell → GET /scanner → plugin installed but surfaces: [&quot;mobile&quot;]</span></span>
<span class="line"><span>              → &quot;Not available on this surface&quot; state (not 404)</span></span></code></pre></div><p><strong>Surface-gated element inside a plugin, server-rendered:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>request → middleware injects x-sovereign-surface: mobile</span></span>
<span class="line"><span>        → plugin server component: await sdk.device.getSurface() === &#39;mobile&#39;</span></span>
<span class="line"><span>        → renders the native photo-picker button, no flash</span></span></code></pre></div><p><strong>Install prompt, client-only tier:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>browser tab → useDeviceEnvironment() → null on first render (renders nothing)</span></span>
<span class="line"><span>            → after mount: { surface: &#39;browser&#39;, installed: false }</span></span>
<span class="line"><span>            → renders &quot;Add to Home Screen&quot; hint</span></span>
<span class="line"><span>installed PWA → after mount: { installed: true } → hint stays hidden</span></span></code></pre></div><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Client-only detection</strong> (<code>window.Capacitor</code>, <code>matchMedia</code>). Rejected as the primary mechanism: the hard rule against browser globals in render forces <code>useEffect</code>, which means a visible flash whenever whole elements are gated. Retained for the one tier that genuinely cannot be server-side.</li><li><strong><code>NEXT_PUBLIC_SOVEREIGN_SURFACE</code>.</strong> Rejected — build-time inlining freezes the value; Docker images build without <code>.env</code>. Existing hard rule.</li><li><strong>A cookie carrying <code>display-mode</code> so the server can see PWA-ness.</strong> Rejected: it is wrong on first load, and it makes SSR vary on a value the SW may have cached under a different one — precisely the bug §5 exists to avoid.</li><li><strong>Extending <code>runtime/src/capabilities.ts</code> with surface capabilities.</strong> Rejected — authorization/availability category error; see &quot;Current state&quot;.</li><li><strong>A general feature-flag service</strong> with a DB table and targeting rules. Rejected as over-engineering for single-tenant self-hosted.</li><li><strong>Reusing <code>shell</code>/<code>shellConfig</code> for surface gating.</strong> Rejected — <code>shell</code> is a build-time composition target and a layout choice, not a runtime environment query. Overloading it would make one field answer two unrelated questions.</li><li><strong>A fourth surface value <code>pwa</code>.</strong> Rejected — not server-knowable, so the middleware could never populate it honestly. Modelled as the <code>installed</code> refinement instead.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li>Exact User-Agent token grammar, and whether <code>sovereign-desktop</code> (already shipped) can adopt it in a patch release or needs a coordinated version.</li><li>Whether <code>getShellVersion()</code> should feed a compatibility gate (shell too old for this instance), reusing RFC 0024&#39;s machinery, or stay informational. Leaning informational until a real incompatibility exists.</li><li>Whether <code>surfaces</code> should also accept <code>installed</code> as a pseudo-value for &quot;installed PWA only&quot;. Leaning no — it would make a server-filtered field depend on a client-only fact.</li><li>Whether the per-cache surface-keying decision (§5) belongs in this RFC or in RFC 0082, where the first genuinely surface-varying document appears.</li></ol><h2 id="adoption-path" tabindex="-1">Adoption path <a class="header-anchor" href="#adoption-path" aria-label="Permalink to &quot;Adoption path&quot;">​</a></h2><ol><li><strong>Epic task 3.32</strong> — middleware normalization + injected header, inbound header stripping, <code>sdk.device.*</code> server tier, <code>device-client</code> subpath with <code>useDeviceEnvironment</code>, the <code>architecture-rules.md</code> hard rule about surface never gating authorization, and <code>docs/plugin-development.md</code> coverage. <code>@sovereignfs/sdk</code> minor bump.</li><li><strong>Epic task 3.33</strong> — manifest <code>surfaces</code> field, Launcher and sidebar/drawer filtering, the &quot;not available on this surface&quot; state. <code>@sovereignfs/manifest</code> minor bump.</li><li><code>sovereign-desktop</code> adopts the User-Agent token so <code>desktop</code> is a real producer.</li><li>Epic tasks 17.7 and 20.3 extend <code>device-client</code> with capability probes and native bridges, rather than inventing a parallel environment model.</li></ol><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>0.1</td><td>July 2026</td><td>Initial draft</td></tr></tbody></table>`,67)])])}const g=s(n,[["render",o]]);export{u as __pageData,g as default};
